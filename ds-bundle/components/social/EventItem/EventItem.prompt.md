EventItem from portal-frontend. Use via `window.PortalUI.EventItem` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<DSProvider>` (full provider chain in README.md — components read theme/i18n from that context).

An upcoming-event row — React port of the Olympus `.event-item`
(Favorit Page - Events 2615-2670): an accent date badge, the title, host,
place (small-pin-icon), stacked attendee avatars, and a Join button.
