// Re-export of portal-frontend@0.1.0 PostControlButtons. Implementation is in the root _ds_bundle.js (window.PortalUI).
Object.assign(window, { PostControlButtons: window.PortalUI.PostControlButtons });
