import * as React from 'react';

/**
 * Composer — from portal-frontend@0.1.0.
 */
export interface ComposerProps {
  [key: string]: unknown;
}

export declare const Composer: React.ComponentType<ComposerProps>;
