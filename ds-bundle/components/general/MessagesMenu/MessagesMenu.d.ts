import * as React from 'react';

/**
 * MessagesMenu — from portal-frontend@0.1.0.
 */
export interface MessagesMenuProps {
open: boolean; onToggle: () => void;
}

export declare const MessagesMenu: React.ComponentType<MessagesMenuProps>;
