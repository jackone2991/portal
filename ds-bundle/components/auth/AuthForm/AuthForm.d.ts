import * as React from 'react';

/**
 * AuthForm — from portal-frontend@0.1.0.
 */
export interface AuthFormProps {
defaultTab?: "login" | "register";
}

export declare const AuthForm: React.ComponentType<AuthFormProps>;
