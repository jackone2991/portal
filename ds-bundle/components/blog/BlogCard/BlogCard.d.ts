import * as React from 'react';

/**
 * BlogCard — from portal-frontend@0.1.0.
 */
export interface BlogCardProps {
  [key: string]: unknown;
}

export declare const BlogCard: React.ComponentType<BlogCardProps>;
