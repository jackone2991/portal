/* @ds-bundle: {"namespace":"PortalUI","components":[{"name":"ActivityFeed","sourcePath":"components/widget/ActivityFeed/ActivityFeed.jsx"},{"name":"AddBook","sourcePath":"components/popup/AddBook/AddBook.jsx"},{"name":"AudioPlayer","sourcePath":"components/music/AudioPlayer/AudioPlayer.jsx"},{"name":"AuthForm","sourcePath":"components/auth/AuthForm/AuthForm.jsx"},{"name":"AuthLanding","sourcePath":"components/auth/AuthLanding/AuthLanding.jsx"},{"name":"Avatar","sourcePath":"components/general/Avatar/Avatar.jsx"},{"name":"BadgeCard","sourcePath":"components/social/BadgeCard/BadgeCard.jsx"},{"name":"BirthdayCard","sourcePath":"components/widget/BirthdayCard/BirthdayCard.jsx"},{"name":"BlogCard","sourcePath":"components/blog/BlogCard/BlogCard.jsx"},{"name":"CalendarWidget","sourcePath":"components/widget/CalendarWidget/CalendarWidget.jsx"},{"name":"ChatResponsive","sourcePath":"components/popup/ChatResponsive/ChatResponsive.jsx"},{"name":"ChoseFromMyPhoto","sourcePath":"components/popup/ChoseFromMyPhoto/ChoseFromMyPhoto.jsx"},{"name":"ComicIndexView","sourcePath":"components/comic/ComicIndexView/ComicIndexView.jsx"},{"name":"CommentForm","sourcePath":"components/comment/CommentForm/CommentForm.jsx"},{"name":"CommentItem","sourcePath":"components/comment/CommentItem/CommentItem.jsx"},{"name":"CommentThread","sourcePath":"components/comment/CommentThread/CommentThread.jsx"},{"name":"Composer","sourcePath":"components/general/Composer/Composer.jsx"},{"name":"ControlBlockButtons","sourcePath":"components/profile/ControlBlockButtons/ControlBlockButtons.jsx"},{"name":"EventItem","sourcePath":"components/social/EventItem/EventItem.jsx"},{"name":"FormField","sourcePath":"components/form/FormField/FormField.jsx"},{"name":"FriendCard","sourcePath":"components/social/FriendCard/FriendCard.jsx"},{"name":"FriendRequestItem","sourcePath":"components/social/FriendRequestItem/FriendRequestItem.jsx"},{"name":"FriendRequestsMenu","sourcePath":"components/general/FriendRequestsMenu/FriendRequestsMenu.jsx"},{"name":"FriendSuggestions","sourcePath":"components/widget/FriendSuggestions/FriendSuggestions.jsx"},{"name":"GoToTop","sourcePath":"components/partials/GoToTop/GoToTop.jsx"},{"name":"HelloPreloader","sourcePath":"components/partials/HelloPreloader/HelloPreloader.jsx"},{"name":"HomeView","sourcePath":"components/home/HomeView/HomeView.jsx"},{"name":"Icon","sourcePath":"components/general/Icon/Icon.jsx"},{"name":"LoginView","sourcePath":"components/auth/LoginView/LoginView.jsx"},{"name":"MasterBase","sourcePath":"components/master/MasterBase/MasterBase.jsx"},{"name":"MasterPublic","sourcePath":"components/master/MasterPublic/MasterPublic.jsx"},{"name":"MessagesMenu","sourcePath":"components/general/MessagesMenu/MessagesMenu.jsx"},{"name":"NotificationsMenu","sourcePath":"components/general/NotificationsMenu/NotificationsMenu.jsx"},{"name":"NovelDetailView","sourcePath":"components/novel/NovelDetailView/NovelDetailView.jsx"},{"name":"PagesWidget","sourcePath":"components/widget/PagesWidget/PagesWidget.jsx"},{"name":"PersonalInfoWidget","sourcePath":"components/widget/PersonalInfoWidget/PersonalInfoWidget.jsx"},{"name":"PlaylistWidget","sourcePath":"components/music/PlaylistWidget/PlaylistWidget.jsx"},{"name":"Post","sourcePath":"components/general/Post/Post.jsx"},{"name":"PostControlButtons","sourcePath":"components/post/PostControlButtons/PostControlButtons.jsx"},{"name":"ProfileHeader","sourcePath":"components/profile/ProfileHeader/ProfileHeader.jsx"},{"name":"ReactionBar","sourcePath":"components/post/ReactionBar/ReactionBar.jsx"},{"name":"RegisterView","sourcePath":"components/auth/RegisterView/RegisterView.jsx"},{"name":"SelectField","sourcePath":"components/form/SelectField/SelectField.jsx"},{"name":"SidebarCenter","sourcePath":"components/menu/SidebarCenter/SidebarCenter.jsx"},{"name":"SidebarLeft","sourcePath":"components/menu/SidebarLeft/SidebarLeft.jsx"},{"name":"SidebarRight","sourcePath":"components/menu/SidebarRight/SidebarRight.jsx"},{"name":"TagSelect","sourcePath":"components/form/TagSelect/TagSelect.jsx"},{"name":"ToggleRow","sourcePath":"components/form/ToggleRow/ToggleRow.jsx"},{"name":"TopMenu","sourcePath":"components/headers/TopMenu/TopMenu.jsx"},{"name":"TrackItem","sourcePath":"components/music/TrackItem/TrackItem.jsx"},{"name":"UpdateHeaderPhoto","sourcePath":"components/popup/UpdateHeaderPhoto/UpdateHeaderPhoto.jsx"},{"name":"UploadStudio","sourcePath":"components/upload/UploadStudio/UploadStudio.jsx"},{"name":"WeatherWidget","sourcePath":"components/widget/WeatherWidget/WeatherWidget.jsx"},{"name":"WidgetCard","sourcePath":"components/widget/WidgetCard/WidgetCard.jsx"}],"sourceHashes":{"components/widget/ActivityFeed/ActivityFeed.jsx":"7d780fe14604","components/widget/ActivityFeed/ActivityFeed.d.ts":"8b486d78f855","components/widget/ActivityFeed/ActivityFeed.prompt.md":"f73a9dd2a222","components/popup/AddBook/AddBook.jsx":"342bf3316fa6","components/popup/AddBook/AddBook.d.ts":"955a5a8afa4f","components/popup/AddBook/AddBook.prompt.md":"01b8a578dfcb","components/music/AudioPlayer/AudioPlayer.jsx":"5838ad0176db","components/music/AudioPlayer/AudioPlayer.d.ts":"f9937a613082","components/music/AudioPlayer/AudioPlayer.prompt.md":"21d4ecf9ce8b","components/auth/AuthForm/AuthForm.jsx":"1e61c6521418","components/auth/AuthForm/AuthForm.d.ts":"aa7c72c0e540","components/auth/AuthForm/AuthForm.prompt.md":"a1d2e05687e3","components/auth/AuthLanding/AuthLanding.jsx":"8fb2228bac01","components/auth/AuthLanding/AuthLanding.d.ts":"ebe7795bc4c4","components/auth/AuthLanding/AuthLanding.prompt.md":"304b549afe05","components/general/Avatar/Avatar.jsx":"de2a9e21a364","components/general/Avatar/Avatar.d.ts":"cef099484f1a","components/general/Avatar/Avatar.prompt.md":"28cd154c7a58","components/social/BadgeCard/BadgeCard.jsx":"efa4a4d130a2","components/social/BadgeCard/BadgeCard.d.ts":"1a048347711e","components/social/BadgeCard/BadgeCard.prompt.md":"0cae404ebaf0","components/widget/BirthdayCard/BirthdayCard.jsx":"61b2b764bca9","components/widget/BirthdayCard/BirthdayCard.d.ts":"169c1800a17f","components/widget/BirthdayCard/BirthdayCard.prompt.md":"11c05b8aad9c","components/blog/BlogCard/BlogCard.jsx":"74fe53b4fae4","components/blog/BlogCard/BlogCard.d.ts":"5a355bf69952","components/blog/BlogCard/BlogCard.prompt.md":"40d84fd02627","components/widget/CalendarWidget/CalendarWidget.jsx":"e9a8b24134c7","components/widget/CalendarWidget/CalendarWidget.d.ts":"71b8eb108e7c","components/widget/CalendarWidget/CalendarWidget.prompt.md":"952e08c5e8e5","components/popup/ChatResponsive/ChatResponsive.jsx":"731a0df6f69c","components/popup/ChatResponsive/ChatResponsive.d.ts":"af5c346f0de7","components/popup/ChatResponsive/ChatResponsive.prompt.md":"c94a075c52fa","components/popup/ChoseFromMyPhoto/ChoseFromMyPhoto.jsx":"6351148a1e60","components/popup/ChoseFromMyPhoto/ChoseFromMyPhoto.d.ts":"6b3b302ef68f","components/popup/ChoseFromMyPhoto/ChoseFromMyPhoto.prompt.md":"53243c634d4a","components/comic/ComicIndexView/ComicIndexView.jsx":"4f28ea91cd2e","components/comic/ComicIndexView/ComicIndexView.d.ts":"35a2fcfc8e80","components/comic/ComicIndexView/ComicIndexView.prompt.md":"b968d5495e25","components/comment/CommentForm/CommentForm.jsx":"1c8a99defa49","components/comment/CommentForm/CommentForm.d.ts":"75d3009d0f00","components/comment/CommentForm/CommentForm.prompt.md":"60cb5b0b1258","components/comment/CommentItem/CommentItem.jsx":"6fb44b469784","components/comment/CommentItem/CommentItem.d.ts":"991573d78046","components/comment/CommentItem/CommentItem.prompt.md":"dc69c22361c1","components/comment/CommentThread/CommentThread.jsx":"6b7db8e67335","components/comment/CommentThread/CommentThread.d.ts":"462dfc0bfdf0","components/comment/CommentThread/CommentThread.prompt.md":"6024a885e2ea","components/general/Composer/Composer.jsx":"db23f5d95c44","components/general/Composer/Composer.d.ts":"d013ccadccf6","components/general/Composer/Composer.prompt.md":"e175ac291c78","components/profile/ControlBlockButtons/ControlBlockButtons.jsx":"9f5ea6dd02bb","components/profile/ControlBlockButtons/ControlBlockButtons.d.ts":"ff2cbc2650da","components/profile/ControlBlockButtons/ControlBlockButtons.prompt.md":"e1533879d5cb","components/social/EventItem/EventItem.jsx":"10a68507a826","components/social/EventItem/EventItem.d.ts":"7b4345824b98","components/social/EventItem/EventItem.prompt.md":"cc266956cb6c","components/form/FormField/FormField.jsx":"63b6f5ed57e0","components/form/FormField/FormField.d.ts":"90014143311a","components/form/FormField/FormField.prompt.md":"f174a5a62df9","components/social/FriendCard/FriendCard.jsx":"e64fc2419908","components/social/FriendCard/FriendCard.d.ts":"482754771ea9","components/social/FriendCard/FriendCard.prompt.md":"159a089de580","components/social/FriendRequestItem/FriendRequestItem.jsx":"65eee2d97545","components/social/FriendRequestItem/FriendRequestItem.d.ts":"52fa23811754","components/social/FriendRequestItem/FriendRequestItem.prompt.md":"528b63cb335c","components/general/FriendRequestsMenu/FriendRequestsMenu.jsx":"a08be07b3629","components/general/FriendRequestsMenu/FriendRequestsMenu.d.ts":"7fbf638c31d2","components/general/FriendRequestsMenu/FriendRequestsMenu.prompt.md":"c68f7472fcaf","components/widget/FriendSuggestions/FriendSuggestions.jsx":"9e987e330b53","components/widget/FriendSuggestions/FriendSuggestions.d.ts":"6dac979dce19","components/widget/FriendSuggestions/FriendSuggestions.prompt.md":"e09056352a81","components/partials/GoToTop/GoToTop.jsx":"ebc1876e812d","components/partials/GoToTop/GoToTop.d.ts":"dfc5a6093c65","components/partials/GoToTop/GoToTop.prompt.md":"a7cdd653a640","components/partials/HelloPreloader/HelloPreloader.jsx":"649f0aa9d0b0","components/partials/HelloPreloader/HelloPreloader.d.ts":"b1f073ad1018","components/partials/HelloPreloader/HelloPreloader.prompt.md":"8b3aa58951f6","components/home/HomeView/HomeView.jsx":"427c8d2a64cd","components/home/HomeView/HomeView.d.ts":"45e4f6c68587","components/home/HomeView/HomeView.prompt.md":"4114d3bc965d","components/general/Icon/Icon.jsx":"327e6a175e5e","components/general/Icon/Icon.d.ts":"b953cbae66e0","components/general/Icon/Icon.prompt.md":"3fc1f07b2b6a","components/auth/LoginView/LoginView.jsx":"b86ff10c6c9c","components/auth/LoginView/LoginView.d.ts":"12e5552a431b","components/auth/LoginView/LoginView.prompt.md":"319ac8630185","components/master/MasterBase/MasterBase.jsx":"794ed6094191","components/master/MasterBase/MasterBase.d.ts":"78c99aa225e5","components/master/MasterBase/MasterBase.prompt.md":"de08fd4cc44d","components/master/MasterPublic/MasterPublic.jsx":"68d654a8c687","components/master/MasterPublic/MasterPublic.d.ts":"f8e3f66856a2","components/master/MasterPublic/MasterPublic.prompt.md":"ac73aaeed1f5","components/general/MessagesMenu/MessagesMenu.jsx":"8f7d104afb2d","components/general/MessagesMenu/MessagesMenu.d.ts":"48058318671f","components/general/MessagesMenu/MessagesMenu.prompt.md":"2a02efd43086","components/general/NotificationsMenu/NotificationsMenu.jsx":"8f3b3658a39e","components/general/NotificationsMenu/NotificationsMenu.d.ts":"9f8d15496d0b","components/general/NotificationsMenu/NotificationsMenu.prompt.md":"99f463cc7de0","components/novel/NovelDetailView/NovelDetailView.jsx":"9e60657d9e70","components/novel/NovelDetailView/NovelDetailView.d.ts":"4d7af66ef501","components/novel/NovelDetailView/NovelDetailView.prompt.md":"524cdaadd871","components/widget/PagesWidget/PagesWidget.jsx":"519f4039a6a4","components/widget/PagesWidget/PagesWidget.d.ts":"b8788812f282","components/widget/PagesWidget/PagesWidget.prompt.md":"aef313730b35","components/widget/PersonalInfoWidget/PersonalInfoWidget.jsx":"c77e6162454c","components/widget/PersonalInfoWidget/PersonalInfoWidget.d.ts":"81ab6fc38c0d","components/widget/PersonalInfoWidget/PersonalInfoWidget.prompt.md":"33f7eab8849e","components/music/PlaylistWidget/PlaylistWidget.jsx":"d141b1b1da88","components/music/PlaylistWidget/PlaylistWidget.d.ts":"003d70e56a97","components/music/PlaylistWidget/PlaylistWidget.prompt.md":"c8d60820436c","components/general/Post/Post.jsx":"543b6ccfbac2","components/general/Post/Post.d.ts":"0ea5d52ff044","components/general/Post/Post.prompt.md":"860707350182","components/post/PostControlButtons/PostControlButtons.jsx":"046b20a4f13f","components/post/PostControlButtons/PostControlButtons.d.ts":"40c709a63556","components/post/PostControlButtons/PostControlButtons.prompt.md":"e0e97ecb9bcf","components/profile/ProfileHeader/ProfileHeader.jsx":"17dc135c32e5","components/profile/ProfileHeader/ProfileHeader.d.ts":"c9881fdefc41","components/profile/ProfileHeader/ProfileHeader.prompt.md":"8eaed02cc313","components/post/ReactionBar/ReactionBar.jsx":"04d72b6d490c","components/post/ReactionBar/ReactionBar.d.ts":"90e910fa88a3","components/post/ReactionBar/ReactionBar.prompt.md":"5446f93193bf","components/auth/RegisterView/RegisterView.jsx":"0f9760f1341e","components/auth/RegisterView/RegisterView.d.ts":"92a8c302c44c","components/auth/RegisterView/RegisterView.prompt.md":"6ca50c2600a0","components/form/SelectField/SelectField.jsx":"52557bb1e40a","components/form/SelectField/SelectField.d.ts":"e663d927abec","components/form/SelectField/SelectField.prompt.md":"441b0b51704e","components/menu/SidebarCenter/SidebarCenter.jsx":"8d50d316a50b","components/menu/SidebarCenter/SidebarCenter.d.ts":"e2a916efabc5","components/menu/SidebarCenter/SidebarCenter.prompt.md":"d18e20ead63a","components/menu/SidebarLeft/SidebarLeft.jsx":"b6aae23633b9","components/menu/SidebarLeft/SidebarLeft.d.ts":"f12def96299c","components/menu/SidebarLeft/SidebarLeft.prompt.md":"e9b262d2b815","components/menu/SidebarRight/SidebarRight.jsx":"e9b448900d53","components/menu/SidebarRight/SidebarRight.d.ts":"0d4d15dd7d60","components/menu/SidebarRight/SidebarRight.prompt.md":"50b7c004f1d4","components/form/TagSelect/TagSelect.jsx":"af91776a8220","components/form/TagSelect/TagSelect.d.ts":"87f6b55f58b9","components/form/TagSelect/TagSelect.prompt.md":"b33852eb9270","components/form/ToggleRow/ToggleRow.jsx":"35633469de90","components/form/ToggleRow/ToggleRow.d.ts":"928f35533522","components/form/ToggleRow/ToggleRow.prompt.md":"79a8971f5545","components/headers/TopMenu/TopMenu.jsx":"3540cfee1989","components/headers/TopMenu/TopMenu.d.ts":"6516d4db52ab","components/headers/TopMenu/TopMenu.prompt.md":"7bbf0356c5e0","components/music/TrackItem/TrackItem.jsx":"db070a19f13d","components/music/TrackItem/TrackItem.d.ts":"91c728ee02c2","components/music/TrackItem/TrackItem.prompt.md":"00b43be10f28","components/popup/UpdateHeaderPhoto/UpdateHeaderPhoto.jsx":"1625f80c3af1","components/popup/UpdateHeaderPhoto/UpdateHeaderPhoto.d.ts":"261b915ce28d","components/popup/UpdateHeaderPhoto/UpdateHeaderPhoto.prompt.md":"b4e809dc5e46","components/upload/UploadStudio/UploadStudio.jsx":"e4e9f9702fee","components/upload/UploadStudio/UploadStudio.d.ts":"1e0a5a083112","components/upload/UploadStudio/UploadStudio.prompt.md":"74f724f476de","components/widget/WeatherWidget/WeatherWidget.jsx":"68162d56f018","components/widget/WeatherWidget/WeatherWidget.d.ts":"6d59df58d78a","components/widget/WeatherWidget/WeatherWidget.prompt.md":"77d3e21f57d4","components/widget/WidgetCard/WidgetCard.jsx":"2dde1e2d0e88","components/widget/WidgetCard/WidgetCard.d.ts":"79ae7519da81","components/widget/WidgetCard/WidgetCard.prompt.md":"c73ea6104733"},"inlinedExternals":["@floating-ui/core","@floating-ui/dom","@floating-ui/utils","@vidstack/react","media-captions","portal-frontend"],"builtBy":"cc-design-sync"} */
var PortalUI = (() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __typeError = (msg) => {
    throw TypeError(msg);
  };
  var __defNormalProp = (obj, key2, value) => key2 in obj ? __defProp(obj, key2, { enumerable: true, configurable: true, writable: true, value }) : obj[key2] = value;
  var __esm = (fn, res, err) => function __init() {
    if (err) throw err[0];
    try {
      return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
    } catch (e2) {
      throw err = [e2], e2;
    }
  };
  var __commonJS = (cb, mod) => function __require() {
    try {
      return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
    } catch (e2) {
      throw mod = 0, e2;
    }
  };
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key2 of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key2) && key2 !== except)
          __defProp(to, key2, { get: () => from[key2], enumerable: !(desc = __getOwnPropDesc(from, key2)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
  var __publicField = (obj, key2, value) => __defNormalProp(obj, typeof key2 !== "symbol" ? key2 + "" : key2, value);
  var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
  var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
  var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
  var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
  var __privateMethod = (obj, member, method2) => (__accessCheck(obj, member, "access private method"), method2);
  var __privateWrapper = (obj, member, setter, getter) => ({
    set _(value) {
      __privateSet(obj, member, value, setter);
    },
    get _() {
      return __privateGet(obj, member, getter);
    }
  });

  // <define:import.meta.env>
  var init_define_import_meta_env = __esm({
    "<define:import.meta.env>"() {
    }
  });

  // shim:react-shim
  var require_react_shim = __commonJS({
    "shim:react-shim"(exports, module) {
      init_define_import_meta_env();
      var R = window.React;
      function np(p, k) {
        var o2 = {};
        for (var x in p) if (x !== "children") o2[x] = p[x];
        if (k !== void 0) o2.key = k;
        return o2;
      }
      function jsx(t2, p, k) {
        var c = p && p.children;
        return c === void 0 ? R.createElement(t2, np(p, k)) : R.createElement(t2, np(p, k), c);
      }
      function jsxs(t2, p, k) {
        return R.createElement.apply(R, [t2, np(p, k)].concat(p.children));
      }
      module.exports = R;
      module.exports.jsx = jsx;
      module.exports.jsxs = jsxs;
      module.exports.jsxDEV = function(t2, p, k, s) {
        return (s ? jsxs : jsx)(t2, p, k);
      };
      module.exports.Fragment = R.Fragment;
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-BLEs9rMG.js
  var __defProp2, __exportAll;
  var init_vidstack_BLEs9rMG = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-BLEs9rMG.js"() {
      "use client";
      init_define_import_meta_env();
      __defProp2 = Object.defineProperty;
      __exportAll = (all, no_symbols) => {
        let target = {};
        for (var name in all) __defProp2(target, name, {
          get: all[name],
          enumerable: true
        });
        if (!no_symbols) __defProp2(target, Symbol.toStringTag, { value: "Module" });
        return target;
      };
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-B9fTkxQO.js
  var vidstack_B9fTkxQO_exports = {};
  __export(vidstack_B9fTkxQO_exports, {
    Component: () => Component2,
    DOMEvent: () => DOMEvent,
    EventsController: () => EventsController,
    EventsTarget: () => EventsTarget,
    Icon$0: () => Icon$0,
    Icon$104: () => Icon$104,
    Icon$105: () => Icon$105,
    Icon$11: () => Icon$11,
    Icon$13: () => Icon$13,
    Icon$16: () => Icon$16,
    Icon$19: () => Icon$19,
    Icon$22: () => Icon$22,
    Icon$24: () => Icon$24,
    Icon$26: () => Icon$26,
    Icon$27: () => Icon$27,
    Icon$31: () => Icon$31,
    Icon$33: () => Icon$33,
    Icon$34: () => Icon$34,
    Icon$35: () => Icon$35,
    Icon$39: () => Icon$39,
    Icon$40: () => Icon$40,
    Icon$5: () => Icon$5,
    Icon$53: () => Icon$53,
    Icon$54: () => Icon$54,
    Icon$56: () => Icon$56,
    Icon$58: () => Icon$58,
    Icon$59: () => Icon$59,
    Icon$60: () => Icon$60,
    Icon$61: () => Icon$61,
    Icon$62: () => Icon$62,
    Icon$63: () => Icon$63,
    Icon$74: () => Icon$74,
    Icon$77: () => Icon$77,
    Icon$8: () => Icon$8,
    Icon$81: () => Icon$81,
    Icon$86: () => Icon$86,
    Icon$88: () => Icon$88,
    State: () => State,
    ViewController: () => ViewController,
    animationFrameThrottle: () => animationFrameThrottle,
    appendTriggerEvent: () => appendTriggerEvent,
    ariaBool: () => ariaBool,
    camelToKebabCase: () => camelToKebabCase,
    chromecast_exports: () => chromecast_exports,
    composeRefs: () => composeRefs,
    computed: () => computed,
    createContext: () => createContext2,
    createDisposalBin: () => createDisposalBin,
    createReactComponent: () => createReactComponent,
    createScope: () => createScope,
    deferredPromise: () => deferredPromise,
    effect: () => effect,
    findTriggerEvent: () => findTriggerEvent,
    fscreen: () => fscreen,
    functionDebounce: () => functionDebounce,
    functionThrottle: () => functionThrottle,
    getScope: () => getScope,
    hasProvidedContext: () => hasProvidedContext,
    hasTriggerEvent: () => hasTriggerEvent,
    isArray: () => isArray,
    isBoolean: () => isBoolean,
    isDOMNode: () => isDOMNode,
    isFunction: () => isFunction,
    isKeyboardClick: () => isKeyboardClick,
    isKeyboardEvent: () => isKeyboardEvent,
    isMouseEvent: () => isMouseEvent,
    isNil: () => isNil,
    isNull: () => isNull,
    isNumber: () => isNumber,
    isObject: () => isObject,
    isPointerEvent: () => isPointerEvent,
    isString: () => isString,
    isTouchEvent: () => isTouchEvent,
    isUndefined: () => isUndefined,
    isWriteSignal: () => isWriteSignal,
    kebabToCamelCase: () => kebabToCamelCase,
    keysOf: () => keysOf,
    listenEvent: () => listenEvent,
    method: () => method,
    noop: () => noop,
    onDispose: () => onDispose,
    peek: () => peek,
    prop: () => prop,
    provideContext: () => provideContext,
    r: () => r,
    scoped: () => scoped,
    setAttribute: () => setAttribute,
    setStyle: () => setStyle,
    signal: () => signal,
    tick: () => tick,
    toggleClass: () => toggleClass,
    untrack: () => untrack,
    uppercaseFirstChar: () => uppercaseFirstChar,
    useContext: () => useContext2,
    useReactContext: () => useReactContext,
    useReactScope: () => useReactScope,
    useSignal: () => useSignal,
    useSignalRecord: () => useSignalRecord,
    useState: () => useState18,
    useStateContext: () => useStateContext,
    waitIdlePeriod: () => waitIdlePeriod,
    waitTimeout: () => waitTimeout,
    walkTriggerEventChain: () => walkTriggerEventChain,
    wasEnterKeyPressed: () => wasEnterKeyPressed
  });
  function flushEffects() {
    scheduledEffects = true;
    queueMicrotask(runEffects);
  }
  function runEffects() {
    if (!effects.length) {
      scheduledEffects = false;
      return;
    }
    runningEffects = true;
    for (let i = 0; i < effects.length; i++) if (effects[i].$st !== STATE_CLEAN) runTop(effects[i]);
    effects = [];
    scheduledEffects = false;
    runningEffects = false;
  }
  function runTop(node) {
    let ancestors = [node];
    while (node = node[SCOPE]) if (node.$e && node.$st !== STATE_CLEAN) ancestors.push(node);
    for (let i = ancestors.length - 1; i >= 0; i--) updateCheck(ancestors[i]);
  }
  function root(init) {
    const scope = createScope();
    return compute(scope, !init.length ? init : init.bind(null, dispose.bind(scope)), null);
  }
  function peek(fn) {
    return compute(currentScope, fn, null);
  }
  function untrack(fn) {
    return compute(null, fn, null);
  }
  function tick() {
    if (!runningEffects) runEffects();
  }
  function getScope() {
    return currentScope;
  }
  function scoped(run, scope) {
    try {
      return compute(scope, run, null);
    } catch (error) {
      handleError(scope, error);
      return;
    }
  }
  function getContext(key2, scope = currentScope) {
    return scope?.$cx[key2];
  }
  function setContext(key2, value, scope = currentScope) {
    if (scope) scope.$cx = {
      ...scope.$cx,
      [key2]: value
    };
  }
  function onDispose(disposable) {
    if (!disposable || !currentScope) return disposable || NOOP;
    const node = currentScope;
    if (!node.$d) node.$d = disposable;
    else if (Array.isArray(node.$d)) node.$d.push(disposable);
    else node.$d = [node.$d, disposable];
    return function removeDispose() {
      if (node.$st === STATE_DISPOSED) return;
      disposable.call(null);
      if (isFunction$1(node.$d)) node.$d = null;
      else if (Array.isArray(node.$d)) node.$d.splice(node.$d.indexOf(disposable), 1);
    };
  }
  function dispose(self = true) {
    if (this.$st === STATE_DISPOSED) return;
    if (this.$h) if (Array.isArray(this.$h)) for (let i = this.$h.length - 1; i >= 0; i--) dispose.call(this.$h[i]);
    else dispose.call(this.$h);
    if (self) {
      const parent = this[SCOPE];
      if (parent) if (Array.isArray(parent.$h)) parent.$h.splice(parent.$h.indexOf(this), 1);
      else parent.$h = null;
      disposeNode(this);
    }
  }
  function disposeNode(node) {
    node.$st = STATE_DISPOSED;
    if (node.$d) emptyDisposal(node);
    if (node.$s) removeSourceObservers(node, 0);
    node[SCOPE] = null;
    node.$s = null;
    node.$o = null;
    node.$h = null;
    node.$cx = defaultContext;
    node.$eh = null;
  }
  function emptyDisposal(scope) {
    try {
      if (Array.isArray(scope.$d)) for (let i = scope.$d.length - 1; i >= 0; i--) {
        const callable = scope.$d[i];
        callable.call(callable);
      }
      else scope.$d.call(scope.$d);
      scope.$d = null;
    } catch (error) {
      handleError(scope, error);
    }
  }
  function compute(scope, compute2, observer) {
    const prevScope = currentScope, prevObserver = currentObserver;
    currentScope = scope;
    currentObserver = observer;
    try {
      return compute2.call(scope);
    } finally {
      currentScope = prevScope;
      currentObserver = prevObserver;
    }
  }
  function handleError(scope, error) {
    if (!scope || !scope.$eh) throw error;
    let i = 0, len = scope.$eh.length, currentError = error;
    for (i = 0; i < len; i++) try {
      scope.$eh[i](currentError);
      break;
    } catch (error2) {
      currentError = error2;
    }
    if (i === len) throw currentError;
  }
  function read() {
    if (this.$st === STATE_DISPOSED) return this.$v;
    if (currentObserver && !this.$e) if (!currentObservers && currentObserver.$s && currentObserver.$s[currentObserversIndex] == this) currentObserversIndex++;
    else if (!currentObservers) currentObservers = [this];
    else currentObservers.push(this);
    if (this.$c) updateCheck(this);
    return this.$v;
  }
  function write(newValue) {
    const value = isFunction$1(newValue) ? newValue(this.$v) : newValue;
    if (this.$ch(this.$v, value)) {
      this.$v = value;
      if (this.$o) for (let i = 0; i < this.$o.length; i++) notify(this.$o[i], STATE_DIRTY);
    }
    return this.$v;
  }
  function createScope() {
    return new ScopeNode();
  }
  function createComputation(initialValue, compute2, options) {
    return new ComputeNode(initialValue, compute2, options);
  }
  function isNotEqual(a, b) {
    return a !== b;
  }
  function isFunction$1(value) {
    return typeof value === "function";
  }
  function updateCheck(node) {
    if (node.$st === STATE_CHECK) for (let i = 0; i < node.$s.length; i++) {
      updateCheck(node.$s[i]);
      if (node.$st === STATE_DIRTY) break;
    }
    if (node.$st === STATE_DIRTY) update(node);
    else node.$st = STATE_CLEAN;
  }
  function cleanup(node) {
    if (node.$h) dispose.call(node, false);
    if (node.$d) emptyDisposal(node);
    node.$eh = node[SCOPE] ? node[SCOPE].$eh : null;
  }
  function update(node) {
    let prevObservers = currentObservers, prevObserversIndex = currentObserversIndex;
    currentObservers = null;
    currentObserversIndex = 0;
    try {
      cleanup(node);
      const result = compute(node, node.$c, node);
      updateObservers(node);
      if (!node.$e && node.$i) write.call(node, result);
      else {
        node.$v = result;
        node.$i = true;
      }
    } catch (error) {
      updateObservers(node);
      handleError(node, error);
    } finally {
      currentObservers = prevObservers;
      currentObserversIndex = prevObserversIndex;
      node.$st = STATE_CLEAN;
    }
  }
  function updateObservers(node) {
    if (currentObservers) {
      if (node.$s) removeSourceObservers(node, currentObserversIndex);
      if (node.$s && currentObserversIndex > 0) {
        node.$s.length = currentObserversIndex + currentObservers.length;
        for (let i = 0; i < currentObservers.length; i++) node.$s[currentObserversIndex + i] = currentObservers[i];
      } else node.$s = currentObservers;
      let source;
      for (let i = currentObserversIndex; i < node.$s.length; i++) {
        source = node.$s[i];
        if (!source.$o) source.$o = [node];
        else source.$o.push(node);
      }
    } else if (node.$s && currentObserversIndex < node.$s.length) {
      removeSourceObservers(node, currentObserversIndex);
      node.$s.length = currentObserversIndex;
    }
  }
  function notify(node, state) {
    if (node.$st >= state) return;
    if (node.$e && node.$st === STATE_CLEAN) {
      effects.push(node);
      if (!scheduledEffects) flushEffects();
    }
    node.$st = state;
    if (node.$o) for (let i = 0; i < node.$o.length; i++) notify(node.$o[i], STATE_CHECK);
  }
  function removeSourceObservers(node, index) {
    let source, swap;
    for (let i = index; i < node.$s.length; i++) {
      source = node.$s[i];
      if (source.$o) {
        swap = source.$o.indexOf(node);
        source.$o[swap] = source.$o[source.$o.length - 1];
        source.$o.pop();
      }
    }
  }
  function signal(initialValue, options) {
    const node = createComputation(initialValue, null, options), signal2 = read.bind(node);
    signal2[SCOPE] = true;
    signal2.set = write.bind(node);
    return signal2;
  }
  function isReadSignal(fn) {
    return isFunction$1(fn) && SCOPE in fn;
  }
  function computed(compute2, options) {
    const node = createComputation(options?.initial, compute2, options), signal2 = read.bind(node);
    signal2[SCOPE] = true;
    return signal2;
  }
  function effect$1(effect2, options) {
    const signal2 = createComputation(null, function runEffect() {
      let effectResult = effect2();
      isFunction$1(effectResult) && onDispose(effectResult);
      return null;
    }, void 0);
    signal2.$e = true;
    update(signal2);
    return dispose.bind(signal2, true);
  }
  function isWriteSignal(fn) {
    return isReadSignal(fn) && "set" in fn;
  }
  function noop(...args) {
  }
  function isNull(value) {
    return value === null;
  }
  function isUndefined(value) {
    return typeof value === "undefined";
  }
  function isNil(value) {
    return isNull(value) || isUndefined(value);
  }
  function isObject(value) {
    return value?.constructor === Object;
  }
  function isNumber(value) {
    return typeof value === "number" && !Number.isNaN(value);
  }
  function isString(value) {
    return typeof value === "string";
  }
  function isBoolean(value) {
    return typeof value === "boolean";
  }
  function isFunction(value) {
    return typeof value === "function";
  }
  function isArray(value) {
    return Array.isArray(value);
  }
  function isDOMEvent(event) {
    return !!event?.[DOM_EVENT];
  }
  function walkTriggerEventChain(event, callback) {
    if (!isDOMEvent(event)) return;
    return event.triggers.walk(callback);
  }
  function findTriggerEvent(event, type) {
    return isDOMEvent(event) ? event.triggers.findType(type) : void 0;
  }
  function hasTriggerEvent(event, type) {
    return !!findTriggerEvent(event, type);
  }
  function appendTriggerEvent(event, trigger) {
    if (trigger) event.triggers.add(trigger);
  }
  function listenEvent(target, type, handler, options) {
    target.addEventListener(type, handler, options);
    return onDispose(() => target.removeEventListener(type, handler, options));
  }
  function anySignal(...signals) {
    const controller = new AbortController(), options = { signal: controller.signal };
    function onAbort(event) {
      controller.abort(event.target.reason);
    }
    for (const signal2 of signals) {
      if (signal2.aborted) {
        controller.abort(signal2.reason);
        break;
      }
      signal2.addEventListener("abort", onAbort, options);
    }
    return controller.signal;
  }
  function isPointerEvent(event) {
    return !!event?.type.startsWith("pointer");
  }
  function isTouchEvent(event) {
    return !!event?.type.startsWith("touch");
  }
  function isMouseEvent(event) {
    return /^(click|mouse)/.test(event?.type ?? "");
  }
  function isKeyboardEvent(event) {
    return !!event?.type.startsWith("key");
  }
  function wasEnterKeyPressed(event) {
    return isKeyboardEvent(event) && event.key === "Enter";
  }
  function isKeyboardClick(event) {
    return isKeyboardEvent(event) && (event.key === "Enter" || event.key === " ");
  }
  function isDOMNode(node) {
    return node instanceof Node;
  }
  function setAttribute(host, name, value) {
    if (!host) return;
    else if (!value && value !== "" && value !== 0) host.removeAttribute(name);
    else {
      const attrValue = value === true ? "" : value + "";
      if (host.getAttribute(name) !== attrValue) host.setAttribute(name, attrValue);
    }
  }
  function setStyle(host, property, value) {
    if (!host) return;
    else if (!value && value !== 0) host.style.removeProperty(property);
    else host.style.setProperty(property, value + "");
  }
  function toggleClass(host, name, value) {
    host.classList[value ? "add" : "remove"](name);
  }
  function camelToKebabCase(str) {
    return str.replace(/([a-z])([A-Z])/g, "$1-$2").toLowerCase();
  }
  function kebabToCamelCase(str) {
    return str.replace(/-./g, (x) => x[1].toUpperCase());
  }
  function kebabToPascalCase(str) {
    return kebabToTitleCase(str).replace(/\s/g, "");
  }
  function kebabToTitleCase(str) {
    return uppercaseFirstChar(str.replace(/-./g, (x) => " " + x[1].toUpperCase()));
  }
  function uppercaseFirstChar(str) {
    return str.charAt(0).toUpperCase() + str.slice(1);
  }
  function ariaBool(value) {
    return value ? "true" : "false";
  }
  function createDisposalBin() {
    const disposal = /* @__PURE__ */ new Set();
    return {
      add(...callbacks) {
        for (const callback of callbacks) disposal.add(callback);
      },
      empty() {
        for (const callback of disposal) callback();
        disposal.clear();
      }
    };
  }
  function keysOf(obj) {
    return Object.keys(obj);
  }
  function deferredPromise() {
    let resolve, reject;
    return {
      promise: new Promise((res, rej) => {
        resolve = res;
        reject = rej;
      }),
      resolve,
      reject
    };
  }
  function waitTimeout(delay) {
    return new Promise((resolve) => setTimeout(resolve, delay));
  }
  function animationFrameThrottle(func) {
    let id2 = -1, lastArgs;
    function throttle2(...args) {
      lastArgs = args;
      if (id2 >= 0) return;
      id2 = window.requestAnimationFrame(() => {
        func.apply(this, lastArgs);
        id2 = -1;
        lastArgs = void 0;
      });
    }
    return throttle2;
  }
  function waitIdlePeriod(callback, options) {
    return new Promise((resolve) => {
      requestIdleCallback((deadline) => {
        callback?.(deadline);
        resolve();
      }, options);
    });
  }
  function createContext2(provide) {
    return {
      id: /* @__PURE__ */ Symbol(),
      provide
    };
  }
  function provideContext(context, value, scope = getScope()) {
    const hasProvidedValue = !isUndefined(value);
    setContext(context.id, hasProvidedValue ? value : context.provide?.(), scope);
  }
  function useContext2(context) {
    return getContext(context.id);
  }
  function hasProvidedContext(context) {
    return !isUndefined(getContext(context.id));
  }
  function createInstanceProps(props) {
    const $props = {};
    for (const name of Object.keys(props)) {
      const def = props[name];
      $props[name] = signal(def, def);
    }
    return $props;
  }
  function createComponent(Component3, init) {
    return root(() => {
      currentInstance.$$ = new Instance(Component3, getScope(), init);
      const component = new Component3();
      currentInstance.$$.component = component;
      currentInstance.$$ = null;
      return component;
    });
  }
  function prop(target, propertyKey, descriptor) {
    if (!target[PROPS]) target[PROPS] = /* @__PURE__ */ new Set();
    target[PROPS].add(propertyKey);
  }
  function method(target, propertyKey, descriptor) {
    if (!target[METHODS]) target[METHODS] = /* @__PURE__ */ new Set();
    target[METHODS].add(propertyKey);
  }
  function useState18(state) {
    return useContext2(state);
  }
  function throttle(fn, interval, options) {
    var timeoutId = null;
    var throttledFn = null;
    var leading = options && options.leading;
    var trailing = options && options.trailing;
    if (leading == null) leading = true;
    if (trailing == null) trailing = !leading;
    if (leading == true) trailing = false;
    var cancel = function() {
      if (timeoutId) {
        clearTimeout(timeoutId);
        timeoutId = null;
      }
    };
    var flush = function() {
      var call = throttledFn;
      cancel();
      if (call) call();
    };
    var throttleWrapper = function() {
      var callNow = leading && !timeoutId;
      var context = this;
      var args = arguments;
      throttledFn = function() {
        return fn.apply(context, args);
      };
      if (!timeoutId) timeoutId = setTimeout(function() {
        timeoutId = null;
        if (trailing) return throttledFn();
      }, interval);
      if (callNow) {
        callNow = false;
        return throttledFn();
      }
    };
    throttleWrapper.cancel = cancel;
    throttleWrapper.flush = flush;
    return throttleWrapper;
  }
  function debounce(fn, wait, callFirst) {
    var timeout = null;
    var debouncedFn = null;
    var clear = function() {
      if (timeout) {
        clearTimeout(timeout);
        debouncedFn = null;
        timeout = null;
      }
    };
    var flush = function() {
      var call = debouncedFn;
      clear();
      if (call) call();
    };
    var debounceWrapper = function() {
      if (!wait) return fn.apply(this, arguments);
      var context = this;
      var args = arguments;
      var callNow = callFirst && !timeout;
      clear();
      debouncedFn = function() {
        fn.apply(context, args);
      };
      timeout = setTimeout(function() {
        timeout = null;
        if (!callNow) {
          var call = debouncedFn;
          debouncedFn = null;
          return call();
        }
      }, wait);
      if (callNow) return debouncedFn();
    };
    debounceWrapper.cancel = clear;
    debounceWrapper.flush = flush;
    return debounceWrapper;
  }
  function WithScope(scope, ...children) {
    return React4.createElement(ReactScopeContext.Provider, { value: scope }, ...children);
  }
  function useReactScope() {
    return React4.useContext(ReactScopeContext).current;
  }
  function useReactContext(context) {
    const scope = useReactScope();
    return React4.useMemo(() => getContext(context.id, scope), [scope]);
  }
  function setRef(ref, value) {
    if (typeof ref === "function") ref(value);
    else if (ref) ref.current = value;
  }
  function composeRefs(...refs) {
    return (node) => refs.forEach((ref) => setRef(ref, node));
  }
  function createClientComponent(Component3, options) {
    const forwardComponent = React4.forwardRef((props, forwardRef8) => {
      let parentScopeRef = React4.useContext(ReactScopeContext), scopeRef = React4.useRef(null), stateRef = React4.useRef();
      if (!stateRef.current) {
        const state2 = createInternalState(), component = initComponent(Component3, state2, props, parentScopeRef.current);
        state2.component = component;
        stateRef.current = state2;
        scopeRef.current = component.scope;
      }
      function onAttach() {
        let state2 = stateRef.current, scope = parentScopeRef.current;
        clearTimeout(state2.destroyId);
        state2.destroyId = -1;
        if (state2.component.$$.destroyed) {
          const component = initComponent(Component3, state2, props, scope);
          state2.component = component;
          state2.attached = false;
          state2.forwardRef = false;
          scopeRef.current = component.scope;
        }
        if (state2.el) attachToHost(state2, state2.el);
        if (!state2.forwardRef) {
          setRef(forwardRef8, state2.component);
          state2.forwardRef = true;
        }
        return () => detachFromHost(state2);
      }
      function onRefChange(el) {
        const state2 = stateRef.current;
        if (!state2.forwardRef) {
          state2.el = el;
          return;
        }
        clearTimeout(state2.refChangeId);
        state2.refChangeId = window.setTimeout(() => {
          const state3 = stateRef.current;
          state3.refChangeId = -1;
          if (state3.el === el) return;
          detachFromHost(state3);
          if (el) attachToHost(state3, el);
          state3.el = el;
        });
      }
      React4.useEffect(() => {
        const state2 = stateRef.current;
        clearTimeout(state2.destroyId);
        state2.destroyId = -1;
        return function onDestroy() {
          if (!isFunction(props.children)) return;
          clearTimeout(state2.refChangeId);
          state2.refChangeId = -1;
          clearTimeout(state2.connectId);
          state2.connectId = -1;
          clearTimeout(state2.destroyId);
          state2.destroyId = window.setTimeout(() => {
            state2.destroyId = -1;
            detachFromHost(state2);
            state2.component.$$.destroy();
            state2.component.$$[ON_DISPATCH] = null;
            state2.callbacks = {};
            state2.domCallbacks = {};
            scopeRef.current = null;
          });
        };
      }, []);
      React4.useEffect(tick);
      let state = stateRef.current, { children, ...renderProps } = props, attrs = {}, prevPropNames = state.prevProps, newPropNames = Object.keys(renderProps);
      state.callbacks = {};
      for (const name of [...prevPropNames, ...newPropNames]) if (options.props.has(name)) state.component.$props[name].set(isUndefined(renderProps[name]) ? Component3.props?.[name] : renderProps[name]);
      else if (options.events?.has(name) || options.eventsRE?.test(name)) state.callbacks[name] = renderProps[name];
      else if (options.domEvents?.has(name) || options.domEventsRE?.test(name)) {
        let type = camelToKebabCase(name.slice(2));
        state.domCallbacks[type] = renderProps[name];
        if (!newPropNames.includes(name)) {
          state.el?.removeEventListener(type, state.onDOMEvent);
          state.listening?.delete(type);
        } else if (state.el && !state.listening?.has(type)) {
          if (!state.listening) state.listening = /* @__PURE__ */ new Set();
          state.listening.add(type);
          state.el.addEventListener(type, state.onDOMEvent);
        }
      } else attrs[name] = renderProps[name];
      state.prevProps = newPropNames;
      return WithScope(scopeRef, React4.createElement(AttachEffect, { effect: onAttach }), isFunction(children) ? children?.({
        ...attrs,
        suppressHydrationWarning: true,
        ref: onRefChange
      }, state.component) : children);
    });
    forwardComponent.displayName = Component3.name + "Bridge";
    return forwardComponent;
  }
  function AttachEffect({ effect: effect2 }) {
    React4.useEffect(effect2, []);
    return null;
  }
  function createInternalState() {
    const state = {
      el: null,
      prevProps: [],
      callbacks: {},
      domCallbacks: {},
      refChangeId: -1,
      connectId: -1,
      destroyId: -1,
      attached: false,
      forwardRef: false,
      listening: null,
      onDOMEvent(event) {
        const args = !isUndefined(event.detail) ? [event.detail, event] : [event];
        state.domCallbacks[event.type]?.(...args);
      }
    };
    return state;
  }
  function attachToHost(state, el) {
    if (state.el === el && state.attached) return;
    else if (state.attached) detachFromHost(state);
    if (state.domCallbacks) {
      if (!state.listening) state.listening = /* @__PURE__ */ new Set();
      for (const type of Object.keys(state.domCallbacks)) {
        if (state.listening.has(type)) continue;
        el.addEventListener(type, state.onDOMEvent);
        state.listening.add(type);
      }
    }
    state.component.$$.attach(el);
    state.connectId = window.setTimeout(() => {
      state.component.$$.connect();
      state.connectId = -1;
    });
    state.attached = true;
  }
  function detachFromHost(state) {
    if (!state.attached) return;
    clearTimeout(state.connectId);
    state.connectId = -1;
    state.component.$$.detach();
    state.attached = false;
    if (state.el && state.listening) {
      for (const type of state.listening) state.el.removeEventListener(type, state.onDOMEvent);
      state.listening.clear();
    }
  }
  function onDispatch(event) {
    let callbackProp = eventTypeToCallbackName.get(event.type), args = !isUndefined(event.detail) ? [event.detail, event] : [event];
    if (!callbackProp) eventTypeToCallbackName.set(event.type, callbackProp = `on${kebabToPascalCase(event.type)}`);
    this.callbacks[callbackProp]?.(...args);
  }
  function initComponent(Component3, state, props, scope) {
    const component = createComponent(Component3, {
      props,
      scope
    });
    component.$$[ON_DISPATCH] = onDispatch.bind(state);
    component.$$.setup();
    return component;
  }
  function useStateContext(state) {
    return useReactContext(state);
  }
  function useSignal(signal2, key2) {
    const [, scheduleReactUpdate] = React4.useState();
    React4.useEffect(() => {
      return effect$1(() => {
        signal2();
        scheduleReactUpdate({});
      });
    }, [key2 ?? signal2]);
    return signal2();
  }
  function useSignalRecord($state) {
    const [, scheduleReactUpdate] = React4.useState(), tracking = React4.useRef(null);
    if (tracking.current == null) tracking.current = {
      state: {},
      $update: signal({}),
      props: /* @__PURE__ */ new Set()
    };
    React4.useEffect(() => {
      let { state, $update, props } = tracking.current;
      return effect(() => {
        for (const prop2 of props) {
          const value = $state[prop2]();
          state[prop2] = isArray(value) ? [...value] : value;
        }
        $update();
        scheduleReactUpdate({});
      });
    }, [$state]);
    return React4.useMemo(() => {
      let { state, $update, props } = tracking.current, scheduledUpdate = false;
      props.clear();
      return new Proxy(state, {
        get(_, prop2) {
          if (!props.has(prop2) && prop2 in $state) {
            props.add(prop2);
            const value = $state[prop2]();
            state[prop2] = isArray(value) ? [...value] : value;
            if (!scheduledUpdate) {
              $update.set({});
              scheduledUpdate = true;
              queueMicrotask(() => scheduledUpdate = false);
            }
          }
          return state[prop2];
        },
        set(_, prop2, newValue) {
          if (!(prop2 in $state)) state[prop2] = newValue;
          return true;
        }
      });
    }, [$state]);
  }
  function createReactComponent(Component3, options) {
    return createClientComponent(Component3, {
      props: new Set(Object.keys(Component3.props || {})),
      events: new Set(options?.events),
      eventsRE: options?.eventsRegex,
      domEvents: options?.domEvents,
      domEventsRE: options?.domEventsRegex
    });
  }
  var React4, SCOPE, scheduledEffects, runningEffects, currentScope, currentObserver, currentObservers, currentObserversIndex, effects, defaultContext, NOOP, STATE_CLEAN, STATE_CHECK, STATE_DIRTY, STATE_DISPOSED, ScopeNode, ScopeProto, ComputeNode, ComputeProto, effect, EVENT, DOM_EVENT, DOMEvent, EventTriggers, EventsTarget, _target, _controller, _a, EventsController, requestIdleCallback, key, webkit, moz, ms, document$1, vendor, fscreen, PROPS, METHODS, ON_DISPATCH, EMPTY_PROPS, _setupCallbacks, _attachCallbacks, _connectCallbacks, _destroyCallbacks, _Instance_instances, attachAttrs_fn, attachStyles_fn, setAttr_fn, setStyle_fn, _a2, Instance, currentInstance, ViewController, Component2, _descriptors, _a3, State, functionThrottle, functionDebounce, t, e, n, o, l, r, ReactScopeContext, _a4, _context, _provide, _scope, eventTypeToCallbackName, Icon$0, Icon$5, Icon$8, Icon$11, Icon$13, Icon$16, Icon$19, Icon$22, chromecast_exports, Icon$24, Icon$26, Icon$27, Icon$31, Icon$33, Icon$34, Icon$35, Icon$39, Icon$40, Icon$53, Icon$54, Icon$56, Icon$58, Icon$59, Icon$60, Icon$61, Icon$62, Icon$63, Icon$74, Icon$77, Icon$81, Icon$86, Icon$88, Icon$104, Icon$105;
  var init_vidstack_B9fTkxQO = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-B9fTkxQO.js"() {
      "use client";
      init_define_import_meta_env();
      init_vidstack_BLEs9rMG();
      React4 = __toESM(require_react_shim(), 1);
      SCOPE = /* @__PURE__ */ Symbol(0);
      scheduledEffects = false;
      runningEffects = false;
      currentScope = null;
      currentObserver = null;
      currentObservers = null;
      currentObserversIndex = 0;
      effects = [];
      defaultContext = {};
      NOOP = () => {
      };
      STATE_CLEAN = 0;
      STATE_CHECK = 1;
      STATE_DIRTY = 2;
      STATE_DISPOSED = 3;
      ScopeNode = function Scope() {
        this[SCOPE] = null;
        this.$h = null;
        if (currentScope) currentScope.append(this);
      };
      ScopeProto = ScopeNode.prototype;
      ScopeProto.$cx = defaultContext;
      ScopeProto.$eh = null;
      ScopeProto.$c = null;
      ScopeProto.$d = null;
      ScopeProto.append = function(child) {
        child[SCOPE] = this;
        if (!this.$h) this.$h = child;
        else if (Array.isArray(this.$h)) this.$h.push(child);
        else this.$h = [this.$h, child];
        child.$cx = child.$cx === defaultContext ? this.$cx : {
          ...this.$cx,
          ...child.$cx
        };
        if (this.$eh) child.$eh = !child.$eh ? this.$eh : [...child.$eh, ...this.$eh];
      };
      ScopeProto.dispose = function() {
        dispose.call(this);
      };
      ComputeNode = function Computation(initialValue, compute2, options) {
        ScopeNode.call(this);
        this.$st = compute2 ? STATE_DIRTY : STATE_CLEAN;
        this.$i = false;
        this.$e = false;
        this.$s = null;
        this.$o = null;
        this.$v = initialValue;
        if (compute2) this.$c = compute2;
        if (options && options.dirty) this.$ch = options.dirty;
      };
      ComputeProto = ComputeNode.prototype;
      Object.setPrototypeOf(ComputeProto, ScopeProto);
      ComputeProto.$ch = isNotEqual;
      ComputeProto.call = read;
      effect = effect$1;
      EVENT = Event;
      DOM_EVENT = /* @__PURE__ */ Symbol("DOM_EVENT");
      DOMEvent = class extends EVENT {
        /**
        * The preceding event that was responsible for this event being fired.
        */
        get trigger() {
          return this.triggers.source;
        }
        /**
        * The origin event that lead to this event being fired.
        */
        get originEvent() {
          return this.triggers.origin;
        }
        /**
        * Whether the origin event was triggered by the user.
        *
        * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/Event/isTrusted}
        */
        get isOriginTrusted() {
          return this.triggers.origin?.isTrusted ?? false;
        }
        constructor(type, ...init) {
          super(type, init[0]);
          this[DOM_EVENT] = true;
          this.triggers = new EventTriggers();
          this.detail = init[0]?.detail;
          const trigger = init[0]?.trigger;
          if (trigger) this.triggers.add(trigger);
        }
      };
      EventTriggers = class {
        constructor() {
          this.chain = [];
        }
        get source() {
          return this.chain[0];
        }
        get origin() {
          return this.chain[this.chain.length - 1];
        }
        /**
        * Appends the event to the end of the chain.
        */
        add(event) {
          this.chain.push(event);
          if (isDOMEvent(event)) this.chain.push(...event.triggers);
        }
        /**
        * Removes the event from the chain and returns it (if found).
        */
        remove(event) {
          return this.chain.splice(this.chain.indexOf(event), 1)[0];
        }
        /**
        * Returns whether the chain contains the given `event`.
        */
        has(event) {
          return this.chain.some((e2) => e2 === event);
        }
        /**
        * Returns whether the chain contains the given event type.
        */
        hasType(type) {
          return !!this.findType(type);
        }
        /**
        * Returns the first event with the given `type` found in the chain.
        */
        findType(type) {
          return this.chain.find((e2) => e2.type === type);
        }
        /**
        * Walks an event chain on a given `event`, and invokes the given `callback` for each trigger event.
        */
        walk(callback) {
          for (const event of this.chain) {
            const returnValue = callback(event);
            if (returnValue) return [event, returnValue];
          }
        }
        [Symbol.iterator]() {
          return this.chain.values();
        }
      };
      EventsTarget = class extends EventTarget {
        addEventListener(type, callback, options) {
          return super.addEventListener(type, callback, options);
        }
        removeEventListener(type, callback, options) {
          return super.removeEventListener(type, callback, options);
        }
      };
      EventsController = (_a = class {
        constructor(target) {
          __privateAdd(this, _target);
          __privateAdd(this, _controller);
          __privateSet(this, _target, target);
          __privateSet(this, _controller, new AbortController());
          onDispose(this.abort.bind(this));
        }
        get signal() {
          return __privateGet(this, _controller).signal;
        }
        add(type, handler, options) {
          if (this.signal.aborted) throw Error("aborted");
          __privateGet(this, _target).addEventListener(type, handler, {
            ...options,
            signal: options?.signal ? anySignal(this.signal, options.signal) : this.signal
          });
          return this;
        }
        remove(type, handler) {
          __privateGet(this, _target).removeEventListener(type, handler);
          return this;
        }
        abort(reason) {
          __privateGet(this, _controller).abort(reason);
        }
      }, _target = new WeakMap(), _controller = new WeakMap(), _a);
      requestIdleCallback = typeof window !== "undefined" ? "requestIdleCallback" in window ? window.requestIdleCallback : (cb) => window.setTimeout(cb, 1) : noop;
      key = {
        fullscreenEnabled: 0,
        fullscreenElement: 1,
        requestFullscreen: 2,
        exitFullscreen: 3,
        fullscreenchange: 4,
        fullscreenerror: 5,
        fullscreen: 6
      };
      webkit = [
        "webkitFullscreenEnabled",
        "webkitFullscreenElement",
        "webkitRequestFullscreen",
        "webkitExitFullscreen",
        "webkitfullscreenchange",
        "webkitfullscreenerror",
        "-webkit-full-screen"
      ];
      moz = [
        "mozFullScreenEnabled",
        "mozFullScreenElement",
        "mozRequestFullScreen",
        "mozCancelFullScreen",
        "mozfullscreenchange",
        "mozfullscreenerror",
        "-moz-full-screen"
      ];
      ms = [
        "msFullscreenEnabled",
        "msFullscreenElement",
        "msRequestFullscreen",
        "msExitFullscreen",
        "MSFullscreenChange",
        "MSFullscreenError",
        "-ms-fullscreen"
      ];
      document$1 = typeof window !== "undefined" && typeof window.document !== "undefined" ? window.document : {};
      vendor = "fullscreenEnabled" in document$1 && Object.keys(key) || webkit[0] in document$1 && webkit || moz[0] in document$1 && moz || ms[0] in document$1 && ms || [];
      fscreen = {
        requestFullscreen: function(element) {
          return element[vendor[key.requestFullscreen]]();
        },
        requestFullscreenFunction: function(element) {
          return element[vendor[key.requestFullscreen]];
        },
        get exitFullscreen() {
          return document$1[vendor[key.exitFullscreen]].bind(document$1);
        },
        get fullscreenPseudoClass() {
          return ":" + vendor[key.fullscreen];
        },
        addEventListener: function(type, handler, options) {
          return document$1.addEventListener(vendor[key[type]], handler, options);
        },
        removeEventListener: function(type, handler, options) {
          return document$1.removeEventListener(vendor[key[type]], handler, options);
        },
        get fullscreenEnabled() {
          return Boolean(document$1[vendor[key.fullscreenEnabled]]);
        },
        set fullscreenEnabled(val) {
        },
        get fullscreenElement() {
          return document$1[vendor[key.fullscreenElement]];
        },
        set fullscreenElement(val) {
        },
        get onfullscreenchange() {
          return document$1[("on" + vendor[key.fullscreenchange]).toLowerCase()];
        },
        set onfullscreenchange(handler) {
          return document$1[("on" + vendor[key.fullscreenchange]).toLowerCase()] = handler;
        },
        get onfullscreenerror() {
          return document$1[("on" + vendor[key.fullscreenerror]).toLowerCase()];
        },
        set onfullscreenerror(handler) {
          return document$1[("on" + vendor[key.fullscreenerror]).toLowerCase()] = handler;
        }
      };
      PROPS = /* @__PURE__ */ Symbol(0);
      METHODS = /* @__PURE__ */ Symbol(0);
      ON_DISPATCH = /* @__PURE__ */ Symbol(0);
      EMPTY_PROPS = {};
      Instance = (_a2 = class {
        constructor(Component3, scope, init) {
          __privateAdd(this, _Instance_instances);
          __privateAdd(this, _setupCallbacks);
          __privateAdd(this, _attachCallbacks);
          __privateAdd(this, _connectCallbacks);
          __privateAdd(this, _destroyCallbacks);
          this[ON_DISPATCH] = null;
          this.$el = signal(null);
          this.el = null;
          this.scope = null;
          this.attachScope = null;
          this.connectScope = null;
          this.component = null;
          this.destroyed = false;
          this.props = EMPTY_PROPS;
          this.attrs = null;
          this.styles = null;
          __privateSet(this, _setupCallbacks, []);
          __privateSet(this, _attachCallbacks, []);
          __privateSet(this, _connectCallbacks, []);
          __privateSet(this, _destroyCallbacks, []);
          this.scope = scope;
          if (init?.scope) init.scope.append(scope);
          let stateFactory = Component3.state, props = Component3.props;
          if (stateFactory) {
            this.$state = stateFactory.create();
            this.state = new Proxy(this.$state, { get: (_, prop2) => this.$state[prop2]() });
            provideContext(stateFactory, this.$state);
          }
          if (props) {
            this.props = createInstanceProps(props);
            if (init?.props) for (const prop2 of Object.keys(init.props)) this.props[prop2]?.set(init.props[prop2]);
          }
          onDispose(this.destroy.bind(this));
        }
        setup() {
          scoped(() => {
            for (const callback of __privateGet(this, _setupCallbacks)) callback();
          }, this.scope);
        }
        attach(el) {
          if (this.el) return;
          this.el = el;
          this.$el.set(el);
          scoped(() => {
            this.attachScope = createScope();
            scoped(() => {
              for (const callback of __privateGet(this, _attachCallbacks)) callback(this.el);
              __privateMethod(this, _Instance_instances, attachAttrs_fn).call(this);
              __privateMethod(this, _Instance_instances, attachStyles_fn).call(this);
            }, this.attachScope);
          }, this.scope);
          el.dispatchEvent(new Event("attached"));
        }
        detach() {
          this.attachScope?.dispose();
          this.attachScope = null;
          this.connectScope = null;
          this.el = null;
          this.$el.set(null);
        }
        connect() {
          if (!this.el || !this.attachScope || !__privateGet(this, _connectCallbacks).length) return;
          scoped(() => {
            this.connectScope = createScope();
            scoped(() => {
              for (const callback of __privateGet(this, _connectCallbacks)) callback(this.el);
            }, this.connectScope);
          }, this.attachScope);
        }
        disconnect() {
          this.connectScope?.dispose();
          this.connectScope = null;
        }
        destroy() {
          if (this.destroyed) return;
          this.destroyed = true;
          scoped(() => {
            for (const callback of __privateGet(this, _destroyCallbacks)) callback(this.el);
          }, this.scope);
          const el = this.el;
          this.detach();
          this.scope.dispose();
          __privateGet(this, _setupCallbacks).length = 0;
          __privateGet(this, _attachCallbacks).length = 0;
          __privateGet(this, _connectCallbacks).length = 0;
          __privateGet(this, _destroyCallbacks).length = 0;
          this.component = null;
          this.attrs = null;
          this.styles = null;
          this.props = EMPTY_PROPS;
          this.scope = null;
          this.state = EMPTY_PROPS;
          this.$state = null;
          if (el) delete el.$;
        }
        addHooks(target) {
          if (target.onSetup) __privateGet(this, _setupCallbacks).push(target.onSetup.bind(target));
          if (target.onAttach) __privateGet(this, _attachCallbacks).push(target.onAttach.bind(target));
          if (target.onConnect) __privateGet(this, _connectCallbacks).push(target.onConnect.bind(target));
          if (target.onDestroy) __privateGet(this, _destroyCallbacks).push(target.onDestroy.bind(target));
        }
      }, _setupCallbacks = new WeakMap(), _attachCallbacks = new WeakMap(), _connectCallbacks = new WeakMap(), _destroyCallbacks = new WeakMap(), _Instance_instances = new WeakSet(), attachAttrs_fn = function() {
        if (!this.attrs) return;
        for (const name of Object.keys(this.attrs)) if (isFunction(this.attrs[name])) effect(__privateMethod(this, _Instance_instances, setAttr_fn).bind(this, name));
        else setAttribute(this.el, name, this.attrs[name]);
      }, attachStyles_fn = function() {
        if (!this.styles) return;
        for (const name of Object.keys(this.styles)) if (isFunction(this.styles[name])) effect(__privateMethod(this, _Instance_instances, setStyle_fn).bind(this, name));
        else setStyle(this.el, name, this.styles[name]);
      }, setAttr_fn = function(name) {
        setAttribute(this.el, name, this.attrs[name].call(this.component));
      }, setStyle_fn = function(name) {
        setStyle(this.el, name, this.styles[name].call(this.component));
      }, _a2);
      currentInstance = { $$: null };
      ViewController = class extends EventTarget {
        get el() {
          return this.$$.el;
        }
        get $el() {
          return this.$$.$el();
        }
        get scope() {
          return this.$$.scope;
        }
        get attachScope() {
          return this.$$.attachScope;
        }
        get connectScope() {
          return this.$$.connectScope;
        }
        /** @internal */
        get $props() {
          return this.$$.props;
        }
        /** @internal */
        get $state() {
          return this.$$.$state;
        }
        get state() {
          return this.$$.state;
        }
        constructor() {
          super();
          if (currentInstance.$$) this.attach(currentInstance);
        }
        attach({ $$ }) {
          this.$$ = $$;
          $$.addHooks(this);
          return this;
        }
        addEventListener(type, callback, options) {
          this.listen(type, callback, options);
        }
        removeEventListener(type, callback, options) {
          this.el?.removeEventListener(type, callback, options);
        }
        /**
        * This method can be used to specify attributes that should be set on the host element. Any
        * attributes that are assigned to a function will be considered a signal and updated accordingly.
        */
        setAttributes(attributes) {
          if (!this.$$.attrs) this.$$.attrs = {};
          Object.assign(this.$$.attrs, attributes);
        }
        /**
        * This method can be used to specify styles that should set be set on the host element. Any
        * styles that are assigned to a function will be considered a signal and updated accordingly.
        */
        setStyles(styles) {
          if (!this.$$.styles) this.$$.styles = {};
          Object.assign(this.$$.styles, styles);
        }
        /**
        * This method is used to satisfy the CSS variables contract specified on the current
        * component. Other CSS variables can be set via the `setStyles` method.
        */
        setCSSVars(vars) {
          this.setStyles(vars);
        }
        /**
        * Type-safe utility for creating component DOM events.
        */
        createEvent(type, ...init) {
          return new DOMEvent(type, init[0]);
        }
        /**
        * Creates a `DOMEvent` and dispatches it from the host element. This method is typed to
        * match all component events.
        */
        dispatch(type, ...init) {
          if (!this.el) return false;
          const event = type instanceof Event ? type : new DOMEvent(type, init[0]);
          Object.defineProperty(event, "target", { get: () => this.$$.component });
          return untrack(() => {
            this.$$[ON_DISPATCH]?.(event);
            return this.el.dispatchEvent(event);
          });
        }
        dispatchEvent(event) {
          return this.dispatch(event);
        }
        /**
        * Adds an event listener for the given `type` and returns a function which can be invoked to
        * remove the event listener.
        *
        * - The listener is removed if the current scope is disposed.
        * - This method is safe to use on the server (noop).
        */
        listen(type, handler, options) {
          if (!this.el) return noop;
          return listenEvent(this.el, type, handler, options);
        }
      };
      Component2 = class extends ViewController {
        subscribe(callback) {
          return scoped(() => effect(() => callback(this.state)), this.$$.scope);
        }
        destroy() {
          this.$$.destroy();
        }
      };
      State = (_a3 = class {
        constructor(record) {
          __privateAdd(this, _descriptors);
          this.id = /* @__PURE__ */ Symbol(0);
          this.record = record;
          __privateSet(this, _descriptors, Object.getOwnPropertyDescriptors(record));
        }
        create() {
          const store = {}, state = new Proxy(store, { get: (_, prop2) => store[prop2]() });
          for (const name of Object.keys(this.record)) {
            const getter = __privateGet(this, _descriptors)[name].get;
            store[name] = getter ? computed(getter.bind(state)) : signal(this.record[name]);
          }
          return store;
        }
        reset(record, filter) {
          for (const name of Object.keys(record)) if (!__privateGet(this, _descriptors)[name].get && (!filter || filter(name))) record[name].set(this.record[name]);
        }
      }, _descriptors = new WeakMap(), _a3);
      functionThrottle = throttle;
      functionDebounce = debounce;
      t = (t2) => "object" == typeof t2 && null != t2 && 1 === t2.nodeType;
      e = (t2, e2) => (!e2 || "hidden" !== t2) && "visible" !== t2 && "clip" !== t2;
      n = (t2, n2) => {
        if (t2.clientHeight < t2.scrollHeight || t2.clientWidth < t2.scrollWidth) {
          const o2 = getComputedStyle(t2, null);
          return e(o2.overflowY, n2) || e(o2.overflowX, n2) || ((t3) => {
            const e2 = ((t4) => {
              if (!t4.ownerDocument || !t4.ownerDocument.defaultView) return null;
              try {
                return t4.ownerDocument.defaultView.frameElement;
              } catch (t5) {
                return null;
              }
            })(t3);
            return !!e2 && (e2.clientHeight < t3.scrollHeight || e2.clientWidth < t3.scrollWidth);
          })(t2);
        }
        return false;
      };
      o = (t2, e2, n2, o2, l2, r2, i, s) => r2 < t2 && i > e2 || r2 > t2 && i < e2 ? 0 : r2 <= t2 && s <= n2 || i >= e2 && s >= n2 ? r2 - t2 - o2 : i > e2 && s < n2 || r2 < t2 && s > n2 ? i - e2 + l2 : 0;
      l = (t2) => {
        const e2 = t2.parentElement;
        return null == e2 ? t2.getRootNode().host || null : e2;
      };
      r = (e2, r2) => {
        var i, s, d, h;
        if ("undefined" == typeof document) return [];
        const { scrollMode: c, block: f, inline: u, boundary: a, skipOverflowHiddenElements: g } = r2, p = "function" == typeof a ? a : (t2) => t2 !== a;
        if (!t(e2)) throw new TypeError("Invalid target");
        const m = document.scrollingElement || document.documentElement, w = [];
        let W = e2;
        for (; t(W) && p(W); ) {
          if (W = l(W), W === m) {
            w.push(W);
            break;
          }
          null != W && W === document.body && n(W) && !n(document.documentElement) || null != W && n(W, g) && w.push(W);
        }
        const b = null != (s = null == (i = window.visualViewport) ? void 0 : i.width) ? s : innerWidth, H = null != (h = null == (d = window.visualViewport) ? void 0 : d.height) ? h : innerHeight, { scrollX: y, scrollY: M } = window, { height: v, width: E, top: x, right: C, bottom: I, left: R } = e2.getBoundingClientRect(), { top: T, right: B, bottom: F, left: V } = ((t2) => {
          const e3 = window.getComputedStyle(t2);
          return {
            top: parseFloat(e3.scrollMarginTop) || 0,
            right: parseFloat(e3.scrollMarginRight) || 0,
            bottom: parseFloat(e3.scrollMarginBottom) || 0,
            left: parseFloat(e3.scrollMarginLeft) || 0
          };
        })(e2);
        let k = "start" === f || "nearest" === f ? x - T : "end" === f ? I + F : x + v / 2 - T + F, D = "center" === u ? R + E / 2 - V + B : "end" === u ? C + B : R - V;
        const L = [];
        for (let t2 = 0; t2 < w.length; t2++) {
          const e3 = w[t2], { height: l2, width: r3, top: i2, right: s2, bottom: d2, left: h2 } = e3.getBoundingClientRect();
          if ("if-needed" === c && x >= 0 && R >= 0 && I <= H && C <= b && (e3 === m && !n(e3) || x >= i2 && I <= d2 && R >= h2 && C <= s2)) return L;
          const a2 = getComputedStyle(e3), g2 = parseInt(a2.borderLeftWidth, 10), p2 = parseInt(a2.borderTopWidth, 10), W2 = parseInt(a2.borderRightWidth, 10), T2 = parseInt(a2.borderBottomWidth, 10);
          let B2 = 0, F2 = 0;
          const V2 = "offsetWidth" in e3 ? e3.offsetWidth - e3.clientWidth - g2 - W2 : 0, S = "offsetHeight" in e3 ? e3.offsetHeight - e3.clientHeight - p2 - T2 : 0, X = "offsetWidth" in e3 ? 0 === e3.offsetWidth ? 0 : r3 / e3.offsetWidth : 0, Y = "offsetHeight" in e3 ? 0 === e3.offsetHeight ? 0 : l2 / e3.offsetHeight : 0;
          if (m === e3) B2 = "start" === f ? k : "end" === f ? k - H : "nearest" === f ? o(M, M + H, H, p2, T2, M + k, M + k + v, v) : k - H / 2, F2 = "start" === u ? D : "center" === u ? D - b / 2 : "end" === u ? D - b : o(y, y + b, b, g2, W2, y + D, y + D + E, E), B2 = Math.max(0, B2 + M), F2 = Math.max(0, F2 + y);
          else {
            B2 = "start" === f ? k - i2 - p2 : "end" === f ? k - d2 + T2 + S : "nearest" === f ? o(i2, d2, l2, p2, T2 + S, k, k + v, v) : k - (i2 + l2 / 2) + S / 2, F2 = "start" === u ? D - h2 - g2 : "center" === u ? D - (h2 + r3 / 2) + V2 / 2 : "end" === u ? D - s2 + W2 + V2 : o(h2, s2, r3, g2, W2 + V2, D, D + E, E);
            const { scrollLeft: t3, scrollTop: n2 } = e3;
            B2 = 0 === Y ? 0 : Math.max(0, Math.min(n2 + B2 / Y, e3.scrollHeight - l2 / Y + S)), F2 = 0 === X ? 0 : Math.max(0, Math.min(t3 + F2 / X, e3.scrollWidth - r3 / X + V2)), k += n2 - B2, D += t3 - F2;
          }
          L.push({
            el: e3,
            top: B2,
            left: F2
          });
        }
        return L;
      };
      ReactScopeContext = React4.createContext({ current: null });
      ReactScopeContext.displayName = "Scope";
      _a4 = class extends React4.Component {
        constructor(props, context) {
          var _a106;
          super(props);
          __privateAdd(this, _scope);
          __privateSet(this, _scope, { current: createScope() });
          if (context) context.append(__privateGet(this, _scope).current);
          const Ctor = this.constructor;
          if (__privateGet(Ctor, _context)) provideContext(__privateGet(Ctor, _context), (_a106 = __privateGet(Ctor, _provide)) == null ? void 0 : _a106.call(Ctor), __privateGet(this, _scope).current);
        }
        componentWillUnmount() {
          __privateGet(this, _scope).current?.dispose();
          __privateGet(this, _scope).current = null;
        }
        render() {
          return WithScope(__privateGet(this, _scope), this.props?.children);
        }
      }, _context = new WeakMap(), _provide = new WeakMap(), _scope = new WeakMap(), _a4.contextType = ReactScopeContext, __privateAdd(_a4, _context), __privateAdd(_a4, _provide), _a4;
      eventTypeToCallbackName = /* @__PURE__ */ new Map();
      Icon$0 = `<path fill-rule="evenodd" clip-rule="evenodd" d="M15.0007 28.7923C15.0007 29.0152 14.9774 29.096 14.9339 29.1775C14.8903 29.259 14.8263 29.323 14.7449 29.3665C14.6634 29.4101 14.5826 29.4333 14.3597 29.4333H12.575C12.3521 29.4333 12.2713 29.4101 12.1898 29.3665C12.1083 29.323 12.0443 29.259 12.0008 29.1775C11.9572 29.096 11.934 29.0152 11.934 28.7923V12.2993L5.97496 12.3C5.75208 12.3 5.67125 12.2768 5.58977 12.2332C5.50829 12.1896 5.44434 12.1257 5.40077 12.0442C5.35719 11.9627 5.33398 11.8819 5.33398 11.659V9.87429C5.33398 9.65141 5.35719 9.57059 5.40077 9.48911C5.44434 9.40762 5.50829 9.34368 5.58977 9.3001C5.67125 9.25652 5.75208 9.23332 5.97496 9.23332H26.0263C26.2492 9.23332 26.33 9.25652 26.4115 9.3001C26.493 9.34368 26.557 9.40762 26.6005 9.48911C26.6441 9.57059 26.6673 9.65141 26.6673 9.87429V11.659C26.6673 11.8819 26.6441 11.9627 26.6005 12.0442C26.557 12.1257 26.493 12.1896 26.4115 12.2332C26.33 12.2768 26.2492 12.3 26.0263 12.3L20.067 12.2993L20.0673 28.7923C20.0673 29.0152 20.0441 29.096 20.0005 29.1775C19.957 29.259 19.893 29.323 19.8115 29.3665C19.73 29.4101 19.6492 29.4333 19.4263 29.4333H17.6416C17.4187 29.4333 17.3379 29.4101 17.2564 29.3665C17.175 29.323 17.111 29.259 17.0674 29.1775C17.0239 29.096 17.0007 29.0152 17.0007 28.7923L17 22.7663H15L15.0007 28.7923Z" fill="currentColor"/> <path d="M16.0007 7.89998C17.4734 7.89998 18.6673 6.70608 18.6673 5.23332C18.6673 3.76056 17.4734 2.56665 16.0007 2.56665C14.5279 2.56665 13.334 3.76056 13.334 5.23332C13.334 6.70608 14.5279 7.89998 16.0007 7.89998Z" fill="currentColor"/>`;
      Icon$5 = `<path d="M5.33334 6.00001C5.33334 5.63182 5.63181 5.33334 6 5.33334H26C26.3682 5.33334 26.6667 5.63182 26.6667 6.00001V20.6667C26.6667 21.0349 26.3682 21.3333 26 21.3333H23.7072C23.4956 21.3333 23.2966 21.233 23.171 21.0628L22.1859 19.7295C21.8607 19.2894 22.1749 18.6667 22.7221 18.6667H23.3333C23.7015 18.6667 24 18.3682 24 18V8.66668C24 8.29849 23.7015 8.00001 23.3333 8.00001H8.66667C8.29848 8.00001 8 8.29849 8 8.66668V18C8 18.3682 8.29848 18.6667 8.66667 18.6667H9.29357C9.84072 18.6667 10.1549 19.2894 9.82976 19.7295L8.84467 21.0628C8.71898 21.233 8.52 21.3333 8.30848 21.3333H6C5.63181 21.3333 5.33334 21.0349 5.33334 20.6667V6.00001Z" fill="currentColor"/> <path d="M8.78528 25.6038C8.46013 26.0439 8.77431 26.6667 9.32147 26.6667L22.6785 26.6667C23.2256 26.6667 23.5398 26.0439 23.2146 25.6038L16.5358 16.5653C16.2693 16.2046 15.73 16.2047 15.4635 16.5653L8.78528 25.6038Z" fill="currentColor"/>`;
      Icon$8 = `<path d="M17.4853 18.9093C17.4853 19.0281 17.6289 19.0875 17.7129 19.0035L22.4185 14.2979C22.6788 14.0376 23.1009 14.0376 23.3613 14.2979L24.7755 15.7122C25.0359 15.9725 25.0359 16.3946 24.7755 16.655L16.2902 25.1403C16.0299 25.4006 15.6078 25.4006 15.3474 25.1403L13.9332 23.726L13.9319 23.7247L6.86189 16.6547C6.60154 16.3944 6.60154 15.9723 6.86189 15.7119L8.2761 14.2977C8.53645 14.0373 8.95856 14.0373 9.21891 14.2977L13.9243 19.0031C14.0083 19.0871 14.1519 19.0276 14.1519 18.9088L14.1519 6.00004C14.1519 5.63185 14.4504 5.33337 14.8186 5.33337L16.8186 5.33337C17.1868 5.33337 17.4853 5.63185 17.4853 6.00004L17.4853 18.9093Z" fill="currentColor"/>`;
      Icon$11 = `<path d="M13.0908 14.3334C12.972 14.3334 12.9125 14.1898 12.9965 14.1058L17.7021 9.40022C17.9625 9.13987 17.9625 8.71776 17.7021 8.45741L16.2879 7.04319C16.0275 6.78284 15.6054 6.78284 15.3451 7.04319L6.8598 15.5285C6.59945 15.7888 6.59945 16.2109 6.8598 16.4713L8.27401 17.8855L8.27536 17.8868L15.3453 24.9568C15.6057 25.2172 16.0278 25.2172 16.2881 24.9568L17.7024 23.5426C17.9627 23.2822 17.9627 22.8601 17.7024 22.5998L12.9969 17.8944C12.9129 17.8104 12.9724 17.6668 13.0912 17.6668L26 17.6668C26.3682 17.6668 26.6667 17.3683 26.6667 17.0001V15.0001C26.6667 14.6319 26.3682 14.3334 26 14.3334L13.0908 14.3334Z" fill="currentColor"/>`;
      Icon$13 = `<path d="M14.1521 13.0929C14.1521 12.9741 14.0085 12.9147 13.9245 12.9987L9.21891 17.7043C8.95856 17.9646 8.53645 17.9646 8.2761 17.7043L6.86189 16.29C6.60154 16.0297 6.60154 15.6076 6.86189 15.3472L15.3472 6.86195C15.6075 6.6016 16.0296 6.6016 16.29 6.86195L17.7042 8.27616L17.7055 8.27751L24.7755 15.3475C25.0359 15.6078 25.0359 16.0299 24.7755 16.2903L23.3613 17.7045C23.1009 17.9649 22.6788 17.9649 22.4185 17.7045L17.7131 12.9991C17.6291 12.9151 17.4855 12.9746 17.4855 13.0934V26.0022C17.4855 26.3704 17.187 26.6688 16.8188 26.6688H14.8188C14.4506 26.6688 14.1521 26.3704 14.1521 26.0022L14.1521 13.0929Z" fill="currentColor"/>`;
      Icon$16 = `<path d="M16.6927 25.3346C16.3245 25.3346 16.026 25.0361 16.026 24.6679L16.026 7.3346C16.026 6.96641 16.3245 6.66794 16.6927 6.66794L18.6927 6.66794C19.0609 6.66794 19.3594 6.96642 19.3594 7.3346L19.3594 24.6679C19.3594 25.0361 19.0609 25.3346 18.6927 25.3346H16.6927Z" fill="currentColor"/> <path d="M24.026 25.3346C23.6578 25.3346 23.3594 25.0361 23.3594 24.6679L23.3594 7.3346C23.3594 6.96641 23.6578 6.66794 24.026 6.66794L26.026 6.66794C26.3942 6.66794 26.6927 6.96642 26.6927 7.3346V24.6679C26.6927 25.0361 26.3942 25.3346 26.026 25.3346H24.026Z" fill="currentColor"/> <path d="M5.48113 23.9407C5.38584 24.2963 5.59689 24.6619 5.95254 24.7572L7.88439 25.2748C8.24003 25.3701 8.60559 25.159 8.70089 24.8034L13.1871 8.06067C13.2824 7.70503 13.0713 7.33947 12.7157 7.24417L10.7838 6.72654C10.4282 6.63124 10.0626 6.8423 9.96733 7.19794L5.48113 23.9407Z" fill="currentColor"/>`;
      Icon$19 = `<path fill-rule="evenodd" clip-rule="evenodd" d="M24.9266 7.57992C25.015 7.60672 25.0886 7.64746 25.2462 7.80506L26.956 9.51488C27.1136 9.67248 27.1543 9.74604 27.1811 9.83447C27.2079 9.9229 27.2079 10.0133 27.1811 10.1018C27.1543 10.1902 27.1136 10.2638 26.956 10.4214L13.1822 24.1951C13.0246 24.3527 12.951 24.3935 12.8626 24.4203C12.797 24.4402 12.7304 24.4453 12.6642 24.4357L12.7319 24.4203C12.6435 24.4471 12.553 24.4471 12.4646 24.4203C12.3762 24.3935 12.3026 24.3527 12.145 24.1951L5.04407 17.0942C4.88647 16.9366 4.84573 16.863 4.81893 16.7746C4.79213 16.6862 4.79213 16.5957 4.81893 16.5073C4.84573 16.4189 4.88647 16.3453 5.04407 16.1877L6.7539 14.4779C6.9115 14.3203 6.98506 14.2796 7.07349 14.2528C7.16191 14.226 7.25235 14.226 7.34078 14.2528C7.42921 14.2796 7.50277 14.3203 7.66037 14.4779L12.6628 19.4808L24.3397 7.80506C24.4973 7.64746 24.5709 7.60672 24.6593 7.57992C24.7477 7.55311 24.8382 7.55311 24.9266 7.57992Z" fill="currentColor"/>`;
      Icon$22 = `<path d="M17.947 16.095C17.999 16.043 17.999 15.9585 17.947 15.9065L11.6295 9.58899C11.3691 9.32864 11.3691 8.90653 11.6295 8.64618L13.2323 7.04341C13.4926 6.78306 13.9147 6.78306 14.1751 7.04341L21.0289 13.8973C21.0392 13.9064 21.0493 13.9158 21.0591 13.9257L22.6619 15.5285C22.9223 15.7888 22.9223 16.2109 22.6619 16.4713L14.1766 24.9565C13.9163 25.2169 13.4942 25.2169 13.2338 24.9565L11.631 23.3538C11.3707 23.0934 11.3707 22.6713 11.631 22.411L17.947 16.095Z" fill="currentColor"/>`;
      chromecast_exports = /* @__PURE__ */ __exportAll({ default: () => Icon$24 });
      Icon$24 = `<path fill-rule="evenodd" clip-rule="evenodd" d="M6 7C5.63181 7 5.33333 7.29848 5.33333 7.66667V14.8667C5.33333 14.9403 5.39361 14.9999 5.46724 15.0009C10.8844 15.0719 15.2614 19.449 15.3325 24.8661C15.3334 24.9397 15.393 25 15.4667 25H26C26.3682 25 26.6667 24.7015 26.6667 24.3333V7.66667C26.6667 7.29848 26.3682 7 26 7H6ZM17.0119 22.2294C17.0263 22.29 17.0802 22.3333 17.1425 22.3333H23.3333C23.7015 22.3333 24 22.0349 24 21.6667V10.3333C24 9.96514 23.7015 9.66667 23.3333 9.66667H8.66667C8.29848 9.66667 8 9.96514 8 10.3333V13.1909C8 13.2531 8.04332 13.3071 8.10392 13.3214C12.5063 14.3618 15.9715 17.827 17.0119 22.2294Z" fill="currentColor"/> <path d="M13.2 25C13.2736 25 13.3334 24.9398 13.3322 24.8661C13.2615 20.5544 9.77889 17.0718 5.46718 17.0011C5.39356 16.9999 5.33333 17.0597 5.33333 17.1333V18.8667C5.33333 18.9403 5.39348 18.9999 5.4671 19.0015C8.67465 19.0716 11.2617 21.6587 11.3319 24.8662C11.3335 24.9399 11.393 25 11.4667 25H13.2Z" fill="currentColor"/> <path d="M5.33333 21.1333C5.33333 21.0597 5.39332 20.9998 5.46692 21.0022C7.57033 21.0712 9.26217 22.763 9.33114 24.8664C9.33356 24.94 9.27364 25 9.2 25H6C5.63181 25 5.33333 24.7015 5.33333 24.3333V21.1333Z" fill="currentColor"/>`;
      Icon$26 = `<path d="M8 28.0003C8 27.6321 8.29848 27.3336 8.66667 27.3336H23.3333C23.7015 27.3336 24 27.6321 24 28.0003V29.3336C24 29.7018 23.7015 30.0003 23.3333 30.0003H8.66667C8.29848 30.0003 8 29.7018 8 29.3336V28.0003Z" fill="currentColor"/> <path fill-rule="evenodd" clip-rule="evenodd" d="M4.66602 6.66699C4.29783 6.66699 3.99935 6.96547 3.99935 7.33366V24.667C3.99935 25.0352 4.29783 25.3337 4.66602 25.3337H27.3327C27.7009 25.3337 27.9994 25.0352 27.9994 24.667V7.33366C27.9994 6.96547 27.7009 6.66699 27.3327 6.66699H4.66602ZM8.66659 21.3333C8.2984 21.3333 7.99992 21.0349 7.99992 20.6667V11.3333C7.99992 10.9651 8.2984 10.6667 8.66659 10.6667H13.9999C14.3681 10.6667 14.6666 10.9651 14.6666 11.3333V12.6667C14.6666 13.0349 14.3681 13.3333 13.9999 13.3333H10.7999C10.7263 13.3333 10.6666 13.393 10.6666 13.4667V18.5333C10.6666 18.607 10.7263 18.6667 10.7999 18.6667H13.9999C14.3681 18.6667 14.6666 18.9651 14.6666 19.3333V20.6667C14.6666 21.0349 14.3681 21.3333 13.9999 21.3333H8.66659ZM17.9999 21.3333C17.6317 21.3333 17.3333 21.0349 17.3333 20.6667V11.3333C17.3333 10.9651 17.6317 10.6667 17.9999 10.6667H23.3333C23.7014 10.6667 23.9999 10.9651 23.9999 11.3333V12.6667C23.9999 13.0349 23.7014 13.3333 23.3333 13.3333H20.1333C20.0596 13.3333 19.9999 13.393 19.9999 13.4667V18.5333C19.9999 18.607 20.0596 18.6667 20.1333 18.6667H23.3333C23.7014 18.6667 23.9999 18.9651 23.9999 19.3333V20.6667C23.9999 21.0349 23.7014 21.3333 23.3333 21.3333H17.9999Z" fill="currentColor"/>`;
      Icon$27 = `<path fill-rule="evenodd" clip-rule="evenodd" d="M4.6661 6.66699C4.29791 6.66699 3.99943 6.96547 3.99943 7.33366V24.667C3.99943 25.0352 4.29791 25.3337 4.6661 25.3337H27.3328C27.701 25.3337 27.9994 25.0352 27.9994 24.667V7.33366C27.9994 6.96547 27.701 6.66699 27.3328 6.66699H4.6661ZM8.66667 21.3333C8.29848 21.3333 8 21.0349 8 20.6667V11.3333C8 10.9651 8.29848 10.6667 8.66667 10.6667H14C14.3682 10.6667 14.6667 10.9651 14.6667 11.3333V12.6667C14.6667 13.0349 14.3682 13.3333 14 13.3333H10.8C10.7264 13.3333 10.6667 13.393 10.6667 13.4667V18.5333C10.6667 18.607 10.7264 18.6667 10.8 18.6667H14C14.3682 18.6667 14.6667 18.9651 14.6667 19.3333V20.6667C14.6667 21.0349 14.3682 21.3333 14 21.3333H8.66667ZM18 21.3333C17.6318 21.3333 17.3333 21.0349 17.3333 20.6667V11.3333C17.3333 10.9651 17.6318 10.6667 18 10.6667H23.3333C23.7015 10.6667 24 10.9651 24 11.3333V12.6667C24 13.0349 23.7015 13.3333 23.3333 13.3333H20.1333C20.0597 13.3333 20 13.393 20 13.4667V18.5333C20 18.607 20.0597 18.6667 20.1333 18.6667H23.3333C23.7015 18.6667 24 18.9651 24 19.3333V20.6667C24 21.0349 23.7015 21.3333 23.3333 21.3333H18Z" fill="currentColor"/>`;
      Icon$31 = `<path d="M14.2225 13.7867C14.3065 13.8706 14.4501 13.8112 14.4501 13.6924V5.99955C14.4501 5.63136 14.7486 5.33289 15.1167 5.33289H16.8501C17.2183 5.33289 17.5167 5.63136 17.5167 5.99955V13.6916C17.5167 13.8104 17.6604 13.8699 17.7444 13.7859L19.9433 11.5869C20.2037 11.3266 20.6258 11.3266 20.8861 11.5869L22.1118 12.8126C22.3722 13.0729 22.3722 13.4951 22.1118 13.7554L16.4549 19.4123C16.1946 19.6726 15.772 19.6731 15.5116 19.4128L9.85479 13.7559C9.59444 13.4956 9.59444 13.0734 9.85479 12.8131L11.0804 11.5874C11.3408 11.3271 11.7629 11.3271 12.0233 11.5874L14.2225 13.7867Z" fill="currentColor"/> <path d="M5.99998 20.267C5.63179 20.267 5.33331 20.5654 5.33331 20.9336V25.9997C5.33331 26.3678 5.63179 26.6663 5.99998 26.6663H26C26.3682 26.6663 26.6666 26.3678 26.6666 25.9997V20.9336C26.6666 20.5654 26.3682 20.267 26 20.267H24.2666C23.8985 20.267 23.6 20.5654 23.6 20.9336V22.9333C23.6 23.3014 23.3015 23.5999 22.9333 23.5999H9.06638C8.69819 23.5999 8.39972 23.3014 8.39972 22.9333V20.9336C8.39972 20.5654 8.10124 20.267 7.73305 20.267H5.99998Z" fill="currentColor"/>`;
      Icon$33 = `<path d="M16 20C18.2091 20 20 18.2092 20 16C20 13.7909 18.2091 12 16 12C13.7909 12 12 13.7909 12 16C12 18.2092 13.7909 20 16 20Z" fill="currentColor"/> <path fill-rule="evenodd" clip-rule="evenodd" d="M28 16.0058C28 18.671 23.5 25.3334 16 25.3334C8.5 25.3334 4 18.6762 4 16.0058C4 13.3354 8.50447 6.66669 16 6.66669C23.4955 6.66669 28 13.3406 28 16.0058ZM25.3318 15.9934C25.3328 16.0017 25.3328 16.0099 25.3318 16.0182C25.3274 16.0571 25.3108 16.1728 25.2485 16.3708C25.1691 16.6229 25.0352 16.9462 24.8327 17.3216C24.4264 18.0749 23.7969 18.9398 22.9567 19.754C21.2791 21.3798 18.9148 22.6667 16 22.6667C13.0845 22.6667 10.7202 21.3805 9.04298 19.7557C8.20295 18.9419 7.57362 18.0773 7.16745 17.3241C6.96499 16.9486 6.83114 16.6252 6.75172 16.3729C6.67942 16.1431 6.66856 16.0243 6.66695 16.0066L6.66695 16.005C6.66859 15.9871 6.67951 15.8682 6.75188 15.6383C6.83145 15.3854 6.96554 15.0614 7.16831 14.6853C7.57507 13.9306 8.20514 13.0644 9.04577 12.249C10.7245 10.6208 13.0886 9.33335 16 9.33335C18.9108 9.33335 21.2748 10.6215 22.9539 12.2507C23.7947 13.0664 24.4249 13.933 24.8318 14.6877C25.0346 15.0639 25.1688 15.3878 25.2483 15.6404C25.3107 15.8386 25.3274 15.9545 25.3318 15.9934Z" fill="currentColor"/>`;
      Icon$34 = `<path d="M15.8747 8.11857C16.3148 7.79342 16.9375 8.10759 16.9375 8.65476V14.2575C16.9375 14.3669 17.0621 14.4297 17.1501 14.3647L25.6038 8.11857C26.0439 7.79342 26.6667 8.10759 26.6667 8.65476V23.3451C26.6667 23.8923 26.0439 24.2064 25.6038 23.8813L17.1501 17.6346C17.0621 17.5695 16.9375 17.6324 16.9375 17.7418L16.9375 23.3451C16.9375 23.8923 16.3147 24.2064 15.8747 23.8813L5.93387 16.5358C5.57322 16.2693 5.57323 15.7299 5.93389 15.4634L15.8747 8.11857Z" fill="currentColor"/>`;
      Icon$35 = `<path d="M16.1253 8.11866C15.6852 7.7935 15.0625 8.10768 15.0625 8.65484V14.2576C15.0625 14.367 14.9379 14.4298 14.8499 14.3648L6.39615 8.11866C5.95607 7.7935 5.33331 8.10768 5.33331 8.65484V23.3452C5.33331 23.8923 5.9561 24.2065 6.39617 23.8813L14.8499 17.6347C14.9379 17.5696 15.0625 17.6325 15.0625 17.7419L15.0625 23.3452C15.0625 23.8923 15.6853 24.2065 16.1253 23.8813L26.0661 16.5358C26.4268 16.2694 26.4268 15.73 26.0661 15.4635L16.1253 8.11866Z" fill="currentColor"/>`;
      Icon$39 = `<path d="M19.3334 13.3333C18.9652 13.3333 18.6667 13.0349 18.6667 12.6667L18.6667 7.33333C18.6667 6.96514 18.9652 6.66666 19.3334 6.66666H21.3334C21.7015 6.66666 22 6.96514 22 7.33333V9.86666C22 9.9403 22.0597 10 22.1334 10L24.6667 10C25.0349 10 25.3334 10.2985 25.3334 10.6667V12.6667C25.3334 13.0349 25.0349 13.3333 24.6667 13.3333L19.3334 13.3333Z" fill="currentColor"/> <path d="M13.3334 19.3333C13.3334 18.9651 13.0349 18.6667 12.6667 18.6667H7.33335C6.96516 18.6667 6.66669 18.9651 6.66669 19.3333V21.3333C6.66669 21.7015 6.96516 22 7.33335 22H9.86669C9.94032 22 10 22.0597 10 22.1333L10 24.6667C10 25.0349 10.2985 25.3333 10.6667 25.3333H12.6667C13.0349 25.3333 13.3334 25.0349 13.3334 24.6667L13.3334 19.3333Z" fill="currentColor"/> <path d="M18.6667 24.6667C18.6667 25.0349 18.9652 25.3333 19.3334 25.3333H21.3334C21.7015 25.3333 22 25.0349 22 24.6667V22.1333C22 22.0597 22.0597 22 22.1334 22H24.6667C25.0349 22 25.3334 21.7015 25.3334 21.3333V19.3333C25.3334 18.9651 25.0349 18.6667 24.6667 18.6667L19.3334 18.6667C18.9652 18.6667 18.6667 18.9651 18.6667 19.3333L18.6667 24.6667Z" fill="currentColor"/> <path d="M10.6667 13.3333H12.6667C13.0349 13.3333 13.3334 13.0349 13.3334 12.6667L13.3334 10.6667V7.33333C13.3334 6.96514 13.0349 6.66666 12.6667 6.66666H10.6667C10.2985 6.66666 10 6.96514 10 7.33333L10 9.86666C10 9.9403 9.94033 10 9.86669 10L7.33335 10C6.96516 10 6.66669 10.2985 6.66669 10.6667V12.6667C6.66669 13.0349 6.96516 13.3333 7.33335 13.3333L10.6667 13.3333Z" fill="currentColor"/>`;
      Icon$40 = `<path d="M25.3299 7.26517C25.2958 6.929 25.0119 6.66666 24.6667 6.66666H19.3334C18.9652 6.66666 18.6667 6.96514 18.6667 7.33333V9.33333C18.6667 9.70152 18.9652 10 19.3334 10L21.8667 10C21.9403 10 22 10.0597 22 10.1333V12.6667C22 13.0349 22.2985 13.3333 22.6667 13.3333H24.6667C25.0349 13.3333 25.3334 13.0349 25.3334 12.6667V7.33333C25.3334 7.31032 25.3322 7.28758 25.3299 7.26517Z" fill="currentColor"/> <path d="M22 21.8667C22 21.9403 21.9403 22 21.8667 22L19.3334 22C18.9652 22 18.6667 22.2985 18.6667 22.6667V24.6667C18.6667 25.0349 18.9652 25.3333 19.3334 25.3333L24.6667 25.3333C25.0349 25.3333 25.3334 25.0349 25.3334 24.6667V19.3333C25.3334 18.9651 25.0349 18.6667 24.6667 18.6667H22.6667C22.2985 18.6667 22 18.9651 22 19.3333V21.8667Z" fill="currentColor"/> <path d="M12.6667 22H10.1334C10.0597 22 10 21.9403 10 21.8667V19.3333C10 18.9651 9.70154 18.6667 9.33335 18.6667H7.33335C6.96516 18.6667 6.66669 18.9651 6.66669 19.3333V24.6667C6.66669 25.0349 6.96516 25.3333 7.33335 25.3333H12.6667C13.0349 25.3333 13.3334 25.0349 13.3334 24.6667V22.6667C13.3334 22.2985 13.0349 22 12.6667 22Z" fill="currentColor"/> <path d="M10 12.6667V10.1333C10 10.0597 10.0597 10 10.1334 10L12.6667 10C13.0349 10 13.3334 9.70152 13.3334 9.33333V7.33333C13.3334 6.96514 13.0349 6.66666 12.6667 6.66666H7.33335C6.96516 6.66666 6.66669 6.96514 6.66669 7.33333V12.6667C6.66669 13.0349 6.96516 13.3333 7.33335 13.3333H9.33335C9.70154 13.3333 10 13.0349 10 12.6667Z" fill="currentColor"/>`;
      Icon$53 = `<path fill-rule="evenodd" clip-rule="evenodd" d="M26.6667 5.99998C26.6667 5.63179 26.3682 5.33331 26 5.33331H11.3333C10.9651 5.33331 10.6667 5.63179 10.6667 5.99998V17.5714C10.6667 17.6694 10.5644 17.7342 10.4741 17.6962C9.91823 17.4625 9.30754 17.3333 8.66667 17.3333C6.08934 17.3333 4 19.4226 4 22C4 24.5773 6.08934 26.6666 8.66667 26.6666C11.244 26.6666 13.3333 24.5773 13.3333 22V8.66665C13.3333 8.29846 13.6318 7.99998 14 7.99998L23.3333 7.99998C23.7015 7.99998 24 8.29846 24 8.66665V14.9048C24 15.0027 23.8978 15.0675 23.8075 15.0296C23.2516 14.7958 22.6409 14.6666 22 14.6666C19.4227 14.6666 17.3333 16.756 17.3333 19.3333C17.3333 21.9106 19.4227 24 22 24C24.5773 24 26.6667 21.9106 26.6667 19.3333V5.99998ZM22 21.3333C23.1046 21.3333 24 20.4379 24 19.3333C24 18.2287 23.1046 17.3333 22 17.3333C20.8954 17.3333 20 18.2287 20 19.3333C20 20.4379 20.8954 21.3333 22 21.3333ZM8.66667 24C9.77124 24 10.6667 23.1045 10.6667 22C10.6667 20.8954 9.77124 20 8.66667 20C7.5621 20 6.66667 20.8954 6.66667 22C6.66667 23.1045 7.5621 24 8.66667 24Z" fill="currentColor"/>`;
      Icon$54 = `<path d="M17.5091 24.6594C17.5091 25.2066 16.8864 25.5208 16.4463 25.1956L9.44847 20.0252C9.42553 20.0083 9.39776 19.9991 9.36923 19.9991H4.66667C4.29848 19.9991 4 19.7006 4 19.3325V12.6658C4 12.2976 4.29848 11.9991 4.66667 11.9991H9.37115C9.39967 11.9991 9.42745 11.99 9.45039 11.973L16.4463 6.8036C16.8863 6.47842 17.5091 6.79259 17.5091 7.33977L17.5091 24.6594Z" fill="currentColor"/> <path d="M28.8621 13.6422C29.1225 13.3818 29.1225 12.9597 28.8621 12.6994L27.9193 11.7566C27.659 11.4962 27.2368 11.4962 26.9765 11.7566L24.7134 14.0197C24.6613 14.0717 24.5769 14.0717 24.5248 14.0197L22.262 11.7568C22.0016 11.4964 21.5795 11.4964 21.3191 11.7568L20.3763 12.6996C20.116 12.9599 20.116 13.382 20.3763 13.6424L22.6392 15.9053C22.6913 15.9573 22.6913 16.0418 22.6392 16.0938L20.3768 18.3562C20.1165 18.6166 20.1165 19.0387 20.3768 19.299L21.3196 20.2419C21.58 20.5022 22.0021 20.5022 22.2624 20.2418L24.5248 17.9795C24.5769 17.9274 24.6613 17.9274 24.7134 17.9795L26.976 20.2421C27.2363 20.5024 27.6585 20.5024 27.9188 20.2421L28.8616 19.2992C29.122 19.0389 29.122 18.6168 28.8616 18.3564L26.599 16.0938C26.547 16.0418 26.547 15.9573 26.599 15.9053L28.8621 13.6422Z" fill="currentColor"/>`;
      Icon$56 = `<path d="M26.6009 16.0725C26.6009 16.424 26.4302 17.1125 25.9409 18.0213C25.4676 18.8976 24.7542 19.8715 23.8182 20.7783C21.9489 22.5905 19.2662 24.0667 15.9342 24.0667C12.6009 24.0667 9.91958 22.5915 8.04891 20.78C7.11424 19.8736 6.40091 18.9 5.92758 18.0236C5.43824 17.1149 5.26758 16.4257 5.26758 16.0725C5.26758 15.7193 5.43824 15.0293 5.92891 14.1193C6.40224 13.2416 7.11558 12.2665 8.05158 11.3587C9.92224 9.54398 12.6049 8.06665 15.9342 8.06665C19.2636 8.06665 21.9449 9.54505 23.8169 11.3604C24.7529 12.2687 25.4662 13.2441 25.9396 14.1216C26.4302 15.0317 26.6009 15.7209 26.6009 16.0725Z" stroke="currentColor" stroke-width="3"/> <path d="M15.9336 20.0667C18.1427 20.0667 19.9336 18.2758 19.9336 16.0667C19.9336 13.8575 18.1427 12.0667 15.9336 12.0667C13.7245 12.0667 11.9336 13.8575 11.9336 16.0667C11.9336 18.2758 13.7245 20.0667 15.9336 20.0667Z" fill="currentColor"/> <path fill-rule="evenodd" clip-rule="evenodd" d="M27.2323 25.0624L6.93878 4.76886C6.78118 4.61126 6.70762 4.57052 6.61919 4.54372C6.53077 4.51692 6.44033 4.51691 6.3519 4.54372C6.26347 4.57052 6.18991 4.61126 6.03231 4.76886L4.77032 6.03085C4.61272 6.18845 4.57198 6.26201 4.54518 6.35044C4.51838 6.43887 4.51838 6.5293 4.54518 6.61773C4.57198 6.70616 4.61272 6.77972 4.77032 6.93732L25.0639 27.2308C25.2215 27.3884 25.295 27.4292 25.3834 27.456C25.4719 27.4828 25.5623 27.4828 25.6507 27.456C25.7392 27.4292 25.8127 27.3885 25.9703 27.2309L27.2323 25.9689C27.3899 25.8113 27.4307 25.7377 27.4575 25.6493C27.4843 25.5608 27.4843 25.4704 27.4575 25.382C27.4307 25.2935 27.3899 25.22 27.2323 25.0624Z" fill="currentColor"/>`;
      Icon$58 = `<path d="M25.14 25.1089C25.0171 25.2532 24.8356 25.3333 24.646 25.3333H22.8124C22.1084 25.3333 21.7734 24.1872 22.2745 23.6927C23.9161 22.0729 24.9336 19.822 24.9336 17.3333C24.9336 12.3997 20.9336 8.39973 16 8.39973C11.0664 8.39973 7.06641 12.3997 7.06641 17.3333C7.06641 19.822 8.08389 22.0729 9.72555 23.6927C10.2266 24.1872 9.89155 25.3333 9.18762 25.3333H7.35398C7.16436 25.3333 6.98294 25.2532 6.86001 25.1089C5.07703 23.015 4 20.2991 4 17.3333C4 10.7057 9.3724 5.33333 16 5.33333C22.6276 5.33333 28 10.7057 28 17.3333C28 20.2991 26.923 23.015 25.14 25.1089Z" fill="currentColor"/> <path d="M21.1992 14.3399C21.4595 14.0796 21.4595 13.6575 21.1992 13.3971L20.2564 12.4543C19.996 12.194 19.5739 12.194 19.3136 12.4543L16.4492 15.3187C16.4185 15.3493 16.3749 15.3629 16.332 15.3568C16.2236 15.3414 16.1127 15.3334 16 15.3334C14.7113 15.3334 13.6667 16.378 13.6667 17.6667C13.6667 18.9554 14.7113 20 16 20C17.2887 20 18.3333 18.9554 18.3333 17.6667C18.3333 17.5464 18.3242 17.4283 18.3067 17.313C18.3001 17.2696 18.3136 17.2255 18.3446 17.1945L21.1992 14.3399Z" fill="currentColor"/>`;
      Icon$59 = `<path d="M8.66667 6.66667C8.29848 6.66667 8 6.96514 8 7.33333V24.6667C8 25.0349 8.29848 25.3333 8.66667 25.3333H12.6667C13.0349 25.3333 13.3333 25.0349 13.3333 24.6667V7.33333C13.3333 6.96514 13.0349 6.66667 12.6667 6.66667H8.66667Z" fill="currentColor"/> <path d="M19.3333 6.66667C18.9651 6.66667 18.6667 6.96514 18.6667 7.33333V24.6667C18.6667 25.0349 18.9651 25.3333 19.3333 25.3333H23.3333C23.7015 25.3333 24 25.0349 24 24.6667V7.33333C24 6.96514 23.7015 6.66667 23.3333 6.66667H19.3333Z" fill="currentColor"/>`;
      Icon$60 = `<path d="M5.33334 26V19.4667C5.33334 19.393 5.39304 19.3333 5.46668 19.3333H7.86668C7.94031 19.3333 8.00001 19.393 8.00001 19.4667V23.3333C8.00001 23.7015 8.29849 24 8.66668 24H23.3333C23.7015 24 24 23.7015 24 23.3333V8.66666C24 8.29847 23.7015 7.99999 23.3333 7.99999H19.4667C19.393 7.99999 19.3333 7.9403 19.3333 7.86666V5.46666C19.3333 5.39302 19.393 5.33333 19.4667 5.33333H26C26.3682 5.33333 26.6667 5.63181 26.6667 5.99999V26C26.6667 26.3682 26.3682 26.6667 26 26.6667H6.00001C5.63182 26.6667 5.33334 26.3682 5.33334 26Z" fill="currentColor"/> <path d="M14.0098 8.42359H10.806C10.6872 8.42359 10.6277 8.56721 10.7117 8.6512L16.5491 14.4886C16.8094 14.7489 16.8094 15.171 16.5491 15.4314L15.3234 16.657C15.0631 16.9174 14.641 16.9174 14.3806 16.657L8.63739 10.9138C8.55339 10.8298 8.40978 10.8893 8.40978 11.0081V14.0236C8.40978 14.3918 8.1113 14.6903 7.74311 14.6903H6.00978C5.64159 14.6903 5.34311 14.3918 5.34311 14.0236L5.34311 6.02359C5.34311 5.6554 5.64159 5.35692 6.00978 5.35692L14.0098 5.35692C14.378 5.35692 14.6764 5.6554 14.6764 6.02359V7.75692C14.6764 8.12511 14.378 8.42359 14.0098 8.42359Z" fill="currentColor"/>`;
      Icon$61 = `<path d="M16 15.3333C15.6318 15.3333 15.3333 15.6318 15.3333 16V20C15.3333 20.3682 15.6318 20.6667 16 20.6667H21.3333C21.7015 20.6667 22 20.3682 22 20V16C22 15.6318 21.7015 15.3333 21.3333 15.3333H16Z" fill="currentColor"/> <path fill-rule="evenodd" clip-rule="evenodd" d="M5.33333 7.33334C5.33333 6.96515 5.63181 6.66667 5.99999 6.66667H26C26.3682 6.66667 26.6667 6.96515 26.6667 7.33334V24.6667C26.6667 25.0349 26.3682 25.3333 26 25.3333H5.99999C5.63181 25.3333 5.33333 25.0349 5.33333 24.6667V7.33334ZM7.99999 10C7.99999 9.63182 8.29847 9.33334 8.66666 9.33334H23.3333C23.7015 9.33334 24 9.63182 24 10V22C24 22.3682 23.7015 22.6667 23.3333 22.6667H8.66666C8.29847 22.6667 7.99999 22.3682 7.99999 22V10Z" fill="currentColor"/>`;
      Icon$62 = `<path d="M10.6667 6.6548C10.6667 6.10764 11.2894 5.79346 11.7295 6.11862L24.377 15.4634C24.7377 15.7298 24.7377 16.2692 24.3771 16.5357L11.7295 25.8813C11.2895 26.2065 10.6667 25.8923 10.6667 25.3451L10.6667 6.6548Z" fill="currentColor"/>`;
      Icon$63 = `<path d="M13.9213 5.53573C14.3146 5.45804 14.6666 5.76987 14.6666 6.17079V7.57215C14.6666 7.89777 14.4305 8.17277 14.114 8.24925C12.5981 8.61559 11.2506 9.41368 10.2091 10.506C9.98474 10.7414 9.62903 10.8079 9.34742 10.6453L8.14112 9.94885C7.79394 9.7484 7.69985 9.28777 7.96359 8.98585C9.48505 7.24409 11.5636 6.00143 13.9213 5.53573Z" fill="currentColor"/> <path d="M5.88974 12.5908C6.01805 12.2101 6.46491 12.0603 6.81279 12.2611L8.01201 12.9535C8.29379 13.1162 8.41396 13.4577 8.32238 13.7699C8.11252 14.4854 7.99998 15.2424 7.99998 16.0257C7.99998 16.809 8.11252 17.566 8.32238 18.2814C8.41396 18.5936 8.29378 18.9352 8.01201 19.0979L6.82742 19.7818C6.48051 19.9821 6.03488 19.8337 5.90521 19.4547C5.5345 18.3712 5.33331 17.2091 5.33331 16C5.33331 14.8078 5.5289 13.6613 5.88974 12.5908Z" fill="currentColor"/> <path d="M8.17106 22.0852C7.82291 22.2862 7.72949 22.7486 7.99532 23.0502C9.51387 24.773 11.5799 26.0017 13.9213 26.4642C14.3146 26.5419 14.6666 26.2301 14.6666 25.8291V24.4792C14.6666 24.1536 14.4305 23.8786 14.114 23.8021C12.5981 23.4358 11.2506 22.6377 10.2091 21.5453C9.98474 21.31 9.62903 21.2435 9.34742 21.4061L8.17106 22.0852Z" fill="currentColor"/> <path d="M17.3333 25.8291C17.3333 26.2301 17.6857 26.5418 18.079 26.4641C22.9748 25.4969 26.6666 21.1796 26.6666 16C26.6666 10.8204 22.9748 6.50302 18.079 5.5358C17.6857 5.4581 17.3333 5.76987 17.3333 6.17079V7.57215C17.3333 7.89777 17.5697 8.17282 17.8862 8.24932C21.3942 9.09721 24 12.2572 24 16.0257C24 19.7942 21.3942 22.9542 17.8862 23.802C17.5697 23.8785 17.3333 24.1536 17.3333 24.4792V25.8291Z" fill="currentColor"/> <path d="M14.3961 10.4163C13.9561 10.0911 13.3333 10.4053 13.3333 10.9525L13.3333 21.0474C13.3333 21.5946 13.9561 21.9087 14.3962 21.5836L21.2273 16.5359C21.5879 16.2694 21.5879 15.73 21.2273 15.4635L14.3961 10.4163Z" fill="currentColor"/>`;
      Icon$74 = `<path d="M15.6038 12.2147C16.0439 12.5399 16.6667 12.2257 16.6667 11.6786V10.1789C16.6667 10.1001 16.7351 10.0384 16.8134 10.0479C20.1116 10.4494 22.6667 13.2593 22.6667 16.6659C22.6667 20.3481 19.6817 23.3332 15.9995 23.3332C12.542 23.3332 9.69927 20.7014 9.36509 17.332C9.32875 16.9655 9.03371 16.6662 8.66548 16.6662L6.66655 16.6666C6.29841 16.6666 5.99769 16.966 6.02187 17.3334C6.36494 22.5454 10.7012 26.6667 16 26.6667C21.5228 26.6667 26 22.1895 26 16.6667C26 11.4103 21.9444 7.10112 16.7916 6.69757C16.7216 6.69209 16.6667 6.63396 16.6667 6.56372V4.98824C16.6667 4.44106 16.0439 4.12689 15.6038 4.45206L11.0765 7.79738C10.7159 8.06387 10.7159 8.60326 11.0766 8.86973L15.6038 12.2147Z" fill="currentColor"/>`;
      Icon$77 = `<path d="M16.6667 10.3452C16.6667 10.8924 16.0439 11.2066 15.6038 10.8814L11.0766 7.5364C10.7159 7.26993 10.7159 6.73054 11.0766 6.46405L15.6038 3.11873C16.0439 2.79356 16.6667 3.10773 16.6667 3.6549V5.22682C16.6667 5.29746 16.7223 5.35579 16.7927 5.36066C22.6821 5.76757 27.3333 10.674 27.3333 16.6667C27.3333 22.9259 22.2592 28 16 28C9.96483 28 5.03145 23.2827 4.68601 17.3341C4.66466 16.9665 4.96518 16.6673 5.33339 16.6673H7.3334C7.70157 16.6673 7.99714 16.9668 8.02743 17.3337C8.36638 21.4399 11.8064 24.6667 16 24.6667C20.4183 24.6667 24 21.085 24 16.6667C24 12.5225 20.8483 9.11428 16.8113 8.70739C16.7337 8.69957 16.6667 8.76096 16.6667 8.83893V10.3452Z" fill="currentColor"/> <path fill-rule="evenodd" clip-rule="evenodd" d="M17.0879 19.679C17.4553 19.9195 17.8928 20.0398 18.4004 20.0398C18.9099 20.0398 19.3474 19.9205 19.7129 19.6818C20.0803 19.4413 20.3635 19.0938 20.5623 18.6392C20.7612 18.1847 20.8606 17.6373 20.8606 16.9972C20.8625 16.3608 20.764 15.8192 20.5652 15.3722C20.3663 14.9252 20.0822 14.5853 19.7129 14.3523C19.3455 14.1175 18.908 14 18.4004 14C17.8928 14 17.4553 14.1175 17.0879 14.3523C16.7224 14.5853 16.4402 14.9252 16.2413 15.3722C16.0443 15.8173 15.9449 16.3589 15.943 16.9972C15.9411 17.6354 16.0396 18.1818 16.2385 18.6364C16.4373 19.089 16.7205 19.4366 17.0879 19.679ZM19.1362 18.4262C18.9487 18.7349 18.7034 18.8892 18.4004 18.8892C18.1996 18.8892 18.0226 18.8211 17.8691 18.6847C17.7157 18.5464 17.5964 18.3372 17.5112 18.0568C17.4279 17.7765 17.3871 17.4233 17.389 16.9972C17.3909 16.3684 17.4847 15.9025 17.6703 15.5995C17.8559 15.2945 18.0993 15.1421 18.4004 15.1421C18.603 15.1421 18.7801 15.2093 18.9316 15.3438C19.0832 15.4782 19.2015 15.6828 19.2868 15.9574C19.372 16.2301 19.4146 16.5767 19.4146 16.9972C19.4165 17.6392 19.3237 18.1156 19.1362 18.4262Z" fill="currentColor"/> <path d="M13.7746 19.8978C13.8482 19.8978 13.9079 19.8381 13.9079 19.7644V14.2129C13.9079 14.1393 13.8482 14.0796 13.7746 14.0796H12.642C12.6171 14.0796 12.5927 14.0865 12.5716 14.0997L11.2322 14.9325C11.1931 14.9568 11.1693 14.9996 11.1693 15.0457V15.9497C11.1693 16.0539 11.2833 16.1178 11.3722 16.0635L12.464 15.396C12.4682 15.3934 12.473 15.3921 12.4779 15.3921C12.4926 15.3921 12.5045 15.404 12.5045 15.4187V19.7644C12.5045 19.8381 12.5642 19.8978 12.6378 19.8978H13.7746Z" fill="currentColor"/>`;
      Icon$81 = `<path d="M15.3333 10.3452C15.3333 10.8924 15.9561 11.2066 16.3962 10.8814L20.9234 7.5364C21.2841 7.26993 21.2841 6.73054 20.9235 6.46405L16.3962 3.11873C15.9561 2.79356 15.3333 3.10773 15.3333 3.6549V5.22682C15.3333 5.29746 15.2778 5.35579 15.2073 5.36066C9.31791 5.76757 4.66667 10.674 4.66667 16.6667C4.66667 22.9259 9.74078 28 16 28C22.0352 28 26.9686 23.2827 27.314 17.3341C27.3354 16.9665 27.0348 16.6673 26.6666 16.6673H24.6666C24.2984 16.6673 24.0029 16.9668 23.9726 17.3337C23.6336 21.4399 20.1937 24.6667 16 24.6667C11.5817 24.6667 8 21.085 8 16.6667C8 12.5225 11.1517 9.11428 15.1887 8.70739C15.2663 8.69957 15.3333 8.76096 15.3333 8.83893V10.3452Z" fill="currentColor"/> <path fill-rule="evenodd" clip-rule="evenodd" d="M17.0879 19.679C17.4553 19.9195 17.8928 20.0398 18.4004 20.0398C18.9099 20.0398 19.3474 19.9205 19.7129 19.6818C20.0803 19.4413 20.3635 19.0938 20.5623 18.6392C20.7612 18.1847 20.8606 17.6373 20.8606 16.9972C20.8625 16.3608 20.764 15.8192 20.5652 15.3722C20.3663 14.9252 20.0822 14.5853 19.7129 14.3523C19.3455 14.1175 18.908 14 18.4004 14C17.8928 14 17.4553 14.1175 17.0879 14.3523C16.7224 14.5853 16.4402 14.9252 16.2413 15.3722C16.0443 15.8173 15.9449 16.3589 15.943 16.9972C15.9411 17.6354 16.0396 18.1818 16.2385 18.6364C16.4373 19.089 16.7205 19.4366 17.0879 19.679ZM19.1362 18.4262C18.9487 18.7349 18.7034 18.8892 18.4004 18.8892C18.1996 18.8892 18.0225 18.8211 17.8691 18.6847C17.7157 18.5464 17.5964 18.3372 17.5112 18.0568C17.4278 17.7765 17.3871 17.4233 17.389 16.9972C17.3909 16.3684 17.4847 15.9025 17.6703 15.5995C17.8559 15.2945 18.0992 15.1421 18.4004 15.1421C18.603 15.1421 18.7801 15.2093 18.9316 15.3438C19.0831 15.4782 19.2015 15.6828 19.2867 15.9574C19.372 16.2301 19.4146 16.5767 19.4146 16.9972C19.4165 17.6392 19.3237 18.1156 19.1362 18.4262Z" fill="currentColor"/> <path d="M13.7746 19.8978C13.8482 19.8978 13.9079 19.8381 13.9079 19.7644V14.2129C13.9079 14.1393 13.8482 14.0796 13.7746 14.0796H12.642C12.6171 14.0796 12.5927 14.0865 12.5716 14.0997L11.2322 14.9325C11.1931 14.9568 11.1693 14.9996 11.1693 15.0457V15.9497C11.1693 16.0539 11.2833 16.1178 11.3722 16.0635L12.464 15.396C12.4682 15.3934 12.473 15.3921 12.4779 15.3921C12.4926 15.3921 12.5045 15.404 12.5045 15.4187V19.7644C12.5045 19.8381 12.5642 19.8978 12.6378 19.8978H13.7746Z" fill="currentColor"/>`;
      Icon$86 = `<path d="M18.6669 10.4001C18.6669 10.7683 18.3684 11.0667 18.0002 11.0667H16.2668C15.8987 11.0667 15.6002 10.7683 15.6002 10.4001V9.86674C15.6002 9.7931 15.5405 9.73341 15.4669 9.73341H5.99998C5.63179 9.73341 5.33331 9.43493 5.33331 9.06674V7.33341C5.33331 6.96522 5.63179 6.66674 5.99998 6.66674H15.4669C15.5405 6.66674 15.6002 6.60704 15.6002 6.53341V6.00007C15.6002 5.63188 15.8987 5.3334 16.2668 5.3334H18.0002C18.3684 5.3334 18.6669 5.63188 18.6669 6.00007V10.4001Z" fill="currentColor"/> <path d="M11.3334 18.8668C11.7016 18.8668 12.0001 18.5683 12.0001 18.2001V13.8001C12.0001 13.4319 11.7016 13.1335 11.3334 13.1335H9.60006C9.23187 13.1335 8.93339 13.4319 8.93339 13.8001V14.3335C8.93339 14.4071 8.8737 14.4668 8.80006 14.4668H6.00006C5.63187 14.4668 5.33339 14.7653 5.33339 15.1335V16.8668C5.33339 17.235 5.63187 17.5335 6.00006 17.5335H8.80006C8.8737 17.5335 8.93339 17.5932 8.93339 17.6668V18.2001C8.93339 18.5683 9.23187 18.8668 9.60006 18.8668H11.3334Z" fill="currentColor"/> <path d="M18.6667 26.0001C18.6667 26.3683 18.3682 26.6668 18 26.6668H16.2667C15.8985 26.6668 15.6 26.3683 15.6 26.0001V25.4668C15.6 25.3931 15.5403 25.3334 15.4667 25.3334H6.00014C5.63195 25.3334 5.33348 25.0349 5.33348 24.6668V22.9334C5.33348 22.5652 5.63195 22.2668 6.00014 22.2668H15.4667C15.5403 22.2668 15.6 22.2071 15.6 22.1334V21.6001C15.6 21.2319 15.8985 20.9334 16.2667 20.9334H18C18.3682 20.9334 18.6667 21.2319 18.6667 21.6001V26.0001Z" fill="currentColor"/> <path d="M22 24.6668C22 25.0349 22.2985 25.3334 22.6667 25.3334H26.0001C26.3683 25.3334 26.6668 25.0349 26.6668 24.6668V22.9334C26.6668 22.5652 26.3683 22.2668 26.0001 22.2668H22.6667C22.2985 22.2668 22 22.5652 22 22.9334V24.6668Z" fill="currentColor"/> <path d="M16.0001 17.5335C15.6319 17.5335 15.3334 17.235 15.3334 16.8668V15.1335C15.3334 14.7653 15.6319 14.4668 16.0001 14.4668H26.0001C26.3683 14.4668 26.6667 14.7653 26.6667 15.1335V16.8668C26.6667 17.235 26.3683 17.5335 26.0001 17.5335H16.0001Z" fill="currentColor"/> <path d="M22.0002 9.06674C22.0002 9.43493 22.2987 9.73341 22.6669 9.73341H26C26.3682 9.73341 26.6666 9.43493 26.6666 9.06674V7.3334C26.6666 6.96521 26.3682 6.66674 26 6.66674H22.6669C22.2987 6.66674 22.0002 6.96522 22.0002 7.33341V9.06674Z" fill="currentColor"/>`;
      Icon$88 = `<path fill-rule="evenodd" clip-rule="evenodd" d="M13.5722 5.33333C13.2429 5.33333 12.9629 5.57382 12.9132 5.89938L12.4063 9.21916C12.4 9.26058 12.3746 9.29655 12.3378 9.31672C12.2387 9.37118 12.1409 9.42779 12.0444 9.48648C12.0086 9.5083 11.9646 9.51242 11.9255 9.49718L8.79572 8.27692C8.48896 8.15732 8.14083 8.27958 7.9762 8.56472L5.5491 12.7686C5.38444 13.0538 5.45271 13.4165 5.70981 13.6223L8.33308 15.7225C8.3658 15.7487 8.38422 15.7887 8.38331 15.8306C8.38209 15.8867 8.38148 15.9429 8.38148 15.9993C8.38148 16.0558 8.3821 16.1121 8.38332 16.1684C8.38423 16.2102 8.36582 16.2503 8.33313 16.2765L5.7103 18.3778C5.45334 18.5836 5.38515 18.9462 5.54978 19.2314L7.97688 23.4352C8.14155 23.7205 8.48981 23.8427 8.79661 23.723L11.926 22.5016C11.9651 22.4864 12.009 22.4905 12.0449 22.5123C12.1412 22.5709 12.2388 22.6274 12.3378 22.6818C12.3745 22.7019 12.4 22.7379 12.4063 22.7793L12.9132 26.0993C12.9629 26.4249 13.2429 26.6654 13.5722 26.6654H18.4264C18.7556 26.6654 19.0356 26.425 19.0854 26.0995L19.5933 22.7801C19.5997 22.7386 19.6252 22.7027 19.6619 22.6825C19.7614 22.6279 19.8596 22.5711 19.9564 22.5121C19.9923 22.4903 20.0362 22.4862 20.0754 22.5015L23.2035 23.7223C23.5103 23.842 23.8585 23.7198 24.0232 23.4346L26.4503 19.2307C26.6149 18.9456 26.5467 18.583 26.2898 18.3771L23.6679 16.2766C23.6352 16.2504 23.6168 16.2104 23.6177 16.1685C23.619 16.1122 23.6196 16.0558 23.6196 15.9993C23.6196 15.9429 23.619 15.8866 23.6177 15.8305C23.6168 15.7886 23.6353 15.7486 23.668 15.7224L26.2903 13.623C26.5474 13.4172 26.6156 13.0544 26.451 12.7692L24.0239 8.56537C23.8592 8.28023 23.5111 8.15797 23.2043 8.27757L20.0758 9.49734C20.0367 9.51258 19.9927 9.50846 19.9569 9.48664C19.8599 9.42762 19.7616 9.37071 19.6618 9.31596C19.6251 9.2958 19.5997 9.25984 19.5933 9.21843L19.0854 5.89915C19.0356 5.57369 18.7556 5.33333 18.4264 5.33333H13.5722ZM16.0001 20.2854C18.3672 20.2854 20.2862 18.3664 20.2862 15.9993C20.2862 13.6322 18.3672 11.7132 16.0001 11.7132C13.6329 11.7132 11.714 13.6322 11.714 15.9993C11.714 18.3664 13.6329 20.2854 16.0001 20.2854Z" fill="currentColor"/>`;
      Icon$104 = `<path d="M17.5091 24.6595C17.5091 25.2066 16.8864 25.5208 16.4463 25.1956L9.44847 20.0252C9.42553 20.0083 9.39776 19.9992 9.36923 19.9992H4.66667C4.29848 19.9992 4 19.7007 4 19.3325V12.6658C4 12.2976 4.29848 11.9992 4.66667 11.9992H9.37115C9.39967 11.9992 9.42745 11.99 9.45039 11.9731L16.4463 6.80363C16.8863 6.47845 17.5091 6.79262 17.5091 7.3398L17.5091 24.6595Z" fill="currentColor"/> <path d="M27.5091 9.33336C27.8773 9.33336 28.1758 9.63184 28.1758 10V22C28.1758 22.3682 27.8773 22.6667 27.5091 22.6667H26.1758C25.8076 22.6667 25.5091 22.3682 25.5091 22V10C25.5091 9.63184 25.8076 9.33336 26.1758 9.33336L27.5091 9.33336Z" fill="currentColor"/> <path d="M22.1758 12C22.544 12 22.8424 12.2985 22.8424 12.6667V19.3334C22.8424 19.7016 22.544 20 22.1758 20H20.8424C20.4743 20 20.1758 19.7016 20.1758 19.3334V12.6667C20.1758 12.2985 20.4743 12 20.8424 12H22.1758Z" fill="currentColor"/>`;
      Icon$105 = `<path d="M17.5091 24.6594C17.5091 25.2066 16.8864 25.5207 16.4463 25.1956L9.44847 20.0252C9.42553 20.0083 9.39776 19.9991 9.36923 19.9991H4.66667C4.29848 19.9991 4 19.7006 4 19.3324V12.6658C4 12.2976 4.29848 11.9991 4.66667 11.9991H9.37115C9.39967 11.9991 9.42745 11.99 9.45039 11.973L16.4463 6.80358C16.8863 6.4784 17.5091 6.79258 17.5091 7.33975L17.5091 24.6594Z" fill="currentColor"/> <path d="M22.8424 12.6667C22.8424 12.2985 22.544 12 22.1758 12H20.8424C20.4743 12 20.1758 12.2985 20.1758 12.6667V19.3333C20.1758 19.7015 20.4743 20 20.8424 20H22.1758C22.544 20 22.8424 19.7015 22.8424 19.3333V12.6667Z" fill="currentColor"/>`;
    }
  });

  // frontend/node_modules/media-captions/dist/prod/srt-parser.js
  var srt_parser_exports = {};
  __export(srt_parser_exports, {
    SRTParser: () => SRTParser,
    default: () => createSRTParser
  });
  function createSRTParser() {
    return new SRTParser();
  }
  var MILLISECOND_SEP_RE, TIMESTAMP_SEP, SRTParser;
  var init_srt_parser = __esm({
    "frontend/node_modules/media-captions/dist/prod/srt-parser.js"() {
      init_define_import_meta_env();
      init_prod();
      MILLISECOND_SEP_RE = /,/g;
      TIMESTAMP_SEP = "-->";
      SRTParser = class extends VTTParser {
        parse(line, lineCount) {
          if (line === "") {
            if (this.c) {
              this.l.push(this.c);
              this.h.onCue?.(this.c);
              this.c = null;
            }
            this.e = VTTBlock.None;
          } else if (this.e === VTTBlock.Cue) {
            this.c.text += (this.c.text ? "\n" : "") + line;
          } else if (line.includes(TIMESTAMP_SEP)) {
            const result = this.q(line, lineCount);
            if (result) {
              this.c = new VTTCue(result[0], result[1], result[2].join(" "));
              this.c.id = this.n;
              this.e = VTTBlock.Cue;
            }
          }
          this.n = line;
        }
        q(line, lineCount) {
          return super.q(line.replace(MILLISECOND_SEP_RE, "."), lineCount);
        }
      };
    }
  });

  // frontend/node_modules/media-captions/dist/prod/errors.js
  var errors_exports = {};
  __export(errors_exports, {
    ParseErrorBuilder: () => ParseErrorBuilder
  });
  var ParseErrorBuilder;
  var init_errors = __esm({
    "frontend/node_modules/media-captions/dist/prod/errors.js"() {
      init_define_import_meta_env();
      init_prod();
      ParseErrorBuilder = {
        r() {
          return new ParseError({
            code: ParseErrorCode.BadSignature,
            reason: "missing WEBVTT file header",
            line: 1
          });
        },
        s(startTime, line) {
          return new ParseError({
            code: ParseErrorCode.BadTimestamp,
            reason: `cue start timestamp \`${startTime}\` is invalid on line ${line}`,
            line
          });
        },
        t(endTime, line) {
          return new ParseError({
            code: ParseErrorCode.BadTimestamp,
            reason: `cue end timestamp \`${endTime}\` is invalid on line ${line}`,
            line
          });
        },
        u(startTime, endTime, line) {
          return new ParseError({
            code: ParseErrorCode.BadTimestamp,
            reason: `cue end timestamp \`${endTime}\` is greater than start \`${startTime}\` on line ${line}`,
            line
          });
        },
        y(name, value, line) {
          return new ParseError({
            code: ParseErrorCode.BadSettingValue,
            reason: `invalid value for cue setting \`${name}\` on line ${line} (value: ${value})`,
            line
          });
        },
        x(name, value, line) {
          return new ParseError({
            code: ParseErrorCode.UnknownSetting,
            reason: `unknown cue setting \`${name}\` on line ${line} (value: ${value})`,
            line
          });
        },
        w(name, value, line) {
          return new ParseError({
            code: ParseErrorCode.BadSettingValue,
            reason: `invalid value for region setting \`${name}\` on line ${line} (value: ${value})`,
            line
          });
        },
        v(name, value, line) {
          return new ParseError({
            code: ParseErrorCode.UnknownSetting,
            reason: `unknown region setting \`${name}\` on line ${line} (value: ${value})`,
            line
          });
        },
        // SSA-specific errors
        T(type, line) {
          return new ParseError({
            code: ParseErrorCode.BadFormat,
            reason: `format missing for \`${type}\` block on line ${line}`,
            line
          });
        }
      };
    }
  });

  // frontend/node_modules/media-captions/dist/prod/ssa-parser.js
  var ssa_parser_exports = {};
  __export(ssa_parser_exports, {
    SSAParser: () => SSAParser,
    default: () => createSSAParser
  });
  function parseColor(color) {
    const abgr = parseInt(color.replace("&H", ""), 16);
    if (abgr >= 0) {
      const a = abgr >> 24 & 255 ^ 255;
      const alpha = a / 255;
      const b = abgr >> 16 & 255;
      const g = abgr >> 8 & 255;
      const r2 = abgr & 255;
      return "rgba(" + [r2, g, b, alpha].join(",") + ")";
    }
    return null;
  }
  function buildTextShadow(x, y, color) {
    const noOfShadows = Math.ceil(2 * Math.PI * x);
    let textShadow = "";
    for (let i = 0; i < noOfShadows; i++) {
      const theta = 2 * Math.PI * i / noOfShadows;
      textShadow += x * Math.cos(theta) + "px " + y * Math.sin(theta) + "px 0 " + color + (i == noOfShadows - 1 ? "" : ",");
    }
    return textShadow;
  }
  function createSSAParser() {
    return new SSAParser();
  }
  var FORMAT_START_RE, STYLE_START_RE, DIALOGUE_START_RE, FORMAT_SPLIT_RE, STYLE_FUNCTION_RE, NEW_LINE_RE, STYLES_SECTION_START_RE, EVENTS_SECTION_START_RE, SSAParser;
  var init_ssa_parser = __esm({
    "frontend/node_modules/media-captions/dist/prod/ssa-parser.js"() {
      init_define_import_meta_env();
      init_prod();
      FORMAT_START_RE = /^Format:[\s\t]*/;
      STYLE_START_RE = /^Style:[\s\t]*/;
      DIALOGUE_START_RE = /^Dialogue:[\s\t]*/;
      FORMAT_SPLIT_RE = /[\s\t]*,[\s\t]*/;
      STYLE_FUNCTION_RE = /\{[^}]+\}/g;
      NEW_LINE_RE = /\\N/g;
      STYLES_SECTION_START_RE = /^\[(.*)[\s\t]?Styles\]$/;
      EVENTS_SECTION_START_RE = /^\[(.*)[\s\t]?Events\]$/;
      SSAParser = class {
        constructor() {
          __publicField(this, "h");
          __publicField(this, "O", 0);
          __publicField(this, "c", null);
          __publicField(this, "l", []);
          __publicField(this, "m", []);
          __publicField(this, "N", null);
          __publicField(this, "f");
          __publicField(this, "P", {});
        }
        async init(init) {
          this.h = init;
          if (init.errors)
            this.f = (await Promise.resolve().then(() => (init_errors(), errors_exports))).ParseErrorBuilder;
        }
        parse(line, lineCount) {
          if (this.O) {
            switch (this.O) {
              case 1:
                if (line === "") {
                  this.O = 0;
                } else if (STYLE_START_RE.test(line)) {
                  if (this.N) {
                    const styles = line.replace(STYLE_START_RE, "").split(FORMAT_SPLIT_RE);
                    this.S(styles);
                  } else {
                    this.g(this.f?.T("Style", lineCount));
                  }
                } else if (FORMAT_START_RE.test(line)) {
                  this.N = line.replace(FORMAT_START_RE, "").split(FORMAT_SPLIT_RE);
                } else if (EVENTS_SECTION_START_RE.test(line)) {
                  this.N = null;
                  this.O = 2;
                }
                break;
              case 2:
                if (line === "") {
                  this.Q();
                } else if (DIALOGUE_START_RE.test(line)) {
                  this.Q();
                  if (this.N) {
                    const dialogue = line.replace(DIALOGUE_START_RE, "").split(FORMAT_SPLIT_RE), cue = this.U(dialogue, lineCount);
                    if (cue)
                      this.c = cue;
                  } else {
                    this.g(this.f?.T("Dialogue", lineCount));
                  }
                } else if (this.c) {
                  this.c.text += "\n" + line.replace(STYLE_FUNCTION_RE, "").replace(NEW_LINE_RE, "\n");
                } else if (FORMAT_START_RE.test(line)) {
                  this.N = line.replace(FORMAT_START_RE, "").split(FORMAT_SPLIT_RE);
                } else if (STYLES_SECTION_START_RE.test(line)) {
                  this.N = null;
                  this.O = 1;
                } else if (EVENTS_SECTION_START_RE.test(line)) {
                  this.N = null;
                }
            }
          } else if (line === "") ;
          else if (STYLES_SECTION_START_RE.test(line)) {
            this.N = null;
            this.O = 1;
          } else if (EVENTS_SECTION_START_RE.test(line)) {
            this.N = null;
            this.O = 2;
          }
        }
        done() {
          return {
            metadata: {},
            cues: this.l,
            regions: [],
            errors: this.m
          };
        }
        Q() {
          if (!this.c)
            return;
          this.l.push(this.c);
          this.h.onCue?.(this.c);
          this.c = null;
        }
        S(values) {
          let name = "Default", styles = {}, outlineX, align = "center", vertical = "bottom", marginV, outlineY = 1.2, outlineColor, bgColor, borderStyle = 3, transform = [];
          for (let i = 0; i < this.N.length; i++) {
            const field = this.N[i], value = values[i];
            switch (field) {
              case "Name":
                name = value;
                break;
              case "Fontname":
                styles["font-family"] = value;
                break;
              case "Fontsize":
                styles["font-size"] = `calc(${value} / var(--overlay-height))`;
                break;
              case "PrimaryColour":
                const color = parseColor(value);
                if (color)
                  styles["--cue-color"] = color;
                break;
              case "BorderStyle":
                borderStyle = parseInt(value, 10);
                break;
              case "BackColour":
                bgColor = parseColor(value);
                break;
              case "OutlineColour":
                const _outlineColor = parseColor(value);
                if (_outlineColor)
                  outlineColor = _outlineColor;
                break;
              case "Bold":
                if (parseInt(value))
                  styles["font-weight"] = "bold";
                break;
              case "Italic":
                if (parseInt(value))
                  styles["font-style"] = "italic";
                break;
              case "Underline":
                if (parseInt(value))
                  styles["text-decoration"] = "underline";
                break;
              case "StrikeOut":
                if (parseInt(value))
                  styles["text-decoration"] = "line-through";
                break;
              case "Spacing":
                styles["letter-spacing"] = value + "px";
                break;
              case "AlphaLevel":
                styles["opacity"] = parseFloat(value);
                break;
              case "ScaleX":
                transform.push(`scaleX(${parseFloat(value) / 100})`);
                break;
              case "ScaleY":
                transform.push(`scaleY(${parseFloat(value) / 100})`);
                break;
              case "Angle":
                transform.push(`rotate(${value}deg)`);
                break;
              case "Shadow":
                outlineY = parseInt(value, 10) * 1.2;
                break;
              case "MarginL":
                styles["--cue-width"] = "auto";
                styles["--cue-left"] = parseFloat(value) + "px";
                break;
              case "MarginR":
                styles["--cue-width"] = "auto";
                styles["--cue-right"] = parseFloat(value) + "px";
                break;
              case "MarginV":
                marginV = parseFloat(value);
                break;
              case "Outline":
                outlineX = parseInt(value, 10);
                break;
              case "Alignment":
                const alignment = parseInt(value, 10);
                if (alignment >= 4)
                  vertical = alignment >= 7 ? "top" : "center";
                switch (alignment % 3) {
                  case 1:
                    align = "start";
                    break;
                  case 2:
                    align = "center";
                    break;
                  case 3:
                    align = "end";
                    break;
                }
            }
          }
          styles.R = vertical;
          styles["--cue-white-space"] = "normal";
          styles["--cue-line-height"] = "normal";
          styles["--cue-text-align"] = align;
          if (vertical === "center") {
            styles[`--cue-top`] = "50%";
            transform.push("translateY(-50%)");
          } else {
            styles[`--cue-${vertical}`] = (marginV || 0) + "px";
          }
          if (borderStyle === 1) {
            styles["--cue-padding-y"] = "0";
          }
          if (borderStyle === 1 || bgColor) {
            styles["--cue-bg-color"] = borderStyle === 1 ? "none" : bgColor;
          }
          if (borderStyle === 3 && outlineColor) {
            styles["--cue-outline"] = `${outlineX}px solid ${outlineColor}`;
          }
          if (borderStyle === 1 && typeof outlineX === "number") {
            const color = bgColor ?? "#000";
            styles["--cue-text-shadow"] = [
              outlineColor && buildTextShadow(outlineX * 1.2, outlineY * 1.2, outlineColor),
              outlineColor ? buildTextShadow(outlineX * (outlineX / 2), outlineY * (outlineX / 2), color) : buildTextShadow(outlineX, outlineY, color)
            ].filter(Boolean).join(", ");
          }
          if (transform.length)
            styles["--cue-transform"] = transform.join(" ");
          this.P[name] = styles;
        }
        U(values, lineCount) {
          const fields = this.V(values);
          const timestamp = this.q(fields.Start, fields.End, lineCount);
          if (!timestamp)
            return;
          const cue = new VTTCue(timestamp[0], timestamp[1], ""), styles = { ...this.P[fields.Style] || {} }, voice = fields.Name ? `<v ${fields.Name}>` : "";
          const vertical = styles.R, marginLeft = fields.MarginL && parseFloat(fields.MarginL), marginRight = fields.MarginR && parseFloat(fields.MarginR), marginV = fields.MarginV && parseFloat(fields.MarginV);
          if (marginLeft) {
            styles["--cue-width"] = "auto";
            styles["--cue-left"] = marginLeft + "px";
          }
          if (marginRight) {
            styles["--cue-width"] = "auto";
            styles["--cue-right"] = marginRight + "px";
          }
          if (marginV && vertical !== "center") {
            styles[`--cue-${vertical}`] = marginV + "px";
          }
          cue.text = voice + values.slice(this.N.length - 1).join(", ").replace(STYLE_FUNCTION_RE, "").replace(NEW_LINE_RE, "\n");
          delete styles.R;
          if (Object.keys(styles).length)
            cue.style = styles;
          return cue;
        }
        V(values) {
          const fields = {};
          for (let i = 0; i < this.N.length; i++) {
            fields[this.N[i]] = values[i];
          }
          return fields;
        }
        q(startTimeText, endTimeText, lineCount) {
          const startTime = parseVTTTimestamp(startTimeText), endTime = parseVTTTimestamp(endTimeText);
          if (startTime !== null && endTime !== null && endTime > startTime) {
            return [startTime, endTime];
          } else {
            if (startTime === null) {
              this.g(this.f?.s(startTimeText, lineCount));
            }
            if (endTime === null) {
              this.g(this.f?.t(endTimeText, lineCount));
            }
            if (startTime != null && endTime !== null && endTime > startTime) {
              this.g(this.f?.u(startTime, endTime, lineCount));
            }
          }
        }
        g(error) {
          if (!error)
            return;
          this.m.push(error);
          if (this.h.strict) {
            this.h.cancel();
            throw error;
          } else {
            this.h.onError?.(error);
          }
        }
      };
    }
  });

  // frontend/node_modules/media-captions/dist/prod/index.js
  async function parseText(text, options) {
    const stream = new ReadableStream({
      start(controller) {
        const lines = text.split(LINE_TERMINATOR_RE);
        for (const line of lines)
          controller.enqueue(line);
        controller.close();
      }
    });
    return parseTextStream(stream, options);
  }
  async function parseTextStream(stream, options) {
    const type = options?.type ?? "vtt";
    let factory;
    if (typeof type === "string") {
      switch (type) {
        case "srt":
          factory = (await Promise.resolve().then(() => (init_srt_parser(), srt_parser_exports))).default;
          break;
        case "ssa":
        case "ass":
          factory = (await Promise.resolve().then(() => (init_ssa_parser(), ssa_parser_exports))).default;
          break;
        default:
          factory = (await Promise.resolve().then(function() {
            return vttParser;
          })).default;
      }
    } else {
      factory = type;
    }
    let result;
    const reader = stream.getReader(), parser = factory(), errors = !!options?.strict || !!options?.errors;
    await parser.init({
      strict: false,
      ...options,
      errors,
      type,
      cancel() {
        reader.cancel();
        result = parser.done(true);
      }
    });
    let i = 1;
    while (true) {
      const { value, done } = await reader.read();
      if (done) {
        parser.parse("", i);
        result = parser.done(false);
        break;
      }
      parser.parse(value, i);
      i++;
    }
    return result;
  }
  async function parseResponse(response, options) {
    const res = await response;
    if (!res.ok || !res.body) {
      let error;
      return {
        metadata: {},
        cues: [],
        regions: [],
        errors: [error]
      };
    }
    const contentType = res.headers.get("content-type") || "", type = contentType.match(/text\/(.*?)(?:;|$)/)?.[1], encoding = contentType.match(/charset=(.*?)(?:;|$)/)?.[1];
    return parseByteStream(res.body, { type, encoding, ...options });
  }
  async function parseByteStream(stream, { encoding = "utf-8", ...options } = {}) {
    const textStream = stream.pipeThrough(new TextLineTransformStream(encoding));
    return parseTextStream(textStream, options);
  }
  function toNumber(text) {
    const num = parseInt(text, 10);
    return !Number.isNaN(num) ? num : null;
  }
  function toPercentage(text) {
    const num = parseInt(text.replace(PERCENT_SIGN$1, ""), 10);
    return !Number.isNaN(num) && num >= 0 && num <= 100 ? num : null;
  }
  function toCoords(text) {
    if (!text.includes(COMMA$1))
      return null;
    const [x, y] = text.split(COMMA$1).map(toPercentage);
    return x !== null && y !== null ? [x, y] : null;
  }
  function toFloat(text) {
    const num = parseFloat(text);
    return !Number.isNaN(num) ? num : null;
  }
  function parseVTTTimestamp(timestamp) {
    const match = timestamp.match(TIMESTAMP_RE);
    if (!match)
      return null;
    const hours = match[1] ? parseInt(match[1], 10) : 0, minutes = parseInt(match[2], 10), seconds = parseInt(match[3], 10), milliseconds = match[4] ? parseInt(match[4].padEnd(3, "0"), 10) : 0, total = hours * 3600 + minutes * 60 + seconds + milliseconds / 1e3;
    if (hours < 0 || minutes < 0 || seconds < 0 || milliseconds < 0 || minutes > 59 || seconds > 59) {
      return null;
    }
    return total;
  }
  function createVTTParser() {
    return new VTTParser();
  }
  function tokenizeVTTCue(cue) {
    let buffer = "", mode = 1, result = [], stack = [], node;
    for (let i = 0; i < cue.text.length; i++) {
      const char = cue.text[i];
      switch (mode) {
        case 1:
          if (char === "<") {
            addText();
            mode = 2;
          } else {
            buffer += char;
          }
          break;
        case 2:
          switch (char) {
            case "\n":
            case "	":
            case " ":
              addNode();
              mode = 4;
              break;
            case ".":
              addNode();
              mode = 3;
              break;
            case "/":
              mode = 5;
              break;
            case ">":
              addNode();
              mode = 1;
              break;
            default:
              if (!buffer && DIGIT_RE.test(char))
                mode = 6;
              buffer += char;
              break;
          }
          break;
        case 3:
          switch (char) {
            case "	":
            case " ":
            case "\n":
              addClass();
              if (node)
                node.class?.trim();
              mode = 4;
              break;
            case ".":
              addClass();
              break;
            case ">":
              addClass();
              if (node)
                node.class?.trim();
              mode = 1;
              break;
            default:
              buffer += char;
          }
          break;
        case 4:
          if (char === ">") {
            buffer = buffer.replace(MULTI_SPACE_RE, " ");
            if (node?.type === "v")
              node.voice = replaceHTMLEntities(buffer);
            else if (node?.type === "lang")
              node.lang = replaceHTMLEntities(buffer);
            buffer = "";
            mode = 1;
          } else {
            buffer += char;
          }
          break;
        case 5:
          if (char === ">") {
            buffer = "";
            node = stack.pop();
            mode = 1;
          }
          break;
        case 6:
          if (char === ">") {
            const time = parseVTTTimestamp(buffer);
            if (time !== null && time >= cue.startTime && time <= cue.endTime) {
              buffer = "timestamp";
              addNode();
              node.time = time;
            }
            buffer = "";
            mode = 1;
          } else {
            buffer += char;
          }
          break;
      }
    }
    function addNode() {
      if (BLOCK_TYPES.has(buffer)) {
        const parent = node;
        node = createBlockNode(buffer);
        if (parent) {
          if (stack[stack.length - 1] !== parent)
            stack.push(parent);
          parent.children.push(node);
        } else
          result.push(node);
      }
      buffer = "";
      mode = 1;
    }
    function addClass() {
      if (node && buffer) {
        const color = buffer.replace("bg_", "");
        if (COLORS.has(color)) {
          node[buffer.startsWith("bg_") ? "bgColor" : "color"] = color;
        } else {
          node.class = !node.class ? buffer : node.class + " " + buffer;
        }
      }
      buffer = "";
    }
    function addText() {
      if (!buffer)
        return;
      const text = { type: "text", data: replaceHTMLEntities(buffer) };
      node ? node.children.push(text) : result.push(text);
      buffer = "";
    }
    if (mode === 1)
      addText();
    return result;
  }
  function createBlockNode(type) {
    return {
      tagName: TAG_NAME[type],
      type,
      children: []
    };
  }
  function replaceHTMLEntities(text) {
    return text.replace(HTML_ENTITY_RE, (entity) => HTML_ENTITIES[entity] || "'");
  }
  function setCSSVar(el, name, value) {
    el.style.setProperty(`--${name}`, value + "");
  }
  function setDataAttr(el, name, value = true) {
    el.setAttribute(`data-${name}`, value === true ? "" : value + "");
  }
  function setPartAttr(el, name) {
    el.setAttribute("data-part", name);
  }
  function getLineHeight(el) {
    return parseFloat(getComputedStyle(el).lineHeight) || 0;
  }
  function createVTTCueTemplate(cue) {
    if (IS_SERVER) {
      throw Error(
        "[media-captions] called `createVTTCueTemplate` on the server - use `renderVTTCueString`"
      );
    }
    const template = document.createElement("template");
    template.innerHTML = renderVTTCueString(cue);
    return { cue, content: template.content };
  }
  function renderVTTCueString(cue, currentTime = 0) {
    return renderVTTTokensString(tokenizeVTTCue(cue), currentTime);
  }
  function renderVTTTokensString(tokens, currentTime = 0) {
    let attrs, result = "";
    for (const token of tokens) {
      if (token.type === "text") {
        result += token.data;
      } else {
        const isTimestamp = token.type === "timestamp";
        attrs = {};
        attrs.class = token.class;
        attrs.title = token.type === "v" && token.voice;
        attrs.lang = token.type === "lang" && token.lang;
        attrs["data-part"] = token.type === "v" && "voice";
        if (isTimestamp) {
          attrs["data-part"] = "timed";
          attrs["data-time"] = token.time;
          attrs["data-future"] = token.time > currentTime;
          attrs["data-past"] = token.time < currentTime;
        }
        attrs.style = `${token.color ? `color: ${token.color};` : ""}${token.bgColor ? `background-color: ${token.bgColor};` : ""}`;
        const attributes = Object.entries(attrs).filter((v) => v[1]).map((v) => `${v[0]}="${v[1] === true ? "" : v[1]}"`).join(" ");
        result += `<${token.tagName}${attributes ? " " + attributes : ""}>${renderVTTTokensString(
          token.children
        )}</${token.tagName}>`;
      }
    }
    return result;
  }
  function updateTimedVTTCueNodes(root2, currentTime) {
    if (IS_SERVER)
      return;
    for (const el of root2.querySelectorAll('[data-part="timed"]')) {
      const time = Number(el.getAttribute("data-time"));
      if (Number.isNaN(time))
        continue;
      if (time > currentTime)
        setDataAttr(el, "future");
      else
        el.removeAttribute("data-future");
      if (time < currentTime)
        setDataAttr(el, "past");
      else
        el.removeAttribute("data-past");
    }
  }
  function debounce2(fn, delay) {
    let timeout = null, args;
    function run() {
      clear();
      fn(...args);
      args = void 0;
    }
    function clear() {
      clearTimeout(timeout);
      timeout = null;
    }
    function debounce22() {
      args = [].slice.call(arguments);
      clear();
      timeout = setTimeout(run, delay);
    }
    return debounce22;
  }
  function createBox(box) {
    if (box instanceof HTMLElement) {
      return {
        top: box.offsetTop,
        width: box.clientWidth,
        height: box.clientHeight,
        left: box.offsetLeft,
        right: box.offsetLeft + box.clientWidth,
        bottom: box.offsetTop + box.clientHeight
      };
    }
    return { ...box };
  }
  function moveBox(box, axis, delta) {
    switch (axis) {
      case "+x":
        box.left += delta;
        box.right += delta;
        break;
      case "-x":
        box.left -= delta;
        box.right -= delta;
        break;
      case "+y":
        box.top += delta;
        box.bottom += delta;
        break;
      case "-y":
        box.top -= delta;
        box.bottom -= delta;
        break;
    }
  }
  function isBoxCollision(a, b) {
    return a.left <= b.right && a.right >= b.left && a.top <= b.bottom && a.bottom >= b.top;
  }
  function isAnyBoxCollision(box, boxes) {
    for (let i = 0; i < boxes.length; i++)
      if (isBoxCollision(box, boxes[i]))
        return boxes[i];
    return null;
  }
  function isWithinBox(container, box) {
    return box.top >= 0 && box.bottom <= container.height && box.left >= 0 && box.right <= container.width;
  }
  function isBoxOutOfBounds(container, box, axis) {
    switch (axis) {
      case "+x":
        return box.left < 0;
      case "-x":
        return box.right > container.width;
      case "+y":
        return box.top < 0;
      case "-y":
        return box.bottom > container.height;
    }
  }
  function calcBoxIntersectPercentage(container, box) {
    const x = Math.max(0, Math.min(container.width, box.right) - Math.max(0, box.left)), y = Math.max(0, Math.min(container.height, box.bottom) - Math.max(0, box.top)), intersectArea = x * y;
    return intersectArea / (container.height * container.width);
  }
  function createCSSBox(container, box) {
    return {
      top: box.top / container.height,
      left: box.left / container.width,
      right: (container.width - box.right) / container.width,
      bottom: (container.height - box.bottom) / container.height
    };
  }
  function resolveRelativeBox(container, box) {
    box.top = box.top * container.height;
    box.left = box.left * container.width;
    box.right = container.width - box.right * container.width;
    box.bottom = container.height - box.bottom * container.height;
    return box;
  }
  function setBoxCSSVars(el, container, box, prefix) {
    const cssBox = createCSSBox(container, box);
    for (const side of BOX_SIDES) {
      setCSSVar(el, `${prefix}-${side}`, cssBox[side] * 100 + "%");
    }
  }
  function avoidBoxCollisions(container, box, boxes, axis) {
    let percentage = 1, positionedBox, startBox = { ...box };
    for (let i = 0; i < axis.length; i++) {
      while (isBoxOutOfBounds(container, box, axis[i]) || isWithinBox(container, box) && isAnyBoxCollision(box, boxes)) {
        moveBox(box, axis[i], 1);
      }
      if (isWithinBox(container, box))
        return box;
      const intersection = calcBoxIntersectPercentage(container, box);
      if (percentage > intersection) {
        positionedBox = { ...box };
        percentage = intersection;
      }
      box = { ...startBox };
    }
    return positionedBox || startBox;
  }
  function positionCue(container, cue, displayEl, boxes) {
    let cueEl = displayEl.firstElementChild, line = computeCueLine(cue), displayBox, axis = [];
    if (!displayEl[STARTING_BOX]) {
      displayEl[STARTING_BOX] = createStartingBox(container, displayEl);
    }
    displayBox = resolveRelativeBox(container, { ...displayEl[STARTING_BOX] });
    if (displayEl[POSITION_OVERRIDE]) {
      axis = [displayEl[POSITION_OVERRIDE] === "top" ? "+y" : "-y", "+x", "-x"];
    } else if (cue.snapToLines) {
      let size2;
      switch (cue.vertical) {
        case "":
          axis = ["+y", "-y"];
          size2 = "height";
          break;
        case "rl":
          axis = ["+x", "-x"];
          size2 = "width";
          break;
        case "lr":
          axis = ["-x", "+x"];
          size2 = "width";
          break;
      }
      let step = getLineHeight(cueEl), position = step * Math.round(line), maxPosition = container[size2] + step, initialAxis = axis[0];
      if (Math.abs(position) > maxPosition) {
        position = position < 0 ? -1 : 1;
        position *= Math.ceil(maxPosition / step) * step;
      }
      if (line < 0) {
        position += cue.vertical === "" ? container.height : container.width;
        axis = axis.reverse();
      }
      moveBox(displayBox, initialAxis, position);
    } else {
      const isHorizontal = cue.vertical === "", posAxis = isHorizontal ? "+y" : "+x", size2 = isHorizontal ? displayBox.height : displayBox.width;
      moveBox(
        displayBox,
        posAxis,
        (isHorizontal ? container.height : container.width) * line / 100
      );
      moveBox(
        displayBox,
        posAxis,
        cue.lineAlign === "center" ? size2 / 2 : cue.lineAlign === "end" ? size2 : 0
      );
      axis = isHorizontal ? ["-y", "+y", "-x", "+x"] : ["-x", "+x", "-y", "+y"];
    }
    displayBox = avoidBoxCollisions(container, displayBox, boxes, axis);
    setBoxCSSVars(displayEl, container, displayBox, "cue");
    return displayBox;
  }
  function createStartingBox(container, cueEl) {
    const box = createBox(cueEl), pos = getStyledPositions(cueEl);
    cueEl[POSITION_OVERRIDE] = false;
    if (pos.top) {
      box.top = pos.top;
      box.bottom = pos.top + box.height;
      cueEl[POSITION_OVERRIDE] = "top";
    }
    if (pos.bottom) {
      const bottom = container.height - pos.bottom;
      box.top = bottom - box.height;
      box.bottom = bottom;
      cueEl[POSITION_OVERRIDE] = "bottom";
    }
    if (pos.left)
      box.left = pos.left;
    if (pos.right)
      box.right = container.width - pos.right;
    return createCSSBox(container, box);
  }
  function getStyledPositions(el) {
    const positions = {};
    for (const side of BOX_SIDES) {
      positions[side] = parseFloat(el.style.getPropertyValue(`--cue-${side}`));
    }
    return positions;
  }
  function computeCueLine(cue) {
    if (cue.line === "auto") {
      if (!cue.snapToLines) {
        return 100;
      } else {
        return -1;
      }
    }
    return cue.line;
  }
  function computeCuePosition(cue) {
    if (cue.position === "auto") {
      switch (cue.align) {
        case "start":
        case "left":
          return 0;
        case "right":
        case "end":
          return 100;
        default:
          return 50;
      }
    }
    return cue.position;
  }
  function computeCuePositionAlignment(cue, dir) {
    if (cue.positionAlign === "auto") {
      switch (cue.align) {
        case "start":
          return dir === "ltr" ? "line-left" : "line-right";
        case "end":
          return dir === "ltr" ? "line-right" : "line-left";
        case "center":
          return "center";
        default:
          return `line-${cue.align}`;
      }
    }
    return cue.positionAlign;
  }
  function positionRegion(container, region, regionEl, boxes) {
    let cues = Array.from(regionEl.querySelectorAll('[data-part="cue-display"]')), height = 0, limit = Math.max(0, cues.length - region.lines);
    for (let i = cues.length - 1; i >= limit; i--) {
      height += cues[i].offsetHeight;
    }
    setCSSVar(regionEl, "region-height", height + "px");
    if (!regionEl[STARTING_BOX]) {
      regionEl[STARTING_BOX] = createCSSBox(container, createBox(regionEl));
    }
    let box = { ...regionEl[STARTING_BOX] };
    box = resolveRelativeBox(container, box);
    box.width = regionEl.clientWidth;
    box.height = height;
    box.right = box.left + box.width;
    box.bottom = box.top + height;
    box = avoidBoxCollisions(container, box, boxes, REGION_AXIS);
    setBoxCSSVars(regionEl, container, box, "region");
    return box;
  }
  var ParseErrorCode, ParseError, LINE_TERMINATOR_RE, TextLineTransformStream, TextStreamLineIterator, TextCue, IS_SERVER, CueBase, VTTCue, VTTRegion, COMMA$1, PERCENT_SIGN$1, HEADER_MAGIC, COMMA, PERCENT_SIGN, SETTING_SEP_RE, SETTING_LINE_RE, NOTE_BLOCK_START, REGION_BLOCK_START, REGION_BLOCK_START_RE, SPACE_RE, TIMESTAMP_SEP2, TIMESTAMP_SEP_RE, ALIGN_RE, LINE_ALIGN_RE, POS_ALIGN_RE, TIMESTAMP_RE, VTTBlock, VTTParser, vttParser, DIGIT_RE, MULTI_SPACE_RE, TAG_NAME, HTML_ENTITIES, HTML_ENTITY_RE, COLORS, BLOCK_TYPES, STARTING_BOX, BOX_SIDES, POSITION_OVERRIDE, REGION_AXIS, CaptionsRenderer;
  var init_prod = __esm({
    "frontend/node_modules/media-captions/dist/prod/index.js"() {
      init_define_import_meta_env();
      ParseErrorCode = {
        LoadFail: 0,
        BadSignature: 1,
        BadTimestamp: 2,
        BadSettingValue: 3,
        BadFormat: 4,
        UnknownSetting: 5
      };
      ParseError = class extends Error {
        constructor(init) {
          super(init.reason);
          __publicField(this, "code");
          __publicField(this, "line");
          this.code = init.code;
          this.line = init.line;
        }
      };
      LINE_TERMINATOR_RE = /\r?\n|\r/gm;
      TextLineTransformStream = class {
        constructor(encoding) {
          __publicField(this, "writable");
          __publicField(this, "readable");
          const transformer = new TextStreamLineIterator(encoding);
          this.writable = new WritableStream({
            write(chunk) {
              transformer.transform(chunk);
            },
            close() {
              transformer.close();
            }
          });
          this.readable = new ReadableStream({
            start(controller) {
              transformer.onLine = (line) => controller.enqueue(line);
              transformer.onClose = () => controller.close();
            }
          });
        }
      };
      TextStreamLineIterator = class {
        constructor(encoding) {
          __publicField(this, "a", "");
          __publicField(this, "b");
          __publicField(this, "onLine");
          __publicField(this, "onClose");
          this.b = new TextDecoder(encoding);
        }
        transform(chunk) {
          this.a += this.b.decode(chunk, { stream: true });
          const lines = this.a.split(LINE_TERMINATOR_RE);
          this.a = lines.pop() || "";
          for (let i = 0; i < lines.length; i++)
            this.onLine(lines[i].trim());
        }
        close() {
          if (this.a)
            this.onLine(this.a.trim());
          this.a = "";
          this.onClose();
        }
      };
      TextCue = class extends EventTarget {
        constructor(startTime, endTime, text) {
          super();
          /**
           * A string that identifies the cue.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/TextTrackCue/id}
           */
          __publicField(this, "id", "");
          /**
           * A `double` that represents the video time that the cue will start being displayed, in seconds.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/TextTrackCue/startTime}
           */
          __publicField(this, "startTime");
          /**
           * A `double` that represents the video time that the cue will stop being displayed, in seconds.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/TextTrackCue/endTime}
           */
          __publicField(this, "endTime");
          /**
           * Returns a string with the contents of the cue.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/VTTCue/text}
           */
          __publicField(this, "text");
          /**
           * A `boolean` for whether the video will pause when this cue stops being displayed.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/TextTrackCue/pauseOnExit}
           */
          __publicField(this, "pauseOnExit", false);
          this.startTime = startTime;
          this.endTime = endTime;
          this.text = text;
        }
        addEventListener(type, listener, options) {
          super.addEventListener(type, listener, options);
        }
        removeEventListener(type, listener, options) {
          super.removeEventListener(type, listener, options);
        }
      };
      IS_SERVER = typeof document === "undefined";
      CueBase = IS_SERVER ? TextCue : window.VTTCue;
      VTTCue = class extends CueBase {
        constructor() {
          super(...arguments);
          /**
           * A `VTTRegion` object describing the video's sub-region that the cue will be drawn onto,
           * or `null` if none is assigned.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/VTTCue/region}
           */
          __publicField(this, "region", null);
          /**
           * The cue writing direction.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/VTTCue/vertical}
           */
          __publicField(this, "vertical", "");
          /**
           * Returns `true` if the `VTTCue.line` attribute is an integer number of lines or a percentage
           * of the video size.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/VTTCue/snapToLines}
           */
          __publicField(this, "snapToLines", true);
          /**
           * Returns the line positioning of the cue. This can be the string `'auto'` or a number whose
           * interpretation depends on the value of `VTTCue.snapToLines`.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/VTTCue/line}
           */
          __publicField(this, "line", "auto");
          /**
           * Returns an enum representing the alignment of the `VTTCue.line`.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/VTTCue/lineAlign}
           */
          __publicField(this, "lineAlign", "start");
          /**
           * Returns the indentation of the cue within the line. This can be the string `'auto'` or a
           * number representing the percentage of the `VTTCue.region`, or the video size if `VTTCue`.region`
           * is `null`.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/VTTCue/position}
           */
          __publicField(this, "position", "auto");
          /**
           * Returns an enum representing the alignment of the cue. This is used to determine what
           * the `VTTCue.position` is anchored to. The default is `'auto'`.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/VTTCue/positionAlign}
           */
          __publicField(this, "positionAlign", "auto");
          /**
           * Returns a double representing the size of the cue, as a percentage of the video size.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/VTTCue/size}
           */
          __publicField(this, "size", 100);
          /**
           * Returns an enum representing the alignment of all the lines of text within the cue box.
           *
           * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/VTTCue/align}
           */
          __publicField(this, "align", "center");
          /**
           * Additional styles associated with the cue.
           */
          __publicField(this, "style");
        }
      };
      VTTRegion = class {
        constructor() {
          /**
           * A string that identifies the region.
           */
          __publicField(this, "id", "");
          /**
           * A `double` representing the width of the region, as a percentage of the video.
           */
          __publicField(this, "width", 100);
          /**
           * A `double` representing the height of the region, in number of lines.
           */
          __publicField(this, "lines", 3);
          /**
           * A `double` representing the region anchor X offset, as a percentage of the region.
           */
          __publicField(this, "regionAnchorX", 0);
          /**
           * A `double` representing the region anchor Y offset, as a percentage of the region.
           */
          __publicField(this, "regionAnchorY", 100);
          /**
           * A `double` representing the viewport anchor X offset, as a percentage of the video.
           */
          __publicField(this, "viewportAnchorX", 0);
          /**
           * A `double` representing the viewport anchor Y offset, as a percentage of the video.
           */
          __publicField(this, "viewportAnchorY", 100);
          /**
           * An enum representing how adding new cues will move existing cues.
           */
          __publicField(this, "scroll", "");
        }
      };
      COMMA$1 = ",";
      PERCENT_SIGN$1 = "%";
      HEADER_MAGIC = "WEBVTT";
      COMMA = ",";
      PERCENT_SIGN = "%";
      SETTING_SEP_RE = /[:=]/;
      SETTING_LINE_RE = /^[\s\t]*(region|vertical|line|position|size|align)[:=]/;
      NOTE_BLOCK_START = "NOTE";
      REGION_BLOCK_START = "REGION";
      REGION_BLOCK_START_RE = /^REGION:?[\s\t]+/;
      SPACE_RE = /[\s\t]+/;
      TIMESTAMP_SEP2 = "-->";
      TIMESTAMP_SEP_RE = /[\s\t]*-->[\s\t]+/;
      ALIGN_RE = /start|center|end|left|right/;
      LINE_ALIGN_RE = /start|center|end/;
      POS_ALIGN_RE = /line-(?:left|right)|center|auto/;
      TIMESTAMP_RE = /^(?:(\d{1,2}):)?(\d{2}):(\d{2})(?:\.(\d{1,3}))?$/;
      VTTBlock = /* @__PURE__ */ ((VTTBlock2) => {
        VTTBlock2[VTTBlock2["None"] = 0] = "None";
        VTTBlock2[VTTBlock2["Header"] = 1] = "Header";
        VTTBlock2[VTTBlock2["Cue"] = 2] = "Cue";
        VTTBlock2[VTTBlock2["Region"] = 3] = "Region";
        VTTBlock2[VTTBlock2["Note"] = 4] = "Note";
        return VTTBlock2;
      })(VTTBlock || {});
      VTTParser = class {
        constructor() {
          __publicField(this, "h");
          __publicField(this, "e", 0);
          __publicField(this, "i", {});
          __publicField(this, "j", {});
          __publicField(this, "l", []);
          __publicField(this, "c", null);
          __publicField(this, "d", null);
          __publicField(this, "m", []);
          __publicField(this, "f");
          __publicField(this, "n", "");
        }
        async init(init) {
          this.h = init;
          if (init.strict)
            this.e = 1;
          if (init.errors)
            this.f = (await Promise.resolve().then(() => (init_errors(), errors_exports))).ParseErrorBuilder;
        }
        parse(line, lineCount) {
          if (line === "") {
            if (this.c) {
              this.l.push(this.c);
              this.h.onCue?.(this.c);
              this.c = null;
            } else if (this.d) {
              this.j[this.d.id] = this.d;
              this.h.onRegion?.(this.d);
              this.d = null;
            } else if (this.e === 1) {
              this.k(line, lineCount);
              this.h.onHeaderMetadata?.(this.i);
            }
            this.e = 0;
          } else if (this.e) {
            switch (this.e) {
              case 1:
                this.k(line, lineCount);
                break;
              case 2:
                if (this.c) {
                  const hasText = this.c.text.length > 0;
                  if (!hasText && SETTING_LINE_RE.test(line)) {
                    this.o(line.split(SPACE_RE), lineCount);
                  } else {
                    this.c.text += (hasText ? "\n" : "") + line;
                  }
                }
                break;
              case 3:
                this.p(line.split(SPACE_RE), lineCount);
                break;
            }
          } else if (line.startsWith(NOTE_BLOCK_START)) {
            this.e = 4;
          } else if (line.startsWith(REGION_BLOCK_START)) {
            this.e = 3;
            this.d = new VTTRegion();
            this.p(line.replace(REGION_BLOCK_START_RE, "").split(SPACE_RE), lineCount);
          } else if (line.includes(TIMESTAMP_SEP2)) {
            const result = this.q(line, lineCount);
            if (result) {
              this.c = new VTTCue(result[0], result[1], "");
              this.c.id = this.n;
              this.o(result[2], lineCount);
            }
            this.e = 2;
          } else if (lineCount === 1) {
            this.k(line, lineCount);
          }
          this.n = line;
        }
        done() {
          return {
            metadata: this.i,
            cues: this.l,
            regions: Object.values(this.j),
            errors: this.m
          };
        }
        k(line, lineCount) {
          if (lineCount > 1) {
            if (SETTING_SEP_RE.test(line)) {
              const [key2, value] = line.split(SETTING_SEP_RE);
              if (key2)
                this.i[key2] = (value || "").replace(SPACE_RE, "");
            }
          } else if (line.startsWith(HEADER_MAGIC)) {
            this.e = 1;
          } else {
            this.g(this.f?.r());
          }
        }
        q(line, lineCount) {
          const [startTimeText, trailingText = ""] = line.split(TIMESTAMP_SEP_RE), [endTimeText, ...settingsText] = trailingText.split(SPACE_RE), startTime = parseVTTTimestamp(startTimeText), endTime = parseVTTTimestamp(endTimeText);
          if (startTime !== null && endTime !== null && endTime > startTime) {
            return [startTime, endTime, settingsText];
          } else {
            if (startTime === null) {
              this.g(this.f?.s(startTimeText, lineCount));
            }
            if (endTime === null) {
              this.g(this.f?.t(endTimeText, lineCount));
            }
            if (startTime != null && endTime !== null && endTime > startTime) {
              this.g(this.f?.u(startTime, endTime, lineCount));
            }
          }
        }
        /**
         * @see {@link https://www.w3.org/TR/webvtt1/#region-settings-parsing}
         */
        p(settings, line) {
          let badValue;
          for (let i = 0; i < settings.length; i++) {
            if (SETTING_SEP_RE.test(settings[i])) {
              badValue = false;
              const [name, value] = settings[i].split(SETTING_SEP_RE);
              switch (name) {
                case "id":
                  this.d.id = value;
                  break;
                case "width":
                  const width = toPercentage(value);
                  if (width !== null)
                    this.d.width = width;
                  else
                    badValue = true;
                  break;
                case "lines":
                  const lines = toNumber(value);
                  if (lines !== null)
                    this.d.lines = lines;
                  else
                    badValue = true;
                  break;
                case "regionanchor":
                  const region = toCoords(value);
                  if (region !== null) {
                    this.d.regionAnchorX = region[0];
                    this.d.regionAnchorY = region[1];
                  } else
                    badValue = true;
                  break;
                case "viewportanchor":
                  const viewport = toCoords(value);
                  if (viewport !== null) {
                    this.d.viewportAnchorX = viewport[0];
                    this.d.viewportAnchorY = viewport[1];
                  } else
                    badValue = true;
                  break;
                case "scroll":
                  if (value === "up")
                    this.d.scroll = "up";
                  else
                    badValue = true;
                  break;
                default:
                  this.g(this.f?.v(name, value, line));
              }
              if (badValue) {
                this.g(this.f?.w(name, value, line));
              }
            }
          }
        }
        /**
         * @see {@link https://www.w3.org/TR/webvtt1/#cue-timings-and-settings-parsing}
         */
        o(settings, line) {
          let badValue;
          for (let i = 0; i < settings.length; i++) {
            badValue = false;
            if (SETTING_SEP_RE.test(settings[i])) {
              const [name, value] = settings[i].split(SETTING_SEP_RE);
              switch (name) {
                case "region":
                  const region = this.j[value];
                  if (region)
                    this.c.region = region;
                  break;
                case "vertical":
                  if (value === "lr" || value === "rl") {
                    this.c.vertical = value;
                    this.c.region = null;
                  } else
                    badValue = true;
                  break;
                case "line":
                  const [linePos, lineAlign] = value.split(COMMA);
                  if (linePos.includes(PERCENT_SIGN)) {
                    const percentage = toPercentage(linePos);
                    if (percentage !== null) {
                      this.c.line = percentage;
                      this.c.snapToLines = false;
                    } else
                      badValue = true;
                  } else {
                    const number = toFloat(linePos);
                    if (number !== null)
                      this.c.line = number;
                    else
                      badValue = true;
                  }
                  if (LINE_ALIGN_RE.test(lineAlign)) {
                    this.c.lineAlign = lineAlign;
                  } else if (lineAlign) {
                    badValue = true;
                  }
                  if (this.c.line !== "auto")
                    this.c.region = null;
                  break;
                case "position":
                  const [colPos, colAlign] = value.split(COMMA), position = toPercentage(colPos);
                  if (position !== null)
                    this.c.position = position;
                  else
                    badValue = true;
                  if (colAlign && POS_ALIGN_RE.test(colAlign)) {
                    this.c.positionAlign = colAlign;
                  } else if (colAlign) {
                    badValue = true;
                  }
                  break;
                case "size":
                  const size2 = toPercentage(value);
                  if (size2 !== null) {
                    this.c.size = size2;
                    if (size2 < 100)
                      this.c.region = null;
                  } else {
                    badValue = true;
                  }
                  break;
                case "align":
                  if (ALIGN_RE.test(value)) {
                    this.c.align = value;
                  } else {
                    badValue = true;
                  }
                  break;
                default:
                  this.g(this.f?.x(name, value, line));
              }
              if (badValue) {
                this.g(this.f?.y(name, value, line));
              }
            }
          }
        }
        g(error) {
          if (!error)
            return;
          this.m.push(error);
          if (this.h.strict) {
            this.h.cancel();
            throw error;
          } else {
            this.h.onError?.(error);
          }
        }
      };
      vttParser = /* @__PURE__ */ Object.freeze({
        __proto__: null,
        VTTBlock,
        VTTParser,
        default: createVTTParser,
        parseVTTTimestamp
      });
      DIGIT_RE = /[0-9]/;
      MULTI_SPACE_RE = /[\s\t]+/;
      TAG_NAME = {
        c: "span",
        i: "i",
        b: "b",
        u: "u",
        ruby: "ruby",
        rt: "rt",
        v: "span",
        lang: "span",
        timestamp: "span"
      };
      HTML_ENTITIES = {
        "&amp;": "&",
        "&lt;": "<",
        "&gt;": ">",
        "&quot;": '"',
        "&#39;": "'",
        "&nbsp;": "\xA0",
        "&lrm;": "\u200E",
        "&rlm;": "\u200F"
      };
      HTML_ENTITY_RE = /&(?:amp|lt|gt|quot|#(0+)?39|nbsp|lrm|rlm);/g;
      COLORS = /* @__PURE__ */ new Set([
        "white",
        "lime",
        "cyan",
        "red",
        "yellow",
        "magenta",
        "blue",
        "black"
      ]);
      BLOCK_TYPES = /* @__PURE__ */ new Set(Object.keys(TAG_NAME));
      STARTING_BOX = /* @__PURE__ */ Symbol(0);
      BOX_SIDES = ["top", "left", "right", "bottom"];
      POSITION_OVERRIDE = /* @__PURE__ */ Symbol(0);
      REGION_AXIS = ["-y", "+y", "-x", "+x"];
      CaptionsRenderer = class {
        constructor(overlay, init) {
          __publicField(this, "overlay");
          __publicField(this, "z");
          __publicField(this, "A", 0);
          __publicField(this, "C", "ltr");
          __publicField(this, "B", []);
          __publicField(this, "D", false);
          __publicField(this, "E");
          __publicField(this, "j", /* @__PURE__ */ new Map());
          __publicField(this, "l", /* @__PURE__ */ new Map());
          __publicField(this, "K", debounce2(() => {
            this.D = false;
            this.G();
            for (const el of this.j.values()) {
              el[STARTING_BOX] = null;
            }
            for (const el of this.l.values()) {
              if (el)
                el[STARTING_BOX] = null;
            }
            this.H(true);
          }, 50));
          this.overlay = overlay;
          this.dir = init?.dir ?? "ltr";
          overlay.setAttribute("translate", "yes");
          overlay.setAttribute("aria-live", "off");
          overlay.setAttribute("aria-atomic", "true");
          setPartAttr(overlay, "captions");
          this.G();
          this.E = new ResizeObserver(this.I.bind(this));
          this.E.observe(overlay);
        }
        /* Text direction. */
        get dir() {
          return this.C;
        }
        set dir(dir) {
          this.C = dir;
          setDataAttr(this.overlay, "dir", dir);
        }
        get currentTime() {
          return this.A;
        }
        set currentTime(time) {
          this.A = time;
          this.update();
        }
        changeTrack({ regions, cues }) {
          this.reset();
          this.J(regions);
          for (const cue of cues)
            this.l.set(cue, null);
          this.update();
        }
        addCue(cue) {
          this.l.set(cue, null);
          this.update();
        }
        removeCue(cue) {
          this.l.delete(cue);
          this.update();
        }
        update(forceUpdate = false) {
          this.H(forceUpdate);
        }
        reset() {
          this.l.clear();
          this.j.clear();
          this.B = [];
          this.overlay.textContent = "";
        }
        destroy() {
          this.reset();
          this.E.disconnect();
        }
        I() {
          this.D = true;
          this.K();
        }
        G() {
          this.z = createBox(this.overlay);
          setCSSVar(this.overlay, "overlay-width", this.z.width + "px");
          setCSSVar(this.overlay, "overlay-height", this.z.height + "px");
        }
        H(forceUpdate = false) {
          if (!this.l.size || this.D)
            return;
          let cue, activeCues = [...this.l.keys()].filter((cue2) => this.A >= cue2.startTime && this.A <= cue2.endTime).sort(
            (cueA, cueB) => cueA.startTime !== cueB.startTime ? cueA.startTime - cueB.startTime : cueA.endTime - cueB.endTime
          ), activeRegions = activeCues.map((cue2) => cue2.region);
          for (let i = 0; i < this.B.length; i++) {
            cue = this.B[i];
            if (activeCues[i] === cue)
              continue;
            if (cue.region && !activeRegions.includes(cue.region)) {
              const regionEl = this.j.get(cue.region.id);
              if (regionEl) {
                regionEl.removeAttribute("data-active");
                forceUpdate = true;
              }
            }
            const cueEl = this.l.get(cue);
            if (cueEl) {
              cueEl.remove();
              forceUpdate = true;
            }
          }
          for (let i = 0; i < activeCues.length; i++) {
            cue = activeCues[i];
            let cueEl = this.l.get(cue);
            if (!cueEl)
              this.l.set(cue, cueEl = this.L(cue));
            const regionEl = this.F(cue) && this.j.get(cue.region.id);
            if (regionEl && !regionEl.hasAttribute("data-active")) {
              requestAnimationFrame(() => setDataAttr(regionEl, "active"));
              forceUpdate = true;
            }
            if (!cueEl.isConnected) {
              (regionEl || this.overlay).append(cueEl);
              forceUpdate = true;
            }
          }
          if (forceUpdate) {
            const boxes = [], seen = /* @__PURE__ */ new Set();
            for (let i = activeCues.length - 1; i >= 0; i--) {
              cue = activeCues[i];
              if (seen.has(cue.region || cue))
                continue;
              const isRegion = this.F(cue), el = isRegion ? this.j.get(cue.region.id) : this.l.get(cue);
              if (isRegion) {
                boxes.push(positionRegion(this.z, cue.region, el, boxes));
              } else {
                boxes.push(positionCue(this.z, cue, el, boxes));
              }
              seen.add(isRegion ? cue.region : cue);
            }
          }
          updateTimedVTTCueNodes(this.overlay, this.A);
          this.B = activeCues;
        }
        J(regions) {
          if (!regions)
            return;
          for (const region of regions) {
            const el = this.M(region);
            this.j.set(region.id, el);
            this.overlay.append(el);
          }
        }
        M(region) {
          const el = document.createElement("div");
          setPartAttr(el, "region");
          setDataAttr(el, "id", region.id);
          setDataAttr(el, "scroll", region.scroll);
          setCSSVar(el, "region-width", region.width + "%");
          setCSSVar(el, "region-anchor-x", region.regionAnchorX);
          setCSSVar(el, "region-anchor-y", region.regionAnchorY);
          setCSSVar(el, "region-viewport-anchor-x", region.viewportAnchorX);
          setCSSVar(el, "region-viewport-anchor-y", region.viewportAnchorY);
          setCSSVar(el, "region-lines", region.lines);
          return el;
        }
        L(cue) {
          const display = document.createElement("div"), position = computeCuePosition(cue), positionAlignment = computeCuePositionAlignment(cue, this.C);
          setPartAttr(display, "cue-display");
          if (cue.vertical !== "")
            setDataAttr(display, "vertical");
          setCSSVar(display, "cue-text-align", cue.align);
          if (cue.style) {
            for (const prop2 of Object.keys(cue.style)) {
              display.style.setProperty(prop2, cue.style[prop2]);
            }
          }
          if (!this.F(cue)) {
            setCSSVar(
              display,
              "cue-writing-mode",
              cue.vertical === "" ? "horizontal-tb" : cue.vertical === "lr" ? "vertical-lr" : "vertical-rl"
            );
            if (!cue.style?.["--cue-width"]) {
              let maxSize = position;
              if (positionAlignment === "line-left") {
                maxSize = 100 - position;
              } else if (positionAlignment === "center" && position <= 50) {
                maxSize = position * 2;
              } else if (positionAlignment === "center" && position > 50) {
                maxSize = (100 - position) * 2;
              }
              const size2 = cue.size < maxSize ? cue.size : maxSize;
              if (cue.vertical === "")
                setCSSVar(display, "cue-width", size2 + "%");
              else
                setCSSVar(display, "cue-height", size2 + "%");
            }
          } else {
            setCSSVar(
              display,
              "cue-offset",
              `${position - (positionAlignment === "line-right" ? 100 : positionAlignment === "center" ? 50 : 0)}%`
            );
          }
          const el = document.createElement("div");
          setPartAttr(el, "cue");
          if (cue.id)
            setDataAttr(el, "id", cue.id);
          el.innerHTML = renderVTTCueString(cue);
          display.append(el);
          return display;
        }
        F(cue) {
          return cue.region && cue.size === 100 && cue.vertical === "" && cue.line === "auto";
        }
      };
    }
  });

  // frontend/node_modules/media-captions/dist/prod.js
  var prod_exports = {};
  __export(prod_exports, {
    CaptionsRenderer: () => CaptionsRenderer,
    ParseError: () => ParseError,
    ParseErrorCode: () => ParseErrorCode,
    TextCue: () => TextCue,
    VTTCue: () => VTTCue,
    VTTRegion: () => VTTRegion,
    createVTTCueTemplate: () => createVTTCueTemplate,
    parseByteStream: () => parseByteStream,
    parseResponse: () => parseResponse,
    parseText: () => parseText,
    parseTextStream: () => parseTextStream,
    parseVTTTimestamp: () => parseVTTTimestamp,
    renderVTTCueString: () => renderVTTCueString,
    renderVTTTokensString: () => renderVTTTokensString,
    tokenizeVTTCue: () => tokenizeVTTCue,
    updateTimedVTTCueNodes: () => updateTimedVTTCueNodes
  });
  var init_prod2 = __esm({
    "frontend/node_modules/media-captions/dist/prod.js"() {
      init_define_import_meta_env();
      init_prod();
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-CvezEmSQ.js
  var vidstack_CvezEmSQ_exports = {};
  __export(vidstack_CvezEmSQ_exports, {
    AUDIO_EXTENSIONS: () => AUDIO_EXTENSIONS,
    AUDIO_TYPES: () => AUDIO_TYPES,
    DASH_VIDEO_EXTENSIONS: () => DASH_VIDEO_EXTENSIONS,
    DASH_VIDEO_TYPES: () => DASH_VIDEO_TYPES,
    HLS_VIDEO_EXTENSIONS: () => HLS_VIDEO_EXTENSIONS,
    HLS_VIDEO_TYPES: () => HLS_VIDEO_TYPES,
    HTMLAirPlayAdapter: () => HTMLAirPlayAdapter,
    HTMLMediaProvider: () => HTMLMediaProvider,
    IS_CHROME: () => IS_CHROME,
    IS_IOS: () => IS_IOS,
    IS_IPHONE: () => IS_IPHONE,
    IS_SERVER: () => IS_SERVER2,
    ListSymbol: () => ListSymbol,
    QualitySymbol: () => QualitySymbol,
    RAFLoop: () => RAFLoop,
    TextTrack: () => TextTrack,
    TextTrackSymbol: () => TextTrackSymbol,
    TimeRange: () => TimeRange,
    VIDEO_EXTENSIONS: () => VIDEO_EXTENSIONS,
    VIDEO_TYPES: () => VIDEO_TYPES,
    VideoProvider: () => VideoProvider,
    appendParamsToURL: () => appendParamsToURL,
    assert: () => assert,
    canChangeVolume: () => canChangeVolume,
    canGoogleCastSrc: () => canGoogleCastSrc,
    canOrientScreen: () => canOrientScreen,
    canPlayAudioType: () => canPlayAudioType,
    canPlayHLSNatively: () => canPlayHLSNatively,
    canPlayVideoType: () => canPlayVideoType,
    canRotateScreen: () => canRotateScreen,
    canUsePictureInPicture: () => canUsePictureInPicture,
    canUseVideoPresentation: () => canUseVideoPresentation,
    clampNumber: () => clampNumber,
    coerceToError: () => coerceToError,
    findActiveCue: () => findActiveCue,
    getDownloadFile: () => getDownloadFile,
    getNumberOfDecimalPlaces: () => getNumberOfDecimalPlaces,
    getRequestCredentials: () => getRequestCredentials,
    getTimeRangesEnd: () => getTimeRangesEnd,
    getTimeRangesStart: () => getTimeRangesStart,
    isAudioSrc: () => isAudioSrc,
    isCueActive: () => isCueActive,
    isDASHSrc: () => isDASHSrc,
    isDASHSupported: () => isDASHSupported,
    isHLSSrc: () => isHLSSrc,
    isHLSSupported: () => isHLSSupported,
    isMediaStream: () => isMediaStream,
    isRemotionProvider: () => isRemotionProvider,
    isRemotionSrc: () => isRemotionSrc,
    isTrackCaptionKind: () => isTrackCaptionKind,
    isVideoSrc: () => isVideoSrc,
    loadScript: () => loadScript,
    normalizeTimeIntervals: () => normalizeTimeIntervals,
    parseJSONCaptionsFile: () => parseJSONCaptionsFile,
    preconnect: () => preconnect,
    provider_exports: () => provider_exports,
    provider_exports$1: () => provider_exports$1,
    round: () => round,
    updateTimeIntervals: () => updateTimeIntervals,
    watchActiveTextTrack: () => watchActiveTextTrack,
    watchCueTextChange: () => watchCueTextChange
  });
  function canOrientScreen() {
    return canRotateScreen() && isFunction(screen.orientation.unlock);
  }
  function canRotateScreen() {
    return !IS_SERVER2 && !isUndefined(window.screen.orientation) && !isUndefined(window.screen.orientation.lock);
  }
  function canPlayAudioType(audio, type) {
    if (IS_SERVER2) return false;
    if (!audio) audio = document.createElement("audio");
    return audio.canPlayType(type).length > 0;
  }
  function canPlayVideoType(video, type) {
    if (IS_SERVER2) return false;
    if (!video) video = document.createElement("video");
    return video.canPlayType(type).length > 0;
  }
  function canPlayHLSNatively(video) {
    if (IS_SERVER2) return false;
    if (!video) video = document.createElement("video");
    return video.canPlayType("application/vnd.apple.mpegurl").length > 0;
  }
  function canUsePictureInPicture(video) {
    if (IS_SERVER2) return false;
    return !!document.pictureInPictureEnabled && !video?.disablePictureInPicture;
  }
  function canUseVideoPresentation(video) {
    if (IS_SERVER2) return false;
    return isFunction(video?.webkitSupportsPresentationMode) && isFunction(video?.webkitSetPresentationMode);
  }
  async function canChangeVolume() {
    const video = document.createElement("video");
    video.volume = 0.5;
    await waitTimeout(0);
    return video.volume === 0.5;
  }
  function getMediaSource() {
    return IS_SERVER2 ? void 0 : window?.ManagedMediaSource ?? window?.MediaSource ?? window?.WebKitMediaSource;
  }
  function getSourceBuffer() {
    return IS_SERVER2 ? void 0 : window?.SourceBuffer ?? window?.WebKitSourceBuffer;
  }
  function isHLSSupported() {
    if (IS_SERVER2) return false;
    const MediaSource = getMediaSource();
    if (isUndefined(MediaSource)) return false;
    const isTypeSupported = MediaSource && isFunction(MediaSource.isTypeSupported) && MediaSource.isTypeSupported('video/mp4; codecs="avc1.42E01E,mp4a.40.2"');
    const SourceBuffer = getSourceBuffer();
    const isSourceBufferValid = isUndefined(SourceBuffer) || !isUndefined(SourceBuffer.prototype) && isFunction(SourceBuffer.prototype.appendBuffer) && isFunction(SourceBuffer.prototype.remove);
    return !!isTypeSupported && !!isSourceBufferValid;
  }
  function isDASHSupported() {
    return isHLSSupported();
  }
  function getTimeRangesStart(range) {
    if (!range.length) return null;
    let min2 = range.start(0);
    for (let i = 1; i < range.length; i++) {
      const value = range.start(i);
      if (value < min2) min2 = value;
    }
    return min2;
  }
  function getTimeRangesEnd(range) {
    if (!range.length) return null;
    let max2 = range.end(0);
    for (let i = 1; i < range.length; i++) {
      const value = range.end(i);
      if (value > max2) max2 = value;
    }
    return max2;
  }
  function normalizeTimeIntervals(intervals) {
    if (intervals.length <= 1) return intervals;
    intervals.sort((a, b) => a[0] - b[0]);
    let normalized = [], current = intervals[0];
    for (let i = 1; i < intervals.length; i++) {
      const next = intervals[i];
      if (current[1] >= next[0] - 1) current = [current[0], Math.max(current[1], next[1])];
      else {
        normalized.push(current);
        current = next;
      }
    }
    normalized.push(current);
    return normalized;
  }
  function updateTimeIntervals(intervals, interval, value) {
    let start = interval[0], end = interval[1];
    if (value < start) return [value, -1];
    else if (value === start) return interval;
    else if (start === -1) {
      interval[0] = value;
      return interval;
    } else if (value > start) {
      interval[1] = value;
      if (end === -1) intervals.push(interval);
    }
    normalizeTimeIntervals(intervals);
    return interval;
  }
  function isAudioSrc({ src, type }) {
    return isString(src) ? AUDIO_EXTENSIONS.test(src) || AUDIO_TYPES.has(type) || src.startsWith("blob:") && type === "audio/object" : type === "audio/object";
  }
  function isVideoSrc(src) {
    return isString(src.src) ? VIDEO_EXTENSIONS.test(src.src) || VIDEO_TYPES.has(src.type) || src.src.startsWith("blob:") && src.type === "video/object" || isHLSSrc(src) && (IS_SERVER2 || canPlayHLSNatively()) : src.type === "video/object";
  }
  function isHLSSrc({ src, type }) {
    return isString(src) && HLS_VIDEO_EXTENSIONS.test(src) || HLS_VIDEO_TYPES.has(type);
  }
  function isDASHSrc({ src, type }) {
    return isString(src) && DASH_VIDEO_EXTENSIONS.test(src) || DASH_VIDEO_TYPES.has(type);
  }
  function canGoogleCastSrc(src) {
    return isString(src.src) && (isAudioSrc(src) || isVideoSrc(src) || isHLSSrc(src) || isDASHSrc(src));
  }
  function isMediaStream(src) {
    return !IS_SERVER2 && typeof window.MediaStream !== "undefined" && src instanceof window.MediaStream;
  }
  function appendParamsToURL(baseUrl, params) {
    const url = new URL(baseUrl);
    for (const key2 of Object.keys(params)) url.searchParams.set(key2, params[key2] + "");
    return url.toString();
  }
  function preconnect(url, rel = "preconnect") {
    if (IS_SERVER2) return false;
    if (!isNull(document.querySelector(`link[href="${url}"]`))) return true;
    const link = document.createElement("link");
    link.rel = rel;
    link.href = url;
    link.crossOrigin = "true";
    document.head.append(link);
    return true;
  }
  function loadScript(src) {
    if (pendingRequests[src]) return pendingRequests[src].promise;
    const promise = deferredPromise();
    if (!isNull(document.querySelector(`script[src="${src}"]`))) {
      promise.resolve();
      return promise.promise;
    }
    pendingRequests[src] = promise;
    const script = document.createElement("script");
    script.src = src;
    script.onload = () => {
      promise.resolve();
      delete pendingRequests[src];
    };
    script.onerror = () => {
      promise.reject();
      delete pendingRequests[src];
    };
    setTimeout(() => document.head.append(script), 0);
    return promise.promise;
  }
  function getRequestCredentials(crossOrigin) {
    return crossOrigin === "use-credentials" ? "include" : isString(crossOrigin) ? "same-origin" : void 0;
  }
  function getDownloadFile({ title, src, download }) {
    const url = isBoolean(download) || download === "" ? src.src : isString(download) ? download : download?.url;
    if (!isValidFileDownload({
      url,
      src,
      download
    })) return null;
    return {
      url,
      name: !isBoolean(download) && !isString(download) && download?.filename || title.toLowerCase() || "media"
    };
  }
  function isValidFileDownload({ url, src, download }) {
    return isString(url) && (download && download !== true || isAudioSrc(src) || isVideoSrc(src));
  }
  function findActiveCue(cues, time) {
    for (let i = 0, len = cues.length; i < len; i++) if (isCueActive(cues[i], time)) return cues[i];
    return null;
  }
  function isCueActive(cue, time) {
    return time >= cue.startTime && time < cue.endTime;
  }
  function watchActiveTextTrack(tracks, kind, onChange) {
    let currentTrack = null, scope = getScope();
    function onModeChange() {
      const kinds = isString(kind) ? [kind] : kind, track = tracks.toArray().find((track2) => kinds.includes(track2.kind) && track2.mode === "showing");
      if (track === currentTrack) return;
      if (!track) {
        onChange(null);
        currentTrack = null;
        return;
      }
      if (track.readyState == 2) onChange(track);
      else {
        onChange(null);
        scoped(() => {
          const off = listenEvent(track, "load", () => {
            onChange(track);
            off();
          }, { once: true });
        }, scope);
      }
      currentTrack = track;
    }
    onModeChange();
    return listenEvent(tracks, "mode-change", onModeChange);
  }
  function watchCueTextChange(tracks, kind, callback) {
    watchActiveTextTrack(tracks, kind, (track) => {
      if (!track) {
        callback("");
        return;
      }
      const onCueChange = () => {
        const activeCue = track?.activeCues[0];
        callback(activeCue?.text || "");
      };
      onCueChange();
      listenEvent(track, "cue-change", onCueChange);
    });
  }
  function isTrackCaptionKind(track) {
    return captionRE.test(track.kind);
  }
  function parseJSONCaptionsFile(json, Cue, Region) {
    const content = isString(json) ? JSON.parse(json) : json;
    let regions = [], cues = [];
    if (content.regions && Region) regions = content.regions.map((region) => Object.assign(new Region(), region));
    if (content.cues || isArray(content)) cues = (isArray(content) ? content : content.cues).filter((content2) => isNumber(content2.startTime) && isNumber(content2.endTime)).map((cue) => Object.assign(new Cue(0, 0, ""), cue));
    return {
      regions,
      cues
    };
  }
  function round(num, decimalPlaces = 2) {
    return Number(num.toFixed(decimalPlaces));
  }
  function getNumberOfDecimalPlaces(num) {
    return String(num).split(".")[1]?.length ?? 0;
  }
  function clampNumber(min2, value, max2) {
    return Math.max(min2, Math.min(max2, value));
  }
  function getOrCreateAudioCtx() {
    return audioContext ?? (audioContext = new AudioContext());
  }
  function createGainNode() {
    const audioCtx = getOrCreateAudioCtx(), gainNode = audioCtx.createGain();
    gainNode.connect(audioCtx.destination);
    gainNodes.push(gainNode);
    return gainNode;
  }
  function createElementSource(el, gainNode) {
    const src = getOrCreateAudioCtx().createMediaElementSource(el);
    if (gainNode) src.connect(gainNode);
    elAudioSources.push(src);
    return src;
  }
  function destroyGainNode(node) {
    const idx = gainNodes.indexOf(node);
    if (idx !== -1) {
      gainNodes.splice(idx, 1);
      node.disconnect();
      freeAudioCtxWhenAllResourcesFreed();
    }
  }
  function destroyElementSource(src) {
    const idx = elAudioSources.indexOf(src);
    if (idx !== -1) {
      elAudioSources.splice(idx, 1);
      src.disconnect();
      freeAudioCtxWhenAllResourcesFreed();
    }
  }
  function freeAudioCtxWhenAllResourcesFreed() {
    if (audioContext && gainNodes.length === 0 && elAudioSources.length === 0) audioContext.close().then(() => {
      audioContext = null;
    });
  }
  function determinePageState(event) {
    if (IS_SERVER2) return "hidden";
    if (event?.type === "blur" || document.visibilityState === "hidden") return "hidden";
    if (document.hasFocus()) return "active";
    return "passive";
  }
  function findTextTrackElement(video, track) {
    return Array.from(video.children).find((el) => el.track === track);
  }
  function getLangName(langCode) {
    try {
      return new Intl.DisplayNames(navigator.languages, { type: "language" }).of(langCode) ?? null;
    } catch (err) {
      return null;
    }
  }
  function coerceToError(error) {
    return error instanceof Error ? error : Error(typeof error === "string" ? error : JSON.stringify(error));
  }
  function assert(condition, message) {
    if (!condition) throw Error(message || "Assertion failed.");
  }
  async function importDASH(loader, callbacks = {}) {
    if (isUndefined(loader)) return void 0;
    callbacks.onLoadStart?.();
    if (isDASHConstructor(loader)) {
      callbacks.onLoaded?.(loader);
      return loader;
    }
    if (isDASHNamespace(loader)) {
      const ctor = loader.MediaPlayer;
      callbacks.onLoaded?.(ctor);
      return ctor;
    }
    try {
      const ctor = (await loader())?.default;
      if (isDASHNamespace(ctor)) {
        callbacks.onLoaded?.(ctor.MediaPlayer);
        return ctor.MediaPlayer;
      }
      if (ctor) callbacks.onLoaded?.(ctor);
      else throw Error("");
      return ctor;
    } catch (err) {
      callbacks.onLoadError?.(err);
    }
  }
  async function loadDASHScript(src, callbacks = {}) {
    if (!isString(src)) return void 0;
    callbacks.onLoadStart?.();
    try {
      await loadScript(src);
      if (!isFunction(window.dashjs.MediaPlayer)) throw Error("");
      const ctor = window.dashjs.MediaPlayer;
      callbacks.onLoaded?.(ctor);
      return ctor;
    } catch (err) {
      callbacks.onLoadError?.(err);
    }
  }
  function isDASHConstructor(value) {
    return value && value.prototype && value.prototype !== Function;
  }
  function isDASHNamespace(value) {
    return value && "MediaPlayer" in value;
  }
  function isRemotionProvider(provider) {
    return provider?.$$PROVIDER_TYPE === "REMOTION";
  }
  function isRemotionSrc(src) {
    return src?.type === "video/remotion";
  }
  var IS_SERVER2, UA, IS_IOS, IS_IPHONE, IS_CHROME, IS_SAFARI, _ranges, _a5, TimeRange, AUDIO_EXTENSIONS, AUDIO_TYPES, VIDEO_EXTENSIONS, VIDEO_TYPES, HLS_VIDEO_EXTENSIONS, DASH_VIDEO_EXTENSIONS, HLS_VIDEO_TYPES, DASH_VIDEO_TYPES, pendingRequests, TextTrackSymbol, _a6, _b, _c, _d, _canLoad, _currentTime, _mode, _metadata, _regions, _cues, _activeCues, _cueIds, _TextTrack_instances, parseContent_fn, load_fn, ready_fn, error_fn, parseJSON_fn, activeCuesChanged_fn, _e, TextTrack, captionRE, ListSymbol, QualitySymbol, audioContext, gainNodes, elAudioSources, _media, _onChange, _gainNode, _srcAudioNode, _AudioGain_instances, destroySrcNode_fn, destroyGainNode_fn, _a7, AudioGain, PAGE_EVENTS, _state, _visibility, _safariBeforeUnloadTimeout, _PageVisibility_instances, handlePageEvent_fn, _a8, PageVisibility, _id, _callback, _RAFLoop_instances, loop_fn, _a9, RAFLoop, _provider, _ctx, _waiting, _attachedLoadStart, _attachedCanPlay, _timeRAF, _pageVisibility, _events, _HTMLMediaEvents_instances, media_get, onDispose_fn, _lastSeenTime, _seekedTo, onAnimationFrame_fn, attachInitialListeners_fn, attachLoadStartListeners_fn, attachCanPlayListeners_fn, _devHandlers, _handleDevEvent, attachEventListener_fn, onDevEvent_fn, updateCurrentTime_fn, onLoadStart_fn, onAbort_fn, onEmptied_fn, onLoadedData_fn, onLoadedMetadata_fn, getCanPlayDetail_fn, onPlay_fn, onPause_fn, onCanPlay_fn, onCanPlayThrough_fn, onPlaying_fn, onStalled_fn, onWaiting_fn, onEnded_fn, attachTimeUpdate_fn, onTimeUpdate_fn, onDurationChange_fn, onVolumeChange_fn, onSeeked_fn, onSeeking_fn, onProgress_fn, onSuspend_fn, onRateChange_fn, onError_fn, _a10, HTMLMediaEvents, _provider2, _ctx2, _NativeAudioTracks_instances, nativeTracks_get, onAddNativeTrack_fn, onRemoveNativeTrack_fn, onChangeNativeTrack_fn, getEnabledNativeTrack_fn, onChangeTrack_fn, _a11, NativeAudioTracks, _HTMLMediaProvider_instances, appendMediaFragment_fn, _a12, HTMLMediaProvider, _media2, _ctx3, _state2, _supported, _HTMLRemotePlaybackAdapter_instances, setup_fn, watchSupported_fn, onStateChange_fn, _a13, HTMLRemotePlaybackAdapter, HTMLAirPlayAdapter, _video, _ctx4, _NativeHLSTextTracks_instances, onAddTrack_fn, onDispose_fn2, _a14, NativeHLSTextTracks, _video2, _media3, _VideoPictureInPicture_instances, onEnter_fn, onExit_fn, _onChange2, _a15, VideoPictureInPicture, _video3, _media4, _mode2, _VideoPresentation_instances, onModeChange_fn, _a16, VideoPresentation, _presentation, _a17, FullscreenPresentationAdapter, _presentation2, _a18, PIPPresentationAdapter, provider_exports$1, VideoProvider, toDOMEventType, _video4, _ctx5, _instance, _callbacks, _stopLiveSync, _DASHController_instances, createDOMEvent_fn, liveSync_fn, liveSyncPosition_fn, dispatchDASHEvent_fn, _currentTrack, _cueTracker, onTextFragmentLoaded_fn, onTextTracksAdded_fn, onTrackChange_fn, onQualityChange_fn, onManifestLoaded_fn, onError_fn2, onFragmentLoadStart_fn, onFragmentLoadComplete_fn, _retryLoadingTimer, onNetworkError_fn, clearRetryTimer_fn, onFatalError_fn, enableAutoQuality_fn, switchAutoBitrate_fn, onUserQualityChange_fn, onUserAudioChange_fn, reset_fn, _a19, DASHController, _lib, _ctx6, _callback2, _DASHLibLoader_instances, startLoading_fn, onLoadStart_fn2, onLoaded_fn, onLoadError_fn, _a20, DASHLibLoader, provider_exports, JS_DELIVR_CDN, _ctor, _controller2, _a21, _library, DASHProvider;
  var init_vidstack_CvezEmSQ = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-CvezEmSQ.js"() {
      "use client";
      init_define_import_meta_env();
      init_vidstack_BLEs9rMG();
      init_vidstack_B9fTkxQO();
      IS_SERVER2 = typeof document === "undefined";
      UA = IS_SERVER2 ? "" : navigator?.userAgent.toLowerCase() || "";
      IS_IOS = !IS_SERVER2 && /iphone|ipad|ipod|ios|crios|fxios/i.test(UA);
      IS_IPHONE = !IS_SERVER2 && /(iphone|ipod)/gi.test(navigator?.platform || "");
      IS_CHROME = !IS_SERVER2 && !!window.chrome;
      !IS_SERVER2 && /crios/i.test(UA);
      IS_SAFARI = !IS_SERVER2 && (!!window.safari || IS_IOS);
      TimeRange = (_a5 = class {
        constructor(start, end) {
          __privateAdd(this, _ranges);
          if (isArray(start)) __privateSet(this, _ranges, start);
          else if (!isUndefined(start) && !isUndefined(end)) __privateSet(this, _ranges, [[start, end]]);
          else __privateSet(this, _ranges, []);
        }
        get length() {
          return __privateGet(this, _ranges).length;
        }
        start(index) {
          return __privateGet(this, _ranges)[index][0] ?? Infinity;
        }
        end(index) {
          return __privateGet(this, _ranges)[index][1] ?? Infinity;
        }
      }, _ranges = new WeakMap(), _a5);
      AUDIO_EXTENSIONS = /\.(m4a|m4b|mp4a|mpga|mp2|mp2a|mp3|m2a|m3a|wav|weba|aac|oga|spx|flac)($|\?)/i;
      AUDIO_TYPES = /* @__PURE__ */ new Set([
        "audio/mpeg",
        "audio/ogg",
        "audio/3gp",
        "audio/mp3",
        "audio/webm",
        "audio/flac",
        "audio/m4a",
        "audio/m4b",
        "audio/mp4a",
        "audio/mp4"
      ]);
      VIDEO_EXTENSIONS = /\.(mp4|og[gv]|webm|mov|m4v)(#t=[,\d+]+)?($|\?)/i;
      VIDEO_TYPES = /* @__PURE__ */ new Set([
        "video/mp4",
        "video/webm",
        "video/3gp",
        "video/ogg",
        "video/avi",
        "video/mpeg"
      ]);
      HLS_VIDEO_EXTENSIONS = /\.(m3u8)($|\?)/i;
      DASH_VIDEO_EXTENSIONS = /\.(mpd)($|\?)/i;
      HLS_VIDEO_TYPES = /* @__PURE__ */ new Set([
        "application/vnd.apple.mpegurl",
        "audio/mpegurl",
        "audio/x-mpegurl",
        "application/x-mpegurl",
        "video/x-mpegurl",
        "video/mpegurl",
        "application/mpegurl"
      ]);
      DASH_VIDEO_TYPES = /* @__PURE__ */ new Set(["application/dash+xml"]);
      pendingRequests = {};
      TextTrackSymbol = {
        crossOrigin: /* @__PURE__ */ Symbol(0),
        readyState: /* @__PURE__ */ Symbol(0),
        updateActiveCues: /* @__PURE__ */ Symbol(0),
        canLoad: /* @__PURE__ */ Symbol(0),
        onModeChange: /* @__PURE__ */ Symbol(0),
        native: /* @__PURE__ */ Symbol(0),
        nativeHLS: /* @__PURE__ */ Symbol(0)
      };
      TextTrack = (_e = class extends EventsTarget {
        constructor(init) {
          super();
          __privateAdd(this, _TextTrack_instances);
          __publicField(this, "src");
          __publicField(this, "content");
          __publicField(this, "type");
          __publicField(this, "encoding");
          __publicField(this, "id", "");
          __publicField(this, "label", "");
          __publicField(this, "language", "");
          __publicField(this, "kind");
          __publicField(this, "default", false);
          __privateAdd(this, _canLoad, false);
          __privateAdd(this, _currentTime, 0);
          __privateAdd(this, _mode, "disabled");
          __privateAdd(this, _metadata, {});
          __privateAdd(this, _regions, []);
          __privateAdd(this, _cues, []);
          __privateAdd(this, _activeCues, []);
          __privateAdd(this, _cueIds, /* @__PURE__ */ new Set());
          /** @internal */
          __publicField(this, _d, 0);
          /** @internal */
          __publicField(this, _c);
          /** @internal */
          __publicField(this, _b, null);
          /** @internal */
          __publicField(this, _a6, null);
          for (const prop2 of Object.keys(init)) this[prop2] = init[prop2];
          if (!this.type) this.type = "vtt";
          if (!IS_SERVER2 && init.content) __privateMethod(this, _TextTrack_instances, parseContent_fn).call(this, init);
          else if (!init.src) this[TextTrackSymbol.readyState] = 2;
        }
        static createId(track) {
          return `vds-${track.type}-${track.kind}-${track.src ?? track.label ?? "?"}`;
        }
        get metadata() {
          return __privateGet(this, _metadata);
        }
        get regions() {
          return __privateGet(this, _regions);
        }
        get cues() {
          return __privateGet(this, _cues);
        }
        get activeCues() {
          return __privateGet(this, _activeCues);
        }
        /**
        * - 0: Not Loading
        * - 1: Loading
        * - 2: Ready
        * - 3: Error
        */
        get readyState() {
          return this[TextTrackSymbol.readyState];
        }
        get mode() {
          return __privateGet(this, _mode);
        }
        set mode(mode) {
          this.setMode(mode);
        }
        addCue(cue, trigger) {
          if (cue.id) {
            if (__privateGet(this, _cueIds).has(cue.id)) return;
            __privateGet(this, _cueIds).add(cue.id);
          }
          let i = 0, length = __privateGet(this, _cues).length;
          for (i = 0; i < length; i++) if (cue.endTime <= __privateGet(this, _cues)[i].startTime) break;
          if (i === length) __privateGet(this, _cues).push(cue);
          else __privateGet(this, _cues).splice(i, 0, cue);
          if (!(cue instanceof TextTrackCue)) this[TextTrackSymbol.native]?.track.addCue(cue);
          this.dispatchEvent(new DOMEvent("add-cue", {
            detail: cue,
            trigger
          }));
          if (isCueActive(cue, __privateGet(this, _currentTime))) this[TextTrackSymbol.updateActiveCues](__privateGet(this, _currentTime), trigger);
        }
        removeCue(cue, trigger) {
          const index = __privateGet(this, _cues).indexOf(cue);
          if (index >= 0) {
            const isActive = __privateGet(this, _activeCues).includes(cue);
            if (cue.id) __privateGet(this, _cueIds).delete(cue.id);
            __privateGet(this, _cues).splice(index, 1);
            this[TextTrackSymbol.native]?.track.removeCue(cue);
            this.dispatchEvent(new DOMEvent("remove-cue", {
              detail: cue,
              trigger
            }));
            if (isActive) this[TextTrackSymbol.updateActiveCues](__privateGet(this, _currentTime), trigger);
          }
        }
        setMode(mode, trigger) {
          if (__privateGet(this, _mode) === mode) return;
          __privateSet(this, _mode, mode);
          if (mode === "disabled") {
            __privateSet(this, _activeCues, []);
            __privateMethod(this, _TextTrack_instances, activeCuesChanged_fn).call(this);
          } else if (this.readyState === 2) this[TextTrackSymbol.updateActiveCues](__privateGet(this, _currentTime), trigger);
          else __privateMethod(this, _TextTrack_instances, load_fn).call(this);
          this.dispatchEvent(new DOMEvent("mode-change", {
            detail: this,
            trigger
          }));
          this[TextTrackSymbol.onModeChange]?.();
        }
        /** @internal */
        [(_d = TextTrackSymbol.readyState, _c = TextTrackSymbol.crossOrigin, _b = TextTrackSymbol.onModeChange, _a6 = TextTrackSymbol.native, TextTrackSymbol.updateActiveCues)](currentTime, trigger) {
          __privateSet(this, _currentTime, currentTime);
          if (this.mode === "disabled" || !__privateGet(this, _cues).length) return;
          const activeCues = [];
          for (let i = 0, length = __privateGet(this, _cues).length; i < length; i++) {
            const cue = __privateGet(this, _cues)[i];
            if (isCueActive(cue, currentTime)) activeCues.push(cue);
          }
          let changed = activeCues.length !== __privateGet(this, _activeCues).length;
          if (!changed) {
            for (let i = 0; i < activeCues.length; i++) if (!__privateGet(this, _activeCues).includes(activeCues[i])) {
              changed = true;
              break;
            }
          }
          __privateSet(this, _activeCues, activeCues);
          if (changed) __privateMethod(this, _TextTrack_instances, activeCuesChanged_fn).call(this, trigger);
        }
        /** @internal */
        [TextTrackSymbol.canLoad]() {
          __privateSet(this, _canLoad, true);
          if (__privateGet(this, _mode) !== "disabled") __privateMethod(this, _TextTrack_instances, load_fn).call(this);
        }
      }, _canLoad = new WeakMap(), _currentTime = new WeakMap(), _mode = new WeakMap(), _metadata = new WeakMap(), _regions = new WeakMap(), _cues = new WeakMap(), _activeCues = new WeakMap(), _cueIds = new WeakMap(), _TextTrack_instances = new WeakSet(), parseContent_fn = function(init) {
        Promise.resolve().then(() => (init_prod2(), prod_exports)).then(({ parseText: parseText2, VTTCue: VTTCue2, VTTRegion: VTTRegion2 }) => {
          if (!isString(init.content) || init.type === "json") {
            __privateMethod(this, _TextTrack_instances, parseJSON_fn).call(this, init.content, VTTCue2, VTTRegion2);
            if (this.readyState !== 3) __privateMethod(this, _TextTrack_instances, ready_fn).call(this);
          } else parseText2(init.content.split("\n").map((line) => line.trim()).join("\n").trim(), { type: init.type }).then(({ cues, regions }) => {
            __privateSet(this, _cues, cues);
            __privateSet(this, _regions, regions);
            __privateMethod(this, _TextTrack_instances, ready_fn).call(this);
          });
        });
      }, load_fn = async function() {
        if (!__privateGet(this, _canLoad) || this[TextTrackSymbol.readyState] > 0) return;
        this[TextTrackSymbol.readyState] = 1;
        this.dispatchEvent(new DOMEvent("load-start"));
        if (!this.src) {
          __privateMethod(this, _TextTrack_instances, ready_fn).call(this);
          return;
        }
        try {
          const { parseResponse: parseResponse2, VTTCue: VTTCue2, VTTRegion: VTTRegion2 } = await Promise.resolve().then(() => (init_prod2(), prod_exports)), crossOrigin = this[TextTrackSymbol.crossOrigin]?.();
          const response = fetch(this.src, {
            headers: this.type === "json" ? { "Content-Type": "application/json" } : void 0,
            credentials: getRequestCredentials(crossOrigin)
          });
          if (this.type === "json") __privateMethod(this, _TextTrack_instances, parseJSON_fn).call(this, await (await response).text(), VTTCue2, VTTRegion2);
          else {
            const { errors, metadata, regions, cues } = await parseResponse2(response, {
              type: this.type,
              encoding: this.encoding
            });
            if (errors[0]?.code === 0) throw errors[0];
            else {
              __privateSet(this, _metadata, metadata);
              __privateSet(this, _regions, regions);
              __privateSet(this, _cues, cues);
            }
          }
          __privateMethod(this, _TextTrack_instances, ready_fn).call(this);
        } catch (error) {
          __privateMethod(this, _TextTrack_instances, error_fn).call(this, error);
        }
      }, ready_fn = function() {
        this[TextTrackSymbol.readyState] = 2;
        if (!this.src || this.type !== "vtt") {
          const native = this[TextTrackSymbol.native];
          if (native && !native.managed) for (const cue of __privateGet(this, _cues)) native.track.addCue(cue);
        }
        const loadEvent = new DOMEvent("load");
        this[TextTrackSymbol.updateActiveCues](__privateGet(this, _currentTime), loadEvent);
        this.dispatchEvent(loadEvent);
      }, error_fn = function(error) {
        this[TextTrackSymbol.readyState] = 3;
        this.dispatchEvent(new DOMEvent("error", { detail: error }));
      }, parseJSON_fn = function(json, VTTCue2, VTTRegion2) {
        try {
          const { regions, cues } = parseJSONCaptionsFile(json, VTTCue2, VTTRegion2);
          __privateSet(this, _regions, regions);
          __privateSet(this, _cues, cues);
        } catch (error) {
          __privateMethod(this, _TextTrack_instances, error_fn).call(this, error);
        }
      }, activeCuesChanged_fn = function(trigger) {
        this.dispatchEvent(new DOMEvent("cue-change", { trigger }));
      }, _e);
      captionRE = /captions|subtitles/;
      ListSymbol = {
        add: /* @__PURE__ */ Symbol(0),
        remove: /* @__PURE__ */ Symbol(0),
        reset: /* @__PURE__ */ Symbol(0),
        select: /* @__PURE__ */ Symbol(0),
        readonly: /* @__PURE__ */ Symbol(0),
        setReadonly: /* @__PURE__ */ Symbol(0),
        onReset: /* @__PURE__ */ Symbol(0),
        onRemove: /* @__PURE__ */ Symbol(0),
        onUserSelect: /* @__PURE__ */ Symbol(0)
      };
      QualitySymbol = {
        setAuto: /* @__PURE__ */ Symbol(0),
        enableAuto: /* @__PURE__ */ Symbol(0)
      };
      audioContext = null;
      gainNodes = [];
      elAudioSources = [];
      AudioGain = (_a7 = class {
        constructor(media, onChange) {
          __privateAdd(this, _AudioGain_instances);
          __privateAdd(this, _media);
          __privateAdd(this, _onChange);
          __privateAdd(this, _gainNode, null);
          __privateAdd(this, _srcAudioNode, null);
          __privateSet(this, _media, media);
          __privateSet(this, _onChange, onChange);
        }
        get currentGain() {
          return __privateGet(this, _gainNode)?.gain?.value ?? null;
        }
        get supported() {
          return true;
        }
        setGain(gain) {
          const currGain = this.currentGain;
          if (gain === this.currentGain) return;
          if (gain === 1 && currGain !== 1) {
            this.removeGain();
            return;
          }
          if (!__privateGet(this, _gainNode)) {
            __privateSet(this, _gainNode, createGainNode());
            if (__privateGet(this, _srcAudioNode)) __privateGet(this, _srcAudioNode).connect(__privateGet(this, _gainNode));
          }
          if (!__privateGet(this, _srcAudioNode)) __privateSet(this, _srcAudioNode, createElementSource(__privateGet(this, _media), __privateGet(this, _gainNode)));
          __privateGet(this, _gainNode).gain.value = gain;
          __privateGet(this, _onChange).call(this, gain);
        }
        removeGain() {
          if (!__privateGet(this, _gainNode)) return;
          if (__privateGet(this, _srcAudioNode)) __privateGet(this, _srcAudioNode).connect(getOrCreateAudioCtx().destination);
          __privateMethod(this, _AudioGain_instances, destroyGainNode_fn).call(this);
          __privateGet(this, _onChange).call(this, null);
        }
        destroy() {
          __privateMethod(this, _AudioGain_instances, destroySrcNode_fn).call(this);
          __privateMethod(this, _AudioGain_instances, destroyGainNode_fn).call(this);
        }
      }, _media = new WeakMap(), _onChange = new WeakMap(), _gainNode = new WeakMap(), _srcAudioNode = new WeakMap(), _AudioGain_instances = new WeakSet(), destroySrcNode_fn = function() {
        if (!__privateGet(this, _srcAudioNode)) return;
        try {
          destroyElementSource(__privateGet(this, _srcAudioNode));
        } catch (e2) {
        } finally {
          __privateSet(this, _srcAudioNode, null);
        }
      }, destroyGainNode_fn = function() {
        if (!__privateGet(this, _gainNode)) return;
        try {
          destroyGainNode(__privateGet(this, _gainNode));
        } catch (e2) {
        } finally {
          __privateSet(this, _gainNode, null);
        }
      }, _a7);
      PAGE_EVENTS = [
        "focus",
        "blur",
        "visibilitychange",
        "pageshow",
        "pagehide"
      ];
      PageVisibility = (_a8 = class {
        constructor() {
          __privateAdd(this, _PageVisibility_instances);
          __privateAdd(this, _state, signal(determinePageState()));
          __privateAdd(this, _visibility, signal(IS_SERVER2 ? "visible" : document.visibilityState));
          __privateAdd(this, _safariBeforeUnloadTimeout);
        }
        connect() {
          const events = new EventsController(window), handlePageEvent = __privateMethod(this, _PageVisibility_instances, handlePageEvent_fn).bind(this);
          for (const eventType of PAGE_EVENTS) events.add(eventType, handlePageEvent);
          if (IS_SAFARI) events.add("beforeunload", (event) => {
            __privateSet(this, _safariBeforeUnloadTimeout, setTimeout(() => {
              if (!(event.defaultPrevented || event.returnValue.length > 0)) {
                __privateGet(this, _state).set("hidden");
                __privateGet(this, _visibility).set("hidden");
              }
            }, 0));
          });
        }
        /**
        * The current page state. Important to note we only account for a subset of page states, as
        * the rest aren't valuable to the player at the moment.
        *
        * - **active:** A page is in the active state if it is visible and has input focus.
        * - **passive:** A page is in the passive state if it is visible and does not have input focus.
        * - **hidden:** A page is in the hidden state if it is not visible.
        *
        * @see https://developers.google.com/web/updates/2018/07/page-lifecycle-api#states
        */
        get pageState() {
          return __privateGet(this, _state).call(this);
        }
        /**
        * The current document visibility state.
        *
        * - **visible:** The page content may be at least partially visible. In practice, this means that
        * the page is the foreground tab of a non-minimized window.
        * - **hidden:** The page content is not visible to the user. In practice this means that the
        * document is either a background tab or part of a minimized window, or the OS screen lock is
        * active.
        *
        * @see https://developer.mozilla.org/en-US/docs/Web/API/Document/visibilityState
        */
        get visibility() {
          return __privateGet(this, _visibility).call(this);
        }
      }, _state = new WeakMap(), _visibility = new WeakMap(), _safariBeforeUnloadTimeout = new WeakMap(), _PageVisibility_instances = new WeakSet(), handlePageEvent_fn = function(event) {
        if (IS_SAFARI) window.clearTimeout(__privateGet(this, _safariBeforeUnloadTimeout));
        if (event.type !== "blur" || __privateGet(this, _state).call(this) === "active") {
          __privateGet(this, _state).set(determinePageState(event));
          __privateGet(this, _visibility).set(document.visibilityState == "hidden" ? "hidden" : "visible");
        }
      }, _a8);
      RAFLoop = (_a9 = class {
        constructor(callback) {
          __privateAdd(this, _RAFLoop_instances);
          __privateAdd(this, _id);
          __privateAdd(this, _callback);
          __privateSet(this, _callback, callback);
        }
        start() {
          if (!isUndefined(__privateGet(this, _id))) return;
          __privateMethod(this, _RAFLoop_instances, loop_fn).call(this);
        }
        stop() {
          if (isNumber(__privateGet(this, _id))) window.cancelAnimationFrame(__privateGet(this, _id));
          __privateSet(this, _id, void 0);
        }
      }, _id = new WeakMap(), _callback = new WeakMap(), _RAFLoop_instances = new WeakSet(), loop_fn = function() {
        __privateSet(this, _id, window.requestAnimationFrame(() => {
          if (isUndefined(__privateGet(this, _id))) return;
          __privateGet(this, _callback).call(this);
          __privateMethod(this, _RAFLoop_instances, loop_fn).call(this);
        }));
      }, _a9);
      HTMLMediaEvents = (_a10 = class {
        constructor(provider, ctx) {
          __privateAdd(this, _HTMLMediaEvents_instances);
          __privateAdd(this, _provider);
          __privateAdd(this, _ctx);
          __privateAdd(this, _waiting, false);
          __privateAdd(this, _attachedLoadStart, false);
          __privateAdd(this, _attachedCanPlay, false);
          __privateAdd(this, _timeRAF, new RAFLoop(__privateMethod(this, _HTMLMediaEvents_instances, onAnimationFrame_fn).bind(this)));
          __privateAdd(this, _pageVisibility, new PageVisibility());
          __privateAdd(this, _events);
          /**
          * The `timeupdate` event fires surprisingly infrequently during playback, meaning your progress
          * bar (or whatever else is synced to the currentTime) moves in a choppy fashion. This helps
          * resolve that by retrieving time updates in a request animation frame loop.
          */
          __privateAdd(this, _lastSeenTime, 0);
          __privateAdd(this, _seekedTo, -1);
          __privateAdd(this, _devHandlers);
          __privateAdd(this, _handleDevEvent);
          __privateSet(this, _provider, provider);
          __privateSet(this, _ctx, ctx);
          __privateSet(this, _events, new EventsController(provider.media));
          __privateMethod(this, _HTMLMediaEvents_instances, attachInitialListeners_fn).call(this);
          __privateGet(this, _pageVisibility).connect();
          effect(__privateMethod(this, _HTMLMediaEvents_instances, attachTimeUpdate_fn).bind(this));
          onDispose(__privateMethod(this, _HTMLMediaEvents_instances, onDispose_fn).bind(this));
        }
      }, _provider = new WeakMap(), _ctx = new WeakMap(), _waiting = new WeakMap(), _attachedLoadStart = new WeakMap(), _attachedCanPlay = new WeakMap(), _timeRAF = new WeakMap(), _pageVisibility = new WeakMap(), _events = new WeakMap(), _HTMLMediaEvents_instances = new WeakSet(), media_get = function() {
        return __privateGet(this, _provider).media;
      }, onDispose_fn = function() {
        __privateSet(this, _attachedLoadStart, false);
        __privateSet(this, _attachedCanPlay, false);
        __privateGet(this, _timeRAF).stop();
        __privateGet(this, _events).abort();
        __privateGet(this, _devHandlers)?.clear();
      }, _lastSeenTime = new WeakMap(), _seekedTo = new WeakMap(), onAnimationFrame_fn = function() {
        const newTime = __privateGet(this, _HTMLMediaEvents_instances, media_get).currentTime;
        if (!(IS_SAFARI && newTime - __privateGet(this, _seekedTo) < 0.35) && __privateGet(this, _lastSeenTime) !== newTime) {
          __privateMethod(this, _HTMLMediaEvents_instances, updateCurrentTime_fn).call(this, newTime);
          __privateSet(this, _lastSeenTime, newTime);
        }
      }, attachInitialListeners_fn = function() {
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "loadstart", __privateMethod(this, _HTMLMediaEvents_instances, onLoadStart_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "abort", __privateMethod(this, _HTMLMediaEvents_instances, onAbort_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "emptied", __privateMethod(this, _HTMLMediaEvents_instances, onEmptied_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "error", __privateMethod(this, _HTMLMediaEvents_instances, onError_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "volumechange", __privateMethod(this, _HTMLMediaEvents_instances, onVolumeChange_fn));
      }, attachLoadStartListeners_fn = function() {
        if (__privateGet(this, _attachedLoadStart)) return;
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "loadeddata", __privateMethod(this, _HTMLMediaEvents_instances, onLoadedData_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "loadedmetadata", __privateMethod(this, _HTMLMediaEvents_instances, onLoadedMetadata_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "canplay", __privateMethod(this, _HTMLMediaEvents_instances, onCanPlay_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "canplaythrough", __privateMethod(this, _HTMLMediaEvents_instances, onCanPlayThrough_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "durationchange", __privateMethod(this, _HTMLMediaEvents_instances, onDurationChange_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "play", __privateMethod(this, _HTMLMediaEvents_instances, onPlay_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "progress", __privateMethod(this, _HTMLMediaEvents_instances, onProgress_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "stalled", __privateMethod(this, _HTMLMediaEvents_instances, onStalled_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "suspend", __privateMethod(this, _HTMLMediaEvents_instances, onSuspend_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "ratechange", __privateMethod(this, _HTMLMediaEvents_instances, onRateChange_fn));
        __privateSet(this, _attachedLoadStart, true);
      }, attachCanPlayListeners_fn = function() {
        if (__privateGet(this, _attachedCanPlay)) return;
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "pause", __privateMethod(this, _HTMLMediaEvents_instances, onPause_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "playing", __privateMethod(this, _HTMLMediaEvents_instances, onPlaying_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "seeked", __privateMethod(this, _HTMLMediaEvents_instances, onSeeked_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "seeking", __privateMethod(this, _HTMLMediaEvents_instances, onSeeking_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "ended", __privateMethod(this, _HTMLMediaEvents_instances, onEnded_fn));
        __privateMethod(this, _HTMLMediaEvents_instances, attachEventListener_fn).call(this, "waiting", __privateMethod(this, _HTMLMediaEvents_instances, onWaiting_fn));
        __privateSet(this, _attachedCanPlay, true);
      }, _devHandlers = new WeakMap(), _handleDevEvent = new WeakMap(), attachEventListener_fn = function(eventType, handler) {
        __privateGet(this, _events).add(eventType, handler.bind(this));
      }, onDevEvent_fn = function(event) {
      }, updateCurrentTime_fn = function(time, trigger) {
        const newTime = Math.min(time, __privateGet(this, _ctx).$state.seekableEnd());
        __privateGet(this, _ctx).notify("time-change", newTime, trigger);
      }, onLoadStart_fn = function(event) {
        if (__privateGet(this, _HTMLMediaEvents_instances, media_get).networkState === 3) {
          __privateMethod(this, _HTMLMediaEvents_instances, onAbort_fn).call(this, event);
          return;
        }
        __privateMethod(this, _HTMLMediaEvents_instances, attachLoadStartListeners_fn).call(this);
        __privateGet(this, _ctx).notify("load-start", void 0, event);
      }, onAbort_fn = function(event) {
        __privateGet(this, _ctx).notify("abort", void 0, event);
      }, onEmptied_fn = function(event) {
        __privateGet(this, _ctx).notify("emptied", void 0, event);
      }, onLoadedData_fn = function(event) {
        __privateGet(this, _ctx).notify("loaded-data", void 0, event);
      }, onLoadedMetadata_fn = function(event) {
        __privateSet(this, _lastSeenTime, 0);
        __privateSet(this, _seekedTo, -1);
        __privateMethod(this, _HTMLMediaEvents_instances, attachCanPlayListeners_fn).call(this);
        __privateGet(this, _ctx).notify("loaded-metadata", void 0, event);
        if (IS_IOS || IS_SAFARI && isHLSSrc(__privateGet(this, _ctx).$state.source())) __privateGet(this, _ctx).delegate.ready(__privateMethod(this, _HTMLMediaEvents_instances, getCanPlayDetail_fn).call(this), event);
      }, getCanPlayDetail_fn = function() {
        return {
          provider: peek(__privateGet(this, _ctx).$provider),
          duration: __privateGet(this, _HTMLMediaEvents_instances, media_get).duration,
          buffered: __privateGet(this, _HTMLMediaEvents_instances, media_get).buffered,
          seekable: __privateGet(this, _HTMLMediaEvents_instances, media_get).seekable
        };
      }, onPlay_fn = function(event) {
        if (!__privateGet(this, _ctx).$state.canPlay()) return;
        __privateGet(this, _ctx).notify("play", void 0, event);
      }, onPause_fn = function(event) {
        if (__privateGet(this, _HTMLMediaEvents_instances, media_get).readyState === 1 && !__privateGet(this, _waiting)) return;
        __privateSet(this, _waiting, false);
        __privateGet(this, _timeRAF).stop();
        __privateGet(this, _ctx).notify("pause", void 0, event);
      }, onCanPlay_fn = function(event) {
        __privateGet(this, _ctx).delegate.ready(__privateMethod(this, _HTMLMediaEvents_instances, getCanPlayDetail_fn).call(this), event);
      }, onCanPlayThrough_fn = function(event) {
        if (__privateGet(this, _ctx).$state.started()) return;
        __privateGet(this, _ctx).notify("can-play-through", __privateMethod(this, _HTMLMediaEvents_instances, getCanPlayDetail_fn).call(this), event);
      }, onPlaying_fn = function(event) {
        if (__privateGet(this, _HTMLMediaEvents_instances, media_get).paused) return;
        __privateSet(this, _waiting, false);
        __privateGet(this, _ctx).notify("playing", void 0, event);
        __privateGet(this, _timeRAF).start();
      }, onStalled_fn = function(event) {
        __privateGet(this, _ctx).notify("stalled", void 0, event);
        if (__privateGet(this, _HTMLMediaEvents_instances, media_get).readyState < 3) {
          __privateSet(this, _waiting, true);
          __privateGet(this, _ctx).notify("waiting", void 0, event);
        }
      }, onWaiting_fn = function(event) {
        if (__privateGet(this, _HTMLMediaEvents_instances, media_get).readyState < 3) {
          __privateSet(this, _waiting, true);
          __privateGet(this, _ctx).notify("waiting", void 0, event);
        }
      }, onEnded_fn = function(event) {
        __privateGet(this, _timeRAF).stop();
        __privateMethod(this, _HTMLMediaEvents_instances, updateCurrentTime_fn).call(this, __privateGet(this, _HTMLMediaEvents_instances, media_get).duration, event);
        __privateGet(this, _ctx).notify("end", void 0, event);
        if (__privateGet(this, _ctx).$state.loop()) {
          if (isNil(__privateGet(this, _HTMLMediaEvents_instances, media_get).controls)) __privateGet(this, _HTMLMediaEvents_instances, media_get).controls = false;
        }
      }, attachTimeUpdate_fn = function() {
        const isPaused = __privateGet(this, _ctx).$state.paused(), isPageHidden = __privateGet(this, _pageVisibility).visibility === "hidden";
        if (isPaused || isPageHidden) listenEvent(__privateGet(this, _HTMLMediaEvents_instances, media_get), "timeupdate", __privateMethod(this, _HTMLMediaEvents_instances, onTimeUpdate_fn).bind(this));
      }, onTimeUpdate_fn = function(event) {
        __privateMethod(this, _HTMLMediaEvents_instances, updateCurrentTime_fn).call(this, __privateGet(this, _HTMLMediaEvents_instances, media_get).currentTime, event);
      }, onDurationChange_fn = function(event) {
        if (__privateGet(this, _ctx).$state.ended()) __privateMethod(this, _HTMLMediaEvents_instances, updateCurrentTime_fn).call(this, __privateGet(this, _HTMLMediaEvents_instances, media_get).duration, event);
        __privateGet(this, _ctx).notify("duration-change", __privateGet(this, _HTMLMediaEvents_instances, media_get).duration, event);
      }, onVolumeChange_fn = function(event) {
        const detail = {
          volume: __privateGet(this, _HTMLMediaEvents_instances, media_get).volume,
          muted: __privateGet(this, _HTMLMediaEvents_instances, media_get).muted
        };
        __privateGet(this, _ctx).notify("volume-change", detail, event);
      }, onSeeked_fn = function(event) {
        __privateSet(this, _seekedTo, __privateGet(this, _HTMLMediaEvents_instances, media_get).currentTime);
        __privateMethod(this, _HTMLMediaEvents_instances, updateCurrentTime_fn).call(this, __privateGet(this, _HTMLMediaEvents_instances, media_get).currentTime, event);
        __privateGet(this, _ctx).notify("seeked", __privateGet(this, _HTMLMediaEvents_instances, media_get).currentTime, event);
        if (Math.trunc(__privateGet(this, _HTMLMediaEvents_instances, media_get).currentTime) === Math.trunc(__privateGet(this, _HTMLMediaEvents_instances, media_get).duration) && getNumberOfDecimalPlaces(__privateGet(this, _HTMLMediaEvents_instances, media_get).duration) > getNumberOfDecimalPlaces(__privateGet(this, _HTMLMediaEvents_instances, media_get).currentTime)) {
          __privateMethod(this, _HTMLMediaEvents_instances, updateCurrentTime_fn).call(this, __privateGet(this, _HTMLMediaEvents_instances, media_get).duration, event);
          if (!__privateGet(this, _HTMLMediaEvents_instances, media_get).ended) __privateGet(this, _ctx).player.dispatch(new DOMEvent("media-play-request", { trigger: event }));
        }
      }, onSeeking_fn = function(event) {
        __privateGet(this, _ctx).notify("seeking", __privateGet(this, _HTMLMediaEvents_instances, media_get).currentTime, event);
      }, onProgress_fn = function(event) {
        const detail = {
          buffered: __privateGet(this, _HTMLMediaEvents_instances, media_get).buffered,
          seekable: __privateGet(this, _HTMLMediaEvents_instances, media_get).seekable
        };
        __privateGet(this, _ctx).notify("progress", detail, event);
      }, onSuspend_fn = function(event) {
        __privateGet(this, _ctx).notify("suspend", void 0, event);
      }, onRateChange_fn = function(event) {
        __privateGet(this, _ctx).notify("rate-change", __privateGet(this, _HTMLMediaEvents_instances, media_get).playbackRate, event);
      }, onError_fn = function(event) {
        const error = __privateGet(this, _HTMLMediaEvents_instances, media_get).error;
        if (!error) return;
        const detail = {
          message: error.message,
          code: error.code,
          mediaError: error
        };
        __privateGet(this, _ctx).notify("error", detail, event);
      }, _a10);
      NativeAudioTracks = (_a11 = class {
        constructor(provider, ctx) {
          __privateAdd(this, _NativeAudioTracks_instances);
          __privateAdd(this, _provider2);
          __privateAdd(this, _ctx2);
          __privateSet(this, _provider2, provider);
          __privateSet(this, _ctx2, ctx);
          __privateGet(this, _NativeAudioTracks_instances, nativeTracks_get).onaddtrack = __privateMethod(this, _NativeAudioTracks_instances, onAddNativeTrack_fn).bind(this);
          __privateGet(this, _NativeAudioTracks_instances, nativeTracks_get).onremovetrack = __privateMethod(this, _NativeAudioTracks_instances, onRemoveNativeTrack_fn).bind(this);
          __privateGet(this, _NativeAudioTracks_instances, nativeTracks_get).onchange = __privateMethod(this, _NativeAudioTracks_instances, onChangeNativeTrack_fn).bind(this);
          listenEvent(__privateGet(this, _ctx2).audioTracks, "change", __privateMethod(this, _NativeAudioTracks_instances, onChangeTrack_fn).bind(this));
        }
      }, _provider2 = new WeakMap(), _ctx2 = new WeakMap(), _NativeAudioTracks_instances = new WeakSet(), nativeTracks_get = function() {
        return __privateGet(this, _provider2).media.audioTracks;
      }, onAddNativeTrack_fn = function(event) {
        const nativeTrack = event.track;
        if (nativeTrack.label === "") return;
        const audioTrack = {
          id: nativeTrack.id.toString() || `native-audio-${__privateGet(this, _ctx2).audioTracks.length}`,
          label: nativeTrack.label,
          language: nativeTrack.language,
          kind: nativeTrack.kind,
          selected: false
        };
        __privateGet(this, _ctx2).audioTracks[ListSymbol.add](audioTrack, event);
        if (nativeTrack.enabled) audioTrack.selected = true;
      }, onRemoveNativeTrack_fn = function(event) {
        const track = __privateGet(this, _ctx2).audioTracks.getById(event.track.id);
        if (track) __privateGet(this, _ctx2).audioTracks[ListSymbol.remove](track, event);
      }, onChangeNativeTrack_fn = function(event) {
        let enabledTrack = __privateMethod(this, _NativeAudioTracks_instances, getEnabledNativeTrack_fn).call(this);
        if (!enabledTrack) return;
        const track = __privateGet(this, _ctx2).audioTracks.getById(enabledTrack.id);
        if (track) __privateGet(this, _ctx2).audioTracks[ListSymbol.select](track, true, event);
      }, getEnabledNativeTrack_fn = function() {
        return Array.from(__privateGet(this, _NativeAudioTracks_instances, nativeTracks_get)).find((track) => track.enabled);
      }, onChangeTrack_fn = function(event) {
        const { current } = event.detail;
        if (!current) return;
        const track = __privateGet(this, _NativeAudioTracks_instances, nativeTracks_get).getTrackById(current.id);
        if (track) {
          const prev = __privateMethod(this, _NativeAudioTracks_instances, getEnabledNativeTrack_fn).call(this);
          if (prev) prev.enabled = false;
          track.enabled = true;
        }
      }, _a11);
      HTMLMediaProvider = (_a12 = class {
        constructor(media, ctx) {
          __privateAdd(this, _HTMLMediaProvider_instances);
          __publicField(this, "media");
          __publicField(this, "ctx");
          __publicField(this, "scope", createScope());
          __publicField(this, "currentSrc", null);
          __publicField(this, "audioGain");
          this.media = media;
          this.ctx = ctx;
          this.audioGain = new AudioGain(media, (gain) => {
            this.ctx.notify("audio-gain-change", gain);
          });
        }
        setup() {
          new HTMLMediaEvents(this, this.ctx);
          if ("audioTracks" in this.media) new NativeAudioTracks(this, this.ctx);
          onDispose(() => {
            this.audioGain.destroy();
            this.media.srcObject = null;
            this.media.removeAttribute("src");
            for (const source of this.media.querySelectorAll("source")) source.remove();
            this.media.load();
          });
        }
        get type() {
          return "";
        }
        setPlaybackRate(rate) {
          this.media.playbackRate = rate;
        }
        async play() {
          return this.media.play();
        }
        async pause() {
          return this.media.pause();
        }
        setMuted(muted) {
          this.media.muted = muted;
        }
        setVolume(volume) {
          this.media.volume = volume;
        }
        setCurrentTime(time) {
          this.media.currentTime = time;
        }
        setPlaysInline(inline2) {
          setAttribute(this.media, "playsinline", inline2);
        }
        async loadSource({ src, type }, preload) {
          this.media.preload = preload || "";
          if (isMediaStream(src)) {
            this.removeSource();
            this.media.srcObject = src;
          } else {
            this.media.srcObject = null;
            if (isString(src)) if (type !== "?") this.appendSource({
              src,
              type
            });
            else {
              this.removeSource();
              this.media.src = __privateMethod(this, _HTMLMediaProvider_instances, appendMediaFragment_fn).call(this, src);
            }
            else {
              this.removeSource();
              this.media.src = window.URL.createObjectURL(src);
            }
          }
          this.media.load();
          this.currentSrc = {
            src,
            type
          };
        }
        /**
        * Append source so it works when requesting AirPlay since hls.js will remove it.
        */
        appendSource(src, defaultType) {
          const prevSource = this.media.querySelector("source[data-vds]"), source = prevSource ?? document.createElement("source");
          setAttribute(source, "src", __privateMethod(this, _HTMLMediaProvider_instances, appendMediaFragment_fn).call(this, src.src));
          setAttribute(source, "type", src.type !== "?" ? src.type : defaultType);
          setAttribute(source, "data-vds", "");
          if (!prevSource) this.media.append(source);
        }
        removeSource() {
          this.media.querySelector("source[data-vds]")?.remove();
        }
      }, _HTMLMediaProvider_instances = new WeakSet(), appendMediaFragment_fn = function(src) {
        const { clipStartTime, clipEndTime } = this.ctx.$state, startTime = clipStartTime(), endTime = clipEndTime();
        if (startTime > 0 && endTime > 0) return `${src}#t=${startTime},${endTime}`;
        else if (startTime > 0) return `${src}#t=${startTime}`;
        else if (endTime > 0) return `${src}#t=0,${endTime}`;
        return src;
      }, _a12);
      HTMLRemotePlaybackAdapter = (_a13 = class {
        constructor(media, ctx) {
          __privateAdd(this, _HTMLRemotePlaybackAdapter_instances);
          __privateAdd(this, _media2);
          __privateAdd(this, _ctx3);
          __privateAdd(this, _state2);
          __privateAdd(this, _supported, signal(false));
          __privateSet(this, _media2, media);
          __privateSet(this, _ctx3, ctx);
          __privateMethod(this, _HTMLRemotePlaybackAdapter_instances, setup_fn).call(this);
        }
        get supported() {
          return __privateGet(this, _supported).call(this);
        }
        async prompt() {
          if (!this.supported) throw Error("Not supported on this platform.");
          if (this.type === "airplay" && __privateGet(this, _media2).webkitShowPlaybackTargetPicker) return __privateGet(this, _media2).webkitShowPlaybackTargetPicker();
          return __privateGet(this, _media2).remote.prompt();
        }
      }, _media2 = new WeakMap(), _ctx3 = new WeakMap(), _state2 = new WeakMap(), _supported = new WeakMap(), _HTMLRemotePlaybackAdapter_instances = new WeakSet(), setup_fn = function() {
        if (IS_SERVER2 || !__privateGet(this, _media2)?.remote || !this.canPrompt) return;
        __privateGet(this, _media2).remote.watchAvailability((available) => {
          __privateGet(this, _supported).set(available);
        }).catch(() => {
          __privateGet(this, _supported).set(false);
        });
        effect(__privateMethod(this, _HTMLRemotePlaybackAdapter_instances, watchSupported_fn).bind(this));
      }, watchSupported_fn = function() {
        if (!__privateGet(this, _supported).call(this)) return;
        const events = [
          "connecting",
          "connect",
          "disconnect"
        ], onStateChange = __privateMethod(this, _HTMLRemotePlaybackAdapter_instances, onStateChange_fn).bind(this);
        onStateChange();
        listenEvent(__privateGet(this, _media2), "playing", onStateChange);
        const remoteEvents = new EventsController(__privateGet(this, _media2).remote);
        for (const type of events) remoteEvents.add(type, onStateChange);
      }, onStateChange_fn = function(event) {
        const state = __privateGet(this, _media2).remote.state;
        if (state === __privateGet(this, _state2)) return;
        const detail = {
          type: this.type,
          state
        };
        __privateGet(this, _ctx3).notify("remote-playback-change", detail, event);
        __privateSet(this, _state2, state);
      }, _a13);
      HTMLAirPlayAdapter = class extends HTMLRemotePlaybackAdapter {
        constructor() {
          super(...arguments);
          __publicField(this, "type", "airplay");
        }
        get canPrompt() {
          return "WebKitPlaybackTargetAvailabilityEvent" in window;
        }
      };
      NativeHLSTextTracks = (_a14 = class {
        constructor(video, ctx) {
          __privateAdd(this, _NativeHLSTextTracks_instances);
          __privateAdd(this, _video);
          __privateAdd(this, _ctx4);
          __privateSet(this, _video, video);
          __privateSet(this, _ctx4, ctx);
          video.textTracks.onaddtrack = __privateMethod(this, _NativeHLSTextTracks_instances, onAddTrack_fn).bind(this);
          onDispose(__privateMethod(this, _NativeHLSTextTracks_instances, onDispose_fn2).bind(this));
        }
      }, _video = new WeakMap(), _ctx4 = new WeakMap(), _NativeHLSTextTracks_instances = new WeakSet(), onAddTrack_fn = function(event) {
        const nativeTrack = event.track;
        if (!nativeTrack || findTextTrackElement(__privateGet(this, _video), nativeTrack)) return;
        const track = new TextTrack({
          id: nativeTrack.id,
          kind: nativeTrack.kind,
          label: nativeTrack.label ?? "",
          language: nativeTrack.language,
          type: "vtt"
        });
        track[TextTrackSymbol.native] = { track: nativeTrack };
        track[TextTrackSymbol.readyState] = 2;
        track[TextTrackSymbol.nativeHLS] = true;
        let lastIndex = 0;
        const onCueChange = (event2) => {
          if (!nativeTrack.cues) return;
          for (let i = lastIndex; i < nativeTrack.cues.length; i++) {
            track.addCue(nativeTrack.cues[i], event2);
            lastIndex++;
          }
        };
        onCueChange(event);
        nativeTrack.oncuechange = onCueChange;
        __privateGet(this, _ctx4).textTracks.add(track, event);
        track.setMode(nativeTrack.mode, event);
      }, onDispose_fn2 = function() {
        __privateGet(this, _video).textTracks.onaddtrack = null;
        for (const track of __privateGet(this, _ctx4).textTracks) {
          const nativeTrack = track[TextTrackSymbol.native]?.track;
          if (nativeTrack?.oncuechange) nativeTrack.oncuechange = null;
        }
      }, _a14);
      VideoPictureInPicture = (_a15 = class {
        constructor(video, media) {
          __privateAdd(this, _VideoPictureInPicture_instances);
          __privateAdd(this, _video2);
          __privateAdd(this, _media3);
          __privateAdd(this, _onChange2, (active, event) => {
            __privateGet(this, _media3).notify("picture-in-picture-change", active, event);
          });
          __privateSet(this, _video2, video);
          __privateSet(this, _media3, media);
          new EventsController(video).add("enterpictureinpicture", __privateMethod(this, _VideoPictureInPicture_instances, onEnter_fn).bind(this)).add("leavepictureinpicture", __privateMethod(this, _VideoPictureInPicture_instances, onExit_fn).bind(this));
        }
        get active() {
          return document.pictureInPictureElement === __privateGet(this, _video2);
        }
        get supported() {
          return canUsePictureInPicture(__privateGet(this, _video2));
        }
        async enter() {
          return __privateGet(this, _video2).requestPictureInPicture();
        }
        exit() {
          return document.exitPictureInPicture();
        }
      }, _video2 = new WeakMap(), _media3 = new WeakMap(), _VideoPictureInPicture_instances = new WeakSet(), onEnter_fn = function(event) {
        __privateGet(this, _onChange2).call(this, true, event);
      }, onExit_fn = function(event) {
        __privateGet(this, _onChange2).call(this, false, event);
      }, _onChange2 = new WeakMap(), _a15);
      VideoPresentation = (_a16 = class {
        constructor(video, media) {
          __privateAdd(this, _VideoPresentation_instances);
          __privateAdd(this, _video3);
          __privateAdd(this, _media4);
          __privateAdd(this, _mode2, "inline");
          __privateSet(this, _video3, video);
          __privateSet(this, _media4, media);
          listenEvent(video, "webkitpresentationmodechanged", __privateMethod(this, _VideoPresentation_instances, onModeChange_fn).bind(this));
        }
        get mode() {
          return __privateGet(this, _mode2);
        }
        get supported() {
          return canUseVideoPresentation(__privateGet(this, _video3));
        }
        async setPresentationMode(mode) {
          if (__privateGet(this, _mode2) === mode) return;
          __privateGet(this, _video3).webkitSetPresentationMode(mode);
        }
      }, _video3 = new WeakMap(), _media4 = new WeakMap(), _mode2 = new WeakMap(), _VideoPresentation_instances = new WeakSet(), onModeChange_fn = function(event) {
        const prevMode = __privateGet(this, _mode2);
        __privateSet(this, _mode2, __privateGet(this, _video3).webkitPresentationMode);
        __privateGet(this, _media4).player?.dispatch(new DOMEvent("video-presentation-change", {
          detail: __privateGet(this, _mode2),
          trigger: event
        }));
        ["fullscreen", "picture-in-picture"].forEach((type) => {
          if (__privateGet(this, _mode2) === type || prevMode === type) __privateGet(this, _media4).notify(`${type}-change`, __privateGet(this, _mode2) === type, event);
        });
      }, _a16);
      FullscreenPresentationAdapter = (_a17 = class {
        constructor(presentation) {
          __privateAdd(this, _presentation);
          __privateSet(this, _presentation, presentation);
        }
        get active() {
          return __privateGet(this, _presentation).mode === "fullscreen";
        }
        get supported() {
          return __privateGet(this, _presentation).supported;
        }
        async enter() {
          __privateGet(this, _presentation).setPresentationMode("fullscreen");
        }
        async exit() {
          __privateGet(this, _presentation).setPresentationMode("inline");
        }
      }, _presentation = new WeakMap(), _a17);
      PIPPresentationAdapter = (_a18 = class {
        constructor(presentation) {
          __privateAdd(this, _presentation2);
          __privateSet(this, _presentation2, presentation);
        }
        get active() {
          return __privateGet(this, _presentation2).mode === "picture-in-picture";
        }
        get supported() {
          return __privateGet(this, _presentation2).supported;
        }
        async enter() {
          __privateGet(this, _presentation2).setPresentationMode("picture-in-picture");
        }
        async exit() {
          __privateGet(this, _presentation2).setPresentationMode("inline");
        }
      }, _presentation2 = new WeakMap(), _a18);
      provider_exports$1 = /* @__PURE__ */ __exportAll({ VideoProvider: () => VideoProvider });
      VideoProvider = class extends HTMLMediaProvider {
        constructor(video, ctx) {
          super(video, ctx);
          __publicField(this, "$$PROVIDER_TYPE", "VIDEO");
          __publicField(this, "airPlay");
          __publicField(this, "fullscreen");
          __publicField(this, "pictureInPicture");
          scoped(() => {
            this.airPlay = new HTMLAirPlayAdapter(video, ctx);
            if (canUseVideoPresentation(video)) {
              const presentation = new VideoPresentation(video, ctx);
              this.fullscreen = new FullscreenPresentationAdapter(presentation);
              this.pictureInPicture = new PIPPresentationAdapter(presentation);
            } else if (canUsePictureInPicture(video)) this.pictureInPicture = new VideoPictureInPicture(video, ctx);
          }, this.scope);
        }
        get type() {
          return "video";
        }
        setup() {
          super.setup();
          if (canPlayHLSNatively(this.video)) new NativeHLSTextTracks(this.video, this.ctx);
          this.ctx.textRenderers.attachVideo(this.video);
          onDispose(() => {
            this.ctx.textRenderers.attachVideo(null);
          });
          if (this.type === "video") this.ctx.notify("provider-setup", this);
        }
        /**
        * The native HTML `<video>` element.
        *
        * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/HTMLVideoElement}
        */
        get video() {
          return this.media;
        }
      };
      toDOMEventType = (type) => `dash-${camelToKebabCase(type)}`;
      DASHController = (_a19 = class {
        constructor(video, ctx) {
          __privateAdd(this, _DASHController_instances);
          __privateAdd(this, _video4);
          __privateAdd(this, _ctx5);
          __privateAdd(this, _instance, null);
          __privateAdd(this, _callbacks, /* @__PURE__ */ new Set());
          __privateAdd(this, _stopLiveSync, null);
          __publicField(this, "config", {});
          __privateAdd(this, _currentTrack, null);
          __privateAdd(this, _cueTracker, {});
          __privateAdd(this, _retryLoadingTimer, -1);
          __privateSet(this, _video4, video);
          __privateSet(this, _ctx5, ctx);
        }
        get instance() {
          return __privateGet(this, _instance);
        }
        setup(ctor) {
          __privateSet(this, _instance, ctor().create());
          const dispatcher = __privateMethod(this, _DASHController_instances, dispatchDASHEvent_fn).bind(this);
          for (const event of Object.values(ctor.events)) __privateGet(this, _instance).on(event, dispatcher);
          __privateGet(this, _instance).on(ctor.events.ERROR, __privateMethod(this, _DASHController_instances, onError_fn2).bind(this));
          for (const callback of __privateGet(this, _callbacks)) callback(__privateGet(this, _instance));
          __privateGet(this, _ctx5).player.dispatch("dash-instance", { detail: __privateGet(this, _instance) });
          __privateGet(this, _instance).initialize(__privateGet(this, _video4), void 0, false);
          __privateGet(this, _instance).updateSettings({
            streaming: {
              text: {
                defaultEnabled: false,
                dispatchForManualRendering: true
              },
              buffer: { fastSwitchEnabled: true }
            },
            ...this.config
          });
          __privateGet(this, _instance).on(ctor.events.FRAGMENT_LOADING_STARTED, __privateMethod(this, _DASHController_instances, onFragmentLoadStart_fn).bind(this));
          __privateGet(this, _instance).on(ctor.events.FRAGMENT_LOADING_COMPLETED, __privateMethod(this, _DASHController_instances, onFragmentLoadComplete_fn).bind(this));
          __privateGet(this, _instance).on(ctor.events.MANIFEST_LOADED, __privateMethod(this, _DASHController_instances, onManifestLoaded_fn).bind(this));
          __privateGet(this, _instance).on(ctor.events.QUALITY_CHANGE_RENDERED, __privateMethod(this, _DASHController_instances, onQualityChange_fn).bind(this));
          __privateGet(this, _instance).on(ctor.events.TEXT_TRACKS_ADDED, __privateMethod(this, _DASHController_instances, onTextTracksAdded_fn).bind(this));
          __privateGet(this, _instance).on(ctor.events.TRACK_CHANGE_RENDERED, __privateMethod(this, _DASHController_instances, onTrackChange_fn).bind(this));
          __privateGet(this, _ctx5).qualities[QualitySymbol.enableAuto] = __privateMethod(this, _DASHController_instances, enableAutoQuality_fn).bind(this);
          listenEvent(__privateGet(this, _ctx5).qualities, "change", __privateMethod(this, _DASHController_instances, onUserQualityChange_fn).bind(this));
          listenEvent(__privateGet(this, _ctx5).audioTracks, "change", __privateMethod(this, _DASHController_instances, onUserAudioChange_fn).bind(this));
          __privateSet(this, _stopLiveSync, effect(__privateMethod(this, _DASHController_instances, liveSync_fn).bind(this)));
        }
        onInstance(callback) {
          __privateGet(this, _callbacks).add(callback);
          return () => __privateGet(this, _callbacks).delete(callback);
        }
        loadSource(src) {
          __privateMethod(this, _DASHController_instances, reset_fn).call(this);
          if (!isString(src.src)) return;
          __privateGet(this, _instance)?.attachSource(src.src);
        }
        destroy() {
          var _a106;
          __privateMethod(this, _DASHController_instances, reset_fn).call(this);
          __privateGet(this, _instance)?.destroy();
          __privateSet(this, _instance, null);
          (_a106 = __privateGet(this, _stopLiveSync)) == null ? void 0 : _a106.call(this);
          __privateSet(this, _stopLiveSync, null);
        }
      }, _video4 = new WeakMap(), _ctx5 = new WeakMap(), _instance = new WeakMap(), _callbacks = new WeakMap(), _stopLiveSync = new WeakMap(), _DASHController_instances = new WeakSet(), createDOMEvent_fn = function(event) {
        return new DOMEvent(toDOMEventType(event.type), { detail: event });
      }, liveSync_fn = function() {
        if (!__privateGet(this, _ctx5).$state.live()) return;
        const raf = new RAFLoop(__privateMethod(this, _DASHController_instances, liveSyncPosition_fn).bind(this));
        raf.start();
        return raf.stop.bind(raf);
      }, liveSyncPosition_fn = function() {
        if (!__privateGet(this, _instance)) return;
        const position = __privateGet(this, _instance).duration() - __privateGet(this, _instance).time();
        __privateGet(this, _ctx5).$state.liveSyncPosition.set(!isNaN(position) ? position : Infinity);
      }, dispatchDASHEvent_fn = function(event) {
        __privateGet(this, _ctx5).player?.dispatch(__privateMethod(this, _DASHController_instances, createDOMEvent_fn).call(this, event));
      }, _currentTrack = new WeakMap(), _cueTracker = new WeakMap(), onTextFragmentLoaded_fn = function(event) {
        const native = __privateGet(this, _currentTrack)?.[TextTrackSymbol.native], cues = (native?.track).cues;
        if (!native || !cues) return;
        const id2 = __privateGet(this, _currentTrack).id, startIndex = __privateGet(this, _cueTracker)[id2] ?? 0, trigger = __privateMethod(this, _DASHController_instances, createDOMEvent_fn).call(this, event);
        for (let i = startIndex; i < cues.length; i++) {
          const cue = cues[i];
          if (!cue.positionAlign) cue.positionAlign = "auto";
          __privateGet(this, _currentTrack).addCue(cue, trigger);
        }
        __privateGet(this, _cueTracker)[id2] = cues.length;
      }, onTextTracksAdded_fn = function(event) {
        if (!__privateGet(this, _instance)) return;
        const data = event.tracks, nativeTextTracks = [...__privateGet(this, _video4).textTracks].filter((track) => "manualMode" in track), trigger = __privateMethod(this, _DASHController_instances, createDOMEvent_fn).call(this, event);
        for (let i = 0; i < nativeTextTracks.length; i++) {
          const textTrackInfo = data[i], nativeTextTrack = nativeTextTracks[i];
          const track = new TextTrack({
            id: `dash-${textTrackInfo.kind}-${i}`,
            label: textTrackInfo?.label ?? textTrackInfo.labels.find((t2) => t2.text)?.text ?? (textTrackInfo?.lang && getLangName(textTrackInfo.lang)) ?? textTrackInfo?.lang ?? void 0,
            language: textTrackInfo.lang ?? void 0,
            kind: textTrackInfo.kind,
            default: textTrackInfo.defaultTrack
          });
          track[TextTrackSymbol.native] = {
            managed: true,
            track: nativeTextTrack
          };
          track[TextTrackSymbol.readyState] = 2;
          track[TextTrackSymbol.onModeChange] = () => {
            if (!__privateGet(this, _instance)) return;
            if (track.mode === "showing") {
              __privateGet(this, _instance).setTextTrack(i);
              __privateSet(this, _currentTrack, track);
            } else {
              __privateGet(this, _instance).setTextTrack(-1);
              __privateSet(this, _currentTrack, null);
            }
          };
          __privateGet(this, _ctx5).textTracks.add(track, trigger);
        }
      }, onTrackChange_fn = function(event) {
        const { mediaType, newMediaInfo } = event;
        if (mediaType === "audio") {
          const track = __privateGet(this, _ctx5).audioTracks.getById(`dash-audio-${newMediaInfo.index}`);
          if (track) {
            const trigger = __privateMethod(this, _DASHController_instances, createDOMEvent_fn).call(this, event);
            __privateGet(this, _ctx5).audioTracks[ListSymbol.select](track, true, trigger);
          }
        }
      }, onQualityChange_fn = function(event) {
        if (event.mediaType !== "video") return;
        const quality = __privateGet(this, _ctx5).qualities[event.newQuality];
        if (quality) {
          const trigger = __privateMethod(this, _DASHController_instances, createDOMEvent_fn).call(this, event);
          __privateGet(this, _ctx5).qualities[ListSymbol.select](quality, true, trigger);
        }
      }, onManifestLoaded_fn = function(event) {
        if (__privateGet(this, _ctx5).$state.canPlay() || !__privateGet(this, _instance)) return;
        const { type, mediaPresentationDuration } = event.data, trigger = __privateMethod(this, _DASHController_instances, createDOMEvent_fn).call(this, event);
        __privateGet(this, _ctx5).notify("stream-type-change", type !== "static" ? "live" : "on-demand", trigger);
        __privateGet(this, _ctx5).notify("duration-change", mediaPresentationDuration, trigger);
        __privateGet(this, _ctx5).qualities[QualitySymbol.setAuto](true, trigger);
        const media = __privateGet(this, _instance).getVideoElement();
        const videoQualities = __privateGet(this, _instance).getTracksForTypeFromManifest("video", event.data);
        const supportedVideoMimeType = [...new Set(videoQualities.map((e2) => e2.mimeType))].find((type2) => type2 && canPlayVideoType(media, type2));
        const videoQuality = videoQualities.find((track) => supportedVideoMimeType === track.mimeType);
        let audioTracks = __privateGet(this, _instance).getTracksForTypeFromManifest("audio", event.data);
        const supportedAudioMimeType = [...new Set(audioTracks.map((e2) => e2.mimeType))].find((type2) => type2 && canPlayAudioType(media, type2));
        audioTracks = audioTracks.filter((track) => supportedAudioMimeType === track.mimeType);
        if (videoQuality) {
          videoQuality.bitrateList.forEach((bitrate, index) => {
            const quality = {
              id: bitrate.id?.toString() ?? `dash-bitrate-${index}`,
              width: bitrate.width ?? 0,
              height: bitrate.height ?? 0,
              bitrate: bitrate.bandwidth ?? 0,
              codec: videoQuality.codec,
              index
            };
            __privateGet(this, _ctx5).qualities[ListSymbol.add](quality, trigger);
          });
          if (isNumber(videoQuality.index)) {
            const quality = __privateGet(this, _ctx5).qualities[videoQuality.index];
            if (quality) __privateGet(this, _ctx5).qualities[ListSymbol.select](quality, true, trigger);
          }
        }
        audioTracks.forEach((audioTrack, index) => {
          const label = audioTrack.labels.find((label2) => {
            return navigator.languages.some((language) => {
              return label2.lang && language.toLowerCase().startsWith(label2.lang.toLowerCase());
            });
          }) || audioTrack.labels[0];
          const localTrack = {
            id: `dash-audio-${audioTrack?.index}`,
            label: label?.text ?? (audioTrack.lang && getLangName(audioTrack.lang)) ?? audioTrack.lang ?? "",
            language: audioTrack.lang ?? "",
            kind: "main",
            mimeType: audioTrack.mimeType,
            codec: audioTrack.codec,
            index
          };
          __privateGet(this, _ctx5).audioTracks[ListSymbol.add](localTrack, trigger);
        });
        media.dispatchEvent(new DOMEvent("canplay", { trigger }));
      }, onError_fn2 = function(event) {
        const { type: eventType, error: data } = event;
        switch (data.code) {
          case 27:
            __privateMethod(this, _DASHController_instances, onNetworkError_fn).call(this, data);
            break;
          default:
            __privateMethod(this, _DASHController_instances, onFatalError_fn).call(this, data);
            break;
        }
      }, onFragmentLoadStart_fn = function() {
        if (__privateGet(this, _retryLoadingTimer) >= 0) __privateMethod(this, _DASHController_instances, clearRetryTimer_fn).call(this);
      }, onFragmentLoadComplete_fn = function(event) {
        if (event.mediaType === "text") requestAnimationFrame(__privateMethod(this, _DASHController_instances, onTextFragmentLoaded_fn).bind(this, event));
      }, _retryLoadingTimer = new WeakMap(), onNetworkError_fn = function(error) {
        __privateMethod(this, _DASHController_instances, clearRetryTimer_fn).call(this);
        __privateGet(this, _instance)?.play();
        __privateSet(this, _retryLoadingTimer, window.setTimeout(() => {
          __privateSet(this, _retryLoadingTimer, -1);
          __privateMethod(this, _DASHController_instances, onFatalError_fn).call(this, error);
        }, 5e3));
      }, clearRetryTimer_fn = function() {
        clearTimeout(__privateGet(this, _retryLoadingTimer));
        __privateSet(this, _retryLoadingTimer, -1);
      }, onFatalError_fn = function(error) {
        __privateGet(this, _ctx5).notify("error", {
          message: error.message ?? "",
          code: 1,
          error
        });
      }, enableAutoQuality_fn = function() {
        __privateMethod(this, _DASHController_instances, switchAutoBitrate_fn).call(this, "video", true);
        const { qualities } = __privateGet(this, _ctx5);
        __privateGet(this, _instance)?.setQualityFor("video", qualities.selectedIndex, true);
      }, switchAutoBitrate_fn = function(type, auto) {
        __privateGet(this, _instance)?.updateSettings({ streaming: { abr: { autoSwitchBitrate: { [type]: auto } } } });
      }, onUserQualityChange_fn = function() {
        const { qualities } = __privateGet(this, _ctx5);
        if (!__privateGet(this, _instance) || qualities.auto || !qualities.selected) return;
        __privateMethod(this, _DASHController_instances, switchAutoBitrate_fn).call(this, "video", false);
        __privateGet(this, _instance).setQualityFor("video", qualities.selectedIndex, qualities.switch === "current");
        if (IS_CHROME) __privateGet(this, _video4).currentTime = __privateGet(this, _video4).currentTime;
      }, onUserAudioChange_fn = function() {
        if (!__privateGet(this, _instance)) return;
        const { audioTracks } = __privateGet(this, _ctx5), selectedTrack = __privateGet(this, _instance).getTracksFor("audio").find((track) => audioTracks.selected && audioTracks.selected.id === `dash-audio-${track.index}`);
        if (selectedTrack) __privateGet(this, _instance).setCurrentTrack(selectedTrack);
      }, reset_fn = function() {
        __privateMethod(this, _DASHController_instances, clearRetryTimer_fn).call(this);
        __privateSet(this, _currentTrack, null);
        __privateSet(this, _cueTracker, {});
      }, _a19);
      DASHLibLoader = (_a20 = class {
        constructor(lib, ctx, callback) {
          __privateAdd(this, _DASHLibLoader_instances);
          __privateAdd(this, _lib);
          __privateAdd(this, _ctx6);
          __privateAdd(this, _callback2);
          __privateSet(this, _lib, lib);
          __privateSet(this, _ctx6, ctx);
          __privateSet(this, _callback2, callback);
          __privateMethod(this, _DASHLibLoader_instances, startLoading_fn).call(this);
        }
      }, _lib = new WeakMap(), _ctx6 = new WeakMap(), _callback2 = new WeakMap(), _DASHLibLoader_instances = new WeakSet(), startLoading_fn = async function() {
        const callbacks = {
          onLoadStart: __privateMethod(this, _DASHLibLoader_instances, onLoadStart_fn2).bind(this),
          onLoaded: __privateMethod(this, _DASHLibLoader_instances, onLoaded_fn).bind(this),
          onLoadError: __privateMethod(this, _DASHLibLoader_instances, onLoadError_fn).bind(this)
        };
        let ctor = await loadDASHScript(__privateGet(this, _lib), callbacks);
        if (isUndefined(ctor) && !isString(__privateGet(this, _lib))) ctor = await importDASH(__privateGet(this, _lib), callbacks);
        if (!ctor) return null;
        if (!window.dashjs.supportsMediaSource()) {
          const message = "[vidstack] `dash.js` is not supported in this environment";
          __privateGet(this, _ctx6).player.dispatch(new DOMEvent("dash-unsupported"));
          __privateGet(this, _ctx6).notify("error", {
            message,
            code: 4
          });
          return null;
        }
        return ctor;
      }, onLoadStart_fn2 = function() {
        __privateGet(this, _ctx6).player.dispatch(new DOMEvent("dash-lib-load-start"));
      }, onLoaded_fn = function(ctor) {
        __privateGet(this, _ctx6).player.dispatch(new DOMEvent("dash-lib-loaded", { detail: ctor }));
        __privateGet(this, _callback2).call(this, ctor);
      }, onLoadError_fn = function(e2) {
        const error = coerceToError(e2);
        __privateGet(this, _ctx6).player.dispatch(new DOMEvent("dash-lib-load-error", { detail: error }));
        __privateGet(this, _ctx6).notify("error", {
          message: error.message,
          code: 4,
          error
        });
      }, _a20);
      provider_exports = /* @__PURE__ */ __exportAll({ DASHProvider: () => DASHProvider });
      JS_DELIVR_CDN = "https://cdn.jsdelivr.net";
      DASHProvider = (_a21 = class extends VideoProvider {
        constructor() {
          super(...arguments);
          __publicField(this, "$$PROVIDER_TYPE", "DASH");
          __privateAdd(this, _ctor, null);
          __privateAdd(this, _controller2, new DASHController(this.video, this.ctx));
          __privateAdd(this, _library, `${JS_DELIVR_CDN}/npm/dashjs@4.7.4/dist/dash.all.min.js`);
        }
        /**
        * The `dash.js` constructor.
        */
        get ctor() {
          return __privateGet(this, _ctor);
        }
        /**
        * The current `dash.js` instance.
        */
        get instance() {
          return __privateGet(this, _controller2).instance;
        }
        get type() {
          return "dash";
        }
        get canLiveSync() {
          return true;
        }
        /**
        * The `dash.js` configuration object.
        *
        * @see {@link https://cdn.dashjs.org/latest/jsdoc/module-Settings.html}
        */
        get config() {
          return __privateGet(this, _controller2).config;
        }
        set config(config) {
          __privateGet(this, _controller2).config = config;
        }
        /**
        * The `dash.js` constructor (supports dynamic imports) or a URL of where it can be found.
        *
        * @defaultValue `https://cdn.jsdelivr.net/npm/dashjs@4.7.4/dist/dash.all.min.js`
        */
        get library() {
          return __privateGet(this, _library);
        }
        set library(library) {
          __privateSet(this, _library, library);
        }
        preconnect() {
          if (!isString(__privateGet(this, _library))) return;
          preconnect(__privateGet(this, _library));
        }
        setup() {
          super.setup();
          new DASHLibLoader(__privateGet(this, _library), this.ctx, (ctor) => {
            __privateSet(this, _ctor, ctor);
            __privateGet(this, _controller2).setup(ctor);
            this.ctx.notify("provider-setup", this);
            const src = peek(this.ctx.$state.source);
            if (src) this.loadSource(src);
          });
        }
        async loadSource(src, preload) {
          if (!isString(src.src)) {
            this.removeSource();
            return;
          }
          this.media.preload = preload || "";
          this.appendSource(src, "application/x-mpegurl");
          __privateGet(this, _controller2).loadSource(src);
          this.currentSrc = src;
        }
        /**
        * The given callback is invoked when a new `dash.js` instance is created and right before it's
        * attached to media.
        */
        onInstance(callback) {
          const instance = __privateGet(this, _controller2).instance;
          if (instance) callback(instance);
          return __privateGet(this, _controller2).onInstance(callback);
        }
        destroy() {
          __privateGet(this, _controller2).destroy();
        }
      }, _ctor = new WeakMap(), _controller2 = new WeakMap(), _library = new WeakMap(), /**
      * Whether `dash.js` is supported in this environment.
      */
      __publicField(_a21, "supported", isDASHSupported()), _a21);
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-CHcsWfRV.js
  var vidstack_CHcsWfRV_exports = {};
  __export(vidstack_CHcsWfRV_exports, {
    AudioProvider: () => AudioProvider
  });
  var AudioProvider;
  var init_vidstack_CHcsWfRV = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-CHcsWfRV.js"() {
      "use client";
      init_define_import_meta_env();
      init_vidstack_B9fTkxQO();
      init_vidstack_CvezEmSQ();
      AudioProvider = class extends HTMLMediaProvider {
        constructor(audio, ctx) {
          super(audio, ctx);
          __publicField(this, "$$PROVIDER_TYPE", "AUDIO");
          __publicField(this, "airPlay");
          scoped(() => {
            this.airPlay = new HTMLAirPlayAdapter(this.media, ctx);
          }, this.scope);
        }
        get type() {
          return "audio";
        }
        setup() {
          super.setup();
          if (this.type === "audio") this.ctx.notify("provider-setup", this);
        }
        /**
        * The native HTML `<audio>` element.
        *
        * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/HTMLAudioElement}
        */
        get audio() {
          return this.media;
        }
      };
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-DiaLSJYx.js
  var vidstack_DiaLSJYx_exports = {};
  __export(vidstack_DiaLSJYx_exports, {
    HLSProvider: () => HLSProvider
  });
  async function importHLS(loader, callbacks = {}) {
    if (isUndefined(loader)) return void 0;
    callbacks.onLoadStart?.();
    if (loader.prototype && loader.prototype !== Function) {
      callbacks.onLoaded?.(loader);
      return loader;
    }
    try {
      const ctor = (await loader())?.default;
      if (ctor && !!ctor.isSupported) callbacks.onLoaded?.(ctor);
      else throw Error("");
      return ctor;
    } catch (err) {
      callbacks.onLoadError?.(err);
    }
  }
  async function loadHLSScript(src, callbacks = {}) {
    if (!isString(src)) return void 0;
    callbacks.onLoadStart?.();
    try {
      await loadScript(src);
      if (!isFunction(window.Hls)) throw Error("");
      const ctor = window.Hls;
      callbacks.onLoaded?.(ctor);
      return ctor;
    } catch (err) {
      callbacks.onLoadError?.(err);
    }
  }
  var toDOMEventType2, _video5, _ctx7, _instance2, _stopLiveSync2, _callbacks2, _HLSController_instances, createDOMEvent_fn2, liveSync_fn2, liveSyncPosition_fn2, dispatchHLSEvent_fn, onTracksFound_fn, onCuesParsed_fn, onAudioSwitch_fn, onLevelSwitched_fn, onLevelUpdated_fn, onLevelLoaded_fn, onError_fn3, onFatalError_fn2, enableAutoQuality_fn2, onUserQualityChange_fn2, onUserAudioChange_fn2, _a22, HLSController, _lib2, _ctx8, _callback3, _HLSLibLoader_instances, startLoading_fn2, onLoadStart_fn3, onLoaded_fn2, onLoadError_fn2, _a23, HLSLibLoader, JS_DELIVR_CDN2, _ctor2, _controller3, _a24, _library2, HLSProvider;
  var init_vidstack_DiaLSJYx = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-DiaLSJYx.js"() {
      "use client";
      init_define_import_meta_env();
      init_vidstack_B9fTkxQO();
      init_vidstack_CvezEmSQ();
      toDOMEventType2 = (type) => camelToKebabCase(type);
      HLSController = (_a22 = class {
        constructor(video, ctx) {
          __privateAdd(this, _HLSController_instances);
          __privateAdd(this, _video5);
          __privateAdd(this, _ctx7);
          __privateAdd(this, _instance2, null);
          __privateAdd(this, _stopLiveSync2, null);
          __publicField(this, "config", {});
          __privateAdd(this, _callbacks2, /* @__PURE__ */ new Set());
          __privateSet(this, _video5, video);
          __privateSet(this, _ctx7, ctx);
        }
        get instance() {
          return __privateGet(this, _instance2);
        }
        setup(ctor) {
          const { streamType } = __privateGet(this, _ctx7).$state;
          const isLive = peek(streamType).includes("live"), isLiveLowLatency = peek(streamType).includes("ll-");
          const instance = __privateSet(this, _instance2, new ctor({
            lowLatencyMode: isLiveLowLatency,
            backBufferLength: isLiveLowLatency ? 4 : isLive ? 8 : void 0,
            renderTextTracksNatively: false,
            ...this.config
          }));
          const guard = (callback) => (eventType, data) => {
            if (__privateGet(this, _instance2) !== instance) return;
            callback(eventType, data);
          };
          const dispatcher = guard(__privateMethod(this, _HLSController_instances, dispatchHLSEvent_fn).bind(this));
          for (const event of Object.values(ctor.Events)) instance.on(event, dispatcher);
          instance.on(ctor.Events.ERROR, guard(__privateMethod(this, _HLSController_instances, onError_fn3).bind(this)));
          for (const callback of __privateGet(this, _callbacks2)) callback(instance);
          __privateGet(this, _ctx7).player.dispatch("hls-instance", { detail: instance });
          instance.attachMedia(__privateGet(this, _video5));
          instance.on(ctor.Events.AUDIO_TRACK_SWITCHED, guard(__privateMethod(this, _HLSController_instances, onAudioSwitch_fn).bind(this)));
          instance.on(ctor.Events.LEVEL_SWITCHED, guard(__privateMethod(this, _HLSController_instances, onLevelSwitched_fn).bind(this)));
          instance.on(ctor.Events.LEVEL_LOADED, guard(__privateMethod(this, _HLSController_instances, onLevelLoaded_fn).bind(this, instance)));
          instance.on(ctor.Events.LEVEL_UPDATED, guard(__privateMethod(this, _HLSController_instances, onLevelUpdated_fn).bind(this)));
          instance.on(ctor.Events.NON_NATIVE_TEXT_TRACKS_FOUND, guard(__privateMethod(this, _HLSController_instances, onTracksFound_fn).bind(this, instance)));
          instance.on(ctor.Events.CUES_PARSED, guard(__privateMethod(this, _HLSController_instances, onCuesParsed_fn).bind(this, instance)));
          __privateGet(this, _ctx7).qualities[QualitySymbol.enableAuto] = __privateMethod(this, _HLSController_instances, enableAutoQuality_fn2).bind(this);
          listenEvent(__privateGet(this, _ctx7).qualities, "change", __privateMethod(this, _HLSController_instances, onUserQualityChange_fn2).bind(this));
          listenEvent(__privateGet(this, _ctx7).audioTracks, "change", __privateMethod(this, _HLSController_instances, onUserAudioChange_fn2).bind(this));
          __privateSet(this, _stopLiveSync2, effect(__privateMethod(this, _HLSController_instances, liveSync_fn2).bind(this)));
        }
        onInstance(callback) {
          __privateGet(this, _callbacks2).add(callback);
          return () => __privateGet(this, _callbacks2).delete(callback);
        }
        loadSource(src) {
          if (!isString(src.src)) return;
          __privateGet(this, _instance2)?.loadSource(src.src);
        }
        destroy() {
          var _a106;
          __privateGet(this, _instance2)?.destroy();
          __privateSet(this, _instance2, null);
          (_a106 = __privateGet(this, _stopLiveSync2)) == null ? void 0 : _a106.call(this);
          __privateSet(this, _stopLiveSync2, null);
        }
      }, _video5 = new WeakMap(), _ctx7 = new WeakMap(), _instance2 = new WeakMap(), _stopLiveSync2 = new WeakMap(), _callbacks2 = new WeakMap(), _HLSController_instances = new WeakSet(), createDOMEvent_fn2 = function(type, data) {
        return new DOMEvent(toDOMEventType2(type), { detail: data });
      }, liveSync_fn2 = function() {
        if (!__privateGet(this, _ctx7).$state.live()) return;
        const raf = new RAFLoop(__privateMethod(this, _HLSController_instances, liveSyncPosition_fn2).bind(this));
        raf.start();
        return raf.stop.bind(raf);
      }, liveSyncPosition_fn2 = function() {
        __privateGet(this, _ctx7).$state.liveSyncPosition.set(__privateGet(this, _instance2)?.liveSyncPosition ?? Infinity);
      }, dispatchHLSEvent_fn = function(type, data) {
        __privateGet(this, _ctx7).player?.dispatch(__privateMethod(this, _HLSController_instances, createDOMEvent_fn2).call(this, type, data));
      }, onTracksFound_fn = function(instance, eventType, data) {
        const event = __privateMethod(this, _HLSController_instances, createDOMEvent_fn2).call(this, eventType, data);
        let currentTrack = -1;
        for (let i = 0; i < data.tracks.length; i++) {
          const nonNativeTrack = data.tracks[i], init = nonNativeTrack.subtitleTrack ?? nonNativeTrack.closedCaptions, track = new TextTrack({
            id: `hls-${nonNativeTrack.kind}-${i}`,
            src: init?.url,
            label: nonNativeTrack.label,
            language: init?.lang,
            kind: nonNativeTrack.kind,
            default: nonNativeTrack.default
          });
          track[TextTrackSymbol.readyState] = 2;
          track[TextTrackSymbol.onModeChange] = () => {
            if (__privateGet(this, _instance2) !== instance) return;
            if (track.mode === "showing") {
              instance.subtitleTrack = i;
              currentTrack = i;
            } else if (currentTrack === i) {
              instance.subtitleTrack = -1;
              currentTrack = -1;
            }
          };
          __privateGet(this, _ctx7).textTracks.add(track, event);
        }
      }, onCuesParsed_fn = function(instance, eventType, data) {
        const index = instance.subtitleTrack, track = __privateGet(this, _ctx7).textTracks.getById(`hls-${data.type}-${index}`);
        if (!track) return;
        const event = __privateMethod(this, _HLSController_instances, createDOMEvent_fn2).call(this, eventType, data);
        for (const cue of data.cues) {
          cue.positionAlign = "auto";
          track.addCue(cue, event);
        }
      }, onAudioSwitch_fn = function(eventType, data) {
        const track = __privateGet(this, _ctx7).audioTracks[data.id];
        if (track) {
          const trigger = __privateMethod(this, _HLSController_instances, createDOMEvent_fn2).call(this, eventType, data);
          __privateGet(this, _ctx7).audioTracks[ListSymbol.select](track, true, trigger);
        }
      }, onLevelSwitched_fn = function(eventType, data) {
        const quality = __privateGet(this, _ctx7).qualities[data.level];
        if (quality) {
          const trigger = __privateMethod(this, _HLSController_instances, createDOMEvent_fn2).call(this, eventType, data);
          __privateGet(this, _ctx7).qualities[ListSymbol.select](quality, true, trigger);
        }
      }, onLevelUpdated_fn = function(eventType, data) {
        if (data.details.totalduration > 0) __privateGet(this, _ctx7).$state.inferredLiveDVRWindow.set(data.details.totalduration);
      }, onLevelLoaded_fn = function(instance, eventType, data) {
        if (__privateGet(this, _ctx7).$state.canPlay()) return;
        const media = instance.media;
        if (!media) return;
        const { type, live, totalduration: duration, targetduration } = data.details, trigger = __privateMethod(this, _HLSController_instances, createDOMEvent_fn2).call(this, eventType, data);
        __privateGet(this, _ctx7).notify("stream-type-change", live ? type === "EVENT" && Number.isFinite(duration) && targetduration >= 10 ? "live:dvr" : "live" : "on-demand", trigger);
        __privateGet(this, _ctx7).notify("duration-change", duration, trigger);
        if (instance.currentLevel === -1) __privateGet(this, _ctx7).qualities[QualitySymbol.setAuto](true, trigger);
        for (const remoteTrack of instance.audioTracks) {
          const localTrack = {
            id: remoteTrack.id.toString(),
            label: remoteTrack.name,
            language: remoteTrack.lang || "",
            kind: "main"
          };
          __privateGet(this, _ctx7).audioTracks[ListSymbol.add](localTrack, trigger);
        }
        for (const level of instance.levels) {
          const videoQuality = {
            id: level.id?.toString() ?? level.height + "p",
            width: level.width,
            height: level.height,
            codec: level.codecSet,
            bitrate: level.bitrate
          };
          __privateGet(this, _ctx7).qualities[ListSymbol.add](videoQuality, trigger);
        }
        media.dispatchEvent(new DOMEvent("canplay", { trigger }));
      }, onError_fn3 = function(eventType, data) {
        if (data.fatal) switch (data.type) {
          case "mediaError":
            __privateGet(this, _instance2)?.recoverMediaError();
            break;
          default:
            __privateMethod(this, _HLSController_instances, onFatalError_fn2).call(this, data.error);
            break;
        }
      }, onFatalError_fn2 = function(error) {
        __privateGet(this, _ctx7).notify("error", {
          message: error.message,
          code: 1,
          error
        });
      }, enableAutoQuality_fn2 = function() {
        if (__privateGet(this, _instance2)) __privateGet(this, _instance2).currentLevel = -1;
      }, onUserQualityChange_fn2 = function() {
        const { qualities } = __privateGet(this, _ctx7);
        if (!__privateGet(this, _instance2) || qualities.auto) return;
        __privateGet(this, _instance2)[qualities.switch + "Level"] = qualities.selectedIndex;
        if (IS_CHROME) __privateGet(this, _video5).currentTime = __privateGet(this, _video5).currentTime;
      }, onUserAudioChange_fn2 = function() {
        const { audioTracks } = __privateGet(this, _ctx7);
        if (__privateGet(this, _instance2) && __privateGet(this, _instance2).audioTrack !== audioTracks.selectedIndex) __privateGet(this, _instance2).audioTrack = audioTracks.selectedIndex;
      }, _a22);
      HLSLibLoader = (_a23 = class {
        constructor(lib, ctx, callback) {
          __privateAdd(this, _HLSLibLoader_instances);
          __privateAdd(this, _lib2);
          __privateAdd(this, _ctx8);
          __privateAdd(this, _callback3);
          __privateSet(this, _lib2, lib);
          __privateSet(this, _ctx8, ctx);
          __privateSet(this, _callback3, callback);
          __privateMethod(this, _HLSLibLoader_instances, startLoading_fn2).call(this);
        }
      }, _lib2 = new WeakMap(), _ctx8 = new WeakMap(), _callback3 = new WeakMap(), _HLSLibLoader_instances = new WeakSet(), startLoading_fn2 = async function() {
        const callbacks = {
          onLoadStart: __privateMethod(this, _HLSLibLoader_instances, onLoadStart_fn3).bind(this),
          onLoaded: __privateMethod(this, _HLSLibLoader_instances, onLoaded_fn2).bind(this),
          onLoadError: __privateMethod(this, _HLSLibLoader_instances, onLoadError_fn2).bind(this)
        };
        let ctor = await loadHLSScript(__privateGet(this, _lib2), callbacks);
        if (isUndefined(ctor) && !isString(__privateGet(this, _lib2))) ctor = await importHLS(__privateGet(this, _lib2), callbacks);
        if (!ctor) return null;
        if (!ctor.isSupported()) {
          const message = "[vidstack] `hls.js` is not supported in this environment";
          __privateGet(this, _ctx8).player.dispatch(new DOMEvent("hls-unsupported"));
          __privateGet(this, _ctx8).notify("error", {
            message,
            code: 4
          });
          return null;
        }
        return ctor;
      }, onLoadStart_fn3 = function() {
        __privateGet(this, _ctx8).player.dispatch(new DOMEvent("hls-lib-load-start"));
      }, onLoaded_fn2 = function(ctor) {
        __privateGet(this, _ctx8).player.dispatch(new DOMEvent("hls-lib-loaded", { detail: ctor }));
        __privateGet(this, _callback3).call(this, ctor);
      }, onLoadError_fn2 = function(e2) {
        const error = coerceToError(e2);
        __privateGet(this, _ctx8).player.dispatch(new DOMEvent("hls-lib-load-error", { detail: error }));
        __privateGet(this, _ctx8).notify("error", {
          message: error.message,
          code: 4,
          error
        });
      }, _a23);
      JS_DELIVR_CDN2 = "https://cdn.jsdelivr.net";
      HLSProvider = (_a24 = class extends VideoProvider {
        constructor() {
          super(...arguments);
          __publicField(this, "$$PROVIDER_TYPE", "HLS");
          __privateAdd(this, _ctor2, null);
          __privateAdd(this, _controller3, new HLSController(this.video, this.ctx));
          __privateAdd(this, _library2, `${JS_DELIVR_CDN2}/npm/hls.js@^1.5.0/dist/hls.min.js`);
        }
        /**
        * The `hls.js` constructor.
        */
        get ctor() {
          return __privateGet(this, _ctor2);
        }
        /**
        * The current `hls.js` instance.
        */
        get instance() {
          return __privateGet(this, _controller3).instance;
        }
        get type() {
          return "hls";
        }
        get canLiveSync() {
          return true;
        }
        /**
        * The `hls.js` configuration object.
        *
        * @see {@link https://github.com/video-dev/hls.js/blob/master/docs/API.md#fine-tuning}
        */
        get config() {
          return __privateGet(this, _controller3).config;
        }
        set config(config) {
          __privateGet(this, _controller3).config = config;
        }
        /**
        * The `hls.js` constructor (supports dynamic imports) or a URL of where it can be found.
        *
        * @defaultValue `https://cdn.jsdelivr.net/npm/hls.js@^1.0.0/dist/hls.min.js`
        */
        get library() {
          return __privateGet(this, _library2);
        }
        set library(library) {
          __privateSet(this, _library2, library);
        }
        preconnect() {
          if (!isString(__privateGet(this, _library2))) return;
          preconnect(__privateGet(this, _library2));
        }
        setup() {
          super.setup();
          new HLSLibLoader(__privateGet(this, _library2), this.ctx, (ctor) => {
            __privateSet(this, _ctor2, ctor);
            __privateGet(this, _controller3).setup(ctor);
            this.ctx.notify("provider-setup", this);
            const src = peek(this.ctx.$state.source);
            if (src) this.loadSource(src);
          });
        }
        async loadSource(src, preload) {
          if (!isString(src.src)) {
            this.removeSource();
            return;
          }
          this.media.preload = preload || "";
          this.appendSource(src, "application/x-mpegurl");
          __privateGet(this, _controller3).loadSource(src);
          this.currentSrc = src;
        }
        /**
        * The given callback is invoked when a new `hls.js` instance is created and right before it's
        * attached to media.
        */
        onInstance(callback) {
          const instance = __privateGet(this, _controller3).instance;
          if (instance) callback(instance);
          return __privateGet(this, _controller3).onInstance(callback);
        }
        destroy() {
          __privateGet(this, _controller3).destroy();
        }
      }, _ctor2 = new WeakMap(), _controller3 = new WeakMap(), _library2 = new WeakMap(), /**
      * Whether `hls.js` is supported in this environment.
      */
      __publicField(_a24, "supported", isHLSSupported()), _a24);
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-CQuJTPFJ.js
  var _iframe, _EmbedProvider_instances, watchSrc_fn, onWindowMessage_fn, _a25, EmbedProvider;
  var init_vidstack_CQuJTPFJ = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-CQuJTPFJ.js"() {
      "use client";
      init_define_import_meta_env();
      init_vidstack_B9fTkxQO();
      init_vidstack_CvezEmSQ();
      EmbedProvider = (_a25 = class {
        constructor(iframe) {
          __privateAdd(this, _EmbedProvider_instances);
          __privateAdd(this, _iframe);
          __publicField(this, "src", signal(""));
          /**
          * Defines which referrer is sent when fetching the resource.
          *
          * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/HTMLIFrameElement/referrerPolicy}
          */
          __publicField(this, "referrerPolicy", null);
          __privateSet(this, _iframe, iframe);
          iframe.setAttribute("frameBorder", "0");
          iframe.setAttribute("aria-hidden", "true");
          iframe.setAttribute("allow", "autoplay; fullscreen; encrypted-media; picture-in-picture; accelerometer; gyroscope");
          if (this.referrerPolicy !== null) iframe.setAttribute("referrerpolicy", this.referrerPolicy);
        }
        get iframe() {
          return __privateGet(this, _iframe);
        }
        setup() {
          listenEvent(window, "message", __privateMethod(this, _EmbedProvider_instances, onWindowMessage_fn).bind(this));
          listenEvent(__privateGet(this, _iframe), "load", this.onLoad.bind(this));
          effect(__privateMethod(this, _EmbedProvider_instances, watchSrc_fn).bind(this));
        }
        postMessage(message, target) {
          if (IS_SERVER2) return;
          __privateGet(this, _iframe).contentWindow?.postMessage(JSON.stringify(message), target ?? "*");
        }
      }, _iframe = new WeakMap(), _EmbedProvider_instances = new WeakSet(), watchSrc_fn = function() {
        const src = this.src();
        if (!src.length) {
          __privateGet(this, _iframe).setAttribute("src", "");
          return;
        }
        const params = peek(() => this.buildParams());
        __privateGet(this, _iframe).setAttribute("src", appendParamsToURL(src, params));
      }, onWindowMessage_fn = function(event) {
        const origin = this.getOrigin();
        if (!((event.source === null || event.source === __privateGet(this, _iframe)?.contentWindow) && (!isString(origin) || origin === event.origin))) return;
        try {
          const message = JSON.parse(event.data);
          if (message) this.onMessage(message, event);
          return;
        } catch (e2) {
        }
        if (event.data) this.onMessage(event.data, event);
      }, _a25);
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-C0fPJIzs.js
  var vidstack_C0fPJIzs_exports = {};
  __export(vidstack_C0fPJIzs_exports, {
    getVimeoVideoInfo: () => getVimeoVideoInfo,
    resolveVimeoVideoId: () => resolveVimeoVideoId
  });
  function resolveVimeoVideoId(src) {
    const matches = src.match(videoIdRE);
    return {
      videoId: matches?.[1],
      hash: matches?.[2]
    };
  }
  async function getVimeoVideoInfo(videoId, abort, videoHash) {
    if (infoCache.has(videoId)) return infoCache.get(videoId);
    if (pendingFetch.has(videoId)) return pendingFetch.get(videoId);
    let oembedSrc = `https://vimeo.com/api/oembed.json?url=https://player.vimeo.com/video/${videoId}`;
    if (videoHash) oembedSrc = oembedSrc.concat(`?h=${videoHash}`);
    const promise = window.fetch(oembedSrc, {
      mode: "cors",
      signal: abort.signal
    }).then((response) => response.json()).then((data) => {
      const info = {
        title: data?.title ?? "",
        duration: data?.duration ?? 0,
        poster: resolveVimeoPoster(data),
        pro: data.account_type !== "basic"
      };
      infoCache.set(videoId, info);
      return info;
    }).finally(() => pendingFetch.delete(videoId));
    pendingFetch.set(videoId, promise);
    return promise;
  }
  function resolveVimeoPoster(data) {
    const thumbnailUrl = data?.thumbnail_url ?? "", thumbnailId = thumbnailUrl.match(/vimeocdn\.com\/video\/([^_/?#]+)_/)?.[1];
    if (!thumbnailId) return thumbnailUrl;
    const dimensions = resolvePosterDimensions(data);
    return dimensions ? `https://i.vimeocdn.com/video/${thumbnailId}_${dimensions.width}x${dimensions.height}.webp` : thumbnailUrl;
  }
  function resolvePosterDimensions(data) {
    const width = data.width || data.thumbnail_width, height = data.height || data.thumbnail_height;
    if (!width || !height) return null;
    if (width > height) return {
      width: posterMaxSize,
      height: Math.round(posterMaxSize * height / width)
    };
    return {
      width: Math.round(posterMaxSize * width / height),
      height: posterMaxSize
    };
  }
  var videoIdRE, infoCache, pendingFetch, posterMaxSize;
  var init_vidstack_C0fPJIzs = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-C0fPJIzs.js"() {
      "use client";
      init_define_import_meta_env();
      videoIdRE = /(?:https:\/\/)?(?:player\.)?vimeo(?:\.com)?\/(?:video\/)?(\d+)(?:(?:\?hash=|\?h=|\/)(.*))?/;
      infoCache = /* @__PURE__ */ new Map();
      pendingFetch = /* @__PURE__ */ new Map();
      posterMaxSize = 1920;
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-GkTzay8D.js
  var vidstack_GkTzay8D_exports = {};
  __export(vidstack_GkTzay8D_exports, {
    VimeoProvider: () => VimeoProvider
  });
  var trackedVimeoEvents, _ctx9, _videoId, _pro, _hash, _currentSrc, _fullscreenActive, _seekableRange, _timeRAF2, _currentCue, _chaptersTrack, _promises, _videoInfoPromise, _VimeoProvider_instances, watchVideoId_fn, watchVideoInfo_fn, watchPro_fn, onAnimationFrame_fn2, _preventTimeUpdates, onTimeUpdate_fn2, onSeeked_fn2, onLoaded_fn3, onReady_fn, onMethod_fn, attachListeners_fn, onPause_fn2, onPlay_fn2, onPlayProgress_fn, onLoadProgress_fn, onBufferStart_fn, onBufferEnd_fn, onWaiting_fn2, onVolumeChange_fn2, onChaptersChange_fn, removeChapters_fn, onQualitiesChange_fn, onQualityChange_fn2, onEvent_fn, onError_fn4, remote_fn, reset_fn2, getPromise_fn, _a26, VimeoProvider;
  var init_vidstack_GkTzay8D = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-GkTzay8D.js"() {
      "use client";
      init_define_import_meta_env();
      init_vidstack_B9fTkxQO();
      init_vidstack_CvezEmSQ();
      init_vidstack_CQuJTPFJ();
      init_vidstack_C0fPJIzs();
      trackedVimeoEvents = [
        "bufferend",
        "bufferstart",
        "durationchange",
        "ended",
        "enterpictureinpicture",
        "error",
        "fullscreenchange",
        "leavepictureinpicture",
        "loaded",
        "playProgress",
        "loadProgress",
        "pause",
        "play",
        "playbackratechange",
        "qualitychange",
        "seeked",
        "seeking",
        "timeupdate",
        "volumechange",
        "waiting"
      ];
      VimeoProvider = (_a26 = class extends EmbedProvider {
        constructor(iframe, ctx) {
          super(iframe);
          __privateAdd(this, _VimeoProvider_instances);
          __publicField(this, "$$PROVIDER_TYPE", "VIMEO");
          __publicField(this, "scope", createScope());
          __publicField(this, "fullscreen");
          __privateAdd(this, _ctx9);
          __privateAdd(this, _videoId, signal(""));
          __privateAdd(this, _pro, signal(false));
          __privateAdd(this, _hash, null);
          __privateAdd(this, _currentSrc, null);
          __privateAdd(this, _fullscreenActive, false);
          __privateAdd(this, _seekableRange, new TimeRange(0, 0));
          __privateAdd(this, _timeRAF2, new RAFLoop(__privateMethod(this, _VimeoProvider_instances, onAnimationFrame_fn2).bind(this)));
          __privateAdd(this, _currentCue, null);
          __privateAdd(this, _chaptersTrack, null);
          __privateAdd(this, _promises, /* @__PURE__ */ new Map());
          __privateAdd(this, _videoInfoPromise, null);
          /**
          * Whether tracking session data should be enabled on the embed, including cookies and analytics.
          * This is turned off by default to be GDPR-compliant.
          *
          * @defaultValue `false`
          */
          __publicField(this, "cookies", false);
          __publicField(this, "title", true);
          __publicField(this, "byline", true);
          __publicField(this, "portrait", true);
          __publicField(this, "color", "00ADEF");
          __privateAdd(this, _preventTimeUpdates, false);
          __privateSet(this, _ctx9, ctx);
          const self = this;
          this.fullscreen = {
            get active() {
              return __privateGet(self, _fullscreenActive);
            },
            supported: true,
            enter: () => __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "requestFullscreen"),
            exit: () => __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "exitFullscreen")
          };
        }
        get type() {
          return "vimeo";
        }
        get currentSrc() {
          return __privateGet(this, _currentSrc);
        }
        get videoId() {
          return __privateGet(this, _videoId).call(this);
        }
        get hash() {
          return __privateGet(this, _hash);
        }
        get isPro() {
          return __privateGet(this, _pro).call(this);
        }
        preconnect() {
          preconnect(this.getOrigin());
        }
        setup() {
          super.setup();
          effect(__privateMethod(this, _VimeoProvider_instances, watchVideoId_fn).bind(this));
          effect(__privateMethod(this, _VimeoProvider_instances, watchVideoInfo_fn).bind(this));
          effect(__privateMethod(this, _VimeoProvider_instances, watchPro_fn).bind(this));
          __privateGet(this, _ctx9).notify("provider-setup", this);
        }
        destroy() {
          __privateMethod(this, _VimeoProvider_instances, reset_fn2).call(this);
          this.fullscreen = void 0;
          const message = "provider destroyed";
          for (const promises of __privateGet(this, _promises).values()) for (const { reject } of promises) reject(message);
          __privateGet(this, _promises).clear();
          __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "destroy");
        }
        async play() {
          return __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "play");
        }
        async pause() {
          return __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "pause");
        }
        setMuted(muted) {
          __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "setMuted", muted);
        }
        setCurrentTime(time) {
          __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "seekTo", time);
          __privateGet(this, _ctx9).notify("seeking", time);
        }
        setVolume(volume) {
          __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "setVolume", volume);
          __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "setMuted", peek(__privateGet(this, _ctx9).$state.muted));
        }
        setPlaybackRate(rate) {
          __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "setPlaybackRate", rate);
        }
        async loadSource(src) {
          if (!isString(src.src)) {
            __privateSet(this, _currentSrc, null);
            __privateSet(this, _hash, null);
            __privateGet(this, _videoId).set("");
            return;
          }
          const { videoId, hash } = resolveVimeoVideoId(src.src);
          __privateGet(this, _videoId).set(videoId ?? "");
          __privateSet(this, _hash, hash ?? null);
          __privateSet(this, _currentSrc, src);
        }
        getOrigin() {
          return "https://player.vimeo.com";
        }
        buildParams() {
          const { keyDisabled } = __privateGet(this, _ctx9).$props, { playsInline, nativeControls } = __privateGet(this, _ctx9).$state, showControls = nativeControls();
          return {
            title: this.title,
            byline: this.byline,
            color: this.color,
            portrait: this.portrait,
            controls: showControls,
            h: this.hash,
            keyboard: showControls && !keyDisabled(),
            transparent: true,
            playsinline: playsInline(),
            dnt: !this.cookies
          };
        }
        onMessage(message, event) {
          if (message.event) __privateMethod(this, _VimeoProvider_instances, onEvent_fn).call(this, message.event, message.data, event);
          else if (message.method) __privateMethod(this, _VimeoProvider_instances, onMethod_fn).call(this, message.method, message.value, event);
        }
        onLoad() {
        }
      }, _ctx9 = new WeakMap(), _videoId = new WeakMap(), _pro = new WeakMap(), _hash = new WeakMap(), _currentSrc = new WeakMap(), _fullscreenActive = new WeakMap(), _seekableRange = new WeakMap(), _timeRAF2 = new WeakMap(), _currentCue = new WeakMap(), _chaptersTrack = new WeakMap(), _promises = new WeakMap(), _videoInfoPromise = new WeakMap(), _VimeoProvider_instances = new WeakSet(), watchVideoId_fn = function() {
        __privateMethod(this, _VimeoProvider_instances, reset_fn2).call(this);
        const videoId = __privateGet(this, _videoId).call(this);
        if (!videoId) {
          this.src.set("");
          return;
        }
        this.src.set(`${this.getOrigin()}/video/${videoId}`);
        __privateGet(this, _ctx9).notify("load-start");
      }, watchVideoInfo_fn = function() {
        const videoId = __privateGet(this, _videoId).call(this);
        if (!videoId) return;
        const promise = deferredPromise(), abort = new AbortController();
        __privateSet(this, _videoInfoPromise, promise);
        getVimeoVideoInfo(videoId, abort, __privateGet(this, _hash)).then((info) => {
          promise.resolve(info);
        }).catch((e2) => {
          promise.reject();
        });
        return () => {
          promise.reject();
          abort.abort();
        };
      }, watchPro_fn = function() {
        const isPro = __privateGet(this, _pro).call(this), { $state, qualities } = __privateGet(this, _ctx9);
        $state.canSetPlaybackRate.set(isPro);
        qualities[ListSymbol.setReadonly](!isPro);
        if (isPro) return listenEvent(qualities, "change", () => {
          if (qualities.auto) return;
          const id2 = qualities.selected?.id;
          if (id2) __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "setQuality", id2);
        });
      }, onAnimationFrame_fn2 = function() {
        __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "getCurrentTime");
      }, _preventTimeUpdates = new WeakMap(), onTimeUpdate_fn2 = function(time, trigger) {
        if (__privateGet(this, _preventTimeUpdates) && time === 0) return;
        const { realCurrentTime, paused, bufferedEnd, seekableEnd, live } = __privateGet(this, _ctx9).$state;
        if (realCurrentTime() === time) return;
        const prevTime = realCurrentTime();
        __privateGet(this, _ctx9).notify("time-change", time, trigger);
        if (Math.abs(prevTime - time) > 1.5) {
          __privateGet(this, _ctx9).notify("seeking", time, trigger);
          if (!paused() && bufferedEnd() < time) __privateGet(this, _ctx9).notify("waiting", void 0, trigger);
        }
        if (!live() && seekableEnd() - time < 0.01) {
          __privateGet(this, _ctx9).notify("end", void 0, trigger);
          __privateSet(this, _preventTimeUpdates, true);
          setTimeout(() => {
            __privateSet(this, _preventTimeUpdates, false);
          }, 500);
        }
      }, onSeeked_fn2 = function(time, trigger) {
        __privateGet(this, _ctx9).notify("seeked", time, trigger);
      }, onLoaded_fn3 = function(trigger) {
        const videoId = __privateGet(this, _videoId).call(this);
        __privateGet(this, _videoInfoPromise)?.promise.then((info) => {
          if (!info) return;
          const { title, poster, duration, pro } = info;
          __privateGet(this, _pro).set(pro);
          __privateGet(this, _ctx9).notify("title-change", title, trigger);
          __privateGet(this, _ctx9).notify("poster-change", poster, trigger);
          __privateGet(this, _ctx9).notify("duration-change", duration, trigger);
          __privateMethod(this, _VimeoProvider_instances, onReady_fn).call(this, duration, trigger);
        }).catch(() => {
          if (videoId !== __privateGet(this, _videoId).call(this)) return;
          __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "getVideoTitle");
          __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "getDuration");
        });
      }, onReady_fn = function(duration, trigger) {
        const { nativeControls } = __privateGet(this, _ctx9).$state, showEmbedControls = nativeControls();
        __privateSet(this, _seekableRange, new TimeRange(0, duration));
        const detail = {
          buffered: new TimeRange(0, 0),
          seekable: __privateGet(this, _seekableRange),
          duration
        };
        __privateGet(this, _ctx9).delegate.ready(detail, trigger);
        if (!showEmbedControls) __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "_hideOverlay");
        __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "getQualities");
        __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "getChapters");
      }, onMethod_fn = function(method2, data, trigger) {
        switch (method2) {
          case "getVideoTitle":
            const videoTitle = data;
            __privateGet(this, _ctx9).notify("title-change", videoTitle, trigger);
            break;
          case "getDuration":
            const duration = data;
            if (!__privateGet(this, _ctx9).$state.canPlay()) __privateMethod(this, _VimeoProvider_instances, onReady_fn).call(this, duration, trigger);
            else __privateGet(this, _ctx9).notify("duration-change", duration, trigger);
            break;
          case "getCurrentTime":
            __privateMethod(this, _VimeoProvider_instances, onTimeUpdate_fn2).call(this, data, trigger);
            break;
          case "getBuffered":
            if (isArray(data) && data.length) __privateMethod(this, _VimeoProvider_instances, onLoadProgress_fn).call(this, data[data.length - 1][1], trigger);
            break;
          case "setMuted":
          case "getMuted":
            __privateMethod(this, _VimeoProvider_instances, onVolumeChange_fn2).call(this, peek(__privateGet(this, _ctx9).$state.volume), data, trigger);
            break;
          case "getChapters":
            __privateMethod(this, _VimeoProvider_instances, onChaptersChange_fn).call(this, data);
            break;
          case "getQualities":
            __privateMethod(this, _VimeoProvider_instances, onQualitiesChange_fn).call(this, data, trigger);
            break;
        }
        __privateMethod(this, _VimeoProvider_instances, getPromise_fn).call(this, method2)?.resolve();
      }, attachListeners_fn = function() {
        for (const type of trackedVimeoEvents) __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "addEventListener", type);
      }, onPause_fn2 = function(trigger) {
        __privateGet(this, _timeRAF2).stop();
        __privateGet(this, _ctx9).notify("pause", void 0, trigger);
      }, onPlay_fn2 = async function(trigger) {
        __privateGet(this, _timeRAF2).start();
        await __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "getMuted");
        __privateGet(this, _ctx9).notify("play", void 0, trigger);
      }, onPlayProgress_fn = function(trigger) {
        const { paused } = __privateGet(this, _ctx9).$state;
        if (!paused() && !__privateGet(this, _preventTimeUpdates)) __privateGet(this, _ctx9).notify("playing", void 0, trigger);
      }, onLoadProgress_fn = function(buffered, trigger) {
        const detail = {
          buffered: new TimeRange(0, buffered),
          seekable: __privateGet(this, _seekableRange)
        };
        __privateGet(this, _ctx9).notify("progress", detail, trigger);
      }, onBufferStart_fn = function(trigger) {
        __privateGet(this, _ctx9).notify("waiting", void 0, trigger);
      }, onBufferEnd_fn = function(trigger) {
        const { paused } = __privateGet(this, _ctx9).$state;
        if (!paused()) __privateGet(this, _ctx9).notify("playing", void 0, trigger);
      }, onWaiting_fn2 = async function(trigger) {
        const { paused } = __privateGet(this, _ctx9).$state;
        if (paused()) {
          await __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "getMuted");
          __privateGet(this, _ctx9).notify("play", void 0, trigger);
        }
        __privateGet(this, _ctx9).notify("waiting", void 0, trigger);
      }, onVolumeChange_fn2 = function(volume, muted, trigger) {
        const detail = {
          volume,
          muted
        };
        __privateGet(this, _ctx9).notify("volume-change", detail, trigger);
      }, onChaptersChange_fn = function(chapters) {
        __privateMethod(this, _VimeoProvider_instances, removeChapters_fn).call(this);
        if (!chapters.length) return;
        const track = new TextTrack({
          kind: "chapters",
          default: true
        }), { seekableEnd } = __privateGet(this, _ctx9).$state;
        for (let i = 0; i < chapters.length; i++) {
          const chapter = chapters[i], nextChapter = chapters[i + 1];
          track.addCue(new window.VTTCue(chapter.startTime, nextChapter?.startTime ?? seekableEnd(), chapter.title));
        }
        __privateSet(this, _chaptersTrack, track);
        __privateGet(this, _ctx9).textTracks.add(track);
      }, removeChapters_fn = function() {
        if (!__privateGet(this, _chaptersTrack)) return;
        __privateGet(this, _ctx9).textTracks.remove(__privateGet(this, _chaptersTrack));
        __privateSet(this, _chaptersTrack, null);
      }, onQualitiesChange_fn = function(qualities, trigger) {
        __privateGet(this, _ctx9).qualities[QualitySymbol.enableAuto] = qualities.some((q) => q.id === "auto") ? () => __privateMethod(this, _VimeoProvider_instances, remote_fn).call(this, "setQuality", "auto") : void 0;
        for (const quality of qualities) {
          if (quality.id === "auto") continue;
          const height = +quality.id.slice(0, -1);
          if (isNaN(height)) continue;
          __privateGet(this, _ctx9).qualities[ListSymbol.add]({
            id: quality.id,
            width: height * (16 / 9),
            height,
            codec: "avc1,h.264",
            bitrate: -1
          }, trigger);
        }
        __privateMethod(this, _VimeoProvider_instances, onQualityChange_fn2).call(this, qualities.find((q) => q.active), trigger);
      }, onQualityChange_fn2 = function({ id: id2 } = {}, trigger) {
        if (!id2) return;
        const isAuto = id2 === "auto", newQuality = __privateGet(this, _ctx9).qualities.getById(id2);
        if (isAuto) {
          __privateGet(this, _ctx9).qualities[QualitySymbol.setAuto](isAuto, trigger);
          __privateGet(this, _ctx9).qualities[ListSymbol.select](void 0, true, trigger);
        } else __privateGet(this, _ctx9).qualities[ListSymbol.select](newQuality ?? void 0, true, trigger);
      }, onEvent_fn = function(event, payload, trigger) {
        switch (event) {
          case "ready":
            __privateMethod(this, _VimeoProvider_instances, attachListeners_fn).call(this);
            break;
          case "loaded":
            __privateMethod(this, _VimeoProvider_instances, onLoaded_fn3).call(this, trigger);
            break;
          case "play":
            __privateMethod(this, _VimeoProvider_instances, onPlay_fn2).call(this, trigger);
            break;
          case "playProgress":
            __privateMethod(this, _VimeoProvider_instances, onPlayProgress_fn).call(this, trigger);
            break;
          case "pause":
            __privateMethod(this, _VimeoProvider_instances, onPause_fn2).call(this, trigger);
            break;
          case "loadProgress":
            __privateMethod(this, _VimeoProvider_instances, onLoadProgress_fn).call(this, payload.seconds, trigger);
            break;
          case "waiting":
            __privateMethod(this, _VimeoProvider_instances, onWaiting_fn2).call(this, trigger);
            break;
          case "bufferstart":
            __privateMethod(this, _VimeoProvider_instances, onBufferStart_fn).call(this, trigger);
            break;
          case "bufferend":
            __privateMethod(this, _VimeoProvider_instances, onBufferEnd_fn).call(this, trigger);
            break;
          case "volumechange":
            __privateMethod(this, _VimeoProvider_instances, onVolumeChange_fn2).call(this, payload.volume, peek(__privateGet(this, _ctx9).$state.muted), trigger);
            break;
          case "durationchange":
            __privateSet(this, _seekableRange, new TimeRange(0, payload.duration));
            __privateGet(this, _ctx9).notify("duration-change", payload.duration, trigger);
            break;
          case "playbackratechange":
            __privateGet(this, _ctx9).notify("rate-change", payload.playbackRate, trigger);
            break;
          case "qualitychange":
            __privateMethod(this, _VimeoProvider_instances, onQualityChange_fn2).call(this, payload, trigger);
            break;
          case "fullscreenchange":
            __privateSet(this, _fullscreenActive, payload.fullscreen);
            __privateGet(this, _ctx9).notify("fullscreen-change", payload.fullscreen, trigger);
            break;
          case "enterpictureinpicture":
            __privateGet(this, _ctx9).notify("picture-in-picture-change", true, trigger);
            break;
          case "leavepictureinpicture":
            __privateGet(this, _ctx9).notify("picture-in-picture-change", false, trigger);
            break;
          case "ended":
            __privateGet(this, _ctx9).notify("end", void 0, trigger);
            break;
          case "error":
            __privateMethod(this, _VimeoProvider_instances, onError_fn4).call(this, payload, trigger);
            break;
          case "seek":
          case "seeked":
            __privateMethod(this, _VimeoProvider_instances, onSeeked_fn2).call(this, payload.seconds, trigger);
            break;
        }
      }, onError_fn4 = function(error, trigger) {
        const { message, method: method2 } = error;
        if (method2 === "setPlaybackRate") __privateGet(this, _pro).set(false);
        if (method2) __privateMethod(this, _VimeoProvider_instances, getPromise_fn).call(this, method2)?.reject(message);
      }, remote_fn = async function(command, arg) {
        let promise = deferredPromise(), promises = __privateGet(this, _promises).get(command);
        if (!promises) __privateGet(this, _promises).set(command, promises = []);
        promises.push(promise);
        this.postMessage({
          method: command,
          value: arg
        });
        return promise.promise;
      }, reset_fn2 = function() {
        __privateGet(this, _timeRAF2).stop();
        __privateSet(this, _seekableRange, new TimeRange(0, 0));
        __privateSet(this, _videoInfoPromise, null);
        __privateSet(this, _currentCue, null);
        __privateGet(this, _pro).set(false);
        __privateMethod(this, _VimeoProvider_instances, removeChapters_fn).call(this);
      }, getPromise_fn = function(command) {
        return __privateGet(this, _promises).get(command)?.shift();
      }, _a26);
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-Dy74GavT.js
  var vidstack_Dy74GavT_exports = {};
  __export(vidstack_Dy74GavT_exports, {
    findYouTubePoster: () => findYouTubePoster,
    resolveYouTubeVideoId: () => resolveYouTubeVideoId
  });
  function resolveYouTubeVideoId(src) {
    return src.match(videoIdRE2)?.[1];
  }
  async function findYouTubePoster(videoId, abort) {
    if (posterCache.has(videoId)) return posterCache.get(videoId);
    if (pendingFetch2.has(videoId)) return pendingFetch2.get(videoId);
    const pending2 = new Promise(async (resolve) => {
      for (const size2 of [
        "maxresdefault",
        "sddefault",
        "hqdefault"
      ]) for (const webp of [true, false]) {
        const url = resolveYouTubePosterURL(videoId, size2, webp);
        if ((await fetch(url, {
          mode: "no-cors",
          signal: abort.signal
        })).status < 400) {
          posterCache.set(videoId, url);
          resolve(url);
          return;
        }
      }
    }).catch(() => "").finally(() => pendingFetch2.delete(videoId));
    pendingFetch2.set(videoId, pending2);
    return pending2;
  }
  function resolveYouTubePosterURL(videoId, size2, webp) {
    return `https://i.ytimg.com/${webp ? "vi_webp" : "vi"}/${videoId}/${size2}.${webp ? "webp" : "jpg"}`;
  }
  var videoIdRE2, posterCache, pendingFetch2;
  var init_vidstack_Dy74GavT = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-Dy74GavT.js"() {
      "use client";
      init_define_import_meta_env();
      videoIdRE2 = /(?:youtu\.be|youtube|youtube\.com|youtube-nocookie\.com)(?:\/shorts)?\/(?:embed\/|v\/|watch\?v=|watch\?.+&v=|)((?:\w|-){11})/;
      posterCache = /* @__PURE__ */ new Map();
      pendingFetch2 = /* @__PURE__ */ new Map();
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-BmcqUMKx.js
  var vidstack_BmcqUMKx_exports = {};
  __export(vidstack_BmcqUMKx_exports, {
    YouTubeProvider: () => YouTubeProvider
  });
  var YouTubePlayerState, _ctx10, _videoId2, _state3, _currentSrc2, _seekingTimer, _invalidPlay, _promises2, _YouTubeProvider_instances, playFail_fn, pauseFail_fn, watchVideoId_fn2, remote_fn2, onReady_fn2, onPause_fn3, onTimeUpdate_fn3, onProgress_fn2, onSeeked_fn3, onEnded_fn2, onStateChange_fn2, reset_fn3, getPromise_fn2, isPending_fn, _a27, YouTubeProvider;
  var init_vidstack_BmcqUMKx = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-BmcqUMKx.js"() {
      "use client";
      init_define_import_meta_env();
      init_vidstack_B9fTkxQO();
      init_vidstack_CvezEmSQ();
      init_vidstack_CQuJTPFJ();
      init_vidstack_Dy74GavT();
      YouTubePlayerState = {
        Unstarted: -1,
        Ended: 0,
        Playing: 1,
        Paused: 2,
        Buffering: 3,
        Cued: 5
      };
      YouTubeProvider = (_a27 = class extends EmbedProvider {
        constructor(iframe, ctx) {
          super(iframe);
          __privateAdd(this, _YouTubeProvider_instances);
          __publicField(this, "$$PROVIDER_TYPE", "YOUTUBE");
          __publicField(this, "scope", createScope());
          __privateAdd(this, _ctx10);
          __privateAdd(this, _videoId2, signal(""));
          __privateAdd(this, _state3, -1);
          __privateAdd(this, _currentSrc2, null);
          __privateAdd(this, _seekingTimer, -1);
          __privateAdd(this, _invalidPlay, false);
          __privateAdd(this, _promises2, /* @__PURE__ */ new Map());
          /**
          * Sets the player's interface language. The parameter value is an ISO 639-1 two-letter
          * language code or a fully specified locale. For example, fr and fr-ca are both valid values.
          * Other language input codes, such as IETF language tags (BCP 47) might also be handled properly.
          *
          * The interface language is used for tooltips in the player and also affects the default caption
          * track. Note that YouTube might select a different caption track language for a particular
          * user based on the user's individual language preferences and the availability of caption tracks.
          *
          * @defaultValue 'en'
          */
          __publicField(this, "language", "en");
          __publicField(this, "color", "red");
          /**
          * Whether cookies should be enabled on the embed. This is turned off by default to be
          * GDPR-compliant.
          *
          * @defaultValue `false`
          */
          __publicField(this, "cookies", false);
          __privateSet(this, _ctx10, ctx);
        }
        get currentSrc() {
          return __privateGet(this, _currentSrc2);
        }
        get type() {
          return "youtube";
        }
        get videoId() {
          return __privateGet(this, _videoId2).call(this);
        }
        preconnect() {
          preconnect(this.getOrigin());
        }
        setup() {
          super.setup();
          effect(__privateMethod(this, _YouTubeProvider_instances, watchVideoId_fn2).bind(this));
          __privateGet(this, _ctx10).notify("provider-setup", this);
        }
        destroy() {
          __privateMethod(this, _YouTubeProvider_instances, reset_fn3).call(this);
          const message = "provider destroyed";
          for (const promises of __privateGet(this, _promises2).values()) for (const { reject } of promises) reject(message);
          __privateGet(this, _promises2).clear();
        }
        async play() {
          return __privateMethod(this, _YouTubeProvider_instances, remote_fn2).call(this, "playVideo");
        }
        async pause() {
          return __privateMethod(this, _YouTubeProvider_instances, remote_fn2).call(this, "pauseVideo");
        }
        setMuted(muted) {
          if (muted) __privateMethod(this, _YouTubeProvider_instances, remote_fn2).call(this, "mute");
          else __privateMethod(this, _YouTubeProvider_instances, remote_fn2).call(this, "unMute");
        }
        setCurrentTime(time) {
          __privateMethod(this, _YouTubeProvider_instances, remote_fn2).call(this, "seekTo", [time, true]);
          __privateGet(this, _ctx10).notify("seeking", time);
        }
        setVolume(volume) {
          __privateMethod(this, _YouTubeProvider_instances, remote_fn2).call(this, "setVolume", volume * 100);
        }
        setPlaybackRate(rate) {
          __privateMethod(this, _YouTubeProvider_instances, remote_fn2).call(this, "setPlaybackRate", rate);
        }
        async loadSource(src) {
          if (!isString(src.src)) {
            __privateSet(this, _currentSrc2, null);
            __privateGet(this, _videoId2).set("");
            return;
          }
          const videoId = resolveYouTubeVideoId(src.src);
          __privateGet(this, _videoId2).set(videoId ?? "");
          __privateSet(this, _currentSrc2, src);
        }
        getOrigin() {
          return !this.cookies ? "https://www.youtube-nocookie.com" : "https://www.youtube.com";
        }
        buildParams() {
          const { keyDisabled } = __privateGet(this, _ctx10).$props, { muted, playsInline, nativeControls } = __privateGet(this, _ctx10).$state, showControls = nativeControls();
          return {
            rel: 0,
            autoplay: 0,
            cc_lang_pref: this.language,
            cc_load_policy: showControls ? 1 : void 0,
            color: this.color,
            controls: showControls ? 1 : 0,
            disablekb: !showControls || keyDisabled() ? 1 : 0,
            enablejsapi: 1,
            fs: 1,
            hl: this.language,
            iv_load_policy: showControls ? 1 : 3,
            mute: muted() ? 1 : 0,
            playsinline: playsInline() ? 1 : 0
          };
        }
        onLoad() {
          window.setTimeout(() => this.postMessage({ event: "listening" }), 100);
        }
        onMessage({ info }, event) {
          if (!info) return;
          const { title, intrinsicDuration, playbackRate } = __privateGet(this, _ctx10).$state;
          if (isObject(info.videoData) && info.videoData.title !== title()) __privateGet(this, _ctx10).notify("title-change", info.videoData.title, event);
          if (isNumber(info.duration) && info.duration !== intrinsicDuration()) {
            if (isNumber(info.videoLoadedFraction)) {
              const buffered = info.progressState?.loaded ?? info.videoLoadedFraction * info.duration, seekable = new TimeRange(0, info.duration);
              __privateMethod(this, _YouTubeProvider_instances, onProgress_fn2).call(this, buffered, seekable, event);
            }
            __privateGet(this, _ctx10).notify("duration-change", info.duration, event);
          }
          if (isNumber(info.playbackRate) && info.playbackRate !== playbackRate()) __privateGet(this, _ctx10).notify("rate-change", info.playbackRate, event);
          if (info.progressState) {
            const { current, seekableStart, seekableEnd, loaded, duration } = info.progressState;
            __privateMethod(this, _YouTubeProvider_instances, onTimeUpdate_fn3).call(this, current, event);
            __privateMethod(this, _YouTubeProvider_instances, onProgress_fn2).call(this, loaded, new TimeRange(seekableStart, seekableEnd), event);
            if (duration !== intrinsicDuration()) __privateGet(this, _ctx10).notify("duration-change", duration, event);
          }
          if (isNumber(info.volume) && isBoolean(info.muted) && !__privateGet(this, _invalidPlay)) {
            const detail = {
              muted: info.muted,
              volume: info.volume / 100
            };
            __privateGet(this, _ctx10).notify("volume-change", detail, event);
          }
          if (isNumber(info.playerState) && info.playerState !== __privateGet(this, _state3)) __privateMethod(this, _YouTubeProvider_instances, onStateChange_fn2).call(this, info.playerState, event);
        }
      }, _ctx10 = new WeakMap(), _videoId2 = new WeakMap(), _state3 = new WeakMap(), _currentSrc2 = new WeakMap(), _seekingTimer = new WeakMap(), _invalidPlay = new WeakMap(), _promises2 = new WeakMap(), _YouTubeProvider_instances = new WeakSet(), playFail_fn = function(message) {
        __privateMethod(this, _YouTubeProvider_instances, getPromise_fn2).call(this, "playVideo")?.reject(message);
      }, pauseFail_fn = function(message) {
        __privateMethod(this, _YouTubeProvider_instances, getPromise_fn2).call(this, "pauseVideo")?.reject(message);
      }, watchVideoId_fn2 = function() {
        __privateMethod(this, _YouTubeProvider_instances, reset_fn3).call(this);
        const videoId = __privateGet(this, _videoId2).call(this);
        if (!videoId) {
          this.src.set("");
          return;
        }
        this.src.set(`${this.getOrigin()}/embed/${videoId}`);
        __privateGet(this, _ctx10).notify("load-start");
      }, remote_fn2 = function(command, arg) {
        let promise = deferredPromise(), promises = __privateGet(this, _promises2).get(command);
        if (!promises) __privateGet(this, _promises2).set(command, promises = []);
        promises.push(promise);
        const args = Array.isArray(arg) ? arg : arg ? [arg] : void 0;
        this.postMessage({
          event: "command",
          func: command,
          args
        });
        return promise.promise;
      }, onReady_fn2 = function(trigger) {
        __privateGet(this, _ctx10).notify("loaded-metadata");
        __privateGet(this, _ctx10).notify("loaded-data");
        __privateGet(this, _ctx10).delegate.ready(void 0, trigger);
      }, onPause_fn3 = function(trigger) {
        __privateMethod(this, _YouTubeProvider_instances, getPromise_fn2).call(this, "pauseVideo")?.resolve();
        __privateGet(this, _ctx10).notify("pause", void 0, trigger);
      }, onTimeUpdate_fn3 = function(time, trigger) {
        const { duration, realCurrentTime } = __privateGet(this, _ctx10).$state, hasEnded = __privateGet(this, _state3) === YouTubePlayerState.Ended, boundTime2 = hasEnded ? duration() : time;
        __privateGet(this, _ctx10).notify("time-change", boundTime2, trigger);
        if (!hasEnded && Math.abs(boundTime2 - realCurrentTime()) > 1) __privateGet(this, _ctx10).notify("seeking", boundTime2, trigger);
      }, onProgress_fn2 = function(buffered, seekable, trigger) {
        const detail = {
          buffered: new TimeRange(0, buffered),
          seekable
        };
        __privateGet(this, _ctx10).notify("progress", detail, trigger);
        const { seeking, realCurrentTime } = __privateGet(this, _ctx10).$state;
        if (seeking() && buffered > realCurrentTime()) __privateMethod(this, _YouTubeProvider_instances, onSeeked_fn3).call(this, trigger);
      }, onSeeked_fn3 = function(trigger) {
        const { paused, realCurrentTime } = __privateGet(this, _ctx10).$state;
        window.clearTimeout(__privateGet(this, _seekingTimer));
        __privateSet(this, _seekingTimer, window.setTimeout(() => {
          __privateGet(this, _ctx10).notify("seeked", realCurrentTime(), trigger);
          __privateSet(this, _seekingTimer, -1);
        }, paused() ? 100 : 0));
      }, onEnded_fn2 = function(trigger) {
        const { seeking } = __privateGet(this, _ctx10).$state;
        if (seeking()) __privateMethod(this, _YouTubeProvider_instances, onSeeked_fn3).call(this, trigger);
        __privateGet(this, _ctx10).notify("pause", void 0, trigger);
        __privateGet(this, _ctx10).notify("end", void 0, trigger);
      }, onStateChange_fn2 = function(state, trigger) {
        const { paused, seeking } = __privateGet(this, _ctx10).$state, isPlaying = state === YouTubePlayerState.Playing, isBuffering = state === YouTubePlayerState.Buffering, isPendingPlay = __privateMethod(this, _YouTubeProvider_instances, isPending_fn).call(this, "playVideo"), isPlay = paused() && (isBuffering || isPlaying);
        if (isBuffering) __privateGet(this, _ctx10).notify("waiting", void 0, trigger);
        if (seeking() && isPlaying) __privateMethod(this, _YouTubeProvider_instances, onSeeked_fn3).call(this, trigger);
        if (__privateGet(this, _invalidPlay) && isPlaying) {
          this.pause();
          __privateSet(this, _invalidPlay, false);
          this.setMuted(__privateGet(this, _ctx10).$state.muted());
          return;
        }
        if (!isPendingPlay && isPlay) {
          __privateSet(this, _invalidPlay, true);
          this.setMuted(true);
          return;
        }
        if (isPlay) {
          __privateMethod(this, _YouTubeProvider_instances, getPromise_fn2).call(this, "playVideo")?.resolve();
          __privateGet(this, _ctx10).notify("play", void 0, trigger);
        }
        switch (state) {
          case YouTubePlayerState.Cued:
            __privateMethod(this, _YouTubeProvider_instances, onReady_fn2).call(this, trigger);
            break;
          case YouTubePlayerState.Playing:
            __privateGet(this, _ctx10).notify("playing", void 0, trigger);
            break;
          case YouTubePlayerState.Paused:
            __privateMethod(this, _YouTubeProvider_instances, onPause_fn3).call(this, trigger);
            break;
          case YouTubePlayerState.Ended:
            __privateMethod(this, _YouTubeProvider_instances, onEnded_fn2).call(this, trigger);
            break;
        }
        __privateSet(this, _state3, state);
      }, reset_fn3 = function() {
        __privateSet(this, _state3, -1);
        __privateSet(this, _seekingTimer, -1);
        __privateSet(this, _invalidPlay, false);
      }, getPromise_fn2 = function(command) {
        return __privateGet(this, _promises2).get(command)?.shift();
      }, isPending_fn = function(command) {
        return Boolean(__privateGet(this, _promises2).get(command)?.length);
      }, _a27);
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-aCAqaLv8.js
  function getCastFrameworkURL() {
    return "https://www.gstatic.com/cv/js/sender/v1/cast_sender.js?loadCastFramework=1";
  }
  function hasLoadedCastFramework() {
    return !!window.cast?.framework;
  }
  function isCastAvailable() {
    return !!window.chrome?.cast?.isAvailable;
  }
  function isCastConnected() {
    return getCastContext().getCastState() === cast.framework.CastState.CONNECTED;
  }
  function getCastContext() {
    return window.cast.framework.CastContext.getInstance();
  }
  function getCastSession() {
    return getCastContext().getCurrentSession();
  }
  function getCastSessionMedia() {
    return getCastSession()?.getSessionObj().media[0];
  }
  function hasActiveCastSession(src) {
    return getCastSessionMedia()?.media.contentId === src?.src;
  }
  function getDefaultCastOptions() {
    return {
      language: "en-US",
      autoJoinPolicy: chrome.cast.AutoJoinPolicy.ORIGIN_SCOPED,
      receiverApplicationId: chrome.cast.media.DEFAULT_MEDIA_RECEIVER_APP_ID,
      resumeSavedSession: true,
      androidReceiverCompatible: true
    };
  }
  function getCastErrorMessage(code) {
    return `Google Cast Error Code: ${code}`;
  }
  function listenCastContextEvent(type, handler) {
    return listenEvent(getCastContext(), type, handler);
  }
  var init_vidstack_aCAqaLv8 = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-aCAqaLv8.js"() {
      "use client";
      init_define_import_meta_env();
      init_vidstack_B9fTkxQO();
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-D2vrrUMs.js
  var vidstack_D2vrrUMs_exports = {};
  __export(vidstack_D2vrrUMs_exports, {
    GoogleCastProvider: () => GoogleCastProvider
  });
  var _info, _GoogleCastMediaInfoBuilder_instances, buildCastTrack_fn, _a28, GoogleCastMediaInfoBuilder, _cast, _ctx11, _onNewLocalTracks, _GoogleCastTracksManager_instances, getLocalAudioTracks_fn, getRemoteTracks_fn, getRemoteActiveIds_fn, syncLocalTracks_fn, editTracksInfo_fn, findLocalTrack_fn, findRemoteTrack_fn, isMatch_fn, _a29, GoogleCastTracksManager, _player, _ctx12, _tracks, _currentSrc3, _state4, _currentTime2, _played, _seekableRange2, _timeRAF3, _playerEventHandlers, _reloadInfo, _isIdle, _GoogleCastProvider_instances, attachCastContextEventListeners_fn, attachCastPlayerEventListeners_fn, reset_fn4, resumeSession_fn, endSession_fn, disconnectFromReceiver_fn, onAnimationFrame_fn3, onRemotePlayerEvent_fn, onCastStateChange_fn, onMediaLoadedChange_fn, onCanControlVolumeChange_fn, onCanSeekChange_fn, getStreamType_fn, onCurrentTimeChange_fn, onDurationChange_fn2, onVolumeChange_fn3, onPausedChange_fn, onProgress_fn3, onPlayerStateChange_fn, getSeekableRange_fn, createEvent_fn, buildMediaInfo_fn, buildLoadRequest_fn, reload_fn, onNewLocalTracks_fn, _a30, GoogleCastProvider;
  var init_vidstack_D2vrrUMs = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-D2vrrUMs.js"() {
      "use client";
      init_define_import_meta_env();
      init_vidstack_B9fTkxQO();
      init_vidstack_CvezEmSQ();
      init_vidstack_aCAqaLv8();
      GoogleCastMediaInfoBuilder = (_a28 = class {
        constructor(src) {
          __privateAdd(this, _GoogleCastMediaInfoBuilder_instances);
          __privateAdd(this, _info);
          __privateSet(this, _info, new chrome.cast.media.MediaInfo(src.src, src.type));
        }
        build() {
          return __privateGet(this, _info);
        }
        setStreamType(streamType) {
          if (streamType.includes("live")) __privateGet(this, _info).streamType = chrome.cast.media.StreamType.LIVE;
          else __privateGet(this, _info).streamType = chrome.cast.media.StreamType.BUFFERED;
          return this;
        }
        setTracks(tracks) {
          __privateGet(this, _info).tracks = tracks.map(__privateMethod(this, _GoogleCastMediaInfoBuilder_instances, buildCastTrack_fn));
          return this;
        }
        setMetadata(title, poster) {
          __privateGet(this, _info).metadata = new chrome.cast.media.GenericMediaMetadata();
          __privateGet(this, _info).metadata.title = title;
          __privateGet(this, _info).metadata.images = [{ url: poster }];
          return this;
        }
      }, _info = new WeakMap(), _GoogleCastMediaInfoBuilder_instances = new WeakSet(), buildCastTrack_fn = function(track, trackId) {
        const castTrack = new chrome.cast.media.Track(trackId, chrome.cast.media.TrackType.TEXT);
        castTrack.name = track.label;
        castTrack.trackContentId = track.src;
        castTrack.trackContentType = "text/vtt";
        castTrack.language = track.language;
        castTrack.subtype = track.kind.toUpperCase();
        return castTrack;
      }, _a28);
      GoogleCastTracksManager = (_a29 = class {
        constructor(cast2, ctx, onNewLocalTracks) {
          __privateAdd(this, _GoogleCastTracksManager_instances);
          __privateAdd(this, _cast);
          __privateAdd(this, _ctx11);
          __privateAdd(this, _onNewLocalTracks);
          __privateSet(this, _cast, cast2);
          __privateSet(this, _ctx11, ctx);
          __privateSet(this, _onNewLocalTracks, onNewLocalTracks);
        }
        setup() {
          const syncRemoteActiveIds = this.syncRemoteActiveIds.bind(this);
          listenEvent(__privateGet(this, _ctx11).audioTracks, "change", syncRemoteActiveIds);
          listenEvent(__privateGet(this, _ctx11).textTracks, "mode-change", syncRemoteActiveIds);
          effect(__privateMethod(this, _GoogleCastTracksManager_instances, syncLocalTracks_fn).bind(this));
        }
        getLocalTextTracks() {
          return __privateGet(this, _ctx11).$state.textTracks().filter((track) => track.src && track.type === "vtt");
        }
        syncRemoteTracks(event) {
          if (!__privateGet(this, _cast).isMediaLoaded) return;
          const localAudioTracks = __privateMethod(this, _GoogleCastTracksManager_instances, getLocalAudioTracks_fn).call(this), localTextTracks = this.getLocalTextTracks(), remoteAudioTracks = __privateMethod(this, _GoogleCastTracksManager_instances, getRemoteTracks_fn).call(this, chrome.cast.media.TrackType.AUDIO), remoteTextTracks = __privateMethod(this, _GoogleCastTracksManager_instances, getRemoteTracks_fn).call(this, chrome.cast.media.TrackType.TEXT);
          for (const remoteAudioTrack of remoteAudioTracks) {
            if (__privateMethod(this, _GoogleCastTracksManager_instances, findLocalTrack_fn).call(this, localAudioTracks, remoteAudioTrack)) continue;
            const localAudioTrack = {
              id: remoteAudioTrack.trackId.toString(),
              label: remoteAudioTrack.name,
              language: remoteAudioTrack.language,
              kind: remoteAudioTrack.subtype ?? "main",
              selected: false
            };
            __privateGet(this, _ctx11).audioTracks[ListSymbol.add](localAudioTrack, event);
          }
          for (const remoteTextTrack of remoteTextTracks) {
            if (__privateMethod(this, _GoogleCastTracksManager_instances, findLocalTrack_fn).call(this, localTextTracks, remoteTextTrack)) continue;
            const localTextTrack = {
              id: remoteTextTrack.trackId.toString(),
              src: remoteTextTrack.trackContentId,
              label: remoteTextTrack.name,
              language: remoteTextTrack.language,
              kind: remoteTextTrack.subtype.toLowerCase()
            };
            __privateGet(this, _ctx11).textTracks.add(localTextTrack, event);
          }
        }
        syncRemoteActiveIds(event) {
          if (!__privateGet(this, _cast).isMediaLoaded) return;
          const activeIds = __privateMethod(this, _GoogleCastTracksManager_instances, getRemoteActiveIds_fn).call(this), editRequest = new chrome.cast.media.EditTracksInfoRequest(activeIds);
          __privateMethod(this, _GoogleCastTracksManager_instances, editTracksInfo_fn).call(this, editRequest).catch((error) => {
          });
        }
      }, _cast = new WeakMap(), _ctx11 = new WeakMap(), _onNewLocalTracks = new WeakMap(), _GoogleCastTracksManager_instances = new WeakSet(), getLocalAudioTracks_fn = function() {
        return __privateGet(this, _ctx11).$state.audioTracks();
      }, getRemoteTracks_fn = function(type) {
        const tracks = __privateGet(this, _cast).mediaInfo?.tracks ?? [];
        return type ? tracks.filter((track) => track.type === type) : tracks;
      }, getRemoteActiveIds_fn = function() {
        const activeIds = [], activeLocalAudioTrack = __privateMethod(this, _GoogleCastTracksManager_instances, getLocalAudioTracks_fn).call(this).find((track) => track.selected), activeLocalTextTracks = this.getLocalTextTracks().filter((track) => track.mode === "showing");
        if (activeLocalAudioTrack) {
          const remoteAudioTracks = __privateMethod(this, _GoogleCastTracksManager_instances, getRemoteTracks_fn).call(this, chrome.cast.media.TrackType.AUDIO), remoteAudioTrack = __privateMethod(this, _GoogleCastTracksManager_instances, findRemoteTrack_fn).call(this, remoteAudioTracks, activeLocalAudioTrack);
          if (remoteAudioTrack) activeIds.push(remoteAudioTrack.trackId);
        }
        if (activeLocalTextTracks?.length) {
          const remoteTextTracks = __privateMethod(this, _GoogleCastTracksManager_instances, getRemoteTracks_fn).call(this, chrome.cast.media.TrackType.TEXT);
          if (remoteTextTracks.length) for (const localTrack of activeLocalTextTracks) {
            const remoteTextTrack = __privateMethod(this, _GoogleCastTracksManager_instances, findRemoteTrack_fn).call(this, remoteTextTracks, localTrack);
            if (remoteTextTrack) activeIds.push(remoteTextTrack.trackId);
          }
        }
        return activeIds;
      }, syncLocalTracks_fn = function() {
        const localTextTracks = this.getLocalTextTracks();
        if (!__privateGet(this, _cast).isMediaLoaded) return;
        const remoteTextTracks = __privateMethod(this, _GoogleCastTracksManager_instances, getRemoteTracks_fn).call(this, chrome.cast.media.TrackType.TEXT);
        for (const localTrack of localTextTracks) if (!__privateMethod(this, _GoogleCastTracksManager_instances, findRemoteTrack_fn).call(this, remoteTextTracks, localTrack)) {
          untrack(() => {
            var _a106;
            return (_a106 = __privateGet(this, _onNewLocalTracks)) == null ? void 0 : _a106.call(this);
          });
          break;
        }
      }, editTracksInfo_fn = function(request) {
        const media = getCastSessionMedia();
        return new Promise((resolve, reject) => media?.editTracksInfo(request, resolve, reject));
      }, findLocalTrack_fn = function(localTracks, remoteTrack) {
        return localTracks.find((localTrack) => __privateMethod(this, _GoogleCastTracksManager_instances, isMatch_fn).call(this, localTrack, remoteTrack));
      }, findRemoteTrack_fn = function(remoteTracks, localTrack) {
        return remoteTracks.find((remoteTrack) => __privateMethod(this, _GoogleCastTracksManager_instances, isMatch_fn).call(this, localTrack, remoteTrack));
      }, isMatch_fn = function(localTrack, remoteTrack) {
        return remoteTrack.name === localTrack.label && remoteTrack.language === localTrack.language && remoteTrack.subtype.toLowerCase() === localTrack.kind.toLowerCase();
      }, _a29);
      GoogleCastProvider = (_a30 = class {
        constructor(player, ctx) {
          __privateAdd(this, _GoogleCastProvider_instances);
          __publicField(this, "$$PROVIDER_TYPE", "GOOGLE_CAST");
          __publicField(this, "scope", createScope());
          __privateAdd(this, _player);
          __privateAdd(this, _ctx12);
          __privateAdd(this, _tracks);
          __privateAdd(this, _currentSrc3, null);
          __privateAdd(this, _state4, "disconnected");
          __privateAdd(this, _currentTime2, 0);
          __privateAdd(this, _played, 0);
          __privateAdd(this, _seekableRange2, new TimeRange(0, 0));
          __privateAdd(this, _timeRAF3, new RAFLoop(__privateMethod(this, _GoogleCastProvider_instances, onAnimationFrame_fn3).bind(this)));
          __privateAdd(this, _playerEventHandlers);
          __privateAdd(this, _reloadInfo, null);
          __privateAdd(this, _isIdle, false);
          __privateSet(this, _player, player);
          __privateSet(this, _ctx12, ctx);
          __privateSet(this, _tracks, new GoogleCastTracksManager(player, ctx, __privateMethod(this, _GoogleCastProvider_instances, onNewLocalTracks_fn).bind(this)));
        }
        get type() {
          return "google-cast";
        }
        get currentSrc() {
          return __privateGet(this, _currentSrc3);
        }
        /**
        * The Google Cast remote player.
        *
        * @see {@link https://developers.google.com/cast/docs/reference/web_sender/cast.framework.RemotePlayer}
        */
        get player() {
          return __privateGet(this, _player);
        }
        /**
        * @see {@link https://developers.google.com/cast/docs/reference/web_sender/cast.framework.CastContext}
        */
        get cast() {
          return getCastContext();
        }
        /**
        * @see {@link https://developers.google.com/cast/docs/reference/web_sender/cast.framework.CastSession}
        */
        get session() {
          return getCastSession();
        }
        /**
        * @see {@link https://developers.google.com/cast/docs/reference/web_sender/chrome.cast.media.Media}
        */
        get media() {
          return getCastSessionMedia();
        }
        /**
        * Whether the current Google Cast session belongs to this provider.
        */
        get hasActiveSession() {
          return hasActiveCastSession(__privateGet(this, _currentSrc3));
        }
        setup() {
          __privateMethod(this, _GoogleCastProvider_instances, attachCastContextEventListeners_fn).call(this);
          __privateMethod(this, _GoogleCastProvider_instances, attachCastPlayerEventListeners_fn).call(this);
          __privateGet(this, _tracks).setup();
          __privateGet(this, _ctx12).notify("provider-setup", this);
        }
        async play() {
          if (!__privateGet(this, _player).isPaused && !__privateGet(this, _isIdle)) return;
          if (__privateGet(this, _isIdle)) {
            await __privateMethod(this, _GoogleCastProvider_instances, reload_fn).call(this, false, 0);
            return;
          }
          __privateGet(this, _player).controller?.playOrPause();
        }
        async pause() {
          if (__privateGet(this, _player).isPaused) return;
          __privateGet(this, _player).controller?.playOrPause();
        }
        getMediaStatus(request) {
          return new Promise((resolve, reject) => {
            this.media?.getStatus(request, resolve, reject);
          });
        }
        setMuted(muted) {
          if (muted && !__privateGet(this, _player).isMuted || !muted && __privateGet(this, _player).isMuted) __privateGet(this, _player).controller?.muteOrUnmute();
        }
        setCurrentTime(time) {
          __privateGet(this, _player).currentTime = time;
          __privateGet(this, _ctx12).notify("seeking", time);
          __privateGet(this, _player).controller?.seek();
        }
        setVolume(volume) {
          __privateGet(this, _player).volumeLevel = volume;
          __privateGet(this, _player).controller?.setVolumeLevel();
        }
        async loadSource(src) {
          if (__privateGet(this, _reloadInfo)?.src !== src) __privateSet(this, _reloadInfo, null);
          if (hasActiveCastSession(src)) {
            __privateMethod(this, _GoogleCastProvider_instances, resumeSession_fn).call(this);
            __privateSet(this, _currentSrc3, src);
            return;
          }
          __privateGet(this, _ctx12).notify("load-start");
          const loadRequest = __privateMethod(this, _GoogleCastProvider_instances, buildLoadRequest_fn).call(this, src), errorCode = await this.session.loadMedia(loadRequest);
          if (errorCode) {
            __privateSet(this, _currentSrc3, null);
            __privateGet(this, _ctx12).notify("error", Error(getCastErrorMessage(errorCode)));
            return;
          }
          __privateSet(this, _currentSrc3, src);
        }
        destroy() {
          __privateMethod(this, _GoogleCastProvider_instances, reset_fn4).call(this);
          __privateMethod(this, _GoogleCastProvider_instances, endSession_fn).call(this);
        }
      }, _player = new WeakMap(), _ctx12 = new WeakMap(), _tracks = new WeakMap(), _currentSrc3 = new WeakMap(), _state4 = new WeakMap(), _currentTime2 = new WeakMap(), _played = new WeakMap(), _seekableRange2 = new WeakMap(), _timeRAF3 = new WeakMap(), _playerEventHandlers = new WeakMap(), _reloadInfo = new WeakMap(), _isIdle = new WeakMap(), _GoogleCastProvider_instances = new WeakSet(), attachCastContextEventListeners_fn = function() {
        listenCastContextEvent(cast.framework.CastContextEventType.CAST_STATE_CHANGED, __privateMethod(this, _GoogleCastProvider_instances, onCastStateChange_fn).bind(this));
      }, attachCastPlayerEventListeners_fn = function() {
        const Event2 = cast.framework.RemotePlayerEventType, handlers = {
          [Event2.IS_CONNECTED_CHANGED]: __privateMethod(this, _GoogleCastProvider_instances, onCastStateChange_fn),
          [Event2.IS_MEDIA_LOADED_CHANGED]: __privateMethod(this, _GoogleCastProvider_instances, onMediaLoadedChange_fn),
          [Event2.CAN_CONTROL_VOLUME_CHANGED]: __privateMethod(this, _GoogleCastProvider_instances, onCanControlVolumeChange_fn),
          [Event2.CAN_SEEK_CHANGED]: __privateMethod(this, _GoogleCastProvider_instances, onCanSeekChange_fn),
          [Event2.DURATION_CHANGED]: __privateMethod(this, _GoogleCastProvider_instances, onDurationChange_fn2),
          [Event2.IS_MUTED_CHANGED]: __privateMethod(this, _GoogleCastProvider_instances, onVolumeChange_fn3),
          [Event2.VOLUME_LEVEL_CHANGED]: __privateMethod(this, _GoogleCastProvider_instances, onVolumeChange_fn3),
          [Event2.IS_PAUSED_CHANGED]: __privateMethod(this, _GoogleCastProvider_instances, onPausedChange_fn),
          [Event2.LIVE_SEEKABLE_RANGE_CHANGED]: __privateMethod(this, _GoogleCastProvider_instances, onProgress_fn3),
          [Event2.PLAYER_STATE_CHANGED]: __privateMethod(this, _GoogleCastProvider_instances, onPlayerStateChange_fn)
        };
        __privateSet(this, _playerEventHandlers, handlers);
        const handler = __privateMethod(this, _GoogleCastProvider_instances, onRemotePlayerEvent_fn).bind(this);
        for (const type of keysOf(handlers)) __privateGet(this, _player).controller.addEventListener(type, handler);
        onDispose(() => {
          for (const type of keysOf(handlers)) __privateGet(this, _player).controller.removeEventListener(type, handler);
        });
      }, reset_fn4 = function() {
        if (!__privateGet(this, _reloadInfo)) {
          __privateSet(this, _played, 0);
          __privateSet(this, _seekableRange2, new TimeRange(0, 0));
        }
        __privateGet(this, _timeRAF3).stop();
        __privateSet(this, _currentTime2, 0);
        __privateSet(this, _reloadInfo, null);
      }, resumeSession_fn = function() {
        const resumeSessionEvent = new DOMEvent("resume-session", { detail: this.session });
        __privateMethod(this, _GoogleCastProvider_instances, onMediaLoadedChange_fn).call(this, resumeSessionEvent);
        const { muted, volume, savedState } = __privateGet(this, _ctx12).$state, localState = savedState();
        this.setCurrentTime(Math.max(__privateGet(this, _player).currentTime, localState?.currentTime ?? 0));
        this.setMuted(muted());
        this.setVolume(volume());
        if (localState?.paused === false) this.play();
      }, endSession_fn = function() {
        this.cast.endCurrentSession(true);
        const { remotePlaybackLoader } = __privateGet(this, _ctx12).$state;
        remotePlaybackLoader.set(null);
      }, disconnectFromReceiver_fn = function() {
        const { savedState } = __privateGet(this, _ctx12).$state;
        savedState.set({
          paused: __privateGet(this, _player).isPaused,
          currentTime: __privateGet(this, _player).currentTime
        });
        __privateMethod(this, _GoogleCastProvider_instances, endSession_fn).call(this);
      }, onAnimationFrame_fn3 = function() {
        __privateMethod(this, _GoogleCastProvider_instances, onCurrentTimeChange_fn).call(this);
      }, onRemotePlayerEvent_fn = function(event) {
        __privateGet(this, _playerEventHandlers)[event.type].call(this, event);
      }, onCastStateChange_fn = function(data) {
        const castState = this.cast.getCastState(), state = castState === cast.framework.CastState.CONNECTED ? "connected" : castState === cast.framework.CastState.CONNECTING ? "connecting" : "disconnected";
        if (__privateGet(this, _state4) === state) return;
        const detail = {
          type: "google-cast",
          state
        }, trigger = __privateMethod(this, _GoogleCastProvider_instances, createEvent_fn).call(this, data);
        __privateSet(this, _state4, state);
        __privateGet(this, _ctx12).notify("remote-playback-change", detail, trigger);
        if (state === "disconnected") __privateMethod(this, _GoogleCastProvider_instances, disconnectFromReceiver_fn).call(this);
      }, onMediaLoadedChange_fn = function(event) {
        if (!!!__privateGet(this, _player).isMediaLoaded) return;
        const src = peek(__privateGet(this, _ctx12).$state.source);
        Promise.resolve().then(() => {
          if (src !== peek(__privateGet(this, _ctx12).$state.source) || !__privateGet(this, _player).isMediaLoaded) return;
          __privateMethod(this, _GoogleCastProvider_instances, reset_fn4).call(this);
          const duration = __privateGet(this, _player).duration;
          __privateSet(this, _seekableRange2, new TimeRange(0, duration));
          const detail = {
            provider: this,
            duration,
            buffered: new TimeRange(0, 0),
            seekable: __privateMethod(this, _GoogleCastProvider_instances, getSeekableRange_fn).call(this)
          }, trigger = __privateMethod(this, _GoogleCastProvider_instances, createEvent_fn).call(this, event);
          __privateGet(this, _ctx12).notify("loaded-metadata", void 0, trigger);
          __privateGet(this, _ctx12).notify("loaded-data", void 0, trigger);
          __privateGet(this, _ctx12).notify("can-play", detail, trigger);
          __privateMethod(this, _GoogleCastProvider_instances, onCanControlVolumeChange_fn).call(this);
          __privateMethod(this, _GoogleCastProvider_instances, onCanSeekChange_fn).call(this, event);
          const { volume, muted } = __privateGet(this, _ctx12).$state;
          this.setVolume(volume());
          this.setMuted(muted());
          __privateGet(this, _timeRAF3).start();
          __privateGet(this, _tracks).syncRemoteTracks(trigger);
          __privateGet(this, _tracks).syncRemoteActiveIds(trigger);
        });
      }, onCanControlVolumeChange_fn = function() {
        __privateGet(this, _ctx12).$state.canSetVolume.set(__privateGet(this, _player).canControlVolume);
      }, onCanSeekChange_fn = function(event) {
        const trigger = __privateMethod(this, _GoogleCastProvider_instances, createEvent_fn).call(this, event);
        __privateGet(this, _ctx12).notify("stream-type-change", __privateMethod(this, _GoogleCastProvider_instances, getStreamType_fn).call(this), trigger);
      }, getStreamType_fn = function() {
        return __privateGet(this, _player).mediaInfo?.streamType === chrome.cast.media.StreamType.LIVE ? __privateGet(this, _player).canSeek ? "live:dvr" : "live" : "on-demand";
      }, onCurrentTimeChange_fn = function() {
        if (__privateGet(this, _reloadInfo)) return;
        const currentTime = __privateGet(this, _player).currentTime;
        if (currentTime === __privateGet(this, _currentTime2)) return;
        __privateGet(this, _ctx12).notify("time-change", currentTime);
        if (currentTime > __privateGet(this, _played)) {
          __privateSet(this, _played, currentTime);
          __privateMethod(this, _GoogleCastProvider_instances, onProgress_fn3).call(this);
        }
        if (__privateGet(this, _ctx12).$state.seeking()) __privateGet(this, _ctx12).notify("seeked", currentTime);
        __privateSet(this, _currentTime2, currentTime);
      }, onDurationChange_fn2 = function(event) {
        if (!__privateGet(this, _player).isMediaLoaded || __privateGet(this, _reloadInfo)) return;
        const duration = __privateGet(this, _player).duration, trigger = __privateMethod(this, _GoogleCastProvider_instances, createEvent_fn).call(this, event);
        __privateSet(this, _seekableRange2, new TimeRange(0, duration));
        __privateGet(this, _ctx12).notify("duration-change", duration, trigger);
      }, onVolumeChange_fn3 = function(event) {
        if (!__privateGet(this, _player).isMediaLoaded) return;
        const detail = {
          muted: __privateGet(this, _player).isMuted,
          volume: __privateGet(this, _player).volumeLevel
        }, trigger = __privateMethod(this, _GoogleCastProvider_instances, createEvent_fn).call(this, event);
        __privateGet(this, _ctx12).notify("volume-change", detail, trigger);
      }, onPausedChange_fn = function(event) {
        const trigger = __privateMethod(this, _GoogleCastProvider_instances, createEvent_fn).call(this, event);
        if (__privateGet(this, _player).isPaused) __privateGet(this, _ctx12).notify("pause", void 0, trigger);
        else __privateGet(this, _ctx12).notify("play", void 0, trigger);
      }, onProgress_fn3 = function(event) {
        const detail = {
          seekable: __privateMethod(this, _GoogleCastProvider_instances, getSeekableRange_fn).call(this),
          buffered: new TimeRange(0, __privateGet(this, _played))
        }, trigger = event ? __privateMethod(this, _GoogleCastProvider_instances, createEvent_fn).call(this, event) : void 0;
        __privateGet(this, _ctx12).notify("progress", detail, trigger);
      }, onPlayerStateChange_fn = function(event) {
        const state = __privateGet(this, _player).playerState, PlayerState = chrome.cast.media.PlayerState;
        __privateSet(this, _isIdle, state === PlayerState.IDLE);
        if (state === PlayerState.PAUSED) return;
        const trigger = __privateMethod(this, _GoogleCastProvider_instances, createEvent_fn).call(this, event);
        switch (state) {
          case PlayerState.PLAYING:
            __privateGet(this, _ctx12).notify("playing", void 0, trigger);
            break;
          case PlayerState.BUFFERING:
            __privateGet(this, _ctx12).notify("waiting", void 0, trigger);
            break;
          case PlayerState.IDLE:
            __privateGet(this, _timeRAF3).stop();
            __privateGet(this, _ctx12).notify("pause");
            __privateGet(this, _ctx12).notify("end");
            break;
        }
      }, getSeekableRange_fn = function() {
        return __privateGet(this, _player).liveSeekableRange ? new TimeRange(__privateGet(this, _player).liveSeekableRange.start, __privateGet(this, _player).liveSeekableRange.end) : __privateGet(this, _seekableRange2);
      }, createEvent_fn = function(detail) {
        return detail instanceof Event ? detail : new DOMEvent(detail.type, { detail });
      }, buildMediaInfo_fn = function(src) {
        const { streamType, title, poster } = __privateGet(this, _ctx12).$state;
        return new GoogleCastMediaInfoBuilder(src).setMetadata(title(), poster()).setStreamType(streamType()).setTracks(__privateGet(this, _tracks).getLocalTextTracks()).build();
      }, buildLoadRequest_fn = function(src) {
        const mediaInfo = __privateMethod(this, _GoogleCastProvider_instances, buildMediaInfo_fn).call(this, src), request = new chrome.cast.media.LoadRequest(mediaInfo), savedState = __privateGet(this, _ctx12).$state.savedState();
        request.autoplay = (__privateGet(this, _reloadInfo)?.paused ?? savedState?.paused) === false;
        request.currentTime = __privateGet(this, _reloadInfo)?.time ?? savedState?.currentTime ?? 0;
        return request;
      }, reload_fn = async function(paused, time) {
        const src = peek(__privateGet(this, _ctx12).$state.source);
        __privateSet(this, _reloadInfo, {
          src,
          paused,
          time
        });
        await this.loadSource(src);
      }, onNewLocalTracks_fn = function() {
        __privateMethod(this, _GoogleCastProvider_instances, reload_fn).call(this, __privateGet(this, _player).isPaused, __privateGet(this, _player).currentTime).catch((error) => {
        });
      }, _a30);
    }
  });

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-Di0HObvT.js
  var vidstack_Di0HObvT_exports = {};
  __export(vidstack_Di0HObvT_exports, {
    GoogleCastLoader: () => GoogleCastLoader
  });
  var _player2, _GoogleCastLoader_instances, loadCastFramework_fn, showPrompt_fn, setOptions_fn, notifyRemoteStateChange_fn, createError_fn, _a31, GoogleCastLoader;
  var init_vidstack_Di0HObvT = __esm({
    "frontend/node_modules/@vidstack/react/prod/chunks/vidstack-Di0HObvT.js"() {
      "use client";
      init_define_import_meta_env();
      init_vidstack_B9fTkxQO();
      init_vidstack_CvezEmSQ();
      init_vidstack_aCAqaLv8();
      GoogleCastLoader = (_a31 = class {
        constructor() {
          __privateAdd(this, _GoogleCastLoader_instances);
          __publicField(this, "name", "google-cast");
          __publicField(this, "target");
          __privateAdd(this, _player2);
        }
        /**
        * @see {@link https://developers.google.com/cast/docs/reference/web_sender/cast.framework.CastContext}
        */
        get cast() {
          return getCastContext();
        }
        mediaType() {
          return "video";
        }
        canPlay(src) {
          return IS_CHROME && !IS_IOS && canGoogleCastSrc(src);
        }
        async prompt(ctx) {
          let loadEvent, openEvent, errorEvent;
          try {
            loadEvent = await __privateMethod(this, _GoogleCastLoader_instances, loadCastFramework_fn).call(this, ctx);
            if (!__privateGet(this, _player2)) {
              __privateSet(this, _player2, new cast.framework.RemotePlayer());
              new cast.framework.RemotePlayerController(__privateGet(this, _player2));
            }
            openEvent = ctx.player.createEvent("google-cast-prompt-open", { trigger: loadEvent });
            ctx.player.dispatchEvent(openEvent);
            __privateMethod(this, _GoogleCastLoader_instances, notifyRemoteStateChange_fn).call(this, ctx, "connecting", openEvent);
            await __privateMethod(this, _GoogleCastLoader_instances, showPrompt_fn).call(this, peek(ctx.$props.googleCast));
            ctx.$state.remotePlaybackInfo.set({ deviceName: getCastSession()?.getCastDevice().friendlyName });
            if (isCastConnected()) __privateMethod(this, _GoogleCastLoader_instances, notifyRemoteStateChange_fn).call(this, ctx, "connected", openEvent);
          } catch (code) {
            const error = code instanceof Error ? code : __privateMethod(this, _GoogleCastLoader_instances, createError_fn).call(this, (code + "").toUpperCase(), "Prompt failed.");
            errorEvent = ctx.player.createEvent("google-cast-prompt-error", {
              detail: error,
              trigger: openEvent ?? loadEvent,
              cancelable: true
            });
            ctx.player.dispatch(errorEvent);
            __privateMethod(this, _GoogleCastLoader_instances, notifyRemoteStateChange_fn).call(this, ctx, isCastConnected() ? "connected" : "disconnected", errorEvent);
            throw error;
          } finally {
            ctx.player.dispatch("google-cast-prompt-close", { trigger: errorEvent ?? openEvent ?? loadEvent });
          }
        }
        async load(ctx) {
          if (IS_SERVER2) throw Error("[vidstack] can not load google cast provider server-side");
          if (!__privateGet(this, _player2)) throw Error("[vidstack] google cast player was not initialized");
          return new (await Promise.resolve().then(() => (init_vidstack_D2vrrUMs(), vidstack_D2vrrUMs_exports))).GoogleCastProvider(__privateGet(this, _player2), ctx);
        }
      }, _player2 = new WeakMap(), _GoogleCastLoader_instances = new WeakSet(), loadCastFramework_fn = async function(ctx) {
        if (hasLoadedCastFramework()) return;
        const loadStartEvent = ctx.player.createEvent("google-cast-load-start");
        ctx.player.dispatch(loadStartEvent);
        await loadScript(getCastFrameworkURL());
        await customElements.whenDefined("google-cast-launcher");
        const loadedEvent = ctx.player.createEvent("google-cast-loaded", { trigger: loadStartEvent });
        ctx.player.dispatch(loadedEvent);
        if (!isCastAvailable()) throw __privateMethod(this, _GoogleCastLoader_instances, createError_fn).call(this, "CAST_NOT_AVAILABLE", "Google Cast not available on this platform.");
        return loadedEvent;
      }, showPrompt_fn = async function(options) {
        __privateMethod(this, _GoogleCastLoader_instances, setOptions_fn).call(this, options);
        const errorCode = await this.cast.requestSession();
        if (errorCode) throw __privateMethod(this, _GoogleCastLoader_instances, createError_fn).call(this, errorCode.toUpperCase(), getCastErrorMessage(errorCode));
      }, setOptions_fn = function(options) {
        this.cast?.setOptions({
          ...getDefaultCastOptions(),
          ...options
        });
      }, notifyRemoteStateChange_fn = function(ctx, state, trigger) {
        const detail = {
          type: "google-cast",
          state
        };
        ctx.notify("remote-playback-change", detail, trigger);
      }, createError_fn = function(code, message) {
        const error = Error(message);
        error.code = code;
        return error;
      }, _a31);
    }
  });

  // shim:react-dom-shim
  var require_react_dom_shim = __commonJS({
    "shim:react-dom-shim"(exports, module) {
      init_define_import_meta_env();
      var D = window.ReactDOM;
      var n2 = function() {
      };
      module.exports = Object.assign({ preload: n2, preinit: n2, preconnect: n2, prefetchDNS: n2, preloadModule: n2, preinitModule: n2 }, D);
    }
  });

  // ds-bundle/.bundle-entry.mjs
  var bundle_entry_exports = {};
  __export(bundle_entry_exports, {
    ActivityFeed: () => ActivityFeed,
    AddBook: () => AddBook,
    AudioPlayer: () => AudioPlayer,
    AuthForm: () => AuthForm,
    AuthLanding: () => AuthLanding,
    Avatar: () => Avatar,
    BadgeCard: () => BadgeCard,
    BirthdayCard: () => BirthdayCard,
    BlogCard: () => BlogCard,
    BtnPrimary: () => BtnPrimary,
    BtnSecondary: () => BtnSecondary,
    CalendarWidget: () => CalendarWidget,
    Card: () => Card,
    ChatResponsive: () => ChatResponsive,
    ChoseFromMyPhoto: () => ChoseFromMyPhoto,
    ComicIndexView: () => ComicIndexView,
    CommentForm: () => CommentForm,
    CommentItem: () => CommentItem,
    CommentThread: () => CommentThread,
    Composer: () => Composer,
    ControlBlockButtons: () => ControlBlockButtons,
    DSProvider: () => DSProvider,
    EventItem: () => EventItem,
    Field: () => Field,
    FormField: () => FormField,
    FriendCard: () => FriendCard,
    FriendRequestItem: () => FriendRequestItem,
    FriendRequestsMenu: () => FriendRequestsMenu,
    FriendSuggestions: () => FriendSuggestions,
    GoToTop: () => GoToTop,
    HelloPreloader: () => HelloPreloader,
    HomeView: () => HomeView,
    Icon: () => Icon,
    LoginView: () => LoginView,
    MasterBase: () => MasterBase,
    MasterPublic: () => MasterPublic,
    MessagesMenu: () => MessagesMenu,
    Modal: () => Modal,
    NotificationsMenu: () => NotificationsMenu,
    NovelDetailView: () => NovelDetailView,
    OLYMP_VIEWBOX: () => OLYMP_VIEWBOX,
    PagesWidget: () => PagesWidget,
    PersonalInfoWidget: () => PersonalInfoWidget,
    PlaylistWidget: () => PlaylistWidget,
    Post: () => Post,
    PostControlButtons: () => PostControlButtons,
    ProfileHeader: () => ProfileHeader,
    ReactionBar: () => ReactionBar,
    RegisterView: () => RegisterView,
    SelectField: () => SelectField,
    SessionKeeper: () => SessionKeeper,
    SidebarCenter: () => SidebarCenter,
    SidebarLeft: () => SidebarLeft,
    SidebarRight: () => SidebarRight,
    SvgSprite: () => SvgSprite,
    TagSelect: () => TagSelect,
    ToggleRow: () => ToggleRow,
    TopMenu: () => TopMenu,
    TrackItem: () => TrackItem,
    UpdateHeaderPhoto: () => UpdateHeaderPhoto,
    UploadStudio: () => UploadStudio,
    WeatherWidget: () => WeatherWidget,
    WidgetCard: () => WidgetCard,
    __dsMainNs: () => pkg_entry_exports,
    inputCls: () => inputCls,
    inputStyle: () => inputStyle
  });
  init_define_import_meta_env();

  // frontend/.ds-shims/ds-provider.tsx
  init_define_import_meta_env();
  var React2 = __toESM(require_react_shim());

  // frontend/src/templates/v1/components/footers/SvgSprite.tsx
  init_define_import_meta_env();
  var OLYMP_VIEWBOX = {
    "accordion-close-icon": "0 0 32 32",
    "accordion-open-icon": "0 0 32 32",
    "add-a-place-icon": "0 0 26 32",
    "add-to-conversation-icon": "0 0 35 32",
    "albums-icon": "0 0 32 32",
    "badge-icon": "0 0 32 32",
    "block-from-chat": "0 0 35 32",
    "block-from-chat-icon": "0 0 35 32",
    "blog-icon": "0 0 29 32",
    "calendar-icon": "0 0 26 32",
    "camera-icon": "0 0 43 32",
    "chat---messages-icon": "0 0 40 32",
    "check-icon": "0 0 37 32",
    "checked-calendar-icon": "0 0 26 32",
    "close-icon": "0 0 32 32",
    "comments-post-icon": "0 0 36 32",
    "computer-icon": "0 0 35 32",
    "cupcake-icon": "0 0 27 32",
    "day-calendar-icon": "0 0 26 32",
    "dropdown-arrow-icon": "0 0 48 32",
    "happy-face-icon": "0 0 32 32",
    "happy-faces-icon": "0 0 33 32",
    "happy-sticker-icon": "0 0 32 32",
    "headphones-icon": "0 0 36 32",
    "heart-icon": "0 0 36 32",
    "home-icon": "0 0 32 32",
    "info-icon": "0 0 32 32",
    "like-post-icon": "0 0 37 32",
    "little-delete": "0 0 32 32",
    "login-icon": "0 0 29 32",
    "logout-icon": "0 0 43 32",
    "magnifying-glass-icon": "0 0 34 32",
    "manage-widgets-icon": "0 0 39 32",
    "menu-icon": "0 0 41 32",
    "month-calendar-icon": "0 0 26 32",
    "multimedia-icon": "0 0 32 32",
    "music-fullscreen-icon": "0 0 512 512",
    "music-minimize-screen-icon": "0 0 512 512",
    "music-next-song-icon": "0 0 512 512",
    "music-no-sound-icon": "0 0 512 512",
    "music-open-playlist-icon": "0 0 512 512",
    "music-pause-icon": "0 0 512 512",
    "music-play-icon-big": "0 0 512 512",
    "music-previous-song-icon": "0 0 512 512",
    "music-repeat-icon": "0 0 512 512",
    "music-shuffle-icon": "0 0 512 512",
    "music-sound-icon": "0 0 512 512",
    "music-video-settings-icon": "0 0 512 512",
    "newsfeed-icon": "0 0 29 32",
    "photos-icon": "0 0 29 32",
    "play-icon": "0 0 28 32",
    "plus-icon": "0 0 32 32",
    "popup-left-arrow": "0 0 18 32",
    "popup-right-arrow": "0 0 18 32",
    "register-icon": "0 0 37 32",
    "remove-playlist-icon": "0 0 38 32",
    "save-playlist-icon": "0 0 35 32",
    "settings-icon": "0 0 32 32",
    "settings-v2-icon": "0 0 32 32",
    "share-icon": "0 0 40 32",
    "share-post-icon": "0 0 37 32",
    "shopping-bag-icon": "0 0 512 512",
    "small-calendar-icon": "0 0 25 32",
    "small-pin-icon": "0 0 25 32",
    "speech-balloon-icon": "0 0 35 32",
    "star-icon": "0 0 32 32",
    "stats-arrow": "0 0 32 32",
    "stats-icon": "0 0 32 32",
    "status-icon": "0 0 36 32",
    "three-dots-icon": "0 0 128 32",
    "thunder-icon": "0 0 26 32",
    "trophy-icon": "0 0 36 32",
    "weather-cloudy-icon": "0 0 512 512",
    "weather-icon": "0 0 38 32",
    "weather-partly-sunny-icon": "0 0 512 512",
    "weather-rain-drops-icon": "0 0 512 512",
    "weather-rain-icon": "0 0 512 512",
    "weather-refresh-icon": "0 0 30 32",
    "weather-snow-icon": "0 0 512 512",
    "weather-storm-icon": "0 0 512 512",
    "weather-strong-winds-icon": "0 0 512 512",
    "weather-sunny-icon": "0 0 512 512",
    "weather-thermometer-icon": "0 0 512 512",
    "weather-wind-icon-header": "0 0 512 512",
    "week-calendar-icon": "0 0 26 32"
  };
  var SPRITE = `<svg xmlns="http://www.w3.org/2000/svg" style="position:absolute;width:0;height:0;overflow:hidden" aria-hidden="true"><symbol id="olymp-stats-arrow" viewBox="0 0 32 32"><path d="M14.222 3.609h3.556v-3.534h-3.556v3.534zM14.222 31.927h3.556v-24.74h-3.556v24.74zM21.918 2.363l-2.514 2.382 10.055 9.527 2.514-2.382-10.055-9.527zM0.027 11.89l2.516 2.382 10.055-9.527-2.514-2.382-10.057 9.527z"></path></symbol><symbol id="olymp-little-delete" viewBox="0 0 32 32"><path d="M32 4.149l-3.973-3.979-11.936 11.941-11.941-11.941-3.979 3.979 11.941 11.936-11.941 11.936 3.979 3.979 11.941-11.936 11.936 11.936 3.973-3.979-11.936-11.936z"></path></symbol><symbol id="olymp-trophy-icon" viewBox="0 0 36 32"><path d="M24 4v10.4c0 3.088-2.692 5.6-6 5.6s-6-2.512-6-5.6v-10.4h12zM28 0h-20v14.4c0 5.302 4.476 9.6 10 9.6s10-4.298 10-9.6v-14.4z"></path>
      <path d="M16 22h4v10h-4v-10z"></path>
      <path d="M12 28h12v4h-12v-4z"></path>
      <path d="M12.014 18h-2c-7.506 0-9.782-7.8-9.936-11.924l-0.078-2.076h12.014v14zM4.356 8c0.402 1.914 1.364 4.598 3.658 5.602v-5.602h-3.658z"></path>
      <path d="M23.986 4h12.014l-0.078 2.076c-0.154 4.124-2.43 11.924-9.936 11.924h-2v-14zM27.986 8v5.602c2.294-1.004 3.256-3.688 3.658-5.602h-3.658z"></path></symbol><symbol id="olymp-block-from-chat" viewBox="0 0 35 32"><path d="M32 0h-28.8c-1.768 0-3.2 1.434-3.2 3.2v16c0 1.766 1.434 3.2 3.2 3.2 0 0 0.6 0 1.6 0v-3.2h-1.6v-16h28.8v19.2c1.766 0 3.2-1.434 3.2-3.2v-16c0-1.766-1.434-3.2-3.2-3.2zM20.8 22.4h1.6v-3.2h-1.6v3.2zM25.6 22.4h3.2v-3.2h-3.2v3.2z"></path>
      <path d="M17.6 19.2l-9.6 6.626v-6.626h-3.2v12.8l12.8-9.6h3.2v-3.2z"></path>
      <path d="M22.125 8.938l-2.262-2.262-2.262 2.262-2.262-2.262-2.264 2.262 2.262 2.262-2.262 2.264 2.264 2.262 2.262-2.262 2.262 2.262 2.262-2.262-2.262-2.264z"></path></symbol><symbol id="olymp-weather-refresh-icon" viewBox="0 0 30 32"><path d="M14.769 32c-8.155 0-14.769-6.614-14.769-14.769 0-4.177 1.762-7.909 4.561-10.582l-3.414-3.412 12.18-1.794-1.794 12.18-3.348-3.35c-0.475 0.468-0.99 0.906-1.371 1.465v0c-1.115 1.585-1.89 3.407-1.89 5.494 0 5.438 4.409 9.846 9.846 9.846s9.846-4.409 9.846-9.846c0-2.4-0.891-4.566-2.316-6.274l3.764-3.136c2.139 2.56 3.476 5.814 3.476 9.41 0 8.155-6.614 14.769-14.769 14.769z"></path></symbol><symbol id="olymp-popup-left-arrow" viewBox="0 0 18 32"><path d="M0 14.222v3.556h3.556v-3.556h-3.556zM6.805 18.514l-2.514 2.514 10.057 10.055 2.514-2.514-10.057-10.055zM14.348 0.914l-10.057 10.057 2.514 2.514 10.057-10.055-2.514-2.516z"></path></symbol><symbol id="olymp-popup-right-arrow" viewBox="0 0 18 32"><path d="M14.222 14.222v3.556h3.556v-3.556h-3.556zM0.916 28.569l2.514 2.514 10.057-10.055-2.514-2.514-10.057 10.055zM0.916 3.429l10.057 10.055 2.514-2.514-10.057-10.057-2.514 2.516z"></path></symbol><symbol id="olymp-register-icon" viewBox="0 0 37 32"><path d="M16 3.213c3.24 0 6.192 1.214 8.446 3.2h4.346c-2.917-3.888-7.549-6.413-12.781-6.413-7.165 0-13.227 4.714-15.259 11.213h3.387c1.899-4.69 6.491-8 11.861-8zM16 28.813c-5.37 0-9.962-3.31-11.861-8h-3.378c2.040 6.485 8.094 11.187 15.25 11.187 5.222 0 9.842-2.515 12.762-6.387h-4.325c-2.256 1.986-5.208 3.2-8.448 3.2zM32 14.413v-4.8h-3.2v4.8h-4.8v3.2h4.8v4.8h3.2v-4.8h4.8v-3.2h-4.8zM3.2 14.413h-3.2v3.2h3.2v-3.2z"></path></symbol><symbol id="olymp-login-icon" viewBox="0 0 29 32"><path d="M0 17.443c0 6.515 4.287 12.026 10.195 13.875v-3.081c-4.263-1.728-7.273-5.901-7.273-10.783 0-4.883 3.009-9.056 7.273-10.784v-3.1c-5.908 1.849-10.195 7.36-10.195 13.872zM18.922 3.578v3.092c4.263 1.728 7.273 5.901 7.273 10.783s-3.009 9.056-7.273 10.783v3.071c5.894-1.855 10.169-7.357 10.169-13.863 0-6.503-4.273-12.007-10.169-13.865zM13.104 14.545h2.909v-14.545h-2.909v14.545zM13.104 32h2.909v-2.909h-2.909v2.909z"></path></symbol><symbol id="olymp-three-dots-icon" viewBox="0 0 128 32"><path d="M112-0.008c-8.84 0-16 7.16-16 16 0 8.832 7.16 15.992 16 15.992s16-7.16 16-15.992c0-8.84-7.16-16-16-16zM64-0.008c-8.84 0-16 7.16-16 16 0 8.832 7.16 15.992 16 15.992s16-7.16 16-15.992c0-8.84-7.16-16-16-16zM16-0.008c-8.832 0-16 7.16-16 16s7.168 15.992 16 15.992 16-7.16 16-15.992c0-8.84-7.16-16-16-16z"></path></symbol><symbol id="olymp-small-pin-icon" viewBox="0 0 25 32"><path d="M12.444 7.111c-2.946 0-5.333 2.389-5.333 5.333 0 2.946 2.388 5.333 5.333 5.333s5.333-2.386 5.333-5.333c0-2.944-2.388-5.333-5.333-5.333zM12.444 14.222c-0.981 0-1.778-0.796-1.778-1.778s0.796-1.778 1.778-1.778 1.778 0.796 1.778 1.778-0.796 1.778-1.778 1.778z"></path>
      <path d="M12.444 0c-4.823 0-8.996 2.891-11.061 7.111h4.194c1.632-2.151 4.087-3.556 6.868-3.556 4.901 0 8.889 4.277 8.889 9.534 0 7.237-6.46 13.865-8.876 15.204-1.838-1.054-6.148-5.36-8.011-10.516h-3.73c2.263 7.817 9.374 14.222 11.728 14.222 2.811 0 12.444-8.951 12.444-18.91 0-7.228-5.573-13.090-12.444-13.090z"></path>
      <path d="M0 10.667h3.556v3.556h-3.556v-3.556z"></path></symbol><symbol id="olymp-small-calendar-icon" viewBox="0 0 25 32"><path d="M21.333 3.556h-1.778v-1.778c0-0.983-0.796-1.778-1.778-1.778-0.983 0-1.778 0.795-1.778 1.778v1.778h-7.111v-1.778c0-0.983-0.796-1.778-1.778-1.778-0.983 0-1.778 0.795-1.778 1.778v1.778h-1.778c-1.963 0-3.556 1.415-3.556 3.159v22.123c0 1.748 1.593 3.163 3.556 3.163h14.222v-3.556h-14.222v-21.333h1.778v1.778c0 0.981 0.795 1.778 1.778 1.778 0.981 0 1.778-0.796 1.778-1.778v-1.778h7.111v1.778c0 0.981 0.795 1.778 1.778 1.778 0.981 0 1.778-0.796 1.778-1.778v-1.778h1.778v17.778h3.556v-18.174c0-1.744-1.593-3.159-3.556-3.159zM21.333 32h3.556v-3.556h-3.556v3.556zM7.111 17.776h10.667v-3.556h-10.667v3.556zM7.111 24.887h3.556v-3.556h-3.556v3.556zM14.222 24.887h3.556v-3.556h-3.556v3.556z"></path></symbol><symbol id="olymp-share-post-icon" viewBox="0 0 37 32"><path d="M11.691 16.788v3.046h6.085v3.048h-6.085v3.072l-6.095-4.584 6.095-4.582zM14.738 10.683l-14.216 10.686 14.216 10.688v-6.129h6.085v-9.144h-6.085v-6.101z"></path>
      <path d="M16.262 6.095h3.048v3.048h-3.048v-3.048z"></path>
      <path d="M33.33 8.25l-10.973-8.25v9.149h3.048v-3.046l6.095 4.582-6.095 4.582v-3.072h-9.134v3.048h6.086v6.129l14.214-10.686z"></path></symbol><symbol id="olymp-like-post-icon" viewBox="0 0 37 32"><path d="M33.449 2.994c-2.066-2.009-4.377-3.006-7.191-3.006-2.69 0-5.691 2.089-7.934 4.306-2.311-2.217-5.221-4.295-8.002-4.295-2.722 0-5.122 0.793-7.12 2.736-4.165 4.043-4.165 10.601 0 14.649 1.189 1.154 12.729 13.655 12.729 13.655 0.656 0.64 1.52 0.96 2.379 0.96 0.862 0 1.721-0.32 2.382-0.96l1.726-1.815-3.237-3.234-0.857 0.905c-3.122-3.381-10.862-11.744-11.936-12.789-1.122-1.090-1.739-2.528-1.737-4.050 0-1.52 0.617-2.955 1.739-4.046 1.024-0.997 2.238-1.44 3.931-1.44 0.777 0 2.512 0.791 4.839 3.022l3.214 3.081 3.163-3.131c2.215-2.19 4.037-2.985 4.718-2.985 1.573 0 2.77 0.512 4.007 1.714 1.12 1.088 1.737 2.523 1.737 4.046s-0.617 2.958-1.669 3.982c-0.13 0.121-0.135 0.123-2.037 2.123l-0.187 0.199 3.23 3.237 0.046-0.048 0.242-0.258c0 0 1.79-1.881 1.824-1.911 4.162-4.043 4.162-10.603 0-14.647z"></path>
      <path d="M22.889 20.578h4.571v4.571h-4.571v-4.571z"></path></symbol><symbol id="olymp-dropdown-arrow-icon" viewBox="0 0 48 32"><path d="M41.888 0.104l-17.952 19.064-17.952-19.064-5.984 6.352 23.936 25.44 23.936-25.44z"></path></symbol><symbol id="olymp-accordion-open-icon" viewBox="0 0 32 32"><path d="M32 12.8v6.4h-32v-6.4h32z"></path>
      <path d="M19.2 32h-6.4v-32h6.4v32z"></path></symbol><symbol id="olymp-comments-post-icon" viewBox="0 0 36 32"><path d="M32 0h-28c-2.21 0-4 1.792-4 4v18c0 2.208 1.792 4 4 4 0 0 0.75 0 2 0v-4h-2v-18h28v22c2.208 0 4-1.792 4-4v-18c0-2.208-1.792-4-4-4zM18 26h2v-4h-2v4zM24 26h4v-4h-4v4zM8 12h20v-4h-20v4zM8 18h12v-4h-12v4z"></path>
      <path d="M18 22l-8 4.282v-4.282h-4v10l12-6v-4z"></path></symbol><symbol id="olymp-accordion-close-icon" viewBox="0 0 32 32"><path d="M32 12.8v6.4h-32v-6.4h32z"></path></symbol><symbol id="olymp-play-icon" viewBox="0 0 28 32"><path d="M24.71 10.664l-15.614-9.574c-1.146-0.698-2.312-1.048-3.478-1.048-2.792 0-5.618 2.188-5.618 6.368v5.59h4c0-4.784 0-5.592 0-5.592 0-1.498 0.628-2.368 1.618-2.368 0.408 0 0.876 0.148 1.388 0.458l15.63 9.584c1.758 1.066 1.848 2.812 0.088 3.878l-15.718 9.58c-0.512 0.314-0.982 0.46-1.39 0.46-0.988 0-1.616-0.868-1.616-2.364 0 0 0-0.6 0-1.586 0-0.012 0-0.036 0-0.048h-4v1.634c0 4.178 2.826 6.364 5.616 6.364 1.166 0 2.334-0.35 3.47-1.042l15.72-9.58c2.026-1.23 3.194-3.164 3.194-5.302 0.002-2.172-1.198-4.144-3.29-5.412z"></path>
      <path d="M0 16h4v4h-4v-4z"></path></symbol><symbol id="olymp-remove-playlist-icon" viewBox="0 0 38 32"><path d="M8.813 19.202c0.442 0 0.8 0.358 0.8 0.8v8.002c0 0.44-0.358 0.797-0.8 0.797s-0.8-0.357-0.8-0.797v-8.002c0-0.442 0.358-0.8 0.8-0.8zM8.813 16.002c-2.206 0-4 1.795-4 4v8.002c0 2.203 1.794 3.997 4 3.997s4-1.794 4-3.997v-8.002c0-2.205-1.794-4-4-4v0z"></path>
      <path d="M6.275 27.138c-1.694-0.067-3.050-1.451-3.050-3.163s1.357-3.096 3.050-3.163v-3.2c-3.475 0.070-6.275 2.896-6.275 6.389s2.8 6.318 6.275 6.387v-3.25z"></path>
      <path d="M23.186 19.202c0.442 0 0.8 0.358 0.8 0.8v8.002c0 0.44-0.358 0.797-0.8 0.797s-0.8-0.357-0.8-0.797v-8.002c0-0.442 0.358-0.8 0.8-0.8zM23.186 16.002c-2.206 0-4 1.795-4 4v8.002c0 2.203 1.794 3.997 4 3.997 2.205 0 3.998-1.794 3.998-3.997v-8.002c0-2.205-1.794-4-3.998-4v0z"></path>
      <path d="M25.725 27.138c1.694-0.067 3.050-1.451 3.050-3.163s-1.357-3.096-3.050-3.163v-3.2c3.475 0.070 6.275 2.896 6.275 6.389s-2.8 6.318-6.275 6.387v-3.25z"></path>
      <path d="M3.963 11.683c-0.485 1.35-0.763 2.8-0.763 4.317v4.218h3.2v-4.218c0-0.848 0.122-1.666 0.328-2.448l-2.765-1.869z"></path>
      <path d="M16 3.2c-1.518 0-2.968 0.278-4.32 0.763l1.862 2.766c0.787-0.208 1.606-0.33 2.458-0.33 5.294 0 9.6 4.306 9.6 9.6v4.218h3.2v-4.218c0-7.069-5.731-12.8-12.8-12.8z"></path>
      <path d="M6.434 6.366h3.2v3.2h-3.2v-3.2z"></path>
      <path d="M28.8 0h9.6v3.2h-9.6v-3.2z"></path></symbol><symbol id="olymp-save-playlist-icon" viewBox="0 0 35 32"><path d="M8.012 20.365c0.401 0 0.727 0.326 0.727 0.727v7.274c0 0.4-0.326 0.724-0.727 0.724s-0.727-0.324-0.727-0.724v-7.274c0-0.401 0.326-0.727 0.727-0.727zM8.012 17.456c-2.006 0-3.636 1.632-3.636 3.636v7.274c0 2.003 1.631 3.633 3.636 3.633s3.636-1.631 3.636-3.633v-7.274c0-2.004-1.631-3.636-3.636-3.636v0z"></path>
      <path d="M5.705 27.58c-1.54-0.061-2.772-1.319-2.772-2.876s1.233-2.815 2.772-2.876v-2.909c-3.159 0.064-5.705 2.633-5.705 5.808s2.545 5.744 5.705 5.807v-2.954z"></path>
      <path d="M21.078 20.365c0.401 0 0.727 0.326 0.727 0.727v7.274c0 0.4-0.326 0.724-0.727 0.724s-0.727-0.324-0.727-0.724v-7.274c0-0.401 0.326-0.727 0.727-0.727zM21.078 17.456c-2.006 0-3.636 1.632-3.636 3.636v7.274c0 2.003 1.631 3.633 3.636 3.633 2.004 0 3.635-1.631 3.635-3.633v-7.274c0-2.004-1.631-3.636-3.635-3.636v0z"></path>
      <path d="M23.386 27.58c1.54-0.061 2.772-1.319 2.772-2.876s-1.233-2.815-2.772-2.876v-2.909c3.159 0.064 5.705 2.633 5.705 5.808s-2.545 5.744-5.705 5.807v-2.954z"></path>
      <path d="M3.603 13.53c-0.441 1.228-0.694 2.545-0.694 3.924v3.834h2.909v-3.834c0-0.771 0.111-1.514 0.298-2.225l-2.513-1.699z"></path>
      <path d="M14.545 5.818c-1.38 0-2.698 0.253-3.927 0.694l1.693 2.515c0.716-0.189 1.46-0.3 2.234-0.3 4.813 0 8.727 3.914 8.727 8.727v3.834h2.909v-3.834c0-6.426-5.21-11.636-11.636-11.636z"></path>
      <path d="M5.849 8.697h2.909v2.909h-2.909v-2.909z"></path>
      <path d="M29.091 0h2.909v8.727h-2.909v-8.727z"></path>
      <path d="M26.182 2.909h8.727v2.909h-8.727v-2.909z"></path></symbol><symbol id="olymp-share-icon" viewBox="0 0 40 32"><path d="M11.168 16.788v3.046h9.132v3.048h-9.132v3.072l-6.095-4.584 6.095-4.582zM14.216 10.683l-14.216 10.685 14.216 10.688v-6.129h9.132v-9.144h-9.132v-6.1z"></path>
      <path d="M15.739 6.095h3.048v3.048h-3.048v-3.048z"></path>
      <path d="M35.854 8.25l-10.973-8.25v6.1h-3.048v3.049h6.095v-3.046l6.095 4.582-6.095 4.582v-3.072h-12.18v3.048h9.132v6.129l14.214-10.686z"></path></symbol><symbol id="olymp-heart-icon" viewBox="0 0 36 32"><path d="M23.111 21.333h3.556v3.556h-3.556v-3.556z"></path>
      <path d="M32.512 2.997c-2.014-2.011-4.263-3.006-7.006-3.006-2.62 0-5.545 2.089-7.728 4.304-2.254-2.217-5.086-4.295-7.797-4.295-2.652 0-4.99 0.793-6.937 2.738-4.057 4.043-4.057 10.599 0 14.647 1.157 1.157 12.402 13.657 12.402 13.657 0.64 0.638 1.481 0.958 2.32 0.958s1.678-0.32 2.318-0.958l1.863-2.012-2.523-2.507-1.655 1.787c-2.078-2.311-11.095-12.324-12.213-13.442-1.291-1.285-2-2.994-2-4.811 0-1.813 0.709-3.518 2-4.804 1.177-1.175 2.54-1.698 4.425-1.698 0.464 0 2.215 0.236 5.303 3.273l2.533 2.492 2.492-2.532c2.208-2.242 4.201-3.244 5.196-3.244 1.769 0 3.113 0.588 4.496 1.97 1.289 1.284 1.998 2.99 1.998 4.804 0 1.815-0.709 3.522-1.966 4.775-0.087 0.085-0.098 0.094-1.9 2.041l-0.156 0.167 2.523 2.51 0.24-0.26c0 0 1.742-1.881 1.774-1.911 4.055-4.043 4.055-10.603-0.002-14.644z"></path></symbol><symbol id="olymp-magnifying-glass-icon" viewBox="0 0 34 32"><path d="M20.809 3.57c-4.76-4.76-12.478-4.76-17.239 0s-4.76 12.48 0 17.239c4.76 4.76 12.48 4.76 17.239 0 4.76-4.759 4.76-12.478 0-17.239zM18.654 18.654c-3.57 3.57-9.361 3.57-12.93 0-3.57-3.57-3.57-9.359 0-12.93s9.361-3.57 12.93 0c3.57 3.569 3.57 9.359 0 12.93z"></path>
      <path d="M24.022 21.907l2.154-2.156 2.157 2.155-2.154 2.156-2.157-2.155z"></path>
      <path d="M28.34 28.364c-0.596 0.597-1.559 0.597-2.155 0l-6.464-6.464-0.834-0.852 4.3-4.3-1.312-1.314-6.466 6.466 8.62 8.619c1.783 1.783 4.683 1.783 6.464 0 1.783-1.781 1.783-4.681 0-6.464l-2.155 2.155c0.596 0.596 0.594 1.562 0 2.155z"></path></symbol><symbol id="olymp-cupcake-icon" viewBox="0 0 27 32"><path d="M5.349 32h3.984v-2.667h-1.671l-0.933-6.541 12.959-0.063 0.312-0.063h-15.979z"></path>
      <path d="M20 22.667l-0.963 6.667h-1.704v2.667h4.016l1.339-9.333z"></path>
      <path d="M25 12.908c-0.921-2.695-3.583-4.743-7.667-4.891v2.673c2.639 0.121 4.513 1.237 5.143 3.080 0.157 0.457 0.435 0.864 0.805 1.176 0.451 0.379 0.719 1.028 0.719 1.737 0 0.649-0.232 1.251-0.635 1.648-0.377 0.372-0.636 0.849-0.74 1.368-0.195 0.957-1.133 1.651-2.203 1.652-0.375-0.047-1.288-0.669-1.587-0.873-0.447-0.305-0.972-0.464-1.503-0.464-0.173 0-0.347 0.017-0.519 0.051-0.7 0.139-1.317 0.553-1.709 1.151-0.605 0.919-1.267 1.468-1.772 1.468-0.596 0-1.305-0.757-1.771-1.467-0.392-0.597-1.008-1.013-1.709-1.152-0.173-0.033-0.347-0.049-0.52-0.049-0.531 0-1.055 0.159-1.5 0.463-0.301 0.204-1.216 0.828-1.557 0.873-1.101 0-2.040-0.695-2.233-1.651-0.105-0.52-0.363-0.996-0.74-1.368-0.405-0.4-0.636-1-0.636-1.649 0-0.709 0.268-1.359 0.717-1.736 0.371-0.312 0.649-0.719 0.805-1.177 0.588-1.716 2.545-2.885 5.144-3.079v-2.675c-3.924 0.225-6.751 2.212-7.667 4.891-1.005 0.847-1.667 2.217-1.667 3.776 0 1.428 0.561 2.691 1.427 3.545 0.436 2.157 2.437 3.789 4.848 3.789 1.073 0 2.248-0.784 3.059-1.336 0.871 1.327 2.227 2.669 4 2.669s3.128-1.343 4-2.669c0.809 0.552 1.984 1.336 3.059 1.336 2.411 0 4.412-1.632 4.848-3.789 0.865-0.856 1.427-2.117 1.427-3.545 0-1.559-0.661-2.929-1.667-3.776z"></path>
      <path d="M12 29.333h2.667v2.667h-2.667v-2.667z"></path>
      <path d="M12 13.332h2.667v-8h-2.667v8zM13.333 0c-0.737 0-1.333 0.596-1.333 1.332v1.333c0 0.736 0.596 1.333 1.333 1.333s1.333-0.597 1.333-1.333v-1.333c0-0.736-0.596-1.332-1.333-1.332z"></path></symbol><symbol id="olymp-weather-icon" viewBox="0 0 38 32"><path d="M18.909 29.091h2.909v2.909h-2.909v-2.909z"></path>
      <path d="M28.236 21.393c0-3.446-3.037-6.243-6.828-6.353-1.809-2.070-4.609-3.405-7.76-3.405-5.252 0-9.536 3.697-9.852 8.362-2.256 1.062-3.796 3.185-3.796 5.638 0 3.516 3.159 6.365 7.059 6.365h8.941v-2.909h-8.941c-2.288 0-4.15-1.549-4.15-3.456 0-1.238 0.815-2.39 2.124-3.005l1.548-0.729 0.116-1.708c0.214-3.168 3.267-5.649 6.95-5.649 2.201 0 4.231 0.879 5.571 2.41l0.836 0.956 1.27 0.036c2.208 0.065 4.004 1.61 4.004 3.446v2.016l1.887 0.707c1.139 0.431 1.876 1.36 1.876 2.368 0 0.644 0 2.608-3.209 2.608h-1.155v2.909h1.155c3.379 0 6.118-1.789 6.118-5.517 0-2.294-1.553-4.26-3.764-5.089z"></path>
      <path d="M21.818 7.273c-4.016 0-7.363 2.727-8.38 6.419l2.561 0.855 0.179 0.087c0.615-2.551 2.899-4.452 5.639-4.452 3.213 0 5.818 2.607 5.818 5.818 0 1.686-0.729 3.193-1.875 4.257 0.522 0.682 1.168 1.529 1.756 2.304 1.841-1.603 3.028-3.932 3.028-6.561 0-4.819-3.908-8.727-8.727-8.727zM13.693 5.818l-2.057-2.058c-0.569-0.567-1.489-0.567-2.057 0-0.569 0.57-0.569 1.489 0 2.058l2.057 2.057c0.569 0.57 1.488 0.57 2.057 0 0.569-0.567 0.569-1.487 0-2.057zM21.818 5.818c0.804 0 1.455-0.652 1.455-1.455v-2.909c0-0.801-0.65-1.455-1.455-1.455s-1.455 0.653-1.455 1.456v2.909c0 0.801 0.65 1.453 1.455 1.453zM36.364 13.091h-2.909c-0.804 0-1.455 0.652-1.455 1.456s0.65 1.453 1.455 1.453h2.909c0.804 0 1.455-0.649 1.455-1.453s-0.65-1.456-1.455-1.456z"></path>
      <path d="M33.277 3.223c0.57 0.569 0.567 1.487-0.001 2.057l-2.057 2.057c-0.569 0.569-1.487 0.57-2.057 0.001-0.569-0.57-0.569-1.489 0-2.060l2.057-2.057c0.569-0.569 1.488-0.567 2.058 0.001z"></path></symbol><symbol id="olymp-star-icon" viewBox="0 0 32 32"><path d="M24.029 27.192h3.2v3.2h-3.2v-3.2z"></path>
      <path d="M31.88 11.91c-0.275-0.826-0.984-1.43-1.837-1.562l-8.309-1.28-3.611-7.763c-0.379-0.816-1.194-1.336-2.090-1.336-0.893 0-1.707 0.522-2.086 1.336l-3.613 7.763-8.309 1.28c-0.854 0.131-1.563 0.736-1.838 1.562-0.275 0.827-0.067 1.739 0.536 2.36l6.088 6.298-1.413 8.731c-0.142 0.88 0.226 1.762 0.947 2.277 0.397 0.28 0.862 0.424 1.328 0.424 0.384 0 0.766-0.098 1.115-0.291l7.243-4.037 4.768 2.656v-3.662l-4.768-2.658-7.184 4.005 1.378-8.517-1.114-1.154-4.922-5.090 8.323-1.282 0.723-1.552 2.798-6.014 3.52 7.566 8.326 1.283-6.038 6.243 0.733 4.53h3.242l-0.56-3.458 6.091-6.298c0.6-0.622 0.806-1.534 0.531-2.362z"></path></symbol><symbol id="olymp-headphones-icon" viewBox="0 0 36 32"><path d="M9.792 17.78c0.491 0 0.889 0.398 0.889 0.889v8.891c0 0.489-0.398 0.885-0.889 0.885s-0.889-0.396-0.889-0.885v-8.891c0-0.491 0.398-0.889 0.889-0.889zM9.792 14.224c-2.452 0-4.444 1.995-4.444 4.444v8.891c0 2.448 1.993 4.441 4.444 4.441s4.444-1.993 4.444-4.441v-8.891c0-2.45-1.993-4.444-4.444-4.444v0z"></path>
      <path d="M6.972 26.597c-1.883-0.075-3.388-1.612-3.388-3.515s1.508-3.44 3.388-3.515v-3.556c-3.861 0.078-6.972 3.218-6.972 7.099s3.111 7.020 6.972 7.097v-3.611z"></path>
      <path d="M25.762 17.78c0.491 0 0.889 0.398 0.889 0.889v8.891c0 0.489-0.398 0.885-0.889 0.885s-0.889-0.396-0.889-0.885v-8.891c0-0.491 0.398-0.889 0.889-0.889zM25.762 14.224c-2.452 0-4.444 1.995-4.444 4.444v8.891c0 2.448 1.993 4.441 4.444 4.441 2.45 0 4.443-1.993 4.443-4.441v-8.891c0-2.45-1.993-4.444-4.443-4.444v0z"></path>
      <path d="M28.583 26.597c1.883-0.075 3.388-1.612 3.388-3.515s-1.508-3.44-3.388-3.515v-3.556c3.861 0.078 6.972 3.218 6.972 7.099s-3.111 7.020-6.972 7.097v-3.611z"></path>
      <path d="M4.404 9.426c-0.539 1.5-0.848 3.111-0.848 4.796v4.686h3.556v-4.686c0-0.942 0.135-1.851 0.364-2.72l-3.072-2.076z"></path>
      <path d="M17.778 0c-1.687 0-3.298 0.309-4.8 0.848l2.069 3.074c0.875-0.231 1.785-0.366 2.731-0.366 5.883 0 10.667 4.784 10.667 10.667v4.686h3.556v-4.686c0-7.854-6.368-14.222-14.222-14.222z"></path>
      <path d="M7.148 3.518h3.556v3.556h-3.556v-3.556z"></path></symbol><symbol id="olymp-block-from-chat-icon" viewBox="0 0 35 32"><path d="M32 0h-28.8c-1.768 0-3.2 1.434-3.2 3.2v16c0 1.766 1.434 3.2 3.2 3.2 0 0 0.6 0 1.6 0v-3.2h-1.6v-16h28.8v19.2c1.766 0 3.2-1.434 3.2-3.2v-16c0-1.766-1.434-3.2-3.2-3.2zM20.8 22.4h1.6v-3.2h-1.6v3.2zM25.6 22.4h3.2v-3.2h-3.2v3.2z"></path>
      <path d="M17.6 19.2l-9.6 6.626v-6.626h-3.2v12.8l12.8-9.6h3.2v-3.2z"></path>
      <path d="M22.125 8.938l-2.262-2.262-2.262 2.262-2.262-2.262-2.264 2.262 2.262 2.262-2.262 2.264 2.264 2.262 2.262-2.262 2.262 2.262 2.262-2.262-2.262-2.264z"></path></symbol><symbol id="olymp-add-to-conversation-icon" viewBox="0 0 35 32"><path d="M32 0h-28.8c-1.768 0-3.2 1.434-3.2 3.2v16c0 1.766 1.434 3.2 3.2 3.2 0 0 0.6 0 1.6 0v-3.2h-1.6v-16h28.8v19.2c1.766 0 3.2-1.434 3.2-3.2v-16c0-1.766-1.434-3.2-3.2-3.2zM20.8 22.4h1.6v-3.2h-1.6v3.2zM25.6 22.4h3.2v-3.2h-3.2v3.2z"></path>
      <path d="M17.6 19.2l-9.6 6.626v-6.626h-3.2v12.8l12.8-9.6h3.2v-3.2z"></path>
      <path d="M22.4 9.6h-3.2v-3.2h-3.2v3.2h-3.2v3.2h3.2v3.2h3.2v-3.2h3.2z"></path></symbol><symbol id="olymp-speech-balloon-icon" viewBox="0 0 35 32"><path d="M32 0h-28.8c-1.768 0-3.2 1.434-3.2 3.2v16c0 1.766 1.434 3.2 3.2 3.2 0 0 0.6 0 1.6 0v-3.2h-1.6v-16h28.8v19.2c1.766 0 3.2-1.434 3.2-3.2v-16c0-1.766-1.434-3.2-3.2-3.2zM20.8 22.4h1.6v-3.2h-1.6v3.2zM25.6 22.4h3.2v-3.2h-3.2v3.2zM27.2 6.4h-20.8v3.2h20.8v-3.2zM6.4 16h12.8v-3.2h-12.8v3.2z"></path>
      <path d="M17.6 19.2l-9.6 6.626v-6.626h-3.2v12.8l12.8-9.6h3.2v-3.2z"></path></symbol><symbol id="olymp-add-a-place-icon" viewBox="0 0 26 32"><path d="M13.091 0.001c-5.697 0-10.531 3.645-12.33 8.726h3.145c1.639-3.433 5.135-5.817 9.185-5.817 5.613 0 10.182 4.567 10.182 10.182 0 7.971-7.881 15.129-10.177 15.956-1.953-0.71-7.364-5.712-9.396-11.594h-3.040c2.268 7.964 9.923 14.545 12.432 14.545 2.957 0 13.091-8.95 13.091-18.908 0-7.231-5.86-13.091-13.091-13.091z"></path>
      <path d="M13.091 7.316c-3.213 0-5.818 2.605-5.818 5.818 0 3.215 2.605 5.818 5.818 5.818s5.818-2.604 5.818-5.818c0-3.215-2.605-5.818-5.818-5.818zM13.091 16.045c-1.606 0-2.909-1.303-2.909-2.909s1.303-2.909 2.909-2.909 2.909 1.303 2.909 2.909-1.303 2.909-2.909 2.909z"></path>
      <path d="M0 11.636h2.909v2.909h-2.909v-2.909z"></path></symbol><symbol id="olymp-chat---messages-icon" viewBox="0 0 40 32"><path d="M24.381 7.621h-21.333c-1.378 0-3.048 1.606-3.048 3.046v13.716c0 1.443 1.67 3.048 3.048 3.048v4.57l12.19-4.568v-3.051l-9.143 3.051v-3.051h-3.048v-13.714h21.333v16.763c1.378 0 3.048-1.605 3.048-3.048v-13.716c0-1.44-1.67-3.046-3.048-3.046zM18.286 27.432h3.048v-3.048h-3.048v3.048zM6.095 16.763h15.238v-3.046h-15.238v3.046zM6.095 21.336h9.143v-3.048h-9.143v3.048zM15.238 3.051h24.381c0-1.443-1.67-3.049-3.048-3.049h-21.333c-1.378 0-3.048 1.606-3.048 3.049v1.527h3.048v-1.527zM36.571 16.763l-4.571-0.002v3.051l-3.048-1.016v3.301l6.095 2.284v-4.568c0.779 0 1.524 0 1.524 0 1.378 0 3.048-1.606 3.048-3.049v-4.571h-3.048v4.571zM36.571 9.144h3.048v-3.048h-3.048v3.048z"></path></symbol><symbol id="olymp-check-icon" viewBox="0 0 37 32"><path d="M11.243 23.184l-7.424-7.421-3.712 3.712 11.136 11.136 18.557-18.557-3.712-3.715-14.845 14.845zM33.512 0.915l-3.712 3.712 3.712 3.712 3.712-3.712-3.712-3.712z"></path></symbol><symbol id="olymp-plus-icon" viewBox="0 0 32 32"><path d="M18.286 0h-4.571v13.714h-13.714v4.571h13.714v13.714h4.571v-13.714h4.571v-4.571h-4.571v-13.714zM27.429 13.714v4.571h4.571v-4.571h-4.571z"></path></symbol><symbol id="olymp-albums-icon" viewBox="0 0 32 32"><path d="M17.778 0v14.222h14.222v-14.222h-14.222zM28.444 10.667h-7.111v-7.111h7.111v7.111zM0 32h14.222v-14.222h-14.222v14.222zM3.556 21.333h7.111v7.111h-7.111v-7.111zM28.444 28.444h-7.111v-7.111h3.556v-3.556h-7.111v14.222h14.222v-7.111h-3.556v3.556zM0 14.222h14.222v-14.222h-14.222v14.222zM3.556 3.556h7.111v7.111h-7.111v-7.111zM28.444 21.333h3.556v-3.556h-3.556v3.556z"></path></symbol><symbol id="olymp-photos-icon" viewBox="0 0 29 32"><path d="M6.4 22.402h16v-16h-16v16zM9.6 9.602h9.6v9.6h-9.6v-9.6zM25.6 32h3.2v-3.2h-3.2v3.2zM25.6 0h-22.4c-1.766 0-3.2 1.432-3.2 3.2v25.6c0 1.766 1.434 3.2 3.2 3.2h19.2v-3.2h-19.2v-25.6h22.4v22.398h3.2v-22.398c0-1.768-1.434-3.2-3.2-3.2z"></path></symbol><symbol id="olymp-day-calendar-icon" viewBox="0 0 26 32"><path d="M23.273 32h2.909v-2.908h-2.909v2.908zM11.636 21.818h2.909v-2.908h-2.909v2.908zM23.273 2.911h-2.909v-1.456c0-0.803-0.652-1.455-1.455-1.455-0.804 0-1.455 0.652-1.455 1.455v1.455h-8.727v-1.455c0-0.803-0.652-1.455-1.455-1.455-0.804 0-1.455 0.652-1.455 1.455v1.455h-2.909c-1.606 0-2.909 1.302-2.909 2.909v23.273c0 1.607 1.303 2.909 2.909 2.909h17.455v-2.908h-17.455v-23.274h2.909v1.455c0 0.803 0.65 1.455 1.455 1.455 0.803 0 1.455-0.652 1.455-1.455v-1.455h8.727v1.455c0 0.803 0.65 1.455 1.455 1.455 0.803 0 1.455-0.652 1.455-1.455v-1.455h2.909v20.364h2.909v-20.364c0-1.606-1.303-2.908-2.909-2.908zM7.273 26.185h11.636v-11.636h-11.636v11.636zM10.182 17.457h5.818v5.818h-5.818v-5.818z"></path></symbol><symbol id="olymp-week-calendar-icon" viewBox="0 0 26 32"><path d="M23.273 32h2.909v-2.908h-2.909v2.908zM23.273 2.911h-2.909v-1.456c0-0.803-0.652-1.455-1.455-1.455-0.804 0-1.455 0.652-1.455 1.455v1.455h-8.727v-1.455c0-0.803-0.652-1.455-1.455-1.455-0.804 0-1.455 0.652-1.455 1.455v1.455h-2.909c-1.606 0-2.909 1.302-2.909 2.909v23.273c0 1.607 1.303 2.909 2.909 2.909h17.455v-2.908h-17.455v-23.274h2.909v1.455c0 0.803 0.65 1.455 1.455 1.455 0.803 0 1.455-0.652 1.455-1.455v-1.455h8.727v1.455c0 0.803 0.65 1.455 1.455 1.455 0.803 0 1.455-0.652 1.455-1.455v-1.455h2.909v20.364h2.909v-20.364c0-1.606-1.303-2.908-2.909-2.908zM7.273 26.182h11.636v-2.908h-11.636v2.908zM18.909 18.909h-11.636v2.911h11.636v-2.911zM18.909 14.545h-11.636v2.909h11.636v-2.909z"></path></symbol><symbol id="olymp-month-calendar-icon" viewBox="0 0 26 32"><path d="M23.273 32h2.909v-2.908h-2.909v2.908zM23.273 2.911h-2.909v-1.456c0-0.803-0.65-1.455-1.455-1.455s-1.455 0.652-1.455 1.455v1.455h-8.727v-1.455c0-0.803-0.652-1.455-1.455-1.455-0.804 0-1.455 0.652-1.455 1.455v1.455h-2.909c-1.606 0-2.909 1.302-2.909 2.909v23.273c0 1.607 1.303 2.909 2.909 2.909h17.455v-2.908h-17.455v-23.274h2.909v1.455c0 0.803 0.65 1.455 1.455 1.455 0.803 0 1.455-0.652 1.455-1.455v-1.455h8.727v1.455c0 0.803 0.65 1.455 1.455 1.455s1.455-0.652 1.455-1.455v-1.455h2.909v20.364h2.909v-20.364c0-1.606-1.303-2.908-2.909-2.908zM7.273 26.182h2.909v-2.909h-2.909v2.909zM11.636 26.182h2.909v-2.909h-2.909v2.909zM16 26.182h2.909v-2.909h-2.909v2.909zM10.182 18.909h-2.909v2.911h2.909v-2.911zM14.545 18.909h-2.909v2.911h2.909v-2.911zM18.909 18.909h-2.909v2.911h2.909v-2.911zM10.182 14.545h-2.909v2.909h2.909v-2.909zM14.545 14.545h-2.909v2.909h2.909v-2.909zM18.909 14.545h-2.909v2.909h2.909v-2.909z"></path></symbol><symbol id="olymp-checked-calendar-icon" viewBox="0 0 26 32"><path d="M23.273 31.999h2.909v-2.908h-2.909v2.908zM23.273 2.909h-2.909v-1.455c0-0.803-0.65-1.455-1.455-1.455s-1.455 0.652-1.455 1.455v1.455h-8.727v-1.455c0-0.803-0.652-1.455-1.455-1.455-0.804 0-1.455 0.652-1.455 1.455v1.455h-2.909c-1.606 0-2.909 1.302-2.909 2.909v23.273c0 1.606 1.303 2.908 2.909 2.908h17.455v-2.908h-17.455v-23.273h2.909v1.455c0 0.803 0.65 1.455 1.455 1.455 0.803 0 1.455-0.652 1.455-1.455v-1.455h8.727v1.455c0 0.803 0.65 1.455 1.455 1.455s1.455-0.652 1.455-1.455v-1.455h2.909v20.364h2.909v-20.364c0-1.607-1.303-2.909-2.909-2.909zM11.033 19.513l-2.057-2.057-2.057 2.057 4.115 4.113 8.227-8.228-2.057-2.057-6.172 6.172z"></path></symbol><symbol id="olymp-multimedia-icon" viewBox="0 0 32 32"><path d="M28.8 0.002h-22.4v3.2h22.4v5.41l-11.57 5.786-7.579-8.496-6.451 5.166v-4.666h-3.2v22.4c0 1.766 1.434 3.198 3.2 3.198h25.6c1.766 0 3.2-1.432 3.2-3.198v-25.602c0-1.766-1.434-3.198-3.2-3.198zM3.2 15.168l6.203-4.963 7.872 8.995h-14.075v-4.032zM28.8 28.802h-25.6v-6.402h25.6v6.402zM28.8 19.2h-7.285l-2.078-2.33 9.363-4.682v7.011zM3.2 0.002h-3.2v3.2h3.2v-3.2zM11.2 24h-3.2v3.2h3.2v-3.2zM17.6 24h-3.2v3.2h3.2v-3.2zM24 24h-3.2v3.2h3.2v-3.2z"></path></symbol><symbol id="olymp-settings-v2-icon" viewBox="0 0 32 32"><path d="M8.889 21.333c-2.32 0-4.272 1.488-5.006 3.556h-3.883v3.556h3.883c0.734 2.066 2.686 3.556 5.006 3.556s4.272-1.49 5.006-3.556h10.994v-3.556h-10.994c-0.734-2.068-2.686-3.556-5.006-3.556zM10.667 28.444h-3.556v-3.556h3.556v3.556zM23.111 10.667c-2.32 0-4.272 1.49-5.006 3.556h-18.105v3.556h18.105c0.734 2.068 2.688 3.556 5.006 3.556s4.272-1.49 5.006-3.556h3.883v-3.556h-3.883c-0.734-2.068-2.686-3.556-5.006-3.556zM24.889 17.778h-3.556v-3.556h3.556v3.556zM13.895 3.556c-0.734-2.068-2.686-3.556-5.006-3.556s-4.272 1.488-5.006 3.556h-3.883v3.556h3.883c0.734 2.066 2.686 3.556 5.006 3.556s4.272-1.488 5.006-3.556h18.105v-3.556h-18.105zM10.667 7.111h-3.556v-3.556h3.556v3.556zM28.444 28.444h3.556v-3.556h-3.556v3.556z"></path></symbol><symbol id="olymp-close-icon" viewBox="0 0 32 32"><path d="M14.222 17.778h3.556v-3.556h-3.556v3.556zM31.084 3.429l-2.514-2.514-10.057 10.057 2.514 2.514 10.057-10.057zM0.916 28.571l2.514 2.514 10.057-10.055-2.516-2.514-10.055 10.055zM18.514 21.029l10.057 10.055 2.514-2.514-10.057-10.055-2.514 2.514zM0.916 3.431l10.057 10.055 2.516-2.514-10.059-10.057-2.514 2.516z"></path></symbol><symbol id="olymp-logout-icon" viewBox="0 0 43 32"><path d="M26.667 3.557c4.962 0 9.232 2.91 11.23 7.111h3.838c-2.197-6.212-8.105-10.668-15.068-10.668s-12.873 4.457-15.070 10.667h3.84c1.998-4.199 6.268-7.109 11.23-7.109zM26.667 28.446c-4.962 0-9.232-2.91-11.23-7.111h-3.84c2.199 6.21 8.107 10.665 15.070 10.665 6.962 0 12.871-4.455 15.070-10.665h-3.838c-2 4.201-6.27 7.111-11.232 7.111zM23.111 17.778v-3.556h-16.306l3.252-3.25-2.514-2.514-7.543 7.541 7.543 7.543 2.514-2.514-3.252-3.252h16.306zM39.111 14.224v3.556h3.556v-3.556h-3.556z"></path></symbol><symbol id="olymp-settings-icon" viewBox="0 0 32 32"><path d="M7.111 3.881v-3.881h-3.556v3.883c-2.068 0.734-3.556 2.686-3.556 5.006s1.488 4.272 3.556 5.006v10.994h3.556v-10.996c2.068-0.734 3.556-2.686 3.556-5.004s-1.488-4.272-3.556-5.008zM7.111 10.667h-3.556v-3.556h3.556v3.556zM28.444 3.881v-3.881h-3.556v3.883c-2.066 0.734-3.556 2.686-3.556 5.006s1.49 4.27 3.556 5.006v18.105h3.556v-18.107c2.066-0.734 3.556-2.686 3.556-5.004s-1.49-4.272-3.556-5.008zM28.444 10.667h-3.556v-3.556h3.556v3.556zM17.778 18.105v-18.105h-3.556v18.105c-2.068 0.734-3.556 2.686-3.556 5.006 0 2.318 1.488 4.27 3.556 5.006v3.883h3.556v-3.883c2.066-0.736 3.556-2.69 3.556-5.006 0-2.32-1.49-4.272-3.556-5.006zM17.778 24.887h-3.556v-3.554h3.556v3.554zM3.556 32h3.556v-3.556h-3.556v3.556z"></path></symbol><symbol id="olymp-blog-icon" viewBox="0 0 29 32"><path d="M25.6 0h-22.4c-1.766 0-3.2 1.434-3.2 3.2v28.8h22.4v-6.4h6.4v-3.2h-9.6v6.4h-16v-25.6h22.4v9.6h3.2v-9.6c0-1.766-1.434-3.2-3.2-3.2zM22.4 6.4h-16v3.2h16v-3.2zM22.4 12.798h-16v3.202h16v-3.202zM6.4 22.4h9.6v-3.2h-9.6v3.2zM25.6 19.2h3.2v-3.2h-3.2v3.2z"></path></symbol><symbol id="olymp-status-icon" viewBox="0 0 36 32"><path d="M32-0.002h-28.444c-1.963 0-3.556 1.593-3.556 3.557v21.332h3.554v0.004h28.444v-3.557h-28.443v-17.778h28.444v24.889h-24.889v3.556h24.889c1.963 0 3.556-1.593 3.556-3.556v-24.889c0-1.964-1.593-3.557-3.556-3.557zM0 32h3.556v-3.556h-3.556v3.556zM7.109 7.111v3.557h10.667v-3.557h-10.667zM7.109 17.778h21.333v-3.556h-21.333v3.556z"></path></symbol><symbol id="olymp-happy-sticker-icon" viewBox="0 0 32 32"><path d="M25.6 0.002h-4.8v3.2h4.8c1.766 0 3.2 1.432 3.2 3.198v12.8h-3.2c-3.534 0-6.4 2.867-6.4 6.402v3.2h-12.8c-1.766 0-3.2-1.434-3.2-3.2v-19.202c0-1.766 1.434-3.198 3.2-3.198h4.8v-3.2h-4.8c-3.534 0-6.4 2.864-6.4 6.398v19.202c0 3.534 2.866 6.398 6.4 6.398h17.6c3.234-3.248 5-4.765 8-7.998v-17.602c0-3.533-2.866-6.398-6.4-6.398zM22.4 28.802v-3.2c0-1.766 1.434-3.2 3.2-3.2h3.2c-1.8 1.618-3.982 3.949-6.4 6.4zM11.2 12.8h-3.2v3.2h3.2v-3.2zM15.978 22.395c2.646 0 4.792-1.358 4.792-3.034v-0.158h-9.586v0.158c0 1.675 2.147 3.034 4.794 3.034zM17.6 0h-3.2v3.2h3.2v-3.2zM23.998 12.8h-3.198v3.2h3.198v-3.2z"></path></symbol><symbol id="olymp-happy-face-icon" viewBox="0 0 32 32"><path d="M16 0c-8.837 0-16 7.16-16 15.989 0 7.166 4.715 13.227 11.213 15.262v-3.39c-4.69-1.899-8-6.49-8-11.859 0-7.070 5.731-12.802 12.8-12.802s12.8 5.731 12.8 12.802c0 5.37-3.312 9.96-8 11.859v3.378c6.485-2.040 11.187-8.094 11.187-15.25 0-8.829-7.165-15.989-16-15.989zM11.211 12.8h-3.2v3.202h3.2v-3.202zM20.813 12.8v3.202h3.2v-3.202h-3.2zM11.198 19.365c0 1.675 2.146 3.032 4.794 3.032s4.794-1.357 4.794-3.032v-0.16h-9.587v0.16zM14.413 32.002h3.2v-3.2h-3.2v3.2z"></path></symbol><symbol id="olymp-computer-icon" viewBox="0 0 35 32"><path d="M32 22.4h-28.8v-19.2h32c0-1.766-1.434-3.2-3.2-3.2h-28.8c-1.766 0-3.2 1.432-3.2 3.2v19.2c0 1.766 1.434 3.202 3.2 3.202h9.6v3.198h-4.8v3.2h19.2v-3.2h-4.8v-3.198h9.6c1.766 0 3.2-1.434 3.2-3.202v-9.598h-3.2v9.598zM19.2 28.8h-3.2v-3.198h3.2v3.198zM32 9.6h3.2v-3.198h-3.2v3.198zM12.8 14.402v-3.2h-3.2v3.2h3.2zM19.2 14.402v-3.2h-3.2v3.2h3.2zM25.6 14.402v-3.2h-3.2v3.2h3.2z"></path></symbol><symbol id="olymp-manage-widgets-icon" viewBox="0 0 39 32"><path d="M0 3.554v24.891c0 1.963 1.593 3.556 3.556 3.556h14.222v-3.556h-14.222v-24.891h14.222v-3.556h-14.222c-1.963 0-3.556 1.593-3.556 3.556zM10.667 24.889v-17.778h-3.556v17.778h3.556zM14.222 24.889h3.556v-17.778h-3.556v17.778zM21.333 3.554h3.556v-3.556h-3.556v3.556zM28.444 3.554h3.556v-3.556h-3.556v3.556zM35.556-0.002v3.556h3.556v-3.556h-3.556zM35.556 10.665h3.556v-3.554h-3.556v3.554zM35.556 17.776h3.556v-3.556h-3.556v3.556zM35.556 24.889h3.556v-3.556h-3.556v3.556zM35.556 32h3.556v-3.556h-3.556v3.556zM28.444 32h3.556v-3.556h-3.556v3.556zM21.333 32h3.556v-3.556h-3.556v3.556z"></path></symbol><symbol id="olymp-badge-icon" viewBox="0 0 32 32"><path d="M25.6 16c0-5.302-4.299-9.6-9.6-9.6s-9.6 4.298-9.6 9.6c0 5.301 4.299 9.598 9.6 9.598s9.6-4.298 9.6-9.598zM9.6 16c0-3.534 2.866-6.4 6.4-6.4s6.4 2.867 6.4 6.4-2.866 6.402-6.4 6.402-6.4-2.867-6.4-6.402zM16 0c-8.837 0-16 7.163-16 16 0 7.163 4.709 13.227 11.2 15.266v-3.405c-4.69-1.899-8-6.491-8-11.861 0-7.069 5.731-12.8 12.8-12.8s12.8 5.731 12.8 12.8c0 5.37-3.31 9.962-8 11.861v3.405c6.491-2.038 11.2-8.102 11.2-15.266 0-8.837-7.165-16-16-16zM14.4 31.998h3.2v-3.198h-3.2v3.198z"></path></symbol><symbol id="olymp-newsfeed-icon" viewBox="0 0 29 32"><path d="M6.4 25.602v-3.202h9.6v3.202h-9.6zM6.4 16h16v3.2h-16v-3.2zM22.4 25.602v6.398h-22.4v-28.8c0-1.766 1.434-3.2 3.2-3.2h22.4c1.766 0 3.2 1.434 3.2 3.2v9.6h-25.6v16.002h16v-6.402h9.6v3.202h-6.4zM25.6 9.6v-6.4h-22.4v6.4h22.4zM8 8h-3.2v-3.2h3.2v3.2zM12.8 8h-3.2v-3.2h3.2v3.2zM17.6 8h-3.2v-3.2h3.2v3.2zM25.6 16h3.2v3.2h-3.2v-3.2z"></path></symbol><symbol id="olymp-camera-icon" viewBox="0 0 43 32"><path d="M21.333 10.667c-3.927 0-7.111 3.182-7.111 7.111 0 3.927 3.184 7.111 7.111 7.111s7.111-3.184 7.111-7.111c0-3.929-3.184-7.111-7.111-7.111zM21.333 21.337c-1.963 0-3.556-1.593-3.556-3.556s1.593-3.556 3.556-3.556 3.556 1.593 3.556 3.556-1.593 3.556-3.556 3.556zM35.556 3.556h-3.556c0-1.964-1.593-3.556-3.556-3.556h-14.222c-1.963 0-3.556 1.591-3.556 3.556h-3.556c-3.927 0-7.111 3.184-7.111 7.111v14.222c0 3.929 3.184 7.111 7.111 7.111 0 0 6.924 0 15.89 0h0.11v-3.556h-16c-1.963 0-3.556-1.593-3.556-3.556v-14.222c0-1.963 1.593-3.556 3.556-3.556h7.111v-3.556h14.222v3.556h7.111c1.963 0 3.556 1.593 3.556 3.556v14.222c0 1.963-1.593 3.556-3.556 3.556h-1.778v3.556c1.122 0 1.778 0 1.778 0 3.927 0 7.111-3.182 7.111-7.111v-14.222c0-3.927-3.184-7.111-7.111-7.111zM26.667 32h3.556v-3.556h-3.556v3.556z"></path></symbol><symbol id="olymp-stats-icon" viewBox="0 0 32 32"><path d="M23.273 10.182h-2.909v1.236l-3.873 12.589-0.765-5.1h-2.941l1.761 11.745v1.347h2.909v-1.236l4.313-14.015 1.505 5.271v1.252h2.909v-2.909l-2.909-8.932v-1.249zM14.416 10.182l-1.325-8.836v-1.345h-2.909v1.233l-5.818 17.673h-4.364v2.912h7.273v-1.455l3.873-12.371 0.49 2.189h2.78zM29.091 20.364v2.909h2.909v-2.909h-2.909zM14.545 13.091h-2.909v2.908h2.909v-2.908z"></path></symbol><symbol id="olymp-calendar-icon" viewBox="0 0 26 32"><path d="M23.273 32h2.909v-2.909h-2.909v2.909zM23.273 2.911h-2.909v-1.456c0-0.801-0.652-1.455-1.455-1.455-0.804 0-1.455 0.653-1.455 1.455v1.456h-8.727v-1.456c0-0.801-0.652-1.455-1.455-1.455-0.804 0-1.455 0.653-1.455 1.455v1.456h-2.909c-1.606 0-2.909 1.302-2.909 2.909v23.271c0 1.607 1.303 2.909 2.909 2.909h17.455v-2.909h-17.455v-23.271h2.909v1.453c0 0.803 0.65 1.455 1.455 1.455 0.803 0 1.455-0.652 1.455-1.455v-1.453h8.727v1.453c0 0.803 0.65 1.455 1.455 1.455 0.803 0 1.455-0.652 1.455-1.455v-1.453h2.909v20.362h2.909v-20.362c0-1.607-1.303-2.909-2.909-2.909zM7.273 26.182h2.909v-2.909h-2.909v2.909zM14.545 26.182v-2.909h-2.909v2.909h2.909zM16 26.182h2.909v-2.909h-2.909v2.909zM14.545 18.909h-7.273v2.909h7.273v-2.909zM18.909 18.909h-2.909v2.909h2.909v-2.909zM14.545 14.547h-7.273v2.908h7.273v-2.908zM18.909 14.547h-2.909v2.908h2.909v-2.908z"></path></symbol><symbol id="olymp-happy-faces-icon" viewBox="0 0 33 32"><path d="M13.329 5.357c-7.364 0-13.333 5.965-13.333 13.325 0 5.971 3.929 11.023 9.344 12.716v-2.823c-3.908-1.584-6.667-5.409-6.667-9.885 0-5.889 4.776-10.665 10.667-10.665s10.667 4.776 10.667 10.665c0 4.476-2.759 8.303-6.667 9.885v2.815c5.404-1.7 9.323-6.745 9.323-12.708 0-7.36-5.969-13.325-13.333-13.325zM9.34 16.025h-2.667v2.665h2.667v-2.665zM17.34 16.025v2.665h2.667v-2.665h-2.667zM9.328 21.495c0 1.395 1.788 2.527 3.995 2.527s3.995-1.132 3.995-2.527v-0.135h-7.989v0.135zM12.007 32.025h2.667v-2.668h-2.667v2.668zM13.805 2.691h8.868c0 0 1.495 0.417 2.183 0.697l2.397-1.797c-1.747-0.993-3.761-1.565-5.913-1.565-2.856 0-5.475 1.001-7.535 2.665zM32.472 7.56l-2.131 2.129c0.195 0.751 0.331 1.523 0.331 2.336 0 1.756-0.488 3.392-1.333 4.789v4.14c2.452-2.195 4-5.38 4-8.931 0.001-1.58-0.311-3.083-0.867-4.464zM30.673 4.025h-2.667v2.667h2.667v-2.667z"></path></symbol><symbol id="olymp-thunder-icon" viewBox="0 0 26 32"><path d="M25.6 11.198h-8l6.4-11.198-18.669 0.005-5.331 17.597h4.798l-1.598 14.398 4.8-4.458v-4.914l-1.6 1.371 1.6-9.602h-3.2l3.2-11.2h9.6l-4.8 11.2h4.8v4.23l8-7.43zM11.2 22.4h3.2v-3.2h-3.2v3.2z"></path></symbol><symbol id="olymp-menu-icon" viewBox="0 0 41 32"><path d="M4.571 0h-4.571v4.571h4.571v-4.571zM9.143 0v4.571h32v-4.571h-32zM13.714 13.714h-13.714v4.571h13.714v-4.571zM18.286 13.714v4.571h4.571v-4.571h-4.571zM27.429 18.286h13.714v-4.571h-13.714v4.571zM0 32h32v-4.569h-32v4.569zM36.571 32h4.571v-4.569h-4.571v4.569z"></path></symbol><symbol id="olymp-shopping-bag-icon" viewBox="0 0 512 512"><path d="m326 512l116 0-23-419-70 0c0-51-42-93-93-93-51 0-93 42-93 93l-70 0-23 419 116 0 0-47-70 0 24-325 23 0 0 46 46 0 0-46 94 0 0 46 46 0 0-46 23 0 24 325-70 0z m-117-419c0-26 21-46 47-46 26 0 47 20 47 46z m24 419l46 0 0-47-46 0z"></path></symbol><symbol id="olymp-home-icon" viewBox="0 0 32 32"><path d="M25.6 32h-6.4v-9.6h-6.4v9.6h-6.4c-1.766 0-3.2-1.434-3.2-3.2v-1.6h3.2v1.6h3.2v-9.6h12.8v9.6h3.2v-16l3.2 1.6v14.4c0 1.766-1.434 3.2-3.2 3.2zM3.2 20.8h3.2v3.2h-3.2v-3.2zM3.2 14.4l3.2-1.6v4.8h-3.2v-3.2z"></path>
      <path d="M16 4.526l11.475 11.474h4.525l-16-16-16 16h4.526z"></path></symbol><symbol id="olymp-info-icon" viewBox="0 0 32 32"><path d="M16 0c-8.837 0-16 7.158-16 15.989 0 7.165 4.714 13.226 11.213 15.261v-3.389c-4.69-1.901-8-6.49-8-11.859 0-7.069 5.731-12.798 12.8-12.798s12.8 5.73 12.8 12.798c0 5.37-3.312 9.962-8 11.859v3.376c6.485-2.040 11.187-8.093 11.187-15.248 0-8.83-7.166-15.989-16-15.989zM14.413 24.002h3.2v-9.6h-3.2v9.6zM14.413 11.202h3.2v-3.2h-3.2v3.2zM14.413 32h3.2v-3.2h-3.2v3.2z"></path></symbol><symbol id="olymp-music-fullscreen-icon" viewBox="0 0 512 512"><path d="m320 0l0 64 80 0-112 112 48 48 112-112 0 80 64 0 0-192z m-256 128l-64 0 0 64 64 0z m128-64l0-64-192 0 0 64 64 0 0 48 112 112 48-48-112-112z m256 320l64 0 0-64-64 0z m0 16l-112-112-48 48 112 112-80 0 0 64 192 0 0-64-64 0z m-272-112l-112 112 0-80-64 0 0 192 192 0 0-64-80 0 112-112z"></path></symbol><symbol id="olymp-music-minimize-screen-icon" viewBox="0 0 512 512"><path d="m32 352l80 0-112 112 48 48 112-112 0 80 64 0 0-192-192 0z m320 0l0-64-64 0 0 192 64 0 0-80 112 112 48-48-112-112z m128 0l0-64-64 0 0 64z m-320-240l-112-112-48 48 112 112 48 0 0 64 64 0 0-192-64 0z m-128 48l0 64 64 0 0-64z m448 0l-80 0 112-112-48-48-112 112 0-80-64 0 0 192 192 0z"></path></symbol><symbol id="olymp-music-next-song-icon" viewBox="0 0 512 512"><path d="m264 199l-167-102c-12-7-25-11-37-11-30 0-60 23-60 68l0 59 43 0c0-51 0-59 0-59 0-16 6-26 17-26 4 0 9 2 15 5l166 103c19 11 20 30 1 41l-167 102c-6 3-11 5-15 5-11 0-17-9-17-25 0 0 0-7 0-17 0 0 0-1 0-1l-43 0 0 18c0 44 30 68 60 68 12 0 25-4 37-11l168-103c21-13 34-33 34-56 0-23-13-44-35-58z m-264 57l43 0 0 43-43 0z m477-57l-167-102c-12-7-24-11-37-11-30 0-60 23-60 68l0 44 43 29c0-6 0-11 0-13 0-52 0-60 0-60 0-16 7-26 17-26 5 0 10 2 15 5l167 103c19 11 19 30 1 41l-168 102c-5 3-10 5-15 5-10 0-17-9-17-25 0 0 0-7 0-17 0-5 0-26 0-49-10 9-21 18-21 19-1 0-10 4-22 8l0 39c0 44 30 68 60 68 13 0 25-4 37-11l168-103c22-13 34-33 34-56 0-23-13-44-35-58z"></path></symbol><symbol id="olymp-music-no-sound-icon" viewBox="0 0 512 512"><path d="m20 195c-11 0-20 7-20 17l0 88c0 10 9 17 20 17 12 0 21-7 21-17l0-88c0-10-9-17-21-17z m205 41l41 0 0 40-41 0z m0 81l0 50-102-59 0-103 102-60 0 50 41 0c0-40 0-70 0-70 0-21-9-33-23-33-6 0-12 2-20 6l-141 83 0 150 141 83c8 4 14 6 20 6 14 0 23-12 23-32 0 0 0-31 0-71 0 0 0 0 0 0z m258-148l-58 58-58-58-29 29 58 58-58 58 29 29 58-58 58 58 29-29-58-58 58-58z"></path></symbol><symbol id="olymp-music-open-playlist-icon" viewBox="0 0 512 512"><path d="m0 114l57 0 0-57-57 0z m114-57l0 57 398 0 0-57z m-114 227l57 0 0-56-57 0z m114 0l398 0 0-56-398 0z m-114 171l57 0 0-57-57 0z m114 0l398 0 0-57-398 0z"></path></symbol><symbol id="olymp-music-pause-icon" viewBox="0 0 512 512"><path d="m373 43c18 0 32 14 32 32l0 362c0 18-14 32-32 32-17 0-32-14-32-32l0-362c0-18 15-32 32-32m0-43c-41 0-74 33-74 75l0 362c0 42 33 75 74 75 42 0 75-33 75-75l0-362c0-42-33-75-75-75z m-234 0c-42 0-75 33-75 75l0 181 43 0 0-181c0-18 14-32 32-32 17 0 32 14 32 32l0 362c0 18-15 32-32 32-18 0-32-14-32-32l0-53-43 0 0 53c0 42 33 75 75 75 41 0 74-33 74-75l0-362c0-42-33-75-74-75z m-75 299l43 0 0 42-43 0z"></path></symbol><symbol id="olymp-music-play-icon-big" viewBox="0 0 512 512"><path d="m422 183l-254-167c-16-11-33-16-49-16-35 0-72 28-72 88l0 336c0 60 37 88 72 88 16 0 33-5 48-16l257-167c26-17 41-44 41-72 0-29-15-56-43-74z m-24 107l-256 167c-8 6-16 8-23 8-16 0-26-15-26-41 0 0 0-10 0-28 0 0 0 0 0 0l-46 0 0-140 46 0c0-84 0-168 0-168 0-26 10-41 26-41 7 0 15 2 23 8l255 167c28 19 30 49 1 68z m-351 13l46 0 0 46-46 0z"></path></symbol><symbol id="olymp-music-previous-song-icon" viewBox="0 0 512 512"><path d="m248 199l167-102c12-7 25-11 37-11 30 0 60 23 60 68l0 59-43 0c0-51 0-59 0-59 0-16-6-26-17-26-4 0-9 2-15 5l-166 103c-19 11-20 30-1 41l167 102c6 3 11 5 15 5 11 0 17-9 17-25 0 0 0-7 0-17 0 0 0-1 0-1l43 0 0 18c0 44-30 68-60 68-12 0-25-4-37-11l-168-103c-21-13-34-33-34-56 0-23 13-44 35-58z m221 57l43 0 0 43-43 0z m-434-57l167-102c12-7 24-11 37-11 30 0 60 23 60 68l0 44-43 29c0-6 0-11 0-13 0-52 0-60 0-60 0-16-7-26-17-26-5 0-10 2-15 5l-167 103c-19 11-19 30-1 41l168 102c5 3 10 5 15 5 10 0 17-9 17-25 0 0 0-7 0-17 0-5 0-26 0-49 10 9 21 18 21 19 1 0 11 4 22 8l0 39c0 44-30 68-60 68-13 0-25-4-37-11l-168-103c-22-13-34-33-34-56 0-23 13-44 35-58z"></path></symbol><symbol id="olymp-music-repeat-icon" viewBox="0 0 512 512"><path d="m384 149l-43 0 0 43 43 0c47 0 85 38 85 85 0 47-38 85-85 85l-85 0 0 43 85 0c71 0 128-57 128-128 0-71-57-128-128-128z m-171 256l43 0 0-43-43 0z m-43-256l-42 0c-71 0-128 57-128 128 0 71 57 128 128 128l43 0 0-43-43 0c-47 0-85-38-85-85 0-47 38-85 85-85l42 0 0 42 86-64-86-63z"></path></symbol><symbol id="olymp-music-shuffle-icon" viewBox="0 0 512 512"><path d="m293 165c-9 13-23 31-37 52 0-1 0-1 0-1-14-21-28-38-37-51-20-28-49-33-64-32l-66 0 0 45 70 0c1 0 14-1 24 13 11 15 28 40 46 65-18 25-35 50-46 65-10 14-23 13-24 13l-70 0 0 45 66 0c15 1 44-4 64-32 9-13 23-30 37-51 0 0 0 0 0-1 14 21 28 39 37 52 20 27 49 33 64 31l65 0 0-44-69 0c-1 0-14 0-24-13-11-15-28-40-46-65 18-25 35-50 46-65 10-13 23-13 24-13l69 0 0-44-65 0c-15-2-44 4-64 31z m-293 169l45 0 0 45-45 0z m423-245l0 133 89-67z m-423 45l45 0 0 44-45 0z m423 289l89-66-89-67z"></path></symbol><symbol id="olymp-music-sound-icon" viewBox="0 0 512 512"><path d="m20 197c-11 0-20 8-20 17l0 84c0 10 9 17 20 17 11 0 19-7 19-17l0-84c0-9-8-17-19-17z m197 39l39 0 0 40-39 0z m0 79l0 48-99-57 0-99 99-58 0 48 39 0c0-38 0-67 0-67 0-20-9-32-22-32-6 0-12 2-19 7l-136 79 0 145 136 79c7 4 13 6 19 6 13 0 22-12 22-32 0 0 0-29 0-67 0 0 0 0 0 0z m224 99c44-43 71-97 71-158 0-61-27-115-72-158l-20 22c38 36 63 83 63 136 0 52-24 100-63 136z m-57-44c35-30 55-69 55-114 0-44-21-84-54-114l-19 23c28 23 46 55 46 91 0 36-19 69-47 93z m-53-47c22-16 36-40 36-67 0-27-14-52-37-68l-15 25c14 11 24 26 24 43 0 19-6 36-22 46z"></path></symbol><symbol id="olymp-music-video-settings-icon" viewBox="0 0 512 512"><path d="m32 512l64 0 0-256-64 0z m64-512l-64 0 0 64 64 0z m-96 192l128 0 0-64-128 0z m480-192l-64 0 0 64 64 0z m-64 512l64 0 0-256-64 0z m-32-384l0 64 128 0 0-64z m-96-128l-64 0 0 256 64 0z m-64 512l64 0 0-64-64 0z m-32-128l128 0 0-64-128 0z"></path></symbol><symbol id="olymp-weather-cloudy-icon" viewBox="0 0 512 512"><path d="m248 384l31 0 0 31-31 0z m123-110c0-46-40-84-90-85-24-28-60-45-102-45-69 0-125 49-129 111-30 14-50 42-50 75 0 47 41 85 93 85l124 0 0-31-124 0c-34 0-62-24-62-54 0-19 12-38 32-47l17-8 1-18c3-46 46-82 98-82 31 0 60 12 79 34l8 11 14 0c34 1 60 24 60 54l0 21 20 8c18 6 29 22 29 38 0 36-27 43-49 43l-30 0 0 31 30 0c44 0 80-24 80-74 0-30-20-56-49-67z m91-66c-4-62-60-111-129-111-42 0-78 18-102 45-18 1-36 6-50 15 15 1 36 5 57 16l8 0 8-10c19-22 48-35 79-35 52 0 95 36 98 83l1 18 17 7c20 10 32 28 32 48 0 29-28 53-62 53l-14 0c1 8 1 19-5 32l19 0c52 0 93-38 93-85 0-33-20-61-50-76z"></path></symbol><symbol id="olymp-weather-partly-sunny-icon" viewBox="0 0 512 512"><path d="m256 444l32 0 0 32-32 0z m126-114c0-47-41-86-92-87-25-29-63-47-105-47-71 0-129 51-134 115-30 15-51 44-51 77 0 49 43 88 96 88l128 0 0-32-128 0c-35 0-64-25-64-56 0-20 13-38 33-48l17-8 1-19c3-48 48-85 102-85 32 0 61 13 81 36l9 10 14 1c34 1 61 25 61 55l0 22 21 8c18 7 30 23 30 40 0 36-27 44-51 44l-30 0 0 32 30 0c46 0 83-25 83-76 0-31-21-58-51-70z m-86-198c-53 0-98 34-114 82l33 5c13-32 44-55 81-55 49 0 88 39 88 88 0 22-8 42-21 57l5 7c0 0 6 13 11 22 23-21 37-52 37-86 0-66-54-120-120-120z m0-96c-9 0-16 7-16 16l0 48c0 9 7 16 16 16 9 0 16-7 16-16l0-48c0-9-7-16-16-16z m200 200l-48 0c-9 0-16 7-16 16 0 9 7 16 16 16l48 0c9 0 16-7 16-16 0-9-7-16-16-16z m-47-137c-7-6-17-6-23 0l-34 34c-6 6-6 17 0 23 6 6 17 6 23 0l34-34c6-6 6-16 0-23z m-283 0c-6-6-16-6-23 0-6 7-6 17 0 23l34 34c6 6 17 6 23 0 6-6 6-17 0-23z"></path></symbol><symbol id="olymp-weather-rain-drops-icon" viewBox="0 0 512 512"><path d="m62 171c21 45 38 89 38 103 0 21-17 38-38 38-20 0-37-17-37-38 0-14 17-58 37-103m0-59c0 0-62 126-62 162 0 35 28 63 62 63 35 0 63-28 63-63 0-36-63-162-63-162z m388-40c20 47 37 91 37 104 0 20-17 36-37 36-21 0-38-16-38-36 0-13 17-57 38-104m0-60c0 0-63 130-63 164 0 34 28 61 63 61 34 0 62-27 62-61 0-34-62-164-62-164z m-175 242c31 62 62 135 62 158 0 35-28 63-62 63-35 0-63-28-63-63 0-23 32-96 63-158m0-54c0 0-87 164-87 212 0 48 39 88 87 88 48 0 87-40 87-88 0-48-87-212-87-212z"></path></symbol><symbol id="olymp-weather-rain-icon" viewBox="0 0 512 512"><path d="m199 43l38 0 0 38-38 0z m-57 294l-29 0c-41 0-75-30-75-66 0-24 15-46 39-58l20-9 2-23c2-38 27-70 62-87l0-41c-56 20-96 68-100 126-36 17-61 52-61 92 0 57 51 104 113 104l29 0z m311-135c0-56-49-102-110-104-17-20-41-36-68-46l0 41c15 7 29 17 40 30l11 13 16 0c41 1 73 30 73 66l0 26 25 9c21 8 36 27 36 48 0 43-33 52-61 52l-36 0 0 38 36 0c55 0 97-29 97-90 0-37-23-70-59-83z m-175 80c-5 9-2 21 7 26 9 5 21 2 26-7 5-9 12-58 12-58 0 0-40 30-45 39z m-97 47c-5 9-2 21 7 26 9 5 21 2 26-7 5-9 12-59 12-59 0 0-39 31-45 40z m49 82c-5 9-2 20 7 25 9 6 21 3 26-6 5-10 12-59 12-59 0 0-39 31-45 40z m86-29c-6 9-3 21 7 26 9 5 20 2 25-7 6-9 12-59 12-59 0 0-39 31-44 40z m-182 59c-6 9-3 21 7 26 9 5 20 2 25-7 6-9 12-59 12-59 0 0-39 31-44 40z"></path></symbol><symbol id="olymp-weather-snow-icon" viewBox="0 0 512 512"><path d="m199 28l38 0 0 38-38 0z m-57 294l-29 0c-41 0-75-29-75-65 0-24 15-46 39-58l20-10 2-22c2-38 27-70 62-87l0-42c-56 21-96 69-100 127-36 17-61 52-61 92 0 57 51 103 113 103l29 0z m311-135c0-56-49-101-110-103-17-21-41-37-68-46l0 41c15 7 29 17 40 30l11 12 16 1c41 1 73 30 73 65l0 27 25 9c21 8 36 27 36 47 0 43-33 52-61 52l-36 0 0 38 36 0c55 0 97-28 97-89 0-38-23-70-59-84z m-182 118l-11-11 24 0c6 0 10-4 10-10 0-5-4-9-10-9l-24 0 11-11c3-3 3-9 0-13-4-4-10-4-14 0l-10 11 0-25c0-5-5-9-10-9-5 0-9 4-9 9l0 25-11-11c-4-4-10-4-13 0-4 4-4 10 0 13l10 11-24 0c-6 0-10 4-10 9 0 6 4 10 10 10l24 0-10 11c-4 3-4 9 0 13 3 4 9 4 13 0l11-11 0 25c0 5 4 9 9 9 5 0 10-4 10-9l0-25 10 11c4 4 10 4 14 0 3-4 3-10 0-13z m81 76l-20 0 9-9c3-3 3-8 0-11-3-3-8-3-11 0l-9 9 0-21c0-4-4-8-8-8-4 0-8 4-8 8l0 21-9-9c-3-3-8-3-11 0-3 3-3 8 0 11l9 9-21 0c-4 0-8 3-8 8 0 4 4 8 8 8l21 0-9 9c-3 3-3 8 0 11 3 3 8 3 11 0l9-9 0 20c0 5 4 8 8 8 4 0 8-3 8-8l0-20 9 9c3 3 8 3 11 0 3-3 3-8 0-11l-9-9 20 0c5 0 8-4 8-8 0-5-3-8-8-8z m-132 47l-21 0 9-9c3-3 3-8 0-11-3-3-8-3-11 0l-9 9 0-20c0-5-3-8-8-8-4 0-8 3-8 8l0 20-9-9c-3-3-8-3-11 0-3 3-3 8 0 11l9 9-20 0c-5 0-8 4-8 8 0 5 3 8 8 8l20 0-9 9c-3 3-3 8 0 11 3 3 8 3 11 0l9-9 0 21c0 4 4 8 8 8 5 0 8-4 8-8l0-21 9 9c3 3 8 3 11 0 3-3 3-8 0-11l-9-9 21 0c4 0 8-3 8-8 0-4-4-8-8-8z"></path></symbol><symbol id="olymp-weather-storm-icon" viewBox="0 0 512 512"><path d="m199 28l38 0 0 38-38 0z m-57 294l-29 0c-41 0-75-29-75-65 0-24 15-46 39-58l20-10 2-22c2-38 27-70 62-87l0-42c-56 21-96 69-100 127-36 17-61 52-61 92 0 57 51 103 113 103l29 0z m311-135c0-56-49-101-110-103-17-21-41-37-68-46l0 41c15 7 29 17 40 30l11 12 16 1c41 1 73 30 73 65l0 27 25 9c21 8 36 27 36 47 0 43-33 52-61 52l-36 0 0 38 36 0c55 0 97-28 97-89 0-38-23-70-59-84z m-148 96l-31 0 28-74-92 96 31 0-27 74z m48 87l-25 0 22-57-73 75 25 0-22 58z m-155-19l-73 75 25 0-22 58 73-76-25 0z"></path></symbol><symbol id="olymp-weather-strong-winds-icon" viewBox="0 0 512 512"><path d="m451 308c-33 0-416 0-416 0l0 35 416 0c15 0 26 11 26 26 0 14-11 26-26 26-10 0-18-6-23-14 0 0 0 0 0 0l-31 16c11 19 31 33 54 33 34 0 61-28 61-61 0-34-27-61-61-61z m0-226c-23 0-43 14-54 33l31 16c0 0 0 0 0 0 5-8 13-14 23-14 15 0 26 12 26 26 0 15-11 26-26 26l-382 0 0 35c95 0 355 0 382 0 34 0 61-27 61-61 0-33-27-61-61-61z m-451 191l69 0 0-34-69 0z m104 0l35 0 0-34-35 0z m70-34l0 34 295 0 0-34z"></path></symbol><symbol id="olymp-weather-sunny-icon" viewBox="0 0 512 512"><path d="m256 114c-58 0-108 35-130 85l43 0c18-28 50-47 87-47 58 0 104 46 104 104 0 58-46 104-104 104-37 0-69-19-87-47l-43 0c22 50 72 85 130 85 79 0 142-63 142-142 0-79-63-142-142-142z m-104 161l0-38-38 0 0 38z m104-180c10 0 19-9 19-19l0-57c0-11-9-19-19-19-10 0-19 8-19 19l0 57c0 10 9 19 19 19z m0 322c-10 0-19 9-19 19l0 57c0 11 9 19 19 19 10 0 19-8 19-19l0-57c0-10-9-19-19-19z m237-180l-57 0c-10 0-19 9-19 19 0 10 9 19 19 19l57 0c11 0 19-9 19-19 0-10-8-19-19-19z m-398 19c0-10-9-19-19-19l-57 0c-11 0-19 9-19 19 0 10 8 19 19 19l57 0c10 0 19-9 19-19z m302-114l40-40c7-8 7-20 0-27-7-7-19-7-27 0l-40 40c-7 8-7 20 0 27 7 7 19 7 27 0z m-282 228l-40 40c-7 8-7 20 0 27 7 7 19 7 27 0l40-40c7-8 7-20 0-27-7-7-19-7-27 0z m282 0c-8-7-20-7-27 0-7 7-7 19 0 27l40 40c8 7 20 7 27 0 7-7 7-19 0-27z m-282-228c8 7 20 7 27 0 7-7 7-19 0-27l-40-40c-8-7-20-7-27 0-7 7-7 19 0 27z"></path></symbol><symbol id="olymp-weather-thermometer-icon" viewBox="0 0 512 512"><path d="m146 402c0 48 31 89 73 104l0-27c-28-13-48-43-48-77 0-38 25-71 61-81l0-26c-49 11-86 55-86 107z m134-107l0 26c36 10 61 43 61 81 0 34-20 64-48 77l0 27c42-15 73-56 73-104 0-52-37-96-86-107z m0-222l-24 0 0 25 24 0z m0 49l-24 0 0 24 24 0z m0 49l-24 0 0 24 24 0z m0 48l-24 0 0 25 24 0z m-36 293l24 0 0-24-24 0z m-12-197l0-266c0-14 11-25 24-25 14 0 25 11 25 25l0 266 24 0c0-114 0-266 0-266 0-27-22-49-49-49-27 0-49 22-49 49 0 0 0 152 0 266z"></path></symbol><symbol id="olymp-weather-wind-icon-header" viewBox="0 0 512 512"><path d="m436 289c-42 0-403 0-403 0l0 21 403 0c30 0 54 25 54 55 0 30-24 54-54 54-21 0-39-11-49-29 0-1 0-1 0-1l-19 11c13 24 38 41 68 41 42 0 76-34 76-76 0-42-34-76-76-76z m0-218c-30 0-55 17-68 41l19 11c0 0 0 0 0-1 10-18 28-29 49-29 30 0 54 24 54 54 0 30-24 55-54 55l-360 0 0 21c98 0 327 0 360 0 42 0 76-34 76-76 0-42-34-76-76-76z m-283 196l21 0 0-22-21 0z m43-22l0 22 207 0 0-22z m-196 22l131 0 0-22-131 0z"></path></symbol></svg>`;
  function SvgSprite() {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        "aria-hidden": true,
        style: { position: "absolute", width: 0, height: 0, overflow: "hidden" },
        dangerouslySetInnerHTML: { __html: SPRITE }
      }
    );
  }

  // frontend/.ds-shims/ds-provider.tsx
  globalThis.process ?? (globalThis.process = {
    env: {}
  });
  function DSProvider({ children }) {
    return React2.createElement(
      React2.Fragment,
      null,
      React2.createElement(SvgSprite, null),
      children
    );
  }

  // ds-bundle/.pkg-entry.mjs
  var pkg_entry_exports = {};
  __export(pkg_entry_exports, {
    ActivityFeed: () => ActivityFeed,
    AddBook: () => AddBook,
    AudioPlayer: () => AudioPlayer,
    AuthForm: () => AuthForm,
    AuthLanding: () => AuthLanding,
    Avatar: () => Avatar,
    BadgeCard: () => BadgeCard,
    BirthdayCard: () => BirthdayCard,
    BlogCard: () => BlogCard,
    BtnPrimary: () => BtnPrimary,
    BtnSecondary: () => BtnSecondary,
    CalendarWidget: () => CalendarWidget,
    Card: () => Card,
    ChatResponsive: () => ChatResponsive,
    ChoseFromMyPhoto: () => ChoseFromMyPhoto,
    ComicIndexView: () => ComicIndexView,
    CommentForm: () => CommentForm,
    CommentItem: () => CommentItem,
    CommentThread: () => CommentThread,
    Composer: () => Composer,
    ControlBlockButtons: () => ControlBlockButtons,
    EventItem: () => EventItem,
    Field: () => Field,
    FormField: () => FormField,
    FriendCard: () => FriendCard,
    FriendRequestItem: () => FriendRequestItem,
    FriendRequestsMenu: () => FriendRequestsMenu,
    FriendSuggestions: () => FriendSuggestions,
    GoToTop: () => GoToTop,
    HelloPreloader: () => HelloPreloader,
    HomeView: () => HomeView,
    Icon: () => Icon,
    LoginView: () => LoginView,
    MasterBase: () => MasterBase,
    MasterPublic: () => MasterPublic,
    MessagesMenu: () => MessagesMenu,
    Modal: () => Modal,
    NotificationsMenu: () => NotificationsMenu,
    NovelDetailView: () => NovelDetailView,
    OLYMP_VIEWBOX: () => OLYMP_VIEWBOX,
    PagesWidget: () => PagesWidget,
    PersonalInfoWidget: () => PersonalInfoWidget,
    PlaylistWidget: () => PlaylistWidget,
    Post: () => Post,
    PostControlButtons: () => PostControlButtons,
    ProfileHeader: () => ProfileHeader,
    ReactionBar: () => ReactionBar,
    RegisterView: () => RegisterView,
    SelectField: () => SelectField,
    SessionKeeper: () => SessionKeeper,
    SidebarCenter: () => SidebarCenter,
    SidebarLeft: () => SidebarLeft,
    SidebarRight: () => SidebarRight,
    SvgSprite: () => SvgSprite,
    TagSelect: () => TagSelect,
    ToggleRow: () => ToggleRow,
    TopMenu: () => TopMenu,
    TrackItem: () => TrackItem,
    UpdateHeaderPhoto: () => UpdateHeaderPhoto,
    UploadStudio: () => UploadStudio,
    WeatherWidget: () => WeatherWidget,
    WidgetCard: () => WidgetCard,
    inputCls: () => inputCls,
    inputStyle: () => inputStyle
  });
  init_define_import_meta_env();

  // frontend/src/templates/v1/components/blog/BlogCard.tsx
  init_define_import_meta_env();

  // frontend/src/templates/v1/components/ui/Avatar.tsx
  init_define_import_meta_env();
  function Avatar({
    name,
    size: size2 = 40,
    className = "",
    status
  }) {
    const initials = name.trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase();
    const hue = [...name].reduce((a, c) => a + c.charCodeAt(0), 0) % 360;
    const dot = Math.max(8, Math.round(size2 * 0.26));
    return /* @__PURE__ */ React.createElement(
      "span",
      {
        className: `relative inline-grid shrink-0 place-items-center rounded-full font-semibold text-white ${className}`,
        style: {
          width: size2,
          height: size2,
          fontSize: size2 * 0.38,
          background: `linear-gradient(135deg, hsl(${hue} 68% 58%), hsl(${(hue + 40) % 360} 68% 46%))`
        },
        "aria-hidden": true
      },
      initials,
      status && /* @__PURE__ */ React.createElement(
        "span",
        {
          className: "absolute rounded-full",
          title: status,
          style: {
            top: 0,
            left: 0,
            width: dot,
            height: dot,
            background: `var(--tpl-status-${status})`,
            boxShadow: "0 0 0 2px #fff"
          }
        }
      )
    );
  }

  // frontend/src/templates/v1/components/ui/Icon.tsx
  init_define_import_meta_env();
  function Icon({
    name,
    size: size2 = 20,
    className,
    style
  }) {
    const vb = OLYMP_VIEWBOX[name] ?? "0 0 32 32";
    const parts = vb.split(/\s+/).map(Number);
    const w = parts[2] || 1;
    const h = parts[3] || 1;
    const width = Math.round(w / h * size2);
    return /* @__PURE__ */ React.createElement(
      "svg",
      {
        width,
        height: size2,
        viewBox: vb,
        fill: "currentColor",
        "aria-hidden": true,
        className,
        style
      },
      /* @__PURE__ */ React.createElement("use", { href: `#olymp-${name}` })
    );
  }

  // frontend/src/templates/v1/components/widget/WidgetCard.tsx
  init_define_import_meta_env();
  function Card({
    children,
    className = "",
    as: Tag = "div"
  }) {
    return /* @__PURE__ */ React.createElement(
      Tag,
      {
        className: `rounded-xl shadow-sm ${className}`,
        style: { background: "var(--tpl-surface)", border: "1px solid var(--tpl-border)" }
      },
      children
    );
  }
  function WidgetCard({
    title,
    more,
    className = "",
    children
  }) {
    return /* @__PURE__ */ React.createElement(Card, { className: `p-4 ${className}` }, title && /* @__PURE__ */ React.createElement("div", { className: "mb-3 flex items-center justify-between" }, /* @__PURE__ */ React.createElement("h3", { className: "text-sm font-bold", style: { color: "var(--tpl-heading)" } }, title), more && /* @__PURE__ */ React.createElement("span", { style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement(Icon, { name: "three-dots-icon", size: 12 }))), children);
  }

  // frontend/src/templates/v1/components/blog/BlogCard.tsx
  function seedGradient(seed) {
    const hue = [...seed].reduce((a, c) => a + c.charCodeAt(0), 0) % 360;
    return `linear-gradient(135deg, hsl(${hue} 70% 60%), hsl(${(hue + 45) % 360} 65% 45%))`;
  }
  function BlogCard({
    title,
    excerpt,
    author,
    date,
    category,
    categoryColor = "var(--tpl-blue)",
    likes = 0,
    comments = 0,
    cover
  }) {
    return /* @__PURE__ */ React.createElement(Card, { as: "article", className: "overflow-hidden" }, /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "relative aspect-[370/261] w-full",
        style: { background: seedGradient(cover ?? title) }
      },
      category && /* @__PURE__ */ React.createElement(
        "span",
        {
          className: "absolute left-4 top-4 rounded-md px-3 py-1 text-[11px] font-bold uppercase tracking-wide text-white shadow-sm",
          style: { background: categoryColor }
        },
        category
      )
    ), /* @__PURE__ */ React.createElement("div", { className: "p-5" }, /* @__PURE__ */ React.createElement("h3", { className: "text-lg font-bold leading-snug", style: { color: "var(--tpl-heading)" } }, title), /* @__PURE__ */ React.createElement("p", { className: "mt-2 text-sm leading-relaxed", style: { color: "var(--tpl-text)" } }, excerpt), /* @__PURE__ */ React.createElement("div", { className: "mt-4 flex items-center gap-2 text-xs", style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement(Avatar, { name: author, size: 28 }), /* @__PURE__ */ React.createElement("span", null, "by", " ", /* @__PURE__ */ React.createElement("b", { style: { color: "var(--tpl-heading)" } }, author)), /* @__PURE__ */ React.createElement("span", { "aria-hidden": true }, "\xB7"), /* @__PURE__ */ React.createElement("time", null, date)), /* @__PURE__ */ React.createElement(
      "footer",
      {
        className: "mt-4 flex items-center gap-5 border-t pt-3 text-sm",
        style: { borderColor: "var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement("span", { className: "inline-flex items-center gap-1.5", style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement(Icon, { name: "heart-icon", size: 16 }), /* @__PURE__ */ React.createElement("span", { className: "text-xs" }, likes)),
      /* @__PURE__ */ React.createElement("span", { className: "inline-flex items-center gap-1.5", style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement(Icon, { name: "speech-balloon-icon", size: 16 }), /* @__PURE__ */ React.createElement("span", { className: "text-xs" }, comments))
    )));
  }

  // frontend/src/templates/v1/components/comment/CommentForm.tsx
  init_define_import_meta_env();
  var import_react = __toESM(require_react_shim());
  function CommentForm({ displayName, onSubmit, className = "" }) {
    const [text, setText] = (0, import_react.useState)("");
    function submit(e2) {
      e2.preventDefault();
      const t2 = text.trim();
      if (!t2) return;
      onSubmit(t2);
      setText("");
    }
    return /* @__PURE__ */ React.createElement("form", { onSubmit: submit, className: `flex items-start gap-3 ${className}` }, /* @__PURE__ */ React.createElement(Avatar, { name: displayName, size: 36 }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "relative rounded-md border",
        style: { borderColor: "var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement(
        "textarea",
        {
          value: text,
          onChange: (e2) => setText(e2.target.value),
          rows: 2,
          placeholder: "Add a comment...",
          className: "min-h-[2.75rem] w-full resize-none border-0 bg-transparent py-2 pl-3 pr-10 text-sm outline-none placeholder:text-[var(--tpl-muted)]",
          style: { color: "var(--tpl-text)" }
        }
      ),
      /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          title: "Add photo",
          "aria-label": "Add photo",
          className: "absolute right-2 top-2 grid h-7 w-7 place-items-center rounded-md transition hover:bg-[var(--tpl-surface-2)]",
          style: { color: "var(--tpl-muted)" }
        },
        /* @__PURE__ */ React.createElement(Icon, { name: "camera-icon", size: 16 })
      )
    ), /* @__PURE__ */ React.createElement("div", { className: "mt-2 flex items-center gap-2" }, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "submit",
        disabled: !text.trim(),
        className: "rounded-md px-4 py-2 text-sm font-semibold text-white transition hover:opacity-90 disabled:opacity-50",
        style: { background: "linear-gradient(135deg, var(--tpl-accent), var(--tpl-accent-2))" }
      },
      "Post Comment"
    ), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: () => setText(""),
        className: "rounded-md border px-4 py-2 text-sm font-semibold transition hover:bg-[var(--tpl-surface-2)]",
        style: { borderColor: "var(--tpl-border)", color: "var(--tpl-muted)" }
      },
      "Cancel"
    ))));
  }

  // frontend/src/templates/v1/components/comment/CommentItem.tsx
  init_define_import_meta_env();
  function CommentItem({ author, time, text, likes = 0, replies }) {
    const hasChildren = !!replies && replies.length > 0;
    return /* @__PURE__ */ React.createElement("li", { className: hasChildren ? "comment-item has-children" : "comment-item" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-3" }, /* @__PURE__ */ React.createElement(Avatar, { name: author, size: 36 }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, author), /* @__PURE__ */ React.createElement("time", { className: "text-xs", style: { color: "var(--tpl-muted)" } }, time)), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "ml-auto text-[var(--tpl-muted)]",
        "aria-label": "Comment options"
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "three-dots-icon", size: 16 })
    )), /* @__PURE__ */ React.createElement("p", { className: "mt-2 text-sm leading-relaxed", style: { color: "var(--tpl-text)" } }, text), /* @__PURE__ */ React.createElement("div", { className: "mt-2 flex items-center gap-4 text-xs" }, /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "inline-flex items-center gap-1.5",
        style: { color: "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "heart-icon", size: 14 }),
      /* @__PURE__ */ React.createElement("span", null, likes)
    ), /* @__PURE__ */ React.createElement(
      "a",
      {
        href: "#",
        className: "font-medium hover:underline",
        style: { color: "var(--tpl-accent)" }
      },
      "Reply"
    )), hasChildren && /* @__PURE__ */ React.createElement("ul", { className: "children mt-4 space-y-4 pl-6" }, replies.map((r2, i) => /* @__PURE__ */ React.createElement(
      CommentItem,
      {
        key: `${r2.author}-${i}`,
        author: r2.author,
        time: r2.time,
        text: r2.text,
        likes: r2.likes
      }
    ))));
  }

  // frontend/src/templates/v1/components/comment/CommentThread.tsx
  init_define_import_meta_env();
  function CommentThread({ comments, className = "" }) {
    return /* @__PURE__ */ React.createElement("div", { className }, /* @__PURE__ */ React.createElement("ul", { className: "comments-list space-y-5" }, comments.map((c, i) => /* @__PURE__ */ React.createElement(
      CommentItem,
      {
        key: `${c.author}-${i}`,
        author: c.author,
        time: c.time,
        text: c.text,
        likes: c.likes,
        replies: c.replies
      }
    ))), /* @__PURE__ */ React.createElement(
      "a",
      {
        href: "#",
        className: "more-comments mt-4 inline-flex items-center gap-1.5 text-xs font-semibold hover:underline",
        style: { color: "var(--tpl-muted)" }
      },
      "View more comments",
      /* @__PURE__ */ React.createElement(Icon, { name: "plus-icon", size: 10 })
    ));
  }

  // frontend/src/templates/v1/components/composer/Composer.tsx
  init_define_import_meta_env();
  var import_react2 = __toESM(require_react_shim());
  var TABS = [
    { key: "status", label: "Status", icon: "status-icon" },
    { key: "media", label: "Multimedia", icon: "multimedia-icon" },
    { key: "blog", label: "Blog Post", icon: "blog-icon" }
  ];
  function Composer({ displayName, onPost, className = "" }) {
    const [tab, setTab] = (0, import_react2.useState)("status");
    const [text, setText] = (0, import_react2.useState)("");
    function submit(e2) {
      e2.preventDefault();
      const t2 = text.trim();
      if (!t2) return;
      onPost(t2);
      setText("");
    }
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `overflow-hidden rounded-xl shadow-sm ${className}`,
        style: { background: "var(--tpl-surface)", border: "1px solid var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex border-b", style: { borderColor: "var(--tpl-border)" } }, TABS.map((t2) => /* @__PURE__ */ React.createElement(
        "button",
        {
          key: t2.key,
          type: "button",
          onClick: () => setTab(t2.key),
          className: "flex items-center gap-2 px-5 py-3.5 text-sm font-semibold transition",
          style: tab === t2.key ? { color: "var(--tpl-accent)", boxShadow: "inset 0 -2px 0 var(--tpl-accent)" } : { color: "var(--tpl-muted)" }
        },
        /* @__PURE__ */ React.createElement(Icon, { name: t2.icon, size: 16 }),
        /* @__PURE__ */ React.createElement("span", { className: "hidden sm:inline" }, t2.label)
      ))),
      /* @__PURE__ */ React.createElement("form", { onSubmit: submit, className: "p-4" }, /* @__PURE__ */ React.createElement("div", { className: "flex gap-3" }, /* @__PURE__ */ React.createElement(Avatar, { name: displayName, size: 40 }), /* @__PURE__ */ React.createElement(
        "textarea",
        {
          value: text,
          onChange: (e2) => setText(e2.target.value),
          rows: 2,
          placeholder: "Share what you are thinking here...",
          className: "min-h-[3rem] w-full resize-none border-0 bg-transparent pt-2 text-sm outline-none placeholder:text-[var(--tpl-muted)]",
          style: { color: "var(--tpl-text)" }
        }
      )), /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "mt-2 flex items-center gap-1 border-t pt-3",
          style: { borderColor: "var(--tpl-border)" }
        },
        /* @__PURE__ */ React.createElement(IconBtn, { label: "Add photos", icon: "camera-icon" }),
        /* @__PURE__ */ React.createElement(IconBtn, { label: "Tag friends", icon: "computer-icon" }),
        /* @__PURE__ */ React.createElement(IconBtn, { label: "Add location", icon: "small-pin-icon" }),
        /* @__PURE__ */ React.createElement("div", { className: "ml-auto flex items-center gap-2" }, /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "button",
            className: "rounded-md border px-4 py-2 text-sm font-semibold transition hover:bg-[var(--tpl-surface-2)]",
            style: { borderColor: "var(--tpl-border)", color: "var(--tpl-muted)" }
          },
          "Preview"
        ), /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "submit",
            disabled: !text.trim(),
            className: "rounded-md px-4 py-2 text-sm font-semibold text-white transition hover:opacity-90 disabled:opacity-50",
            style: { background: "linear-gradient(135deg, var(--tpl-accent), var(--tpl-accent-2))" }
          },
          "Post Status"
        ))
      ))
    );
  }
  function IconBtn({ label, icon }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        title: label,
        "aria-label": label,
        className: "grid h-9 w-9 place-items-center rounded-lg transition hover:bg-[var(--tpl-surface-2)]",
        style: { color: "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: icon, size: 18 })
    );
  }

  // frontend/src/templates/v1/components/form/FormField.tsx
  init_define_import_meta_env();
  var import_react3 = __toESM(require_react_shim());
  function FormField({
    label,
    value,
    onChange,
    type = "text",
    placeholder,
    icon,
    disabled = false
  }) {
    const [focused, setFocused] = (0, import_react3.useState)(false);
    const [internal, setInternal] = (0, import_react3.useState)(value ?? "");
    const controlled = value !== void 0;
    const current = controlled ? value : internal;
    const filled = (current ?? "").length > 0;
    const floated = focused || filled || type === "date";
    function handleChange(e2) {
      if (!controlled) setInternal(e2.target.value);
      onChange?.(e2.target.value);
    }
    const labelStyle = floated ? {
      transform: "translateY(-1.25rem) scale(0.8)",
      color: focused ? "var(--tpl-accent)" : "var(--tpl-muted)"
    } : { transform: "translateY(0) scale(1)", color: "var(--tpl-muted)" };
    return /* @__PURE__ */ React.createElement("div", { className: "relative pt-5", style: { opacity: disabled ? 0.55 : 1 } }, /* @__PURE__ */ React.createElement(
      "label",
      {
        className: "pointer-events-none absolute left-0 top-5 origin-left text-sm transition-all duration-200 ease-out",
        style: labelStyle
      },
      label
    ), /* @__PURE__ */ React.createElement(
      "input",
      {
        type,
        value: current,
        onChange: handleChange,
        onFocus: () => setFocused(true),
        onBlur: () => setFocused(false),
        disabled,
        placeholder: floated ? placeholder : void 0,
        className: "w-full bg-transparent pb-1.5 pr-7 text-sm outline-none disabled:cursor-not-allowed",
        style: { color: "var(--tpl-text)" }
      }
    ), /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "absolute bottom-0 left-0 h-px w-full",
        style: { background: "var(--tpl-border)" }
      }
    ), /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "absolute bottom-0 left-0 h-0.5 w-full origin-left transition-transform duration-200 ease-out",
        style: { background: "var(--tpl-accent)", transform: focused ? "scaleX(1)" : "scaleX(0)" }
      }
    ), icon && /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "pointer-events-none absolute bottom-1.5 right-0",
        style: { color: focused ? "var(--tpl-accent)" : "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: icon, size: 16 })
    ));
  }

  // frontend/src/templates/v1/components/form/SelectField.tsx
  init_define_import_meta_env();
  var import_react4 = __toESM(require_react_shim());
  function SelectField({
    label,
    options,
    value,
    onChange,
    disabled = false
  }) {
    const [focused, setFocused] = (0, import_react4.useState)(false);
    const normalized = options.map(
      (o2) => typeof o2 === "string" ? { value: o2, label: o2 } : o2
    );
    const labelStyle = {
      transform: "translateY(-1.25rem) scale(0.8)",
      color: focused ? "var(--tpl-accent)" : "var(--tpl-muted)"
    };
    return /* @__PURE__ */ React.createElement("div", { className: "relative pt-5", style: { opacity: disabled ? 0.55 : 1 } }, /* @__PURE__ */ React.createElement(
      "label",
      {
        className: "pointer-events-none absolute left-0 top-5 origin-left text-sm transition-all duration-200 ease-out",
        style: labelStyle
      },
      label
    ), /* @__PURE__ */ React.createElement(
      "select",
      {
        value,
        onChange: (e2) => onChange(e2.target.value),
        onFocus: () => setFocused(true),
        onBlur: () => setFocused(false),
        disabled,
        className: "w-full appearance-none bg-transparent pb-1.5 pr-7 text-sm outline-none disabled:cursor-not-allowed",
        style: { color: "var(--tpl-text)" }
      },
      normalized.map((o2) => /* @__PURE__ */ React.createElement("option", { key: o2.value, value: o2.value }, o2.label))
    ), /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "absolute bottom-0 left-0 h-px w-full",
        style: { background: "var(--tpl-border)" }
      }
    ), /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "absolute bottom-0 left-0 h-0.5 w-full origin-left transition-transform duration-200 ease-out",
        style: { background: "var(--tpl-accent)", transform: focused ? "scaleX(1)" : "scaleX(0)" }
      }
    ), /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "pointer-events-none absolute bottom-2 right-0",
        style: { color: focused ? "var(--tpl-accent)" : "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "dropdown-arrow-icon", size: 11 })
    ));
  }

  // frontend/src/templates/v1/components/form/TagSelect.tsx
  init_define_import_meta_env();
  var import_react5 = __toESM(require_react_shim());
  function TagSelect({
    label,
    values,
    onChange,
    suggestions = [],
    placeholder
  }) {
    const [draft, setDraft] = (0, import_react5.useState)("");
    const [focused, setFocused] = (0, import_react5.useState)(false);
    function addTag(raw) {
      const tag = raw.trim();
      setDraft("");
      if (!tag) return;
      if (values.some((v) => v.toLowerCase() === tag.toLowerCase())) return;
      onChange([...values, tag]);
    }
    function removeTag(tag) {
      onChange(values.filter((v) => v !== tag));
    }
    function onKeyDown(e2) {
      if (e2.key === "Enter" || e2.key === ",") {
        e2.preventDefault();
        addTag(draft);
      } else if (e2.key === "Backspace" && draft === "") {
        const last = values[values.length - 1];
        if (last !== void 0) removeTag(last);
      }
    }
    const available = suggestions.filter(
      (s) => !values.some((v) => v.toLowerCase() === s.toLowerCase()) && (draft === "" || s.toLowerCase().includes(draft.toLowerCase()))
    );
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "mb-1.5 block text-xs font-semibold uppercase tracking-wide",
        style: { color: "var(--tpl-muted)" }
      },
      label
    ), /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "flex flex-wrap items-center gap-2 rounded-lg border px-2.5 py-2 transition",
        style: { borderColor: focused ? "var(--tpl-accent)" : "var(--tpl-border)" }
      },
      values.map((tag) => /* @__PURE__ */ React.createElement(
        "span",
        {
          key: tag,
          className: "inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-medium",
          style: { background: "var(--tpl-surface-2)", color: "var(--tpl-text)" }
        },
        tag,
        /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "button",
            onClick: () => removeTag(tag),
            "aria-label": `Remove ${tag}`,
            className: "inline-flex transition hover:text-[var(--tpl-accent)]",
            style: { color: "var(--tpl-muted)" }
          },
          /* @__PURE__ */ React.createElement(Icon, { name: "little-delete", size: 8 })
        )
      )),
      /* @__PURE__ */ React.createElement(
        "input",
        {
          value: draft,
          onChange: (e2) => setDraft(e2.target.value),
          onKeyDown,
          onFocus: () => setFocused(true),
          onBlur: () => setFocused(false),
          placeholder: values.length === 0 ? placeholder : void 0,
          className: "min-w-[6rem] flex-1 bg-transparent py-0.5 text-sm outline-none",
          style: { color: "var(--tpl-text)" }
        }
      )
    ), focused && available.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "mt-1.5 flex flex-wrap gap-1.5" }, available.slice(0, 8).map((s) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: s,
        type: "button",
        onMouseDown: (e2) => {
          e2.preventDefault();
          addTag(s);
        },
        className: "rounded-full border px-2.5 py-1 text-xs transition hover:border-[var(--tpl-accent)] hover:text-[var(--tpl-accent)]",
        style: { borderColor: "var(--tpl-border)", color: "var(--tpl-muted)" }
      },
      "+ ",
      s
    ))));
  }

  // frontend/src/templates/v1/components/form/ToggleRow.tsx
  init_define_import_meta_env();
  function ToggleRow({
    title,
    description,
    checked,
    onChange
  }) {
    return /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between gap-4 py-3" }, /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("div", { className: "text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, title), description && /* @__PURE__ */ React.createElement("p", { className: "mt-0.5 text-xs leading-relaxed", style: { color: "var(--tpl-muted)" } }, description)), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        role: "switch",
        "aria-checked": checked,
        "aria-label": title,
        onClick: () => onChange(!checked),
        className: "relative inline-flex h-6 w-11 shrink-0 cursor-pointer items-center rounded-full transition-colors duration-200",
        style: {
          background: checked ? "var(--tpl-accent)" : "var(--tpl-surface-2)",
          border: `1px solid ${checked ? "var(--tpl-accent)" : "var(--tpl-border)"}`
        }
      },
      /* @__PURE__ */ React.createElement(
        "span",
        {
          className: "inline-block h-4 w-4 rounded-full bg-white shadow transition-transform duration-200 ease-out",
          style: { transform: checked ? "translateX(1.5rem)" : "translateX(0.25rem)" }
        }
      )
    ));
  }

  // frontend/src/templates/v1/components/headers/NotifMenus.tsx
  init_define_import_meta_env();
  var import_react6 = __toESM(require_react_shim());
  function Trigger({
    icon,
    tone,
    badge,
    label,
    open,
    onToggle
  }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: onToggle,
        "aria-label": label,
        "aria-expanded": open,
        className: `relative grid h-9 w-9 place-items-center rounded-full transition ${open ? "bg-white/15 text-white" : "text-white/75 hover:bg-white/10 hover:text-white"}`
      },
      /* @__PURE__ */ React.createElement(Icon, { name: icon, size: 18 }),
      badge > 0 && /* @__PURE__ */ React.createElement(
        "span",
        {
          className: "absolute -right-0.5 -top-0.5 grid h-4 min-w-4 place-items-center rounded-full px-1 text-[10px] font-bold text-white",
          style: { background: tone }
        },
        badge
      )
    );
  }
  function DropdownCard({
    title,
    actions: actions2,
    footer,
    children
  }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        role: "menu",
        className: "absolute right-0 top-full z-50 mt-3 w-[21rem] overflow-hidden rounded-xl border bg-white shadow-2xl",
        style: { borderColor: "var(--tpl-border)", color: "var(--tpl-text)" }
      },
      /* @__PURE__ */ React.createElement(
        "span",
        {
          className: "absolute -top-1.5 right-4 h-3 w-3 rotate-45 border-l border-t bg-white",
          style: { borderColor: "var(--tpl-border)" }
        }
      ),
      /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between border-b px-4 py-2.5", style: { borderColor: "var(--tpl-border)" } }, /* @__PURE__ */ React.createElement("span", { className: "text-[11px] font-bold uppercase tracking-wide", style: { color: "var(--tpl-muted)" } }, title), /* @__PURE__ */ React.createElement("span", { className: "flex gap-3" }, actions2)),
      /* @__PURE__ */ React.createElement("ul", { className: "max-h-[22rem] overflow-y-auto" }, children),
      /* @__PURE__ */ React.createElement(
        "a",
        {
          href: "#",
          className: "block py-3 text-center text-sm font-semibold text-white transition hover:opacity-95",
          style: { background: "var(--tpl-blue)" }
        },
        footer
      )
    );
  }
  function ActionLink({ children, onClick }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick,
        className: "text-[11px] font-bold uppercase tracking-wide transition hover:text-[var(--tpl-accent)]",
        style: { color: "var(--tpl-muted)" }
      },
      children
    );
  }
  function Empty({ children }) {
    return /* @__PURE__ */ React.createElement("li", { className: "px-4 py-8 text-center text-sm", style: { color: "var(--tpl-muted)" } }, children);
  }
  function InlineLink({ children, tone = "var(--tpl-accent)" }) {
    return /* @__PURE__ */ React.createElement("a", { href: "#", className: "font-semibold hover:underline", style: { color: tone } }, children);
  }
  var rowBorder = { borderColor: "var(--tpl-border)" };
  var FRIEND_REQUESTS = [
    { id: 1, name: "Tamara Romanoff", sub: "Mutual Friend: Sarah Hetfield" },
    { id: 2, name: "Tony Stevens", sub: "4 Friends in Common" },
    { id: 3, name: "Green Goo", sub: "8 Friends in Common" },
    { id: 4, name: "Mary Jane Stark", sub: "", info: true }
  ];
  function FriendRequestsMenu({ open, onToggle }) {
    const [items, setItems] = (0, import_react6.useState)(FRIEND_REQUESTS);
    const remove = (id2) => setItems((x) => x.filter((i) => i.id !== id2));
    return /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(Trigger, { icon: "happy-face-icon", tone: "var(--tpl-blue-2)", badge: items.length, label: "Friend requests", open, onToggle }), open && /* @__PURE__ */ React.createElement(
      DropdownCard,
      {
        title: "Friend Requests",
        actions: /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(ActionLink, null, "Settings"), /* @__PURE__ */ React.createElement(ActionLink, null, "Find Friends")),
        footer: "Check all your Events"
      },
      items.length === 0 && /* @__PURE__ */ React.createElement(Empty, null, "No new friend requests"),
      items.map(
        (r2) => r2.info ? /* @__PURE__ */ React.createElement("li", { key: r2.id, className: "flex items-center gap-3 border-b px-4 py-3", style: rowBorder }, /* @__PURE__ */ React.createElement(Avatar, { name: r2.name, size: 40 }), /* @__PURE__ */ React.createElement("p", { className: "min-w-0 flex-1 text-sm", style: { color: "var(--tpl-text)" } }, "You and ", /* @__PURE__ */ React.createElement("b", { style: { color: "var(--tpl-heading)" } }, r2.name), " just became friends. Write on", " ", /* @__PURE__ */ React.createElement(InlineLink, { tone: "var(--tpl-blue)" }, "her wall"), "."), /* @__PURE__ */ React.createElement(IconBtn2, { onClick: () => remove(r2.id), label: "Dismiss", icon: "happy-face-icon" })) : /* @__PURE__ */ React.createElement("li", { key: r2.id, className: "flex items-center gap-3 border-b px-4 py-3", style: rowBorder }, /* @__PURE__ */ React.createElement(Avatar, { name: r2.name, size: 40 }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, r2.name), /* @__PURE__ */ React.createElement("p", { className: "truncate text-xs", style: { color: "var(--tpl-muted)" } }, r2.sub)), /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "button",
            onClick: () => remove(r2.id),
            className: "grid h-8 w-8 shrink-0 place-items-center rounded-md text-white transition hover:opacity-90",
            style: { background: "var(--tpl-blue)" },
            "aria-label": `Accept ${r2.name}`
          },
          /* @__PURE__ */ React.createElement(Icon, { name: "happy-face-icon", size: 16 })
        ), /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "button",
            onClick: () => remove(r2.id),
            className: "grid h-8 w-8 shrink-0 place-items-center rounded-md transition hover:bg-black/5",
            style: { background: "var(--tpl-surface-2)", color: "var(--tpl-muted)" },
            "aria-label": `Decline ${r2.name}`
          },
          /* @__PURE__ */ React.createElement(Icon, { name: "little-delete", size: 12 })
        ))
      )
    ));
  }
  function IconBtn2({ onClick, label, icon }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick,
        className: "grid h-8 w-8 shrink-0 place-items-center rounded-md transition hover:bg-black/5",
        style: { background: "var(--tpl-surface-2)", color: "var(--tpl-muted)" },
        "aria-label": label
      },
      /* @__PURE__ */ React.createElement(Icon, { name: icon, size: 16 })
    );
  }
  var MESSAGES = [
    { id: 1, name: "Elaine Dreyfuss", preview: "Hi James! I just wanted to let you know we have to reschedule the meeting\u2026", time: "Yesterday at 9:56pm" },
    { id: 2, name: "Sarah Hetfield", preview: "Hey! Would you like to hang out this weekend?", time: "March 16th at 10:23am" }
  ];
  function MessagesMenu({ open, onToggle }) {
    const [msgs, setMsgs] = (0, import_react6.useState)(MESSAGES);
    const unread = msgs.filter((m) => !m.read).length;
    const markAll = () => setMsgs((x) => x.map((m) => ({ ...m, read: true })));
    const readOne = (id2) => setMsgs((x) => x.map((m) => m.id === id2 ? { ...m, read: true } : m));
    return /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(Trigger, { icon: "chat---messages-icon", tone: "#7c5ac2", badge: unread, label: "Messages", open, onToggle }), open && /* @__PURE__ */ React.createElement(
      DropdownCard,
      {
        title: "Messages",
        actions: /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(ActionLink, { onClick: markAll }, "Mark as read"), /* @__PURE__ */ React.createElement(ActionLink, null, "Settings")),
        footer: "View All Messages"
      },
      msgs.map((m) => /* @__PURE__ */ React.createElement(
        "li",
        {
          key: m.id,
          onClick: () => readOne(m.id),
          className: "flex cursor-pointer gap-3 border-b px-4 py-3 transition hover:bg-[var(--tpl-surface-2)]",
          style: { ...rowBorder, background: m.read ? void 0 : "rgba(56,169,255,0.06)" }
        },
        /* @__PURE__ */ React.createElement("span", { className: "relative shrink-0" }, /* @__PURE__ */ React.createElement(Avatar, { name: m.name, size: 42 }), !m.read && /* @__PURE__ */ React.createElement("span", { className: "absolute right-0 top-0 h-3 w-3 rounded-full border-2 border-white", style: { background: "var(--tpl-blue)" } })),
        /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, m.name), /* @__PURE__ */ React.createElement("p", { className: "truncate text-xs", style: { color: "var(--tpl-muted)" } }, m.preview), /* @__PURE__ */ React.createElement("p", { className: "mt-0.5 text-[11px]", style: { color: "var(--tpl-muted)" } }, m.time))
      ))
    ));
  }
  var NOTIFS = [
    { id: 1, who: "Mathilda Brinker", action: /* @__PURE__ */ React.createElement(React.Fragment, null, "commented on your ", /* @__PURE__ */ React.createElement(InlineLink, null, "photo"), "."), time: "2 mins ago", icon: "comments-post-icon" },
    { id: 2, who: "Nicholas Grissom", action: /* @__PURE__ */ React.createElement(React.Fragment, null, "liked your ", /* @__PURE__ */ React.createElement(InlineLink, null, "status update"), "."), time: "5 mins ago", icon: "like-post-icon" },
    { id: 3, who: "Sarah Hetfield", action: /* @__PURE__ */ React.createElement(React.Fragment, null, "started following you."), time: "12 mins ago", icon: "happy-face-icon" },
    { id: 4, who: "Jake Parker", action: /* @__PURE__ */ React.createElement(React.Fragment, null, "shared your ", /* @__PURE__ */ React.createElement(InlineLink, null, "post"), "."), time: "1 hour ago", icon: "share-post-icon" }
  ];
  function NotificationsMenu({ open, onToggle }) {
    const [items, setItems] = (0, import_react6.useState)(NOTIFS);
    const unread = items.filter((n2) => !n2.read).length;
    const markAll = () => setItems((x) => x.map((n2) => ({ ...n2, read: true })));
    return /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(Trigger, { icon: "thunder-icon", tone: "var(--tpl-accent)", badge: unread, label: "Notifications", open, onToggle }), open && /* @__PURE__ */ React.createElement(
      DropdownCard,
      {
        title: "Notifications",
        actions: /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(ActionLink, { onClick: markAll }, "Mark as read"), /* @__PURE__ */ React.createElement(ActionLink, null, "Settings")),
        footer: "Check all your Notifications"
      },
      items.map((n2) => /* @__PURE__ */ React.createElement(
        "li",
        {
          key: n2.id,
          className: "flex items-start gap-3 border-b px-4 py-3",
          style: { ...rowBorder, background: n2.read ? void 0 : "rgba(255,94,58,0.05)" }
        },
        /* @__PURE__ */ React.createElement(Avatar, { name: n2.who, size: 38 }),
        /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("p", { className: "text-sm leading-snug", style: { color: "var(--tpl-text)" } }, /* @__PURE__ */ React.createElement("b", { style: { color: "var(--tpl-heading)" } }, n2.who), " ", n2.action), /* @__PURE__ */ React.createElement("p", { className: "mt-0.5 text-[11px]", style: { color: "var(--tpl-muted)" } }, n2.time)),
        /* @__PURE__ */ React.createElement("span", { className: "mt-1 shrink-0", style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement(Icon, { name: n2.icon, size: 16 }))
      ))
    ));
  }

  // frontend/src/templates/v1/components/headers/TopMenu.tsx
  init_define_import_meta_env();
  var import_react7 = __toESM(require_react_shim());

  // frontend/node_modules/portal-frontend/.ds-shims/next-link.tsx
  init_define_import_meta_env();
  var React3 = __toESM(require_react_shim());
  function Link({
    href,
    children,
    ...rest
  }) {
    const to = typeof href === "string" ? href : href?.pathname ?? "#";
    return React3.createElement("a", { href: to, ...rest }, children);
  }

  // frontend/src/templates/v1/components/headers/TopMenu.tsx
  var API_BASE = process.env.NEXT_PUBLIC_API_BASE_URL ?? "https://api.portal.localhost";
  function TopMenu() {
    const [me, setMe] = (0, import_react7.useState)({ name: "Guest", role: "Member" });
    const [menu, setMenu] = (0, import_react7.useState)(null);
    const clusterRef = (0, import_react7.useRef)(null);
    (0, import_react7.useEffect)(() => {
      let alive = true;
      fetch(`${API_BASE}/api/v1/auth/me`, { credentials: "include" }).then((r2) => r2.ok ? r2.json() : null).then((d) => {
        if (alive && d?.display_name) setMe({ name: d.display_name, role: d.roles?.[0] ?? "Member" });
      }).catch(() => {
      });
      return () => {
        alive = false;
      };
    }, []);
    (0, import_react7.useEffect)(() => {
      if (!menu) return;
      const onDoc = (e2) => {
        if (clusterRef.current && !clusterRef.current.contains(e2.target)) setMenu(null);
      };
      const onEsc = (e2) => e2.key === "Escape" && setMenu(null);
      document.addEventListener("mousedown", onDoc);
      document.addEventListener("keydown", onEsc);
      return () => {
        document.removeEventListener("mousedown", onDoc);
        document.removeEventListener("keydown", onEsc);
      };
    }, [menu]);
    const toggle = (k) => setMenu((m) => m === k ? null : k);
    return /* @__PURE__ */ React.createElement("div", { className: "flex h-full items-stretch" }, /* @__PURE__ */ React.createElement(
      Link,
      {
        href: "/",
        className: "flex h-full w-[var(--tpl-rail-w)] shrink-0 items-center gap-3 overflow-hidden text-white transition-[width] duration-200 xl:w-[var(--tpl-sidebar-cur)]",
        style: { background: "linear-gradient(135deg, var(--tpl-accent), var(--tpl-accent-2))" },
        "aria-label": "Portal \u2014 Newsfeed"
      },
      /* @__PURE__ */ React.createElement("span", { className: "grid shrink-0 place-items-center", style: { width: "var(--tpl-rail-w)" } }, /* @__PURE__ */ React.createElement(Icon, { name: "thunder-icon", size: 26 })),
      /* @__PURE__ */ React.createElement("span", { className: "hidden whitespace-nowrap text-lg font-extrabold uppercase tracking-wide xl:block" }, "Portal")
    ), /* @__PURE__ */ React.createElement("div", { className: "flex flex-1 items-center gap-4 px-5" }, /* @__PURE__ */ React.createElement("span", { className: "hidden text-sm font-bold uppercase tracking-wide text-white/90 sm:block" }, "Newsfeed"), /* @__PURE__ */ React.createElement("label", { className: "relative hidden min-w-0 flex-1 items-center md:flex", style: { maxWidth: 380 } }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "search",
        placeholder: "Search here people or pages...",
        className: "w-full rounded-full py-2 pl-4 pr-9 text-sm text-white outline-none placeholder:text-white/45",
        style: { background: "var(--tpl-header-2)" }
      }
    ), /* @__PURE__ */ React.createElement("span", { className: "pointer-events-none absolute right-3 text-white/60" }, /* @__PURE__ */ React.createElement(Icon, { name: "magnifying-glass-icon", size: 16 }))), /* @__PURE__ */ React.createElement(Link, { href: "/", className: "hidden text-sm text-white/80 hover:text-white lg:block" }, "Find Friends"), /* @__PURE__ */ React.createElement("div", { ref: clusterRef, className: "ml-auto flex items-center gap-1.5" }, /* @__PURE__ */ React.createElement(FriendRequestsMenu, { open: menu === "requests", onToggle: () => toggle("requests") }), /* @__PURE__ */ React.createElement(MessagesMenu, { open: menu === "messages", onToggle: () => toggle("messages") }), /* @__PURE__ */ React.createElement(NotificationsMenu, { open: menu === "notifications", onToggle: () => toggle("notifications") }), /* @__PURE__ */ React.createElement(
      ProfileMenu,
      {
        name: me.name,
        role: me.role,
        open: menu === "profile",
        onToggle: () => toggle("profile")
      }
    ))));
  }
  var STATUSES = [
    { key: "online", label: "Online", varName: "--tpl-status-online" },
    { key: "away", label: "Away", varName: "--tpl-status-away" },
    { key: "disconnected", label: "Disconnected", varName: "--tpl-status-offline" },
    { key: "invisible", label: "Invisible", varName: "--tpl-status-invisible" }
  ];
  function ProfileMenu({
    name,
    role,
    open,
    onToggle
  }) {
    const [status, setStatus] = (0, import_react7.useState)("online");
    const [subtitle, setSubtitle] = (0, import_react7.useState)(role);
    const [draft, setDraft] = (0, import_react7.useState)(role);
    (0, import_react7.useEffect)(() => {
      setSubtitle(role);
      setDraft(role);
    }, [role]);
    async function logout() {
      try {
        await fetch(`${API_BASE}/api/v1/auth/refresh`, { method: "POST", credentials: "include" });
        await fetch(`${API_BASE}/api/v1/auth/logout`, { method: "POST", credentials: "include" });
      } catch {
      }
      try {
        localStorage.removeItem("portal_refresh_at");
      } catch {
      }
      window.location.assign("/login");
    }
    function applyCustom() {
      const v = draft.trim();
      if (v) setSubtitle(v);
    }
    return /* @__PURE__ */ React.createElement("div", { className: "relative ml-2" }, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: onToggle,
        "aria-expanded": open,
        "aria-haspopup": "menu",
        className: "flex items-center gap-2.5 rounded-lg px-1.5 py-1 hover:bg-white/5"
      },
      /* @__PURE__ */ React.createElement("span", { className: "hidden max-w-[10rem] text-right leading-tight sm:block" }, /* @__PURE__ */ React.createElement("span", { className: "block truncate text-sm font-semibold text-white" }, name), /* @__PURE__ */ React.createElement("span", { className: "block truncate text-[10px] uppercase tracking-wide text-white/50" }, subtitle)),
      /* @__PURE__ */ React.createElement(Avatar, { name, size: 36, status: status === "disconnected" ? "offline" : status }),
      /* @__PURE__ */ React.createElement("span", { className: `hidden text-white/50 transition-transform sm:block ${open ? "rotate-180" : ""}` }, /* @__PURE__ */ React.createElement(Icon, { name: "dropdown-arrow-icon", size: 12 }))
    ), open && /* @__PURE__ */ React.createElement(
      "div",
      {
        role: "menu",
        className: "absolute right-0 top-full z-50 mt-3 w-72 rounded-xl border bg-white shadow-2xl",
        style: { borderColor: "var(--tpl-border)", color: "var(--tpl-text)" }
      },
      /* @__PURE__ */ React.createElement(
        "span",
        {
          className: "absolute -top-1.5 right-6 h-3 w-3 rotate-45 border-l border-t bg-white",
          style: { borderColor: "var(--tpl-border)" }
        }
      ),
      /* @__PURE__ */ React.createElement(MenuHeader, null, "Your Account"),
      /* @__PURE__ */ React.createElement(MenuItem, { icon: "menu-icon", label: "Profile Settings", href: "/" }),
      /* @__PURE__ */ React.createElement(MenuItem, { icon: "star-icon", label: "Create Fav Page", href: "/" }),
      /* @__PURE__ */ React.createElement(MenuItem, { icon: "logout-icon", label: "Log Out", onClick: logout }),
      /* @__PURE__ */ React.createElement(Divider, null),
      /* @__PURE__ */ React.createElement(MenuHeader, null, "Chat Settings"),
      /* @__PURE__ */ React.createElement("div", { className: "pb-1" }, STATUSES.map((s) => {
        const active = status === s.key;
        return /* @__PURE__ */ React.createElement(
          "button",
          {
            key: s.key,
            type: "button",
            onClick: () => setStatus(s.key),
            className: "flex w-full items-center gap-3 px-4 py-1.5 text-left text-sm transition hover:bg-[var(--tpl-surface-2)]"
          },
          /* @__PURE__ */ React.createElement("span", { className: "h-2.5 w-2.5 rounded-full", style: { background: `var(${s.varName})` } }),
          /* @__PURE__ */ React.createElement("span", { className: active ? "font-semibold" : "", style: { color: active ? "var(--tpl-accent)" : "var(--tpl-text)" } }, s.label)
        );
      })),
      /* @__PURE__ */ React.createElement(Divider, null),
      /* @__PURE__ */ React.createElement(MenuHeader, null, "Custom Status"),
      /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 px-4 pb-3 pt-1" }, /* @__PURE__ */ React.createElement(
        "input",
        {
          value: draft,
          onChange: (e2) => setDraft(e2.target.value),
          onKeyDown: (e2) => e2.key === "Enter" && applyCustom(),
          placeholder: "What's on your mind?",
          className: "min-w-0 flex-1 rounded-md border bg-transparent px-3 py-1.5 text-sm outline-none focus:border-[var(--tpl-accent)]",
          style: { borderColor: "var(--tpl-border)" }
        }
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: applyCustom,
          className: "grid h-8 w-8 shrink-0 place-items-center rounded-md text-white",
          style: { background: "#7c5ac2" },
          "aria-label": "Save status"
        },
        /* @__PURE__ */ React.createElement(Icon, { name: "check-icon", size: 14 })
      )),
      /* @__PURE__ */ React.createElement(Divider, null),
      /* @__PURE__ */ React.createElement(MenuHeader, null, "About Portal"),
      /* @__PURE__ */ React.createElement("div", { className: "pb-2" }, ["Terms and Conditions", "FAQs", "Careers", "Contact"].map((t2) => /* @__PURE__ */ React.createElement(
        "a",
        {
          key: t2,
          href: "#",
          className: "block px-4 py-1.5 text-sm font-medium transition hover:text-[var(--tpl-accent)]",
          style: { color: "var(--tpl-heading)" }
        },
        t2
      )))
    ));
  }
  function MenuHeader({ children }) {
    return /* @__PURE__ */ React.createElement("p", { className: "px-4 pb-1 pt-3 text-[11px] font-bold uppercase tracking-wide", style: { color: "var(--tpl-muted)" } }, children);
  }
  function Divider() {
    return /* @__PURE__ */ React.createElement("div", { className: "my-1 h-px", style: { background: "var(--tpl-border)" } });
  }
  function MenuItem({
    icon,
    label,
    href,
    onClick
  }) {
    const cls = "flex w-full items-center gap-3 px-4 py-2.5 text-left text-sm font-semibold transition hover:bg-[var(--tpl-surface-2)]";
    const inner = /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement(Icon, { name: icon, size: 18 })), /* @__PURE__ */ React.createElement("span", { style: { color: "var(--tpl-heading)" } }, label));
    if (onClick) {
      return /* @__PURE__ */ React.createElement("button", { type: "button", onClick, className: cls }, inner);
    }
    return /* @__PURE__ */ React.createElement(Link, { href: href ?? "/", className: cls }, inner);
  }

  // frontend/src/templates/v1/components/menu/SidebarCenter.tsx
  init_define_import_meta_env();
  function SidebarCenter() {
    return /* @__PURE__ */ React.createElement(
      "header",
      {
        className: "fixed inset-x-0 top-0 z-40 shadow-sm",
        style: { height: "var(--tpl-header-h)", background: "var(--tpl-header)" }
      },
      /* @__PURE__ */ React.createElement(TopMenu, null)
    );
  }

  // frontend/src/templates/v1/components/menu/SidebarLeft.tsx
  init_define_import_meta_env();
  var ITEMS = [
    { icon: "newsfeed-icon", label: "Newsfeed", href: "/", active: true },
    { icon: "multimedia-icon", label: "Upload Video", href: "/upload" },
    { icon: "star-icon", label: "Fav Pages Feed", href: "/library/comic" },
    { icon: "happy-faces-icon", label: "Friend Groups" },
    { icon: "headphones-icon", label: "Music & Playlists", href: "/library/novel/1" },
    { icon: "weather-icon", label: "Weather App" },
    { icon: "calendar-icon", label: "Calendar and Events" },
    { icon: "badge-icon", label: "Community Badges" },
    { icon: "cupcake-icon", label: "Friends Birthdays" },
    { icon: "stats-icon", label: "Account Stats" },
    { icon: "manage-widgets-icon", label: "Manage Widgets" }
  ];
  function SidebarLeft({
    collapsed,
    onToggle
  }) {
    return /* @__PURE__ */ React.createElement(
      "aside",
      {
        className: "fixed left-0 z-30 hidden w-[var(--tpl-sidebar-cur)] flex-col border-r bg-white transition-[width] duration-200 xl:flex",
        style: {
          top: "var(--tpl-header-h)",
          height: "calc(100vh - var(--tpl-header-h))",
          borderColor: "var(--tpl-border)"
        }
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex-1 overflow-y-auto py-3" }, /* @__PURE__ */ React.createElement(Row, { collapsed, onClick: onToggle, label: "Collapse Menu", icon: "menu-icon" }), /* @__PURE__ */ React.createElement("div", { className: "my-2 h-px", style: { background: "var(--tpl-border)" } }), ITEMS.map((it) => /* @__PURE__ */ React.createElement(
        Row,
        {
          key: it.label,
          collapsed,
          icon: it.icon,
          label: it.label,
          href: it.href,
          active: it.active
        }
      ))),
      !collapsed && /* @__PURE__ */ React.createElement("div", { className: "border-t px-6 py-5", style: { borderColor: "var(--tpl-border)" } }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between text-sm", style: { color: "var(--tpl-heading)" } }, /* @__PURE__ */ React.createElement("span", { className: "font-medium" }, "Profile Completion"), /* @__PURE__ */ React.createElement("span", null, "76%")), /* @__PURE__ */ React.createElement("div", { className: "mt-2 h-1.5 overflow-hidden rounded-full", style: { background: "var(--tpl-surface-2)" } }, /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "h-full rounded-full",
          style: { width: "76%", background: "linear-gradient(90deg, var(--tpl-accent), var(--tpl-accent-2))" }
        }
      )), /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-xs leading-relaxed", style: { color: "var(--tpl-muted)" } }, "Complete", " ", /* @__PURE__ */ React.createElement("a", { href: "#", className: "font-medium hover:underline", style: { color: "var(--tpl-accent)" } }, "your profile"), " ", "so people can know more about you!"))
    );
  }
  function Row({
    icon,
    label,
    href,
    active,
    collapsed,
    onClick
  }) {
    const cls = `group flex items-center ${collapsed ? "justify-center px-0" : "gap-4 px-6"} py-2.5 transition hover:bg-[var(--tpl-surface-2)]`;
    const glyph = /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "grid w-6 shrink-0 place-items-center transition group-hover:text-[var(--tpl-accent)]",
        style: { color: active ? "var(--tpl-accent)" : "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: icon, size: 20 })
    );
    const text = !collapsed && /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "whitespace-nowrap text-sm font-medium transition group-hover:text-[var(--tpl-heading)]",
        style: { color: active ? "var(--tpl-heading)" : "var(--tpl-muted)" }
      },
      label
    );
    if (href) {
      return /* @__PURE__ */ React.createElement(Link, { href, className: cls, title: label }, glyph, text);
    }
    return /* @__PURE__ */ React.createElement("button", { type: "button", onClick, className: `${cls} w-full text-left`, title: label }, glyph, text);
  }

  // frontend/src/templates/v1/components/menu/SidebarRight.tsx
  init_define_import_meta_env();
  var import_react8 = __toESM(require_react_shim());
  var STATUS = {
    online: { label: "Online", varName: "--tpl-status-online" },
    work: { label: "At work!", varName: "--tpl-status-work" },
    away: { label: "Away", varName: "--tpl-status-away" },
    offline: { label: "Offline", varName: "--tpl-status-offline" },
    invisible: { label: "Invisible", varName: "--tpl-status-invisible" }
  };
  var GROUPS = [
    {
      title: "Close Friends",
      friends: [
        { name: "Carol Summers", status: "online" },
        { name: "Mathilda Brinker", status: "work" },
        { name: "Michael Maximoff", status: "away" },
        { name: "Rachel Howlett", status: "offline" },
        { name: "Nina Kraviz", status: "online" }
      ]
    },
    {
      title: "My Family",
      friends: [{ name: "Sarah Hetfield", status: "online" }]
    },
    {
      title: "Uncategorized",
      friends: [
        { name: "Bruce Peterson", status: "online" },
        { name: "Chris Greyson", status: "away" },
        { name: "Nicholas Grisom", status: "invisible" },
        { name: "James Spiegel", status: "away" },
        { name: "Diana Jones", status: "online" }
      ]
    }
  ];
  function SidebarRight({
    collapsed,
    onToggle
  }) {
    const [q, setQ] = (0, import_react8.useState)("");
    const groups = (0, import_react8.useMemo)(() => {
      const needle = q.trim().toLowerCase();
      if (!needle) return GROUPS;
      return GROUPS.map((g) => ({
        ...g,
        friends: g.friends.filter((f) => f.name.toLowerCase().includes(needle))
      })).filter((g) => g.friends.length > 0);
    }, [q]);
    const allFriends = (0, import_react8.useMemo)(() => GROUPS.flatMap((g) => g.friends), []);
    return /* @__PURE__ */ React.createElement(
      "aside",
      {
        className: "fixed right-0 z-30 hidden w-[var(--tpl-rightbar-cur)] flex-col border-l bg-white transition-[width] duration-200 xl:flex",
        style: {
          top: "var(--tpl-header-h)",
          height: "calc(100vh - var(--tpl-header-h))",
          borderColor: "var(--tpl-border)"
        }
      },
      collapsed ? (
        /* ── collapsed: avatar rail ── */
        /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "flex-1 overflow-y-auto py-4" }, /* @__PURE__ */ React.createElement("div", { className: "flex flex-col items-center gap-2.5" }, allFriends.map((f, i) => /* @__PURE__ */ React.createElement("button", { key: `${f.name}-${i}`, type: "button", title: f.name, className: "relative", "aria-label": f.name }, /* @__PURE__ */ React.createElement(Avatar, { name: f.name, size: 40 }), /* @__PURE__ */ React.createElement(StatusDot, { status: f.status, ring: true }))))), /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "button",
            onClick: onToggle,
            className: "grid h-11 place-items-center border-t text-[var(--tpl-muted)] transition hover:text-[var(--tpl-accent)]",
            style: { borderColor: "var(--tpl-border)" },
            "aria-label": "Expand friends panel"
          },
          /* @__PURE__ */ React.createElement(Icon, { name: "popup-left-arrow", size: 14 })
        ))
      ) : (
        /* ── expanded: grouped lists ── */
        /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "flex-1 overflow-y-auto" }, groups.map((g) => /* @__PURE__ */ React.createElement("div", { key: g.title }, /* @__PURE__ */ React.createElement(
          "div",
          {
            className: "flex items-center justify-between px-4 pb-1 pt-4 text-[11px] font-bold uppercase tracking-wide",
            style: { color: "var(--tpl-accent)" }
          },
          /* @__PURE__ */ React.createElement("span", null, g.title),
          /* @__PURE__ */ React.createElement("button", { type: "button", className: "text-[var(--tpl-muted)] hover:text-[var(--tpl-heading)]" }, "Settings")
        ), /* @__PURE__ */ React.createElement("ul", null, g.friends.map((f, i) => /* @__PURE__ */ React.createElement(
          "li",
          {
            key: `${f.name}-${i}`,
            className: "group flex items-center gap-3 px-4 py-2 transition hover:bg-[var(--tpl-surface-2)]"
          },
          /* @__PURE__ */ React.createElement("span", { className: "relative shrink-0" }, /* @__PURE__ */ React.createElement(Avatar, { name: f.name, size: 38 }), /* @__PURE__ */ React.createElement(StatusDot, { status: f.status, ring: true })),
          /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, f.name), /* @__PURE__ */ React.createElement("p", { className: "truncate text-[10px] font-semibold uppercase tracking-wide", style: { color: "var(--tpl-muted)" } }, STATUS[f.status].label)),
          /* @__PURE__ */ React.createElement(
            "button",
            {
              type: "button",
              className: "text-[var(--tpl-muted)] opacity-0 transition group-hover:opacity-100",
              "aria-label": "Friend options"
            },
            /* @__PURE__ */ React.createElement(Icon, { name: "three-dots-icon", size: 16 })
          )
        ))))), groups.length === 0 && /* @__PURE__ */ React.createElement("p", { className: "px-4 py-6 text-center text-sm", style: { color: "var(--tpl-muted)" } }, "No friends match \u201C", q, "\u201D.")), /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 border-t px-3 py-3", style: { borderColor: "var(--tpl-border)" } }, /* @__PURE__ */ React.createElement(
          "input",
          {
            value: q,
            onChange: (e2) => setQ(e2.target.value),
            placeholder: "Search Friends...",
            className: "min-w-0 flex-1 rounded-md border bg-transparent px-3 py-1.5 text-sm outline-none focus:border-[var(--tpl-accent)]",
            style: { borderColor: "var(--tpl-border)", color: "var(--tpl-text)" }
          }
        ), /* @__PURE__ */ React.createElement("button", { type: "button", className: "text-[var(--tpl-muted)] hover:text-[var(--tpl-accent)]", "aria-label": "Friend settings" }, /* @__PURE__ */ React.createElement(Icon, { name: "settings-v2-icon", size: 18 })), /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "button",
            onClick: onToggle,
            className: "text-[var(--tpl-muted)] hover:text-[var(--tpl-accent)]",
            "aria-label": "Collapse friends panel"
          },
          /* @__PURE__ */ React.createElement(Icon, { name: "close-icon", size: 16 })
        )), /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "button",
            className: "flex items-center justify-between px-4 py-3.5 text-sm font-bold uppercase tracking-wide text-white transition hover:opacity-95",
            style: { background: "#7c5ac2" }
          },
          /* @__PURE__ */ React.createElement("span", null, "Olympus Chat"),
          /* @__PURE__ */ React.createElement(Icon, { name: "chat---messages-icon", size: 20 })
        ))
      )
    );
  }
  function StatusDot({ status, ring }) {
    return /* @__PURE__ */ React.createElement(
      "span",
      {
        className: `absolute bottom-0 right-0 h-2.5 w-2.5 rounded-full ${ring ? "border-2 border-white" : ""}`,
        style: { background: `var(${STATUS[status].varName})` }
      }
    );
  }

  // frontend/src/templates/v1/components/music/AudioPlayer.tsx
  init_define_import_meta_env();
  var REF_TOTAL_SEC = 200;
  function formatTime(sec) {
    const s = Math.max(0, Math.round(sec));
    const m = Math.floor(s / 60);
    const r2 = s % 60;
    return `${m}:${r2.toString().padStart(2, "0")}`;
  }
  function AudioPlayer({
    track = { title: "The Past Starts Slow...", artist: "System of a Revenge" },
    playing = false,
    progress = 0,
    onPlayPause,
    onNext,
    onPrev
  }) {
    const pct = Math.min(100, Math.max(0, progress));
    const current = formatTime(pct / 100 * REF_TOTAL_SEC);
    const total = formatTime(REF_TOTAL_SEC);
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "flex items-center gap-4 rounded-xl px-4 py-3 shadow-sm",
        style: { background: "var(--tpl-surface)", border: "1px solid var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2", style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: onPrev,
          "aria-label": "Previous track",
          className: "grid h-8 w-8 place-items-center rounded-full transition hover:text-[var(--tpl-heading)]"
        },
        /* @__PURE__ */ React.createElement(Icon, { name: "music-previous-song-icon", size: 16 })
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: onPlayPause,
          "aria-label": playing ? "Pause" : "Play",
          className: "grid h-11 w-11 place-items-center rounded-full text-white shadow-sm transition hover:opacity-90",
          style: { background: "linear-gradient(135deg, var(--tpl-accent), var(--tpl-accent-2))" }
        },
        /* @__PURE__ */ React.createElement(Icon, { name: playing ? "music-pause-icon" : "music-play-icon-big", size: 18 })
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: onNext,
          "aria-label": "Next track",
          className: "grid h-8 w-8 place-items-center rounded-full transition hover:text-[var(--tpl-heading)]"
        },
        /* @__PURE__ */ React.createElement(Icon, { name: "music-next-song-icon", size: 16 })
      )),
      /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-baseline justify-between gap-3" }, /* @__PURE__ */ React.createElement("p", { className: "min-w-0 truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, track.title, /* @__PURE__ */ React.createElement("span", { className: "font-normal", style: { color: "var(--tpl-muted)" } }, " ", "\u2014 ", track.artist)), /* @__PURE__ */ React.createElement("span", { className: "shrink-0 text-xs tabular-nums", style: { color: "var(--tpl-muted)" } }, current, " / ", total)), /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "mt-2 h-1.5 w-full overflow-hidden rounded-full",
          style: { background: "var(--tpl-surface-2)" }
        },
        /* @__PURE__ */ React.createElement(
          "div",
          {
            className: "h-full rounded-full",
            style: {
              width: `${pct}%`,
              background: "linear-gradient(90deg, var(--tpl-accent), var(--tpl-accent-2))"
            }
          }
        )
      )),
      /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-1", style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          "aria-label": "Shuffle",
          className: "grid h-8 w-8 place-items-center rounded-full transition hover:text-[var(--tpl-accent)]"
        },
        /* @__PURE__ */ React.createElement(Icon, { name: "music-shuffle-icon", size: 16 })
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          "aria-label": "Repeat",
          className: "grid h-8 w-8 place-items-center rounded-full transition hover:text-[var(--tpl-accent)]"
        },
        /* @__PURE__ */ React.createElement(Icon, { name: "music-repeat-icon", size: 16 })
      ))
    );
  }

  // frontend/src/templates/v1/components/music/PlaylistWidget.tsx
  init_define_import_meta_env();

  // frontend/src/templates/v1/components/music/TrackItem.tsx
  init_define_import_meta_env();
  function seedGradient2(seed) {
    const hue = [...seed].reduce((a, c) => a + c.charCodeAt(0), 0) % 360;
    return `linear-gradient(135deg, hsl(${hue} 68% 58%), hsl(${(hue + 40) % 360} 68% 46%))`;
  }
  function TrackItem({
    index,
    title,
    artist,
    duration,
    playing = false,
    onPlay
  }) {
    return /* @__PURE__ */ React.createElement("li", { className: "group flex items-center gap-3 rounded-lg px-2 py-2 transition hover:bg-[var(--tpl-surface-2)]" }, /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "w-5 shrink-0 text-center text-xs tabular-nums",
        style: { color: playing ? "var(--tpl-accent)" : "var(--tpl-muted)" }
      },
      index
    ), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: onPlay,
        "aria-label": `Play ${title}`,
        className: "relative grid h-9 w-9 shrink-0 place-items-center overflow-hidden rounded-md",
        style: { background: seedGradient2(title + artist) }
      },
      /* @__PURE__ */ React.createElement(
        "span",
        {
          className: `grid h-full w-full place-items-center bg-black/35 text-white transition ${playing ? "opacity-100" : "opacity-0 group-hover:opacity-100"}`
        },
        /* @__PURE__ */ React.createElement(Icon, { name: "play-icon", size: 12 })
      )
    ), /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement(
      "p",
      {
        className: "truncate text-sm font-semibold",
        style: { color: playing ? "var(--tpl-accent)" : "var(--tpl-heading)" }
      },
      title
    ), /* @__PURE__ */ React.createElement("p", { className: "truncate text-xs", style: { color: "var(--tpl-muted)" } }, artist)), /* @__PURE__ */ React.createElement("time", { className: "shrink-0 text-xs tabular-nums", style: { color: "var(--tpl-muted)" } }, duration), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        "aria-label": "Track options",
        className: "shrink-0 text-[var(--tpl-muted)] transition hover:text-[var(--tpl-heading)]"
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "three-dots-icon", size: 14 })
    ));
  }

  // frontend/src/templates/v1/components/music/PlaylistWidget.tsx
  var SAMPLE_TRACKS = [
    { title: "The Past Starts Slow...", artist: "System of a Revenge", duration: "3:22" },
    { title: "The Pretender", artist: "Kung Fighters", duration: "5:48" },
    { title: "Bohemian Rhapsody", artist: "Green Groove", duration: "4:16" },
    { title: "Sunset Overdrive", artist: "Neon Rebels", duration: "3:51" },
    { title: "Midnight City Lights", artist: "The Wanderers", duration: "4:07" }
  ];
  function PlaylistWidget({
    title = "Playlist",
    tracks = SAMPLE_TRACKS,
    nowPlaying,
    onPlay
  }) {
    return /* @__PURE__ */ React.createElement(WidgetCard, { title, more: true }, /* @__PURE__ */ React.createElement("ol", { className: "space-y-1" }, tracks.map((t2, i) => {
      const index = i + 1;
      return /* @__PURE__ */ React.createElement(
        TrackItem,
        {
          key: `${t2.title}-${index}`,
          index,
          title: t2.title,
          artist: t2.artist,
          duration: t2.duration,
          playing: index === nowPlaying,
          onPlay: () => onPlay?.(index)
        }
      );
    })));
  }

  // frontend/src/templates/v1/components/popup/AddBook.tsx
  init_define_import_meta_env();
  var import_react10 = __toESM(require_react_shim());

  // frontend/src/templates/v1/components/popup/Modal.tsx
  init_define_import_meta_env();
  var import_react9 = __toESM(require_react_shim());
  function Modal({
    open,
    onClose,
    title,
    children,
    width = 520
  }) {
    (0, import_react9.useEffect)(() => {
      if (!open) return;
      const onEsc = (e2) => e2.key === "Escape" && onClose();
      document.addEventListener("keydown", onEsc);
      return () => document.removeEventListener("keydown", onEsc);
    }, [open, onClose]);
    if (!open) return null;
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "fixed inset-0 z-[60] flex items-center justify-center p-4",
        role: "dialog",
        "aria-modal": "true",
        "aria-label": title
      },
      /* @__PURE__ */ React.createElement("div", { className: "absolute inset-0 bg-black/45", onClick: onClose, "aria-hidden": true }),
      /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "relative max-h-[90vh] w-full overflow-hidden rounded-xl shadow-2xl",
          style: { maxWidth: width, background: "var(--tpl-surface)", color: "var(--tpl-text)" }
        },
        /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between border-b px-6 py-4", style: { borderColor: "var(--tpl-border)" } }, /* @__PURE__ */ React.createElement("h6", { className: "text-base font-semibold", style: { color: "var(--tpl-heading)" } }, title), /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "button",
            onClick: onClose,
            "aria-label": "Close",
            className: "text-[var(--tpl-muted)] transition hover:text-[var(--tpl-accent)]"
          },
          /* @__PURE__ */ React.createElement(Icon, { name: "close-icon", size: 15 })
        )),
        children
      )
    );
  }
  function Field({ label, children }) {
    return /* @__PURE__ */ React.createElement("label", { className: "block" }, /* @__PURE__ */ React.createElement("span", { className: "mb-1.5 block text-xs font-semibold uppercase tracking-wide", style: { color: "var(--tpl-muted)" } }, label), children);
  }
  var inputCls = "w-full rounded-md border bg-transparent px-3 py-2 text-sm outline-none transition focus:border-[var(--tpl-accent)]";
  var inputStyle = { borderColor: "var(--tpl-border)", color: "var(--tpl-text)" };
  function BtnSecondary({ children, onClick }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick,
        className: "rounded-md border px-4 py-2 text-sm font-semibold transition hover:bg-[var(--tpl-surface-2)]",
        style: { borderColor: "var(--tpl-border)", color: "var(--tpl-muted)" }
      },
      children
    );
  }
  function BtnPrimary({
    children,
    type = "button",
    onClick,
    disabled
  }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type,
        onClick,
        disabled,
        className: "rounded-md px-4 py-2 text-sm font-semibold text-white transition hover:opacity-90 disabled:opacity-50",
        style: { background: "linear-gradient(135deg, var(--tpl-accent), var(--tpl-accent-2))" }
      },
      children
    );
  }

  // frontend/src/templates/v1/components/popup/AddBook.tsx
  var CATEGORIES = ["Comic", "Novel", "Manga", "Light Novel", "Short Story"];
  function AddBook({
    open = false,
    onClose = () => {
    },
    onSave
  }) {
    const [title, setTitle] = (0, import_react10.useState)("");
    const [author, setAuthor] = (0, import_react10.useState)("");
    const [category, setCategory] = (0, import_react10.useState)(CATEGORIES[0] ?? "Comic");
    const [description, setDescription] = (0, import_react10.useState)("");
    const [cover, setCover] = (0, import_react10.useState)(null);
    function submit(e2) {
      e2.preventDefault();
      if (!title.trim()) return;
      onSave?.({ title: title.trim(), author: author.trim(), category, description: description.trim() });
      onClose();
    }
    return /* @__PURE__ */ React.createElement(Modal, { open, onClose, title: "Add Book", width: 560 }, /* @__PURE__ */ React.createElement("form", { onSubmit: submit, className: "space-y-4 p-6" }, /* @__PURE__ */ React.createElement("div", { className: "grid gap-4 sm:grid-cols-2" }, /* @__PURE__ */ React.createElement(Field, { label: "Title" }, /* @__PURE__ */ React.createElement("input", { className: inputCls, style: inputStyle, value: title, onChange: (e2) => setTitle(e2.target.value), placeholder: "The Majestic Canyon", autoFocus: true })), /* @__PURE__ */ React.createElement(Field, { label: "Author" }, /* @__PURE__ */ React.createElement("input", { className: inputCls, style: inputStyle, value: author, onChange: (e2) => setAuthor(e2.target.value), placeholder: "Marina Valentine" }))), /* @__PURE__ */ React.createElement(Field, { label: "Category" }, /* @__PURE__ */ React.createElement("select", { className: inputCls, style: inputStyle, value: category, onChange: (e2) => setCategory(e2.target.value) }, CATEGORIES.map((c) => /* @__PURE__ */ React.createElement("option", { key: c, value: c }, c)))), /* @__PURE__ */ React.createElement(Field, { label: "Description" }, /* @__PURE__ */ React.createElement(
      "textarea",
      {
        className: `${inputCls} resize-none`,
        style: inputStyle,
        rows: 3,
        value: description,
        onChange: (e2) => setDescription(e2.target.value),
        placeholder: "Write a little description about the book"
      }
    )), /* @__PURE__ */ React.createElement(
      "label",
      {
        className: "flex cursor-pointer items-center gap-3 rounded-md border border-dashed px-4 py-3 text-sm transition hover:bg-[var(--tpl-surface-2)]",
        style: { borderColor: "var(--tpl-border)", color: "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "camera-icon", size: 18 }),
      /* @__PURE__ */ React.createElement("span", null, cover ? "Cover selected" : "Upload a cover image"),
      /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "file",
          accept: "image/*",
          className: "hidden",
          onChange: (e2) => setCover(e2.target.files?.[0]?.name ?? null)
        }
      )
    ), /* @__PURE__ */ React.createElement("div", { className: "flex justify-end gap-2 pt-1" }, /* @__PURE__ */ React.createElement(BtnSecondary, { onClick: onClose }, "Cancel"), /* @__PURE__ */ React.createElement(BtnPrimary, { type: "submit" }, "Add Book"))));
  }

  // frontend/src/templates/v1/components/popup/ChatResponsive.tsx
  init_define_import_meta_env();
  var import_react11 = __toESM(require_react_shim());
  var SEED = [
    { id: "m1", from: "them", text: "Hey! Are we still on for the show this weekend?", time: "Yesterday at 8:10pm" },
    { id: "m2", from: "me", text: "Absolutely \u2014 got the tickets this morning \u{1F3B8}", time: "Yesterday at 8:12pm" },
    { id: "m3", from: "them", text: "Amazing. Iron Maid is going to be incredible live.", time: "Yesterday at 8:13pm" },
    { id: "m4", from: "me", text: "Can\u2019t wait. I\u2019ll pick you up at 7.", time: "Yesterday at 8:15pm" }
  ];
  function ChatResponsive({
    open = false,
    onClose = () => {
    },
    contact = "Marina Valentine",
    online = true
  }) {
    const [messages, setMessages] = (0, import_react11.useState)(SEED);
    const [draft, setDraft] = (0, import_react11.useState)("");
    function send(e2) {
      e2.preventDefault();
      const t2 = draft.trim();
      if (!t2) return;
      setMessages((prev) => [...prev, { id: `me-${prev.length}`, from: "me", text: t2, time: "Just now" }]);
      setDraft("");
    }
    if (!open) return null;
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "fixed bottom-0 right-4 z-[55] flex h-[26rem] w-80 flex-col overflow-hidden rounded-t-xl shadow-2xl",
        style: { background: "var(--tpl-surface)", border: "1px solid var(--tpl-border)" },
        role: "dialog",
        "aria-label": `Chat with ${contact}`
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2.5 px-4 py-3", style: { background: "var(--tpl-header)" } }, /* @__PURE__ */ React.createElement(Avatar, { name: contact, size: 32, status: online ? "online" : "offline" }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1 leading-tight" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm font-semibold text-white" }, contact), /* @__PURE__ */ React.createElement("p", { className: "text-[11px] text-white/60" }, online ? "Online" : "Offline")), /* @__PURE__ */ React.createElement("button", { type: "button", className: "text-white/60 transition hover:text-white", "aria-label": "Options" }, /* @__PURE__ */ React.createElement(Icon, { name: "three-dots-icon", size: 16 })), /* @__PURE__ */ React.createElement("button", { type: "button", onClick: onClose, className: "text-white/60 transition hover:text-white", "aria-label": "Close chat" }, /* @__PURE__ */ React.createElement(Icon, { name: "little-delete", size: 14 }))),
      /* @__PURE__ */ React.createElement("div", { className: "flex-1 space-y-3 overflow-y-auto px-3 py-4", style: { background: "var(--tpl-surface-2)" } }, messages.map((m) => /* @__PURE__ */ React.createElement("div", { key: m.id, className: `flex items-end gap-2 ${m.from === "me" ? "flex-row-reverse" : ""}` }, /* @__PURE__ */ React.createElement(Avatar, { name: m.from === "me" ? "You" : contact, size: 26 }), /* @__PURE__ */ React.createElement("div", { className: `max-w-[70%] ${m.from === "me" ? "text-right" : ""}` }, /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "inline-block rounded-2xl px-3 py-2 text-left text-sm",
          style: m.from === "me" ? { background: "var(--tpl-accent)", color: "#fff" } : { background: "var(--tpl-surface)", color: "var(--tpl-text)", border: "1px solid var(--tpl-border)" }
        },
        m.text
      ), /* @__PURE__ */ React.createElement("div", { className: "mt-0.5 text-[10px]", style: { color: "var(--tpl-muted)" } }, m.time))))),
      /* @__PURE__ */ React.createElement("form", { onSubmit: send, className: "flex items-center gap-2 border-t px-3 py-2.5", style: { borderColor: "var(--tpl-border)" } }, /* @__PURE__ */ React.createElement("button", { type: "button", className: "text-[var(--tpl-muted)] transition hover:text-[var(--tpl-accent)]", "aria-label": "Attach" }, /* @__PURE__ */ React.createElement(Icon, { name: "computer-icon", size: 18 })), /* @__PURE__ */ React.createElement("button", { type: "button", className: "text-[var(--tpl-muted)] transition hover:text-[var(--tpl-accent)]", "aria-label": "Emoji" }, /* @__PURE__ */ React.createElement(Icon, { name: "happy-sticker-icon", size: 18 })), /* @__PURE__ */ React.createElement(
        "input",
        {
          value: draft,
          onChange: (e2) => setDraft(e2.target.value),
          placeholder: "Press enter to post...",
          className: "min-w-0 flex-1 bg-transparent text-sm outline-none placeholder:text-[var(--tpl-muted)]",
          style: { color: "var(--tpl-text)" }
        }
      ))
    );
  }

  // frontend/src/templates/v1/components/popup/ChoseFromMyPhoto.tsx
  init_define_import_meta_env();
  var import_react12 = __toESM(require_react_shim());
  var PHOTOS = Array.from({ length: 9 }, (_, i) => i);
  var ALBUMS = [
    "South America Vacations",
    "Photoshoot Summer 2016",
    "Amazing Street Food",
    "Graffity & Street Art",
    "Amazing Landscapes",
    "The Majestic Canyon"
  ];
  function tile(seed) {
    const h = seed * 47 % 360;
    return `linear-gradient(135deg, hsl(${h} 60% 60%), hsl(${(h + 40) % 360} 60% 45%))`;
  }
  function ChoseFromMyPhoto({
    open = false,
    onClose = () => {
    },
    onConfirm
  }) {
    const [tab, setTab] = (0, import_react12.useState)("photos");
    const [selected, setSelected] = (0, import_react12.useState)(null);
    return /* @__PURE__ */ React.createElement(Modal, { open, onClose, title: "Choose from My Photos", width: 640 }, /* @__PURE__ */ React.createElement("div", { className: "flex gap-1 border-b px-6", style: { borderColor: "var(--tpl-border)" } }, [
      ["photos", "photos-icon", "Photos"],
      ["albums", "albums-icon", "Albums"]
    ].map(([k, ic, label]) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: k,
        type: "button",
        onClick: () => setTab(k),
        className: "flex items-center gap-2 px-4 py-3 text-sm font-semibold transition",
        style: tab === k ? { color: "var(--tpl-accent)", boxShadow: "inset 0 -2px 0 var(--tpl-accent)" } : { color: "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: ic, size: 16 }),
      label
    ))), /* @__PURE__ */ React.createElement("div", { className: "max-h-[52vh] overflow-y-auto p-6" }, tab === "photos" ? /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-3 gap-3" }, PHOTOS.map((p) => {
      const active = selected === p;
      return /* @__PURE__ */ React.createElement(
        "button",
        {
          key: p,
          type: "button",
          onClick: () => setSelected(p),
          className: "relative aspect-[3/2] overflow-hidden rounded-lg ring-offset-2 transition",
          style: { background: tile(p), boxShadow: active ? "0 0 0 3px var(--tpl-accent)" : "none" },
          "aria-pressed": active
        },
        active && /* @__PURE__ */ React.createElement(
          "span",
          {
            className: "absolute right-2 top-2 grid h-6 w-6 place-items-center rounded-full text-white",
            style: { background: "var(--tpl-accent)" }
          },
          /* @__PURE__ */ React.createElement(Icon, { name: "check-icon", size: 12 })
        )
      );
    })) : /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-3 gap-4" }, ALBUMS.map((a, i) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: a,
        type: "button",
        onClick: () => {
          setTab("photos");
          setSelected(i);
        },
        className: "text-left"
      },
      /* @__PURE__ */ React.createElement("span", { className: "block aspect-[5/4] rounded-lg", style: { background: tile(i + 20) } }),
      /* @__PURE__ */ React.createElement("span", { className: "mt-2 block truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, a),
      /* @__PURE__ */ React.createElement("span", { className: "block text-xs", style: { color: "var(--tpl-muted)" } }, "Last added: 2 days ago")
    )))), /* @__PURE__ */ React.createElement("div", { className: "flex justify-end gap-2 border-t px-6 py-4", style: { borderColor: "var(--tpl-border)" } }, /* @__PURE__ */ React.createElement(BtnSecondary, { onClick: onClose }, "Cancel"), /* @__PURE__ */ React.createElement(
      BtnPrimary,
      {
        disabled: selected === null,
        onClick: () => {
          if (selected !== null) {
            onConfirm?.(selected);
            onClose();
          }
        }
      },
      "Confirm Photo"
    )));
  }

  // frontend/src/templates/v1/components/popup/UpdateHeaderPhoto.tsx
  init_define_import_meta_env();
  function UpdateHeaderPhoto({
    open = false,
    onClose = () => {
    },
    onUpload,
    onChooseFromPhotos
  }) {
    return /* @__PURE__ */ React.createElement(Modal, { open, onClose, title: "Update Header Photo", width: 460 }, /* @__PURE__ */ React.createElement("div", { className: "space-y-3 p-6" }, /* @__PURE__ */ React.createElement("label", { className: "block cursor-pointer" }, /* @__PURE__ */ React.createElement(Option2, { icon: "computer-icon", title: "Upload Photo", subtitle: "Browse your computer." }), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "file",
        accept: "image/*",
        className: "hidden",
        onChange: (e2) => {
          const f = e2.target.files?.[0]?.name;
          if (f) onUpload?.(f);
        }
      }
    )), /* @__PURE__ */ React.createElement("button", { type: "button", className: "w-full text-left", onClick: () => onChooseFromPhotos?.() }, /* @__PURE__ */ React.createElement(Option2, { icon: "photos-icon", title: "Choose from My Photos", subtitle: "Choose from your uploaded photos" }))));
  }
  function Option2({ icon, title, subtitle }) {
    return /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "flex items-center gap-4 rounded-lg border px-4 py-4 transition hover:border-[var(--tpl-accent)] hover:bg-[var(--tpl-surface-2)]",
        style: { borderColor: "var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement(
        "span",
        {
          className: "grid h-11 w-11 shrink-0 place-items-center rounded-full text-white",
          style: { background: "linear-gradient(135deg, var(--tpl-accent), var(--tpl-accent-2))" }
        },
        /* @__PURE__ */ React.createElement(Icon, { name: icon, size: 20 })
      ),
      /* @__PURE__ */ React.createElement("span", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("span", { className: "block text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, title), /* @__PURE__ */ React.createElement("span", { className: "block text-xs", style: { color: "var(--tpl-muted)" } }, subtitle))
    );
  }

  // frontend/src/templates/v1/components/post/Post.tsx
  init_define_import_meta_env();

  // frontend/src/templates/v1/components/post/ReactionBar.tsx
  init_define_import_meta_env();
  function ReactionBar({
    likes,
    likedBy,
    likedByLabel,
    comments,
    shares,
    liked,
    className = ""
  }) {
    return /* @__PURE__ */ React.createElement("footer", { className: `mt-4 flex items-center gap-3 text-sm ${className}` }, /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "inline-flex items-center gap-2",
        style: { color: liked ? "var(--tpl-accent)" : "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "heart-icon", size: 16 }),
      /* @__PURE__ */ React.createElement("span", null, likes)
    ), likedBy.length > 0 && /* @__PURE__ */ React.createElement(StackedAvatars, { names: likedBy }), /* @__PURE__ */ React.createElement("span", { className: "truncate text-xs", style: { color: "var(--tpl-muted)" } }, likedByLabel ?? /* @__PURE__ */ React.createElement(DefaultLabel, { likedBy, likes })), /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "ml-auto inline-flex items-center gap-4",
        style: { color: "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement("span", { className: "inline-flex items-center gap-1.5" }, /* @__PURE__ */ React.createElement(Icon, { name: "speech-balloon-icon", size: 16 }), /* @__PURE__ */ React.createElement("span", { className: "text-xs" }, comments)),
      /* @__PURE__ */ React.createElement("span", { className: "inline-flex items-center gap-1.5" }, /* @__PURE__ */ React.createElement(Icon, { name: "share-icon", size: 16 }), /* @__PURE__ */ React.createElement("span", { className: "text-xs" }, shares))
    ));
  }
  function StackedAvatars({ names }) {
    return /* @__PURE__ */ React.createElement("span", { className: "flex" }, names.map((n2, i) => /* @__PURE__ */ React.createElement(
      "span",
      {
        key: n2,
        className: "rounded-full ring-2 ring-white",
        style: { marginLeft: i ? -8 : 0 }
      },
      /* @__PURE__ */ React.createElement(Avatar, { name: n2, size: 22 })
    )));
  }
  function DefaultLabel({ likedBy, likes }) {
    const lead = likedBy[0];
    if (!lead) {
      return /* @__PURE__ */ React.createElement("span", null, "Be the first to like this");
    }
    const first = lead.split(/\s+/)[0];
    const more = Math.max(0, likes - 1);
    return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("b", null, first), more > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, " ", "and ", /* @__PURE__ */ React.createElement("b", null, more, " more")) : null, " ", "liked this");
  }

  // frontend/src/templates/v1/components/post/PostControlButtons.tsx
  init_define_import_meta_env();
  function PostControlButtons({
    liked,
    onLike,
    onAward,
    onComment,
    onShare,
    className = ""
  }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `absolute right-0 top-6 hidden translate-x-1/2 flex-col gap-2 sm:flex ${className}`
      },
      /* @__PURE__ */ React.createElement(Fab, { label: "Award", icon: "trophy-icon", onClick: onAward }),
      /* @__PURE__ */ React.createElement(Fab, { label: "Like", icon: "like-post-icon", active: liked, onClick: onLike }),
      /* @__PURE__ */ React.createElement(Fab, { label: "Comment", icon: "comments-post-icon", onClick: onComment }),
      /* @__PURE__ */ React.createElement(Fab, { label: "Share", icon: "share-icon", onClick: onShare })
    );
  }
  function Fab({
    label,
    icon,
    active,
    onClick
  }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick,
        "aria-label": label,
        "aria-pressed": active,
        className: "grid h-[34px] w-[34px] place-items-center rounded-full text-white shadow-sm transition hover:opacity-90",
        style: { background: active ? "var(--tpl-accent)" : "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: icon, size: 16 })
    );
  }

  // frontend/src/templates/v1/components/post/Post.tsx
  function Post({
    author,
    time,
    action,
    text,
    media,
    likes,
    likedBy,
    likedByLabel,
    comments,
    shares,
    liked,
    onToggleLike,
    className = ""
  }) {
    return /* @__PURE__ */ React.createElement(
      "article",
      {
        className: `relative rounded-xl p-5 shadow-sm ${className}`,
        style: { background: "var(--tpl-surface)", border: "1px solid var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement(PostControlButtons, { liked, onLike: onToggleLike }),
      /* @__PURE__ */ React.createElement("header", { className: "flex items-center gap-3" }, /* @__PURE__ */ React.createElement(Avatar, { name: author, size: 40 }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm" }, /* @__PURE__ */ React.createElement("span", { className: "font-semibold", style: { color: "var(--tpl-heading)" } }, author), " ", action && /* @__PURE__ */ React.createElement("span", { style: { color: "var(--tpl-muted)" } }, action)), /* @__PURE__ */ React.createElement("time", { className: "text-xs", style: { color: "var(--tpl-muted)" } }, time)), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          className: "ml-auto pr-8 text-[var(--tpl-muted)]",
          "aria-label": "Post options"
        },
        /* @__PURE__ */ React.createElement(Icon, { name: "three-dots-icon", size: 18 })
      )),
      /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-sm leading-relaxed", style: { color: "var(--tpl-text)" } }, text),
      media && /* @__PURE__ */ React.createElement(MediaCard, { media }),
      /* @__PURE__ */ React.createElement(
        ReactionBar,
        {
          likes,
          likedBy,
          likedByLabel,
          comments,
          shares,
          liked
        }
      )
    );
  }
  function MediaCard({ media }) {
    if (media.type === "photo") return /* @__PURE__ */ React.createElement(PhotoCard, { media });
    return /* @__PURE__ */ React.createElement(VideoCard, { media });
  }
  function VideoCard({ media }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "mt-3 flex gap-4 overflow-hidden rounded-lg border",
        style: { borderColor: "var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "relative grid h-40 w-44 shrink-0 place-items-center",
          style: { background: "linear-gradient(135deg,#c78a5b,#7c5240)" }
        },
        /* @__PURE__ */ React.createElement(
          "span",
          {
            className: "grid h-14 w-14 place-items-center rounded-full text-white shadow",
            style: { background: "var(--tpl-accent)" }
          },
          /* @__PURE__ */ React.createElement(Icon, { name: "play-icon", size: 20 })
        )
      ),
      /* @__PURE__ */ React.createElement("div", { className: "min-w-0 py-4 pr-4" }, media.title && /* @__PURE__ */ React.createElement("p", { className: "text-base font-semibold", style: { color: "var(--tpl-heading)" } }, media.title), media.desc && /* @__PURE__ */ React.createElement("p", { className: "mt-1 text-xs leading-relaxed", style: { color: "var(--tpl-muted)" } }, media.desc), media.source && /* @__PURE__ */ React.createElement(
        "p",
        {
          className: "mt-3 text-[11px] font-semibold tracking-wide",
          style: { color: "var(--tpl-muted)" }
        },
        media.source
      ))
    );
  }
  function PhotoCard({ media }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "mt-3 overflow-hidden rounded-lg border",
        style: { borderColor: "var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "grid h-64 place-items-center",
          style: { background: "linear-gradient(135deg,#6d4bb8,#8a63d2)" }
        },
        /* @__PURE__ */ React.createElement("span", { className: "text-white/70" }, /* @__PURE__ */ React.createElement(Icon, { name: "photos-icon", size: 40 }))
      ),
      (media.title || media.desc) && /* @__PURE__ */ React.createElement("div", { className: "p-4" }, media.title && /* @__PURE__ */ React.createElement("p", { className: "text-base font-semibold", style: { color: "var(--tpl-heading)" } }, media.title), media.desc && /* @__PURE__ */ React.createElement("p", { className: "mt-1 text-xs leading-relaxed", style: { color: "var(--tpl-muted)" } }, media.desc))
    );
  }

  // frontend/src/templates/v1/components/profile/ControlBlockButtons.tsx
  init_define_import_meta_env();
  function ControlBlockButtons({
    buttons,
    size: size2 = 40,
    className = ""
  }) {
    const defaultIcon = Math.round(size2 * 0.45);
    const inner = size2 * 0.52;
    return /* @__PURE__ */ React.createElement("div", { className: `flex items-center gap-2 ${className}` }, buttons.map((b, i) => {
      const vb = OLYMP_VIEWBOX[b.icon] ?? "0 0 32 32";
      const parts = vb.split(/\s+/).map(Number);
      const aspect = (parts[2] || 1) / (parts[3] || 1);
      const iconSize = Math.min(defaultIcon, Math.round(inner / Math.max(1, aspect)));
      return /* @__PURE__ */ React.createElement(
        "button",
        {
          key: `${b.label}-${i}`,
          type: "button",
          onClick: b.onClick,
          title: b.label,
          "aria-label": b.label,
          className: "grid shrink-0 place-items-center overflow-hidden rounded-full text-white shadow-sm transition hover:opacity-90",
          style: { width: size2, height: size2, background: b.color ?? "var(--tpl-muted)" }
        },
        /* @__PURE__ */ React.createElement(Icon, { name: b.icon, size: iconSize })
      );
    }));
  }

  // frontend/src/templates/v1/components/profile/ProfileHeader.tsx
  init_define_import_meta_env();
  var DEFAULT_TABS = ["Timeline", "About", "Friends", "Photos", "Videos"];
  function coverGradient(seed) {
    const hue = [...seed].reduce((a, c) => a + c.charCodeAt(0), 0) % 360;
    return `linear-gradient(135deg, hsl(${hue} 70% 60%), hsl(${(hue + 55) % 360} 62% 46%))`;
  }
  function ProfileHeader({
    name,
    location: location2,
    tabs = DEFAULT_TABS,
    activeTab,
    onTab,
    onUpdateCover
  }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "overflow-hidden rounded-xl shadow-sm",
        style: { background: "var(--tpl-surface)", border: "1px solid var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement("div", { className: "relative h-40 sm:h-52", style: { background: coverGradient(name) } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: onUpdateCover,
          className: "absolute right-4 top-4 inline-flex items-center gap-2 rounded-md bg-black/30 px-3 py-1.5 text-xs font-semibold text-white backdrop-blur-sm transition hover:bg-black/45"
        },
        /* @__PURE__ */ React.createElement(Icon, { name: "camera-icon", size: 16 }),
        /* @__PURE__ */ React.createElement("span", null, "Update Cover")
      )),
      /* @__PURE__ */ React.createElement("div", { className: "px-5 pb-1 sm:px-6" }, /* @__PURE__ */ React.createElement("div", { className: "flex flex-wrap items-end gap-4" }, /* @__PURE__ */ React.createElement(Avatar, { name, size: 104, className: "-mt-14 shrink-0 ring-4 ring-white" }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1 pb-1" }, /* @__PURE__ */ React.createElement("h1", { className: "truncate text-xl font-bold", style: { color: "var(--tpl-heading)" } }, name), location2 && /* @__PURE__ */ React.createElement(
        "p",
        {
          className: "mt-1 inline-flex items-center gap-1.5 text-sm",
          style: { color: "var(--tpl-muted)" }
        },
        /* @__PURE__ */ React.createElement(Icon, { name: "small-pin-icon", size: 14 }),
        location2
      )), /* @__PURE__ */ React.createElement(
        ControlBlockButtons,
        {
          className: "pb-1",
          buttons: [
            { icon: "happy-face-icon", label: "Add friend", color: "var(--tpl-blue)" },
            { icon: "speech-balloon-icon", label: "Message", color: "var(--tpl-accent)" },
            { icon: "three-dots-icon", label: "Settings" }
          ]
        }
      )), /* @__PURE__ */ React.createElement(
        "nav",
        {
          className: "mt-4 flex flex-wrap border-t",
          style: { borderColor: "var(--tpl-border)" }
        },
        tabs.map((tab) => {
          const active = tab === activeTab;
          return /* @__PURE__ */ React.createElement(
            "button",
            {
              key: tab,
              type: "button",
              onClick: () => onTab?.(tab),
              className: "px-4 py-3 text-sm font-semibold transition",
              style: active ? { color: "var(--tpl-accent)", boxShadow: "inset 0 -2px 0 var(--tpl-accent)" } : { color: "var(--tpl-muted)" }
            },
            tab
          );
        })
      ))
    );
  }

  // frontend/src/templates/v1/components/social/BadgeCard.tsx
  init_define_import_meta_env();
  function BadgeCard({
    icon,
    count,
    title,
    description,
    progress
  }) {
    const pct = progress == null ? null : Math.max(0, Math.min(100, progress));
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "flex items-center gap-4 rounded-xl p-4 shadow-sm",
        style: { background: "var(--tpl-surface)", border: "1px solid var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement("div", { className: "relative shrink-0" }, /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "grid h-14 w-14 place-items-center rounded-full text-white shadow-sm",
          style: { background: "linear-gradient(135deg, var(--tpl-accent), var(--tpl-accent-2))" }
        },
        /* @__PURE__ */ React.createElement(Icon, { name: icon, size: 24 })
      ), count != null && /* @__PURE__ */ React.createElement(
        "span",
        {
          className: "absolute -right-1 -top-1 grid h-5 min-w-5 place-items-center rounded-full px-1 text-[11px] font-bold text-white ring-2 ring-white",
          style: { background: "var(--tpl-blue)" }
        },
        count
      )),
      /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, title), description && /* @__PURE__ */ React.createElement("p", { className: "mt-0.5 text-xs leading-snug", style: { color: "var(--tpl-muted)" } }, description), pct != null && /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "mt-3 h-2 overflow-hidden rounded-full",
          style: { background: "var(--tpl-surface-2)" },
          role: "progressbar",
          "aria-valuenow": pct,
          "aria-valuemin": 0,
          "aria-valuemax": 100
        },
        /* @__PURE__ */ React.createElement(
          "span",
          {
            className: "block h-full rounded-full",
            style: {
              width: `${pct}%`,
              background: "linear-gradient(90deg, var(--tpl-accent), var(--tpl-accent-2))"
            }
          }
        )
      ))
    );
  }

  // frontend/src/templates/v1/components/social/EventItem.tsx
  init_define_import_meta_env();
  function AttendeeStack({ names, max: max2 = 5 }) {
    const shown = names.slice(0, max2);
    const extra = names.length - shown.length;
    return /* @__PURE__ */ React.createElement("span", { className: "flex items-center" }, shown.map((n2, i) => /* @__PURE__ */ React.createElement("span", { key: n2, className: "rounded-full ring-2 ring-white", style: { marginLeft: i ? -8 : 0 } }, /* @__PURE__ */ React.createElement(Avatar, { name: n2, size: 28 }))), extra > 0 && /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "grid h-7 w-7 place-items-center rounded-full text-[11px] font-semibold text-white ring-2 ring-white",
        style: { marginLeft: -8, background: "var(--tpl-blue)" }
      },
      "+",
      extra
    ));
  }
  function EventItem({
    day,
    month,
    title,
    host,
    place,
    attendees,
    onJoin
  }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "flex flex-wrap items-center gap-4 rounded-xl p-4 shadow-sm",
        style: { background: "var(--tpl-surface)", border: "1px solid var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "grid h-16 w-16 shrink-0 place-content-center place-items-center rounded-lg text-white",
          style: { background: "linear-gradient(135deg, var(--tpl-accent), var(--tpl-accent-2))" }
        },
        /* @__PURE__ */ React.createElement("span", { className: "text-2xl font-bold leading-none" }, day),
        /* @__PURE__ */ React.createElement("span", { className: "mt-1 text-[11px] font-semibold uppercase tracking-wide" }, month)
      ),
      /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, title), /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "mt-0.5 flex flex-wrap items-center gap-x-3 gap-y-1 text-xs",
          style: { color: "var(--tpl-muted)" }
        },
        host && /* @__PURE__ */ React.createElement("span", { className: "truncate" }, "Hosted by ", host),
        place && /* @__PURE__ */ React.createElement("span", { className: "inline-flex items-center gap-1" }, /* @__PURE__ */ React.createElement(Icon, { name: "small-pin-icon", size: 12 }), place)
      ), attendees && attendees.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "mt-2" }, /* @__PURE__ */ React.createElement(AttendeeStack, { names: attendees }))),
      /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: onJoin,
          className: "shrink-0 rounded-md px-4 py-2 text-sm font-semibold text-white transition hover:opacity-90",
          style: { background: "linear-gradient(135deg, var(--tpl-accent), var(--tpl-accent-2))" }
        },
        "Join"
      )
    );
  }

  // frontend/src/templates/v1/components/social/FriendCard.tsx
  init_define_import_meta_env();
  function coverGradient2(seed) {
    const hue = [...seed].reduce((a, c) => a + c.charCodeAt(0), 0) % 360;
    return `linear-gradient(135deg, hsl(${hue} 70% 60%), hsl(${(hue + 55) % 360} 62% 46%))`;
  }
  function FriendCard({
    name,
    country,
    stats
  }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "overflow-hidden rounded-xl shadow-sm",
        style: { background: "var(--tpl-surface)", border: "1px solid var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement("div", { className: "h-24", style: { background: coverGradient2(name) } }),
      /* @__PURE__ */ React.createElement("div", { className: "px-4 pb-4" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-end gap-3" }, /* @__PURE__ */ React.createElement(Avatar, { name, size: 72, className: "-mt-9 shrink-0 ring-4 ring-white" }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1 pb-1" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-base font-semibold", style: { color: "var(--tpl-heading)" } }, name), country && /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm", style: { color: "var(--tpl-muted)" } }, country))), stats && /* @__PURE__ */ React.createElement("div", { className: "mt-4 grid grid-cols-3 text-center" }, [
        ["Friends", stats.friends],
        ["Photos", stats.photos],
        ["Videos", stats.videos]
      ].map(([label, count]) => /* @__PURE__ */ React.createElement("div", { key: label }, /* @__PURE__ */ React.createElement("div", { className: "text-base font-bold", style: { color: "var(--tpl-heading)" } }, count), /* @__PURE__ */ React.createElement("div", { className: "text-xs", style: { color: "var(--tpl-muted)" } }, label)))), /* @__PURE__ */ React.createElement(
        ControlBlockButtons,
        {
          className: "mt-4 justify-center",
          size: 36,
          buttons: [
            { icon: "happy-face-icon", label: "Add friend", color: "var(--tpl-blue)" },
            { icon: "speech-balloon-icon", label: "Message", color: "var(--tpl-accent)" }
          ]
        }
      ))
    );
  }

  // frontend/src/templates/v1/components/social/FriendRequestItem.tsx
  init_define_import_meta_env();
  function FriendRequestItem({
    name,
    mutual,
    status,
    onAccept,
    onDecline
  }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "flex items-center gap-3 rounded-xl px-4 py-3 shadow-sm",
        style: { background: "var(--tpl-surface)", border: "1px solid var(--tpl-border)" }
      },
      /* @__PURE__ */ React.createElement(Avatar, { name, size: 48, status, className: "shrink-0" }),
      /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, name), mutual != null && /* @__PURE__ */ React.createElement("p", { className: "truncate text-xs", style: { color: "var(--tpl-muted)" } }, mutual, " mutual friend", mutual === 1 ? "" : "s")),
      /* @__PURE__ */ React.createElement(
        ControlBlockButtons,
        {
          size: 36,
          buttons: [
            { icon: "check-icon", label: "Accept", color: "var(--tpl-accent)", onClick: onAccept },
            { icon: "close-icon", label: "Decline", color: "var(--tpl-muted)", onClick: onDecline }
          ]
        }
      )
    );
  }

  // frontend/src/templates/v1/components/widget/ActivityFeed.tsx
  init_define_import_meta_env();
  var DEFAULT_ITEMS = [
    {
      actor: "Marina Polson",
      action: /* @__PURE__ */ React.createElement(React.Fragment, null, "commented on Jason Mark's ", /* @__PURE__ */ React.createElement(FeedLink, null, "photo"), "."),
      time: "2 mins ago",
      icon: "comments-post-icon"
    },
    {
      actor: "Jake Parker",
      action: /* @__PURE__ */ React.createElement(React.Fragment, null, "liked Nicholas Grissom's ", /* @__PURE__ */ React.createElement(FeedLink, null, "status update"), "."),
      time: "5 mins ago",
      icon: "like-post-icon"
    },
    {
      actor: "Mary Jane Stark",
      action: /* @__PURE__ */ React.createElement(React.Fragment, null, "added 20 new photos to her ", /* @__PURE__ */ React.createElement(FeedLink, null, "gallery album"), "."),
      time: "12 mins ago",
      icon: "photos-icon"
    },
    {
      actor: "Nicholas Grissom",
      action: /* @__PURE__ */ React.createElement(React.Fragment, null, "updated his profile ", /* @__PURE__ */ React.createElement(FeedLink, null, "photo"), "."),
      time: "1 hour ago",
      icon: "happy-face-icon"
    }
  ];
  function ActivityFeed({
    items = DEFAULT_ITEMS,
    title = "Activity Feed"
  } = {}) {
    return /* @__PURE__ */ React.createElement(WidgetCard, { title }, /* @__PURE__ */ React.createElement("ul", { className: "space-y-4" }, items.map((it, i) => /* @__PURE__ */ React.createElement("li", { key: `${it.actor}-${i}`, className: "flex items-start gap-3" }, /* @__PURE__ */ React.createElement(Avatar, { name: it.actor, size: 36 }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("p", { className: "text-sm leading-snug", style: { color: "var(--tpl-text)" } }, /* @__PURE__ */ React.createElement("b", { style: { color: "var(--tpl-heading)" } }, it.actor), " ", it.action), /* @__PURE__ */ React.createElement("p", { className: "mt-0.5 text-xs", style: { color: "var(--tpl-muted)" } }, it.time)), /* @__PURE__ */ React.createElement("span", { className: "mt-1 shrink-0", style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement(Icon, { name: it.icon, size: 16 }))))));
  }
  function FeedLink({ children }) {
    return /* @__PURE__ */ React.createElement("a", { href: "#", className: "font-medium hover:underline", style: { color: "var(--tpl-accent)" } }, children);
  }

  // frontend/src/templates/v1/components/widget/BirthdayCard.tsx
  init_define_import_meta_env();
  function BirthdayCard({
    name = "Marina Valentine",
    message = "Leave her a message with your best wishes on her profile page!"
  } = {}) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "relative overflow-hidden rounded-xl p-5 text-white shadow-sm",
        style: { background: "linear-gradient(150deg, #8a63d2, #6d4bb8)" }
      },
      /* @__PURE__ */ React.createElement(
        "div",
        {
          "aria-hidden": true,
          className: "pointer-events-none absolute inset-0 opacity-20",
          style: {
            background: "radial-gradient(60% 60% at 85% 15%, rgba(255,255,255,0.5), transparent 60%),radial-gradient(50% 50% at 20% 90%, rgba(255,255,255,0.25), transparent 60%)"
          }
        }
      ),
      /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ React.createElement(Icon, { name: "cupcake-icon", size: 22 }), /* @__PURE__ */ React.createElement("button", { type: "button", "aria-label": "Options" }, /* @__PURE__ */ React.createElement(Icon, { name: "three-dots-icon", size: 18 }))), /* @__PURE__ */ React.createElement("div", { className: "mt-4" }, /* @__PURE__ */ React.createElement(Avatar, { name, size: 44, className: "ring-2 ring-white/40" })), /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-sm text-white/80" }, "Today is"), /* @__PURE__ */ React.createElement("h3", { className: "text-2xl font-bold leading-tight" }, name, "'s Birthday!"), /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-sm leading-relaxed text-white/80" }, message))
    );
  }

  // frontend/src/templates/v1/components/widget/CalendarWidget.tsx
  init_define_import_meta_env();
  var DEFAULT_WEEKDAYS = ["MON", "TUE", "WED", "THU", "FRI", "SAT", "SUN"];
  function CalendarWidget({
    month = "May",
    today = 12,
    daysInMonth = 31,
    weekdays = DEFAULT_WEEKDAYS,
    onPrev,
    onNext
  } = {}) {
    const days = Array.from({ length: daysInMonth }, (_, i) => i + 1);
    return /* @__PURE__ */ React.createElement(WidgetCard, null, /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "mb-3 flex items-center justify-between px-1",
        style: { color: "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement("button", { type: "button", "aria-label": "Previous month", onClick: onPrev }, /* @__PURE__ */ React.createElement(Icon, { name: "popup-left-arrow", size: 12 })),
      /* @__PURE__ */ React.createElement("span", { className: "font-semibold", style: { color: "var(--tpl-heading)" } }, month),
      /* @__PURE__ */ React.createElement("button", { type: "button", "aria-label": "Next month", onClick: onNext }, /* @__PURE__ */ React.createElement(Icon, { name: "popup-right-arrow", size: 12 }))
    ), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-7 gap-y-2 text-center text-[11px]" }, weekdays.map((d) => /* @__PURE__ */ React.createElement("span", { key: d, className: "font-semibold", style: { color: "var(--tpl-muted)" } }, d)), days.map((n2) => /* @__PURE__ */ React.createElement(
      "span",
      {
        key: n2,
        className: "grid h-7 place-items-center rounded-full text-xs",
        style: n2 === today ? { background: "var(--tpl-accent)", color: "#fff", margin: "0 auto", width: 28 } : { color: "var(--tpl-text)" }
      },
      n2
    ))));
  }

  // frontend/src/templates/v1/components/widget/FriendSuggestions.tsx
  init_define_import_meta_env();
  var DEFAULT_PEOPLE = [
    { name: "Francine Smith", meta: "8 Friends in Common" },
    { name: "Hugh Wilson", meta: "6 Friends in Common" },
    { name: "Karen Masters", meta: "6 Friends in Common" }
  ];
  function FriendSuggestions({
    people = DEFAULT_PEOPLE,
    title = "Friend Suggestions"
  } = {}) {
    return /* @__PURE__ */ React.createElement(WidgetCard, { title, more: true }, /* @__PURE__ */ React.createElement("ul", { className: "space-y-3" }, people.map((p) => /* @__PURE__ */ React.createElement("li", { key: p.name, className: "flex items-center gap-3" }, /* @__PURE__ */ React.createElement(Avatar, { name: p.name, size: 40 }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, p.name), /* @__PURE__ */ React.createElement("p", { className: "truncate text-xs", style: { color: "var(--tpl-muted)" } }, p.meta)), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "ml-auto grid h-8 w-8 place-items-center rounded-md text-white transition hover:opacity-90",
        style: { background: "var(--tpl-blue)" },
        "aria-label": `Add ${p.name}`
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "happy-face-icon", size: 16 })
    )))));
  }

  // frontend/src/templates/v1/components/widget/PagesWidget.tsx
  init_define_import_meta_env();
  var DEFAULT_PAGES = [
    { name: "The Marina Bar", category: "Restaurant \xB7 Bar" },
    { name: "Tapronus Rock", category: "Rock Band" },
    { name: "Pixel Digital Design", category: "Company" },
    { name: "Thompson's Custom Clothing Boutique", category: "Clothing Store" }
  ];
  function PagesWidget({
    pages = DEFAULT_PAGES,
    title = "Pages You May Like"
  } = {}) {
    return /* @__PURE__ */ React.createElement(WidgetCard, { title, more: true }, /* @__PURE__ */ React.createElement("ul", { className: "space-y-3" }, pages.map((p) => /* @__PURE__ */ React.createElement("li", { key: p.name, className: "flex items-center gap-3" }, /* @__PURE__ */ React.createElement(Avatar, { name: p.name, size: 38 }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, p.name), /* @__PURE__ */ React.createElement("p", { className: "truncate text-xs", style: { color: "var(--tpl-muted)" } }, p.category)), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "ml-auto text-[var(--tpl-muted)] transition hover:text-[var(--tpl-accent)]",
        "aria-label": "Like page"
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "star-icon", size: 18 })
    )))));
  }

  // frontend/src/templates/v1/components/widget/PersonalInfoWidget.tsx
  init_define_import_meta_env();
  var DEFAULT_ITEMS2 = [
    {
      label: "About",
      value: "Hi, I'm James \u2014 a Digital Designer at the \u201CDaydreams\u201D Agency in Pier 56."
    },
    { label: "Lives in", value: "San Francisco, CA" },
    { label: "Email", value: "james@example.com" },
    { label: "Website", value: "portal.example" }
  ];
  function PersonalInfoWidget({
    items = DEFAULT_ITEMS2,
    title = "Personal Info"
  } = {}) {
    return /* @__PURE__ */ React.createElement(WidgetCard, { title }, /* @__PURE__ */ React.createElement("ul", { className: "space-y-3" }, items.map((it, i) => /* @__PURE__ */ React.createElement("li", { key: `${it.label}-${i}` }, /* @__PURE__ */ React.createElement(
      "p",
      {
        className: "text-[11px] font-semibold uppercase tracking-wide",
        style: { color: "var(--tpl-muted)" }
      },
      it.label
    ), /* @__PURE__ */ React.createElement("p", { className: "mt-0.5 text-sm leading-relaxed", style: { color: "var(--tpl-text)" } }, it.value)))));
  }

  // frontend/src/templates/v1/components/widget/WeatherWidget.tsx
  init_define_import_meta_env();
  var DEFAULT_WEEK = [
    { day: "SUN", icon: "weather-sunny-icon", temp: 60 },
    { day: "MON", icon: "weather-sunny-icon", temp: 58 },
    { day: "TUE", icon: "weather-cloudy-icon", temp: 67 },
    { day: "WED", icon: "weather-rain-icon", temp: 70 },
    { day: "THU", icon: "weather-rain-icon", temp: 58 },
    { day: "FRI", icon: "weather-rain-icon", temp: 68 },
    { day: "SAT", icon: "weather-partly-sunny-icon", temp: 65 }
  ];
  function WeatherWidget({
    temp = 64,
    low = 58,
    high = 76,
    condition = "Partly Sunny",
    realFeel = 67,
    chanceOfRain = 49,
    icon = "weather-partly-sunny-icon",
    week = DEFAULT_WEEK,
    date = "Saturday, March 26th",
    location: location2 = "San Francisco, CA"
  } = {}) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "overflow-hidden rounded-xl text-white shadow-sm",
        style: { background: "linear-gradient(160deg, var(--tpl-weather-1), var(--tpl-weather-2))" }
      },
      /* @__PURE__ */ React.createElement("div", { className: "p-5" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-start gap-1" }, /* @__PURE__ */ React.createElement("span", { className: "text-5xl font-light leading-none" }, temp, "\xB0"), /* @__PURE__ */ React.createElement("span", { className: "mt-1 text-xs leading-tight text-white/80" }, low, "\xB0", /* @__PURE__ */ React.createElement("br", null), high, "\xB0")), /* @__PURE__ */ React.createElement(Icon, { name: icon, size: 40 })), /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-lg font-semibold" }, condition), /* @__PURE__ */ React.createElement("p", { className: "text-xs text-white/80" }, "Real Feel: ", realFeel, "\xB0\xA0\xA0\xA0Chance of Rain: ", chanceOfRain, "%")),
      /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "grid grid-cols-7 gap-1 px-3 py-4",
          style: { background: "rgba(255,255,255,0.08)" }
        },
        week.map((d) => /* @__PURE__ */ React.createElement(
          "div",
          {
            key: d.day,
            className: "flex flex-col items-center gap-1.5 text-[10px] text-white/85"
          },
          /* @__PURE__ */ React.createElement("span", { className: "font-semibold" }, d.day),
          /* @__PURE__ */ React.createElement(Icon, { name: d.icon, size: 18 }),
          /* @__PURE__ */ React.createElement("span", null, d.temp, "\xB0")
        ))
      ),
      /* @__PURE__ */ React.createElement("div", { className: "px-5 py-3 text-center text-xs" }, /* @__PURE__ */ React.createElement("p", { className: "font-semibold" }, date), /* @__PURE__ */ React.createElement("p", { className: "text-white/75" }, location2))
    );
  }

  // frontend/src/templates/v1/master/MasterBase.tsx
  init_define_import_meta_env();
  var import_react16 = __toESM(require_react_shim());

  // frontend/src/templates/v1/partials/HelloPreloader.tsx
  init_define_import_meta_env();
  var import_react13 = __toESM(require_react_shim());
  function HelloPreloader() {
    const [done, setDone] = (0, import_react13.useState)(false);
    (0, import_react13.useEffect)(() => {
      const t2 = setTimeout(() => setDone(true), 300);
      return () => clearTimeout(t2);
    }, []);
    if (done) return null;
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        id: "hellopreloader",
        className: "fixed inset-0 z-[100] grid place-items-center",
        style: { background: "var(--background, #0b0b10)" }
      },
      /* @__PURE__ */ React.createElement("div", { className: "animate-pulse text-sm", style: { color: "var(--tpl-muted)" } }, "Loading \u2026")
    );
  }

  // frontend/src/templates/v1/partials/GoToTop.tsx
  init_define_import_meta_env();
  var import_react14 = __toESM(require_react_shim());
  function GoToTop() {
    const [show, setShow] = (0, import_react14.useState)(false);
    (0, import_react14.useEffect)(() => {
      const onScroll = () => setShow(window.scrollY > 300);
      onScroll();
      window.addEventListener("scroll", onScroll, { passive: true });
      return () => window.removeEventListener("scroll", onScroll);
    }, []);
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        "aria-label": "Go to top",
        onClick: () => window.scrollTo({ top: 0, behavior: "smooth" }),
        className: `fixed bottom-6 right-6 z-40 grid h-12 w-12 place-items-center rounded-full text-white shadow-lg transition-opacity duration-200 hover:opacity-90 xl:right-[calc(var(--tpl-rightbar-cur)+1.5rem)] ${show ? "opacity-100" : "pointer-events-none opacity-0"}`,
        style: { background: "linear-gradient(135deg, var(--tpl-accent), var(--tpl-accent-2))" }
      },
      /* @__PURE__ */ React.createElement(
        "svg",
        {
          width: "22",
          height: "22",
          viewBox: "0 0 24 24",
          fill: "none",
          stroke: "currentColor",
          strokeWidth: "2.5",
          strokeLinecap: "round",
          strokeLinejoin: "round",
          "aria-hidden": true
        },
        /* @__PURE__ */ React.createElement("polyline", { points: "18 15 12 9 6 15" })
      )
    );
  }

  // frontend/src/templates/v1/partials/SessionKeeper.tsx
  init_define_import_meta_env();
  var import_react15 = __toESM(require_react_shim());
  var API_BASE2 = process.env.NEXT_PUBLIC_API_BASE_URL ?? "https://api.portal.localhost";
  var REFRESH_MS = 4 * 60 * 1e3;
  var CLAIM_MS = REFRESH_MS - 15e3;
  var KEY = "portal_refresh_at";
  function SessionKeeper() {
    (0, import_react15.useEffect)(() => {
      let stopped = false;
      async function refresh(force) {
        const last = Number(localStorage.getItem(KEY) || 0);
        const now = Date.now();
        if (!force && now - last < CLAIM_MS) return;
        localStorage.setItem(KEY, String(now));
        try {
          const res = await fetch(`${API_BASE2}/api/v1/auth/refresh`, {
            method: "POST",
            credentials: "include"
          });
          if (res.ok) {
            localStorage.setItem(KEY, String(Date.now()));
          } else if (res.status === 401 && !stopped) {
            localStorage.removeItem(KEY);
            window.location.assign("/login");
          }
        } catch {
          localStorage.setItem(KEY, String(last));
        }
      }
      refresh(false);
      const timer = setInterval(() => refresh(false), REFRESH_MS);
      const onVisible = () => {
        if (document.visibilityState === "visible") refresh(false);
      };
      document.addEventListener("visibilitychange", onVisible);
      window.addEventListener("focus", onVisible);
      return () => {
        stopped = true;
        clearInterval(timer);
        document.removeEventListener("visibilitychange", onVisible);
        window.removeEventListener("focus", onVisible);
      };
    }, []);
    return null;
  }

  // frontend/src/templates/v1/master/MasterBase.tsx
  function MasterBase({ children }) {
    const [collapsed, setCollapsed] = (0, import_react16.useState)(false);
    const [rightCollapsed, setRightCollapsed] = (0, import_react16.useState)(false);
    const rootStyle = {
      background: "var(--tpl-bg)",
      "--tpl-sidebar-cur": collapsed ? "var(--tpl-rail-w)" : "var(--tpl-sidebar-w)",
      "--tpl-rightbar-cur": rightCollapsed ? "var(--tpl-rail-w)" : "var(--tpl-rightbar-w)"
    };
    return /* @__PURE__ */ React.createElement("div", { "data-template": "v1", className: "min-h-screen", style: rootStyle }, /* @__PURE__ */ React.createElement(HelloPreloader, null), /* @__PURE__ */ React.createElement(SessionKeeper, null), /* @__PURE__ */ React.createElement(SidebarCenter, null), /* @__PURE__ */ React.createElement(SidebarLeft, { collapsed, onToggle: () => setCollapsed((c) => !c) }), /* @__PURE__ */ React.createElement(SidebarRight, { collapsed: rightCollapsed, onToggle: () => setRightCollapsed((c) => !c) }), /* @__PURE__ */ React.createElement(
      "main",
      {
        className: "min-h-screen transition-[padding] duration-200 xl:pl-[var(--tpl-sidebar-cur)] xl:pr-[var(--tpl-rightbar-cur)]",
        style: { paddingTop: "var(--tpl-header-h)", color: "var(--tpl-text)" }
      },
      /* @__PURE__ */ React.createElement("div", { className: "mx-auto w-full max-w-[1220px] px-3 py-5 sm:px-5" }, children)
    ), /* @__PURE__ */ React.createElement(GoToTop, null), /* @__PURE__ */ React.createElement(UpdateHeaderPhoto, null), /* @__PURE__ */ React.createElement(ChoseFromMyPhoto, null), /* @__PURE__ */ React.createElement(ChatResponsive, null), /* @__PURE__ */ React.createElement(SvgSprite, null));
  }

  // frontend/src/templates/v1/master/MasterPublic.tsx
  init_define_import_meta_env();
  function MasterPublic({ children }) {
    return /* @__PURE__ */ React.createElement("div", { "data-template": "v1", className: "landing-page min-h-screen" }, children, /* @__PURE__ */ React.createElement(SvgSprite, null));
  }

  // frontend/src/templates/v1/views/auth/AuthForm.tsx
  init_define_import_meta_env();
  var import_react17 = __toESM(require_react_shim());
  var API_BASE3 = process.env.NEXT_PUBLIC_API_BASE_URL ?? "https://api.portal.localhost";
  function AuthForm({ defaultTab = "login" }) {
    const [tab, setTab] = (0, import_react17.useState)(defaultTab);
    const [notice, setNotice] = (0, import_react17.useState)(null);
    const [prefillEmail, setPrefillEmail] = (0, import_react17.useState)("");
    function handleRegistered(email) {
      setPrefillEmail(email);
      setNotice("Account created. Please sign in with your new account.");
      setTab("login");
    }
    function switchTo(next) {
      setNotice(null);
      setTab(next);
    }
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "w-full max-w-md rounded-2xl border p-6 shadow-2xl backdrop-blur sm:p-8",
        style: { borderColor: "var(--tpl-border)", background: "var(--tpl-surface)" }
      },
      /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "mb-6 grid grid-cols-2 overflow-hidden rounded-xl border",
          style: { borderColor: "var(--tpl-border)" }
        },
        /* @__PURE__ */ React.createElement(TabButton, { active: tab === "login", onClick: () => switchTo("login") }, /* @__PURE__ */ React.createElement(LogInIcon, null), " Sign in"),
        /* @__PURE__ */ React.createElement(TabButton, { active: tab === "register", onClick: () => switchTo("register") }, /* @__PURE__ */ React.createElement(UserPlusIcon, null), " Register")
      ),
      tab === "login" ? /* @__PURE__ */ React.createElement(
        LoginForm,
        {
          onSwitch: () => switchTo("register"),
          initialEmail: prefillEmail,
          notice
        }
      ) : /* @__PURE__ */ React.createElement(RegisterForm, { onSwitch: () => switchTo("login"), onRegistered: handleRegistered })
    );
  }
  function nextTarget() {
    if (typeof window === "undefined") return "/";
    const n2 = new URLSearchParams(window.location.search).get("next");
    return n2 && n2.startsWith("/") ? n2 : "/";
  }
  async function postAuth(path, payload) {
    const res = await fetch(`${API_BASE3}/api/v1/auth/${path}`, {
      method: "POST",
      credentials: "include",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload)
    });
    if (res.ok) return null;
    let msg = "Something went wrong. Please try again.";
    try {
      const body = await res.json();
      if (body.message) msg = body.message;
    } catch {
    }
    return msg;
  }
  function LoginForm({
    onSwitch,
    initialEmail = "",
    notice = null
  }) {
    const [email, setEmail] = (0, import_react17.useState)(initialEmail);
    const [password, setPassword] = (0, import_react17.useState)("");
    const [remember, setRemember] = (0, import_react17.useState)(true);
    const [error, setError] = (0, import_react17.useState)(null);
    const [loading, setLoading] = (0, import_react17.useState)(false);
    async function onSubmit(e2) {
      e2.preventDefault();
      setError(null);
      setLoading(true);
      const err = await postAuth("login", { email, password, remember });
      if (err) {
        setError(err);
        setLoading(false);
        return;
      }
      try {
        localStorage.setItem("portal_refresh_at", String(Date.now()));
      } catch {
      }
      window.location.assign(nextTarget());
    }
    return /* @__PURE__ */ React.createElement("form", { className: "space-y-4", onSubmit }, /* @__PURE__ */ React.createElement("h2", { className: "text-lg font-semibold" }, "Login to your account"), notice && /* @__PURE__ */ React.createElement(SuccessBanner, null, notice), error && /* @__PURE__ */ React.createElement(ErrorBanner, null, error), /* @__PURE__ */ React.createElement(
      Field2,
      {
        label: "Your Email",
        name: "email",
        type: "email",
        autoComplete: "email",
        placeholder: "you@example.com",
        value: email,
        onChange: setEmail,
        required: true
      }
    ), /* @__PURE__ */ React.createElement(
      Field2,
      {
        label: "Your Password",
        name: "password",
        type: "password",
        autoComplete: "current-password",
        placeholder: "\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022",
        value: password,
        onChange: setPassword,
        required: true
      }
    ), /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between text-sm" }, /* @__PURE__ */ React.createElement(
      "label",
      {
        className: "flex cursor-pointer items-center gap-2",
        style: { color: "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "checkbox",
          className: "accent-[var(--tpl-accent)]",
          checked: remember,
          onChange: (e2) => setRemember(e2.target.checked)
        }
      ),
      " ",
      "Remember me"
    ), /* @__PURE__ */ React.createElement("span", { style: { color: "var(--tpl-muted)" } }, "Forgot password? (coming soon)")), /* @__PURE__ */ React.createElement(PrimaryButton, { loading }, "Login"), /* @__PURE__ */ React.createElement("p", { className: "text-center text-sm", style: { color: "var(--tpl-muted)" } }, "Don't have an account?", " ", /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: onSwitch,
        className: "font-medium hover:underline",
        style: { color: "var(--tpl-accent)" }
      },
      "Register now"
    )));
  }
  function RegisterForm({
    onSwitch,
    onRegistered
  }) {
    const [displayName, setDisplayName] = (0, import_react17.useState)("");
    const [email, setEmail] = (0, import_react17.useState)("");
    const [password, setPassword] = (0, import_react17.useState)("");
    const [accepted, setAccepted] = (0, import_react17.useState)(false);
    const [error, setError] = (0, import_react17.useState)(null);
    const [loading, setLoading] = (0, import_react17.useState)(false);
    async function onSubmit(e2) {
      e2.preventDefault();
      setError(null);
      if (!accepted) {
        setError("Please accept the Terms & Conditions to continue.");
        return;
      }
      if (password.length < 8) {
        setError("Password must be at least 8 characters.");
        return;
      }
      setLoading(true);
      const err = await postAuth("register", {
        email,
        password,
        display_name: displayName
      });
      if (err) {
        setError(err);
        setLoading(false);
        return;
      }
      onRegistered(email);
    }
    return /* @__PURE__ */ React.createElement("form", { className: "space-y-4", onSubmit }, /* @__PURE__ */ React.createElement("h2", { className: "text-lg font-semibold" }, "Create your account"), error && /* @__PURE__ */ React.createElement(ErrorBanner, null, error), /* @__PURE__ */ React.createElement(
      Field2,
      {
        label: "Display name",
        name: "display_name",
        autoComplete: "name",
        placeholder: "Your name",
        value: displayName,
        onChange: setDisplayName
      }
    ), /* @__PURE__ */ React.createElement(
      Field2,
      {
        label: "Your Email",
        name: "email",
        type: "email",
        autoComplete: "email",
        placeholder: "you@example.com",
        value: email,
        onChange: setEmail,
        required: true
      }
    ), /* @__PURE__ */ React.createElement(
      Field2,
      {
        label: "Your Password",
        name: "password",
        type: "password",
        autoComplete: "new-password",
        placeholder: "At least 8 characters",
        value: password,
        onChange: setPassword,
        required: true
      }
    ), /* @__PURE__ */ React.createElement(
      "label",
      {
        className: "flex cursor-pointer items-start gap-2 text-sm",
        style: { color: "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "checkbox",
          className: "mt-0.5 accent-[var(--tpl-accent)]",
          checked: accepted,
          onChange: (e2) => setAccepted(e2.target.checked)
        }
      ),
      /* @__PURE__ */ React.createElement("span", null, "I accept the", " ", /* @__PURE__ */ React.createElement("a", { href: "#", className: "hover:underline", style: { color: "var(--tpl-accent)" } }, "Terms & Conditions"), " ", "of the website")
    ), /* @__PURE__ */ React.createElement(PrimaryButton, { loading }, "Complete registration"), /* @__PURE__ */ React.createElement("p", { className: "text-center text-sm", style: { color: "var(--tpl-muted)" } }, "Already have an account?", " ", /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: onSwitch,
        className: "font-medium hover:underline",
        style: { color: "var(--tpl-accent)" }
      },
      "Sign in"
    )));
  }
  function TabButton({
    active,
    onClick,
    children
  }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick,
        className: "flex items-center justify-center gap-2 px-4 py-2.5 text-sm font-medium transition",
        style: active ? { background: "var(--tpl-accent)", color: "var(--tpl-accent-contrast)" } : { color: "var(--tpl-muted)" }
      },
      children
    );
  }
  function Field2({
    label,
    name,
    type = "text",
    placeholder,
    value,
    onChange,
    autoComplete,
    required
  }) {
    return /* @__PURE__ */ React.createElement("label", { className: "block" }, /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "mb-1.5 block text-xs font-medium uppercase tracking-wide",
        style: { color: "var(--tpl-muted)" }
      },
      label
    ), /* @__PURE__ */ React.createElement(
      "input",
      {
        name,
        type,
        placeholder,
        value,
        onChange: (e2) => onChange(e2.target.value),
        autoComplete,
        required,
        className: "w-full rounded-lg border bg-transparent px-3.5 py-2.5 text-sm outline-none transition focus:border-[var(--tpl-accent)]",
        style: { borderColor: "var(--tpl-border)" }
      }
    ));
  }
  function PrimaryButton({ children, loading }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "submit",
        disabled: loading,
        className: "w-full rounded-lg px-4 py-2.5 text-sm font-semibold transition hover:opacity-90 disabled:cursor-not-allowed disabled:opacity-60",
        style: { background: "var(--tpl-accent)", color: "var(--tpl-accent-contrast)" }
      },
      loading ? "Please wait\u2026" : children
    );
  }
  function ErrorBanner({ children }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        role: "alert",
        className: "rounded-lg border px-3.5 py-2.5 text-sm",
        style: {
          borderColor: "rgba(239,68,68,0.4)",
          background: "rgba(239,68,68,0.08)",
          color: "#ef4444"
        }
      },
      children
    );
  }
  function SuccessBanner({ children }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        role: "status",
        className: "rounded-lg border px-3.5 py-2.5 text-sm",
        style: {
          borderColor: "rgba(34,197,94,0.4)",
          background: "rgba(34,197,94,0.08)",
          color: "#22c55e"
        }
      },
      children
    );
  }
  function iconProps() {
    return {
      width: 16,
      height: 16,
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeWidth: 2,
      strokeLinecap: "round",
      strokeLinejoin: "round",
      "aria-hidden": true
    };
  }
  function LogInIcon() {
    return /* @__PURE__ */ React.createElement("svg", { ...iconProps() }, /* @__PURE__ */ React.createElement("path", { d: "M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4" }), /* @__PURE__ */ React.createElement("polyline", { points: "10 17 15 12 10 7" }), /* @__PURE__ */ React.createElement("line", { x1: "15", y1: "12", x2: "3", y2: "12" }));
  }
  function UserPlusIcon() {
    return /* @__PURE__ */ React.createElement("svg", { ...iconProps() }, /* @__PURE__ */ React.createElement("path", { d: "M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2" }), /* @__PURE__ */ React.createElement("circle", { cx: "9", cy: "7", r: "4" }), /* @__PURE__ */ React.createElement("line", { x1: "19", y1: "8", x2: "19", y2: "14" }), /* @__PURE__ */ React.createElement("line", { x1: "22", y1: "11", x2: "16", y2: "11" }));
  }

  // frontend/src/templates/v1/views/auth/AuthLanding.tsx
  init_define_import_meta_env();
  function AuthLanding({ defaultTab }) {
    const registering = defaultTab === "register";
    return /* @__PURE__ */ React.createElement("div", { className: "relative flex min-h-screen items-center overflow-hidden" }, /* @__PURE__ */ React.createElement(
      "div",
      {
        "aria-hidden": true,
        className: "pointer-events-none absolute inset-0",
        style: {
          background: "radial-gradient(55% 55% at 18% 22%, rgba(255,94,58,0.14), transparent 70%),radial-gradient(45% 45% at 90% 85%, rgba(56,169,255,0.10), transparent 70%)"
        }
      }
    ), /* @__PURE__ */ React.createElement("div", { className: "relative mx-auto grid w-full max-w-6xl items-center gap-10 px-6 py-16 md:grid-cols-2" }, /* @__PURE__ */ React.createElement("div", { className: "landing-content" }, /* @__PURE__ */ React.createElement(
      Link,
      {
        href: "/",
        className: "inline-flex items-center gap-2 text-sm",
        style: { color: "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(
        "span",
        {
          className: "grid h-8 w-8 place-items-center rounded-lg text-sm font-bold",
          style: { background: "var(--tpl-accent)", color: "var(--tpl-accent-contrast)" }
        },
        "S"
      ),
      "Sky Feeling \xB7 Social Network"
    ), /* @__PURE__ */ React.createElement("h1", { className: "mt-6 text-4xl font-bold leading-[1.1] tracking-tight sm:text-5xl" }, registering ? "Join the biggest social network" : "Welcome to the biggest social network"), /* @__PURE__ */ React.createElement("p", { className: "mt-5 max-w-md text-lg", style: { color: "var(--tpl-muted)" } }, "Share your thoughts, write posts, stream your favourite music, earn badges and much more."), /* @__PURE__ */ React.createElement(
      Link,
      {
        href: registering ? "/login" : "/register",
        className: "mt-8 inline-block rounded-lg border px-5 py-2.5 text-sm font-medium transition hover:bg-white/5",
        style: { borderColor: "var(--tpl-border)" }
      },
      registering ? "I already have an account" : "Register now"
    )), /* @__PURE__ */ React.createElement("div", { className: "flex justify-center md:justify-end" }, /* @__PURE__ */ React.createElement(AuthForm, { defaultTab }))));
  }

  // frontend/src/templates/v1/views/auth/LoginView.tsx
  init_define_import_meta_env();
  function LoginView() {
    return /* @__PURE__ */ React.createElement(AuthLanding, { defaultTab: "login" });
  }

  // frontend/src/templates/v1/views/auth/RegisterView.tsx
  init_define_import_meta_env();
  function RegisterView() {
    return /* @__PURE__ */ React.createElement(AuthLanding, { defaultTab: "register" });
  }

  // frontend/src/templates/v1/views/home/HomeView.tsx
  init_define_import_meta_env();
  var import_react18 = __toESM(require_react_shim());
  var API_BASE4 = process.env.NEXT_PUBLIC_API_BASE_URL ?? "https://api.portal.localhost";
  var INITIAL_FEED = [
    {
      id: "p1",
      author: "Marina Valentine",
      action: /* @__PURE__ */ React.createElement(React.Fragment, null, "shared a ", /* @__PURE__ */ React.createElement(A, null, "link")),
      time: "March 4 at 2:05pm",
      text: /* @__PURE__ */ React.createElement(React.Fragment, null, "Hey ", /* @__PURE__ */ React.createElement(A, null, "Cindi"), ", you should really check out this new song by Iron Maid. The next time they come to the city we should totally go!"),
      media: {
        title: "Iron Maid - ChillGroves",
        desc: "Lorem ipsum dolor sit amet, consectetur ipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua...",
        source: "YOUTUBE.COM"
      },
      likes: 18,
      likedByLabel: /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("b", null, "Jenny"), ", ", /* @__PURE__ */ React.createElement("b", null, "Robert"), " and ", /* @__PURE__ */ React.createElement("b", null, "18 more"), " liked this"),
      comments: 0,
      shares: 16
    },
    {
      id: "p2",
      author: "Elaine Dreyfuss",
      action: "",
      time: "9 hours ago",
      text: "Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempo incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris consequat.",
      likes: 24,
      likedByLabel: /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("b", null, "You"), ", ", /* @__PURE__ */ React.createElement("b", null, "Elaine"), " and ", /* @__PURE__ */ React.createElement("b", null, "22 more"), " liked this"),
      comments: 17,
      shares: 24
    },
    {
      id: "p3",
      author: "James Spiegel",
      action: "",
      time: "38 mins ago",
      text: "Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium der doloremque laudantium, totam rem aperiam.",
      likes: 12,
      likedByLabel: /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("b", null, "Nicholas"), ", ", /* @__PURE__ */ React.createElement("b", null, "Diana"), " and ", /* @__PURE__ */ React.createElement("b", null, "10 more"), " liked this"),
      comments: 3,
      shares: 5
    }
  ];
  function HomeView() {
    const [displayName, setDisplayName] = (0, import_react18.useState)("You");
    const [posts, setPosts] = (0, import_react18.useState)(INITIAL_FEED);
    (0, import_react18.useEffect)(() => {
      let alive = true;
      fetch(`${API_BASE4}/api/v1/auth/me`, { credentials: "include" }).then((r2) => r2.ok ? r2.json() : null).then((me) => {
        if (alive && me?.display_name) setDisplayName(me.display_name);
      }).catch(() => {
      });
      return () => {
        alive = false;
      };
    }, []);
    function handlePost(text) {
      setPosts((prev) => [
        {
          id: `me-${prev.length}-${text.length}`,
          author: displayName,
          action: "",
          time: "Just now",
          text,
          likes: 0,
          likedByLabel: /* @__PURE__ */ React.createElement("span", { style: { color: "var(--tpl-muted)" } }, "Be the first to like this"),
          comments: 0,
          shares: 0
        },
        ...prev
      ]);
    }
    return /* @__PURE__ */ React.createElement("div", { className: "grid gap-5 lg:grid-cols-[260px_minmax(0,1fr)] 2xl:grid-cols-[260px_minmax(0,1fr)_300px]" }, /* @__PURE__ */ React.createElement("div", { className: "hidden space-y-5 lg:block" }, /* @__PURE__ */ React.createElement(WeatherWidget2, null), /* @__PURE__ */ React.createElement(CalendarWidget2, null), /* @__PURE__ */ React.createElement(PagesWidget2, null)), /* @__PURE__ */ React.createElement("div", { className: "min-w-0 space-y-5" }, /* @__PURE__ */ React.createElement(Composer2, { displayName, onPost: handlePost }), posts.map((p) => /* @__PURE__ */ React.createElement(
      PostCard,
      {
        key: p.id,
        post: p,
        onToggleLike: () => setPosts(
          (prev) => prev.map(
            (x) => x.id === p.id ? { ...x, liked: !x.liked, likes: x.likes + (x.liked ? -1 : 1) } : x
          )
        )
      }
    ))), /* @__PURE__ */ React.createElement("aside", { className: "hidden space-y-5 2xl:block" }, /* @__PURE__ */ React.createElement(BirthdayCard2, null), /* @__PURE__ */ React.createElement(FriendSuggestions2, null), /* @__PURE__ */ React.createElement(ActivityFeed2, null)));
  }
  function WeatherWidget2() {
    const week = [
      ["SUN", "weather-sunny-icon", 60],
      ["MON", "weather-sunny-icon", 58],
      ["TUE", "weather-cloudy-icon", 67],
      ["WED", "weather-rain-icon", 70],
      ["THU", "weather-rain-icon", 58],
      ["FRI", "weather-rain-icon", 68],
      ["SAT", "weather-partly-sunny-icon", 65]
    ];
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "overflow-hidden rounded-xl text-white shadow-sm",
        style: { background: "linear-gradient(160deg, var(--tpl-weather-1), var(--tpl-weather-2))" }
      },
      /* @__PURE__ */ React.createElement("div", { className: "p-5" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-start gap-1" }, /* @__PURE__ */ React.createElement("span", { className: "text-5xl font-light leading-none" }, "64\xB0"), /* @__PURE__ */ React.createElement("span", { className: "mt-1 text-xs leading-tight text-white/80" }, "58\xB0", /* @__PURE__ */ React.createElement("br", null), "76\xB0")), /* @__PURE__ */ React.createElement(Icon, { name: "weather-partly-sunny-icon", size: 40 })), /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-lg font-semibold" }, "Partly Sunny"), /* @__PURE__ */ React.createElement("p", { className: "text-xs text-white/80" }, "Real Feel: 67\xB0\xA0\xA0\xA0Chance of Rain: 49%")),
      /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-7 gap-1 px-3 py-4", style: { background: "rgba(255,255,255,0.08)" } }, week.map(([d, ic, t2]) => /* @__PURE__ */ React.createElement("div", { key: d, className: "flex flex-col items-center gap-1.5 text-[10px] text-white/85" }, /* @__PURE__ */ React.createElement("span", { className: "font-semibold" }, d), /* @__PURE__ */ React.createElement(Icon, { name: ic, size: 18 }), /* @__PURE__ */ React.createElement("span", null, t2, "\xB0")))),
      /* @__PURE__ */ React.createElement("div", { className: "px-5 py-3 text-center text-xs" }, /* @__PURE__ */ React.createElement("p", { className: "font-semibold" }, "Saturday, March 26th"), /* @__PURE__ */ React.createElement("p", { className: "text-white/75" }, "San Francisco, CA"))
    );
  }
  function CalendarWidget2() {
    const days = Array.from({ length: 31 }, (_, i) => i + 1);
    return /* @__PURE__ */ React.createElement(WidgetCard2, null, /* @__PURE__ */ React.createElement("div", { className: "mb-3 flex items-center justify-between px-1", style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement("button", { type: "button", "aria-label": "Previous month" }, /* @__PURE__ */ React.createElement(Icon, { name: "popup-left-arrow", size: 12 })), /* @__PURE__ */ React.createElement("span", { className: "font-semibold", style: { color: "var(--tpl-heading)" } }, "May"), /* @__PURE__ */ React.createElement("button", { type: "button", "aria-label": "Next month" }, /* @__PURE__ */ React.createElement(Icon, { name: "popup-right-arrow", size: 12 }))), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-7 gap-y-2 text-center text-[11px]" }, ["MON", "TUE", "WED", "THU", "FRI", "SAT", "SUN"].map((d) => /* @__PURE__ */ React.createElement("span", { key: d, className: "font-semibold", style: { color: "var(--tpl-muted)" } }, d)), days.map((n2) => /* @__PURE__ */ React.createElement(
      "span",
      {
        key: n2,
        className: "grid h-7 place-items-center rounded-full text-xs",
        style: n2 === 12 ? { background: "var(--tpl-accent)", color: "#fff", margin: "0 auto", width: 28 } : { color: "var(--tpl-text)" }
      },
      n2
    ))));
  }
  function PagesWidget2() {
    const pages = [
      { name: "The Marina Bar", cat: "Restaurant \xB7 Bar" },
      { name: "Tapronus Rock", cat: "Rock Band" },
      { name: "Pixel Digital Design", cat: "Company" },
      { name: "Thompson's Custom Clothing Boutique", cat: "Clothing Store" }
    ];
    return /* @__PURE__ */ React.createElement(WidgetCard2, { title: "Pages You May Like", more: true }, /* @__PURE__ */ React.createElement("ul", { className: "space-y-3" }, pages.map((p) => /* @__PURE__ */ React.createElement("li", { key: p.name, className: "flex items-center gap-3" }, /* @__PURE__ */ React.createElement(Avatar, { name: p.name, size: 38 }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, p.name), /* @__PURE__ */ React.createElement("p", { className: "truncate text-xs", style: { color: "var(--tpl-muted)" } }, p.cat)), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "ml-auto text-[var(--tpl-muted)] transition hover:text-[var(--tpl-accent)]",
        "aria-label": "Like page"
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "star-icon", size: 18 })
    )))));
  }
  var TABS2 = [
    { key: "status", label: "Status", icon: "status-icon" },
    { key: "media", label: "Multimedia", icon: "multimedia-icon" },
    { key: "blog", label: "Blog Post", icon: "blog-icon" }
  ];
  function Composer2({
    displayName,
    onPost
  }) {
    const [tab, setTab] = (0, import_react18.useState)("status");
    const [text, setText] = (0, import_react18.useState)("");
    function submit(e2) {
      e2.preventDefault();
      const t2 = text.trim();
      if (!t2) return;
      onPost(t2);
      setText("");
    }
    return /* @__PURE__ */ React.createElement(Card2, { className: "overflow-hidden" }, /* @__PURE__ */ React.createElement("div", { className: "flex border-b", style: { borderColor: "var(--tpl-border)" } }, TABS2.map((t2) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: t2.key,
        type: "button",
        onClick: () => setTab(t2.key),
        className: "flex items-center gap-2 px-5 py-3.5 text-sm font-semibold transition",
        style: tab === t2.key ? { color: "var(--tpl-accent)", boxShadow: "inset 0 -2px 0 var(--tpl-accent)" } : { color: "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: t2.icon, size: 16 }),
      /* @__PURE__ */ React.createElement("span", { className: "hidden sm:inline" }, t2.label)
    ))), /* @__PURE__ */ React.createElement("form", { onSubmit: submit, className: "p-4" }, /* @__PURE__ */ React.createElement("div", { className: "flex gap-3" }, /* @__PURE__ */ React.createElement(Avatar, { name: displayName, size: 40 }), /* @__PURE__ */ React.createElement(
      "textarea",
      {
        value: text,
        onChange: (e2) => setText(e2.target.value),
        rows: 2,
        placeholder: "Share what you are thinking here...",
        className: "min-h-[3rem] w-full resize-none border-0 bg-transparent pt-2 text-sm outline-none placeholder:text-[var(--tpl-muted)]",
        style: { color: "var(--tpl-text)" }
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "mt-2 flex items-center gap-1 border-t pt-3", style: { borderColor: "var(--tpl-border)" } }, /* @__PURE__ */ React.createElement(IconBtn3, { label: "Add photos", icon: "camera-icon" }), /* @__PURE__ */ React.createElement(IconBtn3, { label: "Tag friends", icon: "computer-icon" }), /* @__PURE__ */ React.createElement(IconBtn3, { label: "Add location", icon: "small-pin-icon" }), /* @__PURE__ */ React.createElement("div", { className: "ml-auto flex items-center gap-2" }, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "rounded-md border px-4 py-2 text-sm font-semibold transition hover:bg-[var(--tpl-surface-2)]",
        style: { borderColor: "var(--tpl-border)", color: "var(--tpl-muted)" }
      },
      "Preview"
    ), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "submit",
        disabled: !text.trim(),
        className: "rounded-md px-4 py-2 text-sm font-semibold text-white transition hover:opacity-90 disabled:opacity-50",
        style: { background: "linear-gradient(135deg, var(--tpl-accent), var(--tpl-accent-2))" }
      },
      "Post Status"
    )))));
  }
  function PostCard({ post, onToggleLike }) {
    return /* @__PURE__ */ React.createElement(Card2, { as: "article", className: "relative p-5" }, /* @__PURE__ */ React.createElement("div", { className: "absolute right-0 top-6 hidden translate-x-1/2 flex-col gap-2 sm:flex" }, /* @__PURE__ */ React.createElement(QuickFab, { label: "Award", icon: "trophy-icon" }), /* @__PURE__ */ React.createElement(QuickFab, { active: post.liked, onClick: onToggleLike, label: "Like", icon: "like-post-icon" }), /* @__PURE__ */ React.createElement(QuickFab, { label: "Comment", icon: "comments-post-icon" }), /* @__PURE__ */ React.createElement(QuickFab, { label: "Share", icon: "share-icon" })), /* @__PURE__ */ React.createElement("header", { className: "flex items-center gap-3" }, /* @__PURE__ */ React.createElement(Avatar, { name: post.author, size: 40 }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm" }, /* @__PURE__ */ React.createElement("span", { className: "font-semibold", style: { color: "var(--tpl-heading)" } }, post.author), " ", post.action && /* @__PURE__ */ React.createElement("span", { style: { color: "var(--tpl-muted)" } }, post.action)), /* @__PURE__ */ React.createElement("time", { className: "text-xs", style: { color: "var(--tpl-muted)" } }, post.time)), /* @__PURE__ */ React.createElement("button", { type: "button", className: "ml-auto pr-8 text-[var(--tpl-muted)]", "aria-label": "Post options" }, /* @__PURE__ */ React.createElement(Icon, { name: "three-dots-icon", size: 12 }))), /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-sm leading-relaxed", style: { color: "var(--tpl-text)" } }, post.text), post.media && /* @__PURE__ */ React.createElement(VideoCard2, { media: post.media }), /* @__PURE__ */ React.createElement("footer", { className: "mt-4 flex items-center gap-3 text-sm" }, /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "inline-flex items-center gap-2",
        style: { color: post.liked ? "var(--tpl-accent)" : "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "heart-icon", size: 16 }),
      /* @__PURE__ */ React.createElement("span", null, post.likes)
    ), /* @__PURE__ */ React.createElement(StackedAvatars2, { names: ["Jenny R", "Robert K", "Amy L", "Leo M"] }), /* @__PURE__ */ React.createElement("span", { className: "truncate text-xs", style: { color: "var(--tpl-muted)" } }, post.likedByLabel), /* @__PURE__ */ React.createElement("span", { className: "ml-auto inline-flex items-center gap-4", style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement("span", { className: "inline-flex items-center gap-1.5" }, /* @__PURE__ */ React.createElement(Icon, { name: "speech-balloon-icon", size: 16 }), /* @__PURE__ */ React.createElement("span", { className: "text-xs" }, post.comments)), /* @__PURE__ */ React.createElement("span", { className: "inline-flex items-center gap-1.5" }, /* @__PURE__ */ React.createElement(Icon, { name: "share-icon", size: 16 }), /* @__PURE__ */ React.createElement("span", { className: "text-xs" }, post.shares)))));
  }
  function VideoCard2({ media }) {
    return /* @__PURE__ */ React.createElement("div", { className: "mt-3 flex gap-4 overflow-hidden rounded-lg border", style: { borderColor: "var(--tpl-border)" } }, /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "relative grid h-40 w-44 shrink-0 place-items-center",
        style: { background: "linear-gradient(135deg,#c78a5b,#7c5240)" }
      },
      /* @__PURE__ */ React.createElement(
        "span",
        {
          className: "grid h-14 w-14 place-items-center rounded-full text-white shadow",
          style: { background: "var(--tpl-accent)" }
        },
        /* @__PURE__ */ React.createElement(Icon, { name: "play-icon", size: 20 })
      )
    ), /* @__PURE__ */ React.createElement("div", { className: "min-w-0 py-4 pr-4" }, /* @__PURE__ */ React.createElement("p", { className: "text-base font-semibold", style: { color: "var(--tpl-heading)" } }, media.title), /* @__PURE__ */ React.createElement("p", { className: "mt-1 text-xs leading-relaxed", style: { color: "var(--tpl-muted)" } }, media.desc), /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-[11px] font-semibold tracking-wide", style: { color: "var(--tpl-muted)" } }, media.source)));
  }
  function BirthdayCard2() {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "relative overflow-hidden rounded-xl p-5 text-white shadow-sm",
        style: { background: "linear-gradient(150deg, #8a63d2, #6d4bb8)" }
      },
      /* @__PURE__ */ React.createElement(
        "div",
        {
          "aria-hidden": true,
          className: "pointer-events-none absolute inset-0 opacity-20",
          style: {
            background: "radial-gradient(60% 60% at 85% 15%, rgba(255,255,255,0.5), transparent 60%),radial-gradient(50% 50% at 20% 90%, rgba(255,255,255,0.25), transparent 60%)"
          }
        }
      ),
      /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ React.createElement(Icon, { name: "cupcake-icon", size: 22 }), /* @__PURE__ */ React.createElement("button", { type: "button", "aria-label": "Options" }, /* @__PURE__ */ React.createElement(Icon, { name: "three-dots-icon", size: 12 }))), /* @__PURE__ */ React.createElement("div", { className: "mt-4" }, /* @__PURE__ */ React.createElement(Avatar, { name: "Marina Valentine", size: 44, className: "ring-2 ring-white/40" })), /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-sm text-white/80" }, "Today is"), /* @__PURE__ */ React.createElement("h3", { className: "text-2xl font-bold leading-tight" }, "Marina Valentine's Birthday!"), /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-sm leading-relaxed text-white/80" }, "Leave her a message with your best wishes on her profile page!"))
    );
  }
  function FriendSuggestions2() {
    const people = [
      { name: "Francine Smith", meta: "8 Friends in Common" },
      { name: "Hugh Wilson", meta: "6 Friends in Common" },
      { name: "Karen Masters", meta: "6 Friends in Common" }
    ];
    return /* @__PURE__ */ React.createElement(WidgetCard2, { title: "Friend Suggestions", more: true }, /* @__PURE__ */ React.createElement("ul", { className: "space-y-3" }, people.map((p) => /* @__PURE__ */ React.createElement("li", { key: p.name, className: "flex items-center gap-3" }, /* @__PURE__ */ React.createElement(Avatar, { name: p.name, size: 40 }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm font-semibold", style: { color: "var(--tpl-heading)" } }, p.name), /* @__PURE__ */ React.createElement("p", { className: "truncate text-xs", style: { color: "var(--tpl-muted)" } }, p.meta)), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "ml-auto grid h-8 w-8 place-items-center rounded-md text-white transition hover:opacity-90",
        style: { background: "var(--tpl-blue)" },
        "aria-label": `Add ${p.name}`
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "happy-face-icon", size: 16 })
    )))));
  }
  function ActivityFeed2() {
    const items = [
      { who: "Marina Polson", what: /* @__PURE__ */ React.createElement(React.Fragment, null, "commented on Jason Mark's ", /* @__PURE__ */ React.createElement(A, null, "photo"), "."), time: "2 mins ago", icon: "comments-post-icon" },
      { who: "Jake Parker", what: /* @__PURE__ */ React.createElement(React.Fragment, null, "liked Nicholas Grissom's ", /* @__PURE__ */ React.createElement(A, null, "status update"), "."), time: "5 mins ago", icon: "like-post-icon" },
      { who: "Mary Jane Stark", what: /* @__PURE__ */ React.createElement(React.Fragment, null, "added 20 new photos to her ", /* @__PURE__ */ React.createElement(A, null, "gallery album"), "."), time: "12 mins ago", icon: "photos-icon" },
      { who: "Nicholas Grissom", what: /* @__PURE__ */ React.createElement(React.Fragment, null, "updated his profile ", /* @__PURE__ */ React.createElement(A, null, "photo"), "."), time: "1 hour ago", icon: "happy-face-icon" }
    ];
    return /* @__PURE__ */ React.createElement(WidgetCard2, { title: "Activity Feed" }, /* @__PURE__ */ React.createElement("ul", { className: "space-y-4" }, items.map((it) => /* @__PURE__ */ React.createElement("li", { key: it.who, className: "flex items-start gap-3" }, /* @__PURE__ */ React.createElement(Avatar, { name: it.who, size: 36 }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("p", { className: "text-sm leading-snug", style: { color: "var(--tpl-text)" } }, /* @__PURE__ */ React.createElement("b", { style: { color: "var(--tpl-heading)" } }, it.who), " ", it.what), /* @__PURE__ */ React.createElement("p", { className: "mt-0.5 text-xs", style: { color: "var(--tpl-muted)" } }, it.time)), /* @__PURE__ */ React.createElement("span", { className: "mt-1 shrink-0", style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement(Icon, { name: it.icon, size: 16 }))))));
  }
  function Card2({
    children,
    className = "",
    as: Tag = "div"
  }) {
    return /* @__PURE__ */ React.createElement(
      Tag,
      {
        className: `rounded-xl shadow-sm ${className}`,
        style: { background: "var(--tpl-surface)", border: "1px solid var(--tpl-border)" }
      },
      children
    );
  }
  function WidgetCard2({
    title,
    more,
    children
  }) {
    return /* @__PURE__ */ React.createElement(Card2, { className: "p-4" }, title && /* @__PURE__ */ React.createElement("div", { className: "mb-3 flex items-center justify-between" }, /* @__PURE__ */ React.createElement("h3", { className: "text-sm font-bold", style: { color: "var(--tpl-heading)" } }, title), more && /* @__PURE__ */ React.createElement("span", { style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement(Icon, { name: "three-dots-icon", size: 12 }))), children);
  }
  function A({ children }) {
    return /* @__PURE__ */ React.createElement("a", { href: "#", className: "font-medium hover:underline", style: { color: "var(--tpl-accent)" } }, children);
  }
  function StackedAvatars2({ names }) {
    return /* @__PURE__ */ React.createElement("span", { className: "flex" }, names.map((n2, i) => /* @__PURE__ */ React.createElement("span", { key: n2, className: "rounded-full ring-2 ring-white", style: { marginLeft: i ? -8 : 0 } }, /* @__PURE__ */ React.createElement(Avatar, { name: n2, size: 22 }))));
  }
  function IconBtn3({ label, icon }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        title: label,
        "aria-label": label,
        className: "grid h-9 w-9 place-items-center rounded-lg transition hover:bg-[var(--tpl-surface-2)]",
        style: { color: "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: icon, size: 18 })
    );
  }
  function QuickFab({
    onClick,
    active,
    label,
    icon
  }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick,
        "aria-label": label,
        className: "grid h-[34px] w-[34px] place-items-center rounded-full text-white shadow-sm transition hover:opacity-90",
        style: { background: active ? "var(--tpl-accent)" : "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: icon, size: 16 })
    );
  }

  // frontend/src/templates/v1/views/library/comic/ComicIndexView.tsx
  init_define_import_meta_env();
  function ComicIndexView() {
    return /* @__PURE__ */ React.createElement("section", null, /* @__PURE__ */ React.createElement("header", { className: "mb-6 flex items-center justify-between" }, /* @__PURE__ */ React.createElement("h1", { className: "text-2xl font-semibold" }, "Comics"), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "rounded-md px-3 py-1.5 text-sm font-medium",
        style: {
          background: "var(--tpl-accent)",
          color: "var(--tpl-accent-contrast)"
        }
      },
      "Add book"
    )), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-2 gap-4 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5" }, Array.from({ length: 5 }).map((_, i) => /* @__PURE__ */ React.createElement(
      "div",
      {
        key: i,
        className: "aspect-[3/4] rounded-lg border",
        style: {
          borderColor: "var(--tpl-border)",
          background: "var(--tpl-surface)"
        }
      }
    ))));
  }

  // frontend/src/templates/v1/views/library/novel/NovelDetailView.tsx
  init_define_import_meta_env();
  function NovelDetailView({ id: id2 }) {
    return /* @__PURE__ */ React.createElement("section", null, /* @__PURE__ */ React.createElement("h1", { className: "text-2xl font-semibold" }, "Novel #", id2), /* @__PURE__ */ React.createElement("div", { className: "mt-6 grid gap-6 md:grid-cols-[12rem_1fr]" }, /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "aspect-[3/4] rounded-lg border",
        style: {
          borderColor: "var(--tpl-border)",
          background: "var(--tpl-surface)"
        }
      }
    ), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("p", { style: { color: "var(--tpl-muted)" } }, "Synopsis and chapter list go here."))));
  }

  // frontend/src/templates/v1/views/upload/UploadStudio.tsx
  init_define_import_meta_env();
  var import_react20 = __toESM(require_react_shim());

  // frontend/node_modules/@vidstack/react/prod/vidstack.js
  init_define_import_meta_env();
  init_vidstack_B9fTkxQO();

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-DKRnfION.js
  init_define_import_meta_env();
  init_vidstack_B9fTkxQO();
  init_vidstack_CvezEmSQ();

  // frontend/node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
  init_define_import_meta_env();

  // frontend/node_modules/@floating-ui/core/dist/floating-ui.core.mjs
  init_define_import_meta_env();

  // frontend/node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
  init_define_import_meta_env();
  var min = Math.min;
  var max = Math.max;
  var round2 = Math.round;
  var floor = Math.floor;
  var createCoords = (v) => ({
    x: v,
    y: v
  });
  var oppositeSideMap = {
    left: "right",
    right: "left",
    bottom: "top",
    top: "bottom"
  };
  function clamp(start, value, end) {
    return max(start, min(value, end));
  }
  function evaluate(value, param) {
    return typeof value === "function" ? value(param) : value;
  }
  function getSide(placement) {
    return placement.split("-")[0];
  }
  function getAlignment(placement) {
    return placement.split("-")[1];
  }
  function getOppositeAxis(axis) {
    return axis === "x" ? "y" : "x";
  }
  function getAxisLength(axis) {
    return axis === "y" ? "height" : "width";
  }
  function getSideAxis(placement) {
    const firstChar = placement[0];
    return firstChar === "t" || firstChar === "b" ? "y" : "x";
  }
  function getAlignmentAxis(placement) {
    return getOppositeAxis(getSideAxis(placement));
  }
  function getAlignmentSides(placement, rects, rtl) {
    if (rtl === void 0) {
      rtl = false;
    }
    const alignment = getAlignment(placement);
    const alignmentAxis = getAlignmentAxis(placement);
    const length = getAxisLength(alignmentAxis);
    let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
    if (rects.reference[length] > rects.floating[length]) {
      mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
    }
    return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
  }
  function getExpandedPlacements(placement) {
    const oppositePlacement = getOppositePlacement(placement);
    return [getOppositeAlignmentPlacement(placement), oppositePlacement, getOppositeAlignmentPlacement(oppositePlacement)];
  }
  function getOppositeAlignmentPlacement(placement) {
    return placement.includes("start") ? placement.replace("start", "end") : placement.replace("end", "start");
  }
  var lrPlacement = ["left", "right"];
  var rlPlacement = ["right", "left"];
  var tbPlacement = ["top", "bottom"];
  var btPlacement = ["bottom", "top"];
  function getSideList(side, isStart, rtl) {
    switch (side) {
      case "top":
      case "bottom":
        if (rtl) return isStart ? rlPlacement : lrPlacement;
        return isStart ? lrPlacement : rlPlacement;
      case "left":
      case "right":
        return isStart ? tbPlacement : btPlacement;
      default:
        return [];
    }
  }
  function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
    const alignment = getAlignment(placement);
    let list = getSideList(getSide(placement), direction === "start", rtl);
    if (alignment) {
      list = list.map((side) => side + "-" + alignment);
      if (flipAlignment) {
        list = list.concat(list.map(getOppositeAlignmentPlacement));
      }
    }
    return list;
  }
  function getOppositePlacement(placement) {
    const side = getSide(placement);
    return oppositeSideMap[side] + placement.slice(side.length);
  }
  function expandPaddingObject(padding) {
    return {
      top: 0,
      right: 0,
      bottom: 0,
      left: 0,
      ...padding
    };
  }
  function getPaddingObject(padding) {
    return typeof padding !== "number" ? expandPaddingObject(padding) : {
      top: padding,
      right: padding,
      bottom: padding,
      left: padding
    };
  }
  function rectToClientRect(rect) {
    const {
      x,
      y,
      width,
      height
    } = rect;
    return {
      width,
      height,
      top: y,
      left: x,
      right: x + width,
      bottom: y + height,
      x,
      y
    };
  }

  // frontend/node_modules/@floating-ui/core/dist/floating-ui.core.mjs
  function computeCoordsFromPlacement(_ref, placement, rtl) {
    let {
      reference,
      floating
    } = _ref;
    const sideAxis = getSideAxis(placement);
    const alignmentAxis = getAlignmentAxis(placement);
    const alignLength = getAxisLength(alignmentAxis);
    const side = getSide(placement);
    const isVertical = sideAxis === "y";
    const commonX = reference.x + reference.width / 2 - floating.width / 2;
    const commonY = reference.y + reference.height / 2 - floating.height / 2;
    const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
    let coords;
    switch (side) {
      case "top":
        coords = {
          x: commonX,
          y: reference.y - floating.height
        };
        break;
      case "bottom":
        coords = {
          x: commonX,
          y: reference.y + reference.height
        };
        break;
      case "right":
        coords = {
          x: reference.x + reference.width,
          y: commonY
        };
        break;
      case "left":
        coords = {
          x: reference.x - floating.width,
          y: commonY
        };
        break;
      default:
        coords = {
          x: reference.x,
          y: reference.y
        };
    }
    switch (getAlignment(placement)) {
      case "start":
        coords[alignmentAxis] -= commonAlign * (rtl && isVertical ? -1 : 1);
        break;
      case "end":
        coords[alignmentAxis] += commonAlign * (rtl && isVertical ? -1 : 1);
        break;
    }
    return coords;
  }
  async function detectOverflow(state, options) {
    var _await$platform$isEle;
    if (options === void 0) {
      options = {};
    }
    const {
      x,
      y,
      platform: platform2,
      rects,
      elements,
      strategy
    } = state;
    const {
      boundary = "clippingAncestors",
      rootBoundary = "viewport",
      elementContext = "floating",
      altBoundary = false,
      padding = 0
    } = evaluate(options, state);
    const paddingObject = getPaddingObject(padding);
    const altContext = elementContext === "floating" ? "reference" : "floating";
    const element = elements[altBoundary ? altContext : elementContext];
    const clippingClientRect = rectToClientRect(await platform2.getClippingRect({
      element: ((_await$platform$isEle = await (platform2.isElement == null ? void 0 : platform2.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform2.getDocumentElement == null ? void 0 : platform2.getDocumentElement(elements.floating)),
      boundary,
      rootBoundary,
      strategy
    }));
    const rect = elementContext === "floating" ? {
      x,
      y,
      width: rects.floating.width,
      height: rects.floating.height
    } : rects.reference;
    const offsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(elements.floating));
    const offsetScale = await (platform2.isElement == null ? void 0 : platform2.isElement(offsetParent)) ? await (platform2.getScale == null ? void 0 : platform2.getScale(offsetParent)) || {
      x: 1,
      y: 1
    } : {
      x: 1,
      y: 1
    };
    const elementClientRect = rectToClientRect(platform2.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform2.convertOffsetParentRelativeRectToViewportRelativeRect({
      elements,
      rect,
      offsetParent,
      strategy
    }) : rect);
    return {
      top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
      bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
      left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
      right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
    };
  }
  var MAX_RESET_COUNT = 50;
  var computePosition = async (reference, floating, config) => {
    const {
      placement = "bottom",
      strategy = "absolute",
      middleware = [],
      platform: platform2
    } = config;
    const platformWithDetectOverflow = platform2.detectOverflow ? platform2 : {
      ...platform2,
      detectOverflow
    };
    const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(floating));
    let rects = await platform2.getElementRects({
      reference,
      floating,
      strategy
    });
    let {
      x,
      y
    } = computeCoordsFromPlacement(rects, placement, rtl);
    let statefulPlacement = placement;
    let resetCount = 0;
    const middlewareData = {};
    for (let i = 0; i < middleware.length; i++) {
      const currentMiddleware = middleware[i];
      if (!currentMiddleware) {
        continue;
      }
      const {
        name,
        fn
      } = currentMiddleware;
      const {
        x: nextX,
        y: nextY,
        data,
        reset
      } = await fn({
        x,
        y,
        initialPlacement: placement,
        placement: statefulPlacement,
        strategy,
        middlewareData,
        rects,
        platform: platformWithDetectOverflow,
        elements: {
          reference,
          floating
        }
      });
      x = nextX != null ? nextX : x;
      y = nextY != null ? nextY : y;
      middlewareData[name] = {
        ...middlewareData[name],
        ...data
      };
      if (reset && resetCount < MAX_RESET_COUNT) {
        resetCount++;
        if (typeof reset === "object") {
          if (reset.placement) {
            statefulPlacement = reset.placement;
          }
          if (reset.rects) {
            rects = reset.rects === true ? await platform2.getElementRects({
              reference,
              floating,
              strategy
            }) : reset.rects;
          }
          ({
            x,
            y
          } = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
        }
        i = -1;
      }
    }
    return {
      x,
      y,
      placement: statefulPlacement,
      strategy,
      middlewareData
    };
  };
  var flip = function(options) {
    if (options === void 0) {
      options = {};
    }
    return {
      name: "flip",
      options,
      async fn(state) {
        var _middlewareData$arrow, _middlewareData$flip;
        const {
          placement,
          middlewareData,
          rects,
          initialPlacement,
          platform: platform2,
          elements
        } = state;
        const {
          mainAxis: checkMainAxis = true,
          crossAxis: checkCrossAxis = true,
          fallbackPlacements: specifiedFallbackPlacements,
          fallbackStrategy = "bestFit",
          fallbackAxisSideDirection = "none",
          flipAlignment = true,
          ...detectOverflowOptions
        } = evaluate(options, state);
        if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
          return {};
        }
        const side = getSide(placement);
        const initialSideAxis = getSideAxis(initialPlacement);
        const isBasePlacement = getSide(initialPlacement) === initialPlacement;
        const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
        const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
        const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
        if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) {
          fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
        }
        const placements2 = [initialPlacement, ...fallbackPlacements];
        const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
        const overflows = [];
        let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
        if (checkMainAxis) {
          overflows.push(overflow[side]);
        }
        if (checkCrossAxis) {
          const sides2 = getAlignmentSides(placement, rects, rtl);
          overflows.push(overflow[sides2[0]], overflow[sides2[1]]);
        }
        overflowsData = [...overflowsData, {
          placement,
          overflows
        }];
        if (!overflows.every((side2) => side2 <= 0)) {
          var _middlewareData$flip2, _overflowsData$filter;
          const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
          const nextPlacement = placements2[nextIndex];
          if (nextPlacement) {
            const ignoreCrossAxisOverflow = checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false;
            if (!ignoreCrossAxisOverflow || // We leave the current main axis only if every placement on that axis
            // overflows the main axis.
            overflowsData.every((d) => getSideAxis(d.placement) === initialSideAxis ? d.overflows[0] > 0 : true)) {
              return {
                data: {
                  index: nextIndex,
                  overflows: overflowsData
                },
                reset: {
                  placement: nextPlacement
                }
              };
            }
          }
          let resetPlacement = (_overflowsData$filter = overflowsData.filter((d) => d.overflows[0] <= 0).sort((a, b) => a.overflows[1] - b.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
          if (!resetPlacement) {
            switch (fallbackStrategy) {
              case "bestFit": {
                var _overflowsData$filter2;
                const placement2 = (_overflowsData$filter2 = overflowsData.filter((d) => {
                  if (hasFallbackAxisSideDirection) {
                    const currentSideAxis = getSideAxis(d.placement);
                    return currentSideAxis === initialSideAxis || // Create a bias to the `y` side axis due to horizontal
                    // reading directions favoring greater width.
                    currentSideAxis === "y";
                  }
                  return true;
                }).map((d) => [d.placement, d.overflows.filter((overflow2) => overflow2 > 0).reduce((acc, overflow2) => acc + overflow2, 0)]).sort((a, b) => a[1] - b[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
                if (placement2) {
                  resetPlacement = placement2;
                }
                break;
              }
              case "initialPlacement":
                resetPlacement = initialPlacement;
                break;
            }
          }
          if (placement !== resetPlacement) {
            return {
              reset: {
                placement: resetPlacement
              }
            };
          }
        }
        return {};
      }
    };
  };
  var shift = function(options) {
    if (options === void 0) {
      options = {};
    }
    return {
      name: "shift",
      options,
      async fn(state) {
        const {
          x,
          y,
          placement,
          platform: platform2
        } = state;
        const {
          mainAxis: checkMainAxis = true,
          crossAxis: checkCrossAxis = false,
          limiter = {
            fn: (_ref) => {
              let {
                x: x2,
                y: y2
              } = _ref;
              return {
                x: x2,
                y: y2
              };
            }
          },
          ...detectOverflowOptions
        } = evaluate(options, state);
        const coords = {
          x,
          y
        };
        const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
        const crossAxis = getSideAxis(getSide(placement));
        const mainAxis = getOppositeAxis(crossAxis);
        let mainAxisCoord = coords[mainAxis];
        let crossAxisCoord = coords[crossAxis];
        if (checkMainAxis) {
          const minSide = mainAxis === "y" ? "top" : "left";
          const maxSide = mainAxis === "y" ? "bottom" : "right";
          const min2 = mainAxisCoord + overflow[minSide];
          const max2 = mainAxisCoord - overflow[maxSide];
          mainAxisCoord = clamp(min2, mainAxisCoord, max2);
        }
        if (checkCrossAxis) {
          const minSide = crossAxis === "y" ? "top" : "left";
          const maxSide = crossAxis === "y" ? "bottom" : "right";
          const min2 = crossAxisCoord + overflow[minSide];
          const max2 = crossAxisCoord - overflow[maxSide];
          crossAxisCoord = clamp(min2, crossAxisCoord, max2);
        }
        const limitedCoords = limiter.fn({
          ...state,
          [mainAxis]: mainAxisCoord,
          [crossAxis]: crossAxisCoord
        });
        return {
          ...limitedCoords,
          data: {
            x: limitedCoords.x - x,
            y: limitedCoords.y - y,
            enabled: {
              [mainAxis]: checkMainAxis,
              [crossAxis]: checkCrossAxis
            }
          }
        };
      }
    };
  };

  // frontend/node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
  init_define_import_meta_env();
  function hasWindow() {
    return typeof window !== "undefined";
  }
  function getNodeName(node) {
    if (isNode(node)) {
      return (node.nodeName || "").toLowerCase();
    }
    return "#document";
  }
  function getWindow(node) {
    var _node$ownerDocument;
    return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
  }
  function getDocumentElement(node) {
    var _ref;
    return (_ref = (isNode(node) ? node.ownerDocument : node.document) || window.document) == null ? void 0 : _ref.documentElement;
  }
  function isNode(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof Node || value instanceof getWindow(value).Node;
  }
  function isElement(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof Element || value instanceof getWindow(value).Element;
  }
  function isHTMLElement(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
  }
  function isShadowRoot(value) {
    if (!hasWindow() || typeof ShadowRoot === "undefined") {
      return false;
    }
    return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
  }
  function isOverflowElement(element) {
    const {
      overflow,
      overflowX,
      overflowY,
      display
    } = getComputedStyle2(element);
    return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && display !== "inline" && display !== "contents";
  }
  function isTableElement(element) {
    return /^(table|td|th)$/.test(getNodeName(element));
  }
  function isTopLayer(element) {
    try {
      if (element.matches(":popover-open")) {
        return true;
      }
    } catch (_e2) {
    }
    try {
      return element.matches(":modal");
    } catch (_e2) {
      return false;
    }
  }
  var willChangeRe = /transform|translate|scale|rotate|perspective|filter/;
  var containRe = /paint|layout|strict|content/;
  var isNotNone = (value) => !!value && value !== "none";
  var isWebKitValue;
  function isContainingBlock(elementOrCss) {
    const css = isElement(elementOrCss) ? getComputedStyle2(elementOrCss) : elementOrCss;
    return isNotNone(css.transform) || isNotNone(css.translate) || isNotNone(css.scale) || isNotNone(css.rotate) || isNotNone(css.perspective) || !isWebKit() && (isNotNone(css.backdropFilter) || isNotNone(css.filter)) || willChangeRe.test(css.willChange || "") || containRe.test(css.contain || "");
  }
  function getContainingBlock(element) {
    let currentNode = getParentNode(element);
    while (isHTMLElement(currentNode) && !isLastTraversableNode(currentNode)) {
      if (isContainingBlock(currentNode)) {
        return currentNode;
      } else if (isTopLayer(currentNode)) {
        return null;
      }
      currentNode = getParentNode(currentNode);
    }
    return null;
  }
  function isWebKit() {
    if (isWebKitValue == null) {
      isWebKitValue = typeof CSS !== "undefined" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none");
    }
    return isWebKitValue;
  }
  function isLastTraversableNode(node) {
    return /^(html|body|#document)$/.test(getNodeName(node));
  }
  function getComputedStyle2(element) {
    return getWindow(element).getComputedStyle(element);
  }
  function getNodeScroll(element) {
    if (isElement(element)) {
      return {
        scrollLeft: element.scrollLeft,
        scrollTop: element.scrollTop
      };
    }
    return {
      scrollLeft: element.scrollX,
      scrollTop: element.scrollY
    };
  }
  function getParentNode(node) {
    if (getNodeName(node) === "html") {
      return node;
    }
    const result = (
      // Step into the shadow DOM of the parent of a slotted node.
      node.assignedSlot || // DOM Element detected.
      node.parentNode || // ShadowRoot detected.
      isShadowRoot(node) && node.host || // Fallback.
      getDocumentElement(node)
    );
    return isShadowRoot(result) ? result.host : result;
  }
  function getNearestOverflowAncestor(node) {
    const parentNode = getParentNode(node);
    if (isLastTraversableNode(parentNode)) {
      return node.ownerDocument ? node.ownerDocument.body : node.body;
    }
    if (isHTMLElement(parentNode) && isOverflowElement(parentNode)) {
      return parentNode;
    }
    return getNearestOverflowAncestor(parentNode);
  }
  function getOverflowAncestors(node, list, traverseIframes) {
    var _node$ownerDocument2;
    if (list === void 0) {
      list = [];
    }
    if (traverseIframes === void 0) {
      traverseIframes = true;
    }
    const scrollableAncestor = getNearestOverflowAncestor(node);
    const isBody = scrollableAncestor === ((_node$ownerDocument2 = node.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
    const win = getWindow(scrollableAncestor);
    if (isBody) {
      const frameElement = getFrameElement(win);
      return list.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
    } else {
      return list.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
    }
  }
  function getFrameElement(win) {
    return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
  }

  // frontend/node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
  function getCssDimensions(element) {
    const css = getComputedStyle2(element);
    let width = parseFloat(css.width) || 0;
    let height = parseFloat(css.height) || 0;
    const hasOffset = isHTMLElement(element);
    const offsetWidth = hasOffset ? element.offsetWidth : width;
    const offsetHeight = hasOffset ? element.offsetHeight : height;
    const shouldFallback = round2(width) !== offsetWidth || round2(height) !== offsetHeight;
    if (shouldFallback) {
      width = offsetWidth;
      height = offsetHeight;
    }
    return {
      width,
      height,
      $: shouldFallback
    };
  }
  function unwrapElement(element) {
    return !isElement(element) ? element.contextElement : element;
  }
  function getScale(element) {
    const domElement = unwrapElement(element);
    if (!isHTMLElement(domElement)) {
      return createCoords(1);
    }
    const rect = domElement.getBoundingClientRect();
    const {
      width,
      height,
      $
    } = getCssDimensions(domElement);
    let x = ($ ? round2(rect.width) : rect.width) / width;
    let y = ($ ? round2(rect.height) : rect.height) / height;
    if (!x || !Number.isFinite(x)) {
      x = 1;
    }
    if (!y || !Number.isFinite(y)) {
      y = 1;
    }
    return {
      x,
      y
    };
  }
  var noOffsets = /* @__PURE__ */ createCoords(0);
  function getVisualOffsets(element) {
    const win = getWindow(element);
    if (!isWebKit() || !win.visualViewport) {
      return noOffsets;
    }
    return {
      x: win.visualViewport.offsetLeft,
      y: win.visualViewport.offsetTop
    };
  }
  function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
    if (isFixed === void 0) {
      isFixed = false;
    }
    if (!floatingOffsetParent || isFixed && floatingOffsetParent !== getWindow(element)) {
      return false;
    }
    return isFixed;
  }
  function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
    if (includeScale === void 0) {
      includeScale = false;
    }
    if (isFixedStrategy === void 0) {
      isFixedStrategy = false;
    }
    const clientRect = element.getBoundingClientRect();
    const domElement = unwrapElement(element);
    let scale = createCoords(1);
    if (includeScale) {
      if (offsetParent) {
        if (isElement(offsetParent)) {
          scale = getScale(offsetParent);
        }
      } else {
        scale = getScale(element);
      }
    }
    const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
    let x = (clientRect.left + visualOffsets.x) / scale.x;
    let y = (clientRect.top + visualOffsets.y) / scale.y;
    let width = clientRect.width / scale.x;
    let height = clientRect.height / scale.y;
    if (domElement) {
      const win = getWindow(domElement);
      const offsetWin = offsetParent && isElement(offsetParent) ? getWindow(offsetParent) : offsetParent;
      let currentWin = win;
      let currentIFrame = getFrameElement(currentWin);
      while (currentIFrame && offsetParent && offsetWin !== currentWin) {
        const iframeScale = getScale(currentIFrame);
        const iframeRect = currentIFrame.getBoundingClientRect();
        const css = getComputedStyle2(currentIFrame);
        const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
        const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
        x *= iframeScale.x;
        y *= iframeScale.y;
        width *= iframeScale.x;
        height *= iframeScale.y;
        x += left;
        y += top;
        currentWin = getWindow(currentIFrame);
        currentIFrame = getFrameElement(currentWin);
      }
    }
    return rectToClientRect({
      width,
      height,
      x,
      y
    });
  }
  function getWindowScrollBarX(element, rect) {
    const leftScroll = getNodeScroll(element).scrollLeft;
    if (!rect) {
      return getBoundingClientRect(getDocumentElement(element)).left + leftScroll;
    }
    return rect.left + leftScroll;
  }
  function getHTMLOffset(documentElement, scroll) {
    const htmlRect = documentElement.getBoundingClientRect();
    const x = htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect);
    const y = htmlRect.top + scroll.scrollTop;
    return {
      x,
      y
    };
  }
  function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
    let {
      elements,
      rect,
      offsetParent,
      strategy
    } = _ref;
    const isFixed = strategy === "fixed";
    const documentElement = getDocumentElement(offsetParent);
    const topLayer = elements ? isTopLayer(elements.floating) : false;
    if (offsetParent === documentElement || topLayer && isFixed) {
      return rect;
    }
    let scroll = {
      scrollLeft: 0,
      scrollTop: 0
    };
    let scale = createCoords(1);
    const offsets = createCoords(0);
    const isOffsetParentAnElement = isHTMLElement(offsetParent);
    if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
      if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
        scroll = getNodeScroll(offsetParent);
      }
      if (isOffsetParentAnElement) {
        const offsetRect = getBoundingClientRect(offsetParent);
        scale = getScale(offsetParent);
        offsets.x = offsetRect.x + offsetParent.clientLeft;
        offsets.y = offsetRect.y + offsetParent.clientTop;
      }
    }
    const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
    return {
      width: rect.width * scale.x,
      height: rect.height * scale.y,
      x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x + htmlOffset.x,
      y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y + htmlOffset.y
    };
  }
  function getClientRects(element) {
    return Array.from(element.getClientRects());
  }
  function getDocumentRect(element) {
    const html = getDocumentElement(element);
    const scroll = getNodeScroll(element);
    const body = element.ownerDocument.body;
    const width = max(html.scrollWidth, html.clientWidth, body.scrollWidth, body.clientWidth);
    const height = max(html.scrollHeight, html.clientHeight, body.scrollHeight, body.clientHeight);
    let x = -scroll.scrollLeft + getWindowScrollBarX(element);
    const y = -scroll.scrollTop;
    if (getComputedStyle2(body).direction === "rtl") {
      x += max(html.clientWidth, body.clientWidth) - width;
    }
    return {
      width,
      height,
      x,
      y
    };
  }
  var SCROLLBAR_MAX = 25;
  function getViewportRect(element, strategy) {
    const win = getWindow(element);
    const html = getDocumentElement(element);
    const visualViewport = win.visualViewport;
    let width = html.clientWidth;
    let height = html.clientHeight;
    let x = 0;
    let y = 0;
    if (visualViewport) {
      width = visualViewport.width;
      height = visualViewport.height;
      const visualViewportBased = isWebKit();
      if (!visualViewportBased || visualViewportBased && strategy === "fixed") {
        x = visualViewport.offsetLeft;
        y = visualViewport.offsetTop;
      }
    }
    const windowScrollbarX = getWindowScrollBarX(html);
    if (windowScrollbarX <= 0) {
      const doc = html.ownerDocument;
      const body = doc.body;
      const bodyStyles = getComputedStyle(body);
      const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
      const clippingStableScrollbarWidth = Math.abs(html.clientWidth - body.clientWidth - bodyMarginInline);
      if (clippingStableScrollbarWidth <= SCROLLBAR_MAX) {
        width -= clippingStableScrollbarWidth;
      }
    } else if (windowScrollbarX <= SCROLLBAR_MAX) {
      width += windowScrollbarX;
    }
    return {
      width,
      height,
      x,
      y
    };
  }
  function getInnerBoundingClientRect(element, strategy) {
    const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
    const top = clientRect.top + element.clientTop;
    const left = clientRect.left + element.clientLeft;
    const scale = isHTMLElement(element) ? getScale(element) : createCoords(1);
    const width = element.clientWidth * scale.x;
    const height = element.clientHeight * scale.y;
    const x = left * scale.x;
    const y = top * scale.y;
    return {
      width,
      height,
      x,
      y
    };
  }
  function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
    let rect;
    if (clippingAncestor === "viewport") {
      rect = getViewportRect(element, strategy);
    } else if (clippingAncestor === "document") {
      rect = getDocumentRect(getDocumentElement(element));
    } else if (isElement(clippingAncestor)) {
      rect = getInnerBoundingClientRect(clippingAncestor, strategy);
    } else {
      const visualOffsets = getVisualOffsets(element);
      rect = {
        x: clippingAncestor.x - visualOffsets.x,
        y: clippingAncestor.y - visualOffsets.y,
        width: clippingAncestor.width,
        height: clippingAncestor.height
      };
    }
    return rectToClientRect(rect);
  }
  function hasFixedPositionAncestor(element, stopNode) {
    const parentNode = getParentNode(element);
    if (parentNode === stopNode || !isElement(parentNode) || isLastTraversableNode(parentNode)) {
      return false;
    }
    return getComputedStyle2(parentNode).position === "fixed" || hasFixedPositionAncestor(parentNode, stopNode);
  }
  function getClippingElementAncestors(element, cache2) {
    const cachedResult = cache2.get(element);
    if (cachedResult) {
      return cachedResult;
    }
    let result = getOverflowAncestors(element, [], false).filter((el) => isElement(el) && getNodeName(el) !== "body");
    let currentContainingBlockComputedStyle = null;
    const elementIsFixed = getComputedStyle2(element).position === "fixed";
    let currentNode = elementIsFixed ? getParentNode(element) : element;
    while (isElement(currentNode) && !isLastTraversableNode(currentNode)) {
      const computedStyle = getComputedStyle2(currentNode);
      const currentNodeIsContaining = isContainingBlock(currentNode);
      if (!currentNodeIsContaining && computedStyle.position === "fixed") {
        currentContainingBlockComputedStyle = null;
      }
      const shouldDropCurrentNode = elementIsFixed ? !currentNodeIsContaining && !currentContainingBlockComputedStyle : !currentNodeIsContaining && computedStyle.position === "static" && !!currentContainingBlockComputedStyle && (currentContainingBlockComputedStyle.position === "absolute" || currentContainingBlockComputedStyle.position === "fixed") || isOverflowElement(currentNode) && !currentNodeIsContaining && hasFixedPositionAncestor(element, currentNode);
      if (shouldDropCurrentNode) {
        result = result.filter((ancestor) => ancestor !== currentNode);
      } else {
        currentContainingBlockComputedStyle = computedStyle;
      }
      currentNode = getParentNode(currentNode);
    }
    cache2.set(element, result);
    return result;
  }
  function getClippingRect(_ref) {
    let {
      element,
      boundary,
      rootBoundary,
      strategy
    } = _ref;
    const elementClippingAncestors = boundary === "clippingAncestors" ? isTopLayer(element) ? [] : getClippingElementAncestors(element, this._c) : [].concat(boundary);
    const clippingAncestors = [...elementClippingAncestors, rootBoundary];
    const firstRect = getClientRectFromClippingAncestor(element, clippingAncestors[0], strategy);
    let top = firstRect.top;
    let right = firstRect.right;
    let bottom = firstRect.bottom;
    let left = firstRect.left;
    for (let i = 1; i < clippingAncestors.length; i++) {
      const rect = getClientRectFromClippingAncestor(element, clippingAncestors[i], strategy);
      top = max(rect.top, top);
      right = min(rect.right, right);
      bottom = min(rect.bottom, bottom);
      left = max(rect.left, left);
    }
    return {
      width: right - left,
      height: bottom - top,
      x: left,
      y: top
    };
  }
  function getDimensions(element) {
    const {
      width,
      height
    } = getCssDimensions(element);
    return {
      width,
      height
    };
  }
  function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
    const isOffsetParentAnElement = isHTMLElement(offsetParent);
    const documentElement = getDocumentElement(offsetParent);
    const isFixed = strategy === "fixed";
    const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
    let scroll = {
      scrollLeft: 0,
      scrollTop: 0
    };
    const offsets = createCoords(0);
    function setLeftRTLScrollbarOffset() {
      offsets.x = getWindowScrollBarX(documentElement);
    }
    if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
      if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
        scroll = getNodeScroll(offsetParent);
      }
      if (isOffsetParentAnElement) {
        const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
        offsets.x = offsetRect.x + offsetParent.clientLeft;
        offsets.y = offsetRect.y + offsetParent.clientTop;
      } else if (documentElement) {
        setLeftRTLScrollbarOffset();
      }
    }
    if (isFixed && !isOffsetParentAnElement && documentElement) {
      setLeftRTLScrollbarOffset();
    }
    const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
    const x = rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x;
    const y = rect.top + scroll.scrollTop - offsets.y - htmlOffset.y;
    return {
      x,
      y,
      width: rect.width,
      height: rect.height
    };
  }
  function isStaticPositioned(element) {
    return getComputedStyle2(element).position === "static";
  }
  function getTrueOffsetParent(element, polyfill) {
    if (!isHTMLElement(element) || getComputedStyle2(element).position === "fixed") {
      return null;
    }
    if (polyfill) {
      return polyfill(element);
    }
    let rawOffsetParent = element.offsetParent;
    if (getDocumentElement(element) === rawOffsetParent) {
      rawOffsetParent = rawOffsetParent.ownerDocument.body;
    }
    return rawOffsetParent;
  }
  function getOffsetParent(element, polyfill) {
    const win = getWindow(element);
    if (isTopLayer(element)) {
      return win;
    }
    if (!isHTMLElement(element)) {
      let svgOffsetParent = getParentNode(element);
      while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
        if (isElement(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) {
          return svgOffsetParent;
        }
        svgOffsetParent = getParentNode(svgOffsetParent);
      }
      return win;
    }
    let offsetParent = getTrueOffsetParent(element, polyfill);
    while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) {
      offsetParent = getTrueOffsetParent(offsetParent, polyfill);
    }
    if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) {
      return win;
    }
    return offsetParent || getContainingBlock(element) || win;
  }
  var getElementRects = async function(data) {
    const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
    const getDimensionsFn = this.getDimensions;
    const floatingDimensions = await getDimensionsFn(data.floating);
    return {
      reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
      floating: {
        x: 0,
        y: 0,
        width: floatingDimensions.width,
        height: floatingDimensions.height
      }
    };
  };
  function isRTL(element) {
    return getComputedStyle2(element).direction === "rtl";
  }
  var platform = {
    convertOffsetParentRelativeRectToViewportRelativeRect,
    getDocumentElement,
    getClippingRect,
    getOffsetParent,
    getElementRects,
    getClientRects,
    getDimensions,
    getScale,
    isElement,
    isRTL
  };
  function rectsAreEqual(a, b) {
    return a.x === b.x && a.y === b.y && a.width === b.width && a.height === b.height;
  }
  function observeMove(element, onMove) {
    let io = null;
    let timeoutId;
    const root2 = getDocumentElement(element);
    function cleanup2() {
      var _io;
      clearTimeout(timeoutId);
      (_io = io) == null || _io.disconnect();
      io = null;
    }
    function refresh(skip, threshold) {
      if (skip === void 0) {
        skip = false;
      }
      if (threshold === void 0) {
        threshold = 1;
      }
      cleanup2();
      const elementRectForRootMargin = element.getBoundingClientRect();
      const {
        left,
        top,
        width,
        height
      } = elementRectForRootMargin;
      if (!skip) {
        onMove();
      }
      if (!width || !height) {
        return;
      }
      const insetTop = floor(top);
      const insetRight = floor(root2.clientWidth - (left + width));
      const insetBottom = floor(root2.clientHeight - (top + height));
      const insetLeft = floor(left);
      const rootMargin = -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px";
      const options = {
        rootMargin,
        threshold: max(0, min(1, threshold)) || 1
      };
      let isFirstUpdate = true;
      function handleObserve(entries) {
        const ratio = entries[0].intersectionRatio;
        if (ratio !== threshold) {
          if (!isFirstUpdate) {
            return refresh();
          }
          if (!ratio) {
            timeoutId = setTimeout(() => {
              refresh(false, 1e-7);
            }, 1e3);
          } else {
            refresh(false, ratio);
          }
        }
        if (ratio === 1 && !rectsAreEqual(elementRectForRootMargin, element.getBoundingClientRect())) {
          refresh();
        }
        isFirstUpdate = false;
      }
      try {
        io = new IntersectionObserver(handleObserve, {
          ...options,
          // Handle <iframe>s
          root: root2.ownerDocument
        });
      } catch (_e2) {
        io = new IntersectionObserver(handleObserve, options);
      }
      io.observe(element);
    }
    refresh(true);
    return cleanup2;
  }
  function autoUpdate(reference, floating, update2, options) {
    if (options === void 0) {
      options = {};
    }
    const {
      ancestorScroll = true,
      ancestorResize = true,
      elementResize = typeof ResizeObserver === "function",
      layoutShift = typeof IntersectionObserver === "function",
      animationFrame = false
    } = options;
    const referenceEl = unwrapElement(reference);
    const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...floating ? getOverflowAncestors(floating) : []] : [];
    ancestors.forEach((ancestor) => {
      ancestorScroll && ancestor.addEventListener("scroll", update2, {
        passive: true
      });
      ancestorResize && ancestor.addEventListener("resize", update2);
    });
    const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update2) : null;
    let reobserveFrame = -1;
    let resizeObserver = null;
    if (elementResize) {
      resizeObserver = new ResizeObserver((_ref) => {
        let [firstEntry] = _ref;
        if (firstEntry && firstEntry.target === referenceEl && resizeObserver && floating) {
          resizeObserver.unobserve(floating);
          cancelAnimationFrame(reobserveFrame);
          reobserveFrame = requestAnimationFrame(() => {
            var _resizeObserver;
            (_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
          });
        }
        update2();
      });
      if (referenceEl && !animationFrame) {
        resizeObserver.observe(referenceEl);
      }
      if (floating) {
        resizeObserver.observe(floating);
      }
    }
    let frameId;
    let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
    if (animationFrame) {
      frameLoop();
    }
    function frameLoop() {
      const nextRefRect = getBoundingClientRect(reference);
      if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) {
        update2();
      }
      prevRefRect = nextRefRect;
      frameId = requestAnimationFrame(frameLoop);
    }
    update2();
    return () => {
      var _resizeObserver2;
      ancestors.forEach((ancestor) => {
        ancestorScroll && ancestor.removeEventListener("scroll", update2);
        ancestorResize && ancestor.removeEventListener("resize", update2);
      });
      cleanupIo == null || cleanupIo();
      (_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
      resizeObserver = null;
      if (animationFrame) {
        cancelAnimationFrame(frameId);
      }
    };
  }
  var shift2 = shift;
  var flip2 = flip;
  var computePosition2 = (reference, floating, options) => {
    const cache2 = /* @__PURE__ */ new Map();
    const mergedOptions = {
      platform,
      ...options
    };
    const platformWithCache = {
      ...mergedOptions.platform,
      _c: cache2
    };
    return computePosition(reference, floating, {
      ...mergedOptions,
      platform: platformWithCache
    });
  };

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-DKRnfION.js
  var React5 = __toESM(require_react_shim(), 1);
  function isVideoQualitySrc(src) {
    return !isString(src) && "width" in src && "height" in src && isNumber(src.width) && isNumber(src.height);
  }
  var mediaState = new State({
    artist: "",
    artwork: null,
    audioTrack: null,
    audioTracks: [],
    autoPlay: false,
    autoPlayError: null,
    audioGain: null,
    buffered: new TimeRange(),
    canLoad: false,
    canLoadPoster: false,
    canFullscreen: false,
    canOrientScreen: canOrientScreen(),
    canPictureInPicture: false,
    canPlay: false,
    clipStartTime: 0,
    clipEndTime: 0,
    controls: false,
    get iOSControls() {
      return IS_IPHONE && this.mediaType === "video" && (!this.playsInline || !fscreen.fullscreenEnabled && this.fullscreen);
    },
    get nativeControls() {
      return this.controls || this.iOSControls;
    },
    controlsVisible: false,
    get controlsHidden() {
      return !this.controlsVisible;
    },
    crossOrigin: null,
    ended: false,
    error: null,
    fullscreen: false,
    get loop() {
      return this.providedLoop || this.userPrefersLoop;
    },
    logLevel: "silent",
    mediaType: "unknown",
    muted: false,
    paused: true,
    played: new TimeRange(),
    playing: false,
    playsInline: false,
    pictureInPicture: false,
    preload: "metadata",
    playbackRate: 1,
    qualities: [],
    quality: null,
    autoQuality: false,
    canSetQuality: true,
    canSetPlaybackRate: true,
    canSetVolume: false,
    canSetAudioGain: false,
    seekable: new TimeRange(),
    seeking: false,
    source: {
      src: "",
      type: ""
    },
    sources: [],
    started: false,
    textTracks: [],
    textTrack: null,
    get hasCaptions() {
      return this.textTracks.filter(isTrackCaptionKind).length > 0;
    },
    volume: 1,
    waiting: false,
    realCurrentTime: 0,
    get currentTime() {
      return this.ended ? this.duration : this.clipStartTime > 0 ? Math.max(0, Math.min(this.realCurrentTime - this.clipStartTime, this.duration)) : this.realCurrentTime;
    },
    providedDuration: -1,
    intrinsicDuration: 0,
    get duration() {
      return this.seekableWindow;
    },
    get title() {
      return this.providedTitle || this.inferredTitle;
    },
    get poster() {
      return this.providedPoster || this.inferredPoster;
    },
    get viewType() {
      return this.providedViewType !== "unknown" ? this.providedViewType : this.inferredViewType;
    },
    get streamType() {
      return this.providedStreamType !== "unknown" ? this.providedStreamType : this.inferredStreamType;
    },
    get currentSrc() {
      return this.source;
    },
    get bufferedStart() {
      const start = getTimeRangesStart(this.buffered) ?? 0;
      return Math.max(start, this.clipStartTime);
    },
    get bufferedEnd() {
      const end = getTimeRangesEnd(this.buffered) ?? 0;
      return Math.min(this.seekableEnd, Math.max(0, end - this.clipStartTime));
    },
    get bufferedWindow() {
      return Math.max(0, this.bufferedEnd - this.bufferedStart);
    },
    get seekableStart() {
      if (this.isLiveDVR && this.liveDVRWindow > 0) return Math.max(0, this.seekableEnd - this.liveDVRWindow);
      const start = getTimeRangesStart(this.seekable) ?? 0;
      return Math.max(start, this.clipStartTime);
    },
    get seekableEnd() {
      if (this.providedDuration > 0) return this.providedDuration;
      const end = this.liveSyncPosition > 0 ? this.liveSyncPosition : this.canPlay ? getTimeRangesEnd(this.seekable) ?? Infinity : 0;
      return this.clipEndTime > 0 ? Math.min(this.clipEndTime, end) : end;
    },
    get seekableWindow() {
      const window2 = this.seekableEnd - this.seekableStart;
      return !isNaN(window2) ? Math.max(0, window2) : Infinity;
    },
    canAirPlay: false,
    canGoogleCast: false,
    remotePlaybackState: "disconnected",
    remotePlaybackType: "none",
    remotePlaybackLoader: null,
    remotePlaybackInfo: null,
    get isAirPlayConnected() {
      return this.remotePlaybackType === "airplay" && this.remotePlaybackState === "connected";
    },
    get isGoogleCastConnected() {
      return this.remotePlaybackType === "google-cast" && this.remotePlaybackState === "connected";
    },
    pointer: "fine",
    orientation: "landscape",
    width: 0,
    height: 0,
    mediaWidth: 0,
    mediaHeight: 0,
    lastKeyboardAction: null,
    userBehindLiveEdge: false,
    liveEdgeTolerance: 10,
    minLiveDVRWindow: 60,
    get canSeek() {
      return /unknown|on-demand|:dvr/.test(this.streamType) && Number.isFinite(this.duration) && (!this.isLiveDVR || this.duration >= this.liveDVRWindow);
    },
    get live() {
      return this.streamType.includes("live") || !Number.isFinite(this.duration);
    },
    get liveEdgeStart() {
      return this.live && Number.isFinite(this.seekableEnd) ? Math.max(0, this.seekableEnd - this.liveEdgeTolerance) : 0;
    },
    get liveEdge() {
      return this.live && (!this.canSeek || !this.userBehindLiveEdge && this.currentTime >= this.liveEdgeStart);
    },
    get liveEdgeWindow() {
      return this.live && Number.isFinite(this.seekableEnd) ? this.seekableEnd - this.liveEdgeStart : 0;
    },
    get isLiveDVR() {
      return /:dvr/.test(this.streamType);
    },
    get liveDVRWindow() {
      return Math.max(this.inferredLiveDVRWindow, this.minLiveDVRWindow);
    },
    autoPlaying: false,
    providedTitle: "",
    inferredTitle: "",
    providedLoop: false,
    userPrefersLoop: false,
    providedPoster: "",
    inferredPoster: "",
    inferredViewType: "unknown",
    providedViewType: "unknown",
    providedStreamType: "unknown",
    inferredStreamType: "unknown",
    liveSyncPosition: null,
    inferredLiveDVRWindow: 0,
    savedState: null
  });
  var RESET_ON_SRC_QUALITY_CHANGE = /* @__PURE__ */ new Set([
    "autoPlayError",
    "autoPlaying",
    "buffered",
    "canPlay",
    "error",
    "paused",
    "played",
    "playing",
    "seekable",
    "seeking",
    "waiting"
  ]);
  var RESET_ON_SRC_CHANGE = /* @__PURE__ */ new Set([
    ...RESET_ON_SRC_QUALITY_CHANGE,
    "ended",
    "inferredPoster",
    "inferredStreamType",
    "inferredTitle",
    "intrinsicDuration",
    "inferredLiveDVRWindow",
    "liveSyncPosition",
    "realCurrentTime",
    "savedState",
    "started",
    "userBehindLiveEdge"
  ]);
  function softResetMediaState($media, isSourceQualityChange = false) {
    const filter = isSourceQualityChange ? RESET_ON_SRC_QUALITY_CHANGE : RESET_ON_SRC_CHANGE;
    mediaState.reset($media, (prop2) => filter.has(prop2));
    tick();
  }
  function boundTime(time, store) {
    const clippedTime = time + store.clipStartTime(), isStart = clippedTime <= store.seekableStart(), isEnd = clippedTime >= store.seekableEnd();
    if (isStart) return store.seekableStart();
    if (isEnd) return store.seekableEnd();
    if (store.isLiveDVR() && store.liveDVRWindow() > 0 && clippedTime < store.seekableEnd() - store.liveDVRWindow()) return store.bufferedStart();
    return Math.min(Math.max(store.seekableStart() + 0.1, clippedTime), store.seekableEnd() - 0.1);
  }
  var mediaContext = createContext2();
  function useMediaContext() {
    return useContext2(mediaContext);
  }
  var GROUPED_LOG = /* @__PURE__ */ Symbol(0);
  var _a32, _b2;
  var GroupedLog = (_a32 = GROUPED_LOG, _b2 = class {
    constructor(logger, level, title, root2, parent) {
      __publicField(this, "logger");
      __publicField(this, "level");
      __publicField(this, "title");
      __publicField(this, "root");
      __publicField(this, "parent");
      __publicField(this, _a32, true);
      __publicField(this, "logs", []);
      this.logger = logger;
      this.level = level;
      this.title = title;
      this.root = root2;
      this.parent = parent;
    }
    log(...data) {
      this.logs.push({ data });
      return this;
    }
    labelledLog(label, ...data) {
      this.logs.push({
        label,
        data
      });
      return this;
    }
    groupStart(title) {
      return new _b2(this.logger, this.level, title, this.root ?? this, this);
    }
    groupEnd() {
      this.parent?.logs.push(this);
      return this.parent ?? this;
    }
    dispatch() {
      return this.logger.dispatch(this.level, this.root ?? this);
    }
  }, _b2);
  var _target2, _player3, _prevTrackIndex, _logger, _MediaRemoteControl_instances, dispatchRequest_fn, noPlayerWarning_fn, _a33;
  var MediaRemoteControl = (_a33 = class {
    constructor(logger = void 0) {
      __privateAdd(this, _MediaRemoteControl_instances);
      __privateAdd(this, _target2, null);
      __privateAdd(this, _player3, null);
      __privateAdd(this, _prevTrackIndex, -1);
      __privateAdd(this, _logger);
      __privateSet(this, _logger, logger);
    }
    /**
    * Set the target from which to dispatch media requests events from. The events should bubble
    * up from this target to the player element.
    *
    * @example
    * ```ts
    * const button = document.querySelector('button');
    * remote.setTarget(button);
    * ```
    */
    setTarget(target) {
      __privateSet(this, _target2, target);
    }
    /**
    * Returns the current player element. This method will attempt to find the player by
    * searching up from either the given `target` or default target set via `remote.setTarget`.
    *
    * @example
    * ```ts
    * const player = remote.getPlayer();
    * ```
    */
    getPlayer(target) {
      if (__privateGet(this, _player3)) return __privateGet(this, _player3);
      (target ?? __privateGet(this, _target2))?.dispatchEvent(new DOMEvent("find-media-player", {
        detail: (player) => void __privateSet(this, _player3, player),
        bubbles: true,
        composed: true
      }));
      return __privateGet(this, _player3);
    }
    /**
    * Set the current player element so the remote can support toggle methods such as
    * `togglePaused` as they rely on the current media state.
    */
    setPlayer(player) {
      __privateSet(this, _player3, player);
    }
    /**
    * Dispatch a request to start the media loading process. This will only work if the media
    * player has been initialized with a custom loading strategy `load="custom">`.
    *
    * @docs {@link https://www.vidstack.io/docs/player/core-concepts/loading#load-strategies}
    */
    startLoading(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-start-loading", trigger);
    }
    /**
    * Dispatch a request to start the poster loading process. This will only work if the media
    * player has been initialized with a custom poster loading strategy `posterLoad="custom">`.
    *
    * @docs {@link https://www.vidstack.io/docs/player/core-concepts/loading#load-strategies}
    */
    startLoadingPoster(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-poster-start-loading", trigger);
    }
    /**
    * Dispatch a request to connect to AirPlay.
    *
    * @see {@link https://www.apple.com/au/airplay}
    */
    requestAirPlay(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-airplay-request", trigger);
    }
    /**
    * Dispatch a request to connect to Google Cast.
    *
    * @see {@link https://developers.google.com/cast/docs/overview}
    */
    requestGoogleCast(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-google-cast-request", trigger);
    }
    /**
    * Dispatch a request to begin/resume media playback.
    */
    play(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-play-request", trigger);
    }
    /**
    * Dispatch a request to pause media playback.
    */
    pause(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-pause-request", trigger);
    }
    /**
    * Dispatch a request to set the media volume to mute (0).
    */
    mute(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-mute-request", trigger);
    }
    /**
    * Dispatch a request to unmute the media volume and set it back to it's previous state.
    */
    unmute(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-unmute-request", trigger);
    }
    /**
    * Dispatch a request to enter fullscreen.
    *
    * @docs {@link https://www.vidstack.io/docs/player/api/fullscreen#remote-control}
    */
    enterFullscreen(target, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-enter-fullscreen-request", trigger, target);
    }
    /**
    * Dispatch a request to exit fullscreen.
    *
    * @docs {@link https://www.vidstack.io/docs/player/api/fullscreen#remote-control}
    */
    exitFullscreen(target, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-exit-fullscreen-request", trigger, target);
    }
    /**
    * Dispatch a request to lock the screen orientation.
    *
    * @docs {@link https://www.vidstack.io/docs/player/screen-orientation#remote-control}
    */
    lockScreenOrientation(lockType, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-orientation-lock-request", trigger, lockType);
    }
    /**
    * Dispatch a request to unlock the screen orientation.
    *
    * @docs {@link https://www.vidstack.io/docs/player/api/screen-orientation#remote-control}
    */
    unlockScreenOrientation(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-orientation-unlock-request", trigger);
    }
    /**
    * Dispatch a request to enter picture-in-picture mode.
    *
    * @docs {@link https://www.vidstack.io/docs/player/api/picture-in-picture#remote-control}
    */
    enterPictureInPicture(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-enter-pip-request", trigger);
    }
    /**
    * Dispatch a request to exit picture-in-picture mode.
    *
    * @docs {@link https://www.vidstack.io/docs/player/api/picture-in-picture#remote-control}
    */
    exitPictureInPicture(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-exit-pip-request", trigger);
    }
    /**
    * Notify the media player that a seeking process is happening and to seek to the given `time`.
    */
    seeking(time, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-seeking-request", trigger, time);
    }
    /**
    * Notify the media player that a seeking operation has completed and to seek to the given `time`.
    * This is generally called after a series of `remote.seeking()` calls.
    */
    seek(time, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-seek-request", trigger, time);
    }
    seekToLiveEdge(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-live-edge-request", trigger);
    }
    /**
    * Dispatch a request to update the length of the media in seconds.
    *
    * @example
    * ```ts
    * remote.changeDuration(100); // 100 seconds
    * ```
    */
    changeDuration(duration, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-duration-change-request", trigger, duration);
    }
    /**
    * Dispatch a request to update the clip start time. This is the time at which media playback
    * should start at.
    *
    * @example
    * ```ts
    * remote.changeClipStart(100); // start at 100 seconds
    * ```
    */
    changeClipStart(startTime, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-clip-start-change-request", trigger, startTime);
    }
    /**
    * Dispatch a request to update the clip end time. This is the time at which media playback
    * should end at.
    *
    * @example
    * ```ts
    * remote.changeClipEnd(100); // end at 100 seconds
    * ```
    */
    changeClipEnd(endTime, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-clip-end-change-request", trigger, endTime);
    }
    /**
    * Dispatch a request to update the media volume to the given `volume` level which is a value
    * between 0 and 1.
    *
    * @docs {@link https://www.vidstack.io/docs/player/api/audio-gain#remote-control}
    * @example
    * ```ts
    * remote.changeVolume(0); // 0%
    * remote.changeVolume(0.05); // 5%
    * remote.changeVolume(0.5); // 50%
    * remote.changeVolume(0.75); // 70%
    * remote.changeVolume(1); // 100%
    * ```
    */
    changeVolume(volume, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-volume-change-request", trigger, Math.max(0, Math.min(1, volume)));
    }
    /**
    * Dispatch a request to change the current audio track.
    *
    * @example
    * ```ts
    * remote.changeAudioTrack(1); // track at index 1
    * ```
    */
    changeAudioTrack(index, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-audio-track-change-request", trigger, index);
    }
    /**
    * Dispatch a request to change the video quality. The special value `-1` represents auto quality
    * selection.
    *
    * @example
    * ```ts
    * remote.changeQuality(-1); // auto
    * remote.changeQuality(1); // quality at index 1
    * ```
    */
    changeQuality(index, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-quality-change-request", trigger, index);
    }
    /**
    * Request auto quality selection.
    */
    requestAutoQuality(trigger) {
      this.changeQuality(-1, trigger);
    }
    /**
    * Dispatch a request to change the mode of the text track at the given index.
    *
    * @example
    * ```ts
    * remote.changeTextTrackMode(1, 'showing'); // track at index 1
    * ```
    */
    changeTextTrackMode(index, mode, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-text-track-change-request", trigger, {
        index,
        mode
      });
    }
    /**
    * Dispatch a request to change the media playback rate.
    *
    * @example
    * ```ts
    * remote.changePlaybackRate(0.5); // Half the normal speed
    * remote.changePlaybackRate(1); // Normal speed
    * remote.changePlaybackRate(1.5); // 50% faster than normal
    * remote.changePlaybackRate(2); // Double the normal speed
    * ```
    */
    changePlaybackRate(rate, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-rate-change-request", trigger, rate);
    }
    /**
    * Dispatch a request to change the media audio gain.
    *
    * @example
    * ```ts
    * remote.changeAudioGain(1); // Disable audio gain
    * remote.changeAudioGain(1.5); // 50% louder
    * remote.changeAudioGain(2); // 100% louder
    * ```
    */
    changeAudioGain(gain, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-audio-gain-change-request", trigger, gain);
    }
    /**
    * Dispatch a request to resume idle tracking on controls.
    */
    resumeControls(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-resume-controls-request", trigger);
    }
    /**
    * Dispatch a request to pause controls idle tracking. Pausing tracking will result in the
    * controls being visible until `remote.resumeControls()` is called. This method
    * is generally used when building custom controls and you'd like to prevent the UI from
    * disappearing.
    *
    * @example
    * ```ts
    * // Prevent controls hiding while menu is being interacted with.
    * function onSettingsOpen() {
    *   remote.pauseControls();
    * }
    *
    * function onSettingsClose() {
    *   remote.resumeControls();
    * }
    * ```
    */
    pauseControls(trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-pause-controls-request", trigger);
    }
    /**
    * Dispatch a request to toggle the media playback state.
    */
    togglePaused(trigger) {
      const player = this.getPlayer(trigger?.target);
      if (!player) return;
      if (player.state.paused) this.play(trigger);
      else this.pause(trigger);
    }
    /**
    * Dispatch a request to toggle the controls visibility.
    */
    toggleControls(trigger) {
      const player = this.getPlayer(trigger?.target);
      if (!player) return;
      if (!player.controls.showing) player.controls.show(0, trigger);
      else player.controls.hide(0, trigger);
    }
    /**
    * Dispatch a request to toggle the media muted state.
    */
    toggleMuted(trigger) {
      const player = this.getPlayer(trigger?.target);
      if (!player) return;
      if (player.state.muted) this.unmute(trigger);
      else this.mute(trigger);
    }
    /**
    * Dispatch a request to toggle the media fullscreen state.
    *
    * @docs {@link https://www.vidstack.io/docs/player/api/fullscreen#remote-control}
    */
    toggleFullscreen(target, trigger) {
      const player = this.getPlayer(trigger?.target);
      if (!player) return;
      if (player.state.fullscreen) this.exitFullscreen(target, trigger);
      else this.enterFullscreen(target, trigger);
    }
    /**
    * Dispatch a request to toggle the media picture-in-picture mode.
    *
    * @docs {@link https://www.vidstack.io/docs/player/api/picture-in-picture#remote-control}
    */
    togglePictureInPicture(trigger) {
      const player = this.getPlayer(trigger?.target);
      if (!player) return;
      if (player.state.pictureInPicture) this.exitPictureInPicture(trigger);
      else this.enterPictureInPicture(trigger);
    }
    /**
    * Show captions.
    */
    showCaptions(trigger) {
      const player = this.getPlayer(trigger?.target);
      if (!player) return;
      let tracks = player.state.textTracks, index = __privateGet(this, _prevTrackIndex);
      if (!tracks[index] || !isTrackCaptionKind(tracks[index])) index = -1;
      if (index === -1) index = tracks.findIndex((track) => isTrackCaptionKind(track) && track.default);
      if (index === -1) index = tracks.findIndex((track) => isTrackCaptionKind(track));
      if (index >= 0) this.changeTextTrackMode(index, "showing", trigger);
      __privateSet(this, _prevTrackIndex, -1);
    }
    /**
    * Turn captions off.
    */
    disableCaptions(trigger) {
      const player = this.getPlayer(trigger?.target);
      if (!player) return;
      const tracks = player.state.textTracks, track = player.state.textTrack;
      if (track) {
        const index = tracks.indexOf(track);
        this.changeTextTrackMode(index, "disabled", trigger);
        __privateSet(this, _prevTrackIndex, index);
      }
    }
    /**
    * Dispatch a request to toggle the current captions mode.
    */
    toggleCaptions(trigger) {
      const player = this.getPlayer(trigger?.target);
      if (!player) return;
      if (player.state.textTrack) this.disableCaptions();
      else this.showCaptions();
    }
    userPrefersLoopChange(prefersLoop, trigger) {
      __privateMethod(this, _MediaRemoteControl_instances, dispatchRequest_fn).call(this, "media-user-loop-change-request", trigger, prefersLoop);
    }
  }, _target2 = new WeakMap(), _player3 = new WeakMap(), _prevTrackIndex = new WeakMap(), _logger = new WeakMap(), _MediaRemoteControl_instances = new WeakSet(), dispatchRequest_fn = function(type, trigger, detail) {
    const request = new DOMEvent(type, {
      bubbles: true,
      composed: true,
      cancelable: true,
      detail,
      trigger
    });
    let target = trigger?.target || null;
    if (target && target instanceof Component2) target = target.el;
    target = !target || target === document || target === window || target === document.body || __privateGet(this, _player3)?.el && target instanceof Node && !__privateGet(this, _player3).el.contains(target) ? __privateGet(this, _target2) ?? this.getPlayer()?.el : target ?? __privateGet(this, _target2);
    if (__privateGet(this, _player3)) if (type === "media-play-request" && !__privateGet(this, _player3).state.canLoad) target?.dispatchEvent(request);
    else __privateGet(this, _player3).canPlayQueue.enqueue(type, () => target?.dispatchEvent(request));
    else target?.dispatchEvent(request);
  }, noPlayerWarning_fn = function(method2) {
  }, _a33);
  var _data, _a34;
  var LocalMediaStorage = (_a34 = class {
    constructor() {
      __publicField(this, "playerId", "vds-player");
      __publicField(this, "mediaId", null);
      __privateAdd(this, _data, {
        volume: null,
        muted: null,
        audioGain: null,
        time: null,
        lang: null,
        captions: null,
        rate: null,
        quality: null
      });
      __publicField(this, "saveTimeThrottled", functionThrottle(this.saveTime.bind(this), 1e3));
    }
    async getVolume() {
      return __privateGet(this, _data).volume;
    }
    async setVolume(volume) {
      __privateGet(this, _data).volume = volume;
      this.save();
    }
    async getMuted() {
      return __privateGet(this, _data).muted;
    }
    async setMuted(muted) {
      __privateGet(this, _data).muted = muted;
      this.save();
    }
    async getTime() {
      return __privateGet(this, _data).time;
    }
    async setTime(time, ended) {
      const shouldClear = time < 0;
      __privateGet(this, _data).time = !shouldClear ? time : null;
      if (shouldClear || ended) this.saveTime();
      else this.saveTimeThrottled();
    }
    async getLang() {
      return __privateGet(this, _data).lang;
    }
    async setLang(lang) {
      __privateGet(this, _data).lang = lang;
      this.save();
    }
    async getCaptions() {
      return __privateGet(this, _data).captions;
    }
    async setCaptions(enabled) {
      __privateGet(this, _data).captions = enabled;
      this.save();
    }
    async getPlaybackRate() {
      return __privateGet(this, _data).rate;
    }
    async setPlaybackRate(rate) {
      __privateGet(this, _data).rate = rate;
      this.save();
    }
    async getAudioGain() {
      return __privateGet(this, _data).audioGain;
    }
    async setAudioGain(gain) {
      __privateGet(this, _data).audioGain = gain;
      this.save();
    }
    async getVideoQuality() {
      return __privateGet(this, _data).quality;
    }
    async setVideoQuality(quality) {
      __privateGet(this, _data).quality = quality;
      this.save();
    }
    onChange(src, mediaId, playerId = "vds-player") {
      const savedData = playerId ? localStorage.getItem(playerId) : null, savedTime = mediaId ? localStorage.getItem(mediaId) : null;
      this.playerId = playerId;
      this.mediaId = mediaId;
      __privateSet(this, _data, {
        volume: null,
        muted: null,
        audioGain: null,
        lang: null,
        captions: null,
        rate: null,
        quality: null,
        ...savedData ? JSON.parse(savedData) : {},
        time: savedTime ? +savedTime : null
      });
    }
    save() {
      if (IS_SERVER2 || !this.playerId) return;
      const data = JSON.stringify({
        ...__privateGet(this, _data),
        time: void 0
      });
      localStorage.setItem(this.playerId, data);
    }
    saveTime() {
      if (IS_SERVER2 || !this.mediaId) return;
      const data = (__privateGet(this, _data).time ?? 0).toString();
      localStorage.setItem(this.mediaId, data);
    }
  }, _data = new WeakMap(), _a34);
  var _a35;
  var List = class extends EventsTarget {
    constructor() {
      super(...arguments);
      __publicField(this, "items", []);
      /** @internal */
      __publicField(this, _a35, false);
    }
    get length() {
      return this.items.length;
    }
    get readonly() {
      return this[ListSymbol.readonly];
    }
    /**
    * Returns the index of the first occurrence of the given item, or -1 if it is not present.
    */
    indexOf(item) {
      return this.items.indexOf(item);
    }
    /**
    * Returns an item matching the given `id`, or `null` if not present.
    */
    getById(id2) {
      if (id2 === "") return null;
      return this.items.find((item) => item.id === id2) ?? null;
    }
    /**
    * Transform list to an array.
    */
    toArray() {
      return [...this.items];
    }
    [(_a35 = ListSymbol.readonly, Symbol.iterator)]() {
      return this.items.values();
    }
    /** @internal */
    [ListSymbol.add](item, trigger) {
      const index = this.items.length;
      if (!("" + index in this)) Object.defineProperty(this, index, { get() {
        return this.items[index];
      } });
      if (this.items.includes(item)) return;
      this.items.push(item);
      this.dispatchEvent(new DOMEvent("add", {
        detail: item,
        trigger
      }));
    }
    /** @internal */
    [ListSymbol.remove](item, trigger) {
      const index = this.items.indexOf(item);
      if (index >= 0) {
        this[ListSymbol.onRemove]?.(item, trigger);
        this.items.splice(index, 1);
        this.dispatchEvent(new DOMEvent("remove", {
          detail: item,
          trigger
        }));
      }
    }
    /** @internal */
    [ListSymbol.reset](trigger) {
      for (const item of [...this.items]) this[ListSymbol.remove](item, trigger);
      this.items = [];
      this[ListSymbol.setReadonly](false, trigger);
      this[ListSymbol.onReset]?.();
    }
    /** @internal */
    [ListSymbol.setReadonly](readonly, trigger) {
      if (this[ListSymbol.readonly] === readonly) return;
      this[ListSymbol.readonly] = readonly;
      this.dispatchEvent(new DOMEvent("readonly-change", {
        detail: readonly,
        trigger
      }));
    }
  };
  var SELECTED = /* @__PURE__ */ Symbol(0);
  var SelectList = class extends List {
    get selected() {
      return this.items.find((item) => item.selected) ?? null;
    }
    get selectedIndex() {
      return this.items.findIndex((item) => item.selected);
    }
    /** @internal */
    [ListSymbol.onRemove](item, trigger) {
      this[ListSymbol.select](item, false, trigger);
    }
    /** @internal */
    [ListSymbol.add](item, trigger) {
      item[SELECTED] = false;
      Object.defineProperty(item, "selected", {
        get() {
          return this[SELECTED];
        },
        set: (selected) => {
          if (this.readonly) return;
          this[ListSymbol.onUserSelect]?.();
          this[ListSymbol.select](item, selected);
        }
      });
      super[ListSymbol.add](item, trigger);
    }
    /** @internal */
    [ListSymbol.select](item, selected, trigger) {
      if (selected === item?.[SELECTED]) return;
      const prev = this.selected;
      if (item) item[SELECTED] = selected;
      if (!selected ? prev === item : prev !== item) {
        if (prev) prev[SELECTED] = false;
        this.dispatchEvent(new DOMEvent("change", {
          detail: {
            prev,
            current: this.selected
          },
          trigger
        }));
      }
    }
  };
  var AudioTrackList = class extends SelectList {
  };
  function isEventInside(el, event) {
    const target = event.composedPath()[0];
    return isDOMNode(target) && el.contains(target);
  }
  var rafJobs = /* @__PURE__ */ new Set();
  if (!IS_SERVER2) {
    let processJobs = function() {
      for (const job of rafJobs) try {
        job();
      } catch (e2) {
      }
      window.requestAnimationFrame(processJobs);
    };
    processJobs();
  }
  function scheduleRafJob(job) {
    rafJobs.add(job);
    return () => rafJobs.delete(job);
  }
  function setAttributeIfEmpty(target, name, value) {
    if (!target.hasAttribute(name)) target.setAttribute(name, value);
  }
  function setARIALabel(target, $label) {
    if (target.hasAttribute("aria-label") || target.hasAttribute("data-no-label")) return;
    if (!isFunction($label)) {
      setAttribute(target, "aria-label", $label);
      return;
    }
    function updateAriaDescription() {
      setAttribute(target, "aria-label", $label());
    }
    if (IS_SERVER2) updateAriaDescription();
    else effect(updateAriaDescription);
  }
  function isElementVisible(el) {
    const style = getComputedStyle(el);
    return style.display !== "none" && parseInt(style.opacity) > 0;
  }
  function checkVisibility(el) {
    return !!el && ("checkVisibility" in el ? el.checkVisibility({
      checkOpacity: true,
      checkVisibilityCSS: true
    }) : isElementVisible(el));
  }
  function observeVisibility(el, callback) {
    return scheduleRafJob(() => callback(checkVisibility(el)));
  }
  function isElementParent(owner, node, test) {
    while (node) if (node === owner) return true;
    else if (test?.(node)) break;
    else node = node.parentElement;
    return false;
  }
  function onPress(target, handler) {
    return new EventsController(target).add("pointerup", (event) => {
      if (event.button === 0 && !event.defaultPrevented) handler(event);
    }).add("keydown", (event) => {
      if (isKeyboardClick(event)) handler(event);
    });
  }
  function isTouchPinchEvent(event) {
    return isTouchEvent(event) && (event.touches.length > 1 || event.changedTouches.length > 1);
  }
  function requestScopedAnimationFrame(callback) {
    if (IS_SERVER2) return callback();
    let scope = getScope(), id2 = window.requestAnimationFrame(() => {
      scoped(callback, scope);
      id2 = -1;
    });
    return () => void window.cancelAnimationFrame(id2);
  }
  function autoPlacement2(el, trigger, placement, { offsetVarName, xOffset, yOffset, ...options }) {
    if (!el) return;
    const floatingPlacement = placement.replace(" ", "-").replace("-center", "");
    setStyle(el, "visibility", !trigger ? "hidden" : null);
    if (!trigger) return;
    let isTop = placement.includes("top");
    const negateX = (x) => placement.includes("left") ? `calc(-1 * ${x})` : x, negateY = (y) => isTop ? `calc(-1 * ${y})` : y;
    return autoUpdate(trigger, el, () => {
      computePosition2(trigger, el, {
        placement: floatingPlacement,
        middleware: [
          ...options.middleware ?? [],
          flip2({
            fallbackAxisSideDirection: "start",
            crossAxis: false
          }),
          shift2()
        ],
        ...options
      }).then(({ x, y, middlewareData }) => {
        const hasFlipped = !!middlewareData.flip?.index;
        isTop = placement.includes(hasFlipped ? "bottom" : "top");
        el.setAttribute("data-placement", hasFlipped ? placement.startsWith("top") ? placement.replace("top", "bottom") : placement.replace("bottom", "top") : placement);
        Object.assign(el.style, {
          top: `calc(${y + "px"} + ${negateY(yOffset ? yOffset + "px" : `var(--${offsetVarName}-y-offset, 0px)`)})`,
          left: `calc(${x + "px"} + ${negateX(xOffset ? xOffset + "px" : `var(--${offsetVarName}-x-offset, 0px)`)})`
        });
      });
    });
  }
  function hasAnimation(el) {
    return getComputedStyle(el).animationName !== "none";
  }
  function isHTMLElement2(el) {
    return el instanceof HTMLElement;
  }
  var _display, _video6, _track, _tracks2, _NativeTextRenderer_instances, attachTrack_fn, createTrackElement_fn, copyCues_fn, onChange_fn, _a36;
  var NativeTextRenderer = (_a36 = class {
    constructor() {
      __privateAdd(this, _NativeTextRenderer_instances);
      __publicField(this, "priority", 0);
      __privateAdd(this, _display, true);
      __privateAdd(this, _video6, null);
      __privateAdd(this, _track, null);
      __privateAdd(this, _tracks2, /* @__PURE__ */ new Set());
    }
    canRender(_, video) {
      return !!video;
    }
    attach(video) {
      __privateSet(this, _video6, video);
      if (video) video.textTracks.onchange = __privateMethod(this, _NativeTextRenderer_instances, onChange_fn).bind(this);
    }
    addTrack(track) {
      __privateGet(this, _tracks2).add(track);
      __privateMethod(this, _NativeTextRenderer_instances, attachTrack_fn).call(this, track);
    }
    removeTrack(track) {
      track[TextTrackSymbol.native]?.remove?.();
      track[TextTrackSymbol.native] = null;
      __privateGet(this, _tracks2).delete(track);
    }
    changeTrack(track) {
      const current = track?.[TextTrackSymbol.native];
      if (current && current.track.mode !== "showing") current.track.mode = "showing";
      __privateSet(this, _track, track);
    }
    setDisplay(display) {
      __privateSet(this, _display, display);
      __privateMethod(this, _NativeTextRenderer_instances, onChange_fn).call(this);
    }
    detach() {
      if (__privateGet(this, _video6)) __privateGet(this, _video6).textTracks.onchange = null;
      for (const track of __privateGet(this, _tracks2)) this.removeTrack(track);
      __privateGet(this, _tracks2).clear();
      __privateSet(this, _video6, null);
      __privateSet(this, _track, null);
    }
  }, _display = new WeakMap(), _video6 = new WeakMap(), _track = new WeakMap(), _tracks2 = new WeakMap(), _NativeTextRenderer_instances = new WeakSet(), attachTrack_fn = function(track) {
    var _a106;
    if (!__privateGet(this, _video6)) return;
    const el = track[_a106 = TextTrackSymbol.native] ?? (track[_a106] = __privateMethod(this, _NativeTextRenderer_instances, createTrackElement_fn).call(this, track));
    if (isHTMLElement2(el)) {
      __privateGet(this, _video6).append(el);
      el.track.mode = el.default ? "showing" : "disabled";
    }
  }, createTrackElement_fn = function(track) {
    const el = document.createElement("track"), isDefault = track.default || track.mode === "showing", isSupported = track.src && track.type === "vtt";
    el.id = track.id;
    el.src = isSupported ? track.src : "";
    el.label = track.label;
    el.kind = track.kind;
    el.default = isDefault;
    track.language && (el.srclang = track.language);
    if (isDefault && !isSupported) __privateMethod(this, _NativeTextRenderer_instances, copyCues_fn).call(this, track, el.track);
    return el;
  }, copyCues_fn = function(track, native) {
    if (track.src && track.type === "vtt" || native.cues?.length) return;
    for (const cue of track.cues) native.addCue(cue);
  }, onChange_fn = function(event) {
    for (const track of __privateGet(this, _tracks2)) {
      const native = track[TextTrackSymbol.native];
      if (!native) continue;
      if (!__privateGet(this, _display)) {
        native.track.mode = native.managed ? "hidden" : "disabled";
        continue;
      }
      const isShowing = native.track.mode === "showing";
      if (isShowing) __privateMethod(this, _NativeTextRenderer_instances, copyCues_fn).call(this, track, native.track);
      track.setMode(isShowing ? "showing" : "disabled", event);
    }
  }, _a36);
  var _video7, _textTracks, _renderers, _media5, _nativeDisplay, _nativeRenderer, _customRenderer, _TextRenderers_instances, watchControls_fn, addNativeTrack_fn, removeNativeTrack_fn, onAddTrack_fn2, onRemoveTrack_fn, update_fn, detach_fn, _a37;
  var TextRenderers = (_a37 = class {
    constructor(media) {
      __privateAdd(this, _TextRenderers_instances);
      __privateAdd(this, _video7, null);
      __privateAdd(this, _textTracks);
      __privateAdd(this, _renderers, []);
      __privateAdd(this, _media5);
      __privateAdd(this, _nativeDisplay, false);
      __privateAdd(this, _nativeRenderer, null);
      __privateAdd(this, _customRenderer, null);
      __privateSet(this, _media5, media);
      const textTracks = media.textTracks;
      __privateSet(this, _textTracks, textTracks);
      effect(__privateMethod(this, _TextRenderers_instances, watchControls_fn).bind(this));
      onDispose(__privateMethod(this, _TextRenderers_instances, detach_fn).bind(this));
      new EventsController(textTracks).add("add", __privateMethod(this, _TextRenderers_instances, onAddTrack_fn2).bind(this)).add("remove", __privateMethod(this, _TextRenderers_instances, onRemoveTrack_fn).bind(this)).add("mode-change", __privateMethod(this, _TextRenderers_instances, update_fn).bind(this));
    }
    add(renderer) {
      __privateGet(this, _renderers).push(renderer);
      untrack(__privateMethod(this, _TextRenderers_instances, update_fn).bind(this));
    }
    remove(renderer) {
      renderer.detach();
      __privateGet(this, _renderers).splice(__privateGet(this, _renderers).indexOf(renderer), 1);
      untrack(__privateMethod(this, _TextRenderers_instances, update_fn).bind(this));
    }
    /** @internal */
    attachVideo(video) {
      requestAnimationFrame(() => {
        __privateSet(this, _video7, video);
        if (video) {
          __privateSet(this, _nativeRenderer, new NativeTextRenderer());
          __privateGet(this, _nativeRenderer).attach(video);
          for (const track of __privateGet(this, _textTracks)) __privateMethod(this, _TextRenderers_instances, addNativeTrack_fn).call(this, track);
        }
        __privateMethod(this, _TextRenderers_instances, update_fn).call(this);
      });
    }
  }, _video7 = new WeakMap(), _textTracks = new WeakMap(), _renderers = new WeakMap(), _media5 = new WeakMap(), _nativeDisplay = new WeakMap(), _nativeRenderer = new WeakMap(), _customRenderer = new WeakMap(), _TextRenderers_instances = new WeakSet(), watchControls_fn = function() {
    const { nativeControls } = __privateGet(this, _media5).$state;
    __privateSet(this, _nativeDisplay, nativeControls());
    __privateMethod(this, _TextRenderers_instances, update_fn).call(this);
  }, addNativeTrack_fn = function(track) {
    if (!isTrackCaptionKind(track)) return;
    __privateGet(this, _nativeRenderer)?.addTrack(track);
  }, removeNativeTrack_fn = function(track) {
    if (!isTrackCaptionKind(track)) return;
    __privateGet(this, _nativeRenderer)?.removeTrack(track);
  }, onAddTrack_fn2 = function(event) {
    __privateMethod(this, _TextRenderers_instances, addNativeTrack_fn).call(this, event.detail);
  }, onRemoveTrack_fn = function(event) {
    __privateMethod(this, _TextRenderers_instances, removeNativeTrack_fn).call(this, event.detail);
  }, update_fn = function() {
    const currentTrack = __privateGet(this, _textTracks).selected;
    if (__privateGet(this, _video7) && (__privateGet(this, _nativeDisplay) || currentTrack?.[TextTrackSymbol.nativeHLS])) {
      __privateGet(this, _customRenderer)?.changeTrack(null);
      __privateGet(this, _nativeRenderer)?.setDisplay(true);
      __privateGet(this, _nativeRenderer)?.changeTrack(currentTrack);
      return;
    }
    __privateGet(this, _nativeRenderer)?.setDisplay(false);
    __privateGet(this, _nativeRenderer)?.changeTrack(null);
    if (!currentTrack) {
      __privateGet(this, _customRenderer)?.changeTrack(null);
      return;
    }
    const customRenderer = __privateGet(this, _renderers).sort((a, b) => a.priority - b.priority).find((renderer) => renderer.canRender(currentTrack, __privateGet(this, _video7)));
    if (__privateGet(this, _customRenderer) !== customRenderer) {
      __privateGet(this, _customRenderer)?.detach();
      customRenderer?.attach(__privateGet(this, _video7));
      __privateSet(this, _customRenderer, customRenderer ?? null);
    }
    customRenderer?.changeTrack(currentTrack);
  }, detach_fn = function() {
    __privateGet(this, _nativeRenderer)?.detach();
    __privateSet(this, _nativeRenderer, null);
    __privateGet(this, _customRenderer)?.detach();
    __privateSet(this, _customRenderer, null);
  }, _a37);
  var _a38, _canLoad2, _defaults, _storage, _preferredLang, _selectTracks, _pendingRemoval, _onTrackModeChangeBind, _TextTrackList_instances, onTrackModeChange_fn, saveCaptionsTrack_fn, saveLang_fn, _b3;
  var TextTrackList = (_b3 = class extends List {
    constructor() {
      super();
      __privateAdd(this, _TextTrackList_instances);
      __privateAdd(this, _canLoad2, false);
      __privateAdd(this, _defaults, {});
      __privateAdd(this, _storage, null);
      __privateAdd(this, _preferredLang, null);
      /** @internal */
      __publicField(this, _a38);
      __privateAdd(this, _selectTracks, functionDebounce(async () => {
        if (!__privateGet(this, _canLoad2)) return;
        if (!__privateGet(this, _preferredLang) && __privateGet(this, _storage)) __privateSet(this, _preferredLang, await __privateGet(this, _storage).getLang());
        const showCaptions = await __privateGet(this, _storage)?.getCaptions();
        for (const kind of [
          ["captions", "subtitles"],
          "chapters",
          "descriptions",
          "metadata"
        ]) {
          const tracks = this.getByKind(kind);
          if (tracks.find((t2) => t2.mode === "showing")) continue;
          const preferredTrack = __privateGet(this, _preferredLang) ? tracks.find((track2) => track2.language === __privateGet(this, _preferredLang)) : null;
          const defaultTrack = isArray(kind) ? __privateGet(this, _defaults)[kind.find((kind2) => __privateGet(this, _defaults)[kind2]) || ""] : __privateGet(this, _defaults)[kind];
          const track = preferredTrack ?? defaultTrack, isCaptionsKind = track && isTrackCaptionKind(track);
          if (track && (!isCaptionsKind || showCaptions !== false)) {
            track.mode = "showing";
            if (isCaptionsKind) __privateMethod(this, _TextTrackList_instances, saveCaptionsTrack_fn).call(this, track);
          }
        }
      }, 300));
      __privateAdd(this, _pendingRemoval, null);
      __privateAdd(this, _onTrackModeChangeBind, __privateMethod(this, _TextTrackList_instances, onTrackModeChange_fn).bind(this));
    }
    get selected() {
      return this.items.find((t2) => t2.mode === "showing" && isTrackCaptionKind(t2)) ?? null;
    }
    get selectedIndex() {
      const selected = this.selected;
      return selected ? this.indexOf(selected) : -1;
    }
    get preferredLang() {
      return __privateGet(this, _preferredLang);
    }
    set preferredLang(lang) {
      __privateSet(this, _preferredLang, lang);
      __privateMethod(this, _TextTrackList_instances, saveLang_fn).call(this, lang);
    }
    add(init, trigger) {
      const track = init instanceof TextTrack ? init : new TextTrack(init), kind = init.kind === "captions" || init.kind === "subtitles" ? "captions" : init.kind;
      if (__privateGet(this, _defaults)[kind] && init.default) delete init.default;
      track.addEventListener("mode-change", __privateGet(this, _onTrackModeChangeBind));
      this[ListSymbol.add](track, trigger);
      track[TextTrackSymbol.crossOrigin] = this[TextTrackSymbol.crossOrigin];
      if (__privateGet(this, _canLoad2)) track[TextTrackSymbol.canLoad]();
      if (init.default) __privateGet(this, _defaults)[kind] = track;
      __privateGet(this, _selectTracks).call(this);
      return this;
    }
    remove(track, trigger) {
      __privateSet(this, _pendingRemoval, track);
      if (!this.items.includes(track)) return;
      if (track === __privateGet(this, _defaults)[track.kind]) delete __privateGet(this, _defaults)[track.kind];
      track.mode = "disabled";
      track[TextTrackSymbol.onModeChange] = null;
      track.removeEventListener("mode-change", __privateGet(this, _onTrackModeChangeBind));
      this[ListSymbol.remove](track, trigger);
      __privateSet(this, _pendingRemoval, null);
      return this;
    }
    clear(trigger) {
      for (const track of [...this.items]) this.remove(track, trigger);
      return this;
    }
    getByKind(kind) {
      const kinds = Array.isArray(kind) ? kind : [kind];
      return this.items.filter((track) => kinds.includes(track.kind));
    }
    /** @internal */
    [(_a38 = TextTrackSymbol.crossOrigin, TextTrackSymbol.canLoad)]() {
      if (__privateGet(this, _canLoad2)) return;
      for (const track of this.items) track[TextTrackSymbol.canLoad]();
      __privateSet(this, _canLoad2, true);
      __privateGet(this, _selectTracks).call(this);
    }
    setStorage(storage) {
      __privateSet(this, _storage, storage);
    }
  }, _canLoad2 = new WeakMap(), _defaults = new WeakMap(), _storage = new WeakMap(), _preferredLang = new WeakMap(), _selectTracks = new WeakMap(), _pendingRemoval = new WeakMap(), _onTrackModeChangeBind = new WeakMap(), _TextTrackList_instances = new WeakSet(), onTrackModeChange_fn = function(event) {
    const track = event.detail;
    if (__privateGet(this, _storage) && isTrackCaptionKind(track) && track !== __privateGet(this, _pendingRemoval)) __privateMethod(this, _TextTrackList_instances, saveCaptionsTrack_fn).call(this, track);
    if (track.mode === "showing") {
      const kinds = isTrackCaptionKind(track) ? ["captions", "subtitles"] : [track.kind];
      for (const t2 of this.items) if (t2.mode === "showing" && t2 != track && kinds.includes(t2.kind)) t2.mode = "disabled";
    }
    this.dispatchEvent(new DOMEvent("mode-change", {
      detail: event.detail,
      trigger: event
    }));
  }, saveCaptionsTrack_fn = function(track) {
    if (track.mode !== "disabled") __privateMethod(this, _TextTrackList_instances, saveLang_fn).call(this, track.language);
    __privateGet(this, _storage)?.setCaptions?.(track.mode === "showing");
  }, saveLang_fn = function(lang) {
    __privateGet(this, _storage)?.setLang?.(__privateSet(this, _preferredLang, lang));
  }, _b3);
  var _a39, _auto, _b4;
  var VideoQualityList = (_b4 = class extends SelectList {
    constructor() {
      super(...arguments);
      __privateAdd(this, _auto, false);
      /**
      * Configures quality switching:
      *
      * - `current`: Trigger an immediate quality level switch. This will abort the current fragment
      * request if any, flush the whole buffer, and fetch fragment matching with current position
      * and requested quality level.
      *
      * - `next`: Trigger a quality level switch for next fragment. This could eventually flush
      * already buffered next fragment.
      *
      * - `load`: Set quality level for next loaded fragment.
      *
      * @see {@link https://www.vidstack.io/docs/player/api/video-quality#switch}
      * @see {@link https://github.com/video-dev/hls.js/blob/master/docs/API.md#quality-switch-control-api}
      */
      __publicField(this, "switch", "current");
      /** @internal */
      __publicField(this, _a39);
    }
    /**
    * Whether automatic quality selection is enabled.
    */
    get auto() {
      return __privateGet(this, _auto) || this.readonly;
    }
    /** @internal */
    [(_a39 = QualitySymbol.enableAuto, ListSymbol.onUserSelect)]() {
      this[QualitySymbol.setAuto](false);
    }
    /** @internal */
    [ListSymbol.onReset](trigger) {
      this[QualitySymbol.enableAuto] = void 0;
      this[QualitySymbol.setAuto](false, trigger);
    }
    /**
    * Request automatic quality selection (if supported). This will be a no-op if the list is
    * `readonly` as that already implies auto-selection.
    */
    autoSelect(trigger) {
      if (this.readonly || __privateGet(this, _auto) || !this[QualitySymbol.enableAuto]) return;
      this[QualitySymbol.enableAuto]?.(trigger);
      this[QualitySymbol.setAuto](true, trigger);
    }
    getBySrc(src) {
      return this.items.find((quality) => quality.src === src);
    }
    /** @internal */
    [QualitySymbol.setAuto](auto, trigger) {
      if (__privateGet(this, _auto) === auto) return;
      __privateSet(this, _auto, auto);
      this.dispatchEvent(new DOMEvent("auto-change", {
        detail: auto,
        trigger
      }));
    }
  }, _auto = new WeakMap(), _b4);
  function sortVideoQualities(qualities, desc) {
    return [...qualities].sort(desc ? compareVideoQualityDesc : compareVideoQualityAsc);
  }
  function compareVideoQualityAsc(a, b) {
    return a.height === b.height ? (a.bitrate ?? 0) - (b.bitrate ?? 0) : a.height - b.height;
  }
  function compareVideoQualityDesc(a, b) {
    return b.height === a.height ? (b.bitrate ?? 0) - (a.bitrate ?? 0) : b.height - a.height;
  }
  function isHTMLAudioElement(element) {
    return !IS_SERVER2 && element instanceof HTMLAudioElement;
  }
  function isHTMLVideoElement(element) {
    return !IS_SERVER2 && element instanceof HTMLVideoElement;
  }
  function isHTMLMediaElement(element) {
    return isHTMLAudioElement(element) || isHTMLVideoElement(element);
  }
  var MediaPlayerController = class extends ViewController {
  };
  var MEDIA_KEY_SHORTCUTS = {
    togglePaused: "k Space",
    toggleMuted: "m",
    toggleFullscreen: "f",
    togglePictureInPicture: "i",
    toggleCaptions: "c",
    seekBackward: "j J ArrowLeft",
    seekForward: "l L ArrowRight",
    volumeUp: "ArrowUp",
    volumeDown: "ArrowDown",
    speedUp: ">",
    slowDown: "<"
  };
  var MODIFIER_KEYS = /* @__PURE__ */ new Set([
    "Shift",
    "Alt",
    "Meta",
    "Ctrl"
  ]);
  var BUTTON_SELECTORS = 'button, [role="button"]';
  var IGNORE_SELECTORS = 'input, textarea, select, [contenteditable], [role^="menuitem"], [role="timer"]';
  var _media6, _MediaKeyboardController_instances, onTargetChange_fn, onKeyUp_fn, onKeyDown_fn, onPreventVideoKeys_fn, getMatchingMethod_fn, _seekTotal, calcSeekAmount_fn, _timeSlider, forwardTimeKeyboardEvent_fn, seeking_fn, _a40;
  var MediaKeyboardController = (_a40 = class extends MediaPlayerController {
    constructor(media) {
      super();
      __privateAdd(this, _MediaKeyboardController_instances);
      __privateAdd(this, _media6);
      __privateAdd(this, _seekTotal);
      __privateAdd(this, _timeSlider, null);
      __privateSet(this, _media6, media);
    }
    onConnect() {
      effect(__privateMethod(this, _MediaKeyboardController_instances, onTargetChange_fn).bind(this));
    }
  }, _media6 = new WeakMap(), _MediaKeyboardController_instances = new WeakSet(), onTargetChange_fn = function() {
    const { keyDisabled, keyTarget } = this.$props;
    if (keyDisabled()) return;
    const target = keyTarget() === "player" ? this.el : document, $active = signal(false);
    if (target === this.el) new EventsController(this.el).add("focusin", () => $active.set(true)).add("focusout", (event) => {
      if (!this.el.contains(event.target)) $active.set(false);
    });
    else {
      if (!peek($active)) $active.set(document.querySelector("[data-media-player]") === this.el);
      listenEvent(document, "focusin", (event) => {
        const activePlayer = event.composedPath().find((el) => el instanceof Element && el.localName === "media-player");
        if (activePlayer !== void 0) $active.set(this.el === activePlayer);
      });
    }
    effect(() => {
      if (!$active()) return;
      new EventsController(target).add("keyup", __privateMethod(this, _MediaKeyboardController_instances, onKeyUp_fn).bind(this)).add("keydown", __privateMethod(this, _MediaKeyboardController_instances, onKeyDown_fn).bind(this)).add("keydown", __privateMethod(this, _MediaKeyboardController_instances, onPreventVideoKeys_fn).bind(this), { capture: true });
    });
  }, onKeyUp_fn = function(event) {
    const focusedEl = document.activeElement;
    if (!event.key || !this.$state.canSeek() || focusedEl?.matches(IGNORE_SELECTORS)) return;
    let { method: method2, value } = __privateMethod(this, _MediaKeyboardController_instances, getMatchingMethod_fn).call(this, event);
    if (!isString(value) && !isArray(value)) {
      value?.onKeyUp?.({
        event,
        player: __privateGet(this, _media6).player,
        remote: __privateGet(this, _media6).remote
      });
      value?.callback?.(event, __privateGet(this, _media6).remote);
      return;
    }
    if (method2?.startsWith("seek")) {
      event.preventDefault();
      event.stopPropagation();
      if (__privateGet(this, _timeSlider)) {
        __privateMethod(this, _MediaKeyboardController_instances, forwardTimeKeyboardEvent_fn).call(this, event, method2 === "seekForward");
        __privateSet(this, _timeSlider, null);
      } else {
        __privateGet(this, _media6).remote.seek(__privateGet(this, _seekTotal), event);
        __privateSet(this, _seekTotal, void 0);
      }
    }
    if (method2?.startsWith("volume")) this.el.querySelector("[data-media-volume-slider]")?.dispatchEvent(new KeyboardEvent("keyup", {
      key: method2 === "volumeUp" ? "Up" : "Down",
      shiftKey: event.shiftKey,
      trigger: event
    }));
  }, onKeyDown_fn = function(event) {
    if (!event.key || MODIFIER_KEYS.has(event.key)) return;
    const focusedEl = document.activeElement;
    if (focusedEl?.matches(IGNORE_SELECTORS) || isKeyboardClick(event) && focusedEl?.matches(BUTTON_SELECTORS)) return;
    let { method: method2, value } = __privateMethod(this, _MediaKeyboardController_instances, getMatchingMethod_fn).call(this, event), isNumberPress = !event.metaKey && /^[0-9]$/.test(event.key);
    if (!isString(value) && !isArray(value) && !isNumberPress) {
      value?.onKeyDown?.({
        event,
        player: __privateGet(this, _media6).player,
        remote: __privateGet(this, _media6).remote
      });
      value?.callback?.(event, __privateGet(this, _media6).remote);
      return;
    }
    if (!method2 && isNumberPress && !modifierKeyPressed(event)) {
      event.preventDefault();
      event.stopPropagation();
      __privateGet(this, _media6).remote.seek(this.$state.duration() / 10 * Number(event.key), event);
      return;
    }
    if (!method2) return;
    event.preventDefault();
    event.stopPropagation();
    switch (method2) {
      case "seekForward":
      case "seekBackward":
        __privateMethod(this, _MediaKeyboardController_instances, seeking_fn).call(this, event, method2, method2 === "seekForward");
        break;
      case "volumeUp":
      case "volumeDown":
        const volumeSlider = this.el.querySelector("[data-media-volume-slider]");
        if (volumeSlider) volumeSlider.dispatchEvent(new KeyboardEvent("keydown", {
          key: method2 === "volumeUp" ? "Up" : "Down",
          shiftKey: event.shiftKey,
          trigger: event
        }));
        else {
          const value2 = event.shiftKey ? 0.1 : 0.05;
          __privateGet(this, _media6).remote.changeVolume(this.$state.volume() + (method2 === "volumeUp" ? +value2 : -value2), event);
        }
        break;
      case "toggleFullscreen":
        __privateGet(this, _media6).remote.toggleFullscreen("prefer-media", event);
        break;
      case "speedUp":
      case "slowDown":
        const playbackRate = this.$state.playbackRate();
        __privateGet(this, _media6).remote.changePlaybackRate(Math.max(0.25, Math.min(2, playbackRate + (method2 === "speedUp" ? 0.25 : -0.25))), event);
        break;
      default:
        __privateGet(this, _media6).remote[method2]?.(event);
    }
    this.$state.lastKeyboardAction.set({
      action: method2,
      event
    });
  }, onPreventVideoKeys_fn = function(event) {
    if (isHTMLMediaElement(event.target) && __privateMethod(this, _MediaKeyboardController_instances, getMatchingMethod_fn).call(this, event).method) event.preventDefault();
  }, getMatchingMethod_fn = function(event) {
    const keyShortcuts = {
      ...this.$props.keyShortcuts(),
      ...__privateGet(this, _media6).ariaKeys
    };
    const method2 = Object.keys(keyShortcuts).find((method3) => {
      const value = keyShortcuts[method3], keys = isArray(value) ? value.join(" ") : isString(value) ? value : value?.keys;
      return (isArray(keys) ? keys : keys?.split(" "))?.map((key2) => replaceSymbolKeys(key2).replace(/Control/g, "Ctrl").split("+"))?.some((combo) => {
        const modifierKeys = new Set(combo.filter((key2) => MODIFIER_KEYS.has(key2)));
        if ("<>".includes(event.key)) modifierKeys.add("Shift");
        for (const modKey of MODIFIER_KEYS) {
          const modKeyProp = modKey.toLowerCase() + "Key";
          if (!modifierKeys.has(modKey) && event[modKeyProp]) return false;
        }
        return combo.every((key2) => {
          return MODIFIER_KEYS.has(key2) ? event[key2.toLowerCase() + "Key"] : event.key === key2.replace("Space", " ");
        });
      });
    });
    return {
      method: method2,
      value: method2 ? keyShortcuts[method2] : null
    };
  }, _seekTotal = new WeakMap(), calcSeekAmount_fn = function(event, type) {
    const seekBy = event.shiftKey ? 10 : 5;
    return __privateSet(this, _seekTotal, Math.max(0, Math.min((__privateGet(this, _seekTotal) ?? this.$state.currentTime()) + (type === "seekForward" ? +seekBy : -seekBy), this.$state.duration())));
  }, _timeSlider = new WeakMap(), forwardTimeKeyboardEvent_fn = function(event, forward) {
    __privateGet(this, _timeSlider)?.dispatchEvent(new KeyboardEvent(event.type, {
      key: !forward ? "Left" : "Right",
      shiftKey: event.shiftKey,
      trigger: event
    }));
  }, seeking_fn = function(event, type, forward) {
    if (!this.$state.canSeek()) return;
    if (!__privateGet(this, _timeSlider)) __privateSet(this, _timeSlider, this.el.querySelector("[data-media-time-slider]"));
    if (__privateGet(this, _timeSlider)) __privateMethod(this, _MediaKeyboardController_instances, forwardTimeKeyboardEvent_fn).call(this, event, forward);
    else __privateGet(this, _media6).remote.seeking(__privateMethod(this, _MediaKeyboardController_instances, calcSeekAmount_fn).call(this, event, type), event);
  }, _a40);
  var SYMBOL_KEY_MAP = [
    "!",
    "@",
    "#",
    "$",
    "%",
    "^",
    "&",
    "*",
    "(",
    ")"
  ];
  function replaceSymbolKeys(key2) {
    return key2.replace(/Shift\+(\d)/g, (_, num) => SYMBOL_KEY_MAP[num - 1]);
  }
  function modifierKeyPressed(event) {
    for (const key2 of MODIFIER_KEYS) if (event[key2.toLowerCase() + "Key"]) return true;
    return false;
  }
  var _shortcut, _a41;
  var ARIAKeyShortcuts = (_a41 = class extends ViewController {
    constructor(shortcut) {
      super();
      __privateAdd(this, _shortcut);
      __privateSet(this, _shortcut, shortcut);
    }
    onAttach(el) {
      const { $props, ariaKeys } = useMediaContext(), keys = el.getAttribute("aria-keyshortcuts");
      if (keys) {
        ariaKeys[__privateGet(this, _shortcut)] = keys;
        if (!IS_SERVER2) onDispose(() => {
          delete ariaKeys[__privateGet(this, _shortcut)];
        });
        return;
      }
      const shortcuts = $props.keyShortcuts()[__privateGet(this, _shortcut)];
      if (shortcuts) {
        const keys2 = isArray(shortcuts) ? shortcuts.join(" ") : isString(shortcuts) ? shortcuts : shortcuts?.keys;
        el.setAttribute("aria-keyshortcuts", isArray(keys2) ? keys2.join(" ") : keys2);
      }
    }
  }, _shortcut = new WeakMap(), _a41);
  var _idleTimer, _pausedTracking, _hideOnMouseLeave, _isMouseOutside, _focusedItem, _canIdle, _MediaControls_instances, init_fn, watchMouse_fn, watchPaused_fn, onPlay_fn3, onPause_fn4, onEnd_fn, onMouseEnter_fn, onMouseLeave_fn, clearIdleTimer_fn, onStopIdle_fn, changeVisibility_fn, onChange_fn2, _a42;
  var MediaControls = (_a42 = class extends MediaPlayerController {
    constructor() {
      super(...arguments);
      __privateAdd(this, _MediaControls_instances);
      __privateAdd(this, _idleTimer, -2);
      __privateAdd(this, _pausedTracking, false);
      __privateAdd(this, _hideOnMouseLeave, signal(false));
      __privateAdd(this, _isMouseOutside, signal(false));
      __privateAdd(this, _focusedItem, null);
      __privateAdd(this, _canIdle, signal(true));
      /**
      * The default amount of delay in milliseconds while media playback is progressing without user
      * activity to indicate an idle state (i.e., hide controls).
      *
      * @defaultValue 2000
      */
      __publicField(this, "defaultDelay", 2e3);
    }
    /**
    * Whether controls can hide after a delay in user interaction. If this is false, controls will
    * not hide and be user controlled.
    */
    get canIdle() {
      return __privateGet(this, _canIdle).call(this);
    }
    set canIdle(canIdle) {
      __privateGet(this, _canIdle).set(canIdle);
    }
    /**
    * Whether controls visibility should be toggled when the mouse enters and leaves the player
    * container.
    *
    * @defaultValue false
    */
    get hideOnMouseLeave() {
      const { hideControlsOnMouseLeave } = this.$props;
      return __privateGet(this, _hideOnMouseLeave).call(this) || hideControlsOnMouseLeave();
    }
    set hideOnMouseLeave(hide2) {
      __privateGet(this, _hideOnMouseLeave).set(hide2);
    }
    /**
    * Whether media controls are currently visible.
    */
    get showing() {
      return this.$state.controlsVisible();
    }
    /**
    * Show controls.
    */
    show(delay = 0, trigger) {
      __privateMethod(this, _MediaControls_instances, clearIdleTimer_fn).call(this);
      if (!__privateGet(this, _pausedTracking)) __privateMethod(this, _MediaControls_instances, changeVisibility_fn).call(this, true, delay, trigger);
    }
    /**
    * Hide controls.
    */
    hide(delay = this.defaultDelay, trigger) {
      __privateMethod(this, _MediaControls_instances, clearIdleTimer_fn).call(this);
      if (!__privateGet(this, _pausedTracking)) __privateMethod(this, _MediaControls_instances, changeVisibility_fn).call(this, false, delay, trigger);
    }
    /**
    * Whether all idle tracking on controls should be paused until resumed again.
    */
    pause(trigger) {
      __privateSet(this, _pausedTracking, true);
      __privateMethod(this, _MediaControls_instances, clearIdleTimer_fn).call(this);
      __privateMethod(this, _MediaControls_instances, changeVisibility_fn).call(this, true, 0, trigger);
    }
    resume(trigger) {
      __privateSet(this, _pausedTracking, false);
      if (this.$state.paused()) return;
      __privateMethod(this, _MediaControls_instances, changeVisibility_fn).call(this, false, this.defaultDelay, trigger);
    }
    onConnect() {
      effect(__privateMethod(this, _MediaControls_instances, init_fn).bind(this));
    }
  }, _idleTimer = new WeakMap(), _pausedTracking = new WeakMap(), _hideOnMouseLeave = new WeakMap(), _isMouseOutside = new WeakMap(), _focusedItem = new WeakMap(), _canIdle = new WeakMap(), _MediaControls_instances = new WeakSet(), init_fn = function() {
    const { viewType } = this.$state;
    if (!this.el || !__privateGet(this, _canIdle).call(this)) return;
    if (viewType() === "audio") {
      this.show();
      return;
    }
    effect(__privateMethod(this, _MediaControls_instances, watchMouse_fn).bind(this));
    effect(__privateMethod(this, _MediaControls_instances, watchPaused_fn).bind(this));
    const onPlay = __privateMethod(this, _MediaControls_instances, onPlay_fn3).bind(this), onPause = __privateMethod(this, _MediaControls_instances, onPause_fn4).bind(this), onEnd = __privateMethod(this, _MediaControls_instances, onEnd_fn).bind(this);
    new EventsController(this.el).add("can-play", (event) => this.show(0, event)).add("play", onPlay).add("pause", onPause).add("end", onEnd).add("auto-play-fail", onPause);
  }, watchMouse_fn = function() {
    if (!this.el) return;
    const { started, pointer, paused } = this.$state;
    if (!started() || pointer() !== "fine") return;
    const events = new EventsController(this.el), shouldHideOnMouseLeave = this.hideOnMouseLeave;
    if (!shouldHideOnMouseLeave || !__privateGet(this, _isMouseOutside).call(this)) effect(() => {
      if (!paused()) events.add("pointermove", __privateMethod(this, _MediaControls_instances, onStopIdle_fn).bind(this));
    });
    if (shouldHideOnMouseLeave) events.add("mouseenter", __privateMethod(this, _MediaControls_instances, onMouseEnter_fn).bind(this)).add("mouseleave", __privateMethod(this, _MediaControls_instances, onMouseLeave_fn).bind(this));
  }, watchPaused_fn = function() {
    const { paused, started, autoPlayError } = this.$state;
    if (paused() || autoPlayError() && !started()) return;
    const onStopIdle = __privateMethod(this, _MediaControls_instances, onStopIdle_fn).bind(this);
    effect(() => {
      if (!this.el) return;
      const isTouch = this.$state.pointer() === "coarse", events = new EventsController(this.el), eventTypes = [isTouch ? "touchend" : "pointerup", "keydown"];
      for (const eventType of eventTypes) events.add(eventType, onStopIdle, { passive: false });
    });
  }, onPlay_fn3 = function(event) {
    if (event.triggers.hasType("ended")) return;
    this.show(0, event);
    this.hide(void 0, event);
  }, onPause_fn4 = function(event) {
    this.show(0, event);
  }, onEnd_fn = function(event) {
    const { loop } = this.$state;
    if (loop()) this.hide(0, event);
  }, onMouseEnter_fn = function(event) {
    __privateGet(this, _isMouseOutside).set(false);
    this.show(0, event);
    this.hide(void 0, event);
  }, onMouseLeave_fn = function(event) {
    __privateGet(this, _isMouseOutside).set(true);
    this.hide(0, event);
  }, clearIdleTimer_fn = function() {
    window.clearTimeout(__privateGet(this, _idleTimer));
    __privateSet(this, _idleTimer, -1);
  }, onStopIdle_fn = function(event) {
    if (event.MEDIA_GESTURE || __privateGet(this, _pausedTracking) || isTouchPinchEvent(event)) return;
    if (isKeyboardEvent(event)) {
      if (event.key === "Escape") {
        this.el?.focus();
        __privateSet(this, _focusedItem, null);
      } else if (__privateGet(this, _focusedItem)) {
        event.preventDefault();
        requestAnimationFrame(() => {
          __privateGet(this, _focusedItem)?.focus();
          __privateSet(this, _focusedItem, null);
        });
      }
    }
    this.show(0, event);
    this.hide(this.defaultDelay, event);
  }, changeVisibility_fn = function(visible, delay, trigger) {
    if (delay === 0) {
      __privateMethod(this, _MediaControls_instances, onChange_fn2).call(this, visible, trigger);
      return;
    }
    __privateSet(this, _idleTimer, window.setTimeout(() => {
      if (!this.scope) return;
      __privateMethod(this, _MediaControls_instances, onChange_fn2).call(this, visible && !__privateGet(this, _pausedTracking), trigger);
    }, delay));
  }, onChange_fn2 = function(visible, trigger) {
    if (this.$state.controlsVisible() === visible) return;
    this.$state.controlsVisible.set(visible);
    if (!visible && document.activeElement && this.el?.contains(document.activeElement)) {
      __privateSet(this, _focusedItem, document.activeElement);
      requestAnimationFrame(() => {
        this.el?.focus({ preventScroll: true });
      });
    }
    this.dispatch("controls-change", {
      detail: visible,
      trigger
    });
  }, _a42);
  var CAN_FULLSCREEN = fscreen.fullscreenEnabled;
  var _listening, _active, _FullscreenController_instances, onDisconnect_fn, onChange_fn3, onError_fn5, _a43;
  var FullscreenController = (_a43 = class extends ViewController {
    constructor() {
      super(...arguments);
      __privateAdd(this, _FullscreenController_instances);
      /**
      * Tracks whether we're the active fullscreen event listener. Fullscreen events can only be
      * listened to globally on the document so we need to know if they relate to the current host
      * element or not.
      */
      __privateAdd(this, _listening, false);
      __privateAdd(this, _active, false);
    }
    get active() {
      return __privateGet(this, _active);
    }
    get supported() {
      return CAN_FULLSCREEN;
    }
    onConnect() {
      new EventsController(fscreen).add("fullscreenchange", __privateMethod(this, _FullscreenController_instances, onChange_fn3).bind(this)).add("fullscreenerror", __privateMethod(this, _FullscreenController_instances, onError_fn5).bind(this));
      onDispose(__privateMethod(this, _FullscreenController_instances, onDisconnect_fn).bind(this));
    }
    async enter() {
      try {
        __privateSet(this, _listening, true);
        if (!this.el || isFullscreen(this.el)) return;
        assertFullscreenAPI();
        return fscreen.requestFullscreen(this.el);
      } catch (error) {
        __privateSet(this, _listening, false);
        throw error;
      }
    }
    async exit() {
      if (!this.el || !isFullscreen(this.el)) return;
      assertFullscreenAPI();
      return fscreen.exitFullscreen();
    }
  }, _listening = new WeakMap(), _active = new WeakMap(), _FullscreenController_instances = new WeakSet(), onDisconnect_fn = function() {
    if (CAN_FULLSCREEN) this.exit().catch(noop);
  }, onChange_fn3 = function(event) {
    const active = isFullscreen(this.el);
    if (active === __privateGet(this, _active)) return;
    if (!active) __privateSet(this, _listening, false);
    __privateSet(this, _active, active);
    this.dispatch("fullscreen-change", {
      detail: active,
      trigger: event
    });
  }, onError_fn5 = function(event) {
    if (!__privateGet(this, _listening)) return;
    this.dispatch("fullscreen-error", {
      detail: null,
      trigger: event
    });
    __privateSet(this, _listening, false);
  }, _a43);
  function isFullscreen(host) {
    if (fscreen.fullscreenElement === host) return true;
    try {
      return host.matches(fscreen.fullscreenPseudoClass);
    } catch (error) {
      return false;
    }
  }
  function assertFullscreenAPI() {
    if (CAN_FULLSCREEN) return;
    throw Error("[vidstack] no fullscreen API");
  }
  var _type, _locked, _currentLock, _a44, _ScreenOrientationController_instances, onDisconnect_fn2, onOrientationChange_fn, assertScreenOrientationAPI_fn, getScreenOrientation_fn;
  var ScreenOrientationController = (_a44 = class extends ViewController {
    constructor() {
      super(...arguments);
      __privateAdd(this, _ScreenOrientationController_instances);
      __privateAdd(this, _type, signal(__privateMethod(this, _ScreenOrientationController_instances, getScreenOrientation_fn).call(this)));
      __privateAdd(this, _locked, signal(false));
      __privateAdd(this, _currentLock);
    }
    /**
    * The current screen orientation type.
    *
    * @signal
    * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/ScreenOrientation}
    * @see https://w3c.github.io/screen-orientation/#screen-orientation-types-and-locks
    */
    get type() {
      return __privateGet(this, _type).call(this);
    }
    /**
    * Whether the screen orientation is currently locked.
    *
    * @signal
    * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/ScreenOrientation}
    * @see https://w3c.github.io/screen-orientation/#screen-orientation-types-and-locks
    */
    get locked() {
      return __privateGet(this, _locked).call(this);
    }
    /**
    * Whether the viewport is in a portrait orientation.
    *
    * @signal
    */
    get portrait() {
      return __privateGet(this, _type).call(this).startsWith("portrait");
    }
    /**
    * Whether the viewport is in a landscape orientation.
    *
    * @signal
    */
    get landscape() {
      return __privateGet(this, _type).call(this).startsWith("landscape");
    }
    /**
    * Whether the native Screen Orientation API is available.
    */
    get supported() {
      return _a44.supported;
    }
    onConnect() {
      if (this.supported) listenEvent(screen.orientation, "change", __privateMethod(this, _ScreenOrientationController_instances, onOrientationChange_fn).bind(this));
      else {
        const query = window.matchMedia("(orientation: landscape)");
        query.onchange = __privateMethod(this, _ScreenOrientationController_instances, onOrientationChange_fn).bind(this);
        onDispose(() => query.onchange = null);
      }
      onDispose(__privateMethod(this, _ScreenOrientationController_instances, onDisconnect_fn2).bind(this));
    }
    /**
    * Locks the orientation of the screen to the desired orientation type using the
    * Screen Orientation API.
    *
    * @param lockType - The screen lock orientation type.
    * @throws Error - If screen orientation API is unavailable.
    * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/Screen/orientation}
    * @see {@link https://w3c.github.io/screen-orientation}
    */
    async lock(lockType) {
      if (peek(__privateGet(this, _locked)) || __privateGet(this, _currentLock) === lockType) return;
      __privateMethod(this, _ScreenOrientationController_instances, assertScreenOrientationAPI_fn).call(this);
      await screen.orientation.lock(lockType);
      __privateGet(this, _locked).set(true);
      __privateSet(this, _currentLock, lockType);
    }
    /**
    * Unlocks the orientation of the screen to it's default state using the Screen Orientation
    * API. This method will throw an error if the API is unavailable.
    *
    * @throws Error - If screen orientation API is unavailable.
    * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/Screen/orientation}
    * @see {@link https://w3c.github.io/screen-orientation}
    */
    async unlock() {
      if (!peek(__privateGet(this, _locked))) return;
      __privateMethod(this, _ScreenOrientationController_instances, assertScreenOrientationAPI_fn).call(this);
      __privateSet(this, _currentLock, void 0);
      await screen.orientation.unlock();
      __privateGet(this, _locked).set(false);
    }
  }, _type = new WeakMap(), _locked = new WeakMap(), _currentLock = new WeakMap(), _ScreenOrientationController_instances = new WeakSet(), onDisconnect_fn2 = async function() {
    if (this.supported && __privateGet(this, _locked).call(this)) await this.unlock();
  }, onOrientationChange_fn = function(event) {
    __privateGet(this, _type).set(__privateMethod(this, _ScreenOrientationController_instances, getScreenOrientation_fn).call(this));
    this.dispatch("orientation-change", {
      detail: {
        orientation: peek(__privateGet(this, _type)),
        lock: __privateGet(this, _currentLock)
      },
      trigger: event
    });
  }, assertScreenOrientationAPI_fn = function() {
    if (this.supported) return;
    throw Error("[vidstack] no orientation API");
  }, getScreenOrientation_fn = function() {
    if (IS_SERVER2) return "portrait-primary";
    if (this.supported) return window.screen.orientation.type;
    return window.innerWidth >= window.innerHeight ? "landscape-primary" : "portrait-primary";
  }, /**
  * Whether the native Screen Orientation API is available.
  */
  __publicField(_a44, "supported", canOrientScreen()), _a44);
  var AudioProviderLoader = class {
    constructor() {
      __publicField(this, "name", "audio");
      __publicField(this, "target");
    }
    canPlay(src) {
      if (!isAudioSrc(src)) return false;
      return IS_SERVER2 || !isString(src.src) || src.type === "?" || canPlayAudioType(this.target, src.type);
    }
    mediaType() {
      return "audio";
    }
    async load(ctx) {
      if (IS_SERVER2) throw Error("[vidstack] can not load audio provider server-side");
      return new (await Promise.resolve().then(() => (init_vidstack_CHcsWfRV(), vidstack_CHcsWfRV_exports))).AudioProvider(this.target, ctx);
    }
  };
  var VideoProviderLoader = class {
    constructor() {
      __publicField(this, "name", "video");
      __publicField(this, "target");
    }
    canPlay(src) {
      if (!isVideoSrc(src)) return false;
      return IS_SERVER2 || !isString(src.src) || src.type === "?" || canPlayVideoType(this.target, src.type);
    }
    mediaType() {
      return "video";
    }
    async load(ctx) {
      if (IS_SERVER2) throw Error("[vidstack] can not load video provider server-side");
      return new (await Promise.resolve().then(() => (init_vidstack_CvezEmSQ(), vidstack_CvezEmSQ_exports)).then((n2) => n2.provider_exports$1)).VideoProvider(this.target, ctx);
    }
  };
  var _a45;
  var HLSProviderLoader = (_a45 = class extends VideoProviderLoader {
    constructor() {
      super(...arguments);
      __publicField(this, "name", "hls");
    }
    canPlay(src) {
      return _a45.supported && isHLSSrc(src);
    }
    async load(context) {
      if (IS_SERVER2) throw Error("[vidstack] can not load hls provider server-side");
      return new (await Promise.resolve().then(() => (init_vidstack_DiaLSJYx(), vidstack_DiaLSJYx_exports))).HLSProvider(this.target, context);
    }
  }, __publicField(_a45, "supported", isHLSSupported()), _a45);
  var _a46;
  var DASHProviderLoader = (_a46 = class extends VideoProviderLoader {
    constructor() {
      super(...arguments);
      __publicField(this, "name", "dash");
    }
    canPlay(src) {
      return _a46.supported && isDASHSrc(src);
    }
    async load(context) {
      if (IS_SERVER2) throw Error("[vidstack] can not load dash provider server-side");
      return new (await Promise.resolve().then(() => (init_vidstack_CvezEmSQ(), vidstack_CvezEmSQ_exports)).then((n2) => n2.provider_exports)).DASHProvider(this.target, context);
    }
  }, __publicField(_a46, "supported", isDASHSupported()), _a46);
  var VimeoProviderLoader = class {
    constructor() {
      __publicField(this, "name", "vimeo");
      __publicField(this, "target");
    }
    preconnect() {
      for (const url of [
        "https://i.vimeocdn.com",
        "https://f.vimeocdn.com",
        "https://fresnel.vimeocdn.com"
      ]) preconnect(url);
    }
    canPlay(src) {
      return isString(src.src) && src.type === "video/vimeo";
    }
    mediaType() {
      return "video";
    }
    async load(ctx) {
      if (IS_SERVER2) throw Error("[vidstack] can not load vimeo provider server-side");
      return new (await Promise.resolve().then(() => (init_vidstack_GkTzay8D(), vidstack_GkTzay8D_exports))).VimeoProvider(this.target, ctx);
    }
    async loadPoster(src, ctx, abort) {
      const { resolveVimeoVideoId: resolveVimeoVideoId2, getVimeoVideoInfo: getVimeoVideoInfo2 } = await Promise.resolve().then(() => (init_vidstack_C0fPJIzs(), vidstack_C0fPJIzs_exports));
      if (!isString(src.src)) return null;
      const { videoId, hash } = resolveVimeoVideoId2(src.src);
      if (videoId) return getVimeoVideoInfo2(videoId, abort, hash).then((info) => info ? info.poster : null);
      return null;
    }
  };
  var YouTubeProviderLoader = class {
    constructor() {
      __publicField(this, "name", "youtube");
      __publicField(this, "target");
    }
    preconnect() {
      for (const url of [
        "https://www.google.com",
        "https://i.ytimg.com",
        "https://googleads.g.doubleclick.net",
        "https://static.doubleclick.net"
      ]) preconnect(url);
    }
    canPlay(src) {
      return isString(src.src) && src.type === "video/youtube";
    }
    mediaType() {
      return "video";
    }
    async load(ctx) {
      if (IS_SERVER2) throw Error("[vidstack] can not load youtube provider server-side");
      return new (await Promise.resolve().then(() => (init_vidstack_BmcqUMKx(), vidstack_BmcqUMKx_exports))).YouTubeProvider(this.target, ctx);
    }
    async loadPoster(src, ctx, abort) {
      const { findYouTubePoster: findYouTubePoster2, resolveYouTubeVideoId: resolveYouTubeVideoId2 } = await Promise.resolve().then(() => (init_vidstack_Dy74GavT(), vidstack_Dy74GavT_exports));
      const videoId = isString(src.src) && resolveYouTubeVideoId2(src.src);
      if (videoId) return findYouTubePoster2(videoId, abort);
      return null;
    }
  };
  function padNumberWithZeroes(num, expectedLength) {
    const str = String(num);
    const actualLength = str.length;
    if (actualLength < expectedLength) {
      const padLength = expectedLength - actualLength;
      return `${`0`.repeat(padLength)}${num}`;
    }
    return str;
  }
  function parseTime(duration) {
    return {
      hours: Math.trunc(duration / 3600),
      minutes: Math.trunc(duration % 3600 / 60),
      seconds: Math.trunc(duration % 60),
      fraction: Number((duration - Math.trunc(duration)).toPrecision(3))
    };
  }
  function formatTime2(duration, { padHrs = null, padMins = null, showHrs = false, showMs = false } = {}) {
    const { hours, minutes, seconds, fraction } = parseTime(duration), paddedHours = padHrs ? padNumberWithZeroes(hours, 2) : hours, time = `${padMins || isNull(padMins) && duration >= 3600 ? padNumberWithZeroes(minutes, 2) : minutes}:${padNumberWithZeroes(seconds, 2)}${showMs && fraction > 0 ? `.${String(fraction).replace(/^0?\./, "")}` : ""}`;
    return hours > 0 || showHrs ? `${paddedHours}:${time}` : time;
  }
  function formatSpokenTime(duration) {
    const spokenParts = [];
    const { hours, minutes, seconds } = parseTime(duration);
    if (hours > 0) spokenParts.push(`${hours} hour`);
    if (minutes > 0) spokenParts.push(`${minutes} min`);
    if (seconds > 0 || spokenParts.length === 0) spokenParts.push(`${seconds} sec`);
    return spokenParts.join(" ");
  }
  var MEDIA_ATTRIBUTES = /* @__PURE__ */ Symbol(0);
  var mediaAttributes = [
    "autoPlay",
    "canAirPlay",
    "canFullscreen",
    "canGoogleCast",
    "canLoad",
    "canLoadPoster",
    "canPictureInPicture",
    "canPlay",
    "canSeek",
    "ended",
    "fullscreen",
    "isAirPlayConnected",
    "isGoogleCastConnected",
    "live",
    "liveEdge",
    "loop",
    "mediaType",
    "muted",
    "paused",
    "pictureInPicture",
    "playing",
    "playsInline",
    "remotePlaybackState",
    "remotePlaybackType",
    "seeking",
    "started",
    "streamType",
    "viewType",
    "waiting"
  ];
  var mediaPlayerProps = {
    artist: "",
    artwork: null,
    ariaLabel: "",
    autoplay: false,
    autoPlay: false,
    clipStartTime: 0,
    clipEndTime: 0,
    controls: false,
    currentTime: 0,
    crossorigin: null,
    crossOrigin: null,
    duration: -1,
    fullscreenOrientation: "landscape",
    googleCast: {},
    load: "visible",
    posterLoad: "visible",
    logLevel: "silent",
    loop: false,
    muted: false,
    paused: true,
    playsinline: false,
    playsInline: false,
    playbackRate: 1,
    poster: "",
    preload: "metadata",
    preferNativeHLS: false,
    src: "",
    title: "",
    controlsDelay: 2e3,
    hideControlsOnMouseLeave: false,
    viewType: "unknown",
    streamType: "unknown",
    volume: 1,
    liveEdgeTolerance: 10,
    minLiveDVRWindow: 60,
    keyDisabled: false,
    keyTarget: "player",
    keyShortcuts: MEDIA_KEY_SHORTCUTS,
    storage: null
  };
  var _type2, _callback4, _a47;
  var MediaLoadController = (_a47 = class extends MediaPlayerController {
    constructor(type, callback) {
      super();
      __privateAdd(this, _type2);
      __privateAdd(this, _callback4);
      __privateSet(this, _type2, type);
      __privateSet(this, _callback4, callback);
    }
    async onAttach(el) {
      if (IS_SERVER2) return;
      const load = this.$props[__privateGet(this, _type2)]();
      if (load === "eager") requestAnimationFrame(__privateGet(this, _callback4));
      else if (load === "idle") waitIdlePeriod(__privateGet(this, _callback4));
      else if (load === "visible") {
        let dispose2, observer = new IntersectionObserver((entries) => {
          if (!this.scope) return;
          if (entries[0].isIntersecting) {
            dispose2?.();
            dispose2 = void 0;
            __privateGet(this, _callback4).call(this);
          }
        });
        observer.observe(el);
        dispose2 = onDispose(() => observer.disconnect());
      }
    }
  }, _type2 = new WeakMap(), _callback4 = new WeakMap(), _a47);
  var _handle, _media7, _MediaPlayerDelegate_instances, attemptAutoplay_fn, _a48;
  var MediaPlayerDelegate = (_a48 = class {
    constructor(handle, media) {
      __privateAdd(this, _MediaPlayerDelegate_instances);
      __privateAdd(this, _handle);
      __privateAdd(this, _media7);
      __privateSet(this, _handle, handle);
      __privateSet(this, _media7, media);
    }
    notify(type, ...init) {
      if (IS_SERVER2) return;
      __privateGet(this, _handle).call(this, new DOMEvent(type, {
        detail: init?.[0],
        trigger: init?.[1]
      }));
    }
    async ready(info, trigger) {
      if (IS_SERVER2) return;
      return untrack(async () => {
        const { logger } = __privateGet(this, _media7), { autoPlay, canPlay, started, duration, seekable, buffered, remotePlaybackInfo, playsInline, savedState, source } = __privateGet(this, _media7).$state;
        if (canPlay()) return;
        const detail = {
          duration: info?.duration ?? duration(),
          seekable: info?.seekable ?? seekable(),
          buffered: info?.buffered ?? buffered(),
          provider: __privateGet(this, _media7).$provider()
        };
        this.notify("can-play", detail, trigger);
        tick();
        let provider = __privateGet(this, _media7).$provider(), { storage, qualities } = __privateGet(this, _media7), { muted, volume, clipStartTime, playbackRate } = __privateGet(this, _media7).$props;
        await storage?.onLoad?.(source());
        const savedPlaybackTime = savedState()?.currentTime, savedPausedState = savedState()?.paused, storageTime = await storage?.getTime(), startTime = savedPlaybackTime ?? storageTime ?? clipStartTime(), shouldAutoPlay = savedPausedState === false || savedPausedState !== true && !started() && autoPlay();
        if (provider) {
          provider.setVolume(await storage?.getVolume() ?? volume());
          provider.setMuted(muted() || !!await storage?.getMuted());
          const audioGain = await storage?.getAudioGain() ?? 1;
          if (audioGain > 1) provider.audioGain?.setGain?.(audioGain);
          provider.setPlaybackRate?.(await storage?.getPlaybackRate() ?? playbackRate());
          provider.setPlaysInline?.(playsInline());
          if (startTime > 0) provider.setCurrentTime(startTime);
        }
        const prefQuality = await storage?.getVideoQuality();
        if (prefQuality && qualities.length) {
          let currentQuality = null, currentScore = Infinity;
          for (const quality of qualities) {
            const score = Math.abs(prefQuality.width - quality.width) + Math.abs(prefQuality.height - quality.height) + (prefQuality.bitrate ? Math.abs(prefQuality.bitrate - (quality.bitrate ?? 0)) : 0);
            if (score < currentScore) {
              currentQuality = quality;
              currentScore = score;
            }
          }
          if (currentQuality) currentQuality.selected = true;
        }
        if (canPlay() && shouldAutoPlay) await __privateMethod(this, _MediaPlayerDelegate_instances, attemptAutoplay_fn).call(this, trigger);
        else if (storageTime && storageTime > 0) this.notify("started", void 0, trigger);
        remotePlaybackInfo.set(null);
      });
    }
  }, _handle = new WeakMap(), _media7 = new WeakMap(), _MediaPlayerDelegate_instances = new WeakSet(), attemptAutoplay_fn = async function(trigger) {
    const { player, $state: { autoPlaying, muted } } = __privateGet(this, _media7);
    autoPlaying.set(true);
    const attemptEvent = new DOMEvent("auto-play-attempt", { trigger });
    try {
      await player.play(attemptEvent);
    } catch (error) {
    }
  }, _a48);
  var _queue, _a49;
  var Queue = (_a49 = class {
    constructor() {
      __privateAdd(this, _queue, /* @__PURE__ */ new Map());
    }
    /**
    * Queue the given `item` under the given `key` to be processed at a later time by calling
    * `serve(key)`.
    */
    enqueue(key2, item) {
      __privateGet(this, _queue).set(key2, item);
    }
    /**
    * Process item in queue for the given `key`.
    */
    serve(key2) {
      const value = this.peek(key2);
      __privateGet(this, _queue).delete(key2);
      return value;
    }
    /**
    * Peek at item in queue for the given `key`.
    */
    peek(key2) {
      return __privateGet(this, _queue).get(key2);
    }
    /**
    * Removes queued item under the given `key`.
    */
    delete(key2) {
      __privateGet(this, _queue).delete(key2);
    }
    /**
    * Clear all items in the queue.
    */
    clear() {
      __privateGet(this, _queue).clear();
    }
  }, _queue = new WeakMap(), _a49);
  var _serving, _pending, _queue2, _RequestQueue_instances, flush_fn, release_fn, _a50;
  var RequestQueue = (_a50 = class {
    constructor() {
      __privateAdd(this, _RequestQueue_instances);
      __privateAdd(this, _serving, false);
      __privateAdd(this, _pending, deferredPromise());
      __privateAdd(this, _queue2, /* @__PURE__ */ new Map());
    }
    /**
    * The number of callbacks that are currently in queue.
    */
    get size() {
      return __privateGet(this, _queue2).size;
    }
    /**
    * Whether items in the queue are being served immediately, otherwise they're queued to
    * be processed later.
    */
    get isServing() {
      return __privateGet(this, _serving);
    }
    /**
    * Waits for the queue to be flushed (ie: start serving).
    */
    async waitForFlush() {
      if (__privateGet(this, _serving)) return;
      await __privateGet(this, _pending).promise;
    }
    /**
    * Queue the given `callback` to be invoked at a later time by either calling the `serve()` or
    * `start()` methods. If the queue has started serving (i.e., `start()` was already called),
    * then the callback will be invoked immediately.
    *
    * @param key - Uniquely identifies this callback so duplicates are ignored.
    * @param callback - The function to call when this item in the queue is being served.
    */
    enqueue(key2, callback) {
      if (__privateGet(this, _serving)) {
        callback();
        return;
      }
      __privateGet(this, _queue2).delete(key2);
      __privateGet(this, _queue2).set(key2, callback);
    }
    /**
    * Invokes the callback with the given `key` in the queue (if it exists).
    */
    serve(key2) {
      __privateGet(this, _queue2).get(key2)?.();
      __privateGet(this, _queue2).delete(key2);
    }
    /**
    * Flush all queued items and start serving future requests immediately until `stop()` is called.
    */
    start() {
      __privateMethod(this, _RequestQueue_instances, flush_fn).call(this);
      __privateSet(this, _serving, true);
      if (__privateGet(this, _queue2).size > 0) __privateMethod(this, _RequestQueue_instances, flush_fn).call(this);
    }
    /**
    * Stop serving requests, they'll be queued until you begin processing again by calling `start()`.
    */
    stop() {
      __privateSet(this, _serving, false);
    }
    /**
    * Stop serving requests, empty the request queue, and release any promises waiting for the
    * queue to flush.
    */
    reset() {
      this.stop();
      __privateGet(this, _queue2).clear();
      __privateMethod(this, _RequestQueue_instances, release_fn).call(this);
    }
  }, _serving = new WeakMap(), _pending = new WeakMap(), _queue2 = new WeakMap(), _RequestQueue_instances = new WeakSet(), flush_fn = function() {
    for (const key2 of __privateGet(this, _queue2).keys()) this.serve(key2);
    __privateMethod(this, _RequestQueue_instances, release_fn).call(this);
  }, release_fn = function() {
    __privateGet(this, _pending).resolve();
    __privateSet(this, _pending, deferredPromise());
  }, _a50);
  function ariaBool2(value) {
    return value ? "true" : "false";
  }
  function $ariaBool(signal2) {
    return () => ariaBool2(signal2());
  }
  function prefersReducedMotion() {
    if (typeof window === "undefined") return false;
    return window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  }
  var _stateMgr, _request, _media8, _fullscreen, _orientation, _$provider, _providerQueue, _MediaRequestManager_instances, attachLoadPlayListener_fn, watchProvider_fn, handleRequest_fn, handleLoadPlayStrategy_fn, _wasPIPActive, getFullscreenAdapter_fn, throwIfPIPNotSupported_fn, watchControlsDelayChange_fn, watchAudioGainSupport_fn, watchAirPlaySupport_fn, watchGoogleCastSupport_fn, watchFullscreenSupport_fn, watchPiPSupport_fn, _googleCastLoader, onFullscreenChange_fn, onFullscreenError_fn, onPictureInPictureError_fn, logError_fn, _a51;
  var MediaRequestManager = (_a51 = class extends MediaPlayerController {
    constructor(stateMgr, request, media) {
      super();
      __privateAdd(this, _MediaRequestManager_instances);
      __privateAdd(this, _stateMgr);
      __privateAdd(this, _request);
      __privateAdd(this, _media8);
      __publicField(this, "controls");
      __privateAdd(this, _fullscreen);
      __privateAdd(this, _orientation);
      __privateAdd(this, _$provider);
      __privateAdd(this, _providerQueue, new RequestQueue());
      __privateAdd(this, _wasPIPActive, false);
      __privateAdd(this, _googleCastLoader);
      __privateSet(this, _stateMgr, stateMgr);
      __privateSet(this, _request, request);
      __privateSet(this, _media8, media);
      __privateSet(this, _$provider, media.$provider);
      this.controls = new MediaControls();
      __privateSet(this, _fullscreen, new FullscreenController());
      __privateSet(this, _orientation, new ScreenOrientationController());
    }
    onAttach() {
      this.listen("fullscreen-change", __privateMethod(this, _MediaRequestManager_instances, onFullscreenChange_fn).bind(this));
    }
    onConnect(el) {
      const names = Object.getOwnPropertyNames(Object.getPrototypeOf(this)), events = new EventsController(el), handleRequest = __privateMethod(this, _MediaRequestManager_instances, handleRequest_fn).bind(this);
      for (const name of names) if (name.startsWith("media-")) events.add(name, handleRequest);
      __privateMethod(this, _MediaRequestManager_instances, attachLoadPlayListener_fn).call(this);
      effect(__privateMethod(this, _MediaRequestManager_instances, watchProvider_fn).bind(this));
      effect(__privateMethod(this, _MediaRequestManager_instances, watchControlsDelayChange_fn).bind(this));
      effect(__privateMethod(this, _MediaRequestManager_instances, watchAudioGainSupport_fn).bind(this));
      effect(__privateMethod(this, _MediaRequestManager_instances, watchAirPlaySupport_fn).bind(this));
      effect(__privateMethod(this, _MediaRequestManager_instances, watchGoogleCastSupport_fn).bind(this));
      effect(__privateMethod(this, _MediaRequestManager_instances, watchFullscreenSupport_fn).bind(this));
      effect(__privateMethod(this, _MediaRequestManager_instances, watchPiPSupport_fn).bind(this));
    }
    onDestroy() {
      try {
        const destroyEvent = this.createEvent("destroy"), { pictureInPicture, fullscreen } = this.$state;
        if (fullscreen()) this.exitFullscreen("prefer-media", destroyEvent);
        if (pictureInPicture()) this.exitPictureInPicture(destroyEvent);
      } catch (e2) {
      }
      __privateGet(this, _providerQueue).reset();
    }
    async play(trigger) {
      if (IS_SERVER2) return;
      const { canPlay, paused, autoPlaying } = this.$state;
      if (__privateMethod(this, _MediaRequestManager_instances, handleLoadPlayStrategy_fn).call(this, trigger)) return;
      if (!peek(paused)) return;
      if (trigger) __privateGet(this, _request).queue.enqueue("media-play-request", trigger);
      const isAutoPlaying = peek(autoPlaying);
      try {
        const provider = peek(__privateGet(this, _$provider));
        throwIfNotReadyForPlayback(provider, peek(canPlay));
        throwIfAutoplayingWithReducedMotion(isAutoPlaying);
        return await provider.play();
      } catch (error) {
        const errorEvent = this.createEvent("play-fail", {
          detail: coerceToError(error),
          trigger
        });
        errorEvent.autoPlay = isAutoPlaying;
        __privateGet(this, _stateMgr).handle(errorEvent);
        throw error;
      }
    }
    async pause(trigger) {
      if (IS_SERVER2) return;
      const { canPlay, paused } = this.$state;
      if (peek(paused)) return;
      if (trigger) __privateGet(this, _request).queue.enqueue("media-pause-request", trigger);
      try {
        const provider = peek(__privateGet(this, _$provider));
        throwIfNotReadyForPlayback(provider, peek(canPlay));
        return await provider.pause();
      } catch (error) {
        __privateGet(this, _request).queue.delete("media-pause-request");
        throw error;
      }
    }
    setAudioGain(gain, trigger) {
      const { audioGain, canSetAudioGain } = this.$state;
      if (audioGain() === gain) return;
      const provider = __privateGet(this, _$provider).call(this);
      if (!provider?.audioGain || !canSetAudioGain()) throw Error("[vidstack] audio gain api not available");
      if (trigger) __privateGet(this, _request).queue.enqueue("media-audio-gain-change-request", trigger);
      provider.audioGain.setGain(gain);
    }
    seekToLiveEdge(trigger) {
      if (IS_SERVER2) return;
      const { canPlay, live, liveEdge, canSeek, liveSyncPosition, seekableEnd, userBehindLiveEdge } = this.$state;
      userBehindLiveEdge.set(false);
      if (peek(() => !live() || canSeek() && liveEdge())) return;
      const provider = peek(__privateGet(this, _$provider));
      throwIfNotReadyForPlayback(provider, peek(canPlay));
      const end = seekableEnd() - 2, time = Math.min(end, liveSyncPosition() ?? end);
      if (!Number.isFinite(time)) return;
      if (trigger) __privateGet(this, _request).queue.enqueue("media-seek-request", trigger);
      provider.setCurrentTime(time);
    }
    async enterFullscreen(target = "prefer-media", trigger) {
      if (IS_SERVER2) return;
      const adapter = __privateMethod(this, _MediaRequestManager_instances, getFullscreenAdapter_fn).call(this, target);
      throwIfFullscreenNotSupported(target, adapter);
      if (adapter.active) return;
      if (peek(this.$state.pictureInPicture)) {
        __privateSet(this, _wasPIPActive, true);
        await this.exitPictureInPicture(trigger);
      }
      if (trigger) __privateGet(this, _request).queue.enqueue("media-enter-fullscreen-request", trigger);
      return adapter.enter();
    }
    async exitFullscreen(target = "prefer-media", trigger) {
      if (IS_SERVER2) return;
      const adapter = __privateMethod(this, _MediaRequestManager_instances, getFullscreenAdapter_fn).call(this, target);
      throwIfFullscreenNotSupported(target, adapter);
      if (!adapter.active) return;
      if (trigger) __privateGet(this, _request).queue.enqueue("media-exit-fullscreen-request", trigger);
      try {
        const result = await adapter.exit();
        if (__privateGet(this, _wasPIPActive) && peek(this.$state.canPictureInPicture)) await this.enterPictureInPicture();
        return result;
      } finally {
        __privateSet(this, _wasPIPActive, false);
      }
    }
    async enterPictureInPicture(trigger) {
      if (IS_SERVER2) return;
      __privateMethod(this, _MediaRequestManager_instances, throwIfPIPNotSupported_fn).call(this);
      if (this.$state.pictureInPicture()) return;
      if (trigger) __privateGet(this, _request).queue.enqueue("media-enter-pip-request", trigger);
      return await __privateGet(this, _$provider).call(this).pictureInPicture.enter();
    }
    async exitPictureInPicture(trigger) {
      if (IS_SERVER2) return;
      __privateMethod(this, _MediaRequestManager_instances, throwIfPIPNotSupported_fn).call(this);
      if (!this.$state.pictureInPicture()) return;
      if (trigger) __privateGet(this, _request).queue.enqueue("media-exit-pip-request", trigger);
      try {
        if (document?.pictureInPictureElement && document.exitPictureInPicture) {
          await document.exitPictureInPicture();
          return;
        }
        const provider = __privateGet(this, _$provider).call(this);
        if (provider?.pictureInPicture) {
          await provider.pictureInPicture.exit();
          return;
        }
        const videoElements = document?.querySelectorAll?.("video") || [];
        for (const video of videoElements) {
          if (video === document.pictureInPictureElement) {
            if (document.exitPictureInPicture) {
              await document.exitPictureInPicture();
              return;
            }
          }
          if (video.webkitPresentationMode === "picture-in-picture") {
            video.webkitSetPresentationMode("inline");
            return;
          }
        }
      } catch (error) {
      }
    }
    async ["media-airplay-request"](event) {
      try {
        await this.requestAirPlay(event);
      } catch (error) {
      }
    }
    async requestAirPlay(trigger) {
      try {
        const adapter = __privateGet(this, _$provider).call(this)?.airPlay;
        if (!adapter?.supported) throw Error("No AirPlay adapter.");
        if (trigger) __privateGet(this, _request).queue.enqueue("media-airplay-request", trigger);
        return await adapter.prompt();
      } catch (error) {
        __privateGet(this, _request).queue.delete("media-airplay-request");
        throw error;
      }
    }
    async ["media-google-cast-request"](event) {
      try {
        await this.requestGoogleCast(event);
      } catch (error) {
      }
    }
    async requestGoogleCast(trigger) {
      try {
        const { canGoogleCast } = this.$state;
        if (!peek(canGoogleCast)) {
          const error = Error("Cast not available.");
          error.code = "CAST_NOT_AVAILABLE";
          throw error;
        }
        preconnect("https://www.gstatic.com");
        if (!__privateGet(this, _googleCastLoader)) {
          const $module = await Promise.resolve().then(() => (init_vidstack_Di0HObvT(), vidstack_Di0HObvT_exports));
          __privateSet(this, _googleCastLoader, new $module.GoogleCastLoader());
        }
        await __privateGet(this, _googleCastLoader).prompt(__privateGet(this, _media8));
        if (trigger) __privateGet(this, _request).queue.enqueue("media-google-cast-request", trigger);
        const isConnecting = peek(this.$state.remotePlaybackState) !== "disconnected";
        if (isConnecting) this.$state.savedState.set({
          paused: peek(this.$state.paused),
          currentTime: peek(this.$state.currentTime)
        });
        this.$state.remotePlaybackLoader.set(isConnecting ? __privateGet(this, _googleCastLoader) : null);
      } catch (error) {
        __privateGet(this, _request).queue.delete("media-google-cast-request");
        throw error;
      }
    }
    ["media-clip-start-change-request"](event) {
      const { clipStartTime } = this.$state;
      clipStartTime.set(event.detail);
    }
    ["media-clip-end-change-request"](event) {
      const { clipEndTime } = this.$state;
      clipEndTime.set(event.detail);
      this.dispatch("duration-change", {
        detail: event.detail,
        trigger: event
      });
    }
    ["media-duration-change-request"](event) {
      const { providedDuration, clipEndTime } = this.$state;
      providedDuration.set(event.detail);
      if (clipEndTime() <= 0) this.dispatch("duration-change", {
        detail: event.detail,
        trigger: event
      });
    }
    ["media-audio-track-change-request"](event) {
      const { logger, audioTracks } = __privateGet(this, _media8);
      if (audioTracks.readonly) return;
      const track = audioTracks[event.detail];
      if (track) {
        const key2 = event.type;
        __privateGet(this, _request).queue.enqueue(key2, event);
        track.selected = true;
      }
    }
    async ["media-enter-fullscreen-request"](event) {
      try {
        await this.enterFullscreen(event.detail, event);
      } catch (error) {
        __privateMethod(this, _MediaRequestManager_instances, onFullscreenError_fn).call(this, error, event);
      }
    }
    async ["media-exit-fullscreen-request"](event) {
      try {
        await this.exitFullscreen(event.detail, event);
      } catch (error) {
        __privateMethod(this, _MediaRequestManager_instances, onFullscreenError_fn).call(this, error, event);
      }
    }
    async ["media-orientation-lock-request"](event) {
      const key2 = event.type;
      try {
        __privateGet(this, _request).queue.enqueue(key2, event);
        await __privateGet(this, _orientation).lock(event.detail);
      } catch (error) {
        __privateGet(this, _request).queue.delete(key2);
      }
    }
    async ["media-orientation-unlock-request"](event) {
      const key2 = event.type;
      try {
        __privateGet(this, _request).queue.enqueue(key2, event);
        await __privateGet(this, _orientation).unlock();
      } catch (error) {
        __privateGet(this, _request).queue.delete(key2);
      }
    }
    async ["media-enter-pip-request"](event) {
      try {
        await this.enterPictureInPicture(event);
      } catch (error) {
        __privateMethod(this, _MediaRequestManager_instances, onPictureInPictureError_fn).call(this, error, event);
      }
    }
    async ["media-exit-pip-request"](event) {
      try {
        await this.exitPictureInPicture(event);
      } catch (error) {
        __privateMethod(this, _MediaRequestManager_instances, onPictureInPictureError_fn).call(this, error, event);
      }
    }
    ["media-live-edge-request"](event) {
      const { live, liveEdge, canSeek } = this.$state;
      if (!live() || canSeek() && liveEdge()) return;
      try {
        this.seekToLiveEdge(event);
      } catch (error) {
        __privateGet(this, _request).queue.delete("media-seek-request");
      }
    }
    async ["media-loop-request"](event) {
      try {
        __privateGet(this, _request).looping = true;
        __privateGet(this, _request).replaying = true;
        await this.play(event);
      } catch (error) {
        __privateGet(this, _request).looping = false;
      }
    }
    ["media-user-loop-change-request"](event) {
      this.$state.userPrefersLoop.set(event.detail);
    }
    async ["media-pause-request"](event) {
      if (this.$state.paused()) return;
      try {
        await this.pause(event);
      } catch (error) {
      }
    }
    async ["media-play-request"](event) {
      if (!this.$state.paused()) return;
      try {
        await this.play(event);
      } catch (e2) {
      }
    }
    ["media-rate-change-request"](event) {
      const { playbackRate, canSetPlaybackRate } = this.$state;
      if (playbackRate() === event.detail || !canSetPlaybackRate()) return;
      const provider = __privateGet(this, _$provider).call(this);
      if (!provider?.setPlaybackRate) return;
      __privateGet(this, _request).queue.enqueue("media-rate-change-request", event);
      provider.setPlaybackRate(event.detail);
    }
    ["media-audio-gain-change-request"](event) {
      try {
        this.setAudioGain(event.detail, event);
      } catch (e2) {
      }
    }
    ["media-quality-change-request"](event) {
      const { qualities, storage, logger } = __privateGet(this, _media8);
      if (qualities.readonly) return;
      __privateGet(this, _request).queue.enqueue("media-quality-change-request", event);
      const index = event.detail;
      if (index < 0) {
        qualities.autoSelect(event);
        if (event.isOriginTrusted) storage?.setVideoQuality?.(null);
      } else {
        const quality = qualities[index];
        if (quality) {
          quality.selected = true;
          if (event.isOriginTrusted) storage?.setVideoQuality?.({
            id: quality.id,
            width: quality.width,
            height: quality.height,
            bitrate: quality.bitrate
          });
        }
      }
    }
    ["media-pause-controls-request"](event) {
      const key2 = event.type;
      __privateGet(this, _request).queue.enqueue(key2, event);
      this.controls.pause(event);
    }
    ["media-resume-controls-request"](event) {
      const key2 = event.type;
      __privateGet(this, _request).queue.enqueue(key2, event);
      this.controls.resume(event);
    }
    ["media-seek-request"](event) {
      const { canSeek, ended, live, seekableEnd, userBehindLiveEdge } = this.$state, seekTime = event.detail;
      if (ended()) __privateGet(this, _request).replaying = true;
      const key2 = event.type;
      __privateGet(this, _request).seeking = false;
      __privateGet(this, _request).queue.delete(key2);
      const boundedTime = boundTime(seekTime, this.$state);
      if (!Number.isFinite(boundedTime) || !canSeek()) return;
      __privateGet(this, _request).queue.enqueue(key2, event);
      __privateGet(this, _$provider).call(this).setCurrentTime(boundedTime);
      if (live() && event.isOriginTrusted && Math.abs(seekableEnd() - boundedTime) >= 2) userBehindLiveEdge.set(true);
    }
    ["media-seeking-request"](event) {
      const key2 = event.type;
      __privateGet(this, _request).queue.enqueue(key2, event);
      this.$state.seeking.set(true);
      __privateGet(this, _request).seeking = true;
    }
    ["media-start-loading"](event) {
      if (this.$state.canLoad()) return;
      const key2 = event.type;
      __privateGet(this, _request).queue.enqueue(key2, event);
      __privateGet(this, _stateMgr).handle(this.createEvent("can-load"));
    }
    ["media-poster-start-loading"](event) {
      if (this.$state.canLoadPoster()) return;
      const key2 = event.type;
      __privateGet(this, _request).queue.enqueue(key2, event);
      __privateGet(this, _stateMgr).handle(this.createEvent("can-load-poster"));
    }
    ["media-text-track-change-request"](event) {
      const { index, mode } = event.detail, track = __privateGet(this, _media8).textTracks[index];
      if (track) {
        const key2 = event.type;
        __privateGet(this, _request).queue.enqueue(key2, event);
        track.setMode(mode, event);
      }
    }
    ["media-mute-request"](event) {
      if (this.$state.muted()) return;
      const key2 = event.type;
      __privateGet(this, _request).queue.enqueue(key2, event);
      __privateGet(this, _$provider).call(this).setMuted(true);
    }
    ["media-unmute-request"](event) {
      const { muted, volume } = this.$state;
      if (!muted()) return;
      const key2 = event.type;
      __privateGet(this, _request).queue.enqueue(key2, event);
      __privateGet(this, _media8).$provider().setMuted(false);
      if (volume() === 0) {
        __privateGet(this, _request).queue.enqueue(key2, event);
        __privateGet(this, _$provider).call(this).setVolume(0.25);
      }
    }
    ["media-volume-change-request"](event) {
      const { muted, volume } = this.$state;
      const newVolume = event.detail;
      if (volume() === newVolume) return;
      const key2 = event.type;
      __privateGet(this, _request).queue.enqueue(key2, event);
      __privateGet(this, _$provider).call(this).setVolume(newVolume);
      if (newVolume > 0 && muted()) {
        __privateGet(this, _request).queue.enqueue(key2, event);
        __privateGet(this, _$provider).call(this).setMuted(false);
      }
    }
  }, _stateMgr = new WeakMap(), _request = new WeakMap(), _media8 = new WeakMap(), _fullscreen = new WeakMap(), _orientation = new WeakMap(), _$provider = new WeakMap(), _providerQueue = new WeakMap(), _MediaRequestManager_instances = new WeakSet(), attachLoadPlayListener_fn = function() {
    const { load } = this.$props, { canLoad } = this.$state;
    if (load() !== "play" || canLoad()) return;
    const off = this.listen("media-play-request", (event) => {
      __privateMethod(this, _MediaRequestManager_instances, handleLoadPlayStrategy_fn).call(this, event);
      off();
    });
  }, watchProvider_fn = function() {
    const provider = __privateGet(this, _$provider).call(this), canPlay = this.$state.canPlay();
    if (provider && canPlay) __privateGet(this, _providerQueue).start();
    return () => {
      __privateGet(this, _providerQueue).stop();
    };
  }, handleRequest_fn = function(event) {
    event.stopPropagation();
    if (event.defaultPrevented) return;
    if (!this[event.type]) return;
    if (peek(__privateGet(this, _$provider))) this[event.type](event);
    else __privateGet(this, _providerQueue).enqueue(event.type, () => {
      if (peek(__privateGet(this, _$provider))) this[event.type](event);
    });
  }, handleLoadPlayStrategy_fn = function(trigger) {
    const { load } = this.$props, { canLoad } = this.$state;
    if (load() === "play" && !canLoad()) {
      const event = this.createEvent("media-start-loading", { trigger });
      this.dispatchEvent(event);
      __privateGet(this, _providerQueue).enqueue("media-play-request", async () => {
        try {
          await this.play(event);
        } catch (error) {
        }
      });
      return true;
    }
    return false;
  }, _wasPIPActive = new WeakMap(), getFullscreenAdapter_fn = function(target) {
    const provider = peek(__privateGet(this, _$provider));
    return target === "prefer-media" && __privateGet(this, _fullscreen).supported || target === "media" ? __privateGet(this, _fullscreen) : provider?.fullscreen;
  }, throwIfPIPNotSupported_fn = function() {
    if (this.$state.canPictureInPicture()) return;
    throw Error("[vidstack] no pip support");
  }, watchControlsDelayChange_fn = function() {
    this.controls.defaultDelay = this.$props.controlsDelay();
  }, watchAudioGainSupport_fn = function() {
    const { canSetAudioGain } = this.$state, supported = !!__privateGet(this, _$provider).call(this)?.audioGain?.supported;
    canSetAudioGain.set(supported);
  }, watchAirPlaySupport_fn = function() {
    const { canAirPlay } = this.$state, supported = !!__privateGet(this, _$provider).call(this)?.airPlay?.supported;
    canAirPlay.set(supported);
  }, watchGoogleCastSupport_fn = function() {
    const { canGoogleCast, source } = this.$state, supported = IS_CHROME && !IS_IOS && canGoogleCastSrc(source());
    canGoogleCast.set(supported);
  }, watchFullscreenSupport_fn = function() {
    const { canFullscreen: canFullscreen2 } = this.$state, supported = __privateGet(this, _fullscreen).supported || !!__privateGet(this, _$provider).call(this)?.fullscreen?.supported;
    canFullscreen2.set(supported);
  }, watchPiPSupport_fn = function() {
    const { canPictureInPicture } = this.$state, supported = !!__privateGet(this, _$provider).call(this)?.pictureInPicture?.supported;
    canPictureInPicture.set(supported);
  }, _googleCastLoader = new WeakMap(), onFullscreenChange_fn = async function(event) {
    const lockType = peek(this.$props.fullscreenOrientation), isFullscreen2 = event.detail;
    if (isUndefined(lockType) || lockType === "none" || !__privateGet(this, _orientation).supported) return;
    if (isFullscreen2) {
      if (__privateGet(this, _orientation).locked) return;
      this.dispatch("media-orientation-lock-request", {
        detail: lockType,
        trigger: event
      });
    } else if (__privateGet(this, _orientation).locked) this.dispatch("media-orientation-unlock-request", { trigger: event });
  }, onFullscreenError_fn = function(error, request) {
    __privateGet(this, _stateMgr).handle(this.createEvent("fullscreen-error", { detail: coerceToError(error) }));
  }, onPictureInPictureError_fn = function(error, request) {
    __privateGet(this, _stateMgr).handle(this.createEvent("picture-in-picture-error", { detail: coerceToError(error) }));
  }, logError_fn = function(title, error, request) {
  }, _a51);
  function throwIfNotReadyForPlayback(provider, canPlay) {
    if (provider && canPlay) return;
    throw Error("[vidstack] media not ready");
  }
  function throwIfFullscreenNotSupported(target, fullscreen) {
    if (fullscreen?.supported) return;
    throw Error("[vidstack] no fullscreen support");
  }
  function throwIfAutoplayingWithReducedMotion(autoplaying) {
    if (!prefersReducedMotion() || !autoplaying) return;
    throw Error("[vidstack] autoplay blocked");
  }
  var MediaRequestContext = class {
    constructor() {
      __publicField(this, "seeking", false);
      __publicField(this, "looping", false);
      __publicField(this, "replaying", false);
      __publicField(this, "queue", new Queue());
    }
  };
  var TRACKED_EVENT = /* @__PURE__ */ new Set([
    "auto-play",
    "auto-play-fail",
    "can-load",
    "sources-change",
    "source-change",
    "load-start",
    "abort",
    "error",
    "loaded-metadata",
    "loaded-data",
    "can-play",
    "play",
    "play-fail",
    "pause",
    "playing",
    "seeking",
    "seeked",
    "waiting"
  ]);
  var _request2, _media9, _trackedEvents, _clipEnded, _playedIntervals, _playedInterval, _firingWaiting, _waitingTrigger, _isPlayingOnDisconnect, _MediaStateManager_instances, resumePlaybackOnConnect_fn, pausePlaybackOnDisconnect_fn, resetTracking_fn, satisfyRequest_fn, addTextTrackListeners_fn, addQualityListeners_fn, addAudioTrackListeners_fn, onTextTracksChange_fn, onTextTrackModeChange_fn, onAudioTracksChange_fn, onAudioTrackChange_fn, onQualitiesChange_fn2, onQualityChange_fn3, onAutoQualityChange_fn, _stopQualityResizeEffect, watchQualityResize_fn, stopWatchingQualityResize_fn, onCanSetQualityChange_fn, watchCanSetVolume_fn, onSourceQualitiesChange_fn, resetMediaState_fn, onCanPlayDetail_fn, resetPlaybackIfNeeded_fn, updatePlayed_fn, onEndPrecisionChange_fn, saveTime_fn, _fireWaiting, onEnded_fn3, stopWaiting_fn, _a52;
  var MediaStateManager = (_a52 = class extends MediaPlayerController {
    constructor(request, media) {
      super();
      __privateAdd(this, _MediaStateManager_instances);
      __privateAdd(this, _request2);
      __privateAdd(this, _media9);
      __privateAdd(this, _trackedEvents, /* @__PURE__ */ new Map());
      __privateAdd(this, _clipEnded, false);
      __privateAdd(this, _playedIntervals, []);
      __privateAdd(this, _playedInterval, [-1, -1]);
      __privateAdd(this, _firingWaiting, false);
      __privateAdd(this, _waitingTrigger);
      __privateAdd(this, _isPlayingOnDisconnect, false);
      __privateAdd(this, _stopQualityResizeEffect, null);
      __publicField(this, "seeking", functionThrottle((event) => {
        const { seeking, realCurrentTime, paused } = this.$state;
        seeking.set(true);
        realCurrentTime.set(event.detail);
        __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-seeking-request", event);
        if (paused()) {
          __privateSet(this, _waitingTrigger, event);
          __privateGet(this, _fireWaiting).call(this);
        }
        __privateSet(this, _playedInterval, [-1, -1]);
      }, 150, { leading: true }));
      __privateAdd(this, _fireWaiting, functionDebounce(() => {
        if (!__privateGet(this, _waitingTrigger)) return;
        __privateSet(this, _firingWaiting, true);
        const { waiting, playing } = this.$state;
        waiting.set(true);
        playing.set(false);
        const event = this.createEvent("waiting", { trigger: __privateGet(this, _waitingTrigger) });
        __privateGet(this, _trackedEvents).set("waiting", event);
        this.dispatch(event);
        __privateSet(this, _waitingTrigger, void 0);
        __privateSet(this, _firingWaiting, false);
      }, 300));
      __privateSet(this, _request2, request);
      __privateSet(this, _media9, media);
    }
    onAttach(el) {
      el.setAttribute("aria-busy", "true");
      new EventsController(this).add("fullscreen-change", this["fullscreen-change"].bind(this)).add("fullscreen-error", this["fullscreen-error"].bind(this)).add("orientation-change", this["orientation-change"].bind(this));
    }
    onConnect(el) {
      effect(__privateMethod(this, _MediaStateManager_instances, watchCanSetVolume_fn).bind(this));
      __privateMethod(this, _MediaStateManager_instances, addTextTrackListeners_fn).call(this);
      __privateMethod(this, _MediaStateManager_instances, addQualityListeners_fn).call(this);
      __privateMethod(this, _MediaStateManager_instances, addAudioTrackListeners_fn).call(this);
      __privateMethod(this, _MediaStateManager_instances, resumePlaybackOnConnect_fn).call(this);
      onDispose(__privateMethod(this, _MediaStateManager_instances, pausePlaybackOnDisconnect_fn).bind(this));
    }
    onDestroy() {
      const { audioTracks, qualities, textTracks } = __privateGet(this, _media9);
      audioTracks[ListSymbol.reset]();
      qualities[ListSymbol.reset]();
      textTracks[ListSymbol.reset]();
      __privateMethod(this, _MediaStateManager_instances, stopWatchingQualityResize_fn).call(this);
    }
    handle(event) {
      if (!this.scope) return;
      const type = event.type;
      untrack(() => this[event.type]?.(event));
      if (!IS_SERVER2) {
        if (TRACKED_EVENT.has(type)) __privateGet(this, _trackedEvents).set(type, event);
        this.dispatch(event);
      }
    }
    ["provider-change"](event) {
      const prevProvider = __privateGet(this, _media9).$provider(), newProvider = event.detail;
      if (prevProvider?.type === newProvider?.type) return;
      prevProvider?.destroy?.();
      prevProvider?.scope?.dispose();
      __privateGet(this, _media9).$provider.set(event.detail);
      if (prevProvider && event.detail === null) __privateMethod(this, _MediaStateManager_instances, resetMediaState_fn).call(this, event);
    }
    ["provider-loader-change"](event) {
    }
    ["auto-play"](event) {
      this.$state.autoPlayError.set(null);
    }
    ["auto-play-fail"](event) {
      this.$state.autoPlayError.set(event.detail);
      __privateMethod(this, _MediaStateManager_instances, resetTracking_fn).call(this);
    }
    ["can-load"](event) {
      this.$state.canLoad.set(true);
      __privateGet(this, _trackedEvents).set("can-load", event);
      __privateGet(this, _media9).textTracks[TextTrackSymbol.canLoad]();
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-start-loading", event);
    }
    ["can-load-poster"](event) {
      this.$state.canLoadPoster.set(true);
      __privateGet(this, _trackedEvents).set("can-load-poster", event);
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-poster-start-loading", event);
    }
    ["media-type-change"](event) {
      const sourceChangeEvent = __privateGet(this, _trackedEvents).get("source-change");
      if (sourceChangeEvent) event.triggers.add(sourceChangeEvent);
      const viewType = this.$state.viewType();
      this.$state.mediaType.set(event.detail);
      const providedViewType = this.$state.providedViewType(), currentViewType = providedViewType === "unknown" ? event.detail : providedViewType;
      if (viewType !== currentViewType) if (IS_SERVER2) this.$state.inferredViewType.set(currentViewType);
      else setTimeout(() => {
        requestAnimationFrame(() => {
          if (!this.scope) return;
          this.$state.inferredViewType.set(event.detail);
          this.dispatch("view-type-change", {
            detail: currentViewType,
            trigger: event
          });
        });
      }, 0);
    }
    ["stream-type-change"](event) {
      const sourceChangeEvent = __privateGet(this, _trackedEvents).get("source-change");
      if (sourceChangeEvent) event.triggers.add(sourceChangeEvent);
      const { streamType, inferredStreamType } = this.$state;
      inferredStreamType.set(event.detail);
      event.detail = streamType();
    }
    ["rate-change"](event) {
      const { storage } = __privateGet(this, _media9), { canPlay } = this.$state;
      this.$state.playbackRate.set(event.detail);
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-rate-change-request", event);
      if (canPlay()) storage?.setPlaybackRate?.(event.detail);
    }
    ["remote-playback-change"](event) {
      const { remotePlaybackState, remotePlaybackType } = this.$state, { type, state } = event.detail, isConnected = state === "connected";
      remotePlaybackType.set(type);
      remotePlaybackState.set(state);
      const key2 = type === "airplay" ? "media-airplay-request" : "media-google-cast-request";
      if (isConnected) __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, key2, event);
      else {
        const requestEvent = __privateGet(this, _request2).queue.peek(key2);
        if (requestEvent) {
          event.request = requestEvent;
          event.triggers.add(requestEvent);
        }
      }
    }
    ["sources-change"](event) {
      const prevSources = this.$state.sources(), newSources = event.detail;
      this.$state.sources.set(newSources);
      __privateMethod(this, _MediaStateManager_instances, onSourceQualitiesChange_fn).call(this, prevSources, newSources, event);
    }
    ["source-change"](event) {
      event.isQualityChange = event.originEvent?.type === "quality-change";
      const source = event.detail;
      __privateMethod(this, _MediaStateManager_instances, resetMediaState_fn).call(this, event, event.isQualityChange);
      __privateGet(this, _trackedEvents).set(event.type, event);
      this.$state.source.set(source);
      this.el?.setAttribute("aria-busy", "true");
    }
    ["abort"](event) {
      const sourceChangeEvent = __privateGet(this, _trackedEvents).get("source-change");
      if (sourceChangeEvent) event.triggers.add(sourceChangeEvent);
      const canLoadEvent = __privateGet(this, _trackedEvents).get("can-load");
      if (canLoadEvent && !event.triggers.hasType("can-load")) event.triggers.add(canLoadEvent);
    }
    ["load-start"](event) {
      const sourceChangeEvent = __privateGet(this, _trackedEvents).get("source-change");
      if (sourceChangeEvent) event.triggers.add(sourceChangeEvent);
    }
    ["error"](event) {
      this.$state.error.set(event.detail);
      const abortEvent = __privateGet(this, _trackedEvents).get("abort");
      if (abortEvent) event.triggers.add(abortEvent);
    }
    ["loaded-metadata"](event) {
      const loadStartEvent = __privateGet(this, _trackedEvents).get("load-start");
      if (loadStartEvent) event.triggers.add(loadStartEvent);
    }
    ["loaded-data"](event) {
      const loadStartEvent = __privateGet(this, _trackedEvents).get("load-start");
      if (loadStartEvent) event.triggers.add(loadStartEvent);
    }
    ["can-play"](event) {
      const loadedMetadata = __privateGet(this, _trackedEvents).get("loaded-metadata");
      if (loadedMetadata) event.triggers.add(loadedMetadata);
      __privateMethod(this, _MediaStateManager_instances, onCanPlayDetail_fn).call(this, event.detail);
      this.el?.setAttribute("aria-busy", "false");
    }
    ["can-play-through"](event) {
      __privateMethod(this, _MediaStateManager_instances, onCanPlayDetail_fn).call(this, event.detail);
      const canPlay = __privateGet(this, _trackedEvents).get("can-play");
      if (canPlay) event.triggers.add(canPlay);
    }
    ["duration-change"](event) {
      const { live, intrinsicDuration, providedDuration, clipEndTime, ended } = this.$state, time = event.detail;
      if (!live()) {
        const duration = !Number.isNaN(time) ? time : 0;
        intrinsicDuration.set(duration);
        if (ended()) __privateMethod(this, _MediaStateManager_instances, onEndPrecisionChange_fn).call(this, event);
      }
      if (providedDuration() > 0 || clipEndTime() > 0) event.stopImmediatePropagation();
    }
    ["progress"](event) {
      const { buffered, seekable } = this.$state, { buffered: newBuffered, seekable: newSeekable } = event.detail, newBufferedEnd = getTimeRangesEnd(newBuffered), hasBufferedLengthChanged = newBuffered.length !== buffered().length, hasBufferedEndChanged = newBufferedEnd !== getTimeRangesEnd(buffered()), newSeekableEnd = getTimeRangesEnd(newSeekable), hasSeekableLengthChanged = newSeekable.length !== seekable().length, hasSeekableEndChanged = newSeekableEnd !== getTimeRangesEnd(seekable());
      if (hasBufferedLengthChanged || hasBufferedEndChanged) buffered.set(newBuffered);
      if (hasSeekableLengthChanged || hasSeekableEndChanged) seekable.set(newSeekable);
    }
    ["play"](event) {
      const { paused, autoPlayError, ended, autoPlaying, playsInline, pointer, muted, viewType, live, userBehindLiveEdge } = this.$state;
      __privateMethod(this, _MediaStateManager_instances, resetPlaybackIfNeeded_fn).call(this);
      if (!paused()) {
        event.stopImmediatePropagation();
        return;
      }
      event.autoPlay = autoPlaying();
      const waitingEvent = __privateGet(this, _trackedEvents).get("waiting");
      if (waitingEvent) event.triggers.add(waitingEvent);
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-play-request", event);
      __privateGet(this, _trackedEvents).set("play", event);
      paused.set(false);
      autoPlayError.set(null);
      if (event.autoPlay) {
        this.handle(this.createEvent("auto-play", {
          detail: { muted: muted() },
          trigger: event
        }));
        autoPlaying.set(false);
      }
      if (ended() || __privateGet(this, _request2).replaying) {
        __privateGet(this, _request2).replaying = false;
        ended.set(false);
        this.handle(this.createEvent("replay", { trigger: event }));
      }
      if (!playsInline() && viewType() === "video" && pointer() === "coarse") __privateGet(this, _media9).remote.enterFullscreen("prefer-media", event);
      if (live() && !userBehindLiveEdge()) __privateGet(this, _media9).remote.seekToLiveEdge(event);
    }
    ["play-fail"](event) {
      const { muted, autoPlaying } = this.$state;
      const playEvent = __privateGet(this, _trackedEvents).get("play");
      if (playEvent) event.triggers.add(playEvent);
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-play-request", event);
      const { paused, playing } = this.$state;
      paused.set(true);
      playing.set(false);
      __privateMethod(this, _MediaStateManager_instances, resetTracking_fn).call(this);
      __privateGet(this, _trackedEvents).set("play-fail", event);
      if (event.autoPlay) {
        this.handle(this.createEvent("auto-play-fail", {
          detail: {
            muted: muted(),
            error: event.detail
          },
          trigger: event
        }));
        autoPlaying.set(false);
      }
    }
    ["playing"](event) {
      const playEvent = __privateGet(this, _trackedEvents).get("play"), seekedEvent = __privateGet(this, _trackedEvents).get("seeked");
      if (playEvent) event.triggers.add(playEvent);
      else if (seekedEvent) event.triggers.add(seekedEvent);
      setTimeout(() => __privateMethod(this, _MediaStateManager_instances, resetTracking_fn).call(this), 0);
      const { paused, playing, live, liveSyncPosition, seekableEnd, started, currentTime, seeking, ended } = this.$state;
      paused.set(false);
      playing.set(true);
      seeking.set(false);
      ended.set(false);
      if (__privateGet(this, _request2).looping) {
        __privateGet(this, _request2).looping = false;
        return;
      }
      if (live() && !started() && currentTime() === 0) {
        const end = liveSyncPosition() ?? seekableEnd() - 2;
        if (Number.isFinite(end)) __privateGet(this, _media9).$provider().setCurrentTime(end);
      }
      this["started"](event);
    }
    ["started"](event) {
      const { started } = this.$state;
      if (!started()) {
        started.set(true);
        this.handle(this.createEvent("started", { trigger: event }));
      }
    }
    ["pause"](event) {
      if (!this.el?.isConnected) __privateSet(this, _isPlayingOnDisconnect, true);
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-pause-request", event);
      const seekedEvent = __privateGet(this, _trackedEvents).get("seeked");
      if (seekedEvent) event.triggers.add(seekedEvent);
      const { paused, playing } = this.$state;
      paused.set(true);
      playing.set(false);
      if (__privateGet(this, _clipEnded)) setTimeout(() => {
        this.handle(this.createEvent("end", { trigger: event }));
        __privateSet(this, _clipEnded, false);
      }, 0);
      __privateMethod(this, _MediaStateManager_instances, resetTracking_fn).call(this);
    }
    ["time-change"](event) {
      if (__privateGet(this, _request2).looping) {
        event.stopImmediatePropagation();
        return;
      }
      let { waiting, played, clipEndTime, realCurrentTime, currentTime } = this.$state, newTime = event.detail, endTime = clipEndTime();
      realCurrentTime.set(newTime);
      __privateMethod(this, _MediaStateManager_instances, updatePlayed_fn).call(this);
      waiting.set(false);
      for (const track of __privateGet(this, _media9).textTracks) track[TextTrackSymbol.updateActiveCues](newTime, event);
      if (endTime > 0 && newTime >= endTime) {
        __privateSet(this, _clipEnded, true);
        this.dispatch("media-pause-request", { trigger: event });
      }
      __privateMethod(this, _MediaStateManager_instances, saveTime_fn).call(this);
      this.dispatch("time-update", {
        detail: {
          currentTime: currentTime(),
          played: played()
        },
        trigger: event
      });
    }
    ["audio-gain-change"](event) {
      const { storage } = __privateGet(this, _media9), { canPlay, audioGain } = this.$state;
      audioGain.set(event.detail);
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-audio-gain-change-request", event);
      if (canPlay()) storage?.setAudioGain?.(audioGain());
    }
    ["volume-change"](event) {
      const { storage } = __privateGet(this, _media9), { volume, muted, canPlay } = this.$state, detail = event.detail;
      volume.set(detail.volume);
      muted.set(detail.muted || detail.volume === 0);
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-volume-change-request", event);
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, detail.muted ? "media-mute-request" : "media-unmute-request", event);
      if (canPlay()) {
        storage?.setVolume?.(volume());
        storage?.setMuted?.(muted());
      }
    }
    ["seeked"](event) {
      const { seeking, currentTime, realCurrentTime, paused, seekableEnd, ended, live } = this.$state;
      if (__privateGet(this, _request2).seeking) {
        seeking.set(true);
        event.stopImmediatePropagation();
      } else if (seeking()) {
        const waitingEvent = __privateGet(this, _trackedEvents).get("waiting");
        if (waitingEvent) event.triggers.add(waitingEvent);
        const seekingEvent = __privateGet(this, _trackedEvents).get("seeking");
        if (seekingEvent && !event.triggers.has(seekingEvent)) event.triggers.add(seekingEvent);
        if (paused()) __privateMethod(this, _MediaStateManager_instances, stopWaiting_fn).call(this);
        seeking.set(false);
        realCurrentTime.set(event.detail);
        __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-seek-request", event);
        const origin = event?.originEvent;
        if (origin?.isTrusted && !(origin instanceof MessageEvent) && !/seek/.test(origin.type)) this["started"](event);
      }
      if (!live()) if (Math.floor(currentTime()) !== Math.floor(seekableEnd())) ended.set(false);
      else this.end(event);
    }
    ["waiting"](event) {
      if (__privateGet(this, _firingWaiting) || __privateGet(this, _request2).seeking) return;
      event.stopImmediatePropagation();
      __privateSet(this, _waitingTrigger, event);
      __privateGet(this, _fireWaiting).call(this);
    }
    ["end"](event) {
      const { loop, ended } = this.$state;
      if (!loop() && ended()) return;
      if (loop()) {
        setTimeout(() => {
          requestAnimationFrame(() => {
            __privateMethod(this, _MediaStateManager_instances, resetPlaybackIfNeeded_fn).call(this, event);
            this.dispatch("media-loop-request", { trigger: event });
          });
        }, 10);
        return;
      }
      setTimeout(() => __privateMethod(this, _MediaStateManager_instances, onEnded_fn3).call(this, event), 0);
    }
    ["fullscreen-change"](event) {
      const isFullscreen2 = event.detail;
      this.$state.fullscreen.set(isFullscreen2);
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, isFullscreen2 ? "media-enter-fullscreen-request" : "media-exit-fullscreen-request", event);
    }
    ["fullscreen-error"](event) {
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-enter-fullscreen-request", event);
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-exit-fullscreen-request", event);
    }
    ["orientation-change"](event) {
      const isLocked = event.detail.lock;
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, isLocked ? "media-orientation-lock-request" : "media-orientation-unlock-request", event);
    }
    ["picture-in-picture-change"](event) {
      const isPiP = event.detail;
      this.$state.pictureInPicture.set(isPiP);
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, isPiP ? "media-enter-pip-request" : "media-exit-pip-request", event);
    }
    ["picture-in-picture-error"](event) {
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-enter-pip-request", event);
      __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-exit-pip-request", event);
    }
    ["title-change"](event) {
      if (!event.trigger) return;
      event.stopImmediatePropagation();
      this.$state.inferredTitle.set(event.detail);
    }
    ["poster-change"](event) {
      if (!event.trigger) return;
      event.stopImmediatePropagation();
      this.$state.inferredPoster.set(event.detail);
    }
  }, _request2 = new WeakMap(), _media9 = new WeakMap(), _trackedEvents = new WeakMap(), _clipEnded = new WeakMap(), _playedIntervals = new WeakMap(), _playedInterval = new WeakMap(), _firingWaiting = new WeakMap(), _waitingTrigger = new WeakMap(), _isPlayingOnDisconnect = new WeakMap(), _MediaStateManager_instances = new WeakSet(), resumePlaybackOnConnect_fn = function() {
    if (!__privateGet(this, _isPlayingOnDisconnect)) return;
    requestAnimationFrame(() => {
      if (!this.scope) return;
      __privateGet(this, _media9).remote.play(new DOMEvent("dom-connect"));
    });
    __privateSet(this, _isPlayingOnDisconnect, false);
  }, pausePlaybackOnDisconnect_fn = function() {
    if (__privateGet(this, _isPlayingOnDisconnect)) return;
    __privateSet(this, _isPlayingOnDisconnect, !this.$state.paused());
    __privateGet(this, _media9).$provider()?.pause();
  }, resetTracking_fn = function() {
    __privateMethod(this, _MediaStateManager_instances, stopWaiting_fn).call(this);
    __privateSet(this, _clipEnded, false);
    __privateGet(this, _request2).replaying = false;
    __privateGet(this, _request2).looping = false;
    __privateSet(this, _firingWaiting, false);
    __privateSet(this, _waitingTrigger, void 0);
    __privateGet(this, _trackedEvents).clear();
  }, satisfyRequest_fn = function(request, event) {
    const requestEvent = __privateGet(this, _request2).queue.serve(request);
    if (!requestEvent) return;
    event.request = requestEvent;
    event.triggers.add(requestEvent);
  }, addTextTrackListeners_fn = function() {
    __privateMethod(this, _MediaStateManager_instances, onTextTracksChange_fn).call(this);
    __privateMethod(this, _MediaStateManager_instances, onTextTrackModeChange_fn).call(this);
    const textTracks = __privateGet(this, _media9).textTracks;
    new EventsController(textTracks).add("add", __privateMethod(this, _MediaStateManager_instances, onTextTracksChange_fn).bind(this)).add("remove", __privateMethod(this, _MediaStateManager_instances, onTextTracksChange_fn).bind(this)).add("mode-change", __privateMethod(this, _MediaStateManager_instances, onTextTrackModeChange_fn).bind(this));
  }, addQualityListeners_fn = function() {
    const qualities = __privateGet(this, _media9).qualities;
    new EventsController(qualities).add("add", __privateMethod(this, _MediaStateManager_instances, onQualitiesChange_fn2).bind(this)).add("remove", __privateMethod(this, _MediaStateManager_instances, onQualitiesChange_fn2).bind(this)).add("change", __privateMethod(this, _MediaStateManager_instances, onQualityChange_fn3).bind(this)).add("auto-change", __privateMethod(this, _MediaStateManager_instances, onAutoQualityChange_fn).bind(this)).add("readonly-change", __privateMethod(this, _MediaStateManager_instances, onCanSetQualityChange_fn).bind(this));
  }, addAudioTrackListeners_fn = function() {
    const audioTracks = __privateGet(this, _media9).audioTracks;
    new EventsController(audioTracks).add("add", __privateMethod(this, _MediaStateManager_instances, onAudioTracksChange_fn).bind(this)).add("remove", __privateMethod(this, _MediaStateManager_instances, onAudioTracksChange_fn).bind(this)).add("change", __privateMethod(this, _MediaStateManager_instances, onAudioTrackChange_fn).bind(this));
  }, onTextTracksChange_fn = function(event) {
    const { textTracks } = this.$state;
    textTracks.set(__privateGet(this, _media9).textTracks.toArray());
    this.dispatch("text-tracks-change", {
      detail: textTracks(),
      trigger: event
    });
  }, onTextTrackModeChange_fn = function(event) {
    if (event) __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-text-track-change-request", event);
    const current = __privateGet(this, _media9).textTracks.selected, { textTrack } = this.$state;
    if (textTrack() !== current) {
      textTrack.set(current);
      this.dispatch("text-track-change", {
        detail: current,
        trigger: event
      });
    }
  }, onAudioTracksChange_fn = function(event) {
    const { audioTracks } = this.$state;
    audioTracks.set(__privateGet(this, _media9).audioTracks.toArray());
    this.dispatch("audio-tracks-change", {
      detail: audioTracks(),
      trigger: event
    });
  }, onAudioTrackChange_fn = function(event) {
    const { audioTrack } = this.$state;
    audioTrack.set(__privateGet(this, _media9).audioTracks.selected);
    if (event) __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-audio-track-change-request", event);
    this.dispatch("audio-track-change", {
      detail: audioTrack(),
      trigger: event
    });
  }, onQualitiesChange_fn2 = function(event) {
    const { qualities } = this.$state;
    qualities.set(__privateGet(this, _media9).qualities.toArray());
    this.dispatch("qualities-change", {
      detail: qualities(),
      trigger: event
    });
  }, onQualityChange_fn3 = function(event) {
    const { quality } = this.$state;
    quality.set(__privateGet(this, _media9).qualities.selected);
    if (event) __privateMethod(this, _MediaStateManager_instances, satisfyRequest_fn).call(this, "media-quality-change-request", event);
    this.dispatch("quality-change", {
      detail: quality(),
      trigger: event
    });
  }, onAutoQualityChange_fn = function() {
    const { qualities } = __privateGet(this, _media9), isAuto = qualities.auto;
    this.$state.autoQuality.set(isAuto);
    if (!isAuto) __privateMethod(this, _MediaStateManager_instances, stopWatchingQualityResize_fn).call(this);
  }, _stopQualityResizeEffect = new WeakMap(), watchQualityResize_fn = function() {
    __privateMethod(this, _MediaStateManager_instances, stopWatchingQualityResize_fn).call(this);
    __privateSet(this, _stopQualityResizeEffect, effect(() => {
      const { qualities } = __privateGet(this, _media9), { mediaWidth, mediaHeight } = this.$state, w = mediaWidth(), h = mediaHeight();
      if (w === 0 || h === 0) return;
      let selectedQuality = null, minScore = Infinity;
      for (const quality of qualities) {
        const score = Math.abs(quality.width - w) + Math.abs(quality.height - h);
        if (score < minScore) {
          minScore = score;
          selectedQuality = quality;
        }
      }
      if (selectedQuality) qualities[ListSymbol.select](selectedQuality, true, new DOMEvent("resize", { detail: {
        width: w,
        height: h
      } }));
    }));
  }, stopWatchingQualityResize_fn = function() {
    var _a106;
    (_a106 = __privateGet(this, _stopQualityResizeEffect)) == null ? void 0 : _a106.call(this);
    __privateSet(this, _stopQualityResizeEffect, null);
  }, onCanSetQualityChange_fn = function() {
    this.$state.canSetQuality.set(!__privateGet(this, _media9).qualities.readonly);
  }, watchCanSetVolume_fn = function() {
    const { canSetVolume, isGoogleCastConnected } = this.$state;
    if (isGoogleCastConnected()) {
      canSetVolume.set(false);
      return;
    }
    canChangeVolume().then(canSetVolume.set);
  }, onSourceQualitiesChange_fn = function(prevSources, newSources, trigger) {
    let { qualities } = __privateGet(this, _media9), added = false, removed = false;
    for (const prevSrc of prevSources) {
      if (!isVideoQualitySrc(prevSrc)) continue;
      if (!newSources.some((s) => s.src === prevSrc.src)) {
        const quality = qualities.getBySrc(prevSrc.src);
        if (quality) {
          qualities[ListSymbol.remove](quality, trigger);
          removed = true;
        }
      }
    }
    if (removed && !qualities.length) {
      this.$state.savedState.set(null);
      qualities[ListSymbol.reset](trigger);
    }
    for (const src of newSources) {
      if (!isVideoQualitySrc(src) || qualities.getBySrc(src.src)) continue;
      const quality = {
        id: src.id ?? src.height + "p",
        bitrate: null,
        codec: null,
        ...src,
        selected: false
      };
      qualities[ListSymbol.add](quality, trigger);
      added = true;
    }
    if (added && !qualities[QualitySymbol.enableAuto]) {
      __privateMethod(this, _MediaStateManager_instances, watchQualityResize_fn).call(this);
      qualities[QualitySymbol.enableAuto] = __privateMethod(this, _MediaStateManager_instances, watchQualityResize_fn).bind(this);
      qualities[QualitySymbol.setAuto](true, trigger);
    }
  }, resetMediaState_fn = function(event, isSourceQualityChange = false) {
    const { audioTracks, qualities } = __privateGet(this, _media9);
    if (!isSourceQualityChange) {
      __privateSet(this, _playedIntervals, []);
      __privateSet(this, _playedInterval, [-1, -1]);
      audioTracks[ListSymbol.reset](event);
      qualities[ListSymbol.reset](event);
      softResetMediaState(this.$state, isSourceQualityChange);
      __privateMethod(this, _MediaStateManager_instances, resetTracking_fn).call(this);
      return;
    }
    softResetMediaState(this.$state, isSourceQualityChange);
    __privateMethod(this, _MediaStateManager_instances, resetTracking_fn).call(this);
  }, onCanPlayDetail_fn = function(detail) {
    const { seekable, buffered, intrinsicDuration, canPlay } = this.$state;
    canPlay.set(true);
    buffered.set(detail.buffered);
    seekable.set(detail.seekable);
    const seekableEnd = getTimeRangesEnd(detail.seekable) ?? Infinity;
    intrinsicDuration.set(seekableEnd);
  }, resetPlaybackIfNeeded_fn = function(trigger) {
    if (!peek(__privateGet(this, _media9).$provider)) return;
    const { ended, seekableStart, clipEndTime, currentTime, realCurrentTime, duration } = this.$state;
    const shouldReset = ended() || realCurrentTime() < seekableStart() || clipEndTime() > 0 && realCurrentTime() >= clipEndTime() || Math.abs(currentTime() - duration()) < 0.1;
    if (shouldReset) this.dispatch("media-seek-request", {
      detail: seekableStart(),
      trigger
    });
    return shouldReset;
  }, updatePlayed_fn = function() {
    const { currentTime, played, paused } = this.$state;
    if (paused()) return;
    __privateSet(this, _playedInterval, updateTimeIntervals(__privateGet(this, _playedIntervals), __privateGet(this, _playedInterval), currentTime()));
    played.set(new TimeRange(__privateGet(this, _playedIntervals)));
  }, onEndPrecisionChange_fn = function(trigger) {
    const { clipStartTime, clipEndTime, duration } = this.$state;
    if (clipStartTime() > 0 || clipEndTime() > 0) return;
    this.handle(this.createEvent("time-change", {
      detail: duration(),
      trigger
    }));
  }, saveTime_fn = function() {
    const { storage } = __privateGet(this, _media9), { canPlay, realCurrentTime } = this.$state;
    if (canPlay()) storage?.setTime?.(realCurrentTime());
  }, _fireWaiting = new WeakMap(), onEnded_fn3 = function(event) {
    const { storage } = __privateGet(this, _media9), { paused, seeking, ended, duration } = this.$state;
    __privateMethod(this, _MediaStateManager_instances, onEndPrecisionChange_fn).call(this, event);
    if (!paused()) this.dispatch("pause", { trigger: event });
    if (seeking()) this.dispatch("seeked", {
      detail: duration(),
      trigger: event
    });
    ended.set(true);
    __privateMethod(this, _MediaStateManager_instances, resetTracking_fn).call(this);
    storage?.setTime?.(duration(), true);
    this.dispatch("ended", { trigger: event });
  }, stopWaiting_fn = function() {
    __privateGet(this, _fireWaiting).cancel();
    this.$state.waiting.set(false);
  }, _a52);
  var _MediaStateSync_instances, init_fn2, watchProvidedTypes_fn, watchLogLevel_fn, watchMetadata_fn, watchTitle_fn, watchAutoplay_fn, watchLoop_fn, watchControls_fn2, watchPoster_fn, watchCrossOrigin_fn, watchDuration_fn, watchPlaysInline_fn, watchClipStartTime_fn, watchClipEndTime_fn, watchLive_fn, watchLiveTolerance_fn, watchLiveEdge_fn, _a53;
  var MediaStateSync = (_a53 = class extends MediaPlayerController {
    constructor() {
      super(...arguments);
      __privateAdd(this, _MediaStateSync_instances);
    }
    onSetup() {
      __privateMethod(this, _MediaStateSync_instances, init_fn2).call(this);
      if (IS_SERVER2) return;
      const effects2 = [
        __privateMethod(this, _MediaStateSync_instances, watchMetadata_fn),
        __privateMethod(this, _MediaStateSync_instances, watchAutoplay_fn),
        __privateMethod(this, _MediaStateSync_instances, watchClipStartTime_fn),
        __privateMethod(this, _MediaStateSync_instances, watchClipEndTime_fn),
        __privateMethod(this, _MediaStateSync_instances, watchControls_fn2),
        __privateMethod(this, _MediaStateSync_instances, watchCrossOrigin_fn),
        __privateMethod(this, _MediaStateSync_instances, watchDuration_fn),
        __privateMethod(this, _MediaStateSync_instances, watchLive_fn),
        __privateMethod(this, _MediaStateSync_instances, watchLiveEdge_fn),
        __privateMethod(this, _MediaStateSync_instances, watchLiveTolerance_fn),
        __privateMethod(this, _MediaStateSync_instances, watchLoop_fn),
        __privateMethod(this, _MediaStateSync_instances, watchPlaysInline_fn),
        __privateMethod(this, _MediaStateSync_instances, watchPoster_fn),
        __privateMethod(this, _MediaStateSync_instances, watchProvidedTypes_fn),
        __privateMethod(this, _MediaStateSync_instances, watchTitle_fn)
      ];
      for (const callback of effects2) effect(callback.bind(this));
    }
  }, _MediaStateSync_instances = new WeakSet(), init_fn2 = function() {
    const providedProps = {
      duration: "providedDuration",
      loop: "providedLoop",
      poster: "providedPoster",
      streamType: "providedStreamType",
      title: "providedTitle",
      viewType: "providedViewType"
    };
    const skip = /* @__PURE__ */ new Set([
      "currentTime",
      "paused",
      "playbackRate",
      "volume"
    ]);
    for (const prop2 of Object.keys(this.$props)) {
      if (skip.has(prop2)) continue;
      this.$state[providedProps[prop2] ?? prop2]?.set(this.$props[prop2]());
    }
    this.$state.muted.set(this.$props.muted() || this.$props.volume() === 0);
  }, watchProvidedTypes_fn = function() {
    const { viewType, streamType, title, poster, loop } = this.$props, $state = this.$state;
    $state.providedPoster.set(poster());
    $state.providedStreamType.set(streamType());
    $state.providedViewType.set(viewType());
    $state.providedTitle.set(title());
    $state.providedLoop.set(loop());
  }, watchLogLevel_fn = function() {
  }, watchMetadata_fn = function() {
    const { artist, artwork } = this.$props;
    this.$state.artist.set(artist());
    this.$state.artwork.set(artwork());
  }, watchTitle_fn = function() {
    const { title } = this.$state;
    this.dispatch("title-change", { detail: title() });
  }, watchAutoplay_fn = function() {
    const autoPlay = this.$props.autoPlay() || this.$props.autoplay();
    this.$state.autoPlay.set(autoPlay);
    this.dispatch("auto-play-change", { detail: autoPlay });
  }, watchLoop_fn = function() {
    const loop = this.$state.loop();
    this.dispatch("loop-change", { detail: loop });
  }, watchControls_fn2 = function() {
    const controls = this.$props.controls();
    this.$state.controls.set(controls);
  }, watchPoster_fn = function() {
    const { poster } = this.$state;
    this.dispatch("poster-change", { detail: poster() });
  }, watchCrossOrigin_fn = function() {
    const crossOrigin = this.$props.crossOrigin() ?? this.$props.crossorigin(), value = crossOrigin === true ? "" : crossOrigin;
    this.$state.crossOrigin.set(value);
  }, watchDuration_fn = function() {
    const { duration } = this.$props;
    this.dispatch("media-duration-change-request", { detail: duration() });
  }, watchPlaysInline_fn = function() {
    const inline2 = this.$props.playsInline() || this.$props.playsinline();
    this.$state.playsInline.set(inline2);
    this.dispatch("plays-inline-change", { detail: inline2 });
  }, watchClipStartTime_fn = function() {
    const { clipStartTime } = this.$props;
    this.dispatch("media-clip-start-change-request", { detail: clipStartTime() });
  }, watchClipEndTime_fn = function() {
    const { clipEndTime } = this.$props;
    this.dispatch("media-clip-end-change-request", { detail: clipEndTime() });
  }, watchLive_fn = function() {
    this.dispatch("live-change", { detail: this.$state.live() });
  }, watchLiveTolerance_fn = function() {
    this.$state.liveEdgeTolerance.set(this.$props.liveEdgeTolerance());
    this.$state.minLiveDVRWindow.set(this.$props.minLiveDVRWindow());
  }, watchLiveEdge_fn = function() {
    this.dispatch("live-edge-change", { detail: this.$state.liveEdge() });
  }, _a53);
  var actions = [
    "play",
    "pause",
    "seekforward",
    "seekbackward",
    "seekto"
  ];
  var _NavigatorMediaSession_instances, onDisconnect_fn3, onMetadataChange_fn, onPlaybackStateChange_fn, handleAction_fn, _a54;
  var NavigatorMediaSession = (_a54 = class extends MediaPlayerController {
    constructor() {
      super(...arguments);
      __privateAdd(this, _NavigatorMediaSession_instances);
    }
    onConnect() {
      effect(__privateMethod(this, _NavigatorMediaSession_instances, onMetadataChange_fn).bind(this));
      effect(__privateMethod(this, _NavigatorMediaSession_instances, onPlaybackStateChange_fn).bind(this));
      const handleAction = __privateMethod(this, _NavigatorMediaSession_instances, handleAction_fn).bind(this);
      for (const action of actions) navigator.mediaSession.setActionHandler(action, handleAction);
      onDispose(__privateMethod(this, _NavigatorMediaSession_instances, onDisconnect_fn3).bind(this));
    }
  }, _NavigatorMediaSession_instances = new WeakSet(), onDisconnect_fn3 = function() {
    for (const action of actions) navigator.mediaSession.setActionHandler(action, null);
  }, onMetadataChange_fn = function() {
    const { title, artist, artwork, poster } = this.$state;
    navigator.mediaSession.metadata = new MediaMetadata({
      title: title(),
      artist: artist(),
      artwork: artwork() ?? [{ src: poster() }]
    });
  }, onPlaybackStateChange_fn = function() {
    const { canPlay, paused } = this.$state;
    navigator.mediaSession.playbackState = !canPlay() ? "none" : paused() ? "paused" : "playing";
  }, handleAction_fn = function(details) {
    const trigger = new DOMEvent(`media-session-action`, { detail: details });
    switch (details.action) {
      case "play":
        this.dispatch("media-play-request", { trigger });
        break;
      case "pause":
        this.dispatch("media-pause-request", { trigger });
        break;
      case "seekto":
      case "seekforward":
      case "seekbackward":
        this.dispatch("media-seek-request", {
          detail: isNumber(details.seekTime) ? details.seekTime : this.$state.currentTime() + (details.seekOffset ?? (details.action === "seekforward" ? 10 : -10)),
          trigger
        });
        break;
    }
  }, _a54);
  var $keyboard = signal(false);
  if (!IS_SERVER2) {
    listenEvent(document, "pointerdown", () => {
      $keyboard.set(false);
    });
    listenEvent(document, "keydown", (e2) => {
      if (e2.metaKey || e2.altKey || e2.ctrlKey) return;
      $keyboard.set(true);
    });
  }
  var _focused, _FocusVisibleController_instances, onFocus_fn, onBlur_fn, onPointerEnter_fn, onPointerLeave_fn, _a55;
  var FocusVisibleController = (_a55 = class extends ViewController {
    constructor() {
      super(...arguments);
      __privateAdd(this, _FocusVisibleController_instances);
      __privateAdd(this, _focused, signal(false));
    }
    onConnect(el) {
      effect(() => {
        const events = new EventsController(el);
        if (!$keyboard()) {
          __privateGet(this, _focused).set(false);
          updateFocusAttr(el, false);
          events.add("pointerenter", __privateMethod(this, _FocusVisibleController_instances, onPointerEnter_fn).bind(this)).add("pointerleave", __privateMethod(this, _FocusVisibleController_instances, onPointerLeave_fn).bind(this));
          return;
        }
        const active = document.activeElement === el;
        __privateGet(this, _focused).set(active);
        updateFocusAttr(el, active);
        events.add("focus", __privateMethod(this, _FocusVisibleController_instances, onFocus_fn).bind(this)).add("blur", __privateMethod(this, _FocusVisibleController_instances, onBlur_fn).bind(this));
      });
    }
    focused() {
      return __privateGet(this, _focused).call(this);
    }
  }, _focused = new WeakMap(), _FocusVisibleController_instances = new WeakSet(), onFocus_fn = function() {
    __privateGet(this, _focused).set(true);
    updateFocusAttr(this.el, true);
  }, onBlur_fn = function() {
    __privateGet(this, _focused).set(false);
    updateFocusAttr(this.el, false);
  }, onPointerEnter_fn = function() {
    updateHoverAttr(this.el, true);
  }, onPointerLeave_fn = function() {
    updateHoverAttr(this.el, false);
  }, _a55);
  function updateFocusAttr(el, isFocused) {
    setAttribute(el, "data-focus", isFocused);
    setAttribute(el, "data-hocus", isFocused);
  }
  function updateHoverAttr(el, isHovering) {
    setAttribute(el, "data-hocus", isHovering);
    setAttribute(el, "data-hover", isHovering);
  }
  var _a56, _media10, _stateMgr2, _requestMgr, _MediaPlayer_instances, provider_get, props_get, _skipTitleUpdate, watchTitle_fn2, watchOrientation_fn, watchCanPlay_fn, setupMediaAttributes_fn, onFindPlayer_fn, onResize_fn, onPointerChange_fn, watchPaused_fn2, queuePausedUpdate_fn, watchMuted_fn, queueMutedUpdate_fn, watchCurrentTime_fn, queueCurrentTimeUpdate_fn, watchVolume_fn, queueVolumeUpdate_fn, watchPlaybackRate_fn, queuePlaybackRateUpdate_fn, watchPlaysInline_fn2, queuePlaysInlineUpdate_fn, watchStorage_fn, computeMediaId_fn;
  var MediaPlayer = (_a56 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _MediaPlayer_instances);
      __privateAdd(this, _media10);
      __privateAdd(this, _stateMgr2);
      __privateAdd(this, _requestMgr);
      __publicField(this, "canPlayQueue", new RequestQueue());
      __publicField(this, "remoteControl");
      __privateAdd(this, _skipTitleUpdate, false);
      /**
      * Controls the screen orientation of the current browser window and dispatches orientation
      * change events on the player.
      */
      __publicField(this, "orientation");
      new MediaStateSync();
      const context = {
        player: this,
        qualities: new VideoQualityList(),
        audioTracks: new AudioTrackList(),
        storage: null,
        $provider: signal(null),
        $providerSetup: signal(false),
        $props: this.$props,
        $state: this.$state
      };
      context.remote = this.remoteControl = new MediaRemoteControl(void 0);
      context.remote.setPlayer(this);
      context.textTracks = new TextTrackList();
      context.textTracks[TextTrackSymbol.crossOrigin] = this.$state.crossOrigin;
      context.textRenderers = new TextRenderers(context);
      context.ariaKeys = {};
      __privateSet(this, _media10, context);
      provideContext(mediaContext, context);
      this.orientation = new ScreenOrientationController();
      new FocusVisibleController();
      new MediaKeyboardController(context);
      const request = new MediaRequestContext();
      __privateSet(this, _stateMgr2, new MediaStateManager(request, context));
      __privateSet(this, _requestMgr, new MediaRequestManager(__privateGet(this, _stateMgr2), request, context));
      context.delegate = new MediaPlayerDelegate(__privateGet(this, _stateMgr2).handle.bind(__privateGet(this, _stateMgr2)), context);
      context.notify = context.delegate.notify.bind(context.delegate);
      if (typeof navigator !== "undefined" && "mediaSession" in navigator) new NavigatorMediaSession();
      new MediaLoadController("load", this.startLoading.bind(this));
      new MediaLoadController("posterLoad", this.startLoadingPoster.bind(this));
    }
    onSetup() {
      __privateMethod(this, _MediaPlayer_instances, setupMediaAttributes_fn).call(this);
      effect(__privateMethod(this, _MediaPlayer_instances, watchCanPlay_fn).bind(this));
      effect(__privateMethod(this, _MediaPlayer_instances, watchMuted_fn).bind(this));
      effect(__privateMethod(this, _MediaPlayer_instances, watchPaused_fn2).bind(this));
      effect(__privateMethod(this, _MediaPlayer_instances, watchVolume_fn).bind(this));
      effect(__privateMethod(this, _MediaPlayer_instances, watchCurrentTime_fn).bind(this));
      effect(__privateMethod(this, _MediaPlayer_instances, watchPlaysInline_fn2).bind(this));
      effect(__privateMethod(this, _MediaPlayer_instances, watchPlaybackRate_fn).bind(this));
    }
    onAttach(el) {
      el.setAttribute("data-media-player", "");
      setAttributeIfEmpty(el, "tabindex", "0");
      setAttributeIfEmpty(el, "role", "region");
      effect(__privateMethod(this, _MediaPlayer_instances, watchStorage_fn).bind(this));
      if (IS_SERVER2) __privateMethod(this, _MediaPlayer_instances, watchTitle_fn2).call(this);
      else effect(__privateMethod(this, _MediaPlayer_instances, watchTitle_fn2).bind(this));
      if (IS_SERVER2) __privateMethod(this, _MediaPlayer_instances, watchOrientation_fn).call(this);
      else effect(__privateMethod(this, _MediaPlayer_instances, watchOrientation_fn).bind(this));
      listenEvent(el, "find-media-player", __privateMethod(this, _MediaPlayer_instances, onFindPlayer_fn).bind(this));
    }
    onConnect(el) {
      if (IS_IPHONE) setAttribute(el, "data-iphone", "");
      const pointerQuery = window.matchMedia("(pointer: coarse)");
      __privateMethod(this, _MediaPlayer_instances, onPointerChange_fn).call(this, pointerQuery);
      pointerQuery.onchange = __privateMethod(this, _MediaPlayer_instances, onPointerChange_fn).bind(this);
      const resize = new ResizeObserver(animationFrameThrottle(__privateMethod(this, _MediaPlayer_instances, onResize_fn).bind(this)));
      resize.observe(el);
      effect(__privateMethod(this, _MediaPlayer_instances, onResize_fn).bind(this));
      this.dispatch("media-player-connect", {
        detail: this,
        bubbles: true,
        composed: true
      });
      onDispose(() => {
        resize.disconnect();
        pointerQuery.onchange = null;
      });
    }
    onDestroy() {
      __privateGet(this, _media10).player = null;
      this.canPlayQueue.reset();
    }
    /**
    * The current media provider.
    */
    get provider() {
      return __privateGet(this, _MediaPlayer_instances, provider_get);
    }
    /**
    * Media controls settings.
    */
    get controls() {
      return __privateGet(this, _requestMgr).controls;
    }
    set controls(controls) {
      __privateGet(this, _MediaPlayer_instances, props_get).controls.set(controls);
    }
    /**
    * The title of the current media.
    */
    get title() {
      return peek(this.$state.title);
    }
    set title(newTitle) {
      if (__privateGet(this, _skipTitleUpdate)) {
        __privateSet(this, _skipTitleUpdate, false);
        return;
      }
      __privateGet(this, _MediaPlayer_instances, props_get).title.set(newTitle);
    }
    /**
    * A list of all `VideoQuality` objects representing the set of available video renditions.
    *
    * @see {@link https://vidstack.io/docs/player/api/video-quality}
    */
    get qualities() {
      return __privateGet(this, _media10).qualities;
    }
    /**
    * A list of all `AudioTrack` objects representing the set of available audio tracks.
    *
    * @see {@link https://vidstack.io/docs/player/api/audio-tracks}
    */
    get audioTracks() {
      return __privateGet(this, _media10).audioTracks;
    }
    /**
    * A list of all `TextTrack` objects representing the set of available text tracks.
    *
    * @see {@link https://vidstack.io/docs/player/api/text-tracks}
    */
    get textTracks() {
      return __privateGet(this, _media10).textTracks;
    }
    /**
    * Contains text renderers which are responsible for loading, parsing, and rendering text
    * tracks.
    */
    get textRenderers() {
      return __privateGet(this, _media10).textRenderers;
    }
    get duration() {
      return this.$state.duration();
    }
    set duration(duration) {
      __privateGet(this, _MediaPlayer_instances, props_get).duration.set(duration);
    }
    get paused() {
      return peek(this.$state.paused);
    }
    set paused(paused) {
      __privateMethod(this, _MediaPlayer_instances, queuePausedUpdate_fn).call(this, paused);
    }
    get muted() {
      return peek(this.$state.muted);
    }
    set muted(muted) {
      __privateMethod(this, _MediaPlayer_instances, queueMutedUpdate_fn).call(this, muted);
    }
    get currentTime() {
      return peek(this.$state.currentTime);
    }
    set currentTime(time) {
      __privateMethod(this, _MediaPlayer_instances, queueCurrentTimeUpdate_fn).call(this, time);
    }
    get volume() {
      return peek(this.$state.volume);
    }
    set volume(volume) {
      __privateMethod(this, _MediaPlayer_instances, queueVolumeUpdate_fn).call(this, volume);
    }
    get playbackRate() {
      return peek(this.$state.playbackRate);
    }
    set playbackRate(rate) {
      __privateMethod(this, _MediaPlayer_instances, queuePlaybackRateUpdate_fn).call(this, rate);
    }
    /**
    * Begins/resumes playback of the media. If this method is called programmatically before the
    * user has interacted with the player, the promise may be rejected subject to the browser's
    * autoplay policies. This method will throw if called before media is ready for playback.
    *
    * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/play}
    */
    async play(trigger) {
      return __privateGet(this, _requestMgr).play(trigger);
    }
    /**
    * Pauses playback of the media. This method will throw if called before media is ready for
    * playback.
    *
    * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/pause}
    */
    async pause(trigger) {
      return __privateGet(this, _requestMgr).pause(trigger);
    }
    /**
    * Attempts to display the player in fullscreen. The promise will resolve if successful, and
    * reject if not. This method will throw if any fullscreen API is _not_ currently available.
    *
    * @see {@link https://vidstack.io/docs/player/api/fullscreen}
    */
    async enterFullscreen(target, trigger) {
      return __privateGet(this, _requestMgr).enterFullscreen(target, trigger);
    }
    /**
    * Attempts to display the player inline by exiting fullscreen. This method will throw if any
    * fullscreen API is _not_ currently available.
    *
    * @see {@link https://vidstack.io/docs/player/api/fullscreen}
    */
    async exitFullscreen(target, trigger) {
      return __privateGet(this, _requestMgr).exitFullscreen(target, trigger);
    }
    /**
    * Attempts to display the player in picture-in-picture mode. This method will throw if PIP is
    * not supported. This method will also return a `PictureInPictureWindow` if the current
    * provider supports it.
    *
    * @see {@link https://vidstack.io/docs/player/api/picture-in-picture}
    */
    enterPictureInPicture(trigger) {
      return __privateGet(this, _requestMgr).enterPictureInPicture(trigger);
    }
    /**
    * Attempts to display the player in inline by exiting picture-in-picture mode. This method
    * will throw if not supported.
    *
    * @see {@link https://vidstack.io/docs/player/api/picture-in-picture}
    */
    exitPictureInPicture(trigger) {
      return __privateGet(this, _requestMgr).exitPictureInPicture(trigger);
    }
    /**
    * Sets the current time to the live edge (i.e., `duration`). This is a no-op for non-live
    * streams and will throw if called before media is ready for playback.
    *
    * @see {@link https://vidstack.io/docs/player/api/live}
    */
    seekToLiveEdge(trigger) {
      __privateGet(this, _requestMgr).seekToLiveEdge(trigger);
    }
    /**
    * Called when media can begin loading. Calling this method will trigger the initial provider
    * loading process. Calling it more than once has no effect.
    *
    * @see {@link https://vidstack.io/docs/player/core-concepts/loading#load-strategies}
    */
    startLoading(trigger) {
      __privateGet(this, _media10).notify("can-load", void 0, trigger);
    }
    /**
    * Called when the poster image can begin loading. Calling it more than once has no effect.
    *
    * @see {@link https://vidstack.io/docs/player/core-concepts/loading#load-strategies}
    */
    startLoadingPoster(trigger) {
      __privateGet(this, _media10).notify("can-load-poster", void 0, trigger);
    }
    /**
    * Request Apple AirPlay picker to open.
    */
    requestAirPlay(trigger) {
      return __privateGet(this, _requestMgr).requestAirPlay(trigger);
    }
    /**
    * Request Google Cast device picker to open. The Google Cast framework will be loaded if it
    * hasn't yet.
    */
    requestGoogleCast(trigger) {
      return __privateGet(this, _requestMgr).requestGoogleCast(trigger);
    }
    /**
    * Set the audio gain, amplifying volume and enabling a maximum volume above 100%.
    *
    * @see {@link https://vidstack.io/docs/player/api/audio-gain}
    */
    setAudioGain(gain, trigger) {
      return __privateGet(this, _requestMgr).setAudioGain(gain, trigger);
    }
    destroy() {
      super.destroy();
      __privateGet(this, _media10).remote.setPlayer(null);
      this.dispatch("destroy");
    }
  }, _media10 = new WeakMap(), _stateMgr2 = new WeakMap(), _requestMgr = new WeakMap(), _MediaPlayer_instances = new WeakSet(), provider_get = function() {
    return __privateGet(this, _media10).$provider();
  }, props_get = function() {
    return this.$props;
  }, _skipTitleUpdate = new WeakMap(), watchTitle_fn2 = function() {
    const el = this.$el, { ariaLabel } = this.$props, { title, live, viewType, providedTitle } = this.$state, isLive = live(), type = uppercaseFirstChar(viewType()), typeText = type !== "Unknown" ? `${isLive ? "Live " : ""}${type}` : isLive ? "Live" : "Media", currentTitle = title(), label = ariaLabel() || `${typeText} Player` + (currentTitle ? ` - ${currentTitle}` : "");
    setAttribute(this.el, "aria-label", label);
    if (!IS_SERVER2 && el?.hasAttribute("title")) {
      __privateSet(this, _skipTitleUpdate, true);
      el?.removeAttribute("title");
    }
  }, watchOrientation_fn = function() {
    const orientation = this.orientation.landscape ? "landscape" : "portrait";
    this.$state.orientation.set(orientation);
    setAttribute(this.el, "data-orientation", orientation);
    __privateMethod(this, _MediaPlayer_instances, onResize_fn).call(this);
  }, watchCanPlay_fn = function() {
    if (this.$state.canPlay() && __privateGet(this, _MediaPlayer_instances, provider_get)) this.canPlayQueue.start();
    else this.canPlayQueue.stop();
  }, setupMediaAttributes_fn = function() {
    if (_a56[MEDIA_ATTRIBUTES]) {
      this.setAttributes(_a56[MEDIA_ATTRIBUTES]);
      return;
    }
    const $attrs = {
      "data-load": function() {
        return this.$props.load();
      },
      "data-captions": function() {
        const track = this.$state.textTrack();
        return !!track && isTrackCaptionKind(track);
      },
      "data-ios-controls": function() {
        return this.$state.iOSControls();
      },
      "data-controls": function() {
        return this.controls.showing;
      },
      "data-buffering": function() {
        const { canLoad, canPlay, waiting } = this.$state;
        return canLoad() && (!canPlay() || waiting());
      },
      "data-error": function() {
        const { error } = this.$state;
        return !!error();
      },
      "data-autoplay-error": function() {
        const { autoPlayError } = this.$state;
        return !!autoPlayError();
      }
    };
    const alias = {
      autoPlay: "autoplay",
      canAirPlay: "can-airplay",
      canPictureInPicture: "can-pip",
      pictureInPicture: "pip",
      playsInline: "playsinline",
      remotePlaybackState: "remote-state",
      remotePlaybackType: "remote-type",
      isAirPlayConnected: "airplay",
      isGoogleCastConnected: "google-cast"
    };
    for (const prop2 of mediaAttributes) {
      const attrName = "data-" + (alias[prop2] ?? camelToKebabCase(prop2));
      $attrs[attrName] = function() {
        return this.$state[prop2]();
      };
    }
    delete $attrs.title;
    _a56[MEDIA_ATTRIBUTES] = $attrs;
    this.setAttributes($attrs);
  }, onFindPlayer_fn = function(event) {
    event.detail(this);
  }, onResize_fn = function() {
    if (IS_SERVER2 || !this.el) return;
    const width = this.el.clientWidth, height = this.el.clientHeight;
    this.$state.width.set(width);
    this.$state.height.set(height);
    setStyle(this.el, "--player-width", width + "px");
    setStyle(this.el, "--player-height", height + "px");
  }, onPointerChange_fn = function(queryList) {
    if (IS_SERVER2) return;
    const pointer = queryList.matches ? "coarse" : "fine";
    setAttribute(this.el, "data-pointer", pointer);
    this.$state.pointer.set(pointer);
    __privateMethod(this, _MediaPlayer_instances, onResize_fn).call(this);
  }, watchPaused_fn2 = function() {
    __privateMethod(this, _MediaPlayer_instances, queuePausedUpdate_fn).call(this, this.$props.paused());
  }, queuePausedUpdate_fn = function(paused) {
    if (paused) this.canPlayQueue.enqueue("paused", () => __privateGet(this, _requestMgr).pause());
    else this.canPlayQueue.enqueue("paused", () => __privateGet(this, _requestMgr).play());
  }, watchMuted_fn = function() {
    __privateMethod(this, _MediaPlayer_instances, queueMutedUpdate_fn).call(this, this.$props.muted());
  }, queueMutedUpdate_fn = function(muted) {
    this.canPlayQueue.enqueue("muted", () => {
      peek(() => {
        if (__privateGet(this, _MediaPlayer_instances, provider_get)) __privateGet(this, _MediaPlayer_instances, provider_get).setMuted(muted);
      });
    });
  }, watchCurrentTime_fn = function() {
    __privateMethod(this, _MediaPlayer_instances, queueCurrentTimeUpdate_fn).call(this, this.$props.currentTime());
  }, queueCurrentTimeUpdate_fn = function(time) {
    this.canPlayQueue.enqueue("currentTime", () => {
      const { currentTime } = this.$state;
      if (time === peek(currentTime)) return;
      peek(() => {
        if (!__privateGet(this, _MediaPlayer_instances, provider_get)) return;
        const boundedTime = boundTime(time, this.$state);
        if (Number.isFinite(boundedTime)) __privateGet(this, _MediaPlayer_instances, provider_get).setCurrentTime(boundedTime);
      });
    });
  }, watchVolume_fn = function() {
    __privateMethod(this, _MediaPlayer_instances, queueVolumeUpdate_fn).call(this, this.$props.volume());
  }, queueVolumeUpdate_fn = function(volume) {
    const clampedVolume = clampNumber(0, volume, 1);
    this.canPlayQueue.enqueue("volume", () => {
      if (__privateGet(this, _MediaPlayer_instances, provider_get)) __privateGet(this, _MediaPlayer_instances, provider_get).setVolume(clampedVolume);
    });
  }, watchPlaybackRate_fn = function() {
    __privateMethod(this, _MediaPlayer_instances, queuePlaybackRateUpdate_fn).call(this, this.$props.playbackRate());
  }, queuePlaybackRateUpdate_fn = function(rate) {
    this.canPlayQueue.enqueue("rate", () => {
      if (__privateGet(this, _MediaPlayer_instances, provider_get)) __privateGet(this, _MediaPlayer_instances, provider_get).setPlaybackRate?.(rate);
    });
  }, watchPlaysInline_fn2 = function() {
    __privateMethod(this, _MediaPlayer_instances, queuePlaysInlineUpdate_fn).call(this, this.$props.playsInline());
  }, queuePlaysInlineUpdate_fn = function(inline2) {
    this.canPlayQueue.enqueue("playsinline", () => {
      if (__privateGet(this, _MediaPlayer_instances, provider_get)) __privateGet(this, _MediaPlayer_instances, provider_get).setPlaysInline?.(inline2);
    });
  }, watchStorage_fn = function() {
    let storageValue = this.$props.storage(), storage = isString(storageValue) ? new LocalMediaStorage() : storageValue;
    if (storage?.onChange) {
      const { source } = this.$state, playerId = isString(storageValue) ? storageValue : this.el?.id, mediaId = computed(__privateMethod(this, _MediaPlayer_instances, computeMediaId_fn).bind(this));
      effect(() => storage.onChange(source(), mediaId(), playerId || void 0));
    }
    __privateGet(this, _media10).storage = storage;
    __privateGet(this, _media10).textTracks.setStorage(storage);
    onDispose(() => {
      storage?.onDestroy?.();
      __privateGet(this, _media10).storage = null;
      __privateGet(this, _media10).textTracks.setStorage(null);
    });
  }, computeMediaId_fn = function() {
    const { clipStartTime, clipEndTime } = this.$props, { source } = this.$state, src = source();
    return src.src ? `${src.src}:${clipStartTime()}:${clipEndTime()}` : null;
  }, __publicField(_a56, "props", mediaPlayerProps), __publicField(_a56, "state", mediaState), _a56);
  var mediaplayer__proto = MediaPlayer.prototype;
  prop(mediaplayer__proto, "canPlayQueue");
  prop(mediaplayer__proto, "remoteControl");
  prop(mediaplayer__proto, "provider");
  prop(mediaplayer__proto, "controls");
  prop(mediaplayer__proto, "orientation");
  prop(mediaplayer__proto, "title");
  prop(mediaplayer__proto, "qualities");
  prop(mediaplayer__proto, "audioTracks");
  prop(mediaplayer__proto, "textTracks");
  prop(mediaplayer__proto, "textRenderers");
  prop(mediaplayer__proto, "duration");
  prop(mediaplayer__proto, "paused");
  prop(mediaplayer__proto, "muted");
  prop(mediaplayer__proto, "currentTime");
  prop(mediaplayer__proto, "volume");
  prop(mediaplayer__proto, "playbackRate");
  method(mediaplayer__proto, "play");
  method(mediaplayer__proto, "pause");
  method(mediaplayer__proto, "enterFullscreen");
  method(mediaplayer__proto, "exitFullscreen");
  method(mediaplayer__proto, "enterPictureInPicture");
  method(mediaplayer__proto, "exitPictureInPicture");
  method(mediaplayer__proto, "seekToLiveEdge");
  method(mediaplayer__proto, "startLoading");
  method(mediaplayer__proto, "startLoadingPoster");
  method(mediaplayer__proto, "requestAirPlay");
  method(mediaplayer__proto, "requestGoogleCast");
  method(mediaplayer__proto, "setAudioGain");
  function resolveStreamTypeFromDASHManifest(manifestSrc, requestInit) {
    return fetch(manifestSrc, requestInit).then((res) => res.text()).then((manifest) => {
      return /type="static"/.test(manifest) ? "on-demand" : "live";
    });
  }
  function resolveStreamTypeFromHLSManifest(manifestSrc, requestInit) {
    return fetch(manifestSrc, requestInit).then((res) => res.text()).then((manifest) => {
      const renditionURI = resolveHLSRenditionURI(manifest);
      if (renditionURI) return resolveStreamTypeFromHLSManifest(/^https?:/.test(renditionURI) ? renditionURI : new URL(renditionURI, manifestSrc).href, requestInit);
      const streamType = /EXT-X-PLAYLIST-TYPE:\s*VOD/.test(manifest) ? "on-demand" : "live";
      if (streamType === "live" && resolveTargetDuration(manifest) >= 10 && (/#EXT-X-DVR-ENABLED:\s*true/.test(manifest) || manifest.includes("#EXT-X-DISCONTINUITY"))) return "live:dvr";
      return streamType;
    });
  }
  function resolveHLSRenditionURI(manifest) {
    const matches = manifest.match(/#EXT-X-STREAM-INF:[^\n]+(\n[^\n]+)*/g);
    return matches ? matches[0].split("\n")[1].trim() : null;
  }
  function resolveTargetDuration(manifest) {
    const lines = manifest.split("\n");
    for (const line of lines) if (line.startsWith("#EXT-X-TARGETDURATION")) {
      const duration = parseFloat(line.split(":")[1]);
      if (!isNaN(duration)) return duration;
    }
    return -1;
  }
  var sourceTypes = /* @__PURE__ */ new Map();
  var _initialize, _loaders, _domSources, _media11, _loader, _SourceSelection_instances, onSourcesChange_fn, onSourceChange_fn, findNewSource_fn, notifySourceChange_fn, notifyLoaderChange_fn, onSetup_fn, onLoadSource_fn, onLoadPoster_fn, _a57;
  var SourceSelection = (_a57 = class {
    constructor(domSources, media, loader, customLoaders = []) {
      __privateAdd(this, _SourceSelection_instances);
      __privateAdd(this, _initialize, false);
      __privateAdd(this, _loaders);
      __privateAdd(this, _domSources);
      __privateAdd(this, _media11);
      __privateAdd(this, _loader);
      __privateSet(this, _domSources, domSources);
      __privateSet(this, _media11, media);
      __privateSet(this, _loader, loader);
      const DASH_LOADER = new DASHProviderLoader(), HLS_LOADER = new HLSProviderLoader(), VIDEO_LOADER = new VideoProviderLoader(), AUDIO_LOADER = new AudioProviderLoader(), EMBED_LOADERS = [new YouTubeProviderLoader(), new VimeoProviderLoader()];
      __privateSet(this, _loaders, computed(() => {
        const remoteLoader = media.$state.remotePlaybackLoader();
        const loaders = media.$props.preferNativeHLS() ? [
          VIDEO_LOADER,
          AUDIO_LOADER,
          DASH_LOADER,
          HLS_LOADER,
          ...EMBED_LOADERS,
          ...customLoaders
        ] : [
          HLS_LOADER,
          VIDEO_LOADER,
          AUDIO_LOADER,
          DASH_LOADER,
          ...EMBED_LOADERS,
          ...customLoaders
        ];
        return remoteLoader ? [remoteLoader, ...loaders] : loaders;
      }));
      const { $state } = media;
      $state.sources.set(normalizeSrc(media.$props.src()));
      for (const src of $state.sources()) {
        const loader2 = __privateGet(this, _loaders).call(this).find((loader3) => loader3.canPlay(src));
        if (!loader2) continue;
        const mediaType = loader2.mediaType(src);
        media.$state.source.set(src);
        media.$state.mediaType.set(mediaType);
        media.$state.inferredViewType.set(mediaType);
        __privateGet(this, _loader).set(loader2);
        __privateSet(this, _initialize, true);
        break;
      }
    }
    connect() {
      const loader = __privateGet(this, _loader).call(this);
      if (__privateGet(this, _initialize)) {
        __privateMethod(this, _SourceSelection_instances, notifySourceChange_fn).call(this, __privateGet(this, _media11).$state.source(), loader);
        __privateMethod(this, _SourceSelection_instances, notifyLoaderChange_fn).call(this, loader);
        __privateSet(this, _initialize, false);
      }
      effect(__privateMethod(this, _SourceSelection_instances, onSourcesChange_fn).bind(this));
      effect(__privateMethod(this, _SourceSelection_instances, onSourceChange_fn).bind(this));
      effect(__privateMethod(this, _SourceSelection_instances, onSetup_fn).bind(this));
      effect(__privateMethod(this, _SourceSelection_instances, onLoadSource_fn).bind(this));
      effect(__privateMethod(this, _SourceSelection_instances, onLoadPoster_fn).bind(this));
    }
  }, _initialize = new WeakMap(), _loaders = new WeakMap(), _domSources = new WeakMap(), _media11 = new WeakMap(), _loader = new WeakMap(), _SourceSelection_instances = new WeakSet(), onSourcesChange_fn = function() {
    __privateGet(this, _media11).notify("sources-change", [...normalizeSrc(__privateGet(this, _media11).$props.src()), ...__privateGet(this, _domSources).call(this)]);
  }, onSourceChange_fn = function() {
    const { $state } = __privateGet(this, _media11);
    const sources = $state.sources(), currentSource = peek($state.source), newSource = __privateMethod(this, _SourceSelection_instances, findNewSource_fn).call(this, currentSource, sources);
    if (sources[0]?.src && !newSource.src && !newSource.type) {
      const { crossOrigin } = $state, credentials = getRequestCredentials(crossOrigin()), abort = new AbortController();
      Promise.all(sources.map((source) => isString(source.src) && source.type === "?" ? fetch(source.src, {
        method: "HEAD",
        credentials,
        signal: abort.signal
      }).then((res) => {
        source.type = res.headers.get("content-type") || "??";
        sourceTypes.set(source.src, source.type);
        return source;
      }).catch(() => source) : source)).then((sources2) => {
        if (abort.signal.aborted) return;
        const newSource2 = __privateMethod(this, _SourceSelection_instances, findNewSource_fn).call(this, peek($state.source), sources2);
        tick();
        if (!newSource2.src) __privateGet(this, _media11).notify("error", {
          message: "Failed to load resource.",
          code: 4
        });
      });
      return () => abort.abort();
    }
    tick();
  }, findNewSource_fn = function(currentSource, sources) {
    let newSource = {
      src: "",
      type: ""
    }, newLoader = null, triggerEvent = new DOMEvent("sources-change", { detail: { sources } }), loaders = __privateGet(this, _loaders).call(this), { started, paused, currentTime, quality, savedState } = __privateGet(this, _media11).$state;
    for (const src of sources) {
      const loader = loaders.find((loader2) => loader2.canPlay(src));
      if (loader) {
        newSource = src;
        newLoader = loader;
        break;
      }
    }
    if (isVideoQualitySrc(newSource)) {
      const currentQuality = quality(), sourceQuality = sources.find((s) => s.src === currentQuality?.src);
      if (peek(started)) savedState.set({
        paused: peek(paused),
        currentTime: peek(currentTime)
      });
      else savedState.set(null);
      if (sourceQuality) {
        newSource = sourceQuality;
        triggerEvent = new DOMEvent("quality-change", { detail: { quality: currentQuality } });
      }
    }
    if (!isSameSrc(currentSource, newSource)) __privateMethod(this, _SourceSelection_instances, notifySourceChange_fn).call(this, newSource, newLoader, triggerEvent);
    if (newLoader !== peek(__privateGet(this, _loader))) __privateMethod(this, _SourceSelection_instances, notifyLoaderChange_fn).call(this, newLoader, triggerEvent);
    return newSource;
  }, notifySourceChange_fn = function(src, loader, trigger) {
    __privateGet(this, _media11).notify("source-change", src, trigger);
    __privateGet(this, _media11).notify("media-type-change", loader?.mediaType(src) || "unknown", trigger);
  }, notifyLoaderChange_fn = function(loader, trigger) {
    __privateGet(this, _media11).$providerSetup.set(false);
    __privateGet(this, _media11).notify("provider-change", null, trigger);
    loader && peek(() => loader.preconnect?.(__privateGet(this, _media11)));
    __privateGet(this, _loader).set(loader);
    __privateGet(this, _media11).notify("provider-loader-change", loader, trigger);
  }, onSetup_fn = function() {
    const provider = __privateGet(this, _media11).$provider();
    if (!provider || peek(__privateGet(this, _media11).$providerSetup)) return;
    if (__privateGet(this, _media11).$state.canLoad()) {
      scoped(() => provider.setup(), provider.scope);
      __privateGet(this, _media11).$providerSetup.set(true);
      return;
    }
    peek(() => provider.preconnect?.());
  }, onLoadSource_fn = function() {
    if (!__privateGet(this, _media11).$providerSetup()) return;
    const provider = __privateGet(this, _media11).$provider(), source = __privateGet(this, _media11).$state.source(), crossOrigin = peek(__privateGet(this, _media11).$state.crossOrigin), preferNativeHLS = peek(__privateGet(this, _media11).$props.preferNativeHLS);
    if (isSameSrc(provider?.currentSrc, source)) return;
    if (__privateGet(this, _media11).$state.canLoad()) {
      const abort = new AbortController();
      if (isHLSSrc(source)) {
        if (preferNativeHLS || !isHLSSupported()) resolveStreamTypeFromHLSManifest(source.src, {
          credentials: getRequestCredentials(crossOrigin),
          signal: abort.signal
        }).then((streamType) => {
          __privateGet(this, _media11).notify("stream-type-change", streamType);
        }).catch(noop);
      } else if (isDASHSrc(source)) resolveStreamTypeFromDASHManifest(source.src, {
        credentials: getRequestCredentials(crossOrigin),
        signal: abort.signal
      }).then((streamType) => {
        __privateGet(this, _media11).notify("stream-type-change", streamType);
      }).catch(noop);
      else __privateGet(this, _media11).notify("stream-type-change", "on-demand");
      peek(() => {
        const preload = peek(__privateGet(this, _media11).$state.preload);
        return provider?.loadSource(source, preload).catch((error) => {
        });
      });
      return () => abort.abort();
    }
    try {
      isString(source.src) && preconnect(new URL(source.src).origin);
    } catch (error) {
    }
  }, onLoadPoster_fn = function() {
    const loader = __privateGet(this, _loader).call(this), { providedPoster, source, canLoadPoster } = __privateGet(this, _media11).$state;
    if (!loader || !loader.loadPoster || !source() || !canLoadPoster() || providedPoster()) return;
    const abort = new AbortController(), trigger = new DOMEvent("source-change", { detail: source });
    loader.loadPoster(source(), __privateGet(this, _media11), abort).then((url) => {
      __privateGet(this, _media11).notify("poster-change", url || "", trigger);
    }).catch(() => {
      __privateGet(this, _media11).notify("poster-change", "", trigger);
    });
    return () => {
      abort.abort();
    };
  }, _a57);
  function normalizeSrc(src) {
    return (isArray(src) ? src : [src]).map((src2) => {
      if (isString(src2)) return {
        src: src2,
        type: inferType(src2)
      };
      else return {
        ...src2,
        type: inferType(src2.src, src2.type)
      };
    });
  }
  function inferType(src, type) {
    if (isString(type) && type.length) return type;
    else if (isString(src) && sourceTypes.has(src)) return sourceTypes.get(src);
    else if (!type && isHLSSrc({
      src,
      type: ""
    })) return "application/x-mpegurl";
    else if (!type && isDASHSrc({
      src,
      type: ""
    })) return "application/dash+xml";
    else if (!isString(src) || src.startsWith("blob:")) return "video/object";
    else if (src.includes("youtube") || src.includes("youtu.be")) return "video/youtube";
    else if (src.includes("vimeo") && !src.includes("progressive_redirect") && !src.includes(".m3u8")) return "video/vimeo";
    return "?";
  }
  function isSameSrc(a, b) {
    return a?.src === b?.src && a?.type === b?.type;
  }
  var _domTracks, _media12, _prevTracks, _Tracks_instances, onTracksChange_fn, _a58;
  var Tracks = (_a58 = class {
    constructor(domTracks, media) {
      __privateAdd(this, _Tracks_instances);
      __privateAdd(this, _domTracks);
      __privateAdd(this, _media12);
      __privateAdd(this, _prevTracks, []);
      __privateSet(this, _domTracks, domTracks);
      __privateSet(this, _media12, media);
      effect(__privateMethod(this, _Tracks_instances, onTracksChange_fn).bind(this));
    }
  }, _domTracks = new WeakMap(), _media12 = new WeakMap(), _prevTracks = new WeakMap(), _Tracks_instances = new WeakSet(), onTracksChange_fn = function() {
    const newTracks = __privateGet(this, _domTracks).call(this);
    for (const oldTrack of __privateGet(this, _prevTracks)) if (!newTracks.some((t2) => t2.id === oldTrack.id)) {
      const track = oldTrack.id && __privateGet(this, _media12).textTracks.getById(oldTrack.id);
      if (track) __privateGet(this, _media12).textTracks.remove(track);
    }
    for (const newTrack of newTracks) {
      const id2 = newTrack.id || TextTrack.createId(newTrack);
      if (!__privateGet(this, _media12).textTracks.getById(id2)) {
        newTrack.id = id2;
        __privateGet(this, _media12).textTracks.add(newTrack);
      }
    }
    __privateSet(this, _prevTracks, newTracks);
  }, _a58);
  var _a59, _media13, _sources, _domSources2, _domTracks2, _loader2, _target3, _connected, _loadRafId, _MediaProvider_instances, runLoader_fn, destroyProvider_fn, onResize_fn2, onMutation_fn;
  var MediaProvider = (_a59 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _MediaProvider_instances);
      __privateAdd(this, _media13);
      __privateAdd(this, _sources);
      __privateAdd(this, _domSources2, signal([]));
      __privateAdd(this, _domTracks2, signal([]));
      __privateAdd(this, _loader2, null);
      __privateAdd(this, _target3, null);
      __privateAdd(this, _connected, false);
      __privateAdd(this, _loadRafId, -1);
    }
    onSetup() {
      __privateSet(this, _media13, useMediaContext());
      __privateSet(this, _sources, new SourceSelection(__privateGet(this, _domSources2), __privateGet(this, _media13), this.$state.loader, this.$props.loaders()));
    }
    onAttach(el) {
      el.setAttribute("data-media-provider", "");
    }
    onConnect(el) {
      __privateSet(this, _connected, true);
      __privateGet(this, _sources).connect();
      new Tracks(__privateGet(this, _domTracks2), __privateGet(this, _media13));
      const resize = new ResizeObserver(animationFrameThrottle(__privateMethod(this, _MediaProvider_instances, onResize_fn2).bind(this)));
      resize.observe(el);
      const mutations = new MutationObserver(__privateMethod(this, _MediaProvider_instances, onMutation_fn).bind(this));
      mutations.observe(el, {
        attributes: true,
        childList: true
      });
      __privateMethod(this, _MediaProvider_instances, onResize_fn2).call(this);
      __privateMethod(this, _MediaProvider_instances, onMutation_fn).call(this);
      if (__privateGet(this, _target3)) this.load(__privateGet(this, _target3));
      onDispose(() => {
        __privateSet(this, _connected, false);
        resize.disconnect();
        mutations.disconnect();
      });
    }
    load(target) {
      __privateSet(this, _target3, target || null);
      target?.setAttribute("aria-hidden", "true");
      if (!__privateGet(this, _connected)) return;
      window.cancelAnimationFrame(__privateGet(this, _loadRafId));
      __privateSet(this, _loadRafId, requestAnimationFrame(() => __privateMethod(this, _MediaProvider_instances, runLoader_fn).call(this, target)));
      onDispose(() => {
        window.cancelAnimationFrame(__privateGet(this, _loadRafId));
      });
    }
    onDestroy() {
      __privateSet(this, _loader2, null);
      __privateMethod(this, _MediaProvider_instances, destroyProvider_fn).call(this);
    }
  }, _media13 = new WeakMap(), _sources = new WeakMap(), _domSources2 = new WeakMap(), _domTracks2 = new WeakMap(), _loader2 = new WeakMap(), _target3 = new WeakMap(), _connected = new WeakMap(), _loadRafId = new WeakMap(), _MediaProvider_instances = new WeakSet(), runLoader_fn = function(target) {
    if (!this.scope) return;
    const loader = this.$state.loader(), { $provider } = __privateGet(this, _media13);
    if (__privateGet(this, _loader2) === loader && loader?.target === target && peek($provider)) return;
    __privateMethod(this, _MediaProvider_instances, destroyProvider_fn).call(this);
    __privateSet(this, _loader2, loader);
    if (loader) loader.target = target || null;
    if (!loader || !target) return;
    loader.load(__privateGet(this, _media13)).then((provider) => {
      if (!this.scope) return;
      if (peek(this.$state.loader) !== loader) return;
      __privateGet(this, _media13).notify("provider-change", provider);
    });
  }, destroyProvider_fn = function() {
    __privateGet(this, _media13)?.notify("provider-change", null);
  }, onResize_fn2 = function() {
    if (!this.el) return;
    const { player, $state } = __privateGet(this, _media13), width = this.el.offsetWidth, height = this.el.offsetHeight;
    if (!player) return;
    $state.mediaWidth.set(width);
    $state.mediaHeight.set(height);
    if (player.el) {
      setStyle(player.el, "--media-width", width + "px");
      setStyle(player.el, "--media-height", height + "px");
    }
  }, onMutation_fn = function() {
    const sources = [], tracks = [], children = this.el.children;
    for (const el of children) {
      if (el.hasAttribute("data-vds")) continue;
      if (el instanceof HTMLSourceElement) {
        const src = {
          id: el.id,
          src: el.src,
          type: el.type
        };
        for (const prop2 of [
          "id",
          "src",
          "width",
          "height",
          "bitrate",
          "codec"
        ]) {
          const value = el.getAttribute(`data-${prop2}`);
          if (isString(value)) src[prop2] = /id|src|codec/.test(prop2) ? value : Number(value);
        }
        sources.push(src);
      } else if (el instanceof HTMLTrackElement) {
        const track = {
          src: el.src,
          kind: el.track.kind,
          language: el.srclang,
          label: el.label,
          default: el.default,
          type: el.getAttribute("data-type")
        };
        tracks.push({
          id: el.id || TextTrack.createId(track),
          ...track
        });
      }
    }
    __privateGet(this, _domSources2).set(sources);
    __privateGet(this, _domTracks2).set(tracks);
    tick();
  }, __publicField(_a59, "props", { loaders: [] }), __publicField(_a59, "state", new State({ loader: null })), _a59);
  var mediaprovider__proto = MediaProvider.prototype;
  method(mediaprovider__proto, "load");
  var _a60, _media14, _initializing, _MediaAnnouncer_instances, watchPaused_fn3, watchFullscreen_fn, watchPiP_fn, watchCaptions_fn, watchVolume_fn2, _startedSeekingAt, _seekTimer, watchSeeking_fn, translate_fn, watchLabel_fn, setLabel_fn;
  var MediaAnnouncer = (_a60 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _MediaAnnouncer_instances);
      __privateAdd(this, _media14);
      __privateAdd(this, _initializing, false);
      __privateAdd(this, _startedSeekingAt, -1);
      __privateAdd(this, _seekTimer, -1);
    }
    onSetup() {
      __privateSet(this, _media14, useMediaContext());
    }
    onAttach(el) {
      el.style.display = "contents";
    }
    onConnect(el) {
      el.setAttribute("data-media-announcer", "");
      setAttributeIfEmpty(el, "role", "status");
      setAttributeIfEmpty(el, "aria-live", "polite");
      const { busy } = this.$state;
      this.setAttributes({ "aria-busy": () => busy() ? "true" : null });
      __privateSet(this, _initializing, true);
      effect(__privateMethod(this, _MediaAnnouncer_instances, watchPaused_fn3).bind(this));
      effect(__privateMethod(this, _MediaAnnouncer_instances, watchVolume_fn2).bind(this));
      effect(__privateMethod(this, _MediaAnnouncer_instances, watchCaptions_fn).bind(this));
      effect(__privateMethod(this, _MediaAnnouncer_instances, watchFullscreen_fn).bind(this));
      effect(__privateMethod(this, _MediaAnnouncer_instances, watchPiP_fn).bind(this));
      effect(__privateMethod(this, _MediaAnnouncer_instances, watchSeeking_fn).bind(this));
      effect(__privateMethod(this, _MediaAnnouncer_instances, watchLabel_fn).bind(this));
      tick();
      __privateSet(this, _initializing, false);
    }
  }, _media14 = new WeakMap(), _initializing = new WeakMap(), _MediaAnnouncer_instances = new WeakSet(), watchPaused_fn3 = function() {
    const { paused } = __privateGet(this, _media14).$state;
    __privateMethod(this, _MediaAnnouncer_instances, setLabel_fn).call(this, !paused() ? "Play" : "Pause");
  }, watchFullscreen_fn = function() {
    const { fullscreen } = __privateGet(this, _media14).$state;
    __privateMethod(this, _MediaAnnouncer_instances, setLabel_fn).call(this, fullscreen() ? "Enter Fullscreen" : "Exit Fullscreen");
  }, watchPiP_fn = function() {
    const { pictureInPicture } = __privateGet(this, _media14).$state;
    __privateMethod(this, _MediaAnnouncer_instances, setLabel_fn).call(this, pictureInPicture() ? "Enter PiP" : "Exit PiP");
  }, watchCaptions_fn = function() {
    const { textTrack } = __privateGet(this, _media14).$state;
    __privateMethod(this, _MediaAnnouncer_instances, setLabel_fn).call(this, textTrack() ? "Closed-Captions On" : "Closed-Captions Off");
  }, watchVolume_fn2 = function() {
    const { muted, volume, audioGain } = __privateGet(this, _media14).$state;
    __privateMethod(this, _MediaAnnouncer_instances, setLabel_fn).call(this, muted() || volume() === 0 ? "Mute" : `${Math.round(volume() * (audioGain() ?? 1) * 100)}% ${__privateMethod(this, _MediaAnnouncer_instances, translate_fn).call(this, "Volume")}`);
  }, _startedSeekingAt = new WeakMap(), _seekTimer = new WeakMap(), watchSeeking_fn = function() {
    const { seeking, currentTime } = __privateGet(this, _media14).$state, isSeeking = seeking();
    if (__privateGet(this, _startedSeekingAt) > 0) {
      window.clearTimeout(__privateGet(this, _seekTimer));
      __privateSet(this, _seekTimer, window.setTimeout(() => {
        if (!this.scope) return;
        const newTime = peek(currentTime), seconds = Math.abs(newTime - __privateGet(this, _startedSeekingAt));
        if (seconds >= 1) {
          const isForward = newTime >= __privateGet(this, _startedSeekingAt), spokenTime = formatSpokenTime(seconds);
          __privateMethod(this, _MediaAnnouncer_instances, setLabel_fn).call(this, `${__privateMethod(this, _MediaAnnouncer_instances, translate_fn).call(this, isForward ? "Seek Forward" : "Seek Backward")} ${spokenTime}`);
        }
        __privateSet(this, _startedSeekingAt, -1);
        __privateSet(this, _seekTimer, -1);
      }, 300));
    } else if (isSeeking) __privateSet(this, _startedSeekingAt, peek(currentTime));
  }, translate_fn = function(word) {
    const { translations } = this.$props;
    return translations?.()?.[word || ""] ?? word;
  }, watchLabel_fn = function() {
    const { label, busy } = this.$state, $label = __privateMethod(this, _MediaAnnouncer_instances, translate_fn).call(this, label());
    if (__privateGet(this, _initializing)) return;
    busy.set(true);
    const id2 = window.setTimeout(() => void busy.set(false), 150);
    this.el && setAttribute(this.el, "aria-label", $label);
    if (isString($label)) this.dispatch("change", { detail: $label });
    return () => window.clearTimeout(id2);
  }, setLabel_fn = function(word) {
    const { label } = this.$state;
    label.set(word);
  }, __publicField(_a60, "props", { translations: null }), __publicField(_a60, "state", new State({
    label: null,
    busy: false
  })), _a60);
  var _a61, _media15, _Controls_instances, hideControls_fn, watchProps_fn, isShowing_fn;
  var Controls = (_a61 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _Controls_instances);
      __privateAdd(this, _media15);
    }
    onSetup() {
      __privateSet(this, _media15, useMediaContext());
      effect(__privateMethod(this, _Controls_instances, watchProps_fn).bind(this));
    }
    onAttach(el) {
      const { pictureInPicture, fullscreen } = __privateGet(this, _media15).$state;
      setStyle(el, "pointer-events", "none");
      setAttributeIfEmpty(el, "role", "group");
      this.setAttributes({
        "data-visible": __privateMethod(this, _Controls_instances, isShowing_fn).bind(this),
        "data-fullscreen": fullscreen,
        "data-pip": pictureInPicture
      });
      effect(() => {
        this.dispatch("change", { detail: __privateMethod(this, _Controls_instances, isShowing_fn).call(this) });
      });
      effect(__privateMethod(this, _Controls_instances, hideControls_fn).bind(this));
      effect(() => {
        const isFullscreen2 = fullscreen();
        for (const side of [
          "top",
          "right",
          "bottom",
          "left"
        ]) setStyle(el, `padding-${side}`, isFullscreen2 && `env(safe-area-inset-${side})`);
      });
    }
  }, _media15 = new WeakMap(), _Controls_instances = new WeakSet(), hideControls_fn = function() {
    if (!this.el) return;
    const { nativeControls } = __privateGet(this, _media15).$state, isHidden = nativeControls();
    setAttribute(this.el, "aria-hidden", isHidden ? "true" : null);
    setStyle(this.el, "display", isHidden ? "none" : null);
  }, watchProps_fn = function() {
    const { controls } = __privateGet(this, _media15).player, { hideDelay, hideOnMouseLeave } = this.$props;
    controls.defaultDelay = hideDelay() === 2e3 ? __privateGet(this, _media15).$props.controlsDelay() : hideDelay();
    controls.hideOnMouseLeave = hideOnMouseLeave();
  }, isShowing_fn = function() {
    const { controlsVisible } = __privateGet(this, _media15).$state;
    return controlsVisible();
  }, __publicField(_a61, "props", {
    hideDelay: 2e3,
    hideOnMouseLeave: false
  }), _a61);
  var ControlsGroup = class extends Component2 {
    onAttach(el) {
      if (!el.style.pointerEvents) setStyle(el, "pointer-events", "auto");
    }
  };
  var _delegate, _Popper_instances, watchTrigger_fn, _showTimerId, _hideRafId, _stopAnimationEndListener, cancelShowing_fn, _a62;
  var Popper = (_a62 = class extends ViewController {
    constructor(delegate) {
      super();
      __privateAdd(this, _Popper_instances);
      __privateAdd(this, _delegate);
      __privateAdd(this, _showTimerId, -1);
      __privateAdd(this, _hideRafId, -1);
      __privateAdd(this, _stopAnimationEndListener, null);
      __privateSet(this, _delegate, delegate);
      effect(__privateMethod(this, _Popper_instances, watchTrigger_fn).bind(this));
    }
    onDestroy() {
      var _a106;
      (_a106 = __privateGet(this, _stopAnimationEndListener)) == null ? void 0 : _a106.call(this);
      __privateSet(this, _stopAnimationEndListener, null);
    }
    show(trigger) {
      var _a106;
      __privateMethod(this, _Popper_instances, cancelShowing_fn).call(this);
      window.cancelAnimationFrame(__privateGet(this, _hideRafId));
      __privateSet(this, _hideRafId, -1);
      (_a106 = __privateGet(this, _stopAnimationEndListener)) == null ? void 0 : _a106.call(this);
      __privateSet(this, _stopAnimationEndListener, null);
      __privateSet(this, _showTimerId, window.setTimeout(() => {
        __privateSet(this, _showTimerId, -1);
        const content = __privateGet(this, _delegate).content();
        if (content) content.style.removeProperty("display");
        peek(() => __privateGet(this, _delegate).onChange(true, trigger));
      }, __privateGet(this, _delegate).showDelay?.() ?? 0));
    }
    hide(trigger) {
      __privateMethod(this, _Popper_instances, cancelShowing_fn).call(this);
      peek(() => __privateGet(this, _delegate).onChange(false, trigger));
      __privateSet(this, _hideRafId, requestAnimationFrame(() => {
        var _a106;
        __privateMethod(this, _Popper_instances, cancelShowing_fn).call(this);
        __privateSet(this, _hideRafId, -1);
        const content = __privateGet(this, _delegate).content();
        if (content) {
          const onHide = () => {
            content.style.display = "none";
            __privateSet(this, _stopAnimationEndListener, null);
          };
          if (hasAnimation(content)) {
            (_a106 = __privateGet(this, _stopAnimationEndListener)) == null ? void 0 : _a106.call(this);
            const stop = listenEvent(content, "animationend", onHide, { once: true });
            __privateSet(this, _stopAnimationEndListener, stop);
          } else onHide();
        }
      }));
    }
  }, _delegate = new WeakMap(), _Popper_instances = new WeakSet(), watchTrigger_fn = function() {
    const trigger = __privateGet(this, _delegate).trigger();
    if (!trigger) {
      this.hide();
      return;
    }
    const show = this.show.bind(this), hide2 = this.hide.bind(this);
    __privateGet(this, _delegate).listen(trigger, show, hide2);
  }, _showTimerId = new WeakMap(), _hideRafId = new WeakMap(), _stopAnimationEndListener = new WeakMap(), cancelShowing_fn = function() {
    window.clearTimeout(__privateGet(this, _showTimerId));
    __privateSet(this, _showTimerId, -1);
  }, _a62);
  var tooltipContext = createContext2();
  var id = 0;
  var _a63, _id2, _trigger, _content, _showing, _Tooltip_instances, attachTrigger_fn, detachTrigger_fn, attachContent_fn, detachContent_fn, onShowingChange_fn;
  var Tooltip = (_a63 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _Tooltip_instances);
      __privateAdd(this, _id2, `media-tooltip-${++id}`);
      __privateAdd(this, _trigger, signal(null));
      __privateAdd(this, _content, signal(null));
      __privateAdd(this, _showing, signal(false));
      new FocusVisibleController();
      const { showDelay } = this.$props;
      new Popper({
        trigger: __privateGet(this, _trigger),
        content: __privateGet(this, _content),
        showDelay,
        listen(trigger, show, hide2) {
          effect(() => {
            if ($keyboard()) listenEvent(trigger, "focus", show);
            listenEvent(trigger, "blur", hide2);
          });
          new EventsController(trigger).add("touchstart", (e2) => e2.preventDefault(), { passive: false }).add("mouseenter", show).add("mouseleave", hide2);
        },
        onChange: __privateMethod(this, _Tooltip_instances, onShowingChange_fn).bind(this)
      });
    }
    onAttach(el) {
      el.style.setProperty("display", "contents");
    }
    onSetup() {
      provideContext(tooltipContext, {
        trigger: __privateGet(this, _trigger),
        content: __privateGet(this, _content),
        showing: __privateGet(this, _showing),
        attachTrigger: __privateMethod(this, _Tooltip_instances, attachTrigger_fn).bind(this),
        detachTrigger: __privateMethod(this, _Tooltip_instances, detachTrigger_fn).bind(this),
        attachContent: __privateMethod(this, _Tooltip_instances, attachContent_fn).bind(this),
        detachContent: __privateMethod(this, _Tooltip_instances, detachContent_fn).bind(this)
      });
    }
  }, _id2 = new WeakMap(), _trigger = new WeakMap(), _content = new WeakMap(), _showing = new WeakMap(), _Tooltip_instances = new WeakSet(), attachTrigger_fn = function(el) {
    __privateGet(this, _trigger).set(el);
    let tooltipName = el.getAttribute("data-media-tooltip");
    if (tooltipName) this.el?.setAttribute(`data-media-${tooltipName}-tooltip`, "");
    setAttribute(el, "data-describedby", __privateGet(this, _id2));
  }, detachTrigger_fn = function(el) {
    el.removeAttribute("data-describedby");
    el.removeAttribute("aria-describedby");
    __privateGet(this, _trigger).set(null);
  }, attachContent_fn = function(el) {
    el.setAttribute("id", __privateGet(this, _id2));
    el.style.display = "none";
    setAttributeIfEmpty(el, "role", "tooltip");
    __privateGet(this, _content).set(el);
  }, detachContent_fn = function(el) {
    el.removeAttribute("id");
    el.removeAttribute("role");
    __privateGet(this, _content).set(null);
  }, onShowingChange_fn = function(isShowing) {
    const trigger = __privateGet(this, _trigger).call(this), content = __privateGet(this, _content).call(this);
    if (trigger) setAttribute(trigger, "aria-describedby", isShowing ? __privateGet(this, _id2) : null);
    for (const el of [
      this.el,
      trigger,
      content
    ]) el && setAttribute(el, "data-visible", isShowing);
    __privateGet(this, _showing).set(isShowing);
  }, __publicField(_a63, "props", { showDelay: 700 }), _a63);
  var _TooltipTrigger_instances, attach_fn, getButton_fn, _a64;
  var TooltipTrigger = (_a64 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _TooltipTrigger_instances);
      new FocusVisibleController();
    }
    onConnect(el) {
      onDispose(requestScopedAnimationFrame(() => {
        if (!this.connectScope) return;
        __privateMethod(this, _TooltipTrigger_instances, attach_fn).call(this);
        const tooltip = useContext2(tooltipContext);
        onDispose(() => {
          const button = __privateMethod(this, _TooltipTrigger_instances, getButton_fn).call(this);
          button && tooltip.detachTrigger(button);
        });
      }));
    }
  }, _TooltipTrigger_instances = new WeakSet(), attach_fn = function() {
    const button = __privateMethod(this, _TooltipTrigger_instances, getButton_fn).call(this), tooltip = useContext2(tooltipContext);
    button && tooltip.attachTrigger(button);
  }, getButton_fn = function() {
    const candidate = this.el.firstElementChild;
    return candidate?.localName === "button" || candidate?.getAttribute("role") === "button" ? candidate : this.el;
  }, _a64);
  var _a65, _TooltipContent_instances, attach_fn2, watchPlacement_fn, getTrigger_fn;
  var TooltipContent = (_a65 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _TooltipContent_instances);
      new FocusVisibleController();
      const { placement } = this.$props;
      this.setAttributes({ "data-placement": placement });
    }
    onAttach(el) {
      __privateMethod(this, _TooltipContent_instances, attach_fn2).call(this, el);
      Object.assign(el.style, {
        position: "absolute",
        top: 0,
        left: 0,
        width: "max-content"
      });
    }
    onConnect(el) {
      __privateMethod(this, _TooltipContent_instances, attach_fn2).call(this, el);
      const tooltip = useContext2(tooltipContext);
      onDispose(() => tooltip.detachContent(el));
      onDispose(requestScopedAnimationFrame(() => {
        if (!this.connectScope) return;
        effect(__privateMethod(this, _TooltipContent_instances, watchPlacement_fn).bind(this));
      }));
    }
  }, _TooltipContent_instances = new WeakSet(), attach_fn2 = function(el) {
    useContext2(tooltipContext).attachContent(el);
  }, watchPlacement_fn = function() {
    const { showing } = useContext2(tooltipContext);
    if (!showing()) return;
    const { placement, offset: mainOffset, alignOffset } = this.$props;
    return autoPlacement2(this.el, __privateMethod(this, _TooltipContent_instances, getTrigger_fn).call(this), placement(), {
      offsetVarName: "media-tooltip",
      xOffset: alignOffset(),
      yOffset: mainOffset()
    });
  }, getTrigger_fn = function() {
    return useContext2(tooltipContext).trigger();
  }, __publicField(_a65, "props", {
    placement: "top center",
    offset: 0,
    alignOffset: 0
  }), _a65);
  var _a66, _delegate2, _ToggleButtonController_instances, isARIAPressed_fn, onPressed_fn, onMaybePress_fn, onInteraction_fn;
  var ToggleButtonController = (_a66 = class extends ViewController {
    constructor(delegate) {
      super();
      __privateAdd(this, _ToggleButtonController_instances);
      __privateAdd(this, _delegate2);
      __privateSet(this, _delegate2, delegate);
      new FocusVisibleController();
      if (delegate.keyShortcut) new ARIAKeyShortcuts(delegate.keyShortcut);
    }
    onSetup() {
      const { disabled } = this.$props;
      this.setAttributes({
        "data-pressed": __privateGet(this, _delegate2).isPresssed,
        "aria-pressed": __privateMethod(this, _ToggleButtonController_instances, isARIAPressed_fn).bind(this),
        "aria-disabled": () => disabled() ? "true" : null
      });
    }
    onAttach(el) {
      setAttributeIfEmpty(el, "tabindex", "0");
      setAttributeIfEmpty(el, "role", "button");
      setAttributeIfEmpty(el, "type", "button");
    }
    onConnect(el) {
      const events = onPress(el, __privateMethod(this, _ToggleButtonController_instances, onMaybePress_fn).bind(this));
      for (const type of ["click", "touchstart"]) events.add(type, __privateMethod(this, _ToggleButtonController_instances, onInteraction_fn).bind(this), { passive: true });
    }
  }, _delegate2 = new WeakMap(), _ToggleButtonController_instances = new WeakSet(), isARIAPressed_fn = function() {
    return ariaBool(__privateGet(this, _delegate2).isPresssed());
  }, onPressed_fn = function(event) {
    if (isWriteSignal(__privateGet(this, _delegate2).isPresssed)) __privateGet(this, _delegate2).isPresssed.set((p) => !p);
  }, onMaybePress_fn = function(event) {
    if (this.$props.disabled() || this.el.hasAttribute("data-disabled")) {
      event.preventDefault();
      event.stopImmediatePropagation();
      return;
    }
    event.preventDefault();
    (__privateGet(this, _delegate2).onPress ?? __privateMethod(this, _ToggleButtonController_instances, onPressed_fn)).call(this, event);
  }, onInteraction_fn = function(event) {
    if (this.$props.disabled()) {
      event.preventDefault();
      event.stopImmediatePropagation();
    }
  }, __publicField(_a66, "props", { disabled: false }), _a66);
  var _a67, _pressed;
  var ToggleButton = (_a67 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _pressed, signal(false));
      new ToggleButtonController({ isPresssed: __privateGet(this, _pressed) });
    }
    /**
    * Whether the toggle is currently in a `pressed` state.
    */
    get pressed() {
      return __privateGet(this, _pressed).call(this);
    }
  }, _pressed = new WeakMap(), __publicField(_a67, "props", {
    disabled: false,
    defaultPressed: false
  }), _a67);
  var togglebutton__proto = ToggleButton.prototype;
  prop(togglebutton__proto, "pressed");
  var _a68, _media16, _AirPlayButton_instances, onPress_fn, isPressed_fn, getState_fn, getDefaultLabel_fn;
  var AirPlayButton = (_a68 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _AirPlayButton_instances);
      __privateAdd(this, _media16);
      new ToggleButtonController({
        isPresssed: __privateMethod(this, _AirPlayButton_instances, isPressed_fn).bind(this),
        onPress: __privateMethod(this, _AirPlayButton_instances, onPress_fn).bind(this)
      });
    }
    onSetup() {
      __privateSet(this, _media16, useMediaContext());
      const { canAirPlay, isAirPlayConnected } = __privateGet(this, _media16).$state;
      this.setAttributes({
        "data-active": isAirPlayConnected,
        "data-supported": canAirPlay,
        "data-state": __privateMethod(this, _AirPlayButton_instances, getState_fn).bind(this),
        "aria-hidden": $ariaBool(() => !canAirPlay())
      });
    }
    onAttach(el) {
      el.setAttribute("data-media-tooltip", "airplay");
      setARIALabel(el, __privateMethod(this, _AirPlayButton_instances, getDefaultLabel_fn).bind(this));
    }
  }, _media16 = new WeakMap(), _AirPlayButton_instances = new WeakSet(), onPress_fn = function(event) {
    __privateGet(this, _media16).remote.requestAirPlay(event);
  }, isPressed_fn = function() {
    const { remotePlaybackType, remotePlaybackState } = __privateGet(this, _media16).$state;
    return remotePlaybackType() === "airplay" && remotePlaybackState() !== "disconnected";
  }, getState_fn = function() {
    const { remotePlaybackType, remotePlaybackState } = __privateGet(this, _media16).$state;
    return remotePlaybackType() === "airplay" && remotePlaybackState();
  }, getDefaultLabel_fn = function() {
    const { remotePlaybackState } = __privateGet(this, _media16).$state;
    return `AirPlay ${remotePlaybackState()}`;
  }, __publicField(_a68, "props", ToggleButtonController.props), _a68);
  var _a69, _media17, _GoogleCastButton_instances, onPress_fn2, isPressed_fn2, getState_fn2, getDefaultLabel_fn2;
  var GoogleCastButton = (_a69 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _GoogleCastButton_instances);
      __privateAdd(this, _media17);
      new ToggleButtonController({
        isPresssed: __privateMethod(this, _GoogleCastButton_instances, isPressed_fn2).bind(this),
        onPress: __privateMethod(this, _GoogleCastButton_instances, onPress_fn2).bind(this)
      });
    }
    onSetup() {
      __privateSet(this, _media17, useMediaContext());
      const { canGoogleCast, isGoogleCastConnected } = __privateGet(this, _media17).$state;
      this.setAttributes({
        "data-active": isGoogleCastConnected,
        "data-supported": canGoogleCast,
        "data-state": __privateMethod(this, _GoogleCastButton_instances, getState_fn2).bind(this),
        "aria-hidden": $ariaBool(() => !canGoogleCast())
      });
    }
    onAttach(el) {
      el.setAttribute("data-media-tooltip", "google-cast");
      setARIALabel(el, __privateMethod(this, _GoogleCastButton_instances, getDefaultLabel_fn2).bind(this));
    }
  }, _media17 = new WeakMap(), _GoogleCastButton_instances = new WeakSet(), onPress_fn2 = function(event) {
    __privateGet(this, _media17).remote.requestGoogleCast(event);
  }, isPressed_fn2 = function() {
    const { remotePlaybackType, remotePlaybackState } = __privateGet(this, _media17).$state;
    return remotePlaybackType() === "google-cast" && remotePlaybackState() !== "disconnected";
  }, getState_fn2 = function() {
    const { remotePlaybackType, remotePlaybackState } = __privateGet(this, _media17).$state;
    return remotePlaybackType() === "google-cast" && remotePlaybackState();
  }, getDefaultLabel_fn2 = function() {
    const { remotePlaybackState } = __privateGet(this, _media17).$state;
    return `Google Cast ${remotePlaybackState()}`;
  }, __publicField(_a69, "props", ToggleButtonController.props), _a69);
  var _a70, _media18, _PlayButton_instances, onPress_fn3, isPressed_fn3;
  var PlayButton = (_a70 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _PlayButton_instances);
      __privateAdd(this, _media18);
      new ToggleButtonController({
        isPresssed: __privateMethod(this, _PlayButton_instances, isPressed_fn3).bind(this),
        keyShortcut: "togglePaused",
        onPress: __privateMethod(this, _PlayButton_instances, onPress_fn3).bind(this)
      });
    }
    onSetup() {
      __privateSet(this, _media18, useMediaContext());
      const { paused, ended } = __privateGet(this, _media18).$state;
      this.setAttributes({
        "data-paused": paused,
        "data-ended": ended
      });
    }
    onAttach(el) {
      el.setAttribute("data-media-tooltip", "play");
      setARIALabel(el, "Play");
    }
  }, _media18 = new WeakMap(), _PlayButton_instances = new WeakSet(), onPress_fn3 = function(event) {
    const remote = __privateGet(this, _media18).remote;
    __privateMethod(this, _PlayButton_instances, isPressed_fn3).call(this) ? remote.pause(event) : remote.play(event);
  }, isPressed_fn3 = function() {
    const { paused } = __privateGet(this, _media18).$state;
    return !paused();
  }, __publicField(_a70, "props", ToggleButtonController.props), _a70);
  var _a71, _media19, _CaptionButton_instances, onPress_fn4, isPressed_fn4, isHidden_fn;
  var CaptionButton = (_a71 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _CaptionButton_instances);
      __privateAdd(this, _media19);
      new ToggleButtonController({
        isPresssed: __privateMethod(this, _CaptionButton_instances, isPressed_fn4).bind(this),
        keyShortcut: "toggleCaptions",
        onPress: __privateMethod(this, _CaptionButton_instances, onPress_fn4).bind(this)
      });
    }
    onSetup() {
      __privateSet(this, _media19, useMediaContext());
      this.setAttributes({
        "data-active": __privateMethod(this, _CaptionButton_instances, isPressed_fn4).bind(this),
        "data-supported": () => !__privateMethod(this, _CaptionButton_instances, isHidden_fn).call(this),
        "aria-hidden": $ariaBool(__privateMethod(this, _CaptionButton_instances, isHidden_fn).bind(this))
      });
    }
    onAttach(el) {
      el.setAttribute("data-media-tooltip", "caption");
      setARIALabel(el, "Captions");
    }
  }, _media19 = new WeakMap(), _CaptionButton_instances = new WeakSet(), onPress_fn4 = function(event) {
    __privateGet(this, _media19).remote.toggleCaptions(event);
  }, isPressed_fn4 = function() {
    const { textTrack } = __privateGet(this, _media19).$state, track = textTrack();
    return !!track && isTrackCaptionKind(track);
  }, isHidden_fn = function() {
    const { hasCaptions } = __privateGet(this, _media19).$state;
    return !hasCaptions();
  }, __publicField(_a71, "props", ToggleButtonController.props), _a71);
  var _a72, _media20, _FullscreenButton_instances, onPress_fn5, isPressed_fn5, isSupported_fn;
  var FullscreenButton = (_a72 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _FullscreenButton_instances);
      __privateAdd(this, _media20);
      new ToggleButtonController({
        isPresssed: __privateMethod(this, _FullscreenButton_instances, isPressed_fn5).bind(this),
        keyShortcut: "toggleFullscreen",
        onPress: __privateMethod(this, _FullscreenButton_instances, onPress_fn5).bind(this)
      });
    }
    onSetup() {
      __privateSet(this, _media20, useMediaContext());
      const { fullscreen } = __privateGet(this, _media20).$state, isSupported = __privateMethod(this, _FullscreenButton_instances, isSupported_fn).bind(this);
      this.setAttributes({
        "data-active": fullscreen,
        "data-supported": isSupported,
        "aria-hidden": $ariaBool(() => !isSupported())
      });
    }
    onAttach(el) {
      el.setAttribute("data-media-tooltip", "fullscreen");
      setARIALabel(el, "Fullscreen");
    }
  }, _media20 = new WeakMap(), _FullscreenButton_instances = new WeakSet(), onPress_fn5 = function(event) {
    const remote = __privateGet(this, _media20).remote, target = this.$props.target();
    __privateMethod(this, _FullscreenButton_instances, isPressed_fn5).call(this) ? remote.exitFullscreen(target, event) : remote.enterFullscreen(target, event);
  }, isPressed_fn5 = function() {
    const { fullscreen } = __privateGet(this, _media20).$state;
    return fullscreen();
  }, isSupported_fn = function() {
    const { canFullscreen: canFullscreen2 } = __privateGet(this, _media20).$state;
    return canFullscreen2();
  }, __publicField(_a72, "props", {
    ...ToggleButtonController.props,
    target: "prefer-media"
  }), _a72);
  var _a73, _media21, _MuteButton_instances, onPress_fn6, isPressed_fn6, getState_fn3;
  var MuteButton = (_a73 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _MuteButton_instances);
      __privateAdd(this, _media21);
      new ToggleButtonController({
        isPresssed: __privateMethod(this, _MuteButton_instances, isPressed_fn6).bind(this),
        keyShortcut: "toggleMuted",
        onPress: __privateMethod(this, _MuteButton_instances, onPress_fn6).bind(this)
      });
    }
    onSetup() {
      __privateSet(this, _media21, useMediaContext());
      this.setAttributes({
        "data-muted": __privateMethod(this, _MuteButton_instances, isPressed_fn6).bind(this),
        "data-state": __privateMethod(this, _MuteButton_instances, getState_fn3).bind(this)
      });
    }
    onAttach(el) {
      el.setAttribute("data-media-mute-button", "");
      el.setAttribute("data-media-tooltip", "mute");
      setARIALabel(el, "Mute");
    }
  }, _media21 = new WeakMap(), _MuteButton_instances = new WeakSet(), onPress_fn6 = function(event) {
    const remote = __privateGet(this, _media21).remote;
    __privateMethod(this, _MuteButton_instances, isPressed_fn6).call(this) ? remote.unmute(event) : remote.mute(event);
  }, isPressed_fn6 = function() {
    const { muted, volume } = __privateGet(this, _media21).$state;
    return muted() || volume() === 0;
  }, getState_fn3 = function() {
    const { muted, volume } = __privateGet(this, _media21).$state, $volume = volume();
    if (muted() || $volume === 0) return "muted";
    else if ($volume >= 0.5) return "high";
    else if ($volume < 0.5) return "low";
  }, __publicField(_a73, "props", ToggleButtonController.props), _a73);
  var _a74, _media22, _PIPButton_instances, onPress_fn7, isPressed_fn7, isSupported_fn2;
  var PIPButton = (_a74 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _PIPButton_instances);
      __privateAdd(this, _media22);
      new ToggleButtonController({
        isPresssed: __privateMethod(this, _PIPButton_instances, isPressed_fn7).bind(this),
        keyShortcut: "togglePictureInPicture",
        onPress: __privateMethod(this, _PIPButton_instances, onPress_fn7).bind(this)
      });
    }
    onSetup() {
      __privateSet(this, _media22, useMediaContext());
      const { pictureInPicture } = __privateGet(this, _media22).$state, isSupported = __privateMethod(this, _PIPButton_instances, isSupported_fn2).bind(this);
      this.setAttributes({
        "data-active": pictureInPicture,
        "data-supported": isSupported,
        "aria-hidden": $ariaBool(() => !isSupported())
      });
    }
    onAttach(el) {
      el.setAttribute("data-media-tooltip", "pip");
      setARIALabel(el, "PiP");
    }
  }, _media22 = new WeakMap(), _PIPButton_instances = new WeakSet(), onPress_fn7 = function(event) {
    const remote = __privateGet(this, _media22).remote;
    __privateMethod(this, _PIPButton_instances, isPressed_fn7).call(this) ? remote.exitPictureInPicture(event) : remote.enterPictureInPicture(event);
  }, isPressed_fn7 = function() {
    const { pictureInPicture } = __privateGet(this, _media22).$state;
    return pictureInPicture();
  }, isSupported_fn2 = function() {
    const { canPictureInPicture } = __privateGet(this, _media22).$state;
    return canPictureInPicture();
  }, __publicField(_a74, "props", ToggleButtonController.props), _a74);
  var _a75, _media23, _SeekButton_instances, isSupported_fn3, getDefaultLabel_fn3, onPress_fn8;
  var SeekButton = (_a75 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _SeekButton_instances);
      __privateAdd(this, _media23);
      new FocusVisibleController();
    }
    onSetup() {
      __privateSet(this, _media23, useMediaContext());
      const { seeking } = __privateGet(this, _media23).$state, { seconds } = this.$props, isSupported = __privateMethod(this, _SeekButton_instances, isSupported_fn3).bind(this);
      this.setAttributes({
        seconds,
        "data-seeking": seeking,
        "data-supported": isSupported,
        "aria-hidden": $ariaBool(() => !isSupported())
      });
    }
    onAttach(el) {
      setAttributeIfEmpty(el, "tabindex", "0");
      setAttributeIfEmpty(el, "role", "button");
      setAttributeIfEmpty(el, "type", "button");
      el.setAttribute("data-media-tooltip", "seek");
      setARIALabel(el, __privateMethod(this, _SeekButton_instances, getDefaultLabel_fn3).bind(this));
    }
    onConnect(el) {
      onPress(el, __privateMethod(this, _SeekButton_instances, onPress_fn8).bind(this));
    }
  }, _media23 = new WeakMap(), _SeekButton_instances = new WeakSet(), isSupported_fn3 = function() {
    const { canSeek } = __privateGet(this, _media23).$state;
    return canSeek();
  }, getDefaultLabel_fn3 = function() {
    const { seconds } = this.$props;
    return `Seek ${seconds() > 0 ? "forward" : "backward"} ${seconds()} seconds`;
  }, onPress_fn8 = function(event) {
    const { seconds, disabled } = this.$props;
    if (disabled()) return;
    const { currentTime } = __privateGet(this, _media23).$state, seekTo = currentTime() + seconds();
    __privateGet(this, _media23).remote.seek(seekTo, event);
  }, __publicField(_a75, "props", {
    disabled: false,
    seconds: 30
  }), _a75);
  var _a76, _media24, _LiveButton_instances, onPress_fn9;
  var LiveButton = (_a76 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _LiveButton_instances);
      __privateAdd(this, _media24);
      new FocusVisibleController();
    }
    onSetup() {
      __privateSet(this, _media24, useMediaContext());
      const { disabled } = this.$props, { live, liveEdge } = __privateGet(this, _media24).$state, isHidden = () => !live();
      this.setAttributes({
        "data-edge": liveEdge,
        "data-hidden": isHidden,
        "aria-disabled": $ariaBool(() => disabled() || liveEdge()),
        "aria-hidden": $ariaBool(isHidden)
      });
    }
    onAttach(el) {
      setAttributeIfEmpty(el, "tabindex", "0");
      setAttributeIfEmpty(el, "role", "button");
      setAttributeIfEmpty(el, "type", "button");
      el.setAttribute("data-media-tooltip", "live");
    }
    onConnect(el) {
      onPress(el, __privateMethod(this, _LiveButton_instances, onPress_fn9).bind(this));
    }
  }, _media24 = new WeakMap(), _LiveButton_instances = new WeakSet(), onPress_fn9 = function(event) {
    const { disabled } = this.$props, { liveEdge } = __privateGet(this, _media24).$state;
    if (disabled() || liveEdge()) return;
    __privateGet(this, _media24).remote.seekToLiveEdge(event);
  }, __publicField(_a76, "props", { disabled: false }), _a76);
  var sliderState = new State({
    min: 0,
    max: 100,
    value: 0,
    step: 1,
    pointerValue: 0,
    focused: false,
    dragging: false,
    pointing: false,
    hidden: false,
    get active() {
      return this.dragging || this.focused || this.pointing;
    },
    get fillRate() {
      return calcRate(this.min, this.max, this.value);
    },
    get fillPercent() {
      return this.fillRate * 100;
    },
    get pointerRate() {
      return calcRate(this.min, this.max, this.pointerValue);
    },
    get pointerPercent() {
      return this.pointerRate * 100;
    }
  });
  function calcRate(min2, max2, value) {
    const range = max2 - min2, offset2 = value - min2;
    return range > 0 ? offset2 / range : 0;
  }
  var _init, _observer, _IntersectionObserverController_instances, onDisconnect_fn4, _a77;
  var IntersectionObserverController = (_a77 = class extends ViewController {
    constructor(init) {
      super();
      __privateAdd(this, _IntersectionObserverController_instances);
      __privateAdd(this, _init);
      __privateAdd(this, _observer);
      __privateSet(this, _init, init);
    }
    onConnect(el) {
      __privateSet(this, _observer, new IntersectionObserver((entries) => {
        __privateGet(this, _init).callback?.(entries, __privateGet(this, _observer));
      }, __privateGet(this, _init)));
      __privateGet(this, _observer).observe(el);
      onDispose(__privateMethod(this, _IntersectionObserverController_instances, onDisconnect_fn4).bind(this));
    }
  }, _init = new WeakMap(), _observer = new WeakMap(), _IntersectionObserverController_instances = new WeakSet(), /**
  * Disconnect any active intersection observers.
  */
  onDisconnect_fn4 = function() {
    __privateGet(this, _observer)?.disconnect();
    __privateSet(this, _observer, void 0);
  }, _a77);
  var sliderContext = createContext2();
  var sliderObserverContext = createContext2();
  function getClampedValue(min2, max2, value, step) {
    return clampNumber(min2, round(value, getNumberOfDecimalPlaces(step)), max2);
  }
  function getValueFromRate(min2, max2, rate, step) {
    const boundRate = clampNumber(0, rate, 1), stepRatio = (max2 - min2) * boundRate / step;
    return min2 + step * Math.round(stepRatio);
  }
  var SliderKeyDirection = {
    Left: -1,
    ArrowLeft: -1,
    Up: 1,
    ArrowUp: 1,
    Right: 1,
    ArrowRight: 1,
    Down: -1,
    ArrowDown: -1
  };
  var _delegate3, _media25, _observer2, _SliderEventsController_instances, watchSwipeGesture_fn, _provider3, _touch, _touchStartValue, resetTouchState_fn, onTouchStart_fn, onTouchMove_fn, attachEventListeners_fn, attachPointerListeners_fn, onFocus_fn2, updateValue_fn, updatePointerValue_fn, getPointerValue_fn, onPointerEnter_fn2, onPointerMove_fn, onPointerLeave_fn2, onPointerDown_fn, onStartDragging_fn, onStopDragging_fn, _lastDownKey, _repeatedKeys, onKeyDown_fn2, onKeyUp_fn2, calcJumpValue_fn, calcNewKeyValue_fn, onDocumentPointerUp_fn, onDocumentTouchMove_fn, _onDocumentPointerMove, _a78;
  var SliderEventsController = (_a78 = class extends ViewController {
    constructor(delegate, media) {
      super();
      __privateAdd(this, _SliderEventsController_instances);
      __privateAdd(this, _delegate3);
      __privateAdd(this, _media25);
      __privateAdd(this, _observer2);
      __privateAdd(this, _provider3, null);
      __privateAdd(this, _touch, null);
      __privateAdd(this, _touchStartValue, null);
      __privateAdd(this, _lastDownKey);
      __privateAdd(this, _repeatedKeys, false);
      __privateAdd(this, _onDocumentPointerMove, functionThrottle((event) => {
        __privateMethod(this, _SliderEventsController_instances, updatePointerValue_fn).call(this, __privateMethod(this, _SliderEventsController_instances, getPointerValue_fn).call(this, event), event);
      }, 20, { leading: true }));
      __privateSet(this, _delegate3, delegate);
      __privateSet(this, _media25, media);
    }
    onSetup() {
      if (hasProvidedContext(sliderObserverContext)) __privateSet(this, _observer2, useContext2(sliderObserverContext));
    }
    onConnect(el) {
      effect(__privateMethod(this, _SliderEventsController_instances, attachEventListeners_fn).bind(this, el));
      effect(__privateMethod(this, _SliderEventsController_instances, attachPointerListeners_fn).bind(this, el));
      if (__privateGet(this, _delegate3).swipeGesture) effect(__privateMethod(this, _SliderEventsController_instances, watchSwipeGesture_fn).bind(this));
    }
  }, _delegate3 = new WeakMap(), _media25 = new WeakMap(), _observer2 = new WeakMap(), _SliderEventsController_instances = new WeakSet(), watchSwipeGesture_fn = function() {
    const { pointer } = __privateGet(this, _media25).$state;
    if (pointer() !== "coarse" || !__privateGet(this, _delegate3).swipeGesture()) {
      __privateSet(this, _provider3, null);
      __privateMethod(this, _SliderEventsController_instances, resetTouchState_fn).call(this);
      return;
    }
    __privateSet(this, _provider3, __privateGet(this, _media25).player.el?.querySelector("media-provider,[data-media-provider]"));
    if (!__privateGet(this, _provider3)) {
      __privateMethod(this, _SliderEventsController_instances, resetTouchState_fn).call(this);
      return;
    }
    new EventsController(__privateGet(this, _provider3)).add("touchstart", __privateMethod(this, _SliderEventsController_instances, onTouchStart_fn).bind(this), { passive: true }).add("touchmove", __privateMethod(this, _SliderEventsController_instances, onTouchMove_fn).bind(this), { passive: false });
  }, _provider3 = new WeakMap(), _touch = new WeakMap(), _touchStartValue = new WeakMap(), resetTouchState_fn = function() {
    __privateSet(this, _touch, null);
    __privateSet(this, _touchStartValue, null);
  }, onTouchStart_fn = function(event) {
    __privateSet(this, _touch, event.touches[0]);
  }, onTouchMove_fn = function(event) {
    if (isNull(__privateGet(this, _touch)) || isTouchPinchEvent(event)) return;
    const touch = event.touches[0], xDiff = touch.clientX - __privateGet(this, _touch).clientX, yDiff = touch.clientY - __privateGet(this, _touch).clientY, isDragging = this.$state.dragging();
    if (!isDragging && Math.abs(yDiff) > 5) return;
    if (isDragging) return;
    event.preventDefault();
    if (Math.abs(xDiff) > 20) {
      __privateSet(this, _touch, touch);
      __privateSet(this, _touchStartValue, this.$state.value());
      __privateMethod(this, _SliderEventsController_instances, onStartDragging_fn).call(this, __privateGet(this, _touchStartValue), event);
    }
  }, attachEventListeners_fn = function(el) {
    const { hidden } = this.$props;
    listenEvent(el, "focus", __privateMethod(this, _SliderEventsController_instances, onFocus_fn2).bind(this));
    if (hidden() || __privateGet(this, _delegate3).isDisabled()) return;
    new EventsController(el).add("keyup", __privateMethod(this, _SliderEventsController_instances, onKeyUp_fn2).bind(this)).add("keydown", __privateMethod(this, _SliderEventsController_instances, onKeyDown_fn2).bind(this)).add("pointerenter", __privateMethod(this, _SliderEventsController_instances, onPointerEnter_fn2).bind(this)).add("pointermove", __privateMethod(this, _SliderEventsController_instances, onPointerMove_fn).bind(this)).add("pointerleave", __privateMethod(this, _SliderEventsController_instances, onPointerLeave_fn2).bind(this)).add("pointerdown", __privateMethod(this, _SliderEventsController_instances, onPointerDown_fn).bind(this));
  }, attachPointerListeners_fn = function(el) {
    if (__privateGet(this, _delegate3).isDisabled() || !this.$state.dragging()) return;
    new EventsController(document).add("pointerup", __privateMethod(this, _SliderEventsController_instances, onDocumentPointerUp_fn).bind(this), { capture: true }).add("pointermove", __privateGet(this, _onDocumentPointerMove).bind(this)).add("touchmove", __privateMethod(this, _SliderEventsController_instances, onDocumentTouchMove_fn).bind(this), { passive: false });
  }, onFocus_fn2 = function() {
    __privateMethod(this, _SliderEventsController_instances, updatePointerValue_fn).call(this, this.$state.value());
  }, updateValue_fn = function(newValue, trigger) {
    const { value, min: min2, max: max2, dragging } = this.$state;
    const clampedValue = Math.max(min2(), Math.min(newValue, max2()));
    value.set(clampedValue);
    const event = this.createEvent("value-change", {
      detail: clampedValue,
      trigger
    });
    this.dispatch(event);
    __privateGet(this, _delegate3).onValueChange?.(event);
    if (dragging()) {
      const event2 = this.createEvent("drag-value-change", {
        detail: clampedValue,
        trigger
      });
      this.dispatch(event2);
      __privateGet(this, _delegate3).onDragValueChange?.(event2);
    }
  }, updatePointerValue_fn = function(value, trigger) {
    const { pointerValue, dragging } = this.$state;
    pointerValue.set(value);
    this.dispatch("pointer-value-change", {
      detail: value,
      trigger
    });
    if (dragging()) __privateMethod(this, _SliderEventsController_instances, updateValue_fn).call(this, value, trigger);
  }, getPointerValue_fn = function(event) {
    let thumbPositionRate, rect = this.el.getBoundingClientRect(), { min: min2, max: max2 } = this.$state;
    if (this.$props.orientation() === "vertical") {
      const { bottom: trackBottom, height: trackHeight } = rect;
      thumbPositionRate = (trackBottom - event.clientY) / trackHeight;
    } else if (__privateGet(this, _touch) && isNumber(__privateGet(this, _touchStartValue)) && __privateGet(this, _provider3)) {
      const { width } = __privateGet(this, _provider3).getBoundingClientRect(), rate = (event.clientX - __privateGet(this, _touch).clientX) / width, range = max2() - min2(), diff = range * Math.abs(rate);
      thumbPositionRate = (rate < 0 ? __privateGet(this, _touchStartValue) - diff : __privateGet(this, _touchStartValue) + diff) / range;
    } else {
      if (__privateGet(this, _touch) && isNumber(__privateGet(this, _touchStartValue))) __privateMethod(this, _SliderEventsController_instances, resetTouchState_fn).call(this);
      const { left: trackLeft, width: trackWidth } = rect;
      thumbPositionRate = (event.clientX - trackLeft) / trackWidth;
    }
    return Math.max(min2(), Math.min(max2(), __privateGet(this, _delegate3).roundValue(getValueFromRate(min2(), max2(), thumbPositionRate, __privateGet(this, _delegate3).getStep()))));
  }, onPointerEnter_fn2 = function(event) {
    this.$state.pointing.set(true);
  }, onPointerMove_fn = function(event) {
    const { dragging } = this.$state;
    if (dragging()) return;
    __privateMethod(this, _SliderEventsController_instances, updatePointerValue_fn).call(this, __privateMethod(this, _SliderEventsController_instances, getPointerValue_fn).call(this, event), event);
  }, onPointerLeave_fn2 = function(event) {
    this.$state.pointing.set(false);
  }, onPointerDown_fn = function(event) {
    if (event.button !== 0) return;
    const value = __privateMethod(this, _SliderEventsController_instances, getPointerValue_fn).call(this, event);
    __privateMethod(this, _SliderEventsController_instances, onStartDragging_fn).call(this, value, event);
    __privateMethod(this, _SliderEventsController_instances, updatePointerValue_fn).call(this, value, event);
  }, onStartDragging_fn = function(value, trigger) {
    const { dragging } = this.$state;
    if (dragging()) return;
    dragging.set(true);
    __privateGet(this, _media25).remote.pauseControls(trigger);
    const event = this.createEvent("drag-start", {
      detail: value,
      trigger
    });
    this.dispatch(event);
    __privateGet(this, _delegate3).onDragStart?.(event);
    __privateGet(this, _observer2)?.onDragStart?.();
  }, onStopDragging_fn = function(value, trigger) {
    const { dragging } = this.$state;
    if (!dragging()) return;
    dragging.set(false);
    __privateGet(this, _media25).remote.resumeControls(trigger);
    const event = this.createEvent("drag-end", {
      detail: value,
      trigger
    });
    this.dispatch(event);
    __privateGet(this, _delegate3).onDragEnd?.(event);
    __privateMethod(this, _SliderEventsController_instances, resetTouchState_fn).call(this);
    __privateGet(this, _observer2)?.onDragEnd?.();
  }, _lastDownKey = new WeakMap(), _repeatedKeys = new WeakMap(), onKeyDown_fn2 = function(event) {
    if (!Object.keys(SliderKeyDirection).includes(event.key)) return;
    const { key: key2 } = event, jumpValue = __privateMethod(this, _SliderEventsController_instances, calcJumpValue_fn).call(this, event);
    if (!isNull(jumpValue)) {
      __privateMethod(this, _SliderEventsController_instances, updatePointerValue_fn).call(this, jumpValue, event);
      __privateMethod(this, _SliderEventsController_instances, updateValue_fn).call(this, jumpValue, event);
      return;
    }
    const newValue = __privateMethod(this, _SliderEventsController_instances, calcNewKeyValue_fn).call(this, event);
    if (!__privateGet(this, _repeatedKeys)) {
      __privateSet(this, _repeatedKeys, key2 === __privateGet(this, _lastDownKey));
      if (!this.$state.dragging() && __privateGet(this, _repeatedKeys)) __privateMethod(this, _SliderEventsController_instances, onStartDragging_fn).call(this, newValue, event);
    }
    __privateMethod(this, _SliderEventsController_instances, updatePointerValue_fn).call(this, newValue, event);
    __privateSet(this, _lastDownKey, key2);
  }, onKeyUp_fn2 = function(event) {
    if (!Object.keys(SliderKeyDirection).includes(event.key) || !isNull(__privateMethod(this, _SliderEventsController_instances, calcJumpValue_fn).call(this, event))) return;
    const newValue = __privateGet(this, _repeatedKeys) ? this.$state.pointerValue() : __privateMethod(this, _SliderEventsController_instances, calcNewKeyValue_fn).call(this, event);
    __privateMethod(this, _SliderEventsController_instances, updateValue_fn).call(this, newValue, event);
    __privateMethod(this, _SliderEventsController_instances, onStopDragging_fn).call(this, newValue, event);
    __privateSet(this, _lastDownKey, "");
    __privateSet(this, _repeatedKeys, false);
  }, calcJumpValue_fn = function(event) {
    let key2 = event.key, { min: min2, max: max2 } = this.$state;
    if (key2 === "Home" || key2 === "PageUp") return min2();
    else if (key2 === "End" || key2 === "PageDown") return max2();
    else if (!event.metaKey && /^[0-9]$/.test(key2)) return (max2() - min2()) / 10 * Number(key2);
    return null;
  }, calcNewKeyValue_fn = function(event) {
    const { key: key2, shiftKey } = event;
    event.preventDefault();
    event.stopPropagation();
    const { shiftKeyMultiplier } = this.$props;
    const { min: min2, max: max2, value, pointerValue } = this.$state, step = __privateGet(this, _delegate3).getStep(), keyStep = __privateGet(this, _delegate3).getKeyStep();
    const diff = (!shiftKey ? keyStep : keyStep * shiftKeyMultiplier()) * Number(SliderKeyDirection[key2]), steps = ((__privateGet(this, _repeatedKeys) ? pointerValue() : __privateGet(this, _delegate3).getValue?.() ?? value()) + diff) / step;
    return Math.max(min2(), Math.min(max2(), Number((step * steps).toFixed(3))));
  }, onDocumentPointerUp_fn = function(event) {
    if (event.button !== 0) return;
    event.preventDefault();
    event.stopImmediatePropagation();
    const value = __privateMethod(this, _SliderEventsController_instances, getPointerValue_fn).call(this, event);
    __privateMethod(this, _SliderEventsController_instances, updatePointerValue_fn).call(this, value, event);
    __privateMethod(this, _SliderEventsController_instances, onStopDragging_fn).call(this, value, event);
  }, onDocumentTouchMove_fn = function(event) {
    event.preventDefault();
  }, _onDocumentPointerMove = new WeakMap(), _a78);
  var sliderValueFormatContext = createContext2(() => ({}));
  var _a79, _media26, _delegate4, _isVisible, _isIntersecting, _SliderController_instances, onIntersectionChange_fn, watchHidden_fn, watchValue_fn, watchStep_fn, watchDisabled_fn, getARIADisabled_fn, setupAttrs_fn, watchCSSVars_fn, _updateSliderVars;
  var SliderController = (_a79 = class extends ViewController {
    constructor(delegate) {
      super();
      __privateAdd(this, _SliderController_instances);
      __privateAdd(this, _media26);
      __privateAdd(this, _delegate4);
      __privateAdd(this, _isVisible, signal(true));
      __privateAdd(this, _isIntersecting, signal(true));
      __privateAdd(this, _updateSliderVars, animationFrameThrottle((fillPercent, pointerPercent) => {
        this.el?.style.setProperty("--slider-fill", fillPercent + "%");
        this.el?.style.setProperty("--slider-pointer", pointerPercent + "%");
      }));
      __privateSet(this, _delegate4, delegate);
    }
    onSetup() {
      __privateSet(this, _media26, useMediaContext());
      const focus = new FocusVisibleController();
      focus.attach(this);
      this.$state.focused = focus.focused.bind(focus);
      if (!hasProvidedContext(sliderValueFormatContext)) provideContext(sliderValueFormatContext, { default: "value" });
      provideContext(sliderContext, {
        orientation: this.$props.orientation,
        disabled: __privateGet(this, _delegate4).isDisabled,
        preview: signal(null)
      });
      effect(__privateMethod(this, _SliderController_instances, watchValue_fn).bind(this));
      effect(__privateMethod(this, _SliderController_instances, watchStep_fn).bind(this));
      effect(__privateMethod(this, _SliderController_instances, watchDisabled_fn).bind(this));
      __privateMethod(this, _SliderController_instances, setupAttrs_fn).call(this);
      new SliderEventsController(__privateGet(this, _delegate4), __privateGet(this, _media26)).attach(this);
      new IntersectionObserverController({ callback: __privateMethod(this, _SliderController_instances, onIntersectionChange_fn).bind(this) }).attach(this);
    }
    onAttach(el) {
      setAttributeIfEmpty(el, "role", "slider");
      setAttributeIfEmpty(el, "tabindex", "0");
      setAttributeIfEmpty(el, "autocomplete", "off");
      if (IS_SERVER2) __privateMethod(this, _SliderController_instances, watchCSSVars_fn).call(this);
      else effect(__privateMethod(this, _SliderController_instances, watchCSSVars_fn).bind(this));
    }
    onConnect(el) {
      onDispose(observeVisibility(el, __privateGet(this, _isVisible).set));
      effect(__privateMethod(this, _SliderController_instances, watchHidden_fn).bind(this));
    }
  }, _media26 = new WeakMap(), _delegate4 = new WeakMap(), _isVisible = new WeakMap(), _isIntersecting = new WeakMap(), _SliderController_instances = new WeakSet(), onIntersectionChange_fn = function(entries) {
    __privateGet(this, _isIntersecting).set(entries[0].isIntersecting);
  }, watchHidden_fn = function() {
    const { hidden } = this.$props;
    this.$state.hidden.set(hidden() || !__privateGet(this, _isVisible).call(this) || !__privateGet(this, _isIntersecting).call(this));
  }, watchValue_fn = function() {
    const { dragging, value, min: min2, max: max2 } = this.$state;
    if (peek(dragging)) return;
    value.set(getClampedValue(min2(), max2(), value(), __privateGet(this, _delegate4).getStep()));
  }, watchStep_fn = function() {
    this.$state.step.set(__privateGet(this, _delegate4).getStep());
  }, watchDisabled_fn = function() {
    if (!__privateGet(this, _delegate4).isDisabled()) return;
    const { dragging, pointing } = this.$state;
    dragging.set(false);
    pointing.set(false);
  }, getARIADisabled_fn = function() {
    return ariaBool(__privateGet(this, _delegate4).isDisabled());
  }, setupAttrs_fn = function() {
    const { orientation } = this.$props, { dragging, active, pointing } = this.$state;
    this.setAttributes({
      "data-dragging": dragging,
      "data-pointing": pointing,
      "data-active": active,
      "aria-disabled": __privateMethod(this, _SliderController_instances, getARIADisabled_fn).bind(this),
      "aria-valuemin": __privateGet(this, _delegate4).aria.valueMin ?? this.$state.min,
      "aria-valuemax": __privateGet(this, _delegate4).aria.valueMax ?? this.$state.max,
      "aria-valuenow": __privateGet(this, _delegate4).aria.valueNow,
      "aria-valuetext": __privateGet(this, _delegate4).aria.valueText,
      "aria-orientation": orientation
    });
  }, watchCSSVars_fn = function() {
    const { fillPercent, pointerPercent } = this.$state;
    __privateGet(this, _updateSliderVars).call(this, round(fillPercent(), 3), round(pointerPercent(), 3));
  }, _updateSliderVars = new WeakMap(), __publicField(_a79, "props", {
    hidden: false,
    disabled: false,
    step: 1,
    keyStep: 1,
    orientation: "horizontal",
    shiftKeyMultiplier: 5
  }), _a79);
  var _a80, _Slider_instances, getARIAValueNow_fn, getARIAValueText_fn, watchValue_fn2, watchMinMax_fn;
  var Slider = (_a80 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _Slider_instances);
      new SliderController({
        getStep: this.$props.step,
        getKeyStep: this.$props.keyStep,
        roundValue: Math.round,
        isDisabled: this.$props.disabled,
        aria: {
          valueNow: __privateMethod(this, _Slider_instances, getARIAValueNow_fn).bind(this),
          valueText: __privateMethod(this, _Slider_instances, getARIAValueText_fn).bind(this)
        }
      });
    }
    onSetup() {
      effect(__privateMethod(this, _Slider_instances, watchValue_fn2).bind(this));
      effect(__privateMethod(this, _Slider_instances, watchMinMax_fn).bind(this));
    }
  }, _Slider_instances = new WeakSet(), getARIAValueNow_fn = function() {
    const { value } = this.$state;
    return Math.round(value());
  }, getARIAValueText_fn = function() {
    const { value, max: max2 } = this.$state;
    return round(value() / max2() * 100, 2) + "%";
  }, watchValue_fn2 = function() {
    const { value } = this.$props;
    this.$state.value.set(value());
  }, watchMinMax_fn = function() {
    const { min: min2, max: max2 } = this.$props;
    this.$state.min.set(min2());
    this.$state.max.set(max2());
  }, __publicField(_a80, "props", {
    ...SliderController.props,
    min: 0,
    max: 100,
    value: 0
  }), __publicField(_a80, "state", sliderState), _a80);
  var cache = /* @__PURE__ */ new Map();
  var pending = /* @__PURE__ */ new Map();
  var _media27, _src, _crossOrigin, _ThumbnailsLoader_instances, onLoadCues_fn, processImages_fn, processStoryboard_fn, processVTTCues_fn, resolveBaseUrl_fn, resolveURL_fn, resolveData_fn, onError_fn6, _a81;
  var ThumbnailsLoader = (_a81 = class {
    constructor(src, crossOrigin, media) {
      __privateAdd(this, _ThumbnailsLoader_instances);
      __privateAdd(this, _media27);
      __privateAdd(this, _src);
      __privateAdd(this, _crossOrigin);
      __publicField(this, "$images", signal([]));
      __privateSet(this, _src, src);
      __privateSet(this, _crossOrigin, crossOrigin);
      __privateSet(this, _media27, media);
      effect(__privateMethod(this, _ThumbnailsLoader_instances, onLoadCues_fn).bind(this));
    }
    static create(src, crossOrigin) {
      return new _a81(src, crossOrigin, useMediaContext());
    }
  }, _media27 = new WeakMap(), _src = new WeakMap(), _crossOrigin = new WeakMap(), _ThumbnailsLoader_instances = new WeakSet(), onLoadCues_fn = function() {
    const { canLoad } = __privateGet(this, _media27).$state;
    if (!canLoad()) return;
    const src = __privateGet(this, _src).call(this);
    if (!src) return;
    if (isString(src) && cache.has(src)) {
      const cues = cache.get(src);
      cache.delete(src);
      cache.set(src, cues);
      if (cache.size > 99) {
        const firstKey = cache.keys().next().value;
        cache.delete(firstKey);
      }
      this.$images.set(cache.get(src));
    } else if (isString(src)) {
      const crossOrigin = __privateGet(this, _crossOrigin).call(this), currentKey = src + "::" + crossOrigin;
      if (!pending.has(currentKey)) {
        const promise = new Promise(async (resolve, reject) => {
          try {
            const response = await fetch(src, { credentials: getRequestCredentials(crossOrigin) });
            if (response.headers.get("content-type") === "application/json") {
              const json = await response.json();
              if (isArray(json)) if (json[0] && "text" in json[0]) resolve(__privateMethod(this, _ThumbnailsLoader_instances, processVTTCues_fn).call(this, json));
              else {
                for (let i = 0; i < json.length; i++) {
                  const image = json[i];
                  assert(isObject(image), false);
                  assert("url" in image && isString(image.url), false);
                  assert("startTime" in image && isNumber(image.startTime), false);
                }
                resolve(json);
              }
              else resolve(__privateMethod(this, _ThumbnailsLoader_instances, processStoryboard_fn).call(this, json));
              return;
            }
            Promise.resolve().then(() => (init_prod2(), prod_exports)).then(async ({ parseResponse: parseResponse2 }) => {
              try {
                const { cues } = await parseResponse2(response);
                resolve(__privateMethod(this, _ThumbnailsLoader_instances, processVTTCues_fn).call(this, cues));
              } catch (e2) {
                reject(e2);
              }
            });
          } catch (e2) {
            reject(e2);
          }
        }).then((images) => {
          cache.set(currentKey, images);
          return images;
        }).catch((error) => {
          __privateMethod(this, _ThumbnailsLoader_instances, onError_fn6).call(this, src, error);
        }).finally(() => {
          if (isString(currentKey)) pending.delete(currentKey);
        });
        pending.set(currentKey, promise);
      }
      pending.get(currentKey)?.then((images) => {
        this.$images.set(images || []);
      });
    } else if (isArray(src)) try {
      this.$images.set(__privateMethod(this, _ThumbnailsLoader_instances, processImages_fn).call(this, src));
    } catch (error) {
      __privateMethod(this, _ThumbnailsLoader_instances, onError_fn6).call(this, src, error);
    }
    else try {
      this.$images.set(__privateMethod(this, _ThumbnailsLoader_instances, processStoryboard_fn).call(this, src));
    } catch (error) {
      __privateMethod(this, _ThumbnailsLoader_instances, onError_fn6).call(this, src, error);
    }
    return () => {
      this.$images.set([]);
    };
  }, processImages_fn = function(images) {
    const baseURL = __privateMethod(this, _ThumbnailsLoader_instances, resolveBaseUrl_fn).call(this);
    return images.map((img, i) => {
      assert(img.url && isString(img.url), false);
      assert("startTime" in img && isNumber(img.startTime), false);
      return {
        ...img,
        url: isString(img.url) ? __privateMethod(this, _ThumbnailsLoader_instances, resolveURL_fn).call(this, img.url, baseURL) : img.url
      };
    });
  }, processStoryboard_fn = function(board) {
    assert(isString(board.url), false);
    assert(isArray(board.tiles) && board.tiles?.length, false);
    const url = new URL(board.url), images = [];
    const tileWidth = "tile_width" in board ? board.tile_width : board.tileWidth, tileHeight = "tile_height" in board ? board.tile_height : board.tileHeight;
    for (const tile2 of board.tiles) images.push({
      url,
      startTime: "start" in tile2 ? tile2.start : tile2.startTime,
      width: tileWidth,
      height: tileHeight,
      coords: {
        x: tile2.x,
        y: tile2.y
      }
    });
    return images;
  }, processVTTCues_fn = function(cues) {
    for (let i = 0; i < cues.length; i++) {
      const cue = cues[i];
      assert("startTime" in cue && isNumber(cue.startTime), false);
      assert("text" in cue && isString(cue.text), false);
    }
    const images = [], baseURL = __privateMethod(this, _ThumbnailsLoader_instances, resolveBaseUrl_fn).call(this);
    for (const cue of cues) {
      const [url, hash] = cue.text.split("#"), data = __privateMethod(this, _ThumbnailsLoader_instances, resolveData_fn).call(this, hash);
      images.push({
        url: __privateMethod(this, _ThumbnailsLoader_instances, resolveURL_fn).call(this, url, baseURL),
        startTime: cue.startTime,
        endTime: cue.endTime,
        width: data?.w,
        height: data?.h,
        coords: data && isNumber(data.x) && isNumber(data.y) ? {
          x: data.x,
          y: data.y
        } : void 0
      });
    }
    return images;
  }, resolveBaseUrl_fn = function() {
    let baseURL = peek(__privateGet(this, _src));
    if (!isString(baseURL) || !/^https?:/.test(baseURL)) return location.href;
    return baseURL;
  }, resolveURL_fn = function(src, baseURL) {
    return /^https?:/.test(src) ? new URL(src) : new URL(src, baseURL);
  }, resolveData_fn = function(hash) {
    if (!hash) return {};
    const [hashProps, values] = hash.split("="), hashValues = values?.split(","), data = {};
    if (!hashProps || !hashValues) return null;
    for (let i = 0; i < hashProps.length; i++) {
      const value = +hashValues[i];
      if (!isNaN(value)) data[hashProps[i]] = value;
    }
    return data;
  }, onError_fn6 = function(src, error) {
  }, _a81);
  var PRELOAD_SPRITE_BATCH_SIZE = 2;
  var MAX_PRELOADED_SPRITES = 32;
  var preloadedSprites = /* @__PURE__ */ new Map();
  var _a82, _loader3, _styleResets, _Thumbnail_instances, watchImg_fn, watchCrossOrigin_fn2, onLoadStart_fn4, onLoaded_fn4, onError_fn7, isLoading_fn, hasError_fn, watchHidden_fn2, onFindActiveThumbnail_fn, onPreloadNextSprites_fn, resize_fn, style_fn, resetStyles_fn;
  var Thumbnail = (_a82 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _Thumbnail_instances);
      __publicField(this, "media");
      __privateAdd(this, _loader3);
      __privateAdd(this, _styleResets, []);
    }
    onSetup() {
      this.media = useMediaContext();
      __privateSet(this, _loader3, ThumbnailsLoader.create(this.$props.src, this.$state.crossOrigin));
      __privateMethod(this, _Thumbnail_instances, watchCrossOrigin_fn2).call(this);
      this.setAttributes({
        "data-loading": __privateMethod(this, _Thumbnail_instances, isLoading_fn).bind(this),
        "data-error": __privateMethod(this, _Thumbnail_instances, hasError_fn).bind(this),
        "data-hidden": this.$state.hidden,
        "aria-hidden": $ariaBool(this.$state.hidden)
      });
    }
    onConnect(el) {
      effect(__privateMethod(this, _Thumbnail_instances, watchImg_fn).bind(this));
      effect(__privateMethod(this, _Thumbnail_instances, watchHidden_fn2).bind(this));
      effect(__privateMethod(this, _Thumbnail_instances, watchCrossOrigin_fn2).bind(this));
      effect(__privateMethod(this, _Thumbnail_instances, onLoadStart_fn4).bind(this));
      effect(__privateMethod(this, _Thumbnail_instances, onFindActiveThumbnail_fn).bind(this));
      effect(__privateMethod(this, _Thumbnail_instances, onPreloadNextSprites_fn).bind(this));
      effect(__privateMethod(this, _Thumbnail_instances, resize_fn).bind(this));
    }
    getTime() {
      return this.$props.time();
    }
  }, _loader3 = new WeakMap(), _styleResets = new WeakMap(), _Thumbnail_instances = new WeakSet(), watchImg_fn = function() {
    const img = this.$state.img();
    if (!img) return;
    new EventsController(img).add("load", __privateMethod(this, _Thumbnail_instances, onLoaded_fn4).bind(this)).add("error", __privateMethod(this, _Thumbnail_instances, onError_fn7).bind(this));
  }, watchCrossOrigin_fn2 = function() {
    const { crossOrigin: crossOriginProp } = this.$props, { crossOrigin: crossOriginState } = this.$state, { crossOrigin: mediaCrossOrigin } = this.media.$state, crossOrigin = crossOriginProp() !== null ? crossOriginProp() : mediaCrossOrigin();
    crossOriginState.set(crossOrigin === true ? "anonymous" : crossOrigin);
  }, onLoadStart_fn4 = function() {
    const { src, loading, error } = this.$state;
    if (src()) {
      loading.set(true);
      error.set(null);
    }
    return () => {
      __privateMethod(this, _Thumbnail_instances, resetStyles_fn).call(this);
      loading.set(false);
      error.set(null);
    };
  }, onLoaded_fn4 = function() {
    const { loading, error } = this.$state;
    __privateMethod(this, _Thumbnail_instances, resize_fn).call(this);
    loading.set(false);
    error.set(null);
  }, onError_fn7 = function(event) {
    const { loading, error } = this.$state;
    loading.set(false);
    error.set(event);
  }, isLoading_fn = function() {
    const { loading, hidden } = this.$state;
    return !hidden() && loading();
  }, hasError_fn = function() {
    const { error } = this.$state;
    return !isNull(error());
  }, watchHidden_fn2 = function() {
    const { hidden } = this.$state, { duration } = this.media.$state, images = __privateGet(this, _loader3).$images();
    hidden.set(__privateMethod(this, _Thumbnail_instances, hasError_fn).call(this) || !Number.isFinite(duration()) || images.length === 0);
  }, onFindActiveThumbnail_fn = function() {
    let images = __privateGet(this, _loader3).$images();
    if (!images.length) return;
    let time = this.getTime(), { src, activeThumbnail } = this.$state, activeIndex = -1, activeImage = null;
    for (let i = images.length - 1; i >= 0; i--) {
      const image = images[i];
      if (time >= image.startTime && (!image.endTime || time < image.endTime)) {
        activeIndex = i;
        break;
      }
    }
    if (images[activeIndex]) activeImage = images[activeIndex];
    src.set(activeImage?.url.href || "");
    activeThumbnail.set(activeImage);
  }, onPreloadNextSprites_fn = function() {
    if (IS_SERVER2) return;
    const images = __privateGet(this, _loader3).$images(), activeThumbnail = this.$state.activeThumbnail();
    if (!activeThumbnail) return;
    const activeIndex = images.indexOf(activeThumbnail);
    if (activeIndex < 0) return;
    const activeSrc = activeThumbnail.url.href, crossOrigin = this.$state.crossOrigin(), startIndex = activeIndex + 1, endIndex = Math.min(startIndex + PRELOAD_SPRITE_BATCH_SIZE, images.length);
    for (let i = startIndex; i < endIndex; i++) {
      const src = images[i].url.href;
      if (src !== activeSrc) preloadSprite(src, crossOrigin);
    }
  }, resize_fn = function() {
    if (!this.scope || this.$state.hidden()) return;
    const rootEl = this.el, imgEl = this.$state.img(), thumbnail = this.$state.activeThumbnail();
    if (!imgEl || !thumbnail || !rootEl) return;
    let width = thumbnail.width ?? imgEl.naturalWidth, height = thumbnail?.height ?? imgEl.naturalHeight, { maxWidth, maxHeight, minWidth, minHeight, width: elWidth, height: elHeight } = getComputedStyle(this.el);
    if (minWidth === "100%") minWidth = parseFloat(elWidth) + "";
    if (minHeight === "100%") minHeight = parseFloat(elHeight) + "";
    let minRatio = Math.max(parseInt(minWidth) / width, parseInt(minHeight) / height), maxRatio = Math.min(Math.max(parseInt(minWidth), parseInt(maxWidth)) / width, Math.max(parseInt(minHeight), parseInt(maxHeight)) / height), scale = !isNaN(maxRatio) && maxRatio < 1 ? maxRatio : minRatio > 1 ? minRatio : 1;
    __privateMethod(this, _Thumbnail_instances, style_fn).call(this, rootEl, "--thumbnail-width", `${width * scale}px`);
    __privateMethod(this, _Thumbnail_instances, style_fn).call(this, rootEl, "--thumbnail-height", `${height * scale}px`);
    __privateMethod(this, _Thumbnail_instances, style_fn).call(this, rootEl, "--thumbnail-aspect-ratio", String(round(width / height, 5)));
    __privateMethod(this, _Thumbnail_instances, style_fn).call(this, imgEl, "width", `${imgEl.naturalWidth * scale}px`);
    __privateMethod(this, _Thumbnail_instances, style_fn).call(this, imgEl, "height", `${imgEl.naturalHeight * scale}px`);
    __privateMethod(this, _Thumbnail_instances, style_fn).call(this, imgEl, "transform", thumbnail.coords ? `translate(-${thumbnail.coords.x * scale}px, -${thumbnail.coords.y * scale}px)` : "");
    __privateMethod(this, _Thumbnail_instances, style_fn).call(this, imgEl, "max-width", "none");
  }, style_fn = function(el, name, value) {
    el.style.setProperty(name, value);
    __privateGet(this, _styleResets).push(() => el.style.removeProperty(name));
  }, resetStyles_fn = function() {
    for (const reset of __privateGet(this, _styleResets)) reset();
    __privateSet(this, _styleResets, []);
  }, __publicField(_a82, "props", {
    src: null,
    time: 0,
    crossOrigin: null
  }), __publicField(_a82, "state", new State({
    src: "",
    img: null,
    thumbnails: [],
    activeThumbnail: null,
    crossOrigin: null,
    loading: false,
    error: null,
    hidden: false
  })), _a82);
  function preloadSprite(src, crossOrigin) {
    const key2 = `${src}::${crossOrigin}`;
    if (preloadedSprites.has(key2)) {
      const image2 = preloadedSprites.get(key2);
      preloadedSprites.delete(key2);
      preloadedSprites.set(key2, image2);
      return;
    }
    const image = new Image();
    image.decoding = "async";
    image.crossOrigin = crossOrigin;
    image.src = src;
    preloadedSprites.set(key2, image);
    if (preloadedSprites.size > MAX_PRELOADED_SPRITES) {
      const firstKey = preloadedSprites.keys().next().value;
      preloadedSprites.delete(firstKey);
    }
  }
  var _slider, _a83;
  var SliderThumbnail = (_a83 = class extends Thumbnail {
    constructor() {
      super(...arguments);
      __privateAdd(this, _slider);
    }
    onAttach(el) {
      __privateSet(this, _slider, useState18(Slider.state));
    }
    getTime() {
      const { duration, clipStartTime } = this.media.$state;
      return clipStartTime() + __privateGet(this, _slider).pointerRate() * duration();
    }
  }, _slider = new WeakMap(), _a83);
  var _a84, _media28, _slider2, _SliderVideo_instances, watchVideo_fn, watchSrc_fn2, watchCrossOrigin_fn3, isLoading_fn2, hasError_fn2, watchHidden_fn3, onSrcChange_fn, onCanPlay_fn2, onError_fn8, onUpdateTime_fn;
  var SliderVideo = (_a84 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _SliderVideo_instances);
      __privateAdd(this, _media28);
      __privateAdd(this, _slider2);
    }
    get video() {
      return this.$state.video();
    }
    onSetup() {
      __privateSet(this, _media28, useMediaContext());
      __privateSet(this, _slider2, useState18(Slider.state));
      __privateMethod(this, _SliderVideo_instances, watchCrossOrigin_fn3).call(this);
      this.setAttributes({
        "data-loading": __privateMethod(this, _SliderVideo_instances, isLoading_fn2).bind(this),
        "data-hidden": this.$state.hidden,
        "data-error": __privateMethod(this, _SliderVideo_instances, hasError_fn2).bind(this),
        "aria-hidden": $ariaBool(this.$state.hidden)
      });
    }
    onAttach(el) {
      effect(__privateMethod(this, _SliderVideo_instances, watchVideo_fn).bind(this));
      effect(__privateMethod(this, _SliderVideo_instances, watchSrc_fn2).bind(this));
      effect(__privateMethod(this, _SliderVideo_instances, watchCrossOrigin_fn3).bind(this));
      effect(__privateMethod(this, _SliderVideo_instances, watchHidden_fn3).bind(this));
      effect(__privateMethod(this, _SliderVideo_instances, onSrcChange_fn).bind(this));
      effect(__privateMethod(this, _SliderVideo_instances, onUpdateTime_fn).bind(this));
    }
  }, _media28 = new WeakMap(), _slider2 = new WeakMap(), _SliderVideo_instances = new WeakSet(), watchVideo_fn = function() {
    const video = this.$state.video();
    if (!video) return;
    if (video.readyState >= 2) __privateMethod(this, _SliderVideo_instances, onCanPlay_fn2).call(this);
    new EventsController(video).add("canplay", __privateMethod(this, _SliderVideo_instances, onCanPlay_fn2).bind(this)).add("error", __privateMethod(this, _SliderVideo_instances, onError_fn8).bind(this));
  }, watchSrc_fn2 = function() {
    const { src } = this.$state, { canLoad } = __privateGet(this, _media28).$state;
    src.set(canLoad() ? this.$props.src() : null);
  }, watchCrossOrigin_fn3 = function() {
    const { crossOrigin: crossOriginProp } = this.$props, { crossOrigin: crossOriginState } = this.$state, { crossOrigin: mediaCrossOrigin } = __privateGet(this, _media28).$state, crossOrigin = crossOriginProp() !== null ? crossOriginProp() : mediaCrossOrigin();
    crossOriginState.set(crossOrigin === true ? "anonymous" : crossOrigin);
  }, isLoading_fn2 = function() {
    const { canPlay, hidden } = this.$state;
    return !canPlay() && !hidden();
  }, hasError_fn2 = function() {
    const { error } = this.$state;
    return !isNull(error);
  }, watchHidden_fn3 = function() {
    const { src, hidden } = this.$state, { canLoad, duration } = __privateGet(this, _media28).$state;
    hidden.set(canLoad() && (!src() || __privateMethod(this, _SliderVideo_instances, hasError_fn2).call(this) || !Number.isFinite(duration())));
  }, onSrcChange_fn = function() {
    const { src, canPlay, error } = this.$state;
    src();
    canPlay.set(false);
    error.set(null);
  }, onCanPlay_fn2 = function(event) {
    const { canPlay, error } = this.$state;
    canPlay.set(true);
    error.set(null);
    this.dispatch("can-play", { trigger: event });
  }, onError_fn8 = function(event) {
    const { canPlay, error } = this.$state;
    canPlay.set(false);
    error.set(event);
    this.dispatch("error", { trigger: event });
  }, onUpdateTime_fn = function() {
    const { video, canPlay } = this.$state, { duration } = __privateGet(this, _media28).$state, { pointerRate } = __privateGet(this, _slider2), media = video();
    if (canPlay() && media && Number.isFinite(duration()) && Number.isFinite(pointerRate())) media.currentTime = pointerRate() * duration();
  }, __publicField(_a84, "props", {
    src: null,
    crossOrigin: null
  }), __publicField(_a84, "state", new State({
    video: null,
    src: null,
    crossOrigin: null,
    canPlay: false,
    error: null,
    hidden: false
  })), _a84);
  var slidervideo__proto = SliderVideo.prototype;
  prop(slidervideo__proto, "video");
  var _a85, _format, _text, _slider3;
  var SliderValue = (_a85 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _format);
      __privateAdd(this, _text);
      __privateAdd(this, _slider3);
    }
    onSetup() {
      __privateSet(this, _slider3, useState18(Slider.state));
      __privateSet(this, _format, useContext2(sliderValueFormatContext));
      __privateSet(this, _text, computed(this.getValueText.bind(this)));
    }
    /**
    * Returns the current value formatted as text based on prop settings.
    */
    getValueText() {
      const { type, format: $format, decimalPlaces, padHours, padMinutes, showHours, showMs } = this.$props, { value: sliderValue, pointerValue, min: min2, max: max2 } = __privateGet(this, _slider3), format = $format?.() ?? __privateGet(this, _format).default;
      const value = type() === "current" ? sliderValue() : pointerValue();
      if (format === "percent") {
        const percent = value / (max2() - min2()) * 100;
        return (__privateGet(this, _format).percent ?? round)(percent, decimalPlaces()) + "%";
      } else if (format === "time") return (__privateGet(this, _format).time ?? formatTime2)(value, {
        padHrs: padHours(),
        padMins: padMinutes(),
        showHrs: showHours(),
        showMs: showMs()
      });
      else return (__privateGet(this, _format).value?.(value) ?? value.toFixed(2)) + "";
    }
  }, _format = new WeakMap(), _text = new WeakMap(), _slider3 = new WeakMap(), __publicField(_a85, "props", {
    type: "pointer",
    format: null,
    showHours: false,
    showMs: false,
    padHours: null,
    padMinutes: null,
    decimalPlaces: 2
  }), _a85);
  var slidervalue__proto = SliderValue.prototype;
  method(slidervalue__proto, "getValueText");
  var _a86, _slider4, _updatePlacement;
  var SliderPreview = (_a86 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _slider4);
      __privateAdd(this, _updatePlacement, animationFrameThrottle(() => {
        const { disabled, orientation } = __privateGet(this, _slider4);
        if (disabled()) return;
        const el = this.el, { offset: offset2, noClamp } = this.$props;
        if (!el) return;
        updateSliderPreviewPlacement(el, {
          clamp: !noClamp(),
          offset: offset2(),
          orientation: orientation()
        });
      }));
    }
    onSetup() {
      __privateSet(this, _slider4, useContext2(sliderContext));
      const { active } = useState18(Slider.state);
      this.setAttributes({ "data-visible": active });
    }
    onAttach(el) {
      Object.assign(el.style, {
        position: "absolute",
        top: 0,
        left: 0,
        width: "max-content"
      });
    }
    onConnect(el) {
      const { preview } = __privateGet(this, _slider4);
      preview.set(el);
      onDispose(() => preview.set(null));
      effect(__privateGet(this, _updatePlacement).bind(this));
      const resize = new ResizeObserver(__privateGet(this, _updatePlacement).bind(this));
      resize.observe(el);
      onDispose(() => resize.disconnect());
    }
  }, _slider4 = new WeakMap(), _updatePlacement = new WeakMap(), __publicField(_a86, "props", {
    offset: 0,
    noClamp: false
  }), _a86);
  function updateSliderPreviewPlacement(el, { clamp: clamp2, offset: offset2, orientation }) {
    const computedStyle = getComputedStyle(el), width = parseFloat(computedStyle.width), height = parseFloat(computedStyle.height), styles = {
      top: null,
      right: null,
      bottom: null,
      left: null
    };
    styles[orientation === "horizontal" ? "bottom" : "left"] = `calc(100% + var(--media-slider-preview-offset, ${offset2}px))`;
    if (orientation === "horizontal") {
      const widthHalf = width / 2;
      if (!clamp2) styles.left = `calc(var(--slider-pointer) - ${widthHalf}px)`;
      else styles.left = `min(${`max(0px, calc(var(--slider-pointer) - ${widthHalf}px))`}, ${`calc(100% - ${width}px)`})`;
    } else {
      const heightHalf = height / 2;
      if (!clamp2) styles.bottom = `calc(var(--slider-pointer) - ${heightHalf}px)`;
      else styles.bottom = `min(${`max(${heightHalf}px, calc(var(--slider-pointer) - ${heightHalf}px))`}, ${`calc(100% - ${height}px)`})`;
    }
    Object.assign(el.style, styles);
  }
  var _a87, _media29, _VolumeSlider_instances, getARIAValueNow_fn2, getARIAValueText_fn2, getARIAValueMax_fn, isDisabled_fn, watchVolume_fn3, _throttleVolumeChange, onVolumeChange_fn4, onValueChange_fn, onDragValueChange_fn;
  var VolumeSlider = (_a87 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _VolumeSlider_instances);
      __privateAdd(this, _media29);
      __privateAdd(this, _throttleVolumeChange, functionThrottle(__privateMethod(this, _VolumeSlider_instances, onVolumeChange_fn4).bind(this), 25));
    }
    onSetup() {
      __privateSet(this, _media29, useMediaContext());
      const { audioGain } = __privateGet(this, _media29).$state;
      provideContext(sliderValueFormatContext, {
        default: "percent",
        value(value) {
          return (value * (audioGain() ?? 1)).toFixed(2);
        },
        percent(value) {
          return Math.round(value * (audioGain() ?? 1));
        }
      });
      new SliderController({
        getStep: this.$props.step,
        getKeyStep: this.$props.keyStep,
        roundValue: Math.round,
        isDisabled: __privateMethod(this, _VolumeSlider_instances, isDisabled_fn).bind(this),
        aria: {
          valueMax: __privateMethod(this, _VolumeSlider_instances, getARIAValueMax_fn).bind(this),
          valueNow: __privateMethod(this, _VolumeSlider_instances, getARIAValueNow_fn2).bind(this),
          valueText: __privateMethod(this, _VolumeSlider_instances, getARIAValueText_fn2).bind(this)
        },
        onDragValueChange: __privateMethod(this, _VolumeSlider_instances, onDragValueChange_fn).bind(this),
        onValueChange: __privateMethod(this, _VolumeSlider_instances, onValueChange_fn).bind(this)
      }).attach(this);
      effect(__privateMethod(this, _VolumeSlider_instances, watchVolume_fn3).bind(this));
    }
    onAttach(el) {
      el.setAttribute("data-media-volume-slider", "");
      setAttributeIfEmpty(el, "aria-label", "Volume");
      const { canSetVolume } = __privateGet(this, _media29).$state;
      this.setAttributes({
        "data-supported": canSetVolume,
        "aria-hidden": $ariaBool(() => !canSetVolume())
      });
    }
  }, _media29 = new WeakMap(), _VolumeSlider_instances = new WeakSet(), getARIAValueNow_fn2 = function() {
    const { value } = this.$state, { audioGain } = __privateGet(this, _media29).$state;
    return Math.round(value() * (audioGain() ?? 1));
  }, getARIAValueText_fn2 = function() {
    const { value, max: max2 } = this.$state, { audioGain } = __privateGet(this, _media29).$state;
    return round(value() / max2() * (audioGain() ?? 1) * 100, 2) + "%";
  }, getARIAValueMax_fn = function() {
    const { audioGain } = __privateGet(this, _media29).$state;
    return this.$state.max() * (audioGain() ?? 1);
  }, isDisabled_fn = function() {
    const { disabled } = this.$props, { canSetVolume } = __privateGet(this, _media29).$state;
    return disabled() || !canSetVolume();
  }, watchVolume_fn3 = function() {
    const { muted, volume } = __privateGet(this, _media29).$state;
    const newValue = muted() ? 0 : volume() * 100;
    this.$state.value.set(newValue);
    this.dispatch("value-change", { detail: newValue });
  }, _throttleVolumeChange = new WeakMap(), onVolumeChange_fn4 = function(event) {
    if (!event.trigger) return;
    const mediaVolume = round(event.detail / 100, 3);
    __privateGet(this, _media29).remote.changeVolume(mediaVolume, event);
  }, onValueChange_fn = function(event) {
    __privateGet(this, _throttleVolumeChange).call(this, event);
  }, onDragValueChange_fn = function(event) {
    __privateGet(this, _throttleVolumeChange).call(this, event);
  }, __publicField(_a87, "props", {
    ...SliderController.props,
    keyStep: 5,
    shiftKeyMultiplier: 2
  }), __publicField(_a87, "state", sliderState), _a87);
  var _a88, _media30, _AudioGainSlider_instances, getARIAValueNow_fn3, getARIAValueText_fn3, watchMinMax_fn2, watchAudioGain_fn, isDisabled_fn2, onAudioGainChange_fn, onValueChange_fn2, onDragValueChange_fn2;
  var AudioGainSlider = (_a88 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _AudioGainSlider_instances);
      __privateAdd(this, _media30);
    }
    onSetup() {
      __privateSet(this, _media30, useMediaContext());
      provideContext(sliderValueFormatContext, {
        default: "percent",
        percent: (_, decimalPlaces) => {
          return round(this.$state.value(), decimalPlaces) + "%";
        }
      });
      new SliderController({
        getStep: this.$props.step,
        getKeyStep: this.$props.keyStep,
        roundValue: Math.round,
        isDisabled: __privateMethod(this, _AudioGainSlider_instances, isDisabled_fn2).bind(this),
        aria: {
          valueNow: __privateMethod(this, _AudioGainSlider_instances, getARIAValueNow_fn3).bind(this),
          valueText: __privateMethod(this, _AudioGainSlider_instances, getARIAValueText_fn3).bind(this)
        },
        onDragValueChange: __privateMethod(this, _AudioGainSlider_instances, onDragValueChange_fn2).bind(this),
        onValueChange: __privateMethod(this, _AudioGainSlider_instances, onValueChange_fn2).bind(this)
      }).attach(this);
      effect(__privateMethod(this, _AudioGainSlider_instances, watchMinMax_fn2).bind(this));
      effect(__privateMethod(this, _AudioGainSlider_instances, watchAudioGain_fn).bind(this));
    }
    onAttach(el) {
      el.setAttribute("data-media-audio-gain-slider", "");
      setAttributeIfEmpty(el, "aria-label", "Audio Boost");
      const { canSetAudioGain } = __privateGet(this, _media30).$state;
      this.setAttributes({
        "data-supported": canSetAudioGain,
        "aria-hidden": $ariaBool(() => !canSetAudioGain())
      });
    }
  }, _media30 = new WeakMap(), _AudioGainSlider_instances = new WeakSet(), getARIAValueNow_fn3 = function() {
    const { value } = this.$state;
    return Math.round(value());
  }, getARIAValueText_fn3 = function() {
    const { value } = this.$state;
    return value() + "%";
  }, watchMinMax_fn2 = function() {
    const { min: min2, max: max2 } = this.$props;
    this.$state.min.set(min2());
    this.$state.max.set(max2());
  }, watchAudioGain_fn = function() {
    const { audioGain } = __privateGet(this, _media30).$state, value = ((audioGain() ?? 1) - 1) * 100;
    this.$state.value.set(value);
    this.dispatch("value-change", { detail: value });
  }, isDisabled_fn2 = function() {
    const { disabled } = this.$props, { canSetAudioGain } = __privateGet(this, _media30).$state;
    return disabled() || !canSetAudioGain();
  }, onAudioGainChange_fn = function(event) {
    if (!event.trigger) return;
    const gain = round(1 + event.detail / 100, 2);
    __privateGet(this, _media30).remote.changeAudioGain(gain, event);
  }, onValueChange_fn2 = function(event) {
    __privateMethod(this, _AudioGainSlider_instances, onAudioGainChange_fn).call(this, event);
  }, onDragValueChange_fn2 = function(event) {
    __privateMethod(this, _AudioGainSlider_instances, onAudioGainChange_fn).call(this, event);
  }, __publicField(_a88, "props", {
    ...SliderController.props,
    step: 25,
    keyStep: 25,
    shiftKeyMultiplier: 2,
    min: 0,
    max: 300
  }), __publicField(_a88, "state", sliderState), _a88);
  var _a89, _media31, _SpeedSlider_instances, getARIAValueNow_fn4, getARIAValueText_fn4, watchMinMax_fn3, watchPlaybackRate_fn2, roundValue_fn, isDisabled_fn3, _throttledSpeedChange, onPlaybackRateChange_fn, onValueChange_fn3, onDragValueChange_fn3;
  var SpeedSlider = (_a89 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _SpeedSlider_instances);
      __privateAdd(this, _media31);
      __privateAdd(this, _throttledSpeedChange, functionThrottle(__privateMethod(this, _SpeedSlider_instances, onPlaybackRateChange_fn).bind(this), 25));
    }
    onSetup() {
      __privateSet(this, _media31, useMediaContext());
      new SliderController({
        getStep: this.$props.step,
        getKeyStep: this.$props.keyStep,
        roundValue: __privateMethod(this, _SpeedSlider_instances, roundValue_fn),
        isDisabled: __privateMethod(this, _SpeedSlider_instances, isDisabled_fn3).bind(this),
        aria: {
          valueNow: __privateMethod(this, _SpeedSlider_instances, getARIAValueNow_fn4).bind(this),
          valueText: __privateMethod(this, _SpeedSlider_instances, getARIAValueText_fn4).bind(this)
        },
        onDragValueChange: __privateMethod(this, _SpeedSlider_instances, onDragValueChange_fn3).bind(this),
        onValueChange: __privateMethod(this, _SpeedSlider_instances, onValueChange_fn3).bind(this)
      }).attach(this);
      effect(__privateMethod(this, _SpeedSlider_instances, watchMinMax_fn3).bind(this));
      effect(__privateMethod(this, _SpeedSlider_instances, watchPlaybackRate_fn2).bind(this));
    }
    onAttach(el) {
      el.setAttribute("data-media-speed-slider", "");
      setAttributeIfEmpty(el, "aria-label", "Speed");
      const { canSetPlaybackRate } = __privateGet(this, _media31).$state;
      this.setAttributes({
        "data-supported": canSetPlaybackRate,
        "aria-hidden": $ariaBool(() => !canSetPlaybackRate())
      });
    }
  }, _media31 = new WeakMap(), _SpeedSlider_instances = new WeakSet(), getARIAValueNow_fn4 = function() {
    const { value } = this.$state;
    return value();
  }, getARIAValueText_fn4 = function() {
    const { value } = this.$state;
    return value() + "x";
  }, watchMinMax_fn3 = function() {
    const { min: min2, max: max2 } = this.$props;
    this.$state.min.set(min2());
    this.$state.max.set(max2());
  }, watchPlaybackRate_fn2 = function() {
    const { playbackRate } = __privateGet(this, _media31).$state;
    const newValue = playbackRate();
    this.$state.value.set(newValue);
    this.dispatch("value-change", { detail: newValue });
  }, roundValue_fn = function(value) {
    return round(value, 2);
  }, isDisabled_fn3 = function() {
    const { disabled } = this.$props, { canSetPlaybackRate } = __privateGet(this, _media31).$state;
    return disabled() || !canSetPlaybackRate();
  }, _throttledSpeedChange = new WeakMap(), onPlaybackRateChange_fn = function(event) {
    if (!event.trigger) return;
    const rate = event.detail;
    __privateGet(this, _media31).remote.changePlaybackRate(rate, event);
  }, onValueChange_fn3 = function(event) {
    __privateGet(this, _throttledSpeedChange).call(this, event);
  }, onDragValueChange_fn3 = function(event) {
    __privateGet(this, _throttledSpeedChange).call(this, event);
  }, __publicField(_a89, "props", {
    ...SliderController.props,
    step: 0.25,
    keyStep: 0.25,
    shiftKeyMultiplier: 2,
    min: 0,
    max: 2
  }), __publicField(_a89, "state", sliderState), _a89);
  var _a90, _media32, _sortedQualities, _QualitySlider_instances, getARIAValueNow_fn5, getARIAValueText_fn5, watchMax_fn, watchQuality_fn, isDisabled_fn4, _throttledQualityChange, onQualityChange_fn4, onValueChange_fn4, onDragValueChange_fn4;
  var QualitySlider = (_a90 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _QualitySlider_instances);
      __privateAdd(this, _media32);
      __privateAdd(this, _sortedQualities, computed(() => {
        const { qualities } = __privateGet(this, _media32).$state;
        return sortVideoQualities(qualities());
      }));
      __privateAdd(this, _throttledQualityChange, functionThrottle(__privateMethod(this, _QualitySlider_instances, onQualityChange_fn4).bind(this), 25));
    }
    onSetup() {
      __privateSet(this, _media32, useMediaContext());
      new SliderController({
        getStep: this.$props.step,
        getKeyStep: this.$props.keyStep,
        roundValue: Math.round,
        isDisabled: __privateMethod(this, _QualitySlider_instances, isDisabled_fn4).bind(this),
        aria: {
          valueNow: __privateMethod(this, _QualitySlider_instances, getARIAValueNow_fn5).bind(this),
          valueText: __privateMethod(this, _QualitySlider_instances, getARIAValueText_fn5).bind(this)
        },
        onDragValueChange: __privateMethod(this, _QualitySlider_instances, onDragValueChange_fn4).bind(this),
        onValueChange: __privateMethod(this, _QualitySlider_instances, onValueChange_fn4).bind(this)
      }).attach(this);
      effect(__privateMethod(this, _QualitySlider_instances, watchMax_fn).bind(this));
      effect(__privateMethod(this, _QualitySlider_instances, watchQuality_fn).bind(this));
    }
    onAttach(el) {
      el.setAttribute("data-media-quality-slider", "");
      setAttributeIfEmpty(el, "aria-label", "Video Quality");
      const { qualities, canSetQuality } = __privateGet(this, _media32).$state, $supported = computed(() => canSetQuality() && qualities().length > 0);
      this.setAttributes({
        "data-supported": $supported,
        "aria-hidden": $ariaBool(() => !$supported())
      });
    }
  }, _media32 = new WeakMap(), _sortedQualities = new WeakMap(), _QualitySlider_instances = new WeakSet(), getARIAValueNow_fn5 = function() {
    const { value } = this.$state;
    return value();
  }, getARIAValueText_fn5 = function() {
    const { quality } = __privateGet(this, _media32).$state;
    if (!quality()) return "";
    const { height, bitrate } = quality(), bitrateText = bitrate && bitrate > 0 ? `${(bitrate / 1e6).toFixed(2)} Mbps` : null;
    return height ? `${height}p${bitrateText ? ` (${bitrateText})` : ""}` : "Auto";
  }, watchMax_fn = function() {
    const $qualities = __privateGet(this, _sortedQualities).call(this);
    this.$state.max.set(Math.max(0, $qualities.length - 1));
  }, watchQuality_fn = function() {
    let { quality } = __privateGet(this, _media32).$state, $qualities = __privateGet(this, _sortedQualities).call(this), value = Math.max(0, $qualities.indexOf(quality()));
    this.$state.value.set(value);
    this.dispatch("value-change", { detail: value });
  }, isDisabled_fn4 = function() {
    const { disabled } = this.$props, { canSetQuality, qualities } = __privateGet(this, _media32).$state;
    return disabled() || qualities().length <= 1 || !canSetQuality();
  }, _throttledQualityChange = new WeakMap(), onQualityChange_fn4 = function(event) {
    if (!event.trigger) return;
    const { qualities } = __privateGet(this, _media32), quality = peek(__privateGet(this, _sortedQualities))[event.detail];
    __privateGet(this, _media32).remote.changeQuality(qualities.indexOf(quality), event);
  }, onValueChange_fn4 = function(event) {
    __privateGet(this, _throttledQualityChange).call(this, event);
  }, onDragValueChange_fn4 = function(event) {
    __privateGet(this, _throttledQualityChange).call(this, event);
  }, __publicField(_a90, "props", {
    ...SliderController.props,
    step: 1,
    keyStep: 1,
    shiftKeyMultiplier: 1
  }), __publicField(_a90, "state", sliderState), _a90);
  var _a91, _media33, _dispatchSeeking, _chapter, _TimeSlider_instances, calcBufferedPercent_fn, hasChapters_fn, watchSeekingThrottle_fn, watchCurrentTime_fn2, watchPreviewing_fn, seeking_fn2, seek_fn, _playingBeforeDragStart, onDragStart_fn, onDragValueChange_fn5, onDragEnd_fn, onValueChange_fn5, getValue_fn, getStep_fn, getKeyStep_fn, roundValue_fn2, isDisabled_fn5, getARIAValueNow_fn6, getARIAValueText_fn6, percentToTime_fn, timeToPercent_fn, formatValue_fn, formatTime_fn;
  var TimeSlider = (_a91 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _TimeSlider_instances);
      __privateAdd(this, _media33);
      __privateAdd(this, _dispatchSeeking);
      __privateAdd(this, _chapter, signal(null));
      __privateAdd(this, _playingBeforeDragStart, false);
      const { noSwipeGesture } = this.$props;
      new SliderController({
        swipeGesture: () => !noSwipeGesture(),
        getValue: __privateMethod(this, _TimeSlider_instances, getValue_fn).bind(this),
        getStep: __privateMethod(this, _TimeSlider_instances, getStep_fn).bind(this),
        getKeyStep: __privateMethod(this, _TimeSlider_instances, getKeyStep_fn).bind(this),
        roundValue: __privateMethod(this, _TimeSlider_instances, roundValue_fn2),
        isDisabled: __privateMethod(this, _TimeSlider_instances, isDisabled_fn5).bind(this),
        aria: {
          valueNow: __privateMethod(this, _TimeSlider_instances, getARIAValueNow_fn6).bind(this),
          valueText: __privateMethod(this, _TimeSlider_instances, getARIAValueText_fn6).bind(this)
        },
        onDragStart: __privateMethod(this, _TimeSlider_instances, onDragStart_fn).bind(this),
        onDragValueChange: __privateMethod(this, _TimeSlider_instances, onDragValueChange_fn5).bind(this),
        onDragEnd: __privateMethod(this, _TimeSlider_instances, onDragEnd_fn).bind(this),
        onValueChange: __privateMethod(this, _TimeSlider_instances, onValueChange_fn5).bind(this)
      });
    }
    onSetup() {
      __privateSet(this, _media33, useMediaContext());
      provideContext(sliderValueFormatContext, {
        default: "time",
        value: __privateMethod(this, _TimeSlider_instances, formatValue_fn).bind(this),
        time: __privateMethod(this, _TimeSlider_instances, formatTime_fn).bind(this)
      });
      this.setAttributes({ "data-chapters": __privateMethod(this, _TimeSlider_instances, hasChapters_fn).bind(this) });
      this.setStyles({ "--slider-progress": __privateMethod(this, _TimeSlider_instances, calcBufferedPercent_fn).bind(this) });
      effect(__privateMethod(this, _TimeSlider_instances, watchCurrentTime_fn2).bind(this));
      effect(__privateMethod(this, _TimeSlider_instances, watchSeekingThrottle_fn).bind(this));
    }
    onAttach(el) {
      el.setAttribute("data-media-time-slider", "");
      setAttributeIfEmpty(el, "aria-label", "Seek");
    }
    onConnect(el) {
      effect(__privateMethod(this, _TimeSlider_instances, watchPreviewing_fn).bind(this));
      watchActiveTextTrack(__privateGet(this, _media33).textTracks, "chapters", __privateGet(this, _chapter).set);
    }
  }, _media33 = new WeakMap(), _dispatchSeeking = new WeakMap(), _chapter = new WeakMap(), _TimeSlider_instances = new WeakSet(), calcBufferedPercent_fn = function() {
    const { bufferedEnd, duration } = __privateGet(this, _media33).$state;
    return round(Math.min(bufferedEnd() / Math.max(duration(), 1), 1) * 100, 3) + "%";
  }, hasChapters_fn = function() {
    const { duration } = __privateGet(this, _media33).$state;
    return __privateGet(this, _chapter).call(this)?.cues.length && Number.isFinite(duration()) && duration() > 0;
  }, watchSeekingThrottle_fn = function() {
    __privateSet(this, _dispatchSeeking, functionThrottle(__privateMethod(this, _TimeSlider_instances, seeking_fn2).bind(this), this.$props.seekingRequestThrottle()));
  }, watchCurrentTime_fn2 = function() {
    if (this.$state.hidden()) return;
    const { value, dragging } = this.$state, newValue = __privateMethod(this, _TimeSlider_instances, getValue_fn).call(this);
    if (!peek(dragging)) {
      value.set(newValue);
      this.dispatch("value-change", { detail: newValue });
    }
  }, watchPreviewing_fn = function() {
    const player = __privateGet(this, _media33).player.el, { preview } = useContext2(sliderContext);
    player && preview() && setAttribute(player, "data-preview", this.$state.active());
  }, seeking_fn2 = function(time, event) {
    __privateGet(this, _media33).remote.seeking(time, event);
  }, seek_fn = function(time, percent, event) {
    __privateGet(this, _dispatchSeeking).cancel();
    const { live } = __privateGet(this, _media33).$state;
    if (live() && percent >= 99) {
      __privateGet(this, _media33).remote.seekToLiveEdge(event);
      return;
    }
    __privateGet(this, _media33).remote.seek(time, event);
  }, _playingBeforeDragStart = new WeakMap(), onDragStart_fn = function(event) {
    const { pauseWhileDragging } = this.$props;
    if (pauseWhileDragging()) {
      const { paused } = __privateGet(this, _media33).$state;
      __privateSet(this, _playingBeforeDragStart, !paused());
      __privateGet(this, _media33).remote.pause(event);
    }
  }, onDragValueChange_fn5 = function(event) {
    __privateGet(this, _dispatchSeeking).call(this, __privateMethod(this, _TimeSlider_instances, percentToTime_fn).call(this, event.detail), event);
  }, onDragEnd_fn = function(event) {
    const { seeking } = __privateGet(this, _media33).$state;
    if (!peek(seeking)) __privateMethod(this, _TimeSlider_instances, seeking_fn2).call(this, __privateMethod(this, _TimeSlider_instances, percentToTime_fn).call(this, event.detail), event);
    const percent = event.detail;
    __privateMethod(this, _TimeSlider_instances, seek_fn).call(this, __privateMethod(this, _TimeSlider_instances, percentToTime_fn).call(this, percent), percent, event);
    const { pauseWhileDragging } = this.$props;
    if (pauseWhileDragging() && __privateGet(this, _playingBeforeDragStart)) {
      __privateGet(this, _media33).remote.play(event);
      __privateSet(this, _playingBeforeDragStart, false);
    }
  }, onValueChange_fn5 = function(event) {
    const { dragging } = this.$state;
    if (dragging() || !event.trigger) return;
    __privateMethod(this, _TimeSlider_instances, onDragEnd_fn).call(this, event);
  }, getValue_fn = function() {
    const { currentTime } = __privateGet(this, _media33).$state;
    return __privateMethod(this, _TimeSlider_instances, timeToPercent_fn).call(this, currentTime());
  }, getStep_fn = function() {
    const value = this.$props.step() / __privateGet(this, _media33).$state.duration() * 100;
    return Number.isFinite(value) ? value : 1;
  }, getKeyStep_fn = function() {
    const value = this.$props.keyStep() / __privateGet(this, _media33).$state.duration() * 100;
    return Number.isFinite(value) ? value : 1;
  }, roundValue_fn2 = function(value) {
    return round(value, 3);
  }, isDisabled_fn5 = function() {
    const { disabled } = this.$props, { canSeek } = __privateGet(this, _media33).$state;
    return disabled() || !canSeek();
  }, getARIAValueNow_fn6 = function() {
    const { value } = this.$state;
    return Math.round(value());
  }, getARIAValueText_fn6 = function() {
    const time = __privateMethod(this, _TimeSlider_instances, percentToTime_fn).call(this, this.$state.value()), { duration } = __privateGet(this, _media33).$state;
    return Number.isFinite(time) ? `${formatSpokenTime(time)} out of ${formatSpokenTime(duration())}` : "live";
  }, percentToTime_fn = function(percent) {
    const { duration } = __privateGet(this, _media33).$state;
    return round(percent / 100 * duration(), 5);
  }, timeToPercent_fn = function(time) {
    const { liveEdge, duration } = __privateGet(this, _media33).$state, rate = Math.max(0, Math.min(1, liveEdge() ? 1 : Math.min(time, duration()) / duration()));
    return Number.isNaN(rate) ? 0 : Number.isFinite(rate) ? rate * 100 : 100;
  }, formatValue_fn = function(percent) {
    const time = __privateMethod(this, _TimeSlider_instances, percentToTime_fn).call(this, percent), { live, duration } = __privateGet(this, _media33).$state;
    return Number.isFinite(time) ? (live() ? time - duration() : time).toFixed(0) : "LIVE";
  }, formatTime_fn = function(percent, options) {
    const time = __privateMethod(this, _TimeSlider_instances, percentToTime_fn).call(this, percent), { live, duration } = __privateGet(this, _media33).$state, value = live() ? time - duration() : time;
    return Number.isFinite(time) ? `${value < 0 ? "-" : ""}${formatTime2(Math.abs(value), options)}` : "LIVE";
  }, __publicField(_a91, "props", {
    ...SliderController.props,
    step: 0.1,
    keyStep: 5,
    shiftKeyMultiplier: 2,
    pauseWhileDragging: false,
    noSwipeGesture: false,
    seekingRequestThrottle: 100
  }), __publicField(_a91, "state", sliderState), _a91);
  var _a92, _media34, _sliderState, _updateScope, _titleRef, _refs, _$track, _$cues, _activeIndex, _activePointerIndex, _bufferedIndex, _SliderChapters_instances, setTrack_fn, reset_fn5, watch_fn, watchUpdates_fn, watchContainerWidths_fn, watchFillPercent_fn, watchPointerPercent_fn, updateFillPercents_fn, updateFillPercent_fn, findActiveChapterIndex_fn, watchBufferedPercent_fn, _updateBufferedPercent, _bufferedPercent, calcMediaBufferedPercent_fn, getEndTime_fn, calcPercent_fn, fillGaps_fn, watchSource_fn, onTrackChange_fn2, watchMediaDuration_fn, _onCuesChange, onChapterTitleChange_fn, findParentSlider_fn, findChapterTitleRef_fn;
  var SliderChapters = (_a92 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _SliderChapters_instances);
      __privateAdd(this, _media34);
      __privateAdd(this, _sliderState);
      __privateAdd(this, _updateScope);
      __privateAdd(this, _titleRef, null);
      __privateAdd(this, _refs, []);
      __privateAdd(this, _$track, signal(null));
      __privateAdd(this, _$cues, signal([]));
      __privateAdd(this, _activeIndex, signal(-1));
      __privateAdd(this, _activePointerIndex, signal(-1));
      __privateAdd(this, _bufferedIndex, 0);
      __privateAdd(this, _updateBufferedPercent, animationFrameThrottle((bufferedPercent) => {
        let percent, cues = __privateGet(this, _$cues).call(this), { seekableStart } = __privateGet(this, _media34).$state, startTime = seekableStart(), endTime = __privateMethod(this, _SliderChapters_instances, getEndTime_fn).call(this, cues);
        for (let i = __privateGet(this, _bufferedIndex); i < __privateGet(this, _refs).length; i++) {
          percent = __privateMethod(this, _SliderChapters_instances, calcPercent_fn).call(this, cues[i], bufferedPercent, startTime, endTime);
          __privateGet(this, _refs)[i]?.style.setProperty("--chapter-progress", percent + "%");
          if (percent < 100) {
            __privateSet(this, _bufferedIndex, i);
            break;
          }
        }
      }));
      __privateAdd(this, _bufferedPercent, computed(__privateMethod(this, _SliderChapters_instances, calcMediaBufferedPercent_fn).bind(this)));
      __privateAdd(this, _onCuesChange, functionDebounce(() => {
        const track = peek(__privateGet(this, _$track));
        if (!this.scope || !track || !track.cues.length) return;
        __privateGet(this, _$cues).set(__privateMethod(this, _SliderChapters_instances, fillGaps_fn).call(this, track.cues));
        __privateGet(this, _activeIndex).set(0);
        __privateSet(this, _bufferedIndex, 0);
      }, 150, true));
    }
    get cues() {
      return __privateGet(this, _$cues).call(this);
    }
    get activeCue() {
      return __privateGet(this, _$cues).call(this)[__privateGet(this, _activeIndex).call(this)] || null;
    }
    get activePointerCue() {
      return __privateGet(this, _$cues).call(this)[__privateGet(this, _activePointerIndex).call(this)] || null;
    }
    onSetup() {
      __privateSet(this, _media34, useMediaContext());
      __privateSet(this, _sliderState, useState18(TimeSlider.state));
    }
    onAttach(el) {
      watchActiveTextTrack(__privateGet(this, _media34).textTracks, "chapters", __privateMethod(this, _SliderChapters_instances, setTrack_fn).bind(this));
      effect(__privateMethod(this, _SliderChapters_instances, watchSource_fn).bind(this));
    }
    onConnect() {
      onDispose(() => __privateMethod(this, _SliderChapters_instances, reset_fn5).bind(this));
    }
    onDestroy() {
      __privateMethod(this, _SliderChapters_instances, setTrack_fn).call(this, null);
    }
    setRefs(refs) {
      __privateSet(this, _refs, refs);
      __privateGet(this, _updateScope)?.dispose();
      if (__privateGet(this, _refs).length === 1) {
        const el = __privateGet(this, _refs)[0];
        el.style.width = "100%";
        el.style.setProperty("--chapter-fill", "var(--slider-fill)");
        el.style.setProperty("--chapter-progress", "var(--slider-progress)");
      } else if (__privateGet(this, _refs).length > 0) scoped(() => __privateMethod(this, _SliderChapters_instances, watch_fn).call(this), __privateSet(this, _updateScope, createScope()));
    }
  }, _media34 = new WeakMap(), _sliderState = new WeakMap(), _updateScope = new WeakMap(), _titleRef = new WeakMap(), _refs = new WeakMap(), _$track = new WeakMap(), _$cues = new WeakMap(), _activeIndex = new WeakMap(), _activePointerIndex = new WeakMap(), _bufferedIndex = new WeakMap(), _SliderChapters_instances = new WeakSet(), setTrack_fn = function(track) {
    if (peek(__privateGet(this, _$track)) === track) return;
    __privateMethod(this, _SliderChapters_instances, reset_fn5).call(this);
    __privateGet(this, _$track).set(track);
  }, reset_fn5 = function() {
    __privateSet(this, _refs, []);
    __privateGet(this, _$cues).set([]);
    __privateGet(this, _activeIndex).set(-1);
    __privateGet(this, _activePointerIndex).set(-1);
    __privateSet(this, _bufferedIndex, 0);
    __privateGet(this, _updateScope)?.dispose();
  }, watch_fn = function() {
    if (!__privateGet(this, _refs).length) return;
    effect(__privateMethod(this, _SliderChapters_instances, watchUpdates_fn).bind(this));
  }, watchUpdates_fn = function() {
    const { hidden } = __privateGet(this, _sliderState);
    if (hidden()) return;
    effect(__privateMethod(this, _SliderChapters_instances, watchContainerWidths_fn).bind(this));
    effect(__privateMethod(this, _SliderChapters_instances, watchFillPercent_fn).bind(this));
    effect(__privateMethod(this, _SliderChapters_instances, watchPointerPercent_fn).bind(this));
    effect(__privateMethod(this, _SliderChapters_instances, watchBufferedPercent_fn).bind(this));
  }, watchContainerWidths_fn = function() {
    const cues = __privateGet(this, _$cues).call(this);
    if (!cues.length) return;
    let cue, { seekableStart, seekableEnd } = __privateGet(this, _media34).$state, startTime = seekableStart(), duration = (seekableEnd() || cues[cues.length - 1].endTime) - startTime, remainingWidth = 100;
    for (let i = 0; i < cues.length; i++) {
      cue = cues[i];
      if (__privateGet(this, _refs)[i]) {
        const width = i === cues.length - 1 ? remainingWidth : round((cue.endTime - Math.max(startTime, cue.startTime)) / duration * 100, 3);
        __privateGet(this, _refs)[i].style.width = width + "%";
        remainingWidth -= width;
      }
    }
  }, watchFillPercent_fn = function() {
    let { liveEdge, seekableStart, seekableEnd } = __privateGet(this, _media34).$state, { fillPercent, value } = __privateGet(this, _sliderState), cues = __privateGet(this, _$cues).call(this), isLiveEdge = liveEdge(), prevActiveIndex = peek(__privateGet(this, _activeIndex)), currentChapter = cues[prevActiveIndex];
    let currentActiveIndex = isLiveEdge ? __privateGet(this, _$cues).length - 1 : __privateMethod(this, _SliderChapters_instances, findActiveChapterIndex_fn).call(this, currentChapter ? currentChapter.startTime / seekableEnd() * 100 <= peek(value) ? prevActiveIndex : 0 : 0, fillPercent());
    if (isLiveEdge || !currentChapter) __privateMethod(this, _SliderChapters_instances, updateFillPercents_fn).call(this, 0, cues.length, 100);
    else if (currentActiveIndex > prevActiveIndex) __privateMethod(this, _SliderChapters_instances, updateFillPercents_fn).call(this, prevActiveIndex, currentActiveIndex, 100);
    else if (currentActiveIndex < prevActiveIndex) __privateMethod(this, _SliderChapters_instances, updateFillPercents_fn).call(this, currentActiveIndex + 1, prevActiveIndex + 1, 0);
    const percent = isLiveEdge ? 100 : __privateMethod(this, _SliderChapters_instances, calcPercent_fn).call(this, cues[currentActiveIndex], fillPercent(), seekableStart(), __privateMethod(this, _SliderChapters_instances, getEndTime_fn).call(this, cues));
    __privateMethod(this, _SliderChapters_instances, updateFillPercent_fn).call(this, __privateGet(this, _refs)[currentActiveIndex], percent);
    __privateGet(this, _activeIndex).set(currentActiveIndex);
  }, watchPointerPercent_fn = function() {
    let { hidden, pointerPercent } = __privateGet(this, _sliderState);
    if (hidden()) {
      __privateGet(this, _activePointerIndex).set(-1);
      return;
    }
    const activeIndex = __privateMethod(this, _SliderChapters_instances, findActiveChapterIndex_fn).call(this, 0, pointerPercent());
    __privateGet(this, _activePointerIndex).set(activeIndex);
  }, updateFillPercents_fn = function(start, end, percent) {
    for (let i = start; i < end; i++) __privateMethod(this, _SliderChapters_instances, updateFillPercent_fn).call(this, __privateGet(this, _refs)[i], percent);
  }, updateFillPercent_fn = function(ref, percent) {
    if (!ref) return;
    ref.style.setProperty("--chapter-fill", percent + "%");
    setAttribute(ref, "data-active", percent > 0 && percent < 100);
    setAttribute(ref, "data-ended", percent === 100);
  }, findActiveChapterIndex_fn = function(startIndex, percent) {
    let chapterPercent = 0, cues = __privateGet(this, _$cues).call(this);
    if (percent === 0) return 0;
    else if (percent === 100) return cues.length - 1;
    let { seekableStart } = __privateGet(this, _media34).$state, startTime = seekableStart(), endTime = __privateMethod(this, _SliderChapters_instances, getEndTime_fn).call(this, cues);
    for (let i = startIndex; i < cues.length; i++) {
      chapterPercent = __privateMethod(this, _SliderChapters_instances, calcPercent_fn).call(this, cues[i], percent, startTime, endTime);
      if (chapterPercent >= 0 && chapterPercent < 100) return i;
    }
    return 0;
  }, watchBufferedPercent_fn = function() {
    __privateGet(this, _updateBufferedPercent).call(this, __privateGet(this, _bufferedPercent).call(this));
  }, _updateBufferedPercent = new WeakMap(), _bufferedPercent = new WeakMap(), calcMediaBufferedPercent_fn = function() {
    const { bufferedEnd, duration } = __privateGet(this, _media34).$state;
    return round(Math.min(bufferedEnd() / Math.max(duration(), 1), 1), 3) * 100;
  }, getEndTime_fn = function(cues) {
    const { seekableEnd } = __privateGet(this, _media34).$state, endTime = seekableEnd();
    return Number.isFinite(endTime) ? endTime : cues[cues.length - 1]?.endTime || 0;
  }, calcPercent_fn = function(cue, percent, startTime, endTime) {
    if (!cue) return 0;
    if (__privateGet(this, _$cues).call(this).length === 0) return 0;
    const duration = endTime - startTime, cueStartTime = Math.max(0, cue.startTime - startTime), cueEndTime = Math.min(endTime, cue.endTime) - startTime;
    const startRatio = cueStartTime / duration, startPercent = startRatio * 100, endPercent = Math.min(1, startRatio + (cueEndTime - cueStartTime) / duration) * 100;
    return Math.max(0, round(percent >= endPercent ? 100 : (percent - startPercent) / (endPercent - startPercent) * 100, 3));
  }, fillGaps_fn = function(cues) {
    let chapters = [], { seekableStart, seekableEnd, duration } = __privateGet(this, _media34).$state, startTime = seekableStart(), endTime = seekableEnd();
    cues = cues.filter((cue) => cue.startTime <= endTime && cue.endTime >= startTime);
    const firstCue = cues[0];
    if (firstCue && firstCue.startTime > startTime) chapters.push(new window.VTTCue(startTime, firstCue.startTime, ""));
    for (let i = 0; i < cues.length - 1; i++) {
      const currentCue = cues[i], nextCue = cues[i + 1];
      chapters.push(currentCue);
      if (nextCue) {
        const timeDiff = nextCue.startTime - currentCue.endTime;
        if (timeDiff > 0) chapters.push(new window.VTTCue(currentCue.endTime, currentCue.endTime + timeDiff, ""));
      }
    }
    const lastCue = cues[cues.length - 1];
    if (lastCue) {
      chapters.push(lastCue);
      const endTime2 = duration();
      if (endTime2 >= 0 && endTime2 - lastCue.endTime > 1) chapters.push(new window.VTTCue(lastCue.endTime, duration(), ""));
    }
    return chapters;
  }, watchSource_fn = function() {
    const { source } = __privateGet(this, _media34).$state;
    source();
    __privateMethod(this, _SliderChapters_instances, onTrackChange_fn2).call(this);
  }, onTrackChange_fn2 = function() {
    if (!this.scope) return;
    const { disabled } = this.$props;
    if (disabled()) {
      __privateGet(this, _$cues).set([]);
      __privateGet(this, _activeIndex).set(0);
      __privateSet(this, _bufferedIndex, 0);
      return;
    }
    const track = __privateGet(this, _$track).call(this);
    if (track) {
      const onCuesChange = __privateGet(this, _onCuesChange).bind(this);
      onCuesChange();
      new EventsController(track).add("add-cue", onCuesChange).add("remove-cue", onCuesChange);
      effect(__privateMethod(this, _SliderChapters_instances, watchMediaDuration_fn).bind(this));
    }
    __privateSet(this, _titleRef, __privateMethod(this, _SliderChapters_instances, findChapterTitleRef_fn).call(this));
    if (__privateGet(this, _titleRef)) effect(__privateMethod(this, _SliderChapters_instances, onChapterTitleChange_fn).bind(this));
    return () => {
      if (__privateGet(this, _titleRef)) {
        __privateGet(this, _titleRef).textContent = "";
        __privateSet(this, _titleRef, null);
      }
    };
  }, watchMediaDuration_fn = function() {
    __privateGet(this, _media34).$state.duration();
    __privateGet(this, _onCuesChange).call(this);
  }, _onCuesChange = new WeakMap(), onChapterTitleChange_fn = function() {
    const cue = this.activePointerCue || this.activeCue;
    if (__privateGet(this, _titleRef)) __privateGet(this, _titleRef).textContent = cue?.text || "";
  }, findParentSlider_fn = function() {
    let node = this.el;
    while (node && node.getAttribute("role") !== "slider") node = node.parentElement;
    return node;
  }, findChapterTitleRef_fn = function() {
    const slider = __privateMethod(this, _SliderChapters_instances, findParentSlider_fn).call(this);
    return slider ? slider.querySelector('[data-part="chapter-title"]') : null;
  }, __publicField(_a92, "props", { disabled: false }), _a92);
  var sliderchapters__proto = SliderChapters.prototype;
  prop(sliderchapters__proto, "cues");
  prop(sliderchapters__proto, "activeCue");
  prop(sliderchapters__proto, "activePointerCue");
  method(sliderchapters__proto, "setRefs");
  var menuContext = createContext2();
  function scrollIntoView(el, options) {
    const scrolls = r(el, options);
    for (const { el: el2, top, left } of scrolls) el2.scroll({
      top,
      left,
      behavior: options.behavior
    });
  }
  function scrollIntoCenter(el, options = {}) {
    scrollIntoView(el, {
      scrollMode: "if-needed",
      block: "center",
      inline: "center",
      ...options
    });
  }
  var FOCUSABLE_ELEMENTS_SELECTOR = /* @__PURE__ */ [
    "a[href]",
    "[tabindex]",
    "input",
    "select",
    "button"
  ].map((selector) => `${selector}:not([aria-hidden='true'])`).join(",");
  var VALID_KEYS = /* @__PURE__ */ new Set([
    "Escape",
    "Tab",
    "ArrowUp",
    "ArrowDown",
    "Home",
    "PageUp",
    "End",
    "PageDown",
    "Enter",
    " "
  ]);
  var _index, _el, _elements, _delegate5, _MenuFocusController_instances, focusAt_fn, findActiveIndex_fn, onFocus_fn3, validateKeyEvent_fn, onKeyUp_fn3, onKeyDown_fn3, nextIndex_fn, getFocusableElements_fn, _a93;
  var MenuFocusController = (_a93 = class {
    constructor(delegate) {
      __privateAdd(this, _MenuFocusController_instances);
      __privateAdd(this, _index, -1);
      __privateAdd(this, _el, null);
      __privateAdd(this, _elements, []);
      __privateAdd(this, _delegate5);
      __privateSet(this, _delegate5, delegate);
    }
    get items() {
      return __privateGet(this, _elements);
    }
    attachMenu(el) {
      listenEvent(el, "focus", __privateMethod(this, _MenuFocusController_instances, onFocus_fn3).bind(this));
      __privateSet(this, _el, el);
      onDispose(() => {
        __privateSet(this, _el, null);
      });
    }
    listen() {
      if (!__privateGet(this, _el)) return;
      this.update();
      new EventsController(__privateGet(this, _el)).add("keyup", __privateMethod(this, _MenuFocusController_instances, onKeyUp_fn3).bind(this)).add("keydown", __privateMethod(this, _MenuFocusController_instances, onKeyDown_fn3).bind(this));
      onDispose(() => {
        __privateSet(this, _index, -1);
        __privateSet(this, _elements, []);
      });
    }
    update() {
      __privateSet(this, _index, 0);
      __privateSet(this, _elements, __privateMethod(this, _MenuFocusController_instances, getFocusableElements_fn).call(this));
    }
    scroll(index = __privateMethod(this, _MenuFocusController_instances, findActiveIndex_fn).call(this)) {
      const element = __privateGet(this, _elements)[index];
      if (element) requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          scrollIntoCenter(element, {
            behavior: "smooth",
            boundary: (el) => {
              return !el.hasAttribute("data-root");
            }
          });
        });
      });
    }
    focusActive(scroll = true) {
      const index = __privateMethod(this, _MenuFocusController_instances, findActiveIndex_fn).call(this);
      __privateMethod(this, _MenuFocusController_instances, focusAt_fn).call(this, index >= 0 ? index : 0, scroll);
    }
  }, _index = new WeakMap(), _el = new WeakMap(), _elements = new WeakMap(), _delegate5 = new WeakMap(), _MenuFocusController_instances = new WeakSet(), focusAt_fn = function(index, scroll = true) {
    __privateSet(this, _index, index);
    if (__privateGet(this, _elements)[index]) {
      __privateGet(this, _elements)[index].focus({ preventScroll: true });
      if (scroll) this.scroll(index);
    } else __privateGet(this, _el)?.focus({ preventScroll: true });
  }, findActiveIndex_fn = function() {
    return __privateGet(this, _elements).findIndex((el) => document.activeElement === el || el.getAttribute("role") === "menuitemradio" && el.getAttribute("aria-checked") === "true");
  }, onFocus_fn3 = function() {
    if (__privateGet(this, _index) >= 0) return;
    this.update();
    this.focusActive();
  }, validateKeyEvent_fn = function(event) {
    const el = event.target;
    if (wasEnterKeyPressed(event) && el instanceof Element) {
      const role = el.getAttribute("role");
      return !/a|input|select|button/.test(el.localName) && !role;
    }
    return VALID_KEYS.has(event.key);
  }, onKeyUp_fn3 = function(event) {
    if (!__privateMethod(this, _MenuFocusController_instances, validateKeyEvent_fn).call(this, event)) return;
    event.stopPropagation();
    event.preventDefault();
  }, onKeyDown_fn3 = function(event) {
    if (!__privateMethod(this, _MenuFocusController_instances, validateKeyEvent_fn).call(this, event)) return;
    event.stopPropagation();
    event.preventDefault();
    switch (event.key) {
      case "Escape":
        __privateGet(this, _delegate5).closeMenu(event);
        break;
      case "Tab":
        __privateMethod(this, _MenuFocusController_instances, focusAt_fn).call(this, __privateMethod(this, _MenuFocusController_instances, nextIndex_fn).call(this, event.shiftKey ? -1 : 1));
        break;
      case "ArrowUp":
        __privateMethod(this, _MenuFocusController_instances, focusAt_fn).call(this, __privateMethod(this, _MenuFocusController_instances, nextIndex_fn).call(this, -1));
        break;
      case "ArrowDown":
        __privateMethod(this, _MenuFocusController_instances, focusAt_fn).call(this, __privateMethod(this, _MenuFocusController_instances, nextIndex_fn).call(this, 1));
        break;
      case "Home":
      case "PageUp":
        __privateMethod(this, _MenuFocusController_instances, focusAt_fn).call(this, 0);
        break;
      case "End":
      case "PageDown":
        __privateMethod(this, _MenuFocusController_instances, focusAt_fn).call(this, __privateGet(this, _elements).length - 1);
        break;
    }
  }, nextIndex_fn = function(delta) {
    let index = __privateGet(this, _index);
    do
      index = (index + delta + __privateGet(this, _elements).length) % __privateGet(this, _elements).length;
    while (__privateGet(this, _elements)[index]?.offsetParent === null);
    return index;
  }, getFocusableElements_fn = function() {
    if (!__privateGet(this, _el)) return [];
    const focusableElements = __privateGet(this, _el).querySelectorAll(FOCUSABLE_ELEMENTS_SELECTOR), elements = [];
    const is = (node) => {
      return node.getAttribute("role") === "menu";
    };
    for (const el of focusableElements) if (isHTMLElement2(el) && el.offsetParent !== null && isElementParent(__privateGet(this, _el), el, is)) elements.push(el);
    return elements;
  }, _a93);
  function __decorate(decorators, target, key2, desc) {
    var c = arguments.length, r2 = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key2) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r2 = Reflect.decorate(decorators, target, key2, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r2 = (c < 3 ? d(r2) : c > 3 ? d(target, key2, r2) : d(target, key2)) || r2;
    return c > 3 && r2 && Object.defineProperty(target, key2, r2), r2;
  }
  var idCount = 0;
  var _a94, _media35, _menuId, _menuButtonId, _expanded, _disabled, _trigger2, _content2, _parentMenu, _submenus, _menuObserver, _popper, _focus, _isSliderActive, _isTriggerDisabled, _transitionCallbacks, _Menu_instances, observeSliders_fn, watchExpanded_fn, attachMenuButton_fn, attachMenuItems_fn, attachObserver_fn, updateMenuItemsHidden_fn, disableMenuButton_fn, _wasKeyboardExpand, onExpandedChange_fn, updateFocus_fn, isExpanded_fn, isDisabled_fn6, disable_fn, onPointerUp_fn, onWindowPointerUp_fn, getCloseTarget_fn, toggleMediaControls_fn, addSubmenu_fn, _removeSubmenuBind, removeSubmenu_fn, _isSubmenuOpen, _onSubmenuOpenBind, onSubmenuOpen_fn, _onSubmenuCloseBind, onSubmenuClose_fn, _onResize, _isTransitionActive, onResizeTransition_fn;
  var Menu = (_a94 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _Menu_instances);
      __privateAdd(this, _media35);
      __privateAdd(this, _menuId);
      __privateAdd(this, _menuButtonId);
      __privateAdd(this, _expanded, signal(false));
      __privateAdd(this, _disabled, signal(false));
      __privateAdd(this, _trigger2, signal(null));
      __privateAdd(this, _content2, signal(null));
      __privateAdd(this, _parentMenu);
      __privateAdd(this, _submenus, /* @__PURE__ */ new Set());
      __privateAdd(this, _menuObserver, null);
      __privateAdd(this, _popper);
      __privateAdd(this, _focus);
      __privateAdd(this, _isSliderActive, false);
      __privateAdd(this, _isTriggerDisabled, signal(false));
      __privateAdd(this, _transitionCallbacks, /* @__PURE__ */ new Set());
      __privateAdd(this, _wasKeyboardExpand, false);
      __privateAdd(this, _removeSubmenuBind, __privateMethod(this, _Menu_instances, removeSubmenu_fn).bind(this));
      __privateAdd(this, _isSubmenuOpen, false);
      __privateAdd(this, _onSubmenuOpenBind, __privateMethod(this, _Menu_instances, onSubmenuOpen_fn).bind(this));
      __privateAdd(this, _onSubmenuCloseBind, __privateMethod(this, _Menu_instances, onSubmenuClose_fn).bind(this));
      __privateAdd(this, _onResize, animationFrameThrottle(() => {
        const content = peek(__privateGet(this, _content2));
        if (!content || IS_SERVER2) return;
        let height = 0, styles = getComputedStyle(content), children = [...content.children];
        for (const prop2 of [
          "paddingTop",
          "paddingBottom",
          "borderTopWidth",
          "borderBottomWidth"
        ]) height += parseFloat(styles[prop2]) || 0;
        for (const child of children) if (isHTMLElement2(child) && child.style.display === "contents") children.push(...child.children);
        else if (child.nodeType === 3) height += parseFloat(getComputedStyle(child).fontSize);
        else if (isHTMLElement2(child)) {
          if (!isElementVisible(child)) continue;
          const style = getComputedStyle(child);
          height += child.offsetHeight + (parseFloat(style.marginTop) || 0) + (parseFloat(style.marginBottom) || 0);
        }
        setStyle(content, "--menu-height", height + "px");
      }));
      __privateAdd(this, _isTransitionActive, false);
      const { showDelay } = this.$props;
      __privateSet(this, _popper, new Popper({
        trigger: __privateGet(this, _trigger2),
        content: __privateGet(this, _content2),
        showDelay,
        listen: (trigger, show, hide2) => {
          onPress(trigger, (event) => {
            if (__privateGet(this, _expanded).call(this)) hide2(event);
            else show(event);
          });
          const closeTarget = __privateMethod(this, _Menu_instances, getCloseTarget_fn).call(this);
          if (closeTarget) onPress(closeTarget, (event) => {
            event.stopPropagation();
            hide2(event);
          });
        },
        onChange: __privateMethod(this, _Menu_instances, onExpandedChange_fn).bind(this)
      }));
    }
    /**
    * The menu trigger element.
    */
    get triggerElement() {
      return __privateGet(this, _trigger2).call(this);
    }
    /**
    * The menu items element.
    */
    get contentElement() {
      return __privateGet(this, _content2).call(this);
    }
    /**
    * Whether this menu is the child of another menu that contains it.
    */
    get isSubmenu() {
      return !!__privateGet(this, _parentMenu);
    }
    onSetup() {
      __privateSet(this, _media35, useMediaContext());
      const currentIdCount = ++idCount;
      __privateSet(this, _menuId, `media-menu-${currentIdCount}`);
      __privateSet(this, _menuButtonId, `media-menu-button-${currentIdCount}`);
      __privateSet(this, _focus, new MenuFocusController({ closeMenu: this.close.bind(this) }));
      if (hasProvidedContext(menuContext)) __privateSet(this, _parentMenu, useContext2(menuContext));
      __privateMethod(this, _Menu_instances, observeSliders_fn).call(this);
      this.setAttributes({
        "data-open": __privateGet(this, _expanded),
        "data-root": !this.isSubmenu,
        "data-submenu": this.isSubmenu,
        "data-disabled": __privateMethod(this, _Menu_instances, isDisabled_fn6).bind(this)
      });
      provideContext(menuContext, {
        button: __privateGet(this, _trigger2),
        content: __privateGet(this, _content2),
        expanded: __privateGet(this, _expanded),
        hint: signal(""),
        submenu: !!__privateGet(this, _parentMenu),
        disable: __privateMethod(this, _Menu_instances, disable_fn).bind(this),
        attachMenuButton: __privateMethod(this, _Menu_instances, attachMenuButton_fn).bind(this),
        attachMenuItems: __privateMethod(this, _Menu_instances, attachMenuItems_fn).bind(this),
        attachObserver: __privateMethod(this, _Menu_instances, attachObserver_fn).bind(this),
        disableMenuButton: __privateMethod(this, _Menu_instances, disableMenuButton_fn).bind(this),
        addSubmenu: __privateMethod(this, _Menu_instances, addSubmenu_fn).bind(this),
        onTransitionEvent: (callback) => {
          __privateGet(this, _transitionCallbacks).add(callback);
          onDispose(() => {
            __privateGet(this, _transitionCallbacks).delete(callback);
          });
        }
      });
    }
    onAttach(el) {
      el.style.setProperty("display", "contents");
    }
    onConnect(el) {
      effect(__privateMethod(this, _Menu_instances, watchExpanded_fn).bind(this));
      if (this.isSubmenu) __privateGet(this, _parentMenu)?.addSubmenu(this);
    }
    onDestroy() {
      __privateGet(this, _trigger2).set(null);
      __privateGet(this, _content2).set(null);
      __privateSet(this, _menuObserver, null);
      __privateGet(this, _transitionCallbacks).clear();
    }
    /**
    * Open this menu. The first menu item will be focused if a `KeyboardEvent` trigger is provided
    */
    open(trigger) {
      if (peek(__privateGet(this, _expanded))) return;
      __privateGet(this, _popper).show(trigger);
      tick();
    }
    /**
    * Close this menu. The menu button that controls this menu will be focused if a `KeyboardEvent`
    * trigger is provided
    */
    close(trigger) {
      if (!peek(__privateGet(this, _expanded))) return;
      __privateGet(this, _popper).hide(trigger);
      tick();
    }
  }, _media35 = new WeakMap(), _menuId = new WeakMap(), _menuButtonId = new WeakMap(), _expanded = new WeakMap(), _disabled = new WeakMap(), _trigger2 = new WeakMap(), _content2 = new WeakMap(), _parentMenu = new WeakMap(), _submenus = new WeakMap(), _menuObserver = new WeakMap(), _popper = new WeakMap(), _focus = new WeakMap(), _isSliderActive = new WeakMap(), _isTriggerDisabled = new WeakMap(), _transitionCallbacks = new WeakMap(), _Menu_instances = new WeakSet(), observeSliders_fn = function() {
    let sliderActiveTimer = -1, parentSliderObserver = hasProvidedContext(sliderObserverContext) ? useContext2(sliderObserverContext) : null;
    provideContext(sliderObserverContext, {
      onDragStart: () => {
        parentSliderObserver?.onDragStart?.();
        window.clearTimeout(sliderActiveTimer);
        sliderActiveTimer = -1;
        __privateSet(this, _isSliderActive, true);
      },
      onDragEnd: () => {
        parentSliderObserver?.onDragEnd?.();
        sliderActiveTimer = window.setTimeout(() => {
          __privateSet(this, _isSliderActive, false);
          sliderActiveTimer = -1;
        }, 300);
      }
    });
  }, watchExpanded_fn = function() {
    const expanded = __privateMethod(this, _Menu_instances, isExpanded_fn).call(this);
    if (!this.isSubmenu) __privateGet(this, _onResize).call(this);
    __privateMethod(this, _Menu_instances, updateMenuItemsHidden_fn).call(this, expanded);
    if (!expanded) return;
    effect(() => {
      const { height } = __privateGet(this, _media35).$state, content = __privateGet(this, _content2).call(this);
      content && setStyle(content, "--player-height", height() + "px");
    });
    __privateGet(this, _focus).listen();
    this.listen("pointerup", __privateMethod(this, _Menu_instances, onPointerUp_fn).bind(this));
    listenEvent(window, "pointerup", __privateMethod(this, _Menu_instances, onWindowPointerUp_fn).bind(this));
  }, attachMenuButton_fn = function(button) {
    const el = button.el, isMenuItem = this.isSubmenu, isARIADisabled = $ariaBool(__privateMethod(this, _Menu_instances, isDisabled_fn6).bind(this));
    setAttributeIfEmpty(el, "tabindex", isMenuItem ? "-1" : "0");
    setAttributeIfEmpty(el, "role", isMenuItem ? "menuitem" : "button");
    setAttribute(el, "id", __privateGet(this, _menuButtonId));
    setAttribute(el, "aria-haspopup", "menu");
    setAttribute(el, "aria-expanded", "false");
    setAttribute(el, "data-root", !this.isSubmenu);
    setAttribute(el, "data-submenu", this.isSubmenu);
    const watchAttrs = () => {
      setAttribute(el, "data-open", __privateGet(this, _expanded).call(this));
      setAttribute(el, "aria-disabled", isARIADisabled());
    };
    if (IS_SERVER2) watchAttrs();
    else effect(watchAttrs);
    __privateGet(this, _trigger2).set(el);
    onDispose(() => {
      __privateGet(this, _trigger2).set(null);
    });
  }, attachMenuItems_fn = function(items) {
    const el = items.el;
    el.style.setProperty("display", "none");
    setAttribute(el, "id", __privateGet(this, _menuId));
    setAttributeIfEmpty(el, "role", "menu");
    setAttributeIfEmpty(el, "tabindex", "-1");
    setAttribute(el, "data-root", !this.isSubmenu);
    setAttribute(el, "data-submenu", this.isSubmenu);
    __privateGet(this, _content2).set(el);
    onDispose(() => __privateGet(this, _content2).set(null));
    const watchAttrs = () => setAttribute(el, "data-open", __privateGet(this, _expanded).call(this));
    if (IS_SERVER2) watchAttrs();
    else effect(watchAttrs);
    __privateGet(this, _focus).attachMenu(el);
    __privateMethod(this, _Menu_instances, updateMenuItemsHidden_fn).call(this, false);
    const onTransition = __privateMethod(this, _Menu_instances, onResizeTransition_fn).bind(this);
    if (!this.isSubmenu) {
      items.listen("transitionstart", onTransition);
      items.listen("transitionend", onTransition);
      items.listen("animationend", __privateGet(this, _onResize));
      items.listen("vds-menu-resize", __privateGet(this, _onResize));
    } else __privateGet(this, _parentMenu)?.onTransitionEvent(onTransition);
  }, attachObserver_fn = function(observer) {
    __privateSet(this, _menuObserver, observer);
  }, updateMenuItemsHidden_fn = function(expanded) {
    const content = peek(__privateGet(this, _content2));
    if (content) setAttribute(content, "aria-hidden", ariaBool(!expanded));
  }, disableMenuButton_fn = function(disabled) {
    __privateGet(this, _isTriggerDisabled).set(disabled);
  }, _wasKeyboardExpand = new WeakMap(), onExpandedChange_fn = function(isExpanded, event) {
    __privateSet(this, _wasKeyboardExpand, isKeyboardEvent(event));
    event?.stopPropagation();
    if (__privateGet(this, _expanded).call(this) === isExpanded) return;
    if (__privateMethod(this, _Menu_instances, isDisabled_fn6).call(this)) {
      if (isExpanded) __privateGet(this, _popper).hide(event);
      return;
    }
    this.el?.dispatchEvent(new Event("vds-menu-resize", {
      bubbles: true,
      composed: true
    }));
    const trigger = __privateGet(this, _trigger2).call(this), content = __privateGet(this, _content2).call(this);
    if (trigger) {
      setAttribute(trigger, "aria-controls", isExpanded && __privateGet(this, _menuId));
      setAttribute(trigger, "aria-expanded", ariaBool(isExpanded));
    }
    if (content) setAttribute(content, "aria-labelledby", isExpanded && __privateGet(this, _menuButtonId));
    __privateGet(this, _expanded).set(isExpanded);
    __privateMethod(this, _Menu_instances, toggleMediaControls_fn).call(this, event);
    tick();
    if (__privateGet(this, _wasKeyboardExpand)) {
      if (isExpanded) content?.focus();
      else trigger?.focus();
      for (const el of [this.el, content]) el && el.setAttribute("data-keyboard", "");
    } else for (const el of [this.el, content]) el && el.removeAttribute("data-keyboard");
    this.dispatch(isExpanded ? "open" : "close", { trigger: event });
    if (isExpanded) {
      if (!this.isSubmenu && __privateGet(this, _media35).activeMenu !== this) {
        __privateGet(this, _media35).activeMenu?.close(event);
        __privateGet(this, _media35).activeMenu = this;
      }
      __privateGet(this, _menuObserver)?.onOpen?.(event);
    } else {
      if (this.isSubmenu) for (const el of __privateGet(this, _submenus)) el.close(event);
      else __privateGet(this, _media35).activeMenu = null;
      __privateGet(this, _menuObserver)?.onClose?.(event);
    }
    if (isExpanded) requestAnimationFrame(__privateMethod(this, _Menu_instances, updateFocus_fn).bind(this));
  }, updateFocus_fn = function() {
    if (__privateGet(this, _isTransitionActive) || __privateGet(this, _isSubmenuOpen)) return;
    __privateGet(this, _focus).update();
    requestAnimationFrame(() => {
      if (__privateGet(this, _wasKeyboardExpand)) __privateGet(this, _focus).focusActive();
      else __privateGet(this, _focus).scroll();
    });
  }, isExpanded_fn = function() {
    return !__privateMethod(this, _Menu_instances, isDisabled_fn6).call(this) && __privateGet(this, _expanded).call(this);
  }, isDisabled_fn6 = function() {
    return __privateGet(this, _disabled).call(this) || __privateGet(this, _isTriggerDisabled).call(this);
  }, disable_fn = function(disabled) {
    __privateGet(this, _disabled).set(disabled);
  }, onPointerUp_fn = function(event) {
    const content = __privateGet(this, _content2).call(this);
    if (__privateGet(this, _isSliderActive) || content && isEventInside(content, event)) return;
    event.stopPropagation();
  }, onWindowPointerUp_fn = function(event) {
    const content = __privateGet(this, _content2).call(this);
    if (__privateGet(this, _isSliderActive) || content && isEventInside(content, event)) return;
    this.close(event);
  }, getCloseTarget_fn = function() {
    const target = this.el?.querySelector('[data-part="close-target"]');
    return this.el && target && isElementParent(this.el, target, (node) => node.getAttribute("role") === "menu") ? target : null;
  }, toggleMediaControls_fn = function(trigger) {
    if (this.isSubmenu) return;
    if (__privateGet(this, _expanded).call(this)) __privateGet(this, _media35).remote.pauseControls(trigger);
    else __privateGet(this, _media35).remote.resumeControls(trigger);
  }, addSubmenu_fn = function(menu) {
    __privateGet(this, _submenus).add(menu);
    new EventsController(menu).add("open", __privateGet(this, _onSubmenuOpenBind)).add("close", __privateGet(this, _onSubmenuCloseBind));
    onDispose(__privateGet(this, _removeSubmenuBind));
  }, _removeSubmenuBind = new WeakMap(), removeSubmenu_fn = function(menu) {
    __privateGet(this, _submenus).delete(menu);
  }, _isSubmenuOpen = new WeakMap(), _onSubmenuOpenBind = new WeakMap(), onSubmenuOpen_fn = function(event) {
    __privateSet(this, _isSubmenuOpen, true);
    const content = __privateGet(this, _content2).call(this);
    if (this.isSubmenu) this.triggerElement?.setAttribute("aria-hidden", "true");
    for (const target of __privateGet(this, _submenus)) if (target !== event.target) for (const el of [target.el, target.triggerElement]) el?.setAttribute("aria-hidden", "true");
    if (content) {
      const el = event.target.el;
      for (const child of content.children) if (child.contains(el)) child.setAttribute("data-open", "");
      else if (child !== el) child.setAttribute("data-hidden", "");
    }
  }, _onSubmenuCloseBind = new WeakMap(), onSubmenuClose_fn = function(event) {
    __privateSet(this, _isSubmenuOpen, false);
    const content = __privateGet(this, _content2).call(this);
    if (this.isSubmenu) this.triggerElement?.setAttribute("aria-hidden", "false");
    for (const target of __privateGet(this, _submenus)) for (const el of [target.el, target.triggerElement]) el?.setAttribute("aria-hidden", "false");
    if (content) for (const child of content.children) {
      child.removeAttribute("data-open");
      child.removeAttribute("data-hidden");
    }
  }, _onResize = new WeakMap(), _isTransitionActive = new WeakMap(), onResizeTransition_fn = function(event) {
    const content = __privateGet(this, _content2).call(this);
    if (content && event.propertyName === "height") {
      __privateSet(this, _isTransitionActive, event.type === "transitionstart");
      setAttribute(content, "data-transition", __privateGet(this, _isTransitionActive) ? "height" : null);
      if (__privateGet(this, _expanded).call(this)) __privateMethod(this, _Menu_instances, updateFocus_fn).call(this);
    }
    for (const callback of __privateGet(this, _transitionCallbacks)) callback(event);
  }, __publicField(_a94, "props", { showDelay: 0 }), _a94);
  __decorate([prop], Menu.prototype, "triggerElement", null);
  __decorate([prop], Menu.prototype, "contentElement", null);
  __decorate([prop], Menu.prototype, "isSubmenu", null);
  __decorate([method], Menu.prototype, "open", null);
  __decorate([method], Menu.prototype, "close", null);
  var _a95, _menu, _hintEl, _MenuButton_instances, watchDisabled_fn2, watchHintEl_fn, onMutation_fn2;
  var MenuButton = (_a95 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _MenuButton_instances);
      __privateAdd(this, _menu);
      __privateAdd(this, _hintEl, signal(null));
      new FocusVisibleController();
    }
    get expanded() {
      return __privateGet(this, _menu)?.expanded() ?? false;
    }
    onSetup() {
      __privateSet(this, _menu, useContext2(menuContext));
    }
    onAttach(el) {
      __privateGet(this, _menu).attachMenuButton(this);
      effect(__privateMethod(this, _MenuButton_instances, watchDisabled_fn2).bind(this));
      setAttributeIfEmpty(el, "type", "button");
    }
    onConnect(el) {
      effect(__privateMethod(this, _MenuButton_instances, watchHintEl_fn).bind(this));
      __privateMethod(this, _MenuButton_instances, onMutation_fn2).call(this);
      const mutations = new MutationObserver(__privateMethod(this, _MenuButton_instances, onMutation_fn2).bind(this));
      mutations.observe(el, {
        attributeFilter: ["data-part"],
        childList: true,
        subtree: true
      });
      onDispose(() => mutations.disconnect());
      onPress(el, (trigger) => {
        this.dispatch("select", { trigger });
      });
    }
  }, _menu = new WeakMap(), _hintEl = new WeakMap(), _MenuButton_instances = new WeakSet(), watchDisabled_fn2 = function() {
    __privateGet(this, _menu).disableMenuButton(this.$props.disabled());
  }, watchHintEl_fn = function() {
    const el = __privateGet(this, _hintEl).call(this);
    if (!el) return;
    effect(() => {
      const text = __privateGet(this, _menu).hint();
      if (text) el.textContent = text;
    });
  }, onMutation_fn2 = function() {
    const hintEl = this.el?.querySelector('[data-part="hint"]');
    __privateGet(this, _hintEl).set(hintEl ?? null);
  }, __publicField(_a95, "props", { disabled: false }), _a95);
  var menubutton__proto = MenuButton.prototype;
  prop(menubutton__proto, "expanded");
  var MenuItem2 = class extends MenuButton {
  };
  var menuPortalContext = createContext2();
  var _a96, _menu2, _MenuItems_instances, watchPlacement_fn2, hide_fn, getButton_fn2;
  var MenuItems = (_a96 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _MenuItems_instances);
      __privateAdd(this, _menu2);
      new FocusVisibleController();
      const { placement } = this.$props;
      this.setAttributes({ "data-placement": placement });
    }
    onAttach(el) {
      __privateSet(this, _menu2, useContext2(menuContext));
      __privateGet(this, _menu2).attachMenuItems(this);
      if (hasProvidedContext(menuPortalContext)) {
        const portal = useContext2(menuPortalContext);
        if (portal) {
          provideContext(menuPortalContext, null);
          portal.attach(el);
          onDispose(() => portal.attach(null));
        }
      }
    }
    onConnect(el) {
      effect(__privateMethod(this, _MenuItems_instances, watchPlacement_fn2).bind(this));
    }
  }, _menu2 = new WeakMap(), _MenuItems_instances = new WeakSet(), watchPlacement_fn2 = function() {
    const { expanded } = __privateGet(this, _menu2);
    if (!this.el || !expanded()) return;
    const placement = this.$props.placement();
    if (!placement) return;
    Object.assign(this.el.style, {
      position: "absolute",
      top: 0,
      left: 0,
      width: "max-content"
    });
    const { offset: mainOffset, alignOffset } = this.$props;
    onDispose(autoPlacement2(this.el, __privateMethod(this, _MenuItems_instances, getButton_fn2).call(this), placement, {
      offsetVarName: "media-menu",
      xOffset: alignOffset(),
      yOffset: mainOffset()
    }));
    onDispose(__privateMethod(this, _MenuItems_instances, hide_fn).bind(this));
  }, hide_fn = function() {
    if (!this.el) return;
    this.el.removeAttribute("style");
    this.el.style.display = "none";
  }, getButton_fn2 = function() {
    return __privateGet(this, _menu2).button();
  }, __publicField(_a96, "props", {
    placement: null,
    offset: 0,
    alignOffset: 0
  }), _a96);
  var radioControllerContext = createContext2();
  var _group, _value, _controller4, _RadioGroupController_instances, addRadio_fn, removeRadio_fn, _onChangeBind, onChange_fn4, findRadio_fn, _a97;
  var RadioGroupController = (_a97 = class extends ViewController {
    constructor() {
      super(...arguments);
      __privateAdd(this, _RadioGroupController_instances);
      __privateAdd(this, _group, /* @__PURE__ */ new Set());
      __privateAdd(this, _value, signal(""));
      __privateAdd(this, _controller4, null);
      __publicField(this, "onValueChange");
      __privateAdd(this, _onChangeBind, __privateMethod(this, _RadioGroupController_instances, onChange_fn4).bind(this));
    }
    get values() {
      return Array.from(__privateGet(this, _group)).map((radio) => radio.value());
    }
    get value() {
      return __privateGet(this, _value).call(this);
    }
    set value(value) {
      __privateMethod(this, _RadioGroupController_instances, onChange_fn4).call(this, value);
    }
    onSetup() {
      provideContext(radioControllerContext, {
        add: __privateMethod(this, _RadioGroupController_instances, addRadio_fn).bind(this),
        remove: __privateMethod(this, _RadioGroupController_instances, removeRadio_fn).bind(this)
      });
    }
    onAttach(el) {
      if (!hasProvidedContext(menuContext)) setAttributeIfEmpty(el, "role", "radiogroup");
      this.setAttributes({ value: __privateGet(this, _value) });
    }
    onDestroy() {
      __privateGet(this, _group).clear();
    }
  }, _group = new WeakMap(), _value = new WeakMap(), _controller4 = new WeakMap(), _RadioGroupController_instances = new WeakSet(), addRadio_fn = function(radio) {
    if (__privateGet(this, _group).has(radio)) return;
    __privateGet(this, _group).add(radio);
    radio.onCheck = __privateGet(this, _onChangeBind);
    radio.check(radio.value() === __privateGet(this, _value).call(this));
  }, removeRadio_fn = function(radio) {
    radio.onCheck = null;
    __privateGet(this, _group).delete(radio);
  }, _onChangeBind = new WeakMap(), onChange_fn4 = function(newValue, trigger) {
    const currentValue = peek(__privateGet(this, _value));
    if (!newValue || newValue === currentValue) return;
    const currentRadio = __privateMethod(this, _RadioGroupController_instances, findRadio_fn).call(this, currentValue), newRadio = __privateMethod(this, _RadioGroupController_instances, findRadio_fn).call(this, newValue);
    currentRadio?.check(false, trigger);
    newRadio?.check(true, trigger);
    __privateGet(this, _value).set(newValue);
    this.onValueChange?.(newValue, trigger);
  }, findRadio_fn = function(newValue) {
    for (const radio of __privateGet(this, _group)) if (newValue === peek(radio.value)) return radio;
    return null;
  }, _a97);
  var _a98, _controller5, _RadioGroup_instances, watchValue_fn3, onValueChange_fn6;
  var RadioGroup = (_a98 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _RadioGroup_instances);
      __privateAdd(this, _controller5);
      __privateSet(this, _controller5, new RadioGroupController());
      __privateGet(this, _controller5).onValueChange = __privateMethod(this, _RadioGroup_instances, onValueChange_fn6).bind(this);
    }
    /**
    * A list of radio values that belong this group.
    */
    get values() {
      return __privateGet(this, _controller5).values;
    }
    /**
    * The radio value that is checked in this group.
    */
    get value() {
      return __privateGet(this, _controller5).value;
    }
    set value(newValue) {
      __privateGet(this, _controller5).value = newValue;
    }
    onSetup() {
      if (IS_SERVER2) __privateMethod(this, _RadioGroup_instances, watchValue_fn3).call(this);
      else effect(__privateMethod(this, _RadioGroup_instances, watchValue_fn3).bind(this));
    }
  }, _controller5 = new WeakMap(), _RadioGroup_instances = new WeakSet(), watchValue_fn3 = function() {
    __privateGet(this, _controller5).value = this.$props.value();
  }, onValueChange_fn6 = function(value, trigger) {
    const event = this.createEvent("change", {
      detail: value,
      trigger
    });
    this.dispatch(event);
  }, __publicField(_a98, "props", { value: "" }), _a98);
  var radiogroup__proto = RadioGroup.prototype;
  prop(radiogroup__proto, "values");
  prop(radiogroup__proto, "value");
  var _a99, _checked, _controller6, _Radio_instances, onDisconnect_fn5, addToGroup_fn, watchValue_fn4, onPress_fn10, check_fn, onChange_fn5, onSelect_fn;
  var Radio = (_a99 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _Radio_instances);
      __privateAdd(this, _checked, signal(false));
      __privateAdd(this, _controller6, {
        value: this.$props.value,
        check: __privateMethod(this, _Radio_instances, check_fn).bind(this),
        onCheck: null
      });
      new FocusVisibleController();
    }
    /**
    * Whether this radio is currently checked.
    */
    get checked() {
      return __privateGet(this, _checked).call(this);
    }
    onSetup() {
      this.setAttributes({
        value: this.$props.value,
        "data-checked": __privateGet(this, _checked),
        "aria-checked": $ariaBool(__privateGet(this, _checked))
      });
    }
    onAttach(el) {
      const isMenuItem = hasProvidedContext(menuContext);
      setAttributeIfEmpty(el, "tabindex", isMenuItem ? "-1" : "0");
      setAttributeIfEmpty(el, "role", isMenuItem ? "menuitemradio" : "radio");
      effect(__privateMethod(this, _Radio_instances, watchValue_fn4).bind(this));
    }
    onConnect(el) {
      __privateMethod(this, _Radio_instances, addToGroup_fn).call(this);
      onPress(el, __privateMethod(this, _Radio_instances, onPress_fn10).bind(this));
      onDispose(__privateMethod(this, _Radio_instances, onDisconnect_fn5).bind(this));
    }
  }, _checked = new WeakMap(), _controller6 = new WeakMap(), _Radio_instances = new WeakSet(), onDisconnect_fn5 = function() {
    scoped(() => {
      useContext2(radioControllerContext).remove(__privateGet(this, _controller6));
    }, this.connectScope);
  }, addToGroup_fn = function() {
    useContext2(radioControllerContext).add(__privateGet(this, _controller6));
  }, watchValue_fn4 = function() {
    const { value } = this.$props, newValue = value();
    if (peek(__privateGet(this, _checked))) __privateGet(this, _controller6).onCheck?.(newValue);
  }, onPress_fn10 = function(event) {
    if (peek(__privateGet(this, _checked))) return;
    __privateMethod(this, _Radio_instances, onChange_fn5).call(this, true, event);
    __privateMethod(this, _Radio_instances, onSelect_fn).call(this, event);
    __privateGet(this, _controller6).onCheck?.(peek(this.$props.value), event);
  }, check_fn = function(value, trigger) {
    if (peek(__privateGet(this, _checked)) === value) return;
    __privateMethod(this, _Radio_instances, onChange_fn5).call(this, value, trigger);
  }, onChange_fn5 = function(value, trigger) {
    __privateGet(this, _checked).set(value);
    this.dispatch("change", {
      detail: value,
      trigger
    });
  }, onSelect_fn = function(trigger) {
    this.dispatch("select", { trigger });
  }, __publicField(_a99, "props", { value: "" }), _a99);
  var radio__proto = Radio.prototype;
  prop(radio__proto, "checked");
  var _a100, _media36, _provider4, _touchStart, _Gesture_instances, attachListener_fn, _presses, _pressTimerId, acceptEvent_fn, onTouchStart_fn2, isTouchScroll_fn, handleEvent_fn, inBounds_fn, isTopLayer_fn, performAction_fn;
  var Gesture = (_a100 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _Gesture_instances);
      __privateAdd(this, _media36);
      __privateAdd(this, _provider4, null);
      __privateAdd(this, _touchStart, null);
      __privateAdd(this, _presses, 0);
      __privateAdd(this, _pressTimerId, -1);
    }
    onSetup() {
      __privateSet(this, _media36, useMediaContext());
      const { event, action } = this.$props;
      this.setAttributes({
        event,
        action
      });
    }
    onAttach(el) {
      el.setAttribute("data-media-gesture", "");
      el.style.setProperty("pointer-events", "none");
    }
    onConnect(el) {
      __privateSet(this, _provider4, __privateGet(this, _media36).player.el?.querySelector("[data-media-provider]"));
      effect(__privateMethod(this, _Gesture_instances, attachListener_fn).bind(this));
    }
  }, _media36 = new WeakMap(), _provider4 = new WeakMap(), _touchStart = new WeakMap(), _Gesture_instances = new WeakSet(), attachListener_fn = function() {
    let eventType = this.$props.event(), disabled = this.$props.disabled();
    if (!__privateGet(this, _provider4) || !eventType || disabled) return;
    if (/^dbl/.test(eventType)) eventType = eventType.split(/^dbl/)[1];
    if (eventType === "pointerup" || eventType === "pointerdown") {
      if (__privateGet(this, _media36).$state.pointer() === "coarse") {
        if (eventType === "pointerup") listenEvent(__privateGet(this, _provider4), "touchstart", __privateMethod(this, _Gesture_instances, onTouchStart_fn2).bind(this), { passive: true });
        eventType = eventType === "pointerup" ? "touchend" : "touchstart";
      }
    }
    listenEvent(__privateGet(this, _provider4), eventType, __privateMethod(this, _Gesture_instances, acceptEvent_fn).bind(this), { passive: false });
  }, _presses = new WeakMap(), _pressTimerId = new WeakMap(), acceptEvent_fn = function(event) {
    if (this.$props.disabled() || isPointerEvent(event) && (event.button !== 0 || __privateGet(this, _media36).activeMenu) || isTouchEvent(event) && __privateGet(this, _media36).activeMenu || __privateMethod(this, _Gesture_instances, isTouchScroll_fn).call(this, event) || isTouchPinchEvent(event) || !__privateMethod(this, _Gesture_instances, inBounds_fn).call(this, event)) return;
    event.MEDIA_GESTURE = true;
    event.preventDefault();
    if (!peek(this.$props.event)?.startsWith("dbl")) {
      if (__privateGet(this, _presses) === 0) setTimeout(() => {
        if (__privateGet(this, _presses) === 1) __privateMethod(this, _Gesture_instances, handleEvent_fn).call(this, event);
      }, 250);
    } else if (__privateGet(this, _presses) === 1) {
      queueMicrotask(() => __privateMethod(this, _Gesture_instances, handleEvent_fn).call(this, event));
      clearTimeout(__privateGet(this, _pressTimerId));
      __privateSet(this, _presses, 0);
      return;
    }
    if (__privateGet(this, _presses) === 0) __privateSet(this, _pressTimerId, window.setTimeout(() => {
      __privateSet(this, _presses, 0);
    }, 275));
    __privateWrapper(this, _presses)._++;
  }, onTouchStart_fn2 = function(event) {
    if (isTouchPinchEvent(event)) {
      __privateSet(this, _touchStart, null);
      return;
    }
    const touch = event.changedTouches[0] ?? event.touches[0];
    __privateSet(this, _touchStart, touch ? {
      x: touch.clientX,
      y: touch.clientY
    } : null);
  }, isTouchScroll_fn = function(event) {
    if (!isTouchEvent(event) || event.type !== "touchend") return false;
    const touch = event.changedTouches[0] ?? event.touches[0], start = __privateGet(this, _touchStart);
    __privateSet(this, _touchStart, null);
    if (!touch || !start) return false;
    return Math.abs(touch.clientX - start.x) > 10 || Math.abs(touch.clientY - start.y) > 10;
  }, handleEvent_fn = function(event) {
    this.el.setAttribute("data-triggered", "");
    requestAnimationFrame(() => {
      if (__privateMethod(this, _Gesture_instances, isTopLayer_fn).call(this)) __privateMethod(this, _Gesture_instances, performAction_fn).call(this, peek(this.$props.action), event);
      requestAnimationFrame(() => {
        this.el.removeAttribute("data-triggered");
      });
    });
  }, /** Validate event occurred in gesture bounds. */
  inBounds_fn = function(event) {
    if (!this.el) return false;
    if (isPointerEvent(event) || isMouseEvent(event) || isTouchEvent(event)) {
      const touch = isTouchEvent(event) ? event.changedTouches[0] ?? event.touches[0] : void 0;
      const clientX = touch?.clientX ?? event.clientX;
      const clientY = touch?.clientY ?? event.clientY;
      const rect = this.el.getBoundingClientRect();
      const inBounds = clientY >= rect.top && clientY <= rect.bottom && clientX >= rect.left && clientX <= rect.right;
      return event.type.includes("leave") ? !inBounds : inBounds;
    }
    return true;
  }, /** Validate gesture has the highest z-index in this triggered group. */
  isTopLayer_fn = function() {
    const gestures = __privateGet(this, _media36).player.el.querySelectorAll("[data-media-gesture][data-triggered]");
    return Array.from(gestures).sort((a, b) => +getComputedStyle(b).zIndex - +getComputedStyle(a).zIndex)[0] === this.el;
  }, performAction_fn = function(action, trigger) {
    if (!action) return;
    const willTriggerEvent = new DOMEvent("will-trigger", {
      detail: action,
      cancelable: true,
      trigger
    });
    this.dispatchEvent(willTriggerEvent);
    if (willTriggerEvent.defaultPrevented) return;
    const [method2, value] = action.replace(/:([a-z])/, "-$1").split(":");
    if (action.includes(":fullscreen")) __privateGet(this, _media36).remote.toggleFullscreen("prefer-media", trigger);
    else if (action.includes("seek:")) __privateGet(this, _media36).remote.seek(peek(__privateGet(this, _media36).$state.currentTime) + (+value || 0), trigger);
    else __privateGet(this, _media36).remote[kebabToCamelCase(method2)](trigger);
    this.dispatch("trigger", {
      detail: action,
      trigger
    });
  }, __publicField(_a100, "props", {
    disabled: false,
    event: void 0,
    action: void 0
  }), _a100);
  var _track2, _renderer, _events2, _CaptionsTextRenderer_instances, changeTrack_fn, _a101;
  var CaptionsTextRenderer = (_a101 = class {
    constructor(renderer) {
      __privateAdd(this, _CaptionsTextRenderer_instances);
      __publicField(this, "priority", 10);
      __privateAdd(this, _track2, null);
      __privateAdd(this, _renderer);
      __privateAdd(this, _events2);
      __privateSet(this, _renderer, renderer);
    }
    attach() {
    }
    canRender() {
      return true;
    }
    detach() {
      __privateGet(this, _events2)?.abort();
      __privateSet(this, _events2, void 0);
      __privateGet(this, _renderer).reset();
      __privateSet(this, _track2, null);
    }
    changeTrack(track) {
      if (!track || __privateGet(this, _track2) === track) return;
      __privateGet(this, _events2)?.abort();
      __privateSet(this, _events2, new EventsController(track));
      if (track.readyState < 2) {
        __privateGet(this, _renderer).reset();
        __privateGet(this, _events2).add("load", () => __privateMethod(this, _CaptionsTextRenderer_instances, changeTrack_fn).call(this, track), { once: true });
      } else __privateMethod(this, _CaptionsTextRenderer_instances, changeTrack_fn).call(this, track);
      __privateGet(this, _events2).add("add-cue", (event) => {
        __privateGet(this, _renderer).addCue(event.detail);
      }).add("remove-cue", (event) => {
        __privateGet(this, _renderer).removeCue(event.detail);
      });
      __privateSet(this, _track2, track);
    }
  }, _track2 = new WeakMap(), _renderer = new WeakMap(), _events2 = new WeakMap(), _CaptionsTextRenderer_instances = new WeakSet(), changeTrack_fn = function(track) {
    __privateGet(this, _renderer).changeTrack({
      cues: [...track.cues],
      regions: [...track.regions]
    });
  }, _a101);
  var _a102, _media37, _Captions_instances, isHidden_fn2, watchViewType_fn, setupAudioView_fn, onTrackChange_fn3, onCueChange_fn, onUpdateTimedNodes_fn, setupVideoView_fn, watchTextDirection_fn, watchMediaTime_fn, listenToFontStyleChanges_fn, onFontStyleChange_fn, showExample_fn, _hideExampleTimer, hideExample_fn, removeExample_fn, createCueDisplayElement_fn, createCueElement_fn;
  var Captions = (_a102 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _Captions_instances);
      __privateAdd(this, _media37);
      __privateAdd(this, _hideExampleTimer, -1);
    }
    onSetup() {
      __privateSet(this, _media37, useMediaContext());
      this.setAttributes({ "aria-hidden": $ariaBool(__privateMethod(this, _Captions_instances, isHidden_fn2).bind(this)) });
    }
    onAttach(el) {
      el.style.setProperty("pointer-events", "none");
    }
    onConnect(el) {
      if (!_a102.lib()) Promise.resolve().then(() => (init_prod2(), prod_exports)).then((lib) => _a102.lib.set(lib));
      effect(__privateMethod(this, _Captions_instances, watchViewType_fn).bind(this));
    }
  }, _media37 = new WeakMap(), _Captions_instances = new WeakSet(), isHidden_fn2 = function() {
    const { textTrack, remotePlaybackState, iOSControls } = __privateGet(this, _media37).$state, track = textTrack();
    return iOSControls() || remotePlaybackState() === "connected" || !track || !isTrackCaptionKind(track);
  }, watchViewType_fn = function() {
    if (!_a102.lib()) return;
    const { viewType } = __privateGet(this, _media37).$state;
    if (viewType() === "audio") return __privateMethod(this, _Captions_instances, setupAudioView_fn).call(this);
    else return __privateMethod(this, _Captions_instances, setupVideoView_fn).call(this);
  }, setupAudioView_fn = function() {
    effect(__privateMethod(this, _Captions_instances, onTrackChange_fn3).bind(this));
    __privateMethod(this, _Captions_instances, listenToFontStyleChanges_fn).call(this, null);
    return () => {
      this.el.textContent = "";
    };
  }, onTrackChange_fn3 = function() {
    if (__privateMethod(this, _Captions_instances, isHidden_fn2).call(this)) return;
    __privateMethod(this, _Captions_instances, onCueChange_fn).call(this);
    const { textTrack } = __privateGet(this, _media37).$state;
    listenEvent(textTrack(), "cue-change", __privateMethod(this, _Captions_instances, onCueChange_fn).bind(this));
    effect(__privateMethod(this, _Captions_instances, onUpdateTimedNodes_fn).bind(this));
  }, onCueChange_fn = function() {
    this.el.textContent = "";
    if (__privateGet(this, _hideExampleTimer) >= 0) __privateMethod(this, _Captions_instances, removeExample_fn).call(this);
    const { realCurrentTime, textTrack } = __privateGet(this, _media37).$state, { renderVTTCueString: renderVTTCueString2 } = _a102.lib(), time = peek(realCurrentTime), activeCues = peek(textTrack).activeCues;
    for (const cue of activeCues) {
      const displayEl = __privateMethod(this, _Captions_instances, createCueDisplayElement_fn).call(this), cueEl = __privateMethod(this, _Captions_instances, createCueElement_fn).call(this);
      cueEl.innerHTML = renderVTTCueString2(cue, time);
      displayEl.append(cueEl);
      this.el.append(cueEl);
    }
  }, onUpdateTimedNodes_fn = function() {
    const { realCurrentTime } = __privateGet(this, _media37).$state, { updateTimedVTTCueNodes: updateTimedVTTCueNodes2 } = _a102.lib();
    updateTimedVTTCueNodes2(this.el, realCurrentTime());
  }, setupVideoView_fn = function() {
    const { CaptionsRenderer: CaptionsRenderer2 } = _a102.lib(), renderer = new CaptionsRenderer2(this.el), textRenderer = new CaptionsTextRenderer(renderer);
    __privateGet(this, _media37).textRenderers.add(textRenderer);
    effect(__privateMethod(this, _Captions_instances, watchTextDirection_fn).bind(this, renderer));
    effect(__privateMethod(this, _Captions_instances, watchMediaTime_fn).bind(this, renderer));
    __privateMethod(this, _Captions_instances, listenToFontStyleChanges_fn).call(this, renderer);
    return () => {
      this.el.textContent = "";
      __privateGet(this, _media37).textRenderers.remove(textRenderer);
      renderer.destroy();
    };
  }, watchTextDirection_fn = function(renderer) {
    renderer.dir = this.$props.textDir();
  }, watchMediaTime_fn = function(renderer) {
    if (__privateMethod(this, _Captions_instances, isHidden_fn2).call(this)) return;
    const { realCurrentTime, textTrack } = __privateGet(this, _media37).$state;
    renderer.currentTime = realCurrentTime();
    if (__privateGet(this, _hideExampleTimer) >= 0 && textTrack()?.activeCues[0]) __privateMethod(this, _Captions_instances, removeExample_fn).call(this);
  }, listenToFontStyleChanges_fn = function(renderer) {
    const player = __privateGet(this, _media37).player;
    if (!player) return;
    listenEvent(player, "vds-font-change", __privateMethod(this, _Captions_instances, onFontStyleChange_fn).bind(this, renderer));
  }, onFontStyleChange_fn = function(renderer) {
    if (__privateGet(this, _hideExampleTimer) >= 0) {
      __privateMethod(this, _Captions_instances, hideExample_fn).call(this);
      return;
    }
    const { textTrack } = __privateGet(this, _media37).$state;
    if (!textTrack()?.activeCues[0]) __privateMethod(this, _Captions_instances, showExample_fn).call(this);
    else renderer?.update(true);
  }, showExample_fn = function() {
    const display = __privateMethod(this, _Captions_instances, createCueDisplayElement_fn).call(this);
    setAttribute(display, "data-example", "");
    const cue = __privateMethod(this, _Captions_instances, createCueElement_fn).call(this);
    setAttribute(cue, "data-example", "");
    cue.textContent = this.$props.exampleText();
    display?.append(cue);
    this.el?.append(display);
    this.el?.setAttribute("data-example", "");
    __privateMethod(this, _Captions_instances, hideExample_fn).call(this);
  }, _hideExampleTimer = new WeakMap(), hideExample_fn = function() {
    window.clearTimeout(__privateGet(this, _hideExampleTimer));
    __privateSet(this, _hideExampleTimer, window.setTimeout(__privateMethod(this, _Captions_instances, removeExample_fn).bind(this), 2500));
  }, removeExample_fn = function() {
    this.el?.removeAttribute("data-example");
    if (this.el?.querySelector("[data-example]")) this.el.textContent = "";
    __privateSet(this, _hideExampleTimer, -1);
  }, createCueDisplayElement_fn = function() {
    const el = document.createElement("div");
    setAttribute(el, "data-part", "cue-display");
    return el;
  }, createCueElement_fn = function() {
    const el = document.createElement("div");
    setAttribute(el, "data-part", "cue");
    return el;
  }, __publicField(_a102, "props", {
    textDir: "ltr",
    exampleText: "Captions look like this."
  }), __publicField(_a102, "lib", signal(null)), _a102);
  var _a103, _media38, _Poster_instances, hasError_fn3, onPreconnect_fn, watchHidden_fn4, isLoading_fn3, watchImg_fn2, _prevSrc, watchSrc_fn3, watchAlt_fn, watchCrossOrigin_fn4, onLoadStart_fn5, onLoad_fn, onError_fn9;
  var Poster = (_a103 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _Poster_instances);
      __privateAdd(this, _media38);
      __privateAdd(this, _prevSrc, "");
    }
    onSetup() {
      __privateSet(this, _media38, useMediaContext());
      __privateMethod(this, _Poster_instances, watchSrc_fn3).call(this);
      __privateMethod(this, _Poster_instances, watchAlt_fn).call(this);
      __privateMethod(this, _Poster_instances, watchCrossOrigin_fn4).call(this);
      __privateMethod(this, _Poster_instances, watchHidden_fn4).call(this);
    }
    onAttach(el) {
      el.style.setProperty("pointer-events", "none");
      effect(__privateMethod(this, _Poster_instances, watchImg_fn2).bind(this));
      effect(__privateMethod(this, _Poster_instances, watchSrc_fn3).bind(this));
      effect(__privateMethod(this, _Poster_instances, watchAlt_fn).bind(this));
      effect(__privateMethod(this, _Poster_instances, watchCrossOrigin_fn4).bind(this));
      effect(__privateMethod(this, _Poster_instances, watchHidden_fn4).bind(this));
      const { started } = __privateGet(this, _media38).$state;
      this.setAttributes({
        "data-visible": () => !started() && !this.$state.hidden(),
        "data-loading": __privateMethod(this, _Poster_instances, isLoading_fn3).bind(this),
        "data-error": __privateMethod(this, _Poster_instances, hasError_fn3).bind(this),
        "data-hidden": this.$state.hidden
      });
    }
    onConnect(el) {
      effect(__privateMethod(this, _Poster_instances, onPreconnect_fn).bind(this));
      effect(__privateMethod(this, _Poster_instances, onLoadStart_fn5).bind(this));
    }
  }, _media38 = new WeakMap(), _Poster_instances = new WeakSet(), hasError_fn3 = function() {
    const { error } = this.$state;
    return !isNull(error());
  }, onPreconnect_fn = function() {
    const { canLoadPoster, poster } = __privateGet(this, _media38).$state;
    if (!canLoadPoster() && poster()) preconnect(poster(), "preconnect");
  }, watchHidden_fn4 = function() {
    const { src } = this.$props, { poster, nativeControls } = __privateGet(this, _media38).$state;
    this.el && setAttribute(this.el, "display", nativeControls() ? "none" : null);
    this.$state.hidden.set(__privateMethod(this, _Poster_instances, hasError_fn3).call(this) || !(src() || poster()) || nativeControls());
  }, isLoading_fn3 = function() {
    const { loading, hidden } = this.$state;
    return !hidden() && loading();
  }, watchImg_fn2 = function() {
    const img = this.$state.img();
    if (!img) return;
    new EventsController(img).add("load", __privateMethod(this, _Poster_instances, onLoad_fn).bind(this)).add("error", __privateMethod(this, _Poster_instances, onError_fn9).bind(this));
    if (img.complete) __privateMethod(this, _Poster_instances, onLoad_fn).call(this);
  }, _prevSrc = new WeakMap(), watchSrc_fn3 = function() {
    const { poster: defaultPoster } = __privateGet(this, _media38).$props, { canLoadPoster, providedPoster, inferredPoster } = __privateGet(this, _media38).$state;
    const src = this.$props.src() || "", poster = src || defaultPoster() || inferredPoster();
    if (__privateGet(this, _prevSrc) === providedPoster()) providedPoster.set(src);
    this.$state.src.set(canLoadPoster() && poster.length ? poster : null);
    __privateSet(this, _prevSrc, src);
  }, watchAlt_fn = function() {
    const { src } = this.$props, { alt } = this.$state, { poster } = __privateGet(this, _media38).$state;
    alt.set(src() || poster() ? this.$props.alt() : null);
  }, watchCrossOrigin_fn4 = function() {
    const { crossOrigin: crossOriginProp } = this.$props, { crossOrigin: crossOriginState } = this.$state, { crossOrigin: mediaCrossOrigin, poster: src } = __privateGet(this, _media38).$state, crossOrigin = crossOriginProp() !== null ? crossOriginProp() : mediaCrossOrigin();
    crossOriginState.set(/ytimg\.com|vimeo/.test(src() || "") ? null : crossOrigin === true ? "anonymous" : crossOrigin);
  }, onLoadStart_fn5 = function() {
    const { loading, error } = this.$state, { canLoadPoster, poster } = __privateGet(this, _media38).$state;
    loading.set(canLoadPoster() && !!poster());
    error.set(null);
  }, onLoad_fn = function() {
    const { loading, error } = this.$state;
    loading.set(false);
    error.set(null);
  }, onError_fn9 = function(event) {
    const { loading, error } = this.$state;
    loading.set(false);
    error.set(event);
  }, __publicField(_a103, "props", {
    src: null,
    alt: null,
    crossOrigin: null
  }), __publicField(_a103, "state", new State({
    img: null,
    src: null,
    alt: null,
    crossOrigin: null,
    loading: true,
    error: null,
    hidden: false
  })), _a103);
  var _a104, _media39, _invert, _isVisible2, _isIntersecting2, _Time_instances, onIntersectionChange_fn2, watchHidden_fn5, watchToggle_fn, watchTime_fn, watchRole_fn, getSeconds_fn, shouldInvert_fn, onToggle_fn;
  var Time = (_a104 = class extends Component2 {
    constructor() {
      super(...arguments);
      __privateAdd(this, _Time_instances);
      __privateAdd(this, _media39);
      __privateAdd(this, _invert, signal(null));
      __privateAdd(this, _isVisible2, signal(true));
      __privateAdd(this, _isIntersecting2, signal(true));
    }
    onSetup() {
      __privateSet(this, _media39, useMediaContext());
      __privateMethod(this, _Time_instances, watchTime_fn).call(this);
      const { type } = this.$props;
      this.setAttributes({
        "data-type": type,
        "data-remainder": __privateMethod(this, _Time_instances, shouldInvert_fn).bind(this)
      });
      new IntersectionObserverController({ callback: __privateMethod(this, _Time_instances, onIntersectionChange_fn2).bind(this) }).attach(this);
    }
    onAttach(el) {
      if (!el.hasAttribute("role")) effect(__privateMethod(this, _Time_instances, watchRole_fn).bind(this));
      effect(__privateMethod(this, _Time_instances, watchTime_fn).bind(this));
    }
    onConnect(el) {
      onDispose(observeVisibility(el, __privateGet(this, _isVisible2).set));
      effect(__privateMethod(this, _Time_instances, watchHidden_fn5).bind(this));
      effect(__privateMethod(this, _Time_instances, watchToggle_fn).bind(this));
    }
  }, _media39 = new WeakMap(), _invert = new WeakMap(), _isVisible2 = new WeakMap(), _isIntersecting2 = new WeakMap(), _Time_instances = new WeakSet(), onIntersectionChange_fn2 = function(entries) {
    __privateGet(this, _isIntersecting2).set(entries[0].isIntersecting);
  }, watchHidden_fn5 = function() {
    const { hidden } = this.$props;
    this.$state.hidden.set(hidden() || !__privateGet(this, _isVisible2).call(this) || !__privateGet(this, _isIntersecting2).call(this));
  }, watchToggle_fn = function() {
    if (!this.$props.toggle()) {
      __privateGet(this, _invert).set(null);
      return;
    }
    if (this.el) onPress(this.el, __privateMethod(this, _Time_instances, onToggle_fn).bind(this));
  }, watchTime_fn = function() {
    const { hidden, timeText } = this.$state, { duration } = __privateGet(this, _media39).$state;
    if (hidden()) return;
    const { type, padHours, padMinutes, showHours } = this.$props, seconds = __privateMethod(this, _Time_instances, getSeconds_fn).call(this, type()), $duration = duration(), shouldInvert = __privateMethod(this, _Time_instances, shouldInvert_fn).call(this);
    if (!Number.isFinite(seconds + $duration)) {
      timeText.set("LIVE");
      return;
    }
    const formattedTime = formatTime2(shouldInvert ? Math.max(0, $duration - seconds) : seconds, {
      padHrs: padHours(),
      padMins: padMinutes(),
      showHrs: showHours()
    });
    timeText.set((shouldInvert ? "-" : "") + formattedTime);
  }, watchRole_fn = function() {
    if (!this.el) return;
    const { toggle } = this.$props;
    setAttribute(this.el, "role", toggle() ? "timer" : null);
    setAttribute(this.el, "tabindex", toggle() ? 0 : null);
  }, getSeconds_fn = function(type) {
    const { bufferedEnd, duration, currentTime } = __privateGet(this, _media39).$state;
    switch (type) {
      case "buffered":
        return bufferedEnd();
      case "duration":
        return duration();
      default:
        return currentTime();
    }
  }, shouldInvert_fn = function() {
    return this.$props.remainder() && __privateGet(this, _invert).call(this) !== false;
  }, onToggle_fn = function(event) {
    event.preventDefault();
    if (__privateGet(this, _invert).call(this) === null) {
      __privateGet(this, _invert).set(!this.$props.remainder());
      return;
    }
    __privateGet(this, _invert).set((v) => !v);
  }, __publicField(_a104, "props", {
    type: "current",
    showHours: false,
    padHours: null,
    padMinutes: null,
    remainder: false,
    toggle: false,
    hidden: false
  }), __publicField(_a104, "state", new State({
    timeText: "",
    hidden: false
  })), _a104);
  var MediaPlayerInstance = class extends MediaPlayer {
  };
  var MediaProviderInstance = class extends MediaProvider {
  };
  var MediaAnnouncerInstance = class extends MediaAnnouncer {
  };
  var ControlsInstance = class extends Controls {
  };
  var ControlsGroupInstance = class extends ControlsGroup {
  };
  var ToggleButtonInstance = class extends ToggleButton {
  };
  var CaptionButtonInstance = class extends CaptionButton {
  };
  var FullscreenButtonInstance = class extends FullscreenButton {
  };
  var LiveButtonInstance = class extends LiveButton {
  };
  var MuteButtonInstance = class extends MuteButton {
  };
  var PIPButtonInstance = class extends PIPButton {
  };
  var PlayButtonInstance = class extends PlayButton {
  };
  var AirPlayButtonInstance = class extends AirPlayButton {
  };
  var GoogleCastButtonInstance = class extends GoogleCastButton {
  };
  var SeekButtonInstance = class extends SeekButton {
  };
  var TooltipInstance = class extends Tooltip {
  };
  var TooltipTriggerInstance = class extends TooltipTrigger {
  };
  var TooltipContentInstance = class extends TooltipContent {
  };
  var SliderInstance = class extends Slider {
  };
  var TimeSliderInstance = class extends TimeSlider {
  };
  var VolumeSliderInstance = class extends VolumeSlider {
  };
  var AudioGainSliderInstance = class extends AudioGainSlider {
  };
  var SpeedSliderInstance = class extends SpeedSlider {
  };
  var QualitySliderInstance = class extends QualitySlider {
  };
  var SliderThumbnailInstance = class extends SliderThumbnail {
  };
  var SliderValueInstance = class extends SliderValue {
  };
  var SliderVideoInstance = class extends SliderVideo {
  };
  var SliderPreviewInstance = class extends SliderPreview {
  };
  var SliderChaptersInstance = class extends SliderChapters {
  };
  var MenuInstance = class extends Menu {
  };
  var MenuButtonInstance = class extends MenuButton {
  };
  var MenuItemsInstance = class extends MenuItems {
  };
  var MenuItemInstance = class extends MenuItem2 {
  };
  var RadioGroupInstance = class extends RadioGroup {
  };
  var RadioInstance = class extends Radio {
  };
  var CaptionsInstance = class extends Captions {
  };
  var GestureInstance = class extends Gesture {
  };
  var PosterInstance = class extends Poster {
  };
  var ThumbnailInstance = class extends Thumbnail {
  };
  var TimeInstance = class extends Time {
  };
  var Slot = React5.forwardRef((props, forwardedRef) => {
    const { children, ...slotProps } = props;
    const childrenArray = React5.Children.toArray(children);
    const slottable = childrenArray.find(isSlottable);
    if (slottable) {
      const newElement = slottable.props.children;
      const newChildren = childrenArray.map((child) => {
        if (child === slottable) {
          if (React5.Children.count(newElement) > 1) return React5.Children.only(null);
          return React5.isValidElement(newElement) ? newElement.props.children : null;
        } else return child;
      });
      return /* @__PURE__ */ React5.createElement(SlotClone, {
        ...slotProps,
        ref: forwardedRef
      }, React5.isValidElement(newElement) ? React5.cloneElement(newElement, void 0, newChildren) : null);
    }
    return /* @__PURE__ */ React5.createElement(SlotClone, {
      ...slotProps,
      ref: forwardedRef
    }, children);
  });
  Slot.displayName = "Slot";
  var SlotClone = React5.forwardRef((props, forwardedRef) => {
    const { children, ...slotProps } = props;
    if (React5.isValidElement(children)) {
      const childrenRef = getElementRef(children), props2 = mergeProps(slotProps, children.props);
      if (children.type !== React5.Fragment) props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
      return React5.cloneElement(children, props2);
    }
    return React5.Children.count(children) > 1 ? React5.Children.only(null) : null;
  });
  SlotClone.displayName = "SlotClone";
  var Slottable = ({ children }) => {
    return /* @__PURE__ */ React5.createElement(React5.Fragment, null, children);
  };
  function isSlottable(child) {
    return React5.isValidElement(child) && child.type === Slottable;
  }
  function mergeProps(slotProps, childProps) {
    const overrideProps = { ...childProps };
    for (const propName in childProps) {
      const slotPropValue = slotProps[propName];
      const childPropValue = childProps[propName];
      if (/^on[A-Z]/.test(propName)) {
        if (slotPropValue && childPropValue) overrideProps[propName] = (...args) => {
          childPropValue(...args);
          slotPropValue(...args);
        };
        else if (slotPropValue) overrideProps[propName] = slotPropValue;
      } else if (propName === "style") overrideProps[propName] = {
        ...slotPropValue,
        ...childPropValue
      };
      else if (propName === "className") overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
    }
    return {
      ...slotProps,
      ...overrideProps
    };
  }
  function getElementRef(element) {
    let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get, mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) return element.ref;
    getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
    mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) return element.props.ref;
    return element.props.ref || element.ref;
  }
  var Primitive = [
    "button",
    "div",
    "span",
    "img",
    "video",
    "audio"
  ].reduce((primitives, node) => {
    const Node2 = React5.forwardRef((props, forwardedRef) => {
      const { asChild, ...primitiveProps } = props;
      const Comp = asChild ? Slot : node;
      return /* @__PURE__ */ React5.createElement(Comp, {
        ...primitiveProps,
        ref: forwardedRef
      });
    });
    Node2.displayName = `Primitive.${node}`;
    return {
      ...primitives,
      [node]: Node2
    };
  }, {});
  var mediaStateRecord = MediaPlayerInstance.state.record;
  var initialMediaStore = Object.keys(mediaStateRecord).reduce((store, prop2) => ({
    ...store,
    [prop2]() {
      return mediaStateRecord[prop2];
    }
  }), {});
  function useMediaState(prop2, ref) {
    const $state = useStateContext(mediaState);
    return useSignal((ref?.current?.$state || $state || initialMediaStore)[prop2]);
  }

  // frontend/node_modules/@vidstack/react/prod/vidstack.js
  init_vidstack_CvezEmSQ();

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-B7Z5edeb.js
  init_define_import_meta_env();
  init_vidstack_B9fTkxQO();
  init_vidstack_CvezEmSQ();

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-BEp8cIq7.js
  init_define_import_meta_env();
  init_vidstack_B9fTkxQO();
  var import_react19 = __toESM(require_react_shim(), 1);
  var sliderStateRecord = SliderInstance.state.record;
  var initialSliderStore = Object.keys(sliderStateRecord).reduce((store, prop2) => ({
    ...store,
    [prop2]() {
      return sliderStateRecord[prop2];
    }
  }), {});
  function useSliderState(prop2, ref) {
    const $state = useStateContext(sliderState);
    return useSignal((ref?.current?.$state || $state || initialSliderStore)[prop2]);
  }

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-B7Z5edeb.js
  var React6 = __toESM(require_react_shim(), 1);
  var import_react_dom = __toESM(require_react_dom_shim(), 1);
  function useMediaContext2() {
    return useReactContext(mediaContext);
  }
  var AirPlayButtonBridge = createReactComponent(AirPlayButtonInstance, { domEventsRegex: /^onMedia/ });
  var AirPlayButton2 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(AirPlayButtonBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.button, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  AirPlayButton2.displayName = "AirPlayButton";
  var PlayButtonBridge = createReactComponent(PlayButtonInstance, { domEventsRegex: /^onMedia/ });
  var PlayButton2 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(PlayButtonBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.button, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  PlayButton2.displayName = "PlayButton";
  var CaptionButtonBridge = createReactComponent(CaptionButtonInstance, { domEventsRegex: /^onMedia/ });
  var CaptionButton2 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(CaptionButtonBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.button, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  CaptionButton2.displayName = "CaptionButton";
  var FullscreenButtonBridge = createReactComponent(FullscreenButtonInstance, { domEventsRegex: /^onMedia/ });
  var FullscreenButton2 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(FullscreenButtonBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.button, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  FullscreenButton2.displayName = "FullscreenButton";
  var MuteButtonBridge = createReactComponent(MuteButtonInstance, { domEventsRegex: /^onMedia/ });
  var MuteButton2 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(MuteButtonBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.button, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  MuteButton2.displayName = "MuteButton";
  var PIPButtonBridge = createReactComponent(PIPButtonInstance, { domEventsRegex: /^onMedia/ });
  var PIPButton2 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(PIPButtonBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.button, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  PIPButton2.displayName = "PIPButton";
  var SeekButtonBridge = createReactComponent(SeekButtonInstance, { domEventsRegex: /^onMedia/ });
  var SeekButton2 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(SeekButtonBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.button, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  SeekButton2.displayName = "SeekButton";
  var LiveButtonBridge = createReactComponent(LiveButtonInstance, { domEventsRegex: /^onMedia/ });
  var LiveButton2 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(LiveButtonBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.button, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  LiveButton2.displayName = "LiveButton";
  var sliderCallbacks = [
    "onDragStart",
    "onDragEnd",
    "onDragValueChange",
    "onValueChange",
    "onPointerValueChange"
  ];
  var SliderValueBridge = createReactComponent(SliderValueInstance);
  var SliderBridge = createReactComponent(SliderInstance, { events: sliderCallbacks });
  var Root$5 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(SliderBridge, {
      ...props,
      ref: forwardRef8
    }, (props2) => /* @__PURE__ */ React6.createElement(Primitive.div, props2, children));
  });
  Root$5.displayName = "Slider";
  var Thumb = React6.forwardRef((props, forwardRef8) => /* @__PURE__ */ React6.createElement(Primitive.div, {
    ...props,
    ref: forwardRef8
  }));
  Thumb.displayName = "SliderThumb";
  var Track = React6.forwardRef((props, forwardRef8) => /* @__PURE__ */ React6.createElement(Primitive.div, {
    ...props,
    ref: forwardRef8
  }));
  Track.displayName = "SliderTrack";
  var TrackFill = React6.forwardRef((props, forwardRef8) => /* @__PURE__ */ React6.createElement(Primitive.div, {
    ...props,
    ref: forwardRef8
  }));
  TrackFill.displayName = "SliderTrackFill";
  var PreviewBridge = createReactComponent(SliderPreviewInstance);
  var Preview = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(PreviewBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.div, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Preview.displayName = "SliderPreview";
  var Value = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(SliderValueBridge, props, (props2, instance) => {
      const $text = useSignal(() => instance.getValueText(), instance);
      return /* @__PURE__ */ React6.createElement(Primitive.div, {
        ...props2,
        ref: forwardRef8
      }, $text, children);
    });
  });
  Value.displayName = "SliderValue";
  var Steps = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    const $min = useSliderState("min"), $max = useSliderState("max"), $step = useSliderState("step"), steps = ($max - $min) / $step;
    return /* @__PURE__ */ React6.createElement(Primitive.div, {
      ...props,
      ref: forwardRef8
    }, Array.from({ length: Math.floor(steps) + 1 }).map((_, step) => children(step)));
  });
  Steps.displayName = "SliderSteps";
  var VolumeSliderBridge = createReactComponent(VolumeSliderInstance, {
    events: sliderCallbacks,
    domEventsRegex: /^onMedia/
  });
  var Root$4 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(VolumeSliderBridge, {
      ...props,
      ref: forwardRef8
    }, (props2) => /* @__PURE__ */ React6.createElement(Primitive.div, props2, children));
  });
  Root$4.displayName = "VolumeSlider";
  function createVTTCue(startTime = 0, endTime = 0, text = "") {
    if (IS_SERVER2) return {
      startTime,
      endTime,
      text,
      addEventListener: noop,
      removeEventListener: noop,
      dispatchEvent: noop
    };
    return new window.VTTCue(startTime, endTime, text);
  }
  function appendParamsToURL2(baseUrl, params) {
    const url = new URL(baseUrl);
    for (const key2 of Object.keys(params)) url.searchParams.set(key2, params[key2] + "");
    return url.toString();
  }
  var ThumbnailBridge = createReactComponent(ThumbnailInstance);
  var Root$3 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(ThumbnailBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.div, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Root$3.displayName = "Thumbnail";
  var Img = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    const { src, img, crossOrigin } = useStateContext(ThumbnailInstance.state), $src = useSignal(src), $crossOrigin = useSignal(crossOrigin);
    return /* @__PURE__ */ React6.createElement(Primitive.img, {
      crossOrigin: $crossOrigin,
      ...props,
      src: $src || void 0,
      ref: composeRefs(img.set, forwardRef8)
    }, children);
  });
  Img.displayName = "ThumbnailImg";
  var TimeSliderContext = React6.createContext({ $chapters: signal(null) });
  TimeSliderContext.displayName = "TimeSliderContext";
  var TimeSliderBridge = createReactComponent(TimeSliderInstance, {
    events: sliderCallbacks,
    domEventsRegex: /^onMedia/
  });
  var Root$2 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    const $chapters = React6.useMemo(() => signal(null), []);
    return /* @__PURE__ */ React6.createElement(TimeSliderContext.Provider, { value: { $chapters } }, /* @__PURE__ */ React6.createElement(TimeSliderBridge, {
      ...props,
      ref: forwardRef8
    }, (props2) => /* @__PURE__ */ React6.createElement(Primitive.div, props2, children)));
  });
  Root$2.displayName = "TimeSlider";
  var SliderChaptersBridge = createReactComponent(SliderChaptersInstance);
  var Chapters = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(SliderChaptersBridge, props, (props2, instance) => /* @__PURE__ */ React6.createElement(Primitive.div, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, /* @__PURE__ */ React6.createElement(ChapterTracks, { instance }, children)));
  });
  Chapters.displayName = "SliderChapters";
  function ChapterTracks({ instance, children }) {
    const $cues = useSignal(() => instance.cues, instance), refs = React6.useRef([]), emptyCue = React6.useRef(null), { $chapters } = React6.useContext(TimeSliderContext);
    if (!emptyCue.current) emptyCue.current = createVTTCue();
    React6.useEffect(() => {
      $chapters.set(instance);
      return () => void $chapters.set(null);
    }, [instance]);
    React6.useEffect(() => {
      instance.setRefs(refs.current);
    }, [$cues]);
    return children($cues.length ? $cues : [emptyCue.current], (el) => {
      if (!el) {
        refs.current.length = 0;
        return;
      }
      refs.current.push(el);
    });
  }
  ChapterTracks.displayName = "SliderChapterTracks";
  var ChapterTitle = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    const { $chapters } = React6.useContext(TimeSliderContext), [title, setTitle] = React6.useState();
    React6.useEffect(() => {
      return effect(() => {
        const chapters = $chapters();
        setTitle((chapters?.activePointerCue || chapters?.activeCue)?.text || "");
      });
    }, []);
    return /* @__PURE__ */ React6.createElement(Primitive.div, {
      ...props,
      ref: forwardRef8
    }, title, children);
  });
  ChapterTitle.displayName = "SliderChapterTitle";
  var Progress = React6.forwardRef((props, forwardRef8) => /* @__PURE__ */ React6.createElement(Primitive.div, {
    ...props,
    ref: forwardRef8
  }));
  Progress.displayName = "SliderProgress";
  var SliderThumbnailBridge = createReactComponent(SliderThumbnailInstance);
  var ThumbnailRoot = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(SliderThumbnailBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.div, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  ThumbnailRoot.displayName = "SliderThumbnail";
  var Thumbnail2 = {
    Root: ThumbnailRoot,
    Img
  };
  var VideoBridge = createReactComponent(SliderVideoInstance, { events: ["onCanPlay", "onError"] });
  var Video = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(VideoBridge, props, (props2, instance) => /* @__PURE__ */ React6.createElement(VideoProvider2, {
      ...props2,
      instance,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Video.displayName = "SliderVideo";
  var VideoProvider2 = React6.forwardRef(({ instance, children, ...props }, forwardRef8) => {
    const { canLoad } = useStateContext(mediaState), { src, video, crossOrigin } = instance.$state, $src = useSignal(src), $canLoad = useSignal(canLoad), $crossOrigin = useSignal(crossOrigin);
    return /* @__PURE__ */ React6.createElement(Primitive.video, {
      style: { maxWidth: "unset" },
      ...props,
      src: $src || void 0,
      muted: true,
      playsInline: true,
      preload: $canLoad ? "auto" : "none",
      crossOrigin: $crossOrigin || void 0,
      ref: composeRefs(video.set, forwardRef8)
    }, children);
  });
  VideoProvider2.displayName = "SliderVideoProvider";
  var RadioGroupBridge = createReactComponent(RadioGroupInstance, { events: ["onChange"] });
  var Root$1 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(RadioGroupBridge, {
      ...props,
      ref: forwardRef8
    }, (props2) => /* @__PURE__ */ React6.createElement(Primitive.div, props2, children));
  });
  Root$1.displayName = "RadioGroup";
  var ItemBridge$1 = createReactComponent(RadioInstance, { events: ["onChange", "onSelect"] });
  var Item$1 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(ItemBridge$1, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.div, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Item$1.displayName = "RadioItem";
  var MenuBridge = createReactComponent(MenuInstance, {
    events: ["onOpen", "onClose"],
    domEventsRegex: /^onMedia/
  });
  var Root = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(MenuBridge, {
      ...props,
      ref: forwardRef8
    }, (props2, instance) => /* @__PURE__ */ React6.createElement(Primitive.div, {
      ...props2,
      style: {
        display: !instance.isSubmenu ? "contents" : void 0,
        ...props2.style
      }
    }, children));
  });
  Root.displayName = "Menu";
  var ButtonBridge = createReactComponent(MenuButtonInstance, { events: ["onSelect"] });
  var Button = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(ButtonBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.button, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Button.displayName = "MenuButton";
  var Portal = React6.forwardRef(({ container = null, disabled = false, children, ...props }, forwardRef8) => {
    let fullscreen = useMediaState("fullscreen"), shouldPortal = disabled === "fullscreen" ? !fullscreen : !disabled;
    const target = React6.useMemo(() => {
      if (IS_SERVER2) return null;
      return (isString(container) ? document.querySelector(container) : container) ?? document.body;
    }, [container]);
    return !target || !shouldPortal ? children : (0, import_react_dom.createPortal)(/* @__PURE__ */ React6.createElement(Primitive.div, {
      ...props,
      style: {
        display: "contents",
        ...props.style
      },
      ref: forwardRef8
    }, children), target);
  });
  Portal.displayName = "MenuPortal";
  var ItemsBridge = createReactComponent(MenuItemsInstance);
  var Items = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(ItemsBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.div, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Items.displayName = "MenuItems";
  var ItemBridge = createReactComponent(MenuItemInstance);
  var Item = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(ItemBridge, props, (props2) => /* @__PURE__ */ React6.createElement(Primitive.div, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Item.displayName = "MenuItem";
  var GestureBridge = createReactComponent(GestureInstance, { events: ["onWillTrigger", "onTrigger"] });
  var Gesture2 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(GestureBridge, {
      ...props,
      ref: forwardRef8
    }, (props2) => /* @__PURE__ */ React6.createElement(Primitive.div, props2, children));
  });
  Gesture2.displayName = "Gesture";
  var TimeBridge = createReactComponent(TimeInstance);
  var Time2 = React6.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React6.createElement(TimeBridge, props, (props2, instance) => /* @__PURE__ */ React6.createElement(TimeText, {
      ...props2,
      instance,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Time2.displayName = "Time";
  var TimeText = React6.forwardRef(({ instance, children, ...props }, forwardRef8) => {
    const { timeText } = instance.$state, $timeText = useSignal(timeText);
    return /* @__PURE__ */ React6.createElement(Primitive.div, {
      ...props,
      ref: forwardRef8
    }, $timeText, children);
  });
  TimeText.displayName = "TimeText";
  function useMediaPlayer() {
    return useMediaContext2()?.player || null;
  }
  function useAudioOptions() {
    const media = useMediaContext2(), { audioTracks, audioTrack } = media.$state, $audioTracks = useSignal(audioTracks);
    useSignal(audioTrack);
    return React6.useMemo(() => {
      const options = $audioTracks.map((track) => ({
        track,
        label: track.label,
        value: getTrackValue$1(track),
        get selected() {
          return audioTrack() === track;
        },
        select(trigger) {
          const index = audioTracks().indexOf(track);
          if (index >= 0) media.remote.changeAudioTrack(index, trigger);
        }
      }));
      Object.defineProperty(options, "disabled", { get() {
        return options.length <= 1;
      } });
      Object.defineProperty(options, "selectedTrack", { get() {
        return audioTrack();
      } });
      Object.defineProperty(options, "selectedValue", { get() {
        const track = audioTrack();
        return track ? getTrackValue$1(track) : void 0;
      } });
      return options;
    }, [$audioTracks]);
  }
  function getTrackValue$1(track) {
    return track.label.toLowerCase();
  }
  function useCaptionOptions({ off = true } = {}) {
    const media = useMediaContext2(), { textTracks, textTrack } = media.$state, $textTracks = useSignal(textTracks);
    useSignal(textTrack);
    return React6.useMemo(() => {
      const captionTracks = $textTracks.filter(isTrackCaptionKind), options = captionTracks.map((track) => ({
        track,
        label: track.label,
        value: getTrackValue(track),
        get selected() {
          return textTrack() === track;
        },
        select(trigger) {
          const index = textTracks().indexOf(track);
          if (index >= 0) media.remote.changeTextTrackMode(index, "showing", trigger);
        }
      }));
      if (off) options.unshift({
        track: null,
        label: isString(off) ? off : "Off",
        value: "off",
        get selected() {
          return !textTrack();
        },
        select(trigger) {
          media.remote.toggleCaptions(trigger);
        }
      });
      Object.defineProperty(options, "disabled", { get() {
        return !captionTracks.length;
      } });
      Object.defineProperty(options, "selectedTrack", { get() {
        return textTrack();
      } });
      Object.defineProperty(options, "selectedValue", { get() {
        const track = textTrack();
        return track ? getTrackValue(track) : "off";
      } });
      return options;
    }, [$textTracks]);
  }
  function getTrackValue(track) {
    return track.id + ":" + track.kind + "-" + track.label.toLowerCase();
  }
  function useVideoQualityOptions({ auto = true, sort = "descending" } = {}) {
    const media = useMediaContext2(), { qualities, quality, autoQuality, canSetQuality } = media.$state, $qualities = useSignal(qualities);
    useSignal(quality);
    useSignal(autoQuality);
    useSignal(canSetQuality);
    return React6.useMemo(() => {
      const options = sortVideoQualities($qualities, sort === "descending").map((q) => {
        return {
          quality: q,
          label: q.height + "p",
          value: getQualityValue(q),
          bitrateText: q.bitrate && q.bitrate > 0 ? `${(q.bitrate / 1e6).toFixed(2)} Mbps` : null,
          get selected() {
            return q === quality();
          },
          get autoSelected() {
            return autoQuality();
          },
          select(trigger) {
            const index = qualities().indexOf(q);
            if (index >= 0) media.remote.changeQuality(index, trigger);
          }
        };
      });
      if (auto) options.unshift({
        quality: null,
        label: isString(auto) ? auto : "Auto",
        value: "auto",
        bitrateText: null,
        get selected() {
          return autoQuality();
        },
        get autoSelected() {
          return autoQuality();
        },
        select(trigger) {
          media.remote.requestAutoQuality(trigger);
        }
      });
      Object.defineProperty(options, "disabled", { get() {
        return !canSetQuality() || $qualities.length <= 1;
      } });
      Object.defineProperty(options, "selectedQuality", { get() {
        return quality();
      } });
      Object.defineProperty(options, "selectedValue", { get() {
        const $quality = quality();
        return !autoQuality() && $quality ? getQualityValue($quality) : "auto";
      } });
      return options;
    }, [$qualities, sort]);
  }
  function getQualityValue(quality) {
    return quality.height + "_" + quality.bitrate;
  }

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-Bk6FpJg5.js
  init_define_import_meta_env();
  init_vidstack_B9fTkxQO();
  init_vidstack_CvezEmSQ();
  var React7 = __toESM(require_react_shim(), 1);
  var MediaAnnouncerBridge = createReactComponent(MediaAnnouncerInstance, { events: ["onChange"] });
  var MediaAnnouncer2 = React7.forwardRef(({ style, children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React7.createElement(MediaAnnouncerBridge, props, (props2) => /* @__PURE__ */ React7.createElement(Primitive.div, {
      ...props2,
      style: {
        display: "contents",
        ...style
      },
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  MediaAnnouncer2.displayName = "MediaAnnouncer";
  var ControlsBridge = createReactComponent(ControlsInstance);
  var Root$42 = React7.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React7.createElement(ControlsBridge, props, (props2) => /* @__PURE__ */ React7.createElement(Primitive.div, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Root$42.displayName = "Controls";
  var ControlsGroupBridge = createReactComponent(ControlsGroupInstance);
  var Group = React7.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React7.createElement(ControlsGroupBridge, props, (props2) => /* @__PURE__ */ React7.createElement(Primitive.div, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Group.displayName = "ControlsGroup";
  var TooltipBridge = createReactComponent(TooltipInstance);
  function Root$32({ children, ...props }) {
    return /* @__PURE__ */ React7.createElement(TooltipBridge, props, children);
  }
  Root$32.displayName = "Tooltip";
  var TriggerBridge = createReactComponent(TooltipTriggerInstance);
  var Trigger2 = React7.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React7.createElement(TriggerBridge, props, (props2) => /* @__PURE__ */ React7.createElement(Primitive.button, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Trigger2.displayName = "TooltipTrigger";
  var ContentBridge = createReactComponent(TooltipContentInstance);
  var Content = React7.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React7.createElement(ContentBridge, props, (props2) => /* @__PURE__ */ React7.createElement(Primitive.div, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Content.displayName = "TooltipContent";
  var GoogleCastButtonBridge = createReactComponent(GoogleCastButtonInstance, { domEventsRegex: /^onMedia/ });
  var GoogleCastButton2 = React7.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React7.createElement(GoogleCastButtonBridge, props, (props2) => /* @__PURE__ */ React7.createElement(Primitive.button, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  GoogleCastButton2.displayName = "GoogleCastButton";
  var AudioGainSliderBridge = createReactComponent(AudioGainSliderInstance, {
    events: sliderCallbacks,
    domEventsRegex: /^onMedia/
  });
  var Root$22 = React7.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React7.createElement(AudioGainSliderBridge, {
      ...props,
      ref: forwardRef8
    }, (props2) => /* @__PURE__ */ React7.createElement(Primitive.div, props2, children));
  });
  Root$22.displayName = "AudioGainSlider";
  var SpeedSliderBridge = createReactComponent(SpeedSliderInstance, {
    events: sliderCallbacks,
    domEventsRegex: /^onMedia/
  });
  var Root$12 = React7.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React7.createElement(SpeedSliderBridge, {
      ...props,
      ref: forwardRef8
    }, (props2) => /* @__PURE__ */ React7.createElement(Primitive.div, props2, children));
  });
  Root$12.displayName = "SpeedSlider";
  var Title = React7.forwardRef(({ children, ...props }, forwardRef8) => {
    const $title = useMediaState("title");
    return /* @__PURE__ */ React7.createElement(Primitive.span, {
      ...props,
      ref: forwardRef8
    }, $title, children);
  });
  Title.displayName = "Title";
  function useActiveTextCues(track) {
    const [activeCues, setActiveCues] = React7.useState([]);
    React7.useEffect(() => {
      if (!track) {
        setActiveCues([]);
        return;
      }
      function onCuesChange() {
        if (track) setActiveCues(track.activeCues);
      }
      onCuesChange();
      return listenEvent(track, "cue-change", onCuesChange);
    }, [track]);
    return activeCues;
  }
  function useActiveTextTrack(kind) {
    const media = useMediaContext2(), [track, setTrack] = React7.useState(null);
    React7.useEffect(() => {
      return watchActiveTextTrack(media.textTracks, kind, setTrack);
    }, [media.textTracks, kind]);
    return track;
  }
  function useChapterTitle() {
    return useActiveTextCues(useActiveTextTrack("chapters"))[0]?.text || "";
  }
  var ChapterTitle2 = React7.forwardRef(({ defaultText = "", children, ...props }, forwardRef8) => {
    const $chapterTitle = useChapterTitle();
    return /* @__PURE__ */ React7.createElement(Primitive.span, {
      ...props,
      ref: forwardRef8
    }, $chapterTitle || defaultText, children);
  });
  ChapterTitle2.displayName = "ChapterTitle";
  var CaptionsBridge = createReactComponent(CaptionsInstance);
  var Captions2 = React7.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React7.createElement(CaptionsBridge, {
      ...props,
      ref: forwardRef8
    }, (props2) => /* @__PURE__ */ React7.createElement(Primitive.div, props2, children));
  });
  Captions2.displayName = "Captions";
  var Root2 = React7.forwardRef(({ size: size2 = 96, children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React7.createElement("svg", {
      width: size2,
      height: size2,
      fill: "none",
      viewBox: "0 0 120 120",
      "aria-hidden": "true",
      "data-part": "root",
      ...props,
      ref: forwardRef8
    }, children);
  });
  var Track2 = React7.forwardRef(({ width = 8, children, ...props }, ref) => /* @__PURE__ */ React7.createElement("circle", {
    cx: "60",
    cy: "60",
    r: "54",
    stroke: "currentColor",
    strokeWidth: width,
    "data-part": "track",
    ...props,
    ref
  }, children));
  var TrackFill2 = React7.forwardRef(({ width = 8, fillPercent = 50, children, ...props }, ref) => /* @__PURE__ */ React7.createElement("circle", {
    cx: "60",
    cy: "60",
    r: "54",
    stroke: "currentColor",
    pathLength: "100",
    strokeWidth: width,
    strokeDasharray: 100,
    strokeDashoffset: 100 - fillPercent,
    "data-part": "track-fill",
    ...props,
    ref
  }, children));
  function createSignal(initialValue, deps = []) {
    const scope = useReactScope();
    return React7.useMemo(() => scoped(() => signal(initialValue), scope), [scope, ...deps]);
  }
  function createComputed(compute2, deps = []) {
    const scope = useReactScope();
    return React7.useMemo(() => scoped(() => computed(compute2), scope), [scope, ...deps]);
  }
  function createEffect(compute2, deps = []) {
    const scope = useReactScope();
    React7.useEffect(() => scoped(() => effect(compute2), scope), [scope, ...deps]);
  }
  function useScoped(compute2) {
    const scope = useReactScope();
    return React7.useMemo(() => scoped(compute2, scope), [scope]);
  }
  function useTextCues(track) {
    const [cues, setCues] = React7.useState([]);
    React7.useEffect(() => {
      if (!track) return;
      function onCuesChange() {
        if (track) setCues([...track.cues]);
      }
      const events = new EventsController(track).add("add-cue", onCuesChange).add("remove-cue", onCuesChange);
      onCuesChange();
      return () => {
        setCues([]);
        events.abort();
      };
    }, [track]);
    return cues;
  }
  function useChapterOptions() {
    const media = useMediaContext2(), track = useActiveTextTrack("chapters"), cues = useTextCues(track), $startTime = useSignal(media.$state.seekableStart), $endTime = useSignal(media.$state.seekableEnd);
    useActiveTextCues(track);
    return React7.useMemo(() => {
      const options = track ? cues.filter((cue) => cue.startTime <= $endTime && cue.endTime >= $startTime).map((cue, i) => {
        let currentRef = null, stopProgressEffect;
        return {
          cue,
          label: cue.text,
          value: i.toString(),
          startTimeText: formatTime2(Math.max(0, cue.startTime - $startTime)),
          durationText: formatSpokenTime(Math.min($endTime, cue.endTime) - Math.max($startTime, cue.startTime)),
          get selected() {
            return cue === track.activeCues[0];
          },
          setProgressVar(ref) {
            if (!ref || cue !== track.activeCues[0]) {
              stopProgressEffect?.();
              stopProgressEffect = void 0;
              ref?.style.setProperty("--progress", "0%");
              currentRef = null;
              return;
            }
            if (currentRef === ref) return;
            currentRef = ref;
            stopProgressEffect?.();
            stopProgressEffect = effect(() => {
              const { realCurrentTime } = media.$state, time = realCurrentTime(), cueStartTime = Math.max($startTime, cue.startTime), duration = Math.min($endTime, cue.endTime) - cueStartTime, progress = Math.max(0, time - cueStartTime) / duration * 100;
              ref.style.setProperty("--progress", progress.toFixed(3) + "%");
            });
          },
          select(trigger) {
            media.remote.seek(cue.startTime - $startTime, trigger);
          }
        };
      }) : [];
      Object.defineProperty(options, "selectedValue", { get() {
        const index = options.findIndex((option) => option.selected);
        return (index >= 0 ? index : 0).toString();
      } });
      return options;
    }, [
      cues,
      $startTime,
      $endTime
    ]);
  }

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-BUARUqHF.js
  init_define_import_meta_env();
  var React8 = __toESM(require_react_shim(), 1);
  var Icon2 = /* @__PURE__ */ React8.forwardRef((props, ref) => {
    const { width, height, size: size2 = null, paths, ...restProps } = props;
    return React8.createElement("svg", {
      viewBox: "0 0 32 32",
      ...restProps,
      width: width ?? size2,
      height: height ?? size2,
      fill: "none",
      "aria-hidden": "true",
      focusable: "false",
      xmlns: "http://www.w3.org/2000/svg",
      ref,
      dangerouslySetInnerHTML: { __html: paths }
    });
  });
  Icon2.displayName = "VidstackIcon";

  // frontend/node_modules/@vidstack/react/prod/vidstack.js
  var React9 = __toESM(require_react_shim(), 1);
  var DEFAULT_AUDIO_GAINS = [
    1,
    1.25,
    1.5,
    1.75,
    2,
    2.5,
    3,
    4
  ];
  var _a105, _media40, _menu3, _controller7, _AudioGainRadioGroup_instances, watchValue_fn5, watchHintText_fn, watchControllerDisabled_fn, getValue_fn2, onValueChange_fn7;
  var AudioGainRadioGroup = (_a105 = class extends Component2 {
    constructor() {
      super();
      __privateAdd(this, _AudioGainRadioGroup_instances);
      __privateAdd(this, _media40);
      __privateAdd(this, _menu3);
      __privateAdd(this, _controller7);
      __privateSet(this, _controller7, new RadioGroupController());
      __privateGet(this, _controller7).onValueChange = __privateMethod(this, _AudioGainRadioGroup_instances, onValueChange_fn7).bind(this);
    }
    get value() {
      return __privateGet(this, _controller7).value;
    }
    get disabled() {
      const { gains } = this.$props, { canSetAudioGain } = __privateGet(this, _media40).$state;
      return !canSetAudioGain() || gains().length === 0;
    }
    onSetup() {
      __privateSet(this, _media40, useMediaContext());
      if (hasProvidedContext(menuContext)) __privateSet(this, _menu3, useContext2(menuContext));
    }
    onConnect(el) {
      effect(__privateMethod(this, _AudioGainRadioGroup_instances, watchValue_fn5).bind(this));
      effect(__privateMethod(this, _AudioGainRadioGroup_instances, watchHintText_fn).bind(this));
      effect(__privateMethod(this, _AudioGainRadioGroup_instances, watchControllerDisabled_fn).bind(this));
    }
    getOptions() {
      const { gains, normalLabel } = this.$props;
      return gains().map((gain) => ({
        label: gain === 1 || gain === null ? normalLabel : String(gain * 100) + "%",
        value: gain.toString()
      }));
    }
  }, _media40 = new WeakMap(), _menu3 = new WeakMap(), _controller7 = new WeakMap(), _AudioGainRadioGroup_instances = new WeakSet(), watchValue_fn5 = function() {
    __privateGet(this, _controller7).value = __privateMethod(this, _AudioGainRadioGroup_instances, getValue_fn2).call(this);
  }, watchHintText_fn = function() {
    const { normalLabel } = this.$props, { audioGain } = __privateGet(this, _media40).$state, gain = audioGain();
    __privateGet(this, _menu3)?.hint.set(gain === 1 || gain == null ? normalLabel() : String(gain * 100) + "%");
  }, watchControllerDisabled_fn = function() {
    __privateGet(this, _menu3)?.disable(this.disabled);
  }, getValue_fn2 = function() {
    const { audioGain } = __privateGet(this, _media40).$state;
    return audioGain()?.toString() ?? "1";
  }, onValueChange_fn7 = function(value, trigger) {
    if (this.disabled) return;
    const gain = +value;
    __privateGet(this, _media40).remote.changeAudioGain(gain, trigger);
    this.dispatch("change", {
      detail: gain,
      trigger
    });
  }, __publicField(_a105, "props", {
    normalLabel: "Disabled",
    gains: DEFAULT_AUDIO_GAINS
  }), _a105);
  var audiogainradiogroup__proto = AudioGainRadioGroup.prototype;
  prop(audiogainradiogroup__proto, "value");
  prop(audiogainradiogroup__proto, "disabled");
  method(audiogainradiogroup__proto, "getOptions");
  var MediaPlayerBridge = createReactComponent(MediaPlayerInstance, {
    events: [
      "onAbort",
      "onAudioTrackChange",
      "onAudioTracksChange",
      "onAutoPlay",
      "onAutoPlayChange",
      "onAutoPlayFail",
      "onCanLoad",
      "onCanPlay",
      "onCanPlayThrough",
      "onControlsChange",
      "onDestroy",
      "onDurationChange",
      "onEmptied",
      "onEnd",
      "onEnded",
      "onError",
      "onFindMediaPlayer",
      "onFullscreenChange",
      "onFullscreenError",
      "onLiveChange",
      "onLiveEdgeChange",
      "onLoadedData",
      "onLoadedMetadata",
      "onLoadStart",
      "onLoopChange",
      "onOrientationChange",
      "onPause",
      "onPictureInPictureChange",
      "onPictureInPictureError",
      "onPlay",
      "onPlayFail",
      "onPlaying",
      "onPlaysInlineChange",
      "onPosterChange",
      "onProgress",
      "onProviderChange",
      "onProviderLoaderChange",
      "onProviderSetup",
      "onQualitiesChange",
      "onQualityChange",
      "onRateChange",
      "onReplay",
      "onSeeked",
      "onSeeking",
      "onSourceChange",
      "onSourceChange",
      "onStalled",
      "onStarted",
      "onStreamTypeChange",
      "onSuspend",
      "onTextTrackChange",
      "onTextTracksChange",
      "onTimeUpdate",
      "onTitleChange",
      "onVdsLog",
      "onVideoPresentationChange",
      "onVolumeChange",
      "onWaiting"
    ],
    eventsRegex: /^onHls/,
    domEventsRegex: /^onMedia/
  });
  var MediaPlayer2 = React9.forwardRef(({ aspectRatio, children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React9.createElement(MediaPlayerBridge, {
      ...props,
      src: props.src,
      ref: forwardRef8,
      style: {
        aspectRatio,
        ...props.style
      }
    }, (props2) => /* @__PURE__ */ React9.createElement(Primitive.div, props2, children));
  });
  MediaPlayer2.displayName = "MediaPlayer";
  var MediaProviderBridge = createReactComponent(MediaProviderInstance);
  var MediaProvider2 = React9.forwardRef(({ loaders = [], children, iframeProps, mediaProps, ...props }, forwardRef8) => {
    const reactLoaders = React9.useMemo(() => loaders.map((Loader) => new Loader()), loaders);
    return /* @__PURE__ */ React9.createElement(MediaProviderBridge, {
      ...props,
      loaders: reactLoaders,
      ref: forwardRef8
    }, (props2, instance) => /* @__PURE__ */ React9.createElement("div", props2, /* @__PURE__ */ React9.createElement(MediaOutlet, {
      provider: instance,
      mediaProps,
      iframeProps
    }), children));
  });
  MediaProvider2.displayName = "MediaProvider";
  function MediaOutlet({ provider, mediaProps, iframeProps }) {
    const { sources, crossOrigin, poster, remotePlaybackInfo, nativeControls, viewType } = useStateContext(mediaState), { loader } = provider.$state, { $provider: $$provider, $providerSetup: $$providerSetup } = useMediaContext2(), $sources = useSignal(sources), $nativeControls = useSignal(nativeControls), $crossOrigin = useSignal(crossOrigin), $poster = useSignal(poster), $loader = useSignal(loader), $provider = useSignal($$provider), $providerSetup = useSignal($$providerSetup), $remoteInfo = useSignal(remotePlaybackInfo), $mediaType = $loader?.mediaType(), isAudioView = useSignal(viewType) === "audio", isYouTubeEmbed = $loader?.name === "youtube", isVimeoEmbed = $loader?.name === "vimeo", isEmbed = isYouTubeEmbed || isVimeoEmbed, isRemotion = $loader?.name === "remotion", isGoogleCast = $loader?.name === "google-cast", [googleCastIconPaths, setGoogleCastIconPaths] = React9.useState(""), [hasMounted, setHasMounted] = React9.useState(false);
    const loadProviderRef = React9.useCallback((el) => {
      provider.load(el);
    }, [provider]);
    React9.useEffect(() => {
      if (!isGoogleCast || googleCastIconPaths) return;
      Promise.resolve().then(() => (init_vidstack_B9fTkxQO(), vidstack_B9fTkxQO_exports)).then((n2) => n2.chromecast_exports).then((mod) => {
        setGoogleCastIconPaths(mod.default);
      });
    }, [isGoogleCast]);
    React9.useLayoutEffect(() => {
      setHasMounted(true);
    }, []);
    if (isGoogleCast) return /* @__PURE__ */ React9.createElement("div", {
      className: "vds-google-cast",
      ref: loadProviderRef
    }, /* @__PURE__ */ React9.createElement(Icon2, { paths: googleCastIconPaths }), $remoteInfo?.deviceName ? /* @__PURE__ */ React9.createElement("span", { className: "vds-google-cast-info" }, "Google Cast on", " ", /* @__PURE__ */ React9.createElement("span", { className: "vds-google-cast-device-name" }, $remoteInfo.deviceName)) : null);
    if (isRemotion) return /* @__PURE__ */ React9.createElement("div", { "data-remotion-canvas": true }, /* @__PURE__ */ React9.createElement("div", {
      "data-remotion-container": true,
      ref: loadProviderRef
    }, isRemotionProvider($provider) && $providerSetup ? React9.createElement($provider.render) : null));
    return isEmbed ? React9.createElement(React9.Fragment, null, React9.createElement("iframe", {
      ...iframeProps,
      className: (iframeProps?.className ? `${iframeProps.className} ` : "") + isYouTubeEmbed ? "vds-youtube" : "vds-vimeo",
      suppressHydrationWarning: true,
      tabIndex: !$nativeControls ? -1 : void 0,
      "aria-hidden": "true",
      "data-no-controls": !$nativeControls ? "" : void 0,
      ref: loadProviderRef
    }), !$nativeControls && !isAudioView ? React9.createElement("div", { className: "vds-blocker" }) : null) : $mediaType ? React9.createElement($mediaType === "audio" ? "audio" : "video", {
      ...mediaProps,
      controls: $nativeControls ? true : null,
      crossOrigin: typeof $crossOrigin === "boolean" ? "" : $crossOrigin,
      poster: $mediaType === "video" && $nativeControls && $poster ? $poster : null,
      suppressHydrationWarning: true,
      children: !hasMounted ? $sources.map(({ src, type }) => isString(src) ? /* @__PURE__ */ React9.createElement("source", {
        src,
        type: type !== "?" ? type : void 0,
        key: src
      }) : null) : null,
      ref: loadProviderRef
    }) : null;
  }
  MediaOutlet.displayName = "MediaOutlet";
  function createTextTrack(init) {
    const media = useMediaContext2(), track = React9.useMemo(() => new TextTrack(init), Object.values(init));
    React9.useEffect(() => {
      media.textTracks.add(track);
      return () => void media.textTracks.remove(track);
    }, [track]);
    return track;
  }
  function Track3({ lang, ...props }) {
    createTextTrack({
      language: lang,
      ...props
    });
    return null;
  }
  Track3.displayName = "Track";
  var ToggleButtonBridge = createReactComponent(ToggleButtonInstance);
  var ToggleButton2 = React9.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React9.createElement(ToggleButtonBridge, props, (props2) => /* @__PURE__ */ React9.createElement(Primitive.button, {
      ...props2,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  ToggleButton2.displayName = "ToggleButton";
  var QualitySliderBridge = createReactComponent(QualitySliderInstance, {
    events: sliderCallbacks,
    domEventsRegex: /^onMedia/
  });
  var Root$13 = React9.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React9.createElement(QualitySliderBridge, {
      ...props,
      ref: forwardRef8
    }, (props2) => /* @__PURE__ */ React9.createElement(Primitive.div, props2, children));
  });
  Root$13.displayName = "QualitySlider";
  var PosterBridge = createReactComponent(PosterInstance);
  var Poster2 = React9.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React9.createElement(PosterBridge, {
      src: props.asChild && React9.isValidElement(children) ? children.props.src : void 0,
      ...props
    }, (props2, instance) => /* @__PURE__ */ React9.createElement(PosterImg, {
      ...props2,
      instance,
      ref: composeRefs(props2.ref, forwardRef8)
    }, children));
  });
  Poster2.displayName = "Poster";
  var PosterImg = React9.forwardRef(({ instance, children, ...props }, forwardRef8) => {
    const { src, img, alt, crossOrigin, hidden } = instance.$state, $src = useSignal(src), $alt = useSignal(alt), $crossOrigin = useSignal(crossOrigin), $hidden = useSignal(hidden);
    return /* @__PURE__ */ React9.createElement(Primitive.img, {
      ...props,
      src: $src || void 0,
      alt: $alt || void 0,
      crossOrigin: $crossOrigin || void 0,
      ref: composeRefs(img.set, forwardRef8),
      style: { display: $hidden ? "none" : void 0 }
    }, children);
  });
  PosterImg.displayName = "PosterImg";
  var Root3 = React9.forwardRef(({ children, ...props }, forwardRef8) => {
    return /* @__PURE__ */ React9.createElement(Primitive.div, {
      translate: "yes",
      "aria-live": "off",
      "aria-atomic": "true",
      ...props,
      ref: forwardRef8
    }, children);
  });
  Root3.displayName = "Caption";
  var Text = React9.forwardRef((props, forwardRef8) => {
    const textTrack = useMediaState("textTrack"), [activeCue, setActiveCue] = React9.useState();
    React9.useEffect(() => {
      if (!textTrack) return;
      function onCueChange() {
        setActiveCue(textTrack?.activeCues[0]);
      }
      textTrack.addEventListener("cue-change", onCueChange);
      return () => {
        textTrack.removeEventListener("cue-change", onCueChange);
        setActiveCue(void 0);
      };
    }, [textTrack]);
    return /* @__PURE__ */ React9.createElement(Primitive.span, {
      ...props,
      "data-part": "cue",
      dangerouslySetInnerHTML: { __html: activeCue?.text || "" },
      ref: forwardRef8
    });
  });
  Text.displayName = "CaptionText";

  // frontend/node_modules/@vidstack/react/prod/player/vidstack-default-layout.js
  init_define_import_meta_env();

  // frontend/node_modules/@vidstack/react/prod/player/vidstack-default-icons.js
  init_define_import_meta_env();
  init_vidstack_B9fTkxQO();
  var React10 = __toESM(require_react_shim(), 1);
  function createIcon(paths) {
    function DefaultLayoutIcon(props) {
      return /* @__PURE__ */ React10.createElement(Icon2, {
        paths,
        ...props
      });
    }
    DefaultLayoutIcon.displayName = "DefaultLayoutIcon";
    return DefaultLayoutIcon;
  }
  var defaultLayoutIcons = {
    AirPlayButton: { Default: createIcon(Icon$5) },
    GoogleCastButton: { Default: createIcon(Icon$24) },
    PlayButton: {
      Play: createIcon(Icon$62),
      Pause: createIcon(Icon$59),
      Replay: createIcon(Icon$74)
    },
    MuteButton: {
      Mute: createIcon(Icon$54),
      VolumeLow: createIcon(Icon$105),
      VolumeHigh: createIcon(Icon$104)
    },
    CaptionButton: {
      On: createIcon(Icon$26),
      Off: createIcon(Icon$27)
    },
    PIPButton: {
      Enter: createIcon(Icon$61),
      Exit: createIcon(Icon$60)
    },
    FullscreenButton: {
      Enter: createIcon(Icon$40),
      Exit: createIcon(Icon$39)
    },
    SeekButton: {
      Backward: createIcon(Icon$77),
      Forward: createIcon(Icon$81)
    },
    DownloadButton: { Default: createIcon(Icon$31) },
    Menu: {
      Accessibility: createIcon(Icon$0),
      ArrowLeft: createIcon(Icon$11),
      ArrowRight: createIcon(Icon$22),
      Audio: createIcon(Icon$53),
      Chapters: createIcon(Icon$16),
      Captions: createIcon(Icon$27),
      Playback: createIcon(Icon$63),
      Settings: createIcon(Icon$88),
      AudioBoostUp: createIcon(Icon$104),
      AudioBoostDown: createIcon(Icon$105),
      SpeedUp: createIcon(Icon$58),
      SpeedDown: createIcon(Icon$58),
      QualityUp: createIcon(Icon$86),
      QualityDown: createIcon(Icon$86),
      FontSizeUp: createIcon(Icon$13),
      FontSizeDown: createIcon(Icon$8),
      OpacityUp: createIcon(Icon$33),
      OpacityDown: createIcon(Icon$56),
      RadioCheck: createIcon(Icon$19)
    },
    KeyboardDisplay: {
      Play: createIcon(Icon$62),
      Pause: createIcon(Icon$59),
      Mute: createIcon(Icon$54),
      VolumeUp: createIcon(Icon$104),
      VolumeDown: createIcon(Icon$105),
      EnterFullscreen: createIcon(Icon$40),
      ExitFullscreen: createIcon(Icon$39),
      EnterPiP: createIcon(Icon$61),
      ExitPiP: createIcon(Icon$60),
      CaptionsOn: createIcon(Icon$26),
      CaptionsOff: createIcon(Icon$27),
      SeekForward: createIcon(Icon$35),
      SeekBackward: createIcon(Icon$34)
    }
  };

  // frontend/node_modules/@vidstack/react/prod/player/vidstack-default-components.js
  init_define_import_meta_env();
  init_vidstack_B9fTkxQO();
  init_vidstack_CvezEmSQ();

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-Dv9-kFzc.js
  init_define_import_meta_env();
  init_vidstack_B9fTkxQO();
  var RemotionThumbnail = /* @__PURE__ */ signal(null);
  var RemotionSliderThumbnail = /* @__PURE__ */ signal(null);

  // frontend/node_modules/@vidstack/react/prod/chunks/vidstack-D9ibjprz.js
  init_define_import_meta_env();
  init_vidstack_B9fTkxQO();
  var React11 = __toESM(require_react_shim(), 1);
  function useResizeObserver(el, callback) {
    React11.useEffect(() => {
      if (!el) return;
      callback();
      const observer = new ResizeObserver(animationFrameThrottle(callback));
      observer.observe(el);
      return () => observer.disconnect();
    }, [el, callback]);
  }
  function useTransitionActive(el) {
    const [isActive, setIsActive] = React11.useState(false);
    React11.useEffect(() => {
      if (!el) return;
      const events = new EventsController(el).add("transitionstart", () => setIsActive(true)).add("transitionend", () => setIsActive(false));
      return () => events.abort();
    }, [el]);
    return isActive;
  }
  function useMouseEnter(el) {
    const [isMouseEnter, setIsMouseEnter] = React11.useState(false);
    React11.useEffect(() => {
      if (!el) return;
      const events = new EventsController(el).add("mouseenter", () => setIsMouseEnter(true)).add("mouseleave", () => setIsMouseEnter(false));
      return () => events.abort();
    }, [el]);
    return isMouseEnter;
  }
  function useFocusIn(el) {
    const [isFocusIn, setIsFocusIn] = React11.useState(false);
    React11.useEffect(() => {
      if (!el) return;
      const events = new EventsController(el).add("focusin", () => setIsFocusIn(true)).add("focusout", () => setIsFocusIn(false));
      return () => events.abort();
    }, [el]);
    return isFocusIn;
  }
  function useActive(el) {
    const isMouseEnter = useMouseEnter(el), isFocusIn = useFocusIn(el), prevMouseEnter = React11.useRef(false);
    if (prevMouseEnter.current && !isMouseEnter && !isFocusIn) return false;
    prevMouseEnter.current = isMouseEnter;
    return isMouseEnter || isFocusIn;
  }
  function useColorSchemePreference() {
    const [colorScheme, setColorScheme] = React11.useState("dark");
    React11.useEffect(() => {
      const media = window.matchMedia("(prefers-color-scheme: light)");
      function onChange() {
        setColorScheme(media.matches ? "light" : "dark");
      }
      onChange();
      return listenEvent(media, "change", onChange);
    }, []);
    return colorScheme;
  }
  function useLayoutName(name) {
    const player = useMediaPlayer();
    React11.useEffect(() => {
      if (!player) return;
      return effect(() => {
        const el = player.$el;
        el?.setAttribute("data-layout", name);
        return () => el?.removeAttribute("data-layout");
      });
    }, [player]);
  }

  // frontend/node_modules/@vidstack/react/prod/player/vidstack-default-components.js
  var React12 = __toESM(require_react_shim(), 1);
  var import_react_dom2 = __toESM(require_react_dom_shim(), 1);
  var DefaultLayoutContext = React12.createContext({});
  DefaultLayoutContext.displayName = "DefaultLayoutContext";
  function useDefaultLayoutContext() {
    return React12.useContext(DefaultLayoutContext);
  }
  function useDefaultLayoutWord(word) {
    const { translations } = useDefaultLayoutContext();
    return i18n(translations, word);
  }
  function i18n(translations, word) {
    return translations?.[word] ?? word;
  }
  function useColorSchemeClass(colorScheme) {
    const systemColorPreference = useColorSchemePreference();
    if (colorScheme === "default") return null;
    else if (colorScheme === "system") return systemColorPreference;
    else return colorScheme;
  }
  function createDefaultMediaLayout({ type, smLayoutWhen, renderLayout }) {
    const Layout = React12.forwardRef(({ children, className, disableTimeSlider = false, hideQualityBitrate = false, icons, colorScheme = "system", download = null, menuContainer = null, menuGroup = "bottom", noAudioGain = false, audioGains = {
      min: 0,
      max: 300,
      step: 25
    }, noGestures = false, noKeyboardAnimations = false, noModal = false, noScrubGesture, playbackRates = {
      min: 0,
      max: 2,
      step: 0.25
    }, seekStep = 10, showMenuDelay, showTooltipDelay = 700, sliderChaptersMinWidth = 325, slots, smallLayoutWhen = smLayoutWhen, thumbnails = null, translations, ...props }, forwardRef8) => {
      const media = useMediaContext2(), $load = useSignal(media.$props.load), $canLoad = useMediaState("canLoad"), $viewType = useMediaState("viewType"), $streamType = useMediaState("streamType"), $smallWhen = createComputed(() => {
        return isBoolean(smallLayoutWhen) ? smallLayoutWhen : smallLayoutWhen(media.player.state);
      }, [smallLayoutWhen]), userPrefersAnnouncements = createSignal(true), userPrefersKeyboardAnimations = createSignal(true), isMatch = $viewType === type, isSmallLayout = $smallWhen(), isForcedLayout = isBoolean(smallLayoutWhen), isLoadLayout = $load === "play" && !$canLoad, canRender = $canLoad || isForcedLayout || isLoadLayout, colorSchemeClass = useColorSchemeClass(colorScheme), layoutEl = createSignal(null);
      useSignal($smallWhen);
      return /* @__PURE__ */ React12.createElement("div", {
        ...props,
        className: `vds-${type}-layout` + (colorSchemeClass ? ` ${colorSchemeClass}` : "") + (className ? ` ${className}` : ""),
        "data-match": isMatch ? "" : null,
        "data-sm": isSmallLayout ? "" : null,
        "data-lg": !isSmallLayout ? "" : null,
        "data-size": isSmallLayout ? "sm" : "lg",
        "data-no-scrub-gesture": noScrubGesture ? "" : null,
        ref: composeRefs(layoutEl.set, forwardRef8)
      }, canRender && isMatch ? /* @__PURE__ */ React12.createElement(DefaultLayoutContext.Provider, { value: {
        disableTimeSlider,
        hideQualityBitrate,
        icons,
        colorScheme,
        download,
        isSmallLayout,
        menuContainer,
        menuGroup,
        noAudioGain,
        audioGains,
        layoutEl,
        noGestures,
        noKeyboardAnimations,
        noModal,
        noScrubGesture,
        showMenuDelay,
        showTooltipDelay,
        sliderChaptersMinWidth,
        slots,
        seekStep,
        playbackRates,
        thumbnails,
        translations,
        userPrefersAnnouncements,
        userPrefersKeyboardAnimations
      } }, renderLayout({
        streamType: $streamType,
        isSmallLayout,
        isLoadLayout
      }), children) : null);
    });
    Layout.displayName = "DefaultMediaLayout";
    return Layout;
  }
  function useDefaultAudioLayoutSlots() {
    return React12.useContext(DefaultLayoutContext).slots;
  }
  function useDefaultVideoLayoutSlots() {
    return React12.useContext(DefaultLayoutContext).slots;
  }
  function slot(slots, name, defaultValue) {
    const slot2 = slots?.[name], capitalizedName = uppercaseFirstChar(name);
    return /* @__PURE__ */ React12.createElement(React12.Fragment, null, slots?.[`before${capitalizedName}`], isUndefined(slot2) ? defaultValue : slot2, slots?.[`after${capitalizedName}`]);
  }
  function DefaultAnnouncer() {
    const { userPrefersAnnouncements, translations } = useDefaultLayoutContext();
    if (!useSignal(userPrefersAnnouncements)) return null;
    return /* @__PURE__ */ React12.createElement(MediaAnnouncer2, { translations });
  }
  DefaultAnnouncer.displayName = "DefaultAnnouncer";
  function DefaultTooltip({ content, placement, children }) {
    const { showTooltipDelay } = useDefaultLayoutContext();
    return /* @__PURE__ */ React12.createElement(Root$32, { showDelay: showTooltipDelay }, /* @__PURE__ */ React12.createElement(Trigger2, { asChild: true }, children), /* @__PURE__ */ React12.createElement(Content, {
      className: "vds-tooltip-content",
      placement
    }, content));
  }
  DefaultTooltip.displayName = "DefaultTooltip";
  function DefaultPlayButton({ tooltip }) {
    const { icons: Icons } = useDefaultLayoutContext(), playText = useDefaultLayoutWord("Play"), pauseText = useDefaultLayoutWord("Pause"), $paused = useMediaState("paused"), $ended = useMediaState("ended");
    return /* @__PURE__ */ React12.createElement(DefaultTooltip, {
      content: $paused ? playText : pauseText,
      placement: tooltip
    }, /* @__PURE__ */ React12.createElement(PlayButton2, {
      className: "vds-play-button vds-button",
      "aria-label": playText
    }, $ended ? /* @__PURE__ */ React12.createElement(Icons.PlayButton.Replay, { className: "vds-icon" }) : $paused ? /* @__PURE__ */ React12.createElement(Icons.PlayButton.Play, { className: "vds-icon" }) : /* @__PURE__ */ React12.createElement(Icons.PlayButton.Pause, { className: "vds-icon" })));
  }
  DefaultPlayButton.displayName = "DefaultPlayButton";
  var DefaultMuteButton = React12.forwardRef(({ tooltip }, forwardRef8) => {
    const { icons: Icons } = useDefaultLayoutContext(), muteText = useDefaultLayoutWord("Mute"), unmuteText = useDefaultLayoutWord("Unmute"), $muted = useMediaState("muted"), $volume = useMediaState("volume");
    return /* @__PURE__ */ React12.createElement(DefaultTooltip, {
      content: $muted ? unmuteText : muteText,
      placement: tooltip
    }, /* @__PURE__ */ React12.createElement(MuteButton2, {
      className: "vds-mute-button vds-button",
      "aria-label": muteText,
      ref: forwardRef8
    }, $muted || $volume == 0 ? /* @__PURE__ */ React12.createElement(Icons.MuteButton.Mute, { className: "vds-icon" }) : $volume < 0.5 ? /* @__PURE__ */ React12.createElement(Icons.MuteButton.VolumeLow, { className: "vds-icon" }) : /* @__PURE__ */ React12.createElement(Icons.MuteButton.VolumeHigh, { className: "vds-icon" })));
  });
  DefaultMuteButton.displayName = "DefaultMuteButton";
  function DefaultCaptionButton({ tooltip }) {
    const { icons: Icons } = useDefaultLayoutContext(), captionsText = useDefaultLayoutWord("Captions"), onText = useDefaultLayoutWord("Closed-Captions On"), offText = useDefaultLayoutWord("Closed-Captions Off"), $track = useMediaState("textTrack"), isOn = $track && isTrackCaptionKind($track);
    return /* @__PURE__ */ React12.createElement(DefaultTooltip, {
      content: isOn ? onText : offText,
      placement: tooltip
    }, /* @__PURE__ */ React12.createElement(CaptionButton2, {
      className: "vds-caption-button vds-button",
      "aria-label": captionsText
    }, isOn ? /* @__PURE__ */ React12.createElement(Icons.CaptionButton.On, { className: "vds-icon" }) : /* @__PURE__ */ React12.createElement(Icons.CaptionButton.Off, { className: "vds-icon" })));
  }
  DefaultCaptionButton.displayName = "DefaultCaptionButton";
  function DefaultPIPButton({ tooltip }) {
    const { icons: Icons } = useDefaultLayoutContext(), pipText = useDefaultLayoutWord("PiP"), enterText = useDefaultLayoutWord("Enter PiP"), exitText = useDefaultLayoutWord("Exit PiP"), $pip = useMediaState("pictureInPicture");
    return /* @__PURE__ */ React12.createElement(DefaultTooltip, {
      content: $pip ? exitText : enterText,
      placement: tooltip
    }, /* @__PURE__ */ React12.createElement(PIPButton2, {
      className: "vds-pip-button vds-button",
      "aria-label": pipText
    }, $pip ? /* @__PURE__ */ React12.createElement(Icons.PIPButton.Exit, { className: "vds-icon" }) : /* @__PURE__ */ React12.createElement(Icons.PIPButton.Enter, { className: "vds-icon" })));
  }
  DefaultPIPButton.displayName = "DefaultPIPButton";
  function DefaultFullscreenButton({ tooltip }) {
    const { icons: Icons } = useDefaultLayoutContext(), fullscreenText = useDefaultLayoutWord("Fullscreen"), enterText = useDefaultLayoutWord("Enter Fullscreen"), exitText = useDefaultLayoutWord("Exit Fullscreen"), $fullscreen = useMediaState("fullscreen");
    return /* @__PURE__ */ React12.createElement(DefaultTooltip, {
      content: $fullscreen ? exitText : enterText,
      placement: tooltip
    }, /* @__PURE__ */ React12.createElement(FullscreenButton2, {
      className: "vds-fullscreen-button vds-button",
      "aria-label": fullscreenText
    }, $fullscreen ? /* @__PURE__ */ React12.createElement(Icons.FullscreenButton.Exit, { className: "vds-icon" }) : /* @__PURE__ */ React12.createElement(Icons.FullscreenButton.Enter, { className: "vds-icon" })));
  }
  DefaultFullscreenButton.displayName = "DefaultFullscreenButton";
  function DefaultSeekButton({ backward, tooltip }) {
    const { icons: Icons, seekStep } = useDefaultLayoutContext(), seekForwardText = useDefaultLayoutWord("Seek Forward"), seekBackwardText = useDefaultLayoutWord("Seek Backward"), seconds = (backward ? -1 : 1) * seekStep, label = seconds >= 0 ? seekForwardText : seekBackwardText;
    return /* @__PURE__ */ React12.createElement(DefaultTooltip, {
      content: label,
      placement: tooltip
    }, /* @__PURE__ */ React12.createElement(SeekButton2, {
      className: "vds-seek-button vds-button",
      seconds,
      "aria-label": label
    }, seconds >= 0 ? /* @__PURE__ */ React12.createElement(Icons.SeekButton.Forward, { className: "vds-icon" }) : /* @__PURE__ */ React12.createElement(Icons.SeekButton.Backward, { className: "vds-icon" })));
  }
  DefaultSeekButton.displayName = "DefaultSeekButton";
  function DefaultAirPlayButton({ tooltip }) {
    const { icons: Icons } = useDefaultLayoutContext(), airPlayText = useDefaultLayoutWord("AirPlay"), $state = useMediaState("remotePlaybackState"), label = `${airPlayText} ${useDefaultLayoutWord(uppercaseFirstChar($state))}`, Icon3 = ($state === "connecting" ? Icons.AirPlayButton.Connecting : $state === "connected" ? Icons.AirPlayButton.Connected : null) ?? Icons.AirPlayButton.Default;
    return /* @__PURE__ */ React12.createElement(DefaultTooltip, {
      content: airPlayText,
      placement: tooltip
    }, /* @__PURE__ */ React12.createElement(AirPlayButton2, {
      className: "vds-airplay-button vds-button",
      "aria-label": label
    }, /* @__PURE__ */ React12.createElement(Icon3, { className: "vds-icon" })));
  }
  DefaultAirPlayButton.displayName = "DefaultAirPlayButton";
  function DefaultGoogleCastButton({ tooltip }) {
    const { icons: Icons } = useDefaultLayoutContext(), googleCastText = useDefaultLayoutWord("Google Cast"), $state = useMediaState("remotePlaybackState"), label = `${googleCastText} ${useDefaultLayoutWord(uppercaseFirstChar($state))}`, Icon3 = ($state === "connecting" ? Icons.GoogleCastButton.Connecting : $state === "connected" ? Icons.GoogleCastButton.Connected : null) ?? Icons.GoogleCastButton.Default;
    return /* @__PURE__ */ React12.createElement(DefaultTooltip, {
      content: googleCastText,
      placement: tooltip
    }, /* @__PURE__ */ React12.createElement(GoogleCastButton2, {
      className: "vds-google-cast-button vds-button",
      "aria-label": label
    }, /* @__PURE__ */ React12.createElement(Icon3, { className: "vds-icon" })));
  }
  DefaultGoogleCastButton.displayName = "DefaultGoogleCastButton";
  function DefaultLiveButton() {
    const $live = useMediaState("live"), label = useDefaultLayoutWord("Skip To Live"), liveText = useDefaultLayoutWord("LIVE");
    return $live ? /* @__PURE__ */ React12.createElement(LiveButton2, {
      className: "vds-live-button",
      "aria-label": label
    }, /* @__PURE__ */ React12.createElement("span", { className: "vds-live-button-text" }, liveText)) : null;
  }
  DefaultLiveButton.displayName = "DefaultLiveButton";
  function DefaultDownloadButton() {
    const { download, icons: Icons } = useDefaultLayoutContext(), $src = useMediaState("source"), file = getDownloadFile({
      title: useMediaState("title"),
      src: $src,
      download
    }), downloadText = useDefaultLayoutWord("Download");
    return isString(file?.url) ? /* @__PURE__ */ React12.createElement(DefaultTooltip, {
      content: downloadText,
      placement: "top"
    }, /* @__PURE__ */ React12.createElement("a", {
      role: "button",
      className: "vds-download-button vds-button",
      "aria-label": downloadText,
      href: appendParamsToURL2(file.url, { download: file.name }),
      download: file.name,
      target: "_blank"
    }, Icons.DownloadButton ? /* @__PURE__ */ React12.createElement(Icons.DownloadButton.Default, { className: "vds-icon" }) : null)) : null;
  }
  DefaultDownloadButton.displayName = "DefaultDownloadButton";
  function DefaultCaptions() {
    const exampleText = useDefaultLayoutWord("Captions look like this");
    return /* @__PURE__ */ React12.createElement(Captions2, {
      className: "vds-captions",
      exampleText
    });
  }
  DefaultCaptions.displayName = "DefaultCaptions";
  function DefaultControlsSpacer() {
    return /* @__PURE__ */ React12.createElement("div", { className: "vds-controls-spacer" });
  }
  DefaultControlsSpacer.displayName = "DefaultControlsSpacer";
  function useParentDialogEl() {
    const { layoutEl } = useDefaultLayoutContext(), $layoutEl = useSignal(layoutEl);
    return React12.useMemo(() => $layoutEl?.closest("dialog"), [$layoutEl]);
  }
  function DefaultChaptersMenu({ tooltip, placement, portalClass = "" }) {
    const { showMenuDelay, noModal, isSmallLayout, icons: Icons, menuGroup, menuContainer, colorScheme } = useDefaultLayoutContext(), chaptersText = useDefaultLayoutWord("Chapters"), options = useChapterOptions(), disabled = !options.length, { thumbnails } = useDefaultLayoutContext(), $src = useMediaState("currentSrc"), $viewType = useMediaState("viewType"), $offset = !isSmallLayout && menuGroup === "bottom" && $viewType === "video" ? 26 : 0, $RemotionThumbnail = useSignal(RemotionThumbnail), colorSchemeClass = useColorSchemeClass(colorScheme), [isOpen, setIsOpen] = React12.useState(false), dialogEl = useParentDialogEl();
    if (disabled) return null;
    function onOpen() {
      (0, import_react_dom2.flushSync)(() => {
        setIsOpen(true);
      });
    }
    function onClose() {
      setIsOpen(false);
    }
    const Content2 = /* @__PURE__ */ React12.createElement(Items, {
      className: "vds-chapters-menu-items vds-menu-items",
      placement,
      offset: $offset
    }, isOpen ? /* @__PURE__ */ React12.createElement(Root$1, {
      className: "vds-chapters-radio-group vds-radio-group",
      value: options.selectedValue,
      "data-thumbnails": thumbnails ? "" : null
    }, options.map(({ cue, label, value, startTimeText, durationText, select, setProgressVar }) => /* @__PURE__ */ React12.createElement(Item$1, {
      className: "vds-chapter-radio vds-radio",
      value,
      key: value,
      onSelect: select,
      ref: setProgressVar
    }, thumbnails ? /* @__PURE__ */ React12.createElement(Root$3, {
      src: thumbnails,
      className: "vds-thumbnail",
      time: cue.startTime
    }, /* @__PURE__ */ React12.createElement(Img, null)) : $RemotionThumbnail && isRemotionSrc($src) ? /* @__PURE__ */ React12.createElement($RemotionThumbnail, {
      className: "vds-thumbnail",
      frame: cue.startTime * $src.fps
    }) : null, /* @__PURE__ */ React12.createElement("div", { className: "vds-chapter-radio-content" }, /* @__PURE__ */ React12.createElement("span", { className: "vds-chapter-radio-label" }, label), /* @__PURE__ */ React12.createElement("span", { className: "vds-chapter-radio-start-time" }, startTimeText), /* @__PURE__ */ React12.createElement("span", { className: "vds-chapter-radio-duration" }, durationText))))) : null);
    return /* @__PURE__ */ React12.createElement(Root, {
      className: "vds-chapters-menu vds-menu",
      showDelay: showMenuDelay,
      onOpen,
      onClose
    }, /* @__PURE__ */ React12.createElement(DefaultTooltip, {
      content: chaptersText,
      placement: tooltip
    }, /* @__PURE__ */ React12.createElement(Button, {
      className: "vds-menu-button vds-button",
      disabled,
      "aria-label": chaptersText
    }, /* @__PURE__ */ React12.createElement(Icons.Menu.Chapters, { className: "vds-icon" }))), noModal || !isSmallLayout ? Content2 : /* @__PURE__ */ React12.createElement(Portal, {
      container: menuContainer ?? dialogEl,
      className: portalClass + (colorSchemeClass ? ` ${colorSchemeClass}` : ""),
      disabled: "fullscreen",
      "data-sm": isSmallLayout ? "" : null,
      "data-lg": !isSmallLayout ? "" : null,
      "data-size": isSmallLayout ? "sm" : "lg"
    }, Content2));
  }
  DefaultChaptersMenu.displayName = "DefaultChaptersMenu";
  var FONT_COLOR_OPTION = { type: "color" };
  var FONT_FAMILY_OPTION = {
    type: "radio",
    values: {
      "Monospaced Serif": "mono-serif",
      "Proportional Serif": "pro-serif",
      "Monospaced Sans-Serif": "mono-sans",
      "Proportional Sans-Serif": "pro-sans",
      Casual: "casual",
      Cursive: "cursive",
      "Small Capitals": "capitals"
    }
  };
  var FONT_SIZE_OPTION = {
    type: "slider",
    min: 0,
    max: 400,
    step: 25,
    upIcon: null,
    downIcon: null
  };
  var FONT_OPACITY_OPTION = {
    type: "slider",
    min: 0,
    max: 100,
    step: 5,
    upIcon: null,
    downIcon: null
  };
  var FONT_TEXT_SHADOW_OPTION = {
    type: "radio",
    values: [
      "None",
      "Drop Shadow",
      "Raised",
      "Depressed",
      "Outline"
    ]
  };
  var FONT_DEFAULTS = {
    fontFamily: "pro-sans",
    fontSize: "100%",
    textColor: "#ffffff",
    textOpacity: "100%",
    textShadow: "none",
    textBg: "#000000",
    textBgOpacity: "100%",
    displayBg: "#000000",
    displayBgOpacity: "0%"
  };
  var FONT_SIGNALS = Object.keys(FONT_DEFAULTS).reduce((prev, type) => ({
    ...prev,
    [type]: signal(FONT_DEFAULTS[type])
  }), {});
  if (!IS_SERVER2) for (const type of Object.keys(FONT_SIGNALS)) {
    const value = localStorage.getItem(`vds-player:${camelToKebabCase(type)}`);
    if (isString(value)) FONT_SIGNALS[type].set(value);
  }
  function onFontReset() {
    for (const type of Object.keys(FONT_SIGNALS)) {
      const defaultValue = FONT_DEFAULTS[type];
      FONT_SIGNALS[type].set(defaultValue);
    }
  }
  function hexToRgb(hex) {
    const { style } = new Option();
    style.color = hex;
    return style.color.match(/\((.*?)\)/)[1].replace(/,/g, " ");
  }
  var isWatchingVars = false;
  var players = /* @__PURE__ */ new Set();
  function updateFontCssVars() {
    if (IS_SERVER2) return;
    const { player } = useMediaContext();
    players.add(player);
    applyFontCssVars(player);
    onDispose(() => players.delete(player));
    if (!isWatchingVars) {
      scoped(() => {
        for (const type of keysOf(FONT_SIGNALS)) {
          const $value = FONT_SIGNALS[type], defaultValue = FONT_DEFAULTS[type], storageKey = `vds-player:${camelToKebabCase(type)}`;
          effect(() => {
            const value = $value(), isDefaultVarValue = value === defaultValue;
            for (const player2 of players) setFontCssVar(player2, type, value);
            if (isDefaultVarValue) localStorage.removeItem(storageKey);
            else localStorage.setItem(storageKey, value);
          });
        }
      }, null);
      isWatchingVars = true;
    }
  }
  function applyFontCssVars(player) {
    for (const type of keysOf(FONT_SIGNALS)) setFontCssVar(player, type, FONT_SIGNALS[type]());
  }
  function setFontCssVar(player, type, value) {
    const defaultValue = FONT_DEFAULTS[type], varName = `--media-user-${camelToKebabCase(type)}`, varValue = value !== defaultValue ? getCssVarValue(type, value) : null;
    if (type === "fontFamily") {
      const fontVariant = value === "capitals" ? "small-caps" : null;
      player.el?.style.setProperty("--media-user-font-variant", fontVariant);
    }
    player.el?.style.setProperty(varName, varValue);
  }
  function getCssVarValue(type, value) {
    switch (type) {
      case "fontFamily":
        return getFontFamilyCSSVarValue(value);
      case "fontSize":
      case "textOpacity":
      case "textBgOpacity":
      case "displayBgOpacity":
        return percentToRatio(value);
      case "textColor":
        return `rgb(${hexToRgb(value)} / var(--media-user-text-opacity, 1))`;
      case "textShadow":
        return getTextShadowCssVarValue(value);
      case "textBg":
        return `rgb(${hexToRgb(value)} / var(--media-user-text-bg-opacity, 1))`;
      case "displayBg":
        return `rgb(${hexToRgb(value)} / var(--media-user-display-bg-opacity, 1))`;
    }
  }
  function percentToRatio(value) {
    return (parseInt(value) / 100).toString();
  }
  function getFontFamilyCSSVarValue(value) {
    switch (value) {
      case "mono-serif":
        return '"Courier New", Courier, "Nimbus Mono L", "Cutive Mono", monospace';
      case "mono-sans":
        return '"Deja Vu Sans Mono", "Lucida Console", Monaco, Consolas, "PT Mono", monospace';
      case "pro-sans":
        return 'Roboto, "Arial Unicode Ms", Arial, Helvetica, Verdana, "PT Sans Caption", sans-serif';
      case "casual":
        return '"Comic Sans MS", Impact, Handlee, fantasy';
      case "cursive":
        return '"Monotype Corsiva", "URW Chancery L", "Apple Chancery", "Dancing Script", cursive';
      case "capitals":
        return '"Arial Unicode Ms", Arial, Helvetica, Verdana, "Marcellus SC", sans-serif + font-variant=small-caps';
      default:
        return '"Times New Roman", Times, Georgia, Cambria, "PT Serif Caption", serif';
    }
  }
  function getTextShadowCssVarValue(value) {
    switch (value) {
      case "drop shadow":
        return "rgb(34, 34, 34) 1.86389px 1.86389px 2.79583px, rgb(34, 34, 34) 1.86389px 1.86389px 3.72778px, rgb(34, 34, 34) 1.86389px 1.86389px 4.65972px";
      case "raised":
        return "rgb(34, 34, 34) 1px 1px, rgb(34, 34, 34) 2px 2px";
      case "depressed":
        return "rgb(204, 204, 204) 1px 1px, rgb(34, 34, 34) -1px -1px";
      case "outline":
        return "rgb(34, 34, 34) 0px 0px 1.86389px, rgb(34, 34, 34) 0px 0px 1.86389px, rgb(34, 34, 34) 0px 0px 1.86389px, rgb(34, 34, 34) 0px 0px 1.86389px, rgb(34, 34, 34) 0px 0px 1.86389px";
      default:
        return "";
    }
  }
  function DefaultMenuSection({ label, value, children }) {
    const id2 = React12.useId();
    if (!label) return /* @__PURE__ */ React12.createElement("div", { className: "vds-menu-section" }, /* @__PURE__ */ React12.createElement("div", { className: "vds-menu-section-body" }, children));
    return /* @__PURE__ */ React12.createElement("section", {
      className: "vds-menu-section",
      role: "group",
      "aria-labelledby": id2
    }, /* @__PURE__ */ React12.createElement("div", { className: "vds-menu-section-title" }, /* @__PURE__ */ React12.createElement("header", { id: id2 }, label), value ? /* @__PURE__ */ React12.createElement("div", { className: "vds-menu-section-value" }, value) : null), /* @__PURE__ */ React12.createElement("div", { className: "vds-menu-section-body" }, children));
  }
  DefaultMenuSection.displayName = "DefaultMenuSection";
  function DefaultMenuButton({ label, hint = "", Icon: Icon3, disabled = false }) {
    const { icons: Icons } = React12.useContext(DefaultLayoutContext);
    return /* @__PURE__ */ React12.createElement(Button, {
      className: "vds-menu-item",
      disabled
    }, /* @__PURE__ */ React12.createElement(Icons.Menu.ArrowLeft, { className: "vds-menu-close-icon vds-icon" }), Icon3 ? /* @__PURE__ */ React12.createElement(Icon3, { className: "vds-menu-item-icon vds-icon" }) : null, /* @__PURE__ */ React12.createElement("span", { className: "vds-menu-item-label" }, label), /* @__PURE__ */ React12.createElement("span", { className: "vds-menu-item-hint" }, hint), /* @__PURE__ */ React12.createElement(Icons.Menu.ArrowRight, { className: "vds-menu-open-icon vds-icon" }));
  }
  DefaultMenuButton.displayName = "DefaultMenuButton";
  function DefaultMenuItem({ label, children }) {
    return /* @__PURE__ */ React12.createElement("div", { className: "vds-menu-item" }, /* @__PURE__ */ React12.createElement("div", { className: "vds-menu-item-label" }, label), children);
  }
  DefaultMenuItem.displayName = "DefaultMenuItem";
  function DefaultMenuRadioGroup({ value, options, onChange }) {
    const { icons: Icons } = useDefaultLayoutContext();
    return /* @__PURE__ */ React12.createElement(Root$1, {
      className: "vds-radio-group",
      value,
      onChange
    }, options.map((option) => /* @__PURE__ */ React12.createElement(Item$1, {
      className: "vds-radio",
      value: option.value,
      key: option.value
    }, /* @__PURE__ */ React12.createElement(Icons.Menu.RadioCheck, { className: "vds-icon" }), /* @__PURE__ */ React12.createElement("span", {
      className: "vds-radio-label",
      "data-part": "label"
    }, option.label))));
  }
  DefaultMenuRadioGroup.displayName = "DefaultMenuRadioGroup";
  function createRadioOptions(entries) {
    return React12.useMemo(() => isArray(entries) ? entries.map((entry) => ({
      label: entry,
      value: entry.toLowerCase()
    })) : Object.keys(entries).map((label) => ({
      label,
      value: entries[label]
    })), [entries]);
  }
  function DefaultMenuSliderItem({ label, value, UpIcon, DownIcon, children, isMin, isMax }) {
    const hasTitle = label || value, Content2 = /* @__PURE__ */ React12.createElement(React12.Fragment, null, DownIcon ? /* @__PURE__ */ React12.createElement(DownIcon, { className: "vds-icon down" }) : null, children, UpIcon ? /* @__PURE__ */ React12.createElement(UpIcon, { className: "vds-icon up" }) : null);
    return /* @__PURE__ */ React12.createElement("div", {
      className: `vds-menu-item vds-menu-slider-item${hasTitle ? " group" : ""}`,
      "data-min": isMin ? "" : null,
      "data-max": isMax ? "" : null
    }, hasTitle ? /* @__PURE__ */ React12.createElement(React12.Fragment, null, /* @__PURE__ */ React12.createElement("div", { className: "vds-menu-slider-title" }, label ? /* @__PURE__ */ React12.createElement("div", null, label) : null, value ? /* @__PURE__ */ React12.createElement("div", null, value) : null), /* @__PURE__ */ React12.createElement("div", { className: "vds-menu-slider-body" }, Content2)) : Content2);
  }
  DefaultMenuSliderItem.displayName = "DefaultMenuSliderItem";
  function DefaultSliderParts() {
    return /* @__PURE__ */ React12.createElement(React12.Fragment, null, /* @__PURE__ */ React12.createElement(Track, { className: "vds-slider-track" }), /* @__PURE__ */ React12.createElement(TrackFill, { className: "vds-slider-track-fill vds-slider-track" }), /* @__PURE__ */ React12.createElement(Thumb, { className: "vds-slider-thumb" }));
  }
  DefaultSliderParts.displayName = "DefaultSliderParts";
  function DefaultSliderSteps() {
    return /* @__PURE__ */ React12.createElement(Steps, { className: "vds-slider-steps" }, (step) => /* @__PURE__ */ React12.createElement("div", {
      className: "vds-slider-step",
      key: String(step)
    }));
  }
  DefaultSliderSteps.displayName = "DefaultSliderSteps";
  function DefaultFontMenu() {
    const label = useDefaultLayoutWord("Caption Styles"), $hasCaptions = useMediaState("hasCaptions"), fontSectionLabel = useDefaultLayoutWord("Font"), textSectionLabel = useDefaultLayoutWord("Text"), textBgSectionLabel = useDefaultLayoutWord("Text Background"), displayBgSectionLabel = useDefaultLayoutWord("Display Background");
    if (!$hasCaptions) return null;
    return /* @__PURE__ */ React12.createElement(Root, { className: "vds-font-menu vds-menu" }, /* @__PURE__ */ React12.createElement(DefaultMenuButton, { label }), /* @__PURE__ */ React12.createElement(Items, { className: "vds-font-style-items vds-menu-items" }, /* @__PURE__ */ React12.createElement(DefaultMenuSection, { label: fontSectionLabel }, /* @__PURE__ */ React12.createElement(DefaultFontFamilyMenu, null), /* @__PURE__ */ React12.createElement(DefaultFontSizeSlider, null)), /* @__PURE__ */ React12.createElement(DefaultMenuSection, { label: textSectionLabel }, /* @__PURE__ */ React12.createElement(DefaultTextColorInput, null), /* @__PURE__ */ React12.createElement(DefaultTextShadowMenu, null), /* @__PURE__ */ React12.createElement(DefaultTextOpacitySlider, null)), /* @__PURE__ */ React12.createElement(DefaultMenuSection, { label: textBgSectionLabel }, /* @__PURE__ */ React12.createElement(DefaultTextBgInput, null), /* @__PURE__ */ React12.createElement(DefaultTextBgOpacitySlider, null)), /* @__PURE__ */ React12.createElement(DefaultMenuSection, { label: displayBgSectionLabel }, /* @__PURE__ */ React12.createElement(DefaultDisplayBgInput, null), /* @__PURE__ */ React12.createElement(DefaultDisplayBgOpacitySlider, null)), /* @__PURE__ */ React12.createElement(DefaultMenuSection, null, /* @__PURE__ */ React12.createElement(DefaultResetMenuItem, null))));
  }
  DefaultFontMenu.displayName = "DefaultFontMenu";
  function DefaultFontFamilyMenu() {
    return /* @__PURE__ */ React12.createElement(DefaultFontSetting, {
      label: "Family",
      type: "fontFamily",
      option: FONT_FAMILY_OPTION
    });
  }
  DefaultFontFamilyMenu.displayName = "DefaultFontFamilyMenu";
  function DefaultFontSizeSlider() {
    const { icons: Icons } = useDefaultLayoutContext(), option = {
      ...FONT_SIZE_OPTION,
      upIcon: Icons.Menu.FontSizeUp,
      downIcon: Icons.Menu.FontSizeDown
    };
    return /* @__PURE__ */ React12.createElement(DefaultFontSetting, {
      label: "Size",
      type: "fontSize",
      option
    });
  }
  DefaultFontSizeSlider.displayName = "DefaultFontSizeSlider";
  function DefaultTextColorInput() {
    return /* @__PURE__ */ React12.createElement(DefaultFontSetting, {
      label: "Color",
      type: "textColor",
      option: FONT_COLOR_OPTION
    });
  }
  DefaultTextColorInput.displayName = "DefaultTextColorInput";
  function DefaultTextOpacitySlider() {
    const { icons: Icons } = useDefaultLayoutContext(), option = {
      ...FONT_OPACITY_OPTION,
      upIcon: Icons.Menu.OpacityUp,
      downIcon: Icons.Menu.OpacityDown
    };
    return /* @__PURE__ */ React12.createElement(DefaultFontSetting, {
      label: "Opacity",
      type: "textOpacity",
      option
    });
  }
  DefaultTextOpacitySlider.displayName = "DefaultTextOpacitySlider";
  function DefaultTextShadowMenu() {
    return /* @__PURE__ */ React12.createElement(DefaultFontSetting, {
      label: "Shadow",
      type: "textShadow",
      option: FONT_TEXT_SHADOW_OPTION
    });
  }
  DefaultTextShadowMenu.displayName = "DefaultTextShadowMenu";
  function DefaultTextBgInput() {
    return /* @__PURE__ */ React12.createElement(DefaultFontSetting, {
      label: "Color",
      type: "textBg",
      option: FONT_COLOR_OPTION
    });
  }
  DefaultTextBgInput.displayName = "DefaultTextBgInput";
  function DefaultTextBgOpacitySlider() {
    const { icons: Icons } = useDefaultLayoutContext(), option = {
      ...FONT_OPACITY_OPTION,
      upIcon: Icons.Menu.OpacityUp,
      downIcon: Icons.Menu.OpacityDown
    };
    return /* @__PURE__ */ React12.createElement(DefaultFontSetting, {
      label: "Opacity",
      type: "textBgOpacity",
      option
    });
  }
  DefaultTextBgOpacitySlider.displayName = "DefaultTextBgOpacitySlider";
  function DefaultDisplayBgInput() {
    return /* @__PURE__ */ React12.createElement(DefaultFontSetting, {
      label: "Color",
      type: "displayBg",
      option: FONT_COLOR_OPTION
    });
  }
  DefaultDisplayBgInput.displayName = "DefaultDisplayBgInput";
  function DefaultDisplayBgOpacitySlider() {
    const { icons: Icons } = useDefaultLayoutContext(), option = {
      ...FONT_OPACITY_OPTION,
      upIcon: Icons.Menu.OpacityUp,
      downIcon: Icons.Menu.OpacityDown
    };
    return /* @__PURE__ */ React12.createElement(DefaultFontSetting, {
      label: "Opacity",
      type: "displayBgOpacity",
      option
    });
  }
  DefaultDisplayBgOpacitySlider.displayName = "DefaultDisplayBgOpacitySlider";
  function DefaultFontSetting({ label, option, type }) {
    const player = useMediaPlayer(), $currentValue = FONT_SIGNALS[type], $value = useSignal($currentValue), translatedLabel = useDefaultLayoutWord(label);
    const notify2 = React12.useCallback(() => {
      player?.dispatchEvent(new Event("vds-font-change"));
    }, [player]);
    const onChange = React12.useCallback((newValue) => {
      $currentValue.set(newValue);
      notify2();
    }, [$currentValue, notify2]);
    if (option.type === "color") {
      let onColorChange = function(event) {
        onChange(event.target.value);
      };
      return /* @__PURE__ */ React12.createElement(DefaultMenuItem, { label: translatedLabel }, /* @__PURE__ */ React12.createElement("input", {
        className: "vds-color-picker",
        type: "color",
        value: $value,
        onChange: onColorChange
      }));
    }
    if (option.type === "slider") {
      let onSliderValueChange = function(value) {
        onChange(value + "%");
      };
      const { min: min2, max: max2, step, upIcon, downIcon } = option;
      return /* @__PURE__ */ React12.createElement(DefaultMenuSliderItem, {
        label: translatedLabel,
        value: $value,
        UpIcon: upIcon,
        DownIcon: downIcon,
        isMin: $value === min2 + "%",
        isMax: $value === max2 + "%"
      }, /* @__PURE__ */ React12.createElement(Root$5, {
        className: "vds-slider",
        min: min2,
        max: max2,
        step,
        keyStep: step,
        value: parseInt($value),
        "aria-label": translatedLabel,
        onValueChange: onSliderValueChange,
        onDragValueChange: onSliderValueChange
      }, /* @__PURE__ */ React12.createElement(DefaultSliderParts, null), /* @__PURE__ */ React12.createElement(DefaultSliderSteps, null)));
    }
    if (option.type === "radio") return /* @__PURE__ */ React12.createElement(DefaultFontRadioGroup, {
      id: camelToKebabCase(type),
      label: translatedLabel,
      value: $value,
      values: option.values,
      onChange
    });
    return null;
  }
  DefaultFontSetting.displayName = "DefaultFontSetting";
  function DefaultFontRadioGroup({ id: id2, label, value, values, onChange }) {
    const radioOptions = createRadioOptions(values), { translations } = useDefaultLayoutContext(), hint = React12.useMemo(() => {
      return i18n(translations, radioOptions.find((radio) => radio.value === value)?.label || "");
    }, [value, radioOptions]);
    return /* @__PURE__ */ React12.createElement(Root, { className: `vds-${id2}-menu vds-menu` }, /* @__PURE__ */ React12.createElement(DefaultMenuButton, {
      label,
      hint
    }), /* @__PURE__ */ React12.createElement(Items, { className: "vds-menu-items" }, /* @__PURE__ */ React12.createElement(DefaultMenuRadioGroup, {
      value,
      options: radioOptions,
      onChange
    })));
  }
  DefaultFontRadioGroup.displayName = "DefaultFontRadioGroup";
  function DefaultResetMenuItem() {
    const resetText = useDefaultLayoutWord("Reset");
    return /* @__PURE__ */ React12.createElement("button", {
      className: "vds-menu-item",
      role: "menuitem",
      onClick: onFontReset
    }, /* @__PURE__ */ React12.createElement("span", { className: "vds-menu-item-label" }, resetText));
  }
  DefaultResetMenuItem.displayName = "DefaultResetMenuItem";
  function DefaultMenuCheckbox({ label, checked, storageKey, defaultChecked = false, onChange }) {
    const [isChecked, setIsChecked] = React12.useState(defaultChecked), [isActive, setIsActive] = React12.useState(false);
    React12.useEffect(() => {
      const checked2 = !!((storageKey ? localStorage.getItem(storageKey) : null) ?? defaultChecked);
      setIsChecked(checked2);
      onChange?.(checked2);
    }, []);
    React12.useEffect(() => {
      if (isBoolean(checked)) setIsChecked(checked);
    }, [checked]);
    function onPress2(event) {
      if (event && "button" in event && event?.button === 1) return;
      const toggledCheck = !isChecked;
      setIsChecked(toggledCheck);
      if (storageKey) localStorage.setItem(storageKey, toggledCheck ? "1" : "");
      onChange?.(toggledCheck, event?.nativeEvent);
      setIsActive(false);
    }
    function onActive(event) {
      if (event.button !== 0) return;
      setIsActive(true);
    }
    function onKeyDown(event) {
      if (isKeyboardClick(event.nativeEvent)) onPress2();
    }
    return /* @__PURE__ */ React12.createElement("div", {
      className: "vds-menu-checkbox",
      role: "menuitemcheckbox",
      tabIndex: 0,
      "aria-label": label,
      "aria-checked": isChecked ? "true" : "false",
      "data-active": isActive ? "" : null,
      onPointerUp: onPress2,
      onPointerDown: onActive,
      onKeyDown
    });
  }
  DefaultMenuCheckbox.displayName = "DefaultMenuCheckbox";
  function DefaultAccessibilityMenu({ slots }) {
    const label = useDefaultLayoutWord("Accessibility"), { icons: Icons } = useDefaultLayoutContext();
    return /* @__PURE__ */ React12.createElement(Root, { className: "vds-accessibility-menu vds-menu" }, /* @__PURE__ */ React12.createElement(DefaultMenuButton, {
      label,
      Icon: Icons.Menu.Accessibility
    }), /* @__PURE__ */ React12.createElement(Items, { className: "vds-menu-items" }, slot(slots, "accessibilityMenuItemsStart", null), /* @__PURE__ */ React12.createElement(DefaultMenuSection, null, /* @__PURE__ */ React12.createElement(DefaultAnnouncementsMenuCheckbox, null), /* @__PURE__ */ React12.createElement(DefaultKeyboardAnimationsMenuCheckbox, null)), /* @__PURE__ */ React12.createElement(DefaultMenuSection, null, /* @__PURE__ */ React12.createElement(DefaultFontMenu, null)), slot(slots, "accessibilityMenuItemsEnd", null)));
  }
  DefaultAccessibilityMenu.displayName = "DefaultAccessibilityMenu";
  function DefaultAnnouncementsMenuCheckbox() {
    const { userPrefersAnnouncements } = useDefaultLayoutContext(), label = useDefaultLayoutWord("Announcements");
    function onChange(checked) {
      userPrefersAnnouncements.set(checked);
    }
    return /* @__PURE__ */ React12.createElement(DefaultMenuItem, { label }, /* @__PURE__ */ React12.createElement(DefaultMenuCheckbox, {
      label,
      defaultChecked: true,
      storageKey: "vds-player::announcements",
      onChange
    }));
  }
  DefaultAnnouncementsMenuCheckbox.displayName = "DefaultAnnouncementsMenuCheckbox";
  function DefaultKeyboardAnimationsMenuCheckbox() {
    const $viewType = useMediaState("viewType"), { userPrefersKeyboardAnimations, noKeyboardAnimations } = useDefaultLayoutContext(), label = useDefaultLayoutWord("Keyboard Animations");
    if ($viewType !== "video" || noKeyboardAnimations) return null;
    function onChange(checked) {
      userPrefersKeyboardAnimations.set(checked);
    }
    return /* @__PURE__ */ React12.createElement(DefaultMenuItem, { label }, /* @__PURE__ */ React12.createElement(DefaultMenuCheckbox, {
      label,
      defaultChecked: true,
      storageKey: "vds-player::keyboard-animations",
      onChange
    }));
  }
  DefaultKeyboardAnimationsMenuCheckbox.displayName = "DefaultKeyboardAnimationsMenuCheckbox";
  function DefaultAudioMenu({ slots }) {
    const label = useDefaultLayoutWord("Audio"), $canSetAudioGain = useMediaState("canSetAudioGain"), $audioTrack = useMediaState("audioTrack"), $audioTracks = useMediaState("audioTracks"), { noAudioGain, icons: Icons } = useDefaultLayoutContext(), hasGainSlider = $canSetAudioGain && !noAudioGain;
    if (!hasGainSlider && $audioTracks.length <= 1) return null;
    return /* @__PURE__ */ React12.createElement(Root, { className: "vds-audio-menu vds-menu" }, /* @__PURE__ */ React12.createElement(DefaultMenuButton, {
      label,
      hint: $audioTrack?.label,
      Icon: Icons.Menu.Audio
    }), /* @__PURE__ */ React12.createElement(Items, { className: "vds-menu-items" }, slot(slots, "audioMenuItemsStart", null), /* @__PURE__ */ React12.createElement(DefaultAudioTracksMenu, null), hasGainSlider ? /* @__PURE__ */ React12.createElement(DefaultAudioBoostMenuSection, null) : null, slot(slots, "audioMenuItemsEnd", null)));
  }
  DefaultAudioMenu.displayName = "DefaultAudioMenu";
  function DefaultAudioBoostMenuSection() {
    const $audioGain = useMediaState("audioGain"), label = useDefaultLayoutWord("Boost"), value = Math.round((($audioGain ?? 1) - 1) * 100) + "%", $canSetAudioGain = useMediaState("canSetAudioGain"), { noAudioGain, icons: Icons } = useDefaultLayoutContext(), $disabled = !$canSetAudioGain || noAudioGain, min2 = useGainMin(), max2 = useGainMax();
    if ($disabled) return null;
    return /* @__PURE__ */ React12.createElement(DefaultMenuSection, {
      label,
      value
    }, /* @__PURE__ */ React12.createElement(DefaultMenuSliderItem, {
      UpIcon: Icons.Menu.AudioBoostUp,
      DownIcon: Icons.Menu.AudioBoostDown,
      isMin: (($audioGain ?? 1) - 1) * 100 <= min2,
      isMax: (($audioGain ?? 1) - 1) * 100 === max2
    }, /* @__PURE__ */ React12.createElement(DefaultAudioGainSlider, null)));
  }
  DefaultAudioBoostMenuSection.displayName = "DefaultAudioBoostMenuSection";
  function useGainMin() {
    const { audioGains } = useDefaultLayoutContext();
    return (isArray(audioGains) ? audioGains[0] : audioGains?.min) ?? 0;
  }
  function useGainMax() {
    const { audioGains } = useDefaultLayoutContext();
    return (isArray(audioGains) ? audioGains[audioGains.length - 1] : audioGains?.max) ?? 300;
  }
  function useGainStep() {
    const { audioGains } = useDefaultLayoutContext();
    return (isArray(audioGains) ? audioGains[1] - audioGains[0] : audioGains?.step) || 25;
  }
  function DefaultAudioGainSlider() {
    const label = useDefaultLayoutWord("Audio Boost"), min2 = useGainMin(), max2 = useGainMax(), step = useGainStep();
    return /* @__PURE__ */ React12.createElement(Root$22, {
      className: "vds-audio-gain-slider vds-slider",
      "aria-label": label,
      min: min2,
      max: max2,
      step,
      keyStep: step
    }, /* @__PURE__ */ React12.createElement(DefaultSliderParts, null), /* @__PURE__ */ React12.createElement(DefaultSliderSteps, null));
  }
  DefaultAudioGainSlider.displayName = "DefaultAudioGainSlider";
  function DefaultAudioTracksMenu() {
    const { icons: Icons } = useDefaultLayoutContext(), label = useDefaultLayoutWord("Track"), defaultText = useDefaultLayoutWord("Default"), $track = useMediaState("audioTrack"), options = useAudioOptions();
    if (options.disabled) return null;
    return /* @__PURE__ */ React12.createElement(Root, { className: "vds-audio-track-menu vds-menu" }, /* @__PURE__ */ React12.createElement(DefaultMenuButton, {
      label,
      hint: $track?.label ?? defaultText,
      disabled: options.disabled,
      Icon: Icons.Menu.Audio
    }), /* @__PURE__ */ React12.createElement(Items, { className: "vds-menu-items" }, /* @__PURE__ */ React12.createElement(Root$1, {
      className: "vds-audio-radio-group vds-radio-group",
      value: options.selectedValue
    }, options.map(({ label: label2, value, select }) => /* @__PURE__ */ React12.createElement(Item$1, {
      className: "vds-audio-radio vds-radio",
      value,
      onSelect: select,
      key: value
    }, /* @__PURE__ */ React12.createElement(Icons.Menu.RadioCheck, { className: "vds-icon" }), /* @__PURE__ */ React12.createElement("span", { className: "vds-radio-label" }, label2))))));
  }
  DefaultAudioTracksMenu.displayName = "DefaultAudioTracksMenu";
  function DefaultCaptionMenu({ slots }) {
    const { icons: Icons } = useDefaultLayoutContext(), label = useDefaultLayoutWord("Captions"), offText = useDefaultLayoutWord("Off"), options = useCaptionOptions({ off: offText }), hint = options.selectedTrack?.label ?? offText;
    if (options.disabled) return null;
    return /* @__PURE__ */ React12.createElement(Root, { className: "vds-captions-menu vds-menu" }, /* @__PURE__ */ React12.createElement(DefaultMenuButton, {
      label,
      hint,
      disabled: options.disabled,
      Icon: Icons.Menu.Captions
    }), /* @__PURE__ */ React12.createElement(Items, { className: "vds-menu-items" }, slot(slots, "captionsMenuItemsStart", null), /* @__PURE__ */ React12.createElement(Root$1, {
      className: "vds-captions-radio-group vds-radio-group",
      value: options.selectedValue
    }, options.map(({ label: label2, value, select }) => /* @__PURE__ */ React12.createElement(Item$1, {
      className: "vds-caption-radio vds-radio",
      value,
      onSelect: select,
      key: value
    }, /* @__PURE__ */ React12.createElement(Icons.Menu.RadioCheck, { className: "vds-icon" }), /* @__PURE__ */ React12.createElement("span", { className: "vds-radio-label" }, label2)))), slot(slots, "captionsMenuItemsEnd", null)));
  }
  DefaultCaptionMenu.displayName = "DefaultCaptionMenu";
  function DefaultQualityMenu({ slots }) {
    const { hideQualityBitrate, icons: Icons } = useDefaultLayoutContext(), label = useDefaultLayoutWord("Quality"), autoText = useDefaultLayoutWord("Auto"), $canSetQuality = useMediaState("canSetQuality"), $qualities = useMediaState("qualities"), options = useVideoQualityOptions({ auto: autoText });
    if (!$canSetQuality || $qualities.length <= 1 || options.disabled) return null;
    return /* @__PURE__ */ React12.createElement(Root, { className: "vds-quality-menu vds-menu" }, /* @__PURE__ */ React12.createElement(DefaultMenuButton, {
      label,
      Icon: Icons.Menu.QualityUp
    }), /* @__PURE__ */ React12.createElement(Items, { className: "vds-menu-items" }, slot(slots, "qualityMenuItemsStart", null), /* @__PURE__ */ React12.createElement(Root$1, {
      className: "vds-quality-radio-group vds-radio-group",
      value: options.selectedValue
    }, options.map(({ label: label2, value, bitrateText, select }) => /* @__PURE__ */ React12.createElement(Item$1, {
      className: "vds-quality-radio vds-radio",
      value,
      onSelect: select,
      key: value
    }, /* @__PURE__ */ React12.createElement(Icons.Menu.RadioCheck, { className: "vds-icon" }), /* @__PURE__ */ React12.createElement("span", { className: "vds-radio-label" }, label2), !hideQualityBitrate && bitrateText ? /* @__PURE__ */ React12.createElement("span", { className: "vds-radio-hint" }, bitrateText) : null))), slot(slots, "qualityMenuItemsEnd", null)));
  }
  DefaultQualityMenu.displayName = "DefaultQualityMenu";
  function DefaultSpeedMenu({ slots }) {
    const { icons: Icons } = useDefaultLayoutContext(), $playbackRate = useMediaState("playbackRate"), $canSetPlaybackRate = useMediaState("canSetPlaybackRate"), label = useDefaultLayoutWord("Speed"), normalText = useDefaultLayoutWord("Normal"), min2 = useSpeedMin(), max2 = useSpeedMax(), step = useSpeedStep(), value = $playbackRate === 1 ? normalText : $playbackRate + "x";
    if (!$canSetPlaybackRate) return null;
    return /* @__PURE__ */ React12.createElement(Root, { className: "vds-speed-menu vds-menu" }, /* @__PURE__ */ React12.createElement(DefaultMenuButton, {
      label,
      hint: value,
      Icon: Icons.Menu.SpeedUp
    }), /* @__PURE__ */ React12.createElement(Items, { className: "vds-menu-items" }, slot(slots, "speedMenuItemsStart", null), /* @__PURE__ */ React12.createElement(DefaultMenuSection, {
      label,
      value
    }, /* @__PURE__ */ React12.createElement(DefaultMenuSliderItem, {
      UpIcon: Icons.Menu.FontSizeUp,
      DownIcon: Icons.Menu.FontSizeDown,
      isMin: $playbackRate === min2,
      isMax: $playbackRate === max2
    }, /* @__PURE__ */ React12.createElement(Root$12, {
      className: "vds-speed-slider vds-slider",
      "aria-label": label,
      min: min2,
      max: max2,
      step,
      keyStep: step
    }, /* @__PURE__ */ React12.createElement(DefaultSliderParts, null), /* @__PURE__ */ React12.createElement(DefaultSliderSteps, null)))), slot(slots, "speedMenuItemsEnd", null)));
  }
  DefaultSpeedMenu.displayName = "DefaultSpeedMenu";
  function useSpeedMin() {
    const { playbackRates } = useDefaultLayoutContext(), rates = playbackRates;
    return (isArray(rates) ? rates[0] : rates?.min) ?? 0;
  }
  function useSpeedMax() {
    const { playbackRates } = useDefaultLayoutContext(), rates = playbackRates;
    return (isArray(rates) ? rates[rates.length - 1] : rates?.max) ?? 2;
  }
  function useSpeedStep() {
    const { playbackRates } = useDefaultLayoutContext(), rates = playbackRates;
    return (isArray(rates) ? rates[1] - rates[0] : rates?.step) || 0.25;
  }
  function DefaultSettingsMenu({ tooltip, placement, portalClass = "", slots }) {
    const { showMenuDelay, icons: Icons, isSmallLayout, menuContainer, menuGroup, noModal, colorScheme } = useDefaultLayoutContext(), settingsText = useDefaultLayoutWord("Settings"), $viewType = useMediaState("viewType"), $offset = !isSmallLayout && menuGroup === "bottom" && $viewType === "video" ? 26 : 0, colorSchemeClass = useColorSchemeClass(colorScheme), [isOpen, setIsOpen] = React12.useState(false), dialogEl = useParentDialogEl();
    useScoped(updateFontCssVars);
    function onOpen() {
      (0, import_react_dom2.flushSync)(() => {
        setIsOpen(true);
      });
    }
    function onClose() {
      setIsOpen(false);
    }
    const Content2 = /* @__PURE__ */ React12.createElement(Items, {
      className: "vds-settings-menu-items vds-menu-items",
      placement,
      offset: $offset
    }, isOpen ? /* @__PURE__ */ React12.createElement(React12.Fragment, null, slot(slots, "settingsMenuItemsStart", null), slot(slots, "settingsMenuStartItems", null), /* @__PURE__ */ React12.createElement(DefaultSpeedMenu, { slots }), /* @__PURE__ */ React12.createElement(DefaultQualityMenu, { slots }), /* @__PURE__ */ React12.createElement(DefaultAccessibilityMenu, { slots }), /* @__PURE__ */ React12.createElement(DefaultAudioMenu, { slots }), /* @__PURE__ */ React12.createElement(DefaultCaptionMenu, { slots }), slot(slots, "settingsMenuEndItems", null), slot(slots, "settingsMenuItemsEnd", null)) : null);
    return /* @__PURE__ */ React12.createElement(Root, {
      className: "vds-settings-menu vds-menu",
      showDelay: showMenuDelay,
      onOpen,
      onClose
    }, /* @__PURE__ */ React12.createElement(DefaultTooltip, {
      content: settingsText,
      placement: tooltip
    }, /* @__PURE__ */ React12.createElement(Button, {
      className: "vds-menu-button vds-button",
      "aria-label": settingsText
    }, /* @__PURE__ */ React12.createElement(Icons.Menu.Settings, { className: "vds-icon vds-rotate-icon" }))), noModal || !isSmallLayout ? Content2 : /* @__PURE__ */ React12.createElement(Portal, {
      className: portalClass + (colorSchemeClass ? ` ${colorSchemeClass}` : ""),
      container: menuContainer ?? dialogEl,
      disabled: "fullscreen",
      "data-sm": isSmallLayout ? "" : null,
      "data-lg": !isSmallLayout ? "" : null,
      "data-size": isSmallLayout ? "sm" : "lg",
      "data-view-type": $viewType
    }, Content2));
  }
  DefaultSettingsMenu.displayName = "DefaultSettingsMenu";
  function DefaultVolumePopup({ tooltip, orientation, slots }) {
    const $pointer = useMediaState("pointer"), $muted = useMediaState("muted"), $canSetVolume = useMediaState("canSetVolume"), [rootEl, setRootEl] = React12.useState(null), isRootActive = useActive(rootEl), muteButton = slot(slots, "muteButton", /* @__PURE__ */ React12.createElement(DefaultMuteButton, { tooltip }));
    if (!$canSetVolume) return muteButton;
    return $pointer === "coarse" && !$muted ? null : /* @__PURE__ */ React12.createElement("div", {
      className: "vds-volume",
      "data-active": isRootActive ? "" : null,
      ref: setRootEl
    }, muteButton, /* @__PURE__ */ React12.createElement("div", { className: "vds-volume-popup" }, slot(slots, "volumeSlider", /* @__PURE__ */ React12.createElement(DefaultVolumeSlider, { orientation }))));
  }
  DefaultVolumePopup.displayName = "DefaultVolumePopup";
  function DefaultVolumeSlider(props) {
    const label = useDefaultLayoutWord("Volume");
    return /* @__PURE__ */ React12.createElement(Root$4, {
      className: "vds-volume-slider vds-slider",
      "aria-label": label,
      ...props
    }, /* @__PURE__ */ React12.createElement(Track, { className: "vds-slider-track" }), /* @__PURE__ */ React12.createElement(TrackFill, { className: "vds-slider-track-fill vds-slider-track" }), /* @__PURE__ */ React12.createElement(Thumb, { className: "vds-slider-thumb" }), /* @__PURE__ */ React12.createElement(Preview, {
      className: "vds-slider-preview",
      noClamp: true
    }, /* @__PURE__ */ React12.createElement(Value, { className: "vds-slider-value" })));
  }
  DefaultVolumeSlider.displayName = "DefaultVolumeSlider";
  function DefaultTimeSlider() {
    const [instance, setInstance] = React12.useState(null), [width, setWidth] = React12.useState(0), $src = useMediaState("currentSrc"), { thumbnails, sliderChaptersMinWidth, disableTimeSlider, seekStep, noScrubGesture } = useDefaultLayoutContext(), label = useDefaultLayoutWord("Seek"), $RemotionSliderThumbnail = useSignal(RemotionSliderThumbnail);
    const onResize = React12.useCallback(() => {
      const el = instance?.el;
      el && setWidth(el.clientWidth);
    }, [instance]);
    useResizeObserver(instance?.el, onResize);
    return /* @__PURE__ */ React12.createElement(Root$2, {
      className: "vds-time-slider vds-slider",
      "aria-label": label,
      disabled: disableTimeSlider,
      noSwipeGesture: noScrubGesture,
      keyStep: seekStep,
      ref: setInstance
    }, /* @__PURE__ */ React12.createElement(Chapters, {
      className: "vds-slider-chapters",
      disabled: width < sliderChaptersMinWidth
    }, (cues, forwardRef8) => cues.map((cue) => /* @__PURE__ */ React12.createElement("div", {
      className: "vds-slider-chapter",
      key: cue.startTime,
      ref: forwardRef8
    }, /* @__PURE__ */ React12.createElement(Track, { className: "vds-slider-track" }), /* @__PURE__ */ React12.createElement(TrackFill, { className: "vds-slider-track-fill vds-slider-track" }), /* @__PURE__ */ React12.createElement(Progress, { className: "vds-slider-progress vds-slider-track" })))), /* @__PURE__ */ React12.createElement(Thumb, { className: "vds-slider-thumb" }), /* @__PURE__ */ React12.createElement(Preview, { className: "vds-slider-preview" }, thumbnails ? /* @__PURE__ */ React12.createElement(Thumbnail2.Root, {
      src: thumbnails,
      className: "vds-slider-thumbnail vds-thumbnail"
    }, /* @__PURE__ */ React12.createElement(Thumbnail2.Img, null)) : $RemotionSliderThumbnail && isRemotionSrc($src) ? /* @__PURE__ */ React12.createElement($RemotionSliderThumbnail, { className: "vds-slider-thumbnail vds-thumbnail" }) : null, /* @__PURE__ */ React12.createElement(ChapterTitle, { className: "vds-slider-chapter-title" }), /* @__PURE__ */ React12.createElement(Value, { className: "vds-slider-value" })));
  }
  DefaultTimeSlider.displayName = "DefaultTimeSlider";
  function DefaultTimeGroup({ slots }) {
    if (!useMediaState("duration")) return null;
    return /* @__PURE__ */ React12.createElement("div", { className: "vds-time-group" }, slot(slots, "currentTime", /* @__PURE__ */ React12.createElement(Time2, {
      className: "vds-time",
      type: "current"
    })), slot(slots, "timeDivider", /* @__PURE__ */ React12.createElement("div", { className: "vds-time-divider" }, "/")), slot(slots, "endTime", /* @__PURE__ */ React12.createElement(Time2, {
      className: "vds-time",
      type: "duration"
    })));
  }
  DefaultTimeGroup.displayName = "DefaultTimeGroup";
  function DefaultTimeInfo({ slots }) {
    return useMediaState("live") ? slot(slots, "liveButton", /* @__PURE__ */ React12.createElement(DefaultLiveButton, null)) : /* @__PURE__ */ React12.createElement(DefaultTimeGroup, { slots });
  }
  DefaultTimeInfo.displayName = "DefaultTimeInfo";
  function DefaultTimeInvert({ slots }) {
    const $live = useMediaState("live"), $duration = useMediaState("duration");
    return $live ? slot(slots, "liveButton", /* @__PURE__ */ React12.createElement(DefaultLiveButton, null)) : slot(slots, "endTime", $duration ? /* @__PURE__ */ React12.createElement(Time2, {
      className: "vds-time",
      type: "current",
      toggle: true,
      remainder: true
    }) : null);
  }
  DefaultTimeInvert.displayName = "DefaultTimeInvert";
  var MediaLayout$1 = createDefaultMediaLayout({
    type: "audio",
    smLayoutWhen({ width }) {
      return width < 576;
    },
    renderLayout: () => /* @__PURE__ */ React12.createElement(AudioLayout, null)
  });
  function DefaultAudioLayout(props) {
    const [scrubbing, setScrubbing] = React12.useState(false), $pointer = useMediaState("pointer");
    const onStartScrubbing = React12.useCallback((event) => {
      const { target } = event;
      if (!!!(target instanceof HTMLElement && target.closest(".vds-time-slider"))) return;
      event.nativeEvent.stopImmediatePropagation();
      setScrubbing(true);
    }, []);
    const onStopScrubbing = React12.useCallback(() => {
      setScrubbing(false);
    }, []);
    React12.useEffect(() => {
      if (scrubbing) return listenEvent(window, "pointerdown", onStopScrubbing);
    }, [scrubbing, onStopScrubbing]);
    return /* @__PURE__ */ React12.createElement(MediaLayout$1, {
      ...props,
      "data-scrubbing": scrubbing ? "" : null,
      onPointerDown: scrubbing ? (e2) => e2.stopPropagation() : void 0,
      onPointerDownCapture: $pointer === "coarse" && !scrubbing ? onStartScrubbing : void 0
    });
  }
  DefaultAudioLayout.displayName = "DefaultAudioLayout";
  function AudioLayout() {
    const slots = useDefaultAudioLayoutSlots();
    useLayoutName("audio");
    return /* @__PURE__ */ React12.createElement(React12.Fragment, null, /* @__PURE__ */ React12.createElement(DefaultAnnouncer, null), /* @__PURE__ */ React12.createElement(DefaultCaptions, null), /* @__PURE__ */ React12.createElement(Root$42, { className: "vds-controls" }, /* @__PURE__ */ React12.createElement(Group, { className: "vds-controls-group" }, slot(slots, "seekBackwardButton", /* @__PURE__ */ React12.createElement(DefaultSeekButton, {
      backward: true,
      tooltip: "top start"
    })), slot(slots, "playButton", /* @__PURE__ */ React12.createElement(DefaultPlayButton, { tooltip: "top center" })), slot(slots, "seekForwardButton", /* @__PURE__ */ React12.createElement(DefaultSeekButton, { tooltip: "top center" })), /* @__PURE__ */ React12.createElement(DefaultAudioTitle, null), slot(slots, "timeSlider", /* @__PURE__ */ React12.createElement(DefaultTimeSlider, null)), /* @__PURE__ */ React12.createElement(DefaultTimeInvert, { slots }), /* @__PURE__ */ React12.createElement(DefaultVolumePopup, {
      orientation: "vertical",
      tooltip: "top",
      slots
    }), slot(slots, "captionButton", /* @__PURE__ */ React12.createElement(DefaultCaptionButton, { tooltip: "top center" })), slot(slots, "downloadButton", /* @__PURE__ */ React12.createElement(DefaultDownloadButton, null)), /* @__PURE__ */ React12.createElement(DefaultAudioMenus, { slots }))));
  }
  AudioLayout.displayName = "AudioLayout";
  function DefaultAudioMenus({ slots }) {
    const { isSmallLayout, noModal } = useDefaultLayoutContext(), placement = noModal ? "top end" : !isSmallLayout ? "top end" : null;
    return /* @__PURE__ */ React12.createElement(React12.Fragment, null, slot(slots, "chaptersMenu", /* @__PURE__ */ React12.createElement(DefaultChaptersMenu, {
      tooltip: "top",
      placement,
      portalClass: "vds-audio-layout"
    })), slot(slots, "settingsMenu", /* @__PURE__ */ React12.createElement(DefaultSettingsMenu, {
      tooltip: "top end",
      placement,
      portalClass: "vds-audio-layout",
      slots
    })));
  }
  DefaultAudioMenus.displayName = "DefaultAudioMenus";
  function DefaultAudioTitle() {
    const [rootEl, setRootEl] = React12.useState(null), media = useMediaContext2(), { translations } = useDefaultLayoutContext(), [isTextOverflowing, setIsTextOverflowing] = React12.useState(false);
    const isContinued = createComputed(() => {
      const { started, currentTime } = media.$state;
      return started() || currentTime() > 0;
    });
    const $title = useSignal(createComputed(() => {
      const { title, ended } = media.$state;
      if (!title()) return "";
      return `${i18n(translations, ended() ? "Replay" : isContinued() ? "Continue" : "Play")}: ${title()}`;
    }));
    const chapterTitle = useChapterTitle(), $chapterTitle = useSignal(isContinued) ? chapterTitle : "", isTransitionActive = useTransitionActive(rootEl);
    React12.useEffect(() => {
      if (isTransitionActive && document.activeElement === document.body) media.player.el?.focus({ preventScroll: true });
    }, []);
    useResizeObserver(rootEl, React12.useCallback(() => {
      const el = rootEl, isOverflowing = !!el && !isTransitionActive && el.clientWidth < el.children[0].clientWidth;
      if (el) toggleClass(el, "vds-marquee", isOverflowing);
      setIsTextOverflowing(isOverflowing);
    }, [rootEl, isTransitionActive]));
    return $title ? /* @__PURE__ */ React12.createElement("span", {
      className: "vds-title",
      title: $title,
      ref: setRootEl
    }, /* @__PURE__ */ React12.createElement(AudioTitle, {
      title: $title,
      chapterTitle: $chapterTitle
    }), isTextOverflowing && !isTransitionActive ? /* @__PURE__ */ React12.createElement(AudioTitle, {
      title: $title,
      chapterTitle: $chapterTitle
    }) : null) : /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null);
  }
  DefaultAudioTitle.displayName = "DefaultAudioTitle";
  function AudioTitle({ title, chapterTitle }) {
    const slots = useDefaultAudioLayoutSlots();
    return /* @__PURE__ */ React12.createElement("span", { className: "vds-title-text" }, slot(slots, "title", title), slot(slots, "chapterTitle", /* @__PURE__ */ React12.createElement("span", { className: "vds-chapter-title" }, chapterTitle)));
  }
  AudioTitle.displayName = "AudioTitle";
  var DefaultKeyboardDisplay = React12.forwardRef(({ icons: Icons, ...props }, forwardRef8) => {
    const [visible, setVisible] = React12.useState(false), [Icon3, setIcon] = React12.useState(null), [count, setCount] = React12.useState(0), $lastKeyboardAction = useMediaState("lastKeyboardAction");
    React12.useEffect(() => {
      setCount((n2) => n2 + 1);
    }, [$lastKeyboardAction]);
    const actionDataAttr = React12.useMemo(() => {
      const action = $lastKeyboardAction?.action;
      return action && visible ? camelToKebabCase(action) : null;
    }, [visible, $lastKeyboardAction]);
    const className = React12.useMemo(() => `vds-kb-action${!visible ? " hidden" : ""}${props.className ? ` ${props.className}` : ""}`, [visible]);
    const $text = useSignal(createComputed(getText));
    createEffect(() => {
      const Icon4 = getIcon(Icons);
      setIcon(() => Icon4);
    }, [Icons]);
    React12.useEffect(() => {
      setVisible(!!$lastKeyboardAction);
      const id2 = setTimeout(() => setVisible(false), 500);
      return () => {
        setVisible(false);
        window.clearTimeout(id2);
      };
    }, [$lastKeyboardAction]);
    return Icon3 ? /* @__PURE__ */ React12.createElement(Primitive.div, {
      ...props,
      className,
      "data-action": actionDataAttr,
      ref: forwardRef8
    }, /* @__PURE__ */ React12.createElement("div", { className: "vds-kb-text-wrapper" }, /* @__PURE__ */ React12.createElement("div", { className: "vds-kb-text" }, $text)), /* @__PURE__ */ React12.createElement("div", {
      className: "vds-kb-bezel",
      key: count
    }, /* @__PURE__ */ React12.createElement("div", { className: "vds-kb-icon" }, /* @__PURE__ */ React12.createElement(Icon3, null)))) : null;
  });
  DefaultKeyboardDisplay.displayName = "DefaultKeyboardDisplay";
  function getText() {
    const { $state } = useContext2(mediaContext), action = $state.lastKeyboardAction()?.action, audioGain = $state.audioGain() ?? 1;
    switch (action) {
      case "toggleMuted":
        return $state.muted() ? "0%" : getVolumeText($state.volume(), audioGain);
      case "volumeUp":
      case "volumeDown":
        return getVolumeText($state.volume(), audioGain);
      default:
        return "";
    }
  }
  function getVolumeText(volume, gain) {
    return `${Math.round(volume * gain * 100)}%`;
  }
  function getIcon(Icons) {
    const { $state } = useContext2(mediaContext);
    switch ($state.lastKeyboardAction()?.action) {
      case "togglePaused":
        return !$state.paused() ? Icons.Play : Icons.Pause;
      case "toggleMuted":
        return $state.muted() || $state.volume() === 0 ? Icons.Mute : $state.volume() >= 0.5 ? Icons.VolumeUp : Icons.VolumeDown;
      case "toggleFullscreen":
        return $state.fullscreen() ? Icons.EnterFullscreen : Icons.ExitFullscreen;
      case "togglePictureInPicture":
        return $state.pictureInPicture() ? Icons.EnterPiP : Icons.ExitPiP;
      case "toggleCaptions":
        return $state.hasCaptions() ? $state.textTrack() ? Icons.CaptionsOn : Icons.CaptionsOff : null;
      case "volumeUp":
        return Icons.VolumeUp;
      case "volumeDown":
        return Icons.VolumeDown;
      case "seekForward":
        return Icons.SeekForward;
      case "seekBackward":
        return Icons.SeekBackward;
      default:
        return null;
    }
  }
  function DefaultTitle() {
    const $started = useMediaState("started"), $title = useMediaState("title");
    return useActiveTextTrack("chapters") && ($started || !$title) ? /* @__PURE__ */ React12.createElement(ChapterTitle2, { className: "vds-chapter-title" }) : /* @__PURE__ */ React12.createElement(Title, { className: "vds-chapter-title" });
  }
  DefaultTitle.displayName = "DefaultTitle";
  var MediaLayout = createDefaultMediaLayout({
    type: "video",
    smLayoutWhen({ width, height }) {
      return width < 576 || height < 380;
    },
    renderLayout(props) {
      return /* @__PURE__ */ React12.createElement(VideoLayout, props);
    }
  });
  function DefaultVideoLayout(props) {
    return /* @__PURE__ */ React12.createElement(MediaLayout, props);
  }
  DefaultVideoLayout.displayName = "DefaultVideoLayout";
  function VideoLayout({ streamType, isLoadLayout, isSmallLayout }) {
    useLayoutName("video");
    return isLoadLayout ? /* @__PURE__ */ React12.createElement(DefaultVideoLoadLayout, null) : streamType === "unknown" ? /* @__PURE__ */ React12.createElement(DefaultBufferingIndicator, null) : isSmallLayout ? /* @__PURE__ */ React12.createElement(DefaultVideoSmallLayout, null) : /* @__PURE__ */ React12.createElement(DefaultVideoLargeLayout, null);
  }
  VideoLayout.displayName = "VideoLayout";
  function DefaultVideoLargeLayout() {
    const { menuGroup } = useDefaultLayoutContext(), baseSlots = useDefaultVideoLayoutSlots(), slots = {
      ...baseSlots,
      ...baseSlots?.largeLayout
    };
    return /* @__PURE__ */ React12.createElement(React12.Fragment, null, /* @__PURE__ */ React12.createElement(DefaultAnnouncer, null), /* @__PURE__ */ React12.createElement(DefaultVideoGestures, null), /* @__PURE__ */ React12.createElement(DefaultVideoKeyboardDisplay, null), slot(slots, "bufferingIndicator", /* @__PURE__ */ React12.createElement(DefaultBufferingIndicator, null)), slot(slots, "captions", /* @__PURE__ */ React12.createElement(DefaultCaptions, null)), /* @__PURE__ */ React12.createElement(Root$42, { className: "vds-controls" }, /* @__PURE__ */ React12.createElement(Group, { className: "vds-controls-group" }, slot(slots, "topControlsGroupStart", null), /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null), slot(slots, "topControlsGroupCenter", null), /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null), slot(slots, "topControlsGroupEnd", null), menuGroup === "top" && /* @__PURE__ */ React12.createElement(DefaultVideoMenus, { slots })), /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null), /* @__PURE__ */ React12.createElement(Group, { className: "vds-controls-group" }, slot(slots, "centerControlsGroupStart", null), /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null), slot(slots, "centerControlsGroupCenter", null), /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null), slot(slots, "centerControlsGroupEnd", null)), /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null), /* @__PURE__ */ React12.createElement(Group, { className: "vds-controls-group" }, slot(slots, "timeSlider", /* @__PURE__ */ React12.createElement(DefaultTimeSlider, null))), /* @__PURE__ */ React12.createElement(Group, { className: "vds-controls-group" }, slot(slots, "playButton", /* @__PURE__ */ React12.createElement(DefaultPlayButton, { tooltip: "top start" })), /* @__PURE__ */ React12.createElement(DefaultVolumePopup, {
      orientation: "horizontal",
      tooltip: "top",
      slots
    }), /* @__PURE__ */ React12.createElement(DefaultTimeInfo, { slots }), slot(slots, "chapterTitle", /* @__PURE__ */ React12.createElement(DefaultTitle, null)), slot(slots, "captionButton", /* @__PURE__ */ React12.createElement(DefaultCaptionButton, { tooltip: "top" })), menuGroup === "bottom" && /* @__PURE__ */ React12.createElement(DefaultVideoMenus, { slots }), slot(slots, "airPlayButton", /* @__PURE__ */ React12.createElement(DefaultAirPlayButton, { tooltip: "top" })), slot(slots, "googleCastButton", /* @__PURE__ */ React12.createElement(DefaultGoogleCastButton, { tooltip: "top" })), slot(slots, "downloadButton", /* @__PURE__ */ React12.createElement(DefaultDownloadButton, null)), slot(slots, "pipButton", /* @__PURE__ */ React12.createElement(DefaultPIPButton, { tooltip: "top" })), slot(slots, "fullscreenButton", /* @__PURE__ */ React12.createElement(DefaultFullscreenButton, { tooltip: "top end" })))));
  }
  DefaultVideoLargeLayout.displayName = "DefaultVideoLargeLayout";
  function DefaultVideoSmallLayout() {
    const baseSlots = useDefaultVideoLayoutSlots(), slots = {
      ...baseSlots,
      ...baseSlots?.smallLayout
    };
    return /* @__PURE__ */ React12.createElement(React12.Fragment, null, /* @__PURE__ */ React12.createElement(DefaultAnnouncer, null), /* @__PURE__ */ React12.createElement(DefaultVideoGestures, null), /* @__PURE__ */ React12.createElement(DefaultVideoKeyboardDisplay, null), slot(slots, "bufferingIndicator", /* @__PURE__ */ React12.createElement(DefaultBufferingIndicator, null)), slot(slots, "captions", /* @__PURE__ */ React12.createElement(DefaultCaptions, null)), /* @__PURE__ */ React12.createElement(Root$42, { className: "vds-controls" }, /* @__PURE__ */ React12.createElement(Group, { className: "vds-controls-group" }, slot(slots, "topControlsGroupStart", null), slot(slots, "airPlayButton", /* @__PURE__ */ React12.createElement(DefaultAirPlayButton, { tooltip: "top start" })), slot(slots, "googleCastButton", /* @__PURE__ */ React12.createElement(DefaultGoogleCastButton, { tooltip: "top start" })), /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null), slot(slots, "topControlsGroupCenter", null), /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null), slot(slots, "captionButton", /* @__PURE__ */ React12.createElement(DefaultCaptionButton, { tooltip: "bottom" })), slot(slots, "downloadButton", /* @__PURE__ */ React12.createElement(DefaultDownloadButton, null)), /* @__PURE__ */ React12.createElement(DefaultVideoMenus, { slots }), /* @__PURE__ */ React12.createElement(DefaultVolumePopup, {
      orientation: "vertical",
      tooltip: "bottom end",
      slots
    }), slot(slots, "topControlsGroupEnd", null)), /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null), /* @__PURE__ */ React12.createElement(Group, {
      className: "vds-controls-group",
      style: { pointerEvents: "none" }
    }, slot(slots, "centerControlsGroupStart", null), /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null), slot(slots, "centerControlsGroupCenter", null), slot(slots, "playButton", /* @__PURE__ */ React12.createElement(DefaultPlayButton, { tooltip: "top" })), /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null), slot(slots, "centerControlsGroupEnd", null)), /* @__PURE__ */ React12.createElement(DefaultControlsSpacer, null), /* @__PURE__ */ React12.createElement(Group, { className: "vds-controls-group" }, /* @__PURE__ */ React12.createElement(DefaultTimeInfo, { slots }), slot(slots, "chapterTitle", /* @__PURE__ */ React12.createElement(DefaultTitle, null)), slot(slots, "fullscreenButton", /* @__PURE__ */ React12.createElement(DefaultFullscreenButton, { tooltip: "top end" }))), /* @__PURE__ */ React12.createElement(Group, { className: "vds-controls-group" }, slot(slots, "timeSlider", /* @__PURE__ */ React12.createElement(DefaultTimeSlider, null)))), slot(slots, "startDuration", /* @__PURE__ */ React12.createElement(DefaultVideoStartDuration, null)));
  }
  DefaultVideoSmallLayout.displayName = "DefaultVideoSmallLayout";
  function DefaultVideoStartDuration() {
    if (useMediaState("duration") === 0) return null;
    return /* @__PURE__ */ React12.createElement("div", { className: "vds-start-duration" }, /* @__PURE__ */ React12.createElement(Time2, {
      className: "vds-time",
      type: "duration"
    }));
  }
  DefaultVideoStartDuration.displayName = "DefaultVideoStartDuration";
  function DefaultVideoGestures() {
    const { noGestures } = useDefaultLayoutContext();
    if (noGestures) return null;
    return /* @__PURE__ */ React12.createElement("div", { className: "vds-gestures" }, /* @__PURE__ */ React12.createElement(Gesture2, {
      className: "vds-gesture",
      event: "pointerup",
      action: "toggle:paused"
    }), /* @__PURE__ */ React12.createElement(Gesture2, {
      className: "vds-gesture",
      event: "pointerup",
      action: "toggle:controls"
    }), /* @__PURE__ */ React12.createElement(Gesture2, {
      className: "vds-gesture",
      event: "dblpointerup",
      action: "toggle:fullscreen"
    }), /* @__PURE__ */ React12.createElement(Gesture2, {
      className: "vds-gesture",
      event: "dblpointerup",
      action: "seek:-10"
    }), /* @__PURE__ */ React12.createElement(Gesture2, {
      className: "vds-gesture",
      event: "dblpointerup",
      action: "seek:10"
    }));
  }
  DefaultVideoGestures.displayName = "DefaultVideoGestures";
  function DefaultBufferingIndicator() {
    return /* @__PURE__ */ React12.createElement("div", { className: "vds-buffering-indicator" }, /* @__PURE__ */ React12.createElement(Root2, { className: "vds-buffering-spinner" }, /* @__PURE__ */ React12.createElement(Track2, { className: "vds-buffering-track" }), /* @__PURE__ */ React12.createElement(TrackFill2, { className: "vds-buffering-track-fill" })));
  }
  DefaultBufferingIndicator.displayName = "DefaultBufferingIndicator";
  function DefaultVideoMenus({ slots }) {
    const { isSmallLayout, noModal, menuGroup } = useDefaultLayoutContext(), side = menuGroup === "top" || isSmallLayout ? "bottom" : "top", tooltip = `${side} end`, placement = noModal ? `${side} end` : !isSmallLayout ? `${side} end` : null;
    return /* @__PURE__ */ React12.createElement(React12.Fragment, null, slot(slots, "chaptersMenu", /* @__PURE__ */ React12.createElement(DefaultChaptersMenu, {
      tooltip,
      placement,
      portalClass: "vds-video-layout"
    })), slot(slots, "settingsMenu", /* @__PURE__ */ React12.createElement(DefaultSettingsMenu, {
      tooltip,
      placement,
      portalClass: "vds-video-layout",
      slots
    })));
  }
  DefaultVideoMenus.displayName = "DefaultVideoMenus";
  function DefaultVideoLoadLayout() {
    const { isSmallLayout } = useDefaultLayoutContext(), baseSlots = useDefaultVideoLayoutSlots(), slots = {
      ...baseSlots,
      ...baseSlots?.[isSmallLayout ? "smallLayout" : "largeLayout"]
    };
    return /* @__PURE__ */ React12.createElement("div", { className: "vds-load-container" }, slot(slots, "bufferingIndicator", /* @__PURE__ */ React12.createElement(DefaultBufferingIndicator, null)), slot(slots, "loadButton", /* @__PURE__ */ React12.createElement(DefaultPlayButton, { tooltip: "top" })));
  }
  DefaultVideoLoadLayout.displayName = "DefaultVideoLoadLayout";
  function DefaultVideoKeyboardDisplay() {
    const { noKeyboardAnimations, icons, userPrefersKeyboardAnimations } = useDefaultLayoutContext(), $userPrefersKeyboardAnimations = useSignal(userPrefersKeyboardAnimations);
    if (noKeyboardAnimations || !$userPrefersKeyboardAnimations || !icons.KeyboardDisplay) return null;
    return /* @__PURE__ */ React12.createElement(DefaultKeyboardDisplay, { icons: icons.KeyboardDisplay });
  }
  DefaultVideoKeyboardDisplay.displayName = "DefaultVideoKeyboardDisplay";

  // frontend/src/templates/v1/views/upload/UploadStudio.tsx
  var API = process.env.NEXT_PUBLIC_API_BASE_URL ?? "https://api.portal.localhost";
  function UploadStudio() {
    const [phase, setPhase] = (0, import_react20.useState)("idle");
    const [progress, setProgress] = (0, import_react20.useState)(0);
    const [current, setCurrent] = (0, import_react20.useState)(null);
    const [error, setError] = (0, import_react20.useState)(null);
    const [library, setLibrary] = (0, import_react20.useState)([]);
    const fileRef = (0, import_react20.useRef)(null);
    (0, import_react20.useEffect)(() => {
      void refreshLibrary();
    }, []);
    async function refreshLibrary() {
      try {
        const r2 = await fetch(`${API}/api/v1/assets`, { credentials: "include" });
        if (r2.ok) setLibrary((await r2.json()).assets ?? []);
      } catch {
      }
    }
    async function onFile(file) {
      setError(null);
      setCurrent(null);
      setPhase("uploading");
      setProgress(0);
      try {
        const cr = await fetch(`${API}/api/v1/assets`, {
          method: "POST",
          credentials: "include",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            filename: file.name,
            content_type: file.type || "video/mp4",
            size_bytes: file.size
          })
        });
        if (!cr.ok) throw new Error("Could not start upload.");
        const { asset } = await cr.json();
        await putWithProgress(
          `${API}/api/v1/assets/${asset.id}/source`,
          file,
          setProgress
        );
        const co = await fetch(`${API}/api/v1/assets/${asset.id}/complete`, {
          method: "POST",
          credentials: "include"
        });
        if (!co.ok) throw new Error("Could not finalise upload.");
        setPhase("processing");
        const done = await poll(asset.id);
        if (done.status !== "ready") throw new Error(done.error || "Transcode failed.");
        setCurrent(done);
        setPhase("ready");
        void refreshLibrary();
      } catch (e2) {
        setError(e2 instanceof Error ? e2.message : "Upload failed.");
        setPhase("error");
      }
    }
    function reset() {
      setPhase("idle");
      setCurrent(null);
      setError(null);
      setProgress(0);
      if (fileRef.current) fileRef.current.value = "";
    }
    return /* @__PURE__ */ React.createElement("div", { className: "mx-auto max-w-3xl space-y-5" }, /* @__PURE__ */ React.createElement(Card3, null, /* @__PURE__ */ React.createElement("h1", { className: "text-lg font-bold", style: { color: "var(--tpl-heading)" } }, "Upload a video"), /* @__PURE__ */ React.createElement("p", { className: "mt-1 text-sm", style: { color: "var(--tpl-muted)" } }, "Pick an MP4. It uploads, transcodes to HLS on the worker, then plays back here."), phase === "idle" || phase === "error" ? /* @__PURE__ */ React.createElement("div", { className: "mt-4" }, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: () => fileRef.current?.click(),
        className: "flex w-full flex-col items-center gap-2 rounded-xl border-2 border-dashed py-10 transition hover:bg-[var(--tpl-surface-2)]",
        style: { borderColor: "var(--tpl-border)", color: "var(--tpl-muted)" }
      },
      /* @__PURE__ */ React.createElement(Icon, { name: "camera-icon", size: 28 }),
      /* @__PURE__ */ React.createElement("span", { className: "text-sm font-medium" }, "Choose a video file")
    ), /* @__PURE__ */ React.createElement(
      "input",
      {
        ref: fileRef,
        type: "file",
        accept: "video/*",
        className: "hidden",
        onChange: (e2) => {
          const f = e2.target.files?.[0];
          if (f) void onFile(f);
        }
      }
    ), error && /* @__PURE__ */ React.createElement("p", { className: "mt-3 rounded-lg border px-3 py-2 text-sm", style: { borderColor: "rgba(239,68,68,.4)", background: "rgba(239,68,68,.08)", color: "#ef4444" } }, error)) : phase === "uploading" ? /* @__PURE__ */ React.createElement(Progress2, { label: `Uploading\u2026 ${progress}%`, value: progress }) : phase === "processing" ? /* @__PURE__ */ React.createElement(Progress2, { label: "Transcoding to HLS\u2026", indeterminate: true }) : current && /* @__PURE__ */ React.createElement("div", { className: "mt-4 space-y-3" }, /* @__PURE__ */ React.createElement(Player, { src: current.hls_url }), /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between text-xs", style: { color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement("span", null, current.width, "\xD7", current.height, " \xB7 ", Math.round((current.duration_ms ?? 0) / 1e3), "s \xB7", " ", (current.size_bytes / 1024 / 1024).toFixed(1), " MB"), /* @__PURE__ */ React.createElement("button", { type: "button", onClick: reset, className: "font-semibold", style: { color: "var(--tpl-accent)" } }, "Upload another")))), library.length > 0 && /* @__PURE__ */ React.createElement(Card3, null, /* @__PURE__ */ React.createElement("h2", { className: "mb-3 text-sm font-bold", style: { color: "var(--tpl-heading)" } }, "Your uploads"), /* @__PURE__ */ React.createElement("ul", { className: "divide-y", style: { borderColor: "var(--tpl-border)" } }, library.map((a) => /* @__PURE__ */ React.createElement("li", { key: a.id, className: "flex items-center gap-3 py-2.5" }, /* @__PURE__ */ React.createElement("span", { className: "grid h-8 w-8 place-items-center rounded-md", style: { background: "var(--tpl-surface-2)", color: "var(--tpl-muted)" } }, /* @__PURE__ */ React.createElement(Icon, { name: "play-icon", size: 14 })), /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("p", { className: "truncate text-sm", style: { color: "var(--tpl-heading)" } }, a.id.slice(0, 8), " \xB7 ", a.mime_type), /* @__PURE__ */ React.createElement("p", { className: "text-xs", style: { color: "var(--tpl-muted)" } }, a.status, a.width ? ` \xB7 ${a.width}\xD7${a.height}` : "")), a.status === "ready" && a.hls_url && /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: () => {
          setCurrent(a);
          setPhase("ready");
          window.scrollTo({ top: 0, behavior: "smooth" });
        },
        className: "rounded-md px-3 py-1 text-xs font-semibold text-white",
        style: { background: "var(--tpl-accent)" }
      },
      "Play"
    ))))));
  }
  function Player({ src }) {
    return /* @__PURE__ */ React.createElement(
      MediaPlayer2,
      {
        className: "w-full overflow-hidden rounded-xl",
        title: "Video",
        src: { src, type: "application/vnd.apple.mpegurl" },
        playsInline: true
      },
      /* @__PURE__ */ React.createElement(MediaProvider2, null),
      /* @__PURE__ */ React.createElement(DefaultVideoLayout, { icons: defaultLayoutIcons })
    );
  }
  function Progress2({ label, value, indeterminate }) {
    return /* @__PURE__ */ React.createElement("div", { className: "mt-6" }, /* @__PURE__ */ React.createElement("p", { className: "mb-2 text-sm font-medium", style: { color: "var(--tpl-text)" } }, label), /* @__PURE__ */ React.createElement("div", { className: "h-2 overflow-hidden rounded-full", style: { background: "var(--tpl-surface-2)" } }, /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `h-full rounded-full ${indeterminate ? "animate-pulse" : ""}`,
        style: {
          width: indeterminate ? "100%" : `${value ?? 0}%`,
          background: "linear-gradient(90deg, var(--tpl-accent), var(--tpl-accent-2))",
          transition: "width .2s"
        }
      }
    )));
  }
  function Card3({ children }) {
    return /* @__PURE__ */ React.createElement("div", { className: "rounded-xl p-5 shadow-sm", style: { background: "var(--tpl-surface)", border: "1px solid var(--tpl-border)" } }, children);
  }
  function putWithProgress(url, file, onProgress) {
    return new Promise((resolve, reject) => {
      const xhr = new XMLHttpRequest();
      xhr.open("PUT", url);
      xhr.withCredentials = true;
      xhr.setRequestHeader("Content-Type", file.type || "video/mp4");
      xhr.upload.onprogress = (e2) => {
        if (e2.lengthComputable) onProgress(Math.round(e2.loaded / e2.total * 100));
      };
      xhr.onload = () => xhr.status >= 200 && xhr.status < 300 ? resolve() : reject(new Error(`Upload failed (${xhr.status}).`));
      xhr.onerror = () => reject(new Error("Upload network error."));
      xhr.send(file);
    });
  }
  async function poll(id2) {
    for (let i = 0; i < 120; i++) {
      const r2 = await fetch(`${API}/api/v1/assets/${id2}`, { credentials: "include" });
      if (r2.ok) {
        const a = await r2.json();
        if (a.status === "ready" || a.status === "failed") return a;
      }
      await new Promise((res) => setTimeout(res, 1500));
    }
    return { status: "failed", error: "Timed out waiting for transcode." };
  }
  return __toCommonJS(bundle_entry_exports);
})();
window.PortalUI=PortalUI.__dsMainNs?Object.assign({},PortalUI,PortalUI.__dsMainNs,{__dsMainNs:undefined}):PortalUI;
