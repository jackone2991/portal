/**
 * Modal: add a book (comic/novel) — port of
 * `components/popup/addBook.blade.php`. Triggered from the library views.
 *
 * Placeholder until modal open-state is wired. Port the Blade markup here then.
 */
export function AddBook() {
  return null;
}
