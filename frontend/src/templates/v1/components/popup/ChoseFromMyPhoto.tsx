/**
 * Modal: choose from my photos — port of
 * `components/popup/choseFromMyPhoto.blade.php`.
 *
 * Placeholder until modal open-state is wired. Port the Blade markup here then.
 */
export function ChoseFromMyPhoto() {
  return null;
}
