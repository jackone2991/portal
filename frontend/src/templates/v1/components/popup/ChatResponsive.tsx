/**
 * Modal: responsive chat panel — port of
 * `components/popup/chatResponsive.blade.php`.
 *
 * Placeholder until modal open-state is wired. Port the Blade markup here then.
 */
export function ChatResponsive() {
  return null;
}
