/**
 * Modal: update header/cover photo — port of
 * `components/popup/updateHeaderPhoto.blade.php`.
 *
 * Placeholder: rendered in the app shell but hidden until open-state is wired
 * (e.g. a Zustand `useUiStore` modal slice). Port the Blade markup here then.
 */
export function UpdateHeaderPhoto() {
  return null;
}
