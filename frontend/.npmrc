# pnpm: flat node_modules (npm-shaped) instead of the symlinked store layout.
# `next build` with output: "standalone" copies node_modules by creating
# symlinks, which Windows refuses without Developer Mode (EPERM). A hoisted
# tree needs none. The lockfile is unaffected; Docker (Linux) works either way.
node-linker=hoisted
