root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/worker ./cmd/worker"
  bin = "./tmp/worker"
  delay = 500
  exclude_dir = ["tmp", "bin", "internal/repository"]
  include_ext = ["go", "yaml", "sql"]
  stop_on_error = true

[log]
  time = true
