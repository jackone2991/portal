name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend (build · vet · test · sqlc-drift)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.24"
          cache-dependency-path: backend/go.sum

      - name: Build + vet + test
        run: |
          go build ./...
          go vet ./...
          go test ./... -race -count=1   # storage integration test skips without S3_ENDPOINT

      # sqlc output is committed and used — fail if it drifts from queries/schema.
      - uses: sqlc-dev/setup-sqlc@v4
        with:
          sqlc-version: "1.31.1"

      - name: sqlc drift check
        run: |
          sqlc generate
          if ! git diff --quiet -- internal/modules; then
            echo "::error::Generated sqlc code is stale. Run 'make sqlc' and commit."
            git --no-pager diff -- internal/modules
            exit 1
          fi

  lint:
    name: Backend lint (module boundaries · depguard)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.24"
          cache-dependency-path: backend/go.sum
      # Bump the version if the install step ever fails (any recent v2.x is fine).
      - name: Install golangci-lint (v2)
        run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.0.2
      - name: Enforce module boundaries
        run: golangci-lint run ./...

  openapi:
    name: OpenAPI contract (parse + codegen drift gate)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate shared/openapi.yaml parses
        run: |
          python3 -m pip install --quiet pyyaml
          python3 - <<'PY'
          import yaml
          doc = yaml.safe_load(open("shared/openapi.yaml"))
          assert isinstance(doc, dict), "openapi.yaml is not a mapping"
          for key in ("openapi", "info", "paths"):
              assert key in doc, f"missing top-level key: {key}"
          print("openapi.yaml OK —", doc["openapi"], "·", len(doc["paths"]), "paths")
          PY
      # ADR-10: the committed codegen (Go server stubs + TS client types) must
      # match the spec. Regenerate and fail on any diff, mirroring the sqlc gate.
      - uses: actions/setup-go@v5
        with:
          go-version-file: backend/go.mod
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml
      - name: Install codegen tools
        run: |
          go install github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen@v2.7.2
          cd frontend && pnpm install --frozen-lockfile
      - name: Regenerate from spec
        run: |
          export PATH="$(go env GOPATH)/bin:$PATH"
          make openapi
      - name: Fail on drift
        run: |
          git diff --exit-code -- \
            backend/internal/handler/api.gen.go \
            frontend/src/lib/types.gen.ts \
          || { echo "::error::OpenAPI codegen is stale — run 'make openapi' and commit the result."; exit 1; }

  frontend:
    name: Frontend (build · typecheck · lint)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    env:
      NEXT_PUBLIC_API_BASE_URL: https://api.portal.localhost
      NEXT_PUBLIC_TEMPLATE_VERSION: v1
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml
      - name: Install + build
        run: |
          pnpm install --frozen-lockfile
          pnpm build   # next build runs typecheck + ESLint
