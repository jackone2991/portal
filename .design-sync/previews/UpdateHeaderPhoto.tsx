import { UpdateHeaderPhoto } from "portal-frontend";

export const Dialog = () => (
  <div style={{ position: "relative", transform: "translateZ(0)", minHeight: 360, background: "#f5f6fa" }}>
    <UpdateHeaderPhoto open onClose={() => {}} />
  </div>
);
