import { LoginView } from "portal-frontend";

// Route wrapper for /login — renders the shared AuthLanding two-column layout
// with the sign-in tab active (welcome copy + credential form card). Full-screen
// landing; carries its own ambient background.

export const Login = () => <LoginView />;
